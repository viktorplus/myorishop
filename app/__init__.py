# Single source of truth for the app version shown in the header title.
# Versioning scheme: "1.<N>" — starts at 1.0 and the number after the dot is
# bumped by one on every commit that completes a task (1.0 → 1.1 → … → 1.125).
# Not semver: the trailing segment is a plain incrementing counter.
__version__ = "1.1"

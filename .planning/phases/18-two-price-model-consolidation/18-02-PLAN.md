---
phase: 18-two-price-model-consolidation
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - app/services/catalog.py
  - app/services/export.py
  - app/routes/products.py
  - app/templates/partials/product_price_autofill.html
  - app/templates/pages/categories.html
  - app/templates/partials/product_rows.html
  - app/templates/pages/product_form.html
  - tests/test_catalog.py
  - tests/test_export.py
  - tests/test_pricing_feature.py
autonomous: true
requirements: [PROD-05]
must_haves:
  truths:
    - "Product create/update no longer parses, stores, or audits catalog_cents — _PRICE_FIELDS carries only cost_cents, sale_cents, min_sale_cents (Pitfall 4)"
    - "The product form, product list, categories page, and CSV export render no «Каталог»/catalog price field (criterion 1: no third price surfaces)"
    - "The autofill OOB fragment no longer emits a #catalog input; #cost/#sale OOB inputs still fill"
  artifacts:
    - path: "app/services/catalog.py"
      provides: "_PRICE_FIELDS without catalog_cents"
      contains: "_PRICE_FIELDS = (\"cost_cents\", \"sale_cents\", \"min_sale_cents\")"
    - path: "app/services/export.py"
      provides: "CSV export without the «Каталог» column"
  key_links:
    - from: "app/services/catalog.py::_PRICE_FIELDS"
      to: "product price-change audit loop"
      via: "getattr over the tuple — must not reference a to-be-dropped attribute (Pitfall 4)"
      pattern: "_PRICE_FIELDS"
---

<objective>
Remove every READ and WRITE of `catalog_cents` in the catalog/product/export layer, WHILE LEAVING the `Product.catalog_cents` model attribute in place. This is the "remove references before removing the definition" half of the drop — plan 18-04 removes the attribute and drops the column only after every reference is gone, so no intermediate state does `getattr(product, "catalog_cents")` against a model that has dropped it (Pitfall 4 / the migration-ordering constraint).

Scope is the surface CONTEXT.md correctly identified for this domain, taken from 18-RESEARCH.md §"Authoritative catalog_cents Removal Surface". The receipt domain (services + desktop/mobile routes + receipt templates) is swept in companion plan 18-03 (wave 1); the sale-hint wording is plan 18-06.

Purpose: PROD-05 — collapse product pricing toward exactly two fields (ДЦ/ПЦ).
Output: catalog_cents gone from the catalog service (incl. `_PRICE_FIELDS`), CSV export, the product-form autofill route/fragment, the product-list/categories columns, and the product-form catalog input.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-two-price-model-consolidation/18-RESEARCH.md
@.planning/phases/18-two-price-model-consolidation/18-PATTERNS.md
@.planning/phases/18-two-price-model-consolidation/18-CONTEXT.md
@.planning/phases/18-two-price-model-consolidation/18-VALIDATION.md
@app/services/catalog.py
@app/services/export.py
@app/routes/products.py
@app/templates/partials/product_price_autofill.html
@app/templates/pages/product_form.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove catalog_cents from the catalog service (parse/create/update + _PRICE_FIELDS) and CSV export</name>
  <files>app/services/catalog.py, app/services/export.py, tests/test_catalog.py, tests/test_export.py</files>
  <read_first>
    - app/services/catalog.py:106,125,156,211,236,261 (the six catalog_cents sites: parse at :106/:211, create at :125, `_PRICE_FIELDS` at :156, update dict at :236, assignment at :261)
    - app/services/catalog.py:18-20 (message-constant style, for context) and its own cost_cents/sale_cents twins at each of the six sites
    - app/services/export.py:86 («Каталог» header), :98 (the catalog_cents column) and its adjacent surviving price columns
    - tests/test_catalog.py:58,520,521 (assertions to update — service-level, NOT the migration test at :278 which is plan 18-04)
    - tests/test_export.py:230 (asserts «Каталог» in the CSV header — invert to `not in`)
    - 18-RESEARCH.md §Pitfall 3 (export is a user-visible format change — record it in the summary), §Pitfall 4 (_PRICE_FIELDS getattr), §"Сites CONTEXT.md correctly identified"
  </read_first>
  <action>
    In app/services/catalog.py delete the `catalog_cents` parse (`parse_optional_cents(catalog_raw, ..., "catalog")`) at :106 and :211, the `catalog_cents=catalog_cents` create kwarg at :125, the `"catalog_cents": catalog_cents` update-dict entry at :236, and the `product.catalog_cents = catalog_cents` assignment at :261; also drop the `catalog_raw` parameter these fed if it becomes unused. At :156 change `_PRICE_FIELDS = ("cost_cents", "sale_cents", "catalog_cents", "min_sale_cents")` to `("cost_cents", "sale_cents", "min_sale_cents")` (Pitfall 4 — this tuple drives the price-change audit getattr loop; leaving catalog_cents here would AttributeError once 18-04 drops the attribute). Keep `min_sale_cents` — it is exempt (PROD-05 scope note). In app/services/export.py remove the «Каталог» header at :86 and the catalog_cents value column at :98; `_csv_safe()` must still wrap every remaining cell (T-18-CSV). Update tests/test_catalog.py:58,520,521 to the two-price shape and invert tests/test_export.py:230 to assert `"Каталог" not in` the header (same-literal negative idiom, 18-PATTERNS.md §Q4). Cite `D-01`/`Pitfall 4` in comments.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_catalog.py tests/test_export.py -q` green.
    - `grep -c catalog_cents app/services/catalog.py` returns 0; `grep -c catalog_cents app/services/export.py` returns 0.
    - `grep -n "_PRICE_FIELDS" app/services/catalog.py` shows the 3-element tuple with no catalog_cents.
    - `uv run ruff check app/services/catalog.py app/services/export.py` clean.
  </acceptance_criteria>
  <verification>catalog.py + export.py free of catalog_cents; _PRICE_FIELDS is 3 elements; export test inverted; both test files green.</verification>
</task>

<task type="auto">
  <name>Task 2: Drop catalog from the product-form autofill route + OOB fragment, and from the product-list/categories/product-form surfaces</name>
  <files>app/routes/products.py, app/templates/partials/product_price_autofill.html, app/templates/pages/categories.html, app/templates/partials/product_rows.html, app/templates/pages/product_form.html, tests/test_pricing_feature.py</files>
  <read_first>
    - app/routes/products.py:130-161 (CAT-05 autofill), specifically :133,148,154,155 (`fill_catalog`, the `"catalog_cents": latest.consumer_cents` context key) and the `fill_cost`/`fill_sale` twins that STAY
    - app/templates/partials/product_price_autofill.html:5-16 (the `{% if fill_catalog %}` OOB block at :5-8 goes; the `#cost` :10 and `#sale` :14 OOB blocks STAY)
    - app/templates/pages/product_form.html:77-81 (the `#catalog` input — DELETE) and :103-114 (the `latest_price` reference block — MUST STAY, Pitfall 6; relabelled by plan 18-05, cue-wired by plan 18-07)
    - app/templates/pages/categories.html:34 and app/templates/partials/product_rows.html:60 (catalog_cents list columns — DELETE, keep the adjacent cost/sale columns)
    - tests/test_pricing_feature.py:70-81 (the `/products/lookup-price` route test: `'id="catalog"' in r.text` at :77 → invert to `not in`; `r.text.count("12,00") == 2` at :80 → recompute to the two-field count once catalog is gone; `'hx-swap-oob="true"'` at :81 stays)
    - 18-RESEARCH.md §"Correction: product_form.html:103-114 must NOT be removed", §Pitfall 6; 18-PATTERNS.md §Q2
  </read_first>
  <action>
    In app/routes/products.py:130-161 remove the `fill_catalog` computation and the `"catalog_cents": ...` context key at :155, leaving `fill_cost`/`fill_sale` and the `cost`/`sale` context keys intact (D-22's in-place fix already lets `latest` be non-None for a ДЦ-only code; do not re-add a consumer filter). In product_price_autofill.html delete the `{% if fill_catalog %}` block at :5-8 ONLY — keep the `#cost` (:10) and `#sale` (:14) OOB inputs (plan 18-07 adds their `data-ref-cents`). In product_form.html delete ONLY the catalog `<div class="field">` input at :77-81; the `latest_price` reference block at :103-114 STAYS untouched here (Pitfall 6 — it reads `latest_price`, never `product.catalog_cents`; it is plan 18-05's relabel target and plan 18-07's `data-ref-cents` source). Remove the catalog_cents column from categories.html:34 and product_rows.html:60. Update the tests/test_pricing_feature.py lookup-price assertions per read_first (invert :77, recompute :80). Cite `D-01`/`Pitfall 6` in comments.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_pricing_feature.py tests/test_catalog.py -q` green.
    - `grep -rn "product.catalog_cents\|fill_catalog" app/routes/products.py app/templates/pages/product_form.html app/templates/pages/categories.html app/templates/partials/product_rows.html app/templates/partials/product_price_autofill.html` returns nothing.
    - `grep -n "latest_price" app/templates/pages/product_form.html` still returns the :103-114 block (Pitfall 6 guard — it must NOT have been deleted).
    - `uv run ruff check app/routes/products.py` clean.
  </acceptance_criteria>
  <verification>Autofill route + fragment + product-form input + list columns free of catalog; the latest_price block survives; lookup-price test recomputed and green.</verification>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator form → product create/update | Untrusted price strings cross here; `parse_optional_cents`→`to_cents` stays the sole authority (D-13) |
| product data → CSV export | Exported cells must stay `_csv_safe`-wrapped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-CSV | Tampering | `app/services/export.py` | mitigate | Removing the catalog_cents column REDUCES the exported surface; every remaining cell stays wrapped by `_csv_safe()`. No formula-injection regression. |
| T-18-INPUT | Tampering | `app/services/catalog.py` parse path | mitigate | Dropping the `catalog` form field shrinks the input surface by 3 desktop params; `parse_optional_cents` unchanged for the surviving fields. |
| T-18-SC | Tampering | package installs | accept | N/A — zero packages added this plan. |
</threat_model>

## Artifacts this phase produces

- `app/services/catalog.py::_PRICE_FIELDS` reduced to `("cost_cents", "sale_cents", "min_sale_cents")` **[this plan]**
- CSV export without the «Каталог» column (`app/services/export.py`) **[this plan]** — user-visible format change, record in summary (Pitfall 3 / assumption A1)
- (phase-wide) `alembic/versions/0014_drop_product_catalog_cents.py` — 18-04; `app/static/price-cue.js`, `.price-below`/`.price-above`, `data-ref-cents` — 18-07/18-08; `reference_prices_for_code` — 18-01; `SALE_CARD_FILL_HINT`/`SALE_BATCH_FILL_HINT` — 18-06; «изменить цену» redirect — 18-05.

<verification>
- `uv run pytest tests/test_catalog.py tests/test_export.py tests/test_pricing_feature.py -q` green.
- `grep -rn catalog_cents app/services/catalog.py app/services/export.py app/routes/products.py` returns nothing.
- `uv run ruff check` clean.
- Model attribute `Product.catalog_cents` is STILL present in `app/models.py:153` (this plan must not touch it — that is plan 18-04, gated on this plan).
</verification>

<success_criteria>
- No catalog price is read, written, audited, exported, or rendered in the product/catalog/export layer (PROD-05).
- `_PRICE_FIELDS` is safe for plan 18-04's attribute drop (Pitfall 4 pre-empted).
- The `product_form.html:103-114` reference block survives for plans 18-05/18-07.
</success_criteria>

<output>
Create `.planning/phases/18-two-price-model-consolidation/18-02-SUMMARY.md` when done. Note the CSV «Каталог» column removal as a user-visible export-format change (Pitfall 3).
</output>

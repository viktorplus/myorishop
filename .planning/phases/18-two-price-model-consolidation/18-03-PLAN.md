---
phase: 18-two-price-model-consolidation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/receipts.py
  - app/routes/receipts.py
  - app/routes/mobile_receipts.py
  - app/templates/partials/receipt_form.html
  - app/templates/partials/receipt_rows.html
  - app/templates/mobile_partials/receipts_step_details.html
  - app/templates/mobile_partials/receipts_step_batch.html
  - app/templates/mobile_partials/receipts_step_confirm.html
  - tests/test_receipts.py
  - tests/test_mobile_receipts.py
autonomous: true
requirements: [PROD-05]
must_haves:
  truths:
    - "The receipt (goods-receipt) no longer writes payload.catalog_cents to the ledger; historical receipt payloads are untouched and stay readable (D-04)"
    - "Both the desktop receipt form and the entire mobile receipt wizard complete with no `catalog` field (Pitfall 1 — the wizard threads catalog through 5 endpoints + 3 hidden inputs)"
    - "The receipt→card write-back of ДЦ/ПЦ (D-15, receipts.py:169-196) is preserved unchanged"
    - "The receipt history view (receipt_rows.html) shows no «Каталог» column; unit_cost_cents→ДЦ and unit_price_cents→ПЦ still render (criterion 1 + criterion 4)"
  artifacts:
    - path: "app/services/receipts.py"
      provides: "receipt register/prefill without catalog_cents; ledger payload write stopped"
    - path: "app/routes/mobile_receipts.py"
      provides: "mobile wizard with no `catalog` Form param threaded"
  key_links:
    - from: "app/services/receipts.py register/write-back"
      to: "operations ledger (append-only)"
      via: "stop writing payload.catalog_cents; never UPDATE/DELETE existing ops (D-04)"
      pattern: "payload="
---

<objective>
Remove the `catalog`/`catalog_cents` field from the goods-receipt slice across BOTH the desktop and mobile trees in a single plan — the mobile receipt wizard (`app/routes/mobile_receipts.py`, 12 sites across 5 endpoints + 3 hidden inputs) was entirely omitted from CONTEXT.md's inventory and will `TypeError`/500 if swept apart from desktop (Pitfall 1). The `Product.catalog_cents` model attribute STAYS (removed by plan 18-04 after all references are gone).

Two D-04 rules are load-bearing here: the receipt STOPS writing `payload.catalog_cents` to the append-only ledger at `receipts.py:246`, but the 8 historical receipt payloads already carrying it are never touched (criterion 4 holds by construction — the ledger is trigger-enforced immutable). The receipt→card write-back of ДЦ/ПЦ (D-15, `receipts.py:169-196`) is a do-not-regress constraint, not a build item.

Purpose: PROD-05 — no catalog price on any receipt surface, desktop or mobile.
Output: catalog gone from the receipt service, both receipt routes, the desktop receipt form + history, and all three mobile wizard step templates.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-two-price-model-consolidation/18-RESEARCH.md
@.planning/phases/18-two-price-model-consolidation/18-PATTERNS.md
@.planning/phases/18-two-price-model-consolidation/18-CONTEXT.md
@.planning/phases/18-two-price-model-consolidation/18-VALIDATION.md
@app/services/receipts.py
@app/routes/receipts.py
@app/routes/mobile_receipts.py
@app/templates/partials/receipt_form.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stop the receipt service parsing/writing catalog_cents (incl. the ledger payload) — preserve the D-15 write-back</name>
  <files>app/services/receipts.py, tests/test_receipts.py</files>
  <read_first>
    - app/services/receipts.py:6 (module docstring "payload carries catalog_cents" — reword to note history still carries it, D-04; do not delete)
    - app/services/receipts.py:116 (parse), :157 (create kwarg), :177 (dict entry), :246 (`payload={"catalog_cents": catalog_cents}` — the append-only ledger write that STOPS), :285 (`"catalog": product.catalog_cents` lookup key)
    - app/services/receipts.py:169-196 (the D-15 receipt→card write-back + PD-8 "empty never clears" — MUST NOT change), :261-299 (`lookup_prefill`, incl. the "D-02 superseded" comment at :266-268)
    - tests/test_receipts.py:72,203,684 (payload assertions), :613,632,658,677 (assign `product.catalog_cents` — the assignment still works this plan since the attribute stays, but the write-behaviour under test changes; rewrite so they assert the receipt no longer writes catalog)
    - 18-RESEARCH.md §Pitfall 4, §"Criterion 4" (payload count must read 8 before/after), 18-PATTERNS.md §"Decision-ID comments" (the "D-02 superseded" model)
  </read_first>
  <action>
    In app/services/receipts.py delete the catalog parse at :116, the `catalog_cents=` create kwarg at :157, the `"catalog_cents": catalog_cents` dict entry at :177, the `payload={"catalog_cents": catalog_cents}` ledger write at :246 (D-04: stop writing — the history stays), and the `"catalog": product.catalog_cents` lookup key at :285; drop the now-unused `catalog_raw` parameter from the affected signatures (the route removal in Task 2 stops passing it). Reword the docstring at :6 to state that HISTORICAL payloads still carry catalog_cents (D-04) but new receipts do not write it — do not claim it is gone. Leave receipts.py:169-196 (the D-15 write-back of cost_cents/sale_cents to the card, one price_change op per changed field, PD-8) completely unchanged. Rewrite tests/test_receipts.py:72,203,684 to assert the new receipt writes NO catalog_cents payload, and :613,632,658,677 to stop depending on a receipt-written catalog. Cite `D-04`/`D-15` in comments.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_receipts.py -q` green (incl. the D-15 write-back regression tests and the `-k payload` subset).
    - `grep -c catalog_cents app/services/receipts.py` returns 0 for value writes; the docstring at :6 still MENTIONS catalog_cents in a "history keeps it" sense (D-04) — `grep -n "catalog_cents" app/services/receipts.py | grep -v '^\s*#'` shows only the docstring line, not a write.
    - `uv run ruff check app/services/receipts.py` clean.
  </acceptance_criteria>
  <verification>Receipt service writes no catalog_cents (ledger payload included); D-15 write-back untouched; test_receipts.py green.</verification>
</task>

<task type="auto">
  <name>Task 2: Drop the `catalog` Form param and its step-to-step threading from BOTH receipt routes (Pitfall 1)</name>
  <files>app/routes/receipts.py, app/routes/mobile_receipts.py, tests/test_receipts.py</files>
  <read_first>
    - app/routes/receipts.py:113,126,127,136,142,144,145,174,190,205 (desktop: `catalog` Form param, `typed`/`fill_fields` dicts, context, `catalog_raw=catalog`) and the surviving `cost`/`sale` twins at each site
    - app/routes/mobile_receipts.py:106,127,128,136,145,163,181,201,215,234,248,264 (mobile wizard: `catalog: str = Form("")` on 5 endpoints, `resolved_catalog`/`final_catalog`, context keys, `catalog_raw=catalog` at :264) — the surface CONTEXT.md missed entirely
    - app/routes/receipts.py:23 (the `CARD_FILL_HINT` constant — leave it; it is the D-17/D-23 analog but a receipt concern, not this task)
    - 18-RESEARCH.md §"Sites CONTEXT.md MISSED", §Pitfall 1; 18-PATTERNS.md §Q3 (private route helpers are re-declared per tree, never cross-imported — sweep both trees here)
  </read_first>
  <action>
    Sweep desktop AND mobile in this one task (Pitfall 1). In app/routes/receipts.py remove the `catalog` Form parameter and every `catalog`/`resolved`/`fill_fields`/context reference at :113,126,127,136,142,144,145,174,190,205, and stop passing `catalog_raw=catalog` into the service (Task 1 removed that param). In app/routes/mobile_receipts.py remove `catalog: str = Form("")` from all 5 endpoints and the `resolved_catalog`/`final_catalog`/context-key/`catalog_raw=catalog` threading at :106,127,128,136,145,163,181,201,215,234,248,264. Keep the `cost`/`sale` twins at every site. Do not cross-import private `_`-prefixed helpers between the trees (18-PATTERNS.md §Q3). Update any desktop-route integration assertions in tests/test_receipts.py that referenced the catalog param. Cite `Pitfall 1`/`D-01` in comments.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_receipts.py tests/test_mobile_receipts.py -q` green — the mobile wizard completes end-to-end with no `catalog`.
    - `rg catalog app/routes/mobile_receipts.py` returns nothing (Pitfall 1 warning sign); `grep -c catalog app/routes/receipts.py` returns 0 except the unrelated `CARD_FILL_HINT` wording if present.
    - `uv run ruff check app/routes/receipts.py app/routes/mobile_receipts.py` clean.
  </acceptance_criteria>
  <verification>Both receipt routes free of the catalog param; mobile + desktop receipt integration tests green.</verification>
</task>

<task type="auto">
  <name>Task 3: Remove the catalog field/column from the receipt templates (desktop form + history + all 3 mobile wizard steps); keep price_history.html:22</name>
  <files>app/templates/partials/receipt_form.html, app/templates/partials/receipt_rows.html, app/templates/mobile_partials/receipts_step_details.html, app/templates/mobile_partials/receipts_step_batch.html, app/templates/mobile_partials/receipts_step_confirm.html, tests/test_mobile_receipts.py</files>
  <read_first>
    - app/templates/partials/receipt_form.html:29 (`hx-include="…[name='catalog']…"` — remove the selector fragment, Pitfall 7), :70-76 (the `cost`/`sale` `{% with %}` includes that STAY), :78-79 (the `catalog` `{% with %}` block — DELETE)
    - app/templates/partials/receipt_rows.html:19,31 (the «Каталог» history column reading payload.catalog_cents — DELETE per D-04; unit_cost/unit_price columns stay)
    - app/templates/mobile_partials/receipts_step_details.html:26 (visible `#receipt-catalog` input — DELETE), receipts_step_batch.html:15 (hidden catalog — DELETE), receipts_step_confirm.html:30 (hidden catalog — DELETE); the `cost`/`sale` inputs at :18/:22 (details) and hidden :13,14 / :28,29 STAY
    - app/templates/partials/price_history.html:22 (the catalog_cents audit LABEL branch — MUST NOT be touched, D-04: renders for 0 live rows, removing it would let the else-fallback show a raw string)
    - 18-RESEARCH.md §Pitfall 7, §"Correction: 103-114"; 18-PATTERNS.md §"Money inputs" (the input line the deletes are siblings of)
  </read_first>
  <action>
    In receipt_form.html remove the `[name='catalog']` fragment from the `hx-include` at :29 (Pitfall 7) and delete the `{% with field = "catalog", label = "Цена по каталогу" %}` include block at :78-79; keep the `cost`/`sale` `{% with %}` blocks at :70-76. In receipt_rows.html delete the «Каталог» header and cell at :19,31 (D-04). In the three mobile wizard step templates delete the catalog input/hidden at receipts_step_details.html:26, receipts_step_batch.html:15, receipts_step_confirm.html:30, keeping the cost/sale siblings. DO NOT touch price_history.html:22 (D-04). Confirm the mobile wizard still round-trips its remaining hidden state. Update tests/test_mobile_receipts.py only where it asserted a catalog input/step. Cite `D-04`/`Pitfall 7` in comments.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_mobile_receipts.py tests/test_receipts.py -q` green.
    - `rg "name='catalog'|name=\"catalog\"|receipt-catalog" app/templates/partials/receipt_form.html app/templates/mobile_partials/` returns nothing (Pitfall 7 warning sign).
    - `grep -n "catalog_cents" app/templates/partials/price_history.html` still returns the :22 branch (D-04 guard — it must survive).
    - `grep -c "Каталог" app/templates/partials/receipt_rows.html` returns 0.
  </acceptance_criteria>
  <verification>Receipt form/history + all 3 mobile steps free of catalog; price_history.html:22 preserved; mobile+desktop receipt tests green.</verification>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| receipt form (desktop + mobile wizard) → register_receipt | Untrusted price/qty strings; parsed only in the service (D-13) |
| register_receipt → operations ledger | Append-only, DB-trigger-enforced; new receipts stop writing payload.catalog_cents but never mutate prior ops |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-LEDGER | Repudiation / Tampering | `app/services/receipts.py:246` ledger write + `app/db.py:22-43` triggers | mitigate | Stop WRITING payload.catalog_cents; NEVER UPDATE/DELETE the 8 historical ops (D-04). Ledger immutability stays trigger-enforced; guarded by `tests/test_ledger.py`. Criterion 4 holds by construction. |
| T-18-INPUT | Tampering | both receipt routes | mitigate | Removing `catalog: str = Form("")` shrinks the input surface by 3 desktop + 5 mobile params; surviving fields still parse via `parse_optional_cents`. |
| T-18-SC | Tampering | package installs | accept | N/A — zero packages added this plan. |
</threat_model>

## Artifacts this phase produces

- Receipt slice (service + both routes + templates) free of catalog_cents; ledger payload write stopped (D-04) **[this plan]**
- (phase-wide) `0014` migration — 18-04; `reference_prices_for_code` — 18-01; `price-cue.js`/`.price-below`/`.price-above`/`data-ref-cents` — 18-07/18-08; `SALE_CARD_FILL_HINT`/`SALE_BATCH_FILL_HINT` — 18-06; «изменить цену» redirect — 18-05.

<verification>
- `uv run pytest tests/test_receipts.py tests/test_mobile_receipts.py -q` green.
- `uv run pytest tests/test_ledger.py -q` green (T-18-LEDGER structural guarantee).
- `uv run pytest tests/test_sales.py tests/test_mobile_sales.py -q` — PRICE-01's 9 guards green and UNMODIFIED (collateral sample).
- `rg catalog app/routes/mobile_receipts.py` and `rg "name='catalog'" app/templates` return nothing.
- `Product.catalog_cents` still present in `app/models.py:153` (this plan must not remove it — plan 18-04 does, gated on this plan).
</verification>

<success_criteria>
- No catalog price on any receipt surface, desktop or mobile (PROD-05, criterion 1).
- New receipts write no payload.catalog_cents; historical payloads untouched (criterion 4).
- The D-15 receipt→card write-back and price_history.html:22 both survive.
</success_criteria>

<output>
Create `.planning/phases/18-two-price-model-consolidation/18-03-SUMMARY.md` when done.
</output>

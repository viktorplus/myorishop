---
phase: 18-two-price-model-consolidation
plan: 05
type: execute
wave: 2
depends_on: ["18-02", "18-03"]
files_modified:
  - app/templates/pages/product_form.html
  - app/templates/pages/catalog_detail.html
  - app/routes/products.py
  - tests/test_catalogs_feature.py
autonomous: true
requirements: [PROD-05, PROD-07]
must_haves:
  truths:
    - "The product card and the catalog-detail page name the two prices identically — ДЦ (cost/consultant) and ПЦ (sale/consumer) — the SAME two names everywhere (D-19, criterion 1 as a labelling criterion)"
    - "The min_sale_cents field reads as a guardrail setting (grouped with the low-stock threshold), NOT a third price; its field/name/logic and PRICE-01 behaviour are unchanged (D-21)"
    - "The catalog-detail page shows ДЦ/ПЦ read-only and offers «изменить цену», which opens the product card for that code — editing there, not in the read-only catalog (D-18)"
    - "The product_form latest_price reference block (:103-114) carries the D-09 caveat that the reference is the code's LAST catalog appearance, not today's price"
  artifacts:
    - path: "app/templates/pages/catalog_detail.html"
      provides: "ДЦ/ПЦ labels + «изменить цену» link to the product card"
      contains: "изменить цену"
    - path: "app/routes/products.py"
      provides: "product_new accepts ?code= (prefill new, or redirect to the existing card)"
  key_links:
    - from: "app/templates/pages/catalog_detail.html «изменить цену»"
      to: "the product card for that code"
      via: "/products/new?code=... (redirects to /products/{id}/edit if the code already exists)"
      pattern: "products/new\\?code="
---

<objective>
Make criterion 1 true as a LABELLING criterion (D-19): the product card and the catalog-detail page name exactly two prices, with the same two names — ДЦ and ПЦ — everywhere. Regroup `min_sale_cents` so it reads as a guardrail setting, not a third price (D-21), with zero logic change. And close the dictionary/catalog write-back gap the domain-correct way (D-18): the catalog-detail page shows ДЦ/ПЦ read-only and offers «изменить цену», which opens the editable product card for that code — because `CatalogPrice` is a published historical fact wiped on every re-import, so an inline edit there would be silently destroyed (a direct hit on "without losing any data").

Note (D-20): the "dictionary entry" price surface is `pages/catalog_detail.html` (`/catalogs/{...}`), NOT `/dictionary` — `Dictionary` has zero price columns, and mobile has no catalog/dictionary page at all. Do not build one.

Purpose: PROD-05 (labelling half of criterion 1) + PROD-07 (D-18 edit-from-catalog redirect).
Output: unified ДЦ/ПЦ labels on the card + catalog detail, a regrouped min_sale guardrail, and a working «изменить цену» → product-card link.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-two-price-model-consolidation/18-RESEARCH.md
@.planning/phases/18-two-price-model-consolidation/18-PATTERNS.md
@.planning/phases/18-two-price-model-consolidation/18-CONTEXT.md
@app/templates/pages/product_form.html
@app/templates/pages/catalog_detail.html
@app/routes/products.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unify the product-card price labels to ДЦ/ПЦ, add the D-09 caveat, and regroup min_sale as a guardrail (D-19/D-21)</name>
  <files>app/templates/pages/product_form.html</files>
  <read_first>
    - app/templates/pages/product_form.html:60 («Закупочная цена», cost_cents → ДЦ), :66 («Цена продажи», sale_cents → ПЦ), :72 («Минимальная цена продажи», min_sale_cents → regroup, D-21), :84 (the «Порог "мало на складе"» low-stock threshold — the guardrail neighbour min_sale should join), :103-114 (the latest_price reference block; :107 «Последняя цена по каталогу» + :110 «консультант» → ДЦ, add the D-09 caveat)
    - 18-RESEARCH.md §"Label Consolidation Surface (D-19)" (the target column), §"Correction: 103-114 must NOT be removed" (Pitfall 6), §Open Question Q4 (min_sale label may change; field/logic must not)
    - 18-CONTEXT.md D-19/D-21 and the Claude's-Discretion note on exact Russian wording
  </read_first>
  <action>
    Relabel product_form.html so the two prices read as ДЦ and ПЦ consistently: :60 → a ДЦ label (e.g. «ДЦ — закупочная», the distributor/cost price), :66 → a ПЦ label (e.g. «ПЦ — цена продажи»). In the surviving `latest_price` reference block at :103-114, relabel :107/:110 so the reference pair also reads as ПЦ/ДЦ (:110 «консультант» → ДЦ, D-19's named example) and append the D-09 caveat wording to the `(каталог {{ number }} · {{ year }})` line making clear this is the price when the code was LAST in a catalogue, not today's price. Regroup the `min_sale` field (label :72, input id/name `min_sale` :73) as a GUARDRAIL setting placed beside the low-stock threshold at :84 — reword the label so it reads as a limit/guardrail, not a price (D-21). Do NOT change the `min_sale` input's id, name, value binding, or any logic (criterion 5 / PRICE-01 is frozen); it gets NO `data-ref-cents` here or in plan 18-07 (Pitfall 8). Keep exact wording tasteful and identical to catalog_detail's ДЦ/ПЦ names (Task 2). Cite `D-19`/`D-21`/`D-09` in `{# #}` comments.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_catalog.py -q` green (product-form render tests still pass).
    - The rendered `/products/new` contains "ДЦ" and "ПЦ" label text and still contains `id="cost"`, `id="sale"`, `id="min_sale"` (fields intact); `grep -n "min_sale" app/templates/pages/product_form.html` shows the input unchanged in id/name.
    - `grep -n "latest_price" app/templates/pages/product_form.html` still returns the :103-114 block (Pitfall 6 guard).
    - No `data-ref-cents` added to the min_sale input.
  </acceptance_criteria>
  <verification>Card shows two prices named ДЦ/ПЦ; min_sale reads as a guardrail with unchanged logic; reference block kept + caveated.</verification>
</task>

<task type="auto">
  <name>Task 2: Catalog-detail ДЦ/ПЦ labels + «изменить цену» → product card (D-18/D-19)</name>
  <files>app/templates/pages/catalog_detail.html, app/routes/products.py, tests/test_catalogs_feature.py</files>
  <read_first>
    - app/templates/pages/catalog_detail.html:20 («Цена по каталогу», consumer_cents → ПЦ), :21 («Цена консультанта», consultant_cents → ДЦ, D-19's named example), :25-33 (the per-code row where the «изменить цену» link belongs)
    - app/routes/products.py:29-31 (route-order comment: literal `/products/new` MUST precede `/products/{product_id}`), :164-175 (`product_new` — extend to accept an optional `code` query param), :230-247 (`/products/{id}/edit` — the redirect target when the code already exists)
    - app/routes/catalogs.py:1-5 (read-only route docstring — do NOT add writes here; the edit happens on the product card)
    - 18-RESEARCH.md §"D-18 confirmed" + §"Important mapping" (the surface is catalog_detail, not /dictionary), §Label table; 18-PATTERNS.md §D-18 (role-match)
  </read_first>
  <action>
    In catalog_detail.html relabel the header at :20 to a ПЦ name and :21 to a ДЦ name — identical wording to Task 1's card labels (D-19). Add an «изменить цену» link in each product row (:25-33) pointing to `/products/new?code={{ entry.code }}`. In app/routes/products.py extend `product_new` (:164-175) to accept an optional `code` query param: if a live (non-deleted) product with that code already exists, `RedirectResponse` to `/products/{id}/edit`; otherwise render the new-product form with the `code` field pre-filled (D-18: opens the product card for that code, creating it if it does not exist). Keep the literal-before-parameterized route order (:29-31). Do NOT add any price write to `app/routes/catalogs.py` (it stays read-only). Add a test in tests/test_catalogs_feature.py asserting the catalog-detail page renders «изменить цену» whose href resolves to the product card for the code (substring assertion on the raw href, per 18-PATTERNS.md §Q4). Cite `D-18`/`D-19` in comments.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_catalogs_feature.py -q` green, including the new D-18 link test.
    - The catalog-detail render contains "изменить цену" and an href matching `products/new?code=`.
    - `uv run pytest tests/test_catalog.py -q` green (product_new still serves the plain form when no code is passed).
    - `uv run ruff check app/routes/products.py` clean; `app/routes/catalogs.py` unchanged (still read-only).
  </acceptance_criteria>
  <verification>Catalog detail names ДЦ/ПЦ and links «изменить цену» to the editable product card; catalogs route stays read-only.</verification>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog-detail (read-only) → product card | The edit is redirected to the writable product-card route; the catalog view itself never writes |
| `?code=` query param → product_new | Untrusted code string; used only to prefill/redirect, resolved via an ORM query with bound params |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-XSS | Tampering | catalog_detail «изменить цену» href + labels | mitigate | `entry.code` rendered through Jinja2 autoescape (default on); never `| safe`. Labels are static Russian literals. |
| T-18-ACCESS | Elevation | D-18 redirect target | accept | The link targets an EXISTING product-card route (no new privileged surface); single local operator, no auth by project decision. |
| T-18-SC | Tampering | package installs | accept | N/A — zero packages added this plan. |
</threat_model>

## Artifacts this phase produces

- «изменить цену» link on `pages/catalog_detail.html` → product card (D-18) **[this plan]**
- `product_new` accepts `?code=` (prefill-or-redirect) **[this plan]**
- Unified ДЦ/ПЦ labels on the card + catalog detail; regrouped min_sale guardrail (D-19/D-21) **[this plan]**
- (phase-wide) `0014` migration — 18-04; `reference_prices_for_code` — 18-01; `price-cue.js`/`.price-below`/`.price-above`/`data-ref-cents` — 18-07/18-08; `SALE_CARD_FILL_HINT`/`SALE_BATCH_FILL_HINT` — 18-06.

<verification>
- `uv run pytest tests/test_catalogs_feature.py tests/test_catalog.py -q` green.
- `uv run ruff check` clean.
- `Product.catalog_cents`-free product form still renders both prices as ДЦ/ПЦ and the min_sale guardrail unchanged.
- PRICE-01 sample: `uv run pytest tests/test_sales.py tests/test_mobile_sales.py -q` green, unmodified (min_sale logic untouched).
</verification>

<success_criteria>
- Exactly two price NAMES (ДЦ/ПЦ) appear on the card and catalog detail; min_sale no longer reads as a price (PROD-05, criterion 1 labelling).
- The operator can reach an editable product card from the catalog detail via «изменить цену» (PROD-07, D-18) — no data-losing inline catalog edit.
</success_criteria>

<output>
Create `.planning/phases/18-two-price-model-consolidation/18-05-SUMMARY.md` when done. Record the chosen exact ДЦ/ПЦ label wording so plans 18-07/18-08 mirror it.
</output>

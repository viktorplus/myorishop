---
phase: 18-two-price-model-consolidation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - app/services/pricing.py
  - tests/test_pricing_feature.py
autonomous: true
requirements: [PROD-06]
must_haves:
  truths:
    - "reference_prices_for_code returns (ДЦ, ПЦ) independently — a code with ДЦ but no ПЦ still yields its ДЦ (D-08/D-22, the 1 live starved code)"
    - "An unknown code returns (None, None) — the no-catalog path is a first-class result, not an error (D-07, the MAIN path: 6 of 7 live products)"
    - "latest_price_for_code no longer filters on consumer_cents — the 3 live starved callers (products.py:147, products.py:244, receipts.py:289) now receive the row"
  artifacts:
    - path: "app/services/pricing.py"
      provides: "unfiltered latest_price_for_code + reference_prices_for_code (ДЦ/ПЦ tuple)"
      contains: "def reference_prices_for_code"
    - path: "tests/test_pricing_feature.py"
      provides: "ДЦ-without-ПЦ + unknown-code reference tests"
      contains: "reference_prices_for_code"
  key_links:
    - from: "app/services/pricing.py::reference_prices_for_code"
      to: "CatalogPrice.consultant_cents / CatalogPrice.consumer_cents"
      via: "D-05 pairing consultant→ДЦ, consumer→ПЦ"
      pattern: "consultant_cents|consumer_cents"
---

<objective>
Fix the ДЦ/ПЦ reference lookup so the colour cue (PROD-06) has a reference source that never gates ДЦ on ПЦ's presence, and repair the 3 live production callers the current filter starves.

`latest_price_for_code` (`app/services/pricing.py:14-32`) filters `CatalogPrice.consumer_cents.is_not(None)` at `:28`. Per D-22 (resolving research Q1) this is fixed IN PLACE by dropping that `.where()` clause — the verified blast radius is exactly 1 code, strictly additive (it can only add a previously-missing autofill/reference, never remove one). A thin `reference_prices_for_code` wrapper returns the `(ДЦ, ПЦ)` tuple the cue-wiring plans (18-07, 18-08) consume, per D-05's pairing.

Purpose: give the cue an honest reference contract (D-05/D-06/D-07/D-08/D-09) and close 3 pre-existing autofill bugs at zero extra query cost.
Output: unfiltered `latest_price_for_code`, new `reference_prices_for_code`, rewritten docstrings (closes the deferred `pricing.py:3-5` docstring item), updated tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-two-price-model-consolidation/18-RESEARCH.md
@.planning/phases/18-two-price-model-consolidation/18-PATTERNS.md
@.planning/phases/18-two-price-model-consolidation/18-VALIDATION.md
@app/services/pricing.py
@tests/test_pricing_feature.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): Reference-lookup tests for the ДЦ-without-ПЦ and unknown-code paths</name>
  <files>tests/test_pricing_feature.py</files>
  <read_first>
    - tests/test_pricing_feature.py:30-38 (the `priced` fixture — already seeds `_price("100", 2026, 1, consumer=1200, consultant=700)` = the D-05 ДЦ/ПЦ pair, and `_price("200", 2026, 1, consumer=500)` = ПЦ-only)
    - tests/test_pricing_feature.py:45,52,53 (existing assertions written against the FILTERED contract — must move to the unfiltered contract)
    - app/services/pricing.py:14-32 (current filtered function; the `.where(CatalogPrice.consumer_cents.is_not(None))` at :28 is the D-08 starvation)
    - 18-RESEARCH.md §"The ДЦ/ПЦ Reference Lookup (D-05 + D-08)" and §Pitfall 5; 18-PATTERNS.md §Q4 (assertion idiom)
  </read_first>
  <behavior>
    - reference_prices_for_code(session, "300") == (700, None) for a consultant-only row (ДЦ present, ПЦ NULL) — D-08/D-22 the 1 live starved code
    - reference_prices_for_code(session, "100") == (700, 1200) — D-05 pairing consultant→ДЦ, consumer→ПЦ
    - reference_prices_for_code(session, "NOPE") == (None, None) — D-07 unknown code, first-class result not an error
    - latest_price_for_code(session, "300") returns the row (not None) — the unfiltered contract the 3 callers were written for
  </behavior>
  <action>
    Add a consultant-only row to the `priced` fixture at tests/test_pricing_feature.py:30-38 mirroring "200": `_price("300", 2026, 1, consultant=700)` (no consumer) — this is D-22's ДЦ-without-ПЦ code. Add three tests naming their decisions in the docstring per the 18-PATTERNS.md §Q4 idiom (`"""D-08/D-22: ..."""`): `test_reference_prices_returns_dc_when_pc_null`, `test_reference_prices_pairs_consultant_dc_consumer_pc`, `test_reference_prices_unknown_code_returns_none_none`. Update the existing assertions at :45,52,53 so they assert the UNFILTERED behaviour (a ДЦ-only row is now returned, not None). Do NOT weaken any assertion to hide the change — Pitfall 5's warning sign is this file passing unchanged. These tests import `reference_prices_for_code`, which does not exist yet — the suite is RED until Task 2.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_pricing_feature.py -q` FAILS with ImportError/AttributeError on `reference_prices_for_code` (RED confirms the contract is asserted before implementation).
    - The three new test names exist and the `priced` fixture seeds code "300" consultant-only.
  </acceptance_criteria>
  <verification>Test file references `reference_prices_for_code` and a consultant-only "300" row; run is RED for the stated reason.</verification>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): Drop the consumer filter in place + add reference_prices_for_code + rewrite docstrings</name>
  <files>app/services/pricing.py</files>
  <read_first>
    - app/services/pricing.py:1-6 (module docstring — the "full per-catalog price history" claim at :3-5 is false per D-06/D-09 and is the deferred docstring item)
    - app/services/pricing.py:14-32 (latest_price_for_code; docstring :17-18 "always has consumer_cents set" becomes false)
    - 18-RESEARCH.md §"Required shape for the cue" (the reference_prices_for_code contract), §State of the Art (D-06: 6856 rows / 6856 codes, no per-period history)
  </read_first>
  <action>
    In `latest_price_for_code`, delete the `CatalogPrice.consumer_cents.is_not(None)` clause at pricing.py:28 (D-22 fix-in-place) — leave the `code == code` filter and the `year desc, number desc` ordering untouched. Add `reference_prices_for_code(session, code) -> tuple[int | None, int | None]` that calls the fixed `latest_price_for_code` and returns `(row.consultant_cents, row.consumer_cents)` when a row exists else `(None, None)` — D-05 pairing consultant→ДЦ, consumer→ПЦ; D-07 `(None, None)` is a first-class return. Rewrite the module docstring at :3-5 to state one row per code = that code's LAST catalog appearance (D-09), not a multi-catalog history (closes the deferred docstring item); rewrite the `latest_price_for_code` docstring at :17-18 to drop the false "always has consumer_cents set" and note it no longer gates on ПЦ (D-08/D-22). Cite `D-22`/`D-08`/`D-05`/`D-07`/`D-09` in comments per the 18-PATTERNS.md §"Decision-ID comments" convention. Portable ORM `select().where()` only — no raw SQL (CLAUDE.md).
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_pricing_feature.py -q` GREEN (all 3 new tests + the updated :45,52,53 assertions pass).
    - `uv run ruff check app/services/pricing.py` clean.
    - `grep -n "is_not(None)" app/services/pricing.py` returns nothing (the filter is gone).
  </acceptance_criteria>
  <verification>Filter removed; reference_prices_for_code present; test_pricing_feature.py green; ruff clean.</verification>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog_prices table → reference lookup | Read-only query over imported price data; no operator input reaches SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-SQLI | Tampering | `latest_price_for_code` / `reference_prices_for_code` | mitigate | SQLAlchemy `select().where()` with a bound `code` param — never f-string SQL (also CLAUDE.md portability rule). No change to the query shape beyond removing one clause. |
| T-18-SC | Tampering | package installs | accept | N/A — zero packages added this plan; no supply-chain surface. |
</threat_model>

## Artifacts this phase produces

New symbols this plan creates (phase-wide list repeated for the decision-coverage gate):
- `app/services/pricing.py::reference_prices_for_code` — `(ДЦ, ПЦ)` tuple contract (D-05/D-07/D-08/D-22) **[this plan]**
- unfiltered `app/services/pricing.py::latest_price_for_code` (filter dropped, D-22) **[this plan]**
- `alembic/versions/0014_drop_product_catalog_cents.py` (revision "0014") — plan 18-04
- `app/static/price-cue.js` — plan 18-07
- `.price-below` / `.price-above` CSS classes in `app/static/style.css` — plan 18-07
- `data-ref-cents` attribute (rendered across 11 inputs) — plans 18-07 / 18-08
- `ref_cents` optional param on `partials/receipt_price_inputs.html` — plan 18-08
- `SALE_CARD_FILL_HINT` / `SALE_BATCH_FILL_HINT` in `app/services/sales.py` — plan 18-06
- «изменить цену» link on `pages/catalog_detail.html` → product card (D-18) — plan 18-05

<verification>
- `uv run pytest tests/test_pricing_feature.py -q` green.
- `uv run pytest tests/test_sales.py tests/test_mobile_sales.py -q` — PRICE-01's 9 guard tests still green and UNMODIFIED (this plan does not touch sales; sample confirms no collateral).
- `uv run ruff check` clean.
</verification>

<success_criteria>
- The reference lookup returns ДЦ and ПЦ independently; `(None, None)` for unknown codes (PROD-06 foundation).
- The 3 live starved callers (`products.py:147,244`, `receipts.py:289`) now receive the row — a strictly additive autofill improvement (note it in the phase summary per D-22).
- No PRICE-01 test modified.
</success_criteria>

<output>
Create `.planning/phases/18-two-price-model-consolidation/18-01-SUMMARY.md` when done. Record the D-22 autofill improvement (the 1 code that now fills ДЦ) so it is not a surprise.
</output>

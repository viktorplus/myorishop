---
phase: 08-warehouses
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - app/routes/warehouses.py
  - app/templates/pages/warehouses.html
  - app/templates/partials/warehouse_rows.html
  - app/templates/base.html
  - app/main.py
  - tests/test_warehouses.py
autonomous: true
requirements: [WH-01]

must_haves:
  truths:
    - "Operator can create, edit, and soft-delete/restore a warehouse from a dedicated /warehouses management page (ROADMAP Phase 8 success criterion 1)"
    - "A soft-deleted warehouse stays visible in the /warehouses list (muted row) with a Восстановить button — never a dead end reachable only via a direct URL (D-09, success criterion 3)"
    - "Deleting the last active warehouse shows an inline warning and performs zero writes until confirm=1 is re-POSTed (D-06)"
    - "The nav bar exposes a Склады link to /warehouses from every page"
  artifacts:
    - path: "app/routes/warehouses.py"
      provides: "page + add/update/delete/restore routes"
      contains: "warehouses/{warehouse_id}/delete"
    - path: "app/templates/pages/warehouses.html"
      provides: "warehouse management page"
      contains: "Склады"
    - path: "app/templates/partials/warehouse_rows.html"
      provides: "rows partial, swap target for every POST"
      contains: "warehouse-rows"
  key_links:
    - from: "app/templates/base.html"
      to: "app/routes/warehouses.py"
      via: "nav link href=\"/warehouses\""
      pattern: "href=\"/warehouses\""
    - from: "app/main.py"
      to: "app/routes/warehouses.py"
      via: "app.include_router(warehouses.router)"
      pattern: "warehouses\\.router"
    - from: "app/routes/warehouses.py"
      to: "app/services/warehouses.py"
      via: "list_warehouses/add_warehouse/update_warehouse/soft_delete_warehouse/restore_warehouse calls"
      pattern: "from app.services.warehouses import"
---

<objective>
Deliver the user-facing half of WH-01: a single settings-style `/warehouses` page (D-08) with inline add, inline per-row edit, and inline delete/restore — wired to the `app/services/warehouses.py` service built in Plan 08-01 — plus the nav link and router registration that make it reachable from every screen.

Purpose: complete WH-01 and ROADMAP Phase 8 success criteria 1-3 end-to-end; every write response re-renders the rows partial in place (D-08 — no separate `/new`/`/{id}/edit` page exists for this entity, unlike Products), and a soft-deleted warehouse stays visible with a restore action (D-09 — a deliberate deviation from the Products list convention).
Output: `app/routes/warehouses.py`, `app/templates/pages/warehouses.html`, `app/templates/partials/warehouse_rows.html`, a new nav entry in `base.html`, router registration in `app/main.py`, and the web-layer tests in `tests/test_warehouses.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-warehouses/08-CONTEXT.md
@.planning/phases/08-warehouses/08-RESEARCH.md
@.planning/phases/08-warehouses/08-PATTERNS.md
@.planning/phases/08-warehouses/08-UI-SPEC.md
@.planning/phases/08-warehouses/08-VALIDATION.md
@.planning/phases/08-warehouses/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: /warehouses routes + page + rows partial</name>
  <files>app/routes/warehouses.py, app/templates/pages/warehouses.html, app/templates/partials/warehouse_rows.html, tests/test_warehouses.py</files>
  <read_first>
    E:\dev\myorishop\app\routes\dictionary.py (page/add/edit route shape to mirror exactly for the warehouse page/add/update routes)
    E:\dev\myorishop\app\routes\products.py (product_delete/product_restore, lines 182-193 — response shape to ADAPT, NOT copy verbatim: this app has no `/warehouses/{id}/edit` page to HX-Redirect to/from)
    E:\dev\myorishop\app\templates\pages\dictionary.html (page shell to mirror: extends base.html, inline add form, include rows partial)
    E:\dev\myorishop\app\templates\partials\dictionary_rows.html (row/edit-form/`error_entry_id` branching to mirror and extend with delete/restore/`warning_id` branching)
    E:\dev\myorishop\app\services\warehouses.py (as created by Plan 08-01 — exact signatures this task wires: list_warehouses, add_warehouse, update_warehouse, soft_delete_warehouse, restore_warehouse)
    E:\dev\myorishop\.planning\phases\08-warehouses\08-UI-SPEC.md (Copywriting Contract — exact RU strings for every label/button/warning; Interaction Contract — response-shape and confirm-gate rules; Color/Spacing/Typography tables confirming no new CSS is needed)
    E:\dev\myorishop\.planning\phases\08-warehouses\08-RESEARCH.md ("Code Examples > Route" and "Code Examples > Template row-level branching")
  </read_first>
  <behavior>
    - GET /warehouses returns 200, renders h1 "Склады", an inline add form (autofocused "Название" input, "Адрес" optional input, "Добавить склад" button), and a rows table listing every warehouse (active + deleted).
    - POST /warehouses (add) with a valid name creates a row and re-renders `partials/warehouse_rows.html` (never a redirect, status 200); blank name re-renders the SAME partial at 422 with "Укажите название склада." present.
    - POST /warehouses/{id} (edit) updates name/address in place, same 200/422 rows-partial-only response shape; unknown id raises 404.
    - POST /warehouses/{id}/delete on a non-last-active warehouse soft-deletes immediately (200, rows partial, that row now shows "Восстановить" and is muted, no HX-Redirect header).
    - POST /warehouses/{id}/delete on the LAST active warehouse (no confirm) performs zero writes and renders the inline warning block under that row ("Это последний активный склад" + "Удалить всё равно" button carrying `hx-vals='{"confirm": "1"}'`); re-POSTing the SAME URL with `confirm=1` deletes it.
    - POST /warehouses/{id}/restore clears deleted_at and re-renders the rows partial with that row active again.
  </behavior>
  <action>
    Create app/routes/warehouses.py. Imports: `from fastapi import APIRouter, Depends, Form, HTTPException, Request`; `from sqlalchemy.orm import Session`; `from app.db import get_session`; `from app.routes import templates`; `from app.services.warehouses import add_warehouse, list_warehouses, restore_warehouse, soft_delete_warehouse, update_warehouse`. `router = APIRouter()`. Add a route-order comment matching dictionary.py's convention (literal `/warehouses` routes declared before the parameterized `/warehouses/{warehouse_id}` routes below — not actually ambiguous here since there is no literal sub-path like `/dictionary/lookup`, but keep the comment for consistency).

    Define `GET /warehouses` -> `warehouses_page(request: Request, session: Session = Depends(get_session))`: `context = {"warehouses": list_warehouses(session), "errors": {}, "form": {}}`; return `templates.TemplateResponse(request, "pages/warehouses.html", context)`.

    Define `POST /warehouses` -> `warehouse_add(request: Request, name: str = Form(""), address: str = Form(""), session: Session = Depends(get_session))`: call `add_warehouse(session, name=name, address=address)`; build `context = {"warehouses": list_warehouses(session), "errors": errors, "form": {"name": name, "address": address} if errors else {}}`; return `templates.TemplateResponse(request, "partials/warehouse_rows.html", context, status_code=422 if errors else 200)` — mirrors `dictionary_add` exactly.

    Define `POST /warehouses/{warehouse_id}` -> `warehouse_update(request: Request, warehouse_id: str, name: str = Form(""), address: str = Form(""), session: Session = Depends(get_session))`: call `update_warehouse(session, warehouse_id, name=name, address=address)`; if `"warehouse" in errors`: raise `HTTPException(status_code=404, detail="unknown warehouse")`; else build `context = {"warehouses": list_warehouses(session), "errors": errors, "form": {}, "error_entry_id": warehouse_id if errors else None, "error_form": {"name": name, "address": address} if errors else None}`; return the rows partial at `422 if errors else 200`.

    Define `POST /warehouses/{warehouse_id}/delete` -> `warehouse_delete(request: Request, warehouse_id: str, confirm: str = Form(""), session: Session = Depends(get_session))`: `deleted, warning = soft_delete_warehouse(session, warehouse_id, confirm=confirm == "1")`; `context = {"warehouses": list_warehouses(session), "errors": {}, "form": {}, "warning_id": warehouse_id if warning else None}`; return `templates.TemplateResponse(request, "partials/warehouse_rows.html", context)` at plain 200 (this is a warn-but-allow state, not a validation error).

    Define `POST /warehouses/{warehouse_id}/restore` -> `warehouse_restore(request: Request, warehouse_id: str, session: Session = Depends(get_session))`: call `restore_warehouse(session, warehouse_id)`; `context = {"warehouses": list_warehouses(session), "errors": {}, "form": {}, "warning_id": None}`; return the rows partial at 200. Do NOT import or construct a `Response` with `HX-Redirect` anywhere in this file (Pitfall 1) — every one of the four POST handlers re-renders `partials/warehouse_rows.html`.

    Create app/templates/pages/warehouses.html: `{% extends "base.html" %}` / `{% block content %}`; `<h1>Склады</h1>`; an inline add `<form>` with `hx-post="/warehouses"`, `hx-target="#warehouse-rows"`, `hx-swap="outerHTML"`, `hx-disabled-elt="find button"`, `class="form-actions"`, containing `<input type="text" name="name" placeholder="Название склада" autofocus>`, `<input type="text" name="address" placeholder="Адрес (необязательно)">`, `<button type="submit">Добавить склад</button>`; then `{% include "partials/warehouse_rows.html" %}` — mirrors `pages/dictionary.html` line-for-line with fields/copy swapped per UI-SPEC's Copywriting Contract. No search bar, no pagination on this page (D-10 — matches the roadmap's own phrasing "a warehouse management page", singular, and the expected small cardinality).

    Create app/templates/partials/warehouse_rows.html: swap-target `<div id="warehouse-rows">`. Top error-block for add/edit-level `errors` when `{% if errors and not error_entry_id %}` (mirrors dictionary_rows.html lines 3-10, field names `name`/`address` instead of `code`/`name`). A `<table>` with header row `Название / Адрес / Действия`. Body: `{% for w in warehouses %}` — `<tr{% if w.deleted_at %} class="muted"{% endif %}>`; determine the per-row `name`/`address` input values via the SAME `error_entry_id == w.id and error_form` branching as dictionary_rows.html (fallback to `w.name`/`w.address` when no row-level error), rendering `<input type="text" name="name" value="..." form="edit-{{ w.id }}">` and `<input type="text" name="address" value="..." form="edit-{{ w.id }}">`. Actions cell: an edit `<form id="edit-{{ w.id }}" hx-post="/warehouses/{{ w.id }}" hx-target="#warehouse-rows" hx-swap="outerHTML" hx-disabled-elt="find button">` containing a `button.secondary` "Сохранить склад" submit — PLUS, in the same cell, EITHER (when `not w.deleted_at`) a `button.danger` "Удалить" with `hx-post="/warehouses/{{ w.id }}/delete"`, `hx-confirm="Удалить склад «{{ w.name }}»? Он будет скрыт из списка выбора, история операций сохранится."`, `hx-target="#warehouse-rows"`, `hx-swap="outerHTML"` OR (when `w.deleted_at`) a `button.secondary` "Восстановить" with `hx-post="/warehouses/{{ w.id }}/restore"`, same target/swap. Render per-row validation errors when `error_entry_id == w.id` (mirrors dictionary_rows.html lines 39-41). Immediately after a row, when `warning_id == w.id`, render one extra `<tr><td colspan="3">` containing a `.error-block` with heading "Это последний активный склад", body "Удалить «{{ w.name }}» всё равно? Он будет скрыт из списка выбора, история операций сохранится, но в системе временно не останется ни одного активного склада.", a `button.danger` "Удалить всё равно" with `hx-post="/warehouses/{{ w.id }}/delete"`, `hx-vals='{"confirm": "1"}'`, same target/swap, and NO native `hx-confirm` stacked on top (per UI-SPEC Interaction Contract) — plus a `button.secondary` "Отмена" that dismisses client-side ONLY via `hx-on:click` removing its closest warning `<tr>` from the DOM (no `hx-post`, no server call, mirrors `sale_price_warning.html`'s dismiss pattern). Add a defensive empty-state fallback ("Складов пока нет" heading + explanatory body) for the `{% else %}` branch when `warehouses` is empty — per UI-SPEC this should never trigger post-migration since the seed row is never hard-deleted.

    Add tests to tests/test_warehouses.py: `test_web_warehouses_page_renders` (GET /warehouses -> 200, body contains "Склады", "Добавить склад", and the seeded default warehouse name "Склад по умолчанию"), `test_web_add_and_edit_rows` (POST add with a valid name -> 200, `'id="warehouse-rows"'` present, the new name present, no `<html` in the response; then POST edit on that row -> 200, updated name present), `test_web_add_invalid_returns_swappable_422_partial` (blank name -> 422, "Укажите название склада." present), `test_web_deleted_warehouse_stays_visible_with_restore` (POST add to create a SECOND warehouse first so the seeded default is not the last active one, POST delete on the newly-created one -> assert `response.status_code == 200`, assert `"HX-Redirect" not in response.headers`, assert that warehouse's name is STILL present in the response body AND "Восстановить" is present — D-09), `test_web_delete_last_active_warehouse_warns_then_confirm_deletes` (starting from the single seeded default warehouse with no other active ones present, POST delete on it with no `confirm` -> 200, "Это последний активный склад" and "Удалить всё равно" present in the body, and a follow-up GET /warehouses still shows it as active i.e. NOT muted and without "Восстановить"; then re-POST the same delete URL with `data={"confirm": "1"}` -> 200, that warehouse's row now shows "Восстановить").
  </action>
  <acceptance_criteria>
    - `app/routes/warehouses.py` defines `GET /warehouses`, `POST /warehouses`, `POST /warehouses/{warehouse_id}`, `POST /warehouses/{warehouse_id}/delete`, `POST /warehouses/{warehouse_id}/restore`
    - `grep -c "HX-Redirect" app/routes/warehouses.py` returns 0 (Pitfall 1 — this route module must never use HX-Redirect)
    - `app/templates/partials/warehouse_rows.html` contains `id="warehouse-rows"` and `warning_id`
    - `uv run pytest tests/test_warehouses.py -k web -x` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_warehouses.py -k web -x</automated>
  </verify>
  <done>/warehouses supports create/edit/delete/restore entirely via rows-partial re-renders; a soft-deleted warehouse stays visible with a restore button; deleting the last active warehouse is warn-but-allow with zero writes until confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Nav link + router registration + full-phase verification</name>
  <files>app/templates/base.html, app/main.py, tests/test_warehouses.py</files>
  <read_first>
    E:\dev\myorishop\app\templates\base.html (existing nav `<a>` list — exact insertion point immediately after the `/categories` entry, line 20)
    E:\dev\myorishop\app\main.py (router import tuple, alphabetically ordered — `warehouses` sorts between `sales` and `writeoffs`; `app.include_router(...)` call order, grouped by feature area rather than alphabetically — insert after `categories.router`)
    E:\dev\myorishop\.planning\phases\08-warehouses\08-UI-SPEC.md (section "Screens & Components > Навигация" — full nav order; section "Interaction Contract > Manual UAT gates" — exact steps for the human-check below)
    E:\dev\myorishop\.planning\phases\08-warehouses\08-VALIDATION.md (Per-Task Verification Map — full command list already run across this plan)
  </read_first>
  <behavior>
    - Every page's nav bar includes a "Склады" link to /warehouses, positioned immediately after "Категории" and before "Приход", marked `class="active"` when the request path starts with `/warehouses`.
    - The full pre-existing test suite (247+ tests) plus every new tests/test_warehouses.py test all pass together with zero failures.
  </behavior>
  <action>
    In app/templates/base.html, insert one new nav `<a>` immediately after the existing `/categories` link (line 20) and before the `/receipts/new` link (line 21): `<a href="/warehouses"{% if request.url.path.startswith("/warehouses") %} class="active"{% endif %}>Склады</a>` — identical shape to the existing `/categories` entry, per UI-SPEC's nav order (Главная · Товары · Категории · Склады · Приход · Продажи · Списание · Покупатели · История · Отчёты · Экспорт · Справочник · Резервные копии).

    In app/main.py: add `warehouses` to the `from app.routes import (...)` tuple in its correct alphabetical position (between `sales` and `writeoffs`). Add `app.include_router(warehouses.router)` immediately after the existing `app.include_router(categories.router)` line and before `app.include_router(dictionary.router)` — matches the nav grouping (Склады sits right after Категории).

    Add a test to tests/test_warehouses.py: `test_web_nav_has_warehouses_link` (GET / -> 200, assert `href="/warehouses"` and "Склады" both present in the response body).

    Run the full test suite: `uv run pytest -q`. It must report zero failures across the pre-existing 247+ tests plus every new `tests/test_warehouses.py` test from both Plan 08-01 and this plan.
  </action>
  <acceptance_criteria>
    - `app/templates/base.html` contains `href="/warehouses"` positioned after the `/categories` `<a>` and before the `/receipts/new` `<a>`
    - `app/main.py` imports `warehouses` and calls `app.include_router(warehouses.router)`
    - `uv run pytest -q` reports 0 failed (full suite: pre-existing 247+ tests plus every new warehouse test)
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q</automated>
    <human-check>Start the app (`uv run uvicorn app.main:app --port 8000`) and open http://localhost:8000/warehouses. Confirm: (1) the page shows the seeded "Склад по умолчанию" row on first load with no setup step; (2) adding a warehouse with only a name (no address) succeeds and appears in the list; (3) deleting a NON-last warehouse asks for native browser confirmation only, then disappears into a muted "Восстановить" row with no page navigation; (4) deleting the LAST remaining active warehouse shows the inline "Это последний активный склад" warning with zero navigation, and clicking "Удалить всё равно" completes the delete; (5) "Восстановить" brings a deleted row back to its active styling with a "Удалить" button again; (6) the "Склады" nav link is visible and marked active on this page from every other page in the app.</human-check>
  </verify>
  <done>The /warehouses page is reachable from every screen's nav; the full test suite is green; WH-01 and ROADMAP Phase 8 success criteria 1-3 are satisfied end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser -> POST /warehouses, /warehouses/{id}, /warehouses/{id}/delete, /warehouses/{id}/restore | Untrusted form input: `name`, `address`, `confirm` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering / Information Disclosure | `partials/warehouse_rows.html` rendering `w.name`/`w.address` | mitigate | Jinja2 autoescaping (default, relied on everywhere else in this codebase) — never wrap `w.name`/`w.address` in `\|safe`; matches the T-4-01 convention already established for `sale_oversell.html`/`product` name rendering |
| T-08-05 | Elevation of Privilege | all `/warehouses` routes are unauthenticated | accept | No auth exists anywhere in this v1 single-local-operator app (CLAUDE.md/PROJECT.md decision) — this phase introduces no new risk beyond every other existing route |
| T-08-06 | Repudiation | `confirm=1` re-POST bypassing the last-active-warehouse warning without the UI ever rendering it (e.g. a scripted request) | accept | Same warn-but-allow UX pattern already used for oversell/min-price checks elsewhere in the app (D-06); soft-delete remains fully reversible via restore (D-07) regardless of how `confirm=1` was reached — this is a UX safeguard, not a security control |
| T-08-SC | Tampering | npm/pip/cargo installs | N/A | No new packages introduced by this plan — package legitimacy gate not applicable |
</threat_model>

<verification>
`uv run pytest -q` — full suite (247+ pre-existing tests plus every `tests/test_warehouses.py` test from Plans 08-01 and 08-02) reports zero failures. `uv run ruff check app/routes/warehouses.py app/templates/base.html app/main.py` — no lint errors. Manual UAT per the Task 2 human-check confirms the visual/interaction contract from 08-UI-SPEC.md.
</verification>

<success_criteria>
Operator can create, edit, soft-delete, and restore a warehouse from `/warehouses`; a soft-deleted warehouse stays visible with a restore action; deleting the last active warehouse is warn-but-allow with zero data loss either way; the nav exposes the page from anywhere in the app. This completes WH-01 and ROADMAP Phase 8 success criteria 1-3 in full.
</success_criteria>

<output>
Create `.planning/phases/08-warehouses/08-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `app/routes/warehouses.py` — new router module: `GET /warehouses`, `POST /warehouses`, `POST /warehouses/{warehouse_id}`, `POST /warehouses/{warehouse_id}/delete`, `POST /warehouses/{warehouse_id}/restore`
- `app/templates/pages/warehouses.html` — new page template
- `app/templates/partials/warehouse_rows.html` — new partial template (`#warehouse-rows` swap target, `warning_id` context flag)
- `base.html` nav link `href="/warehouses"` labeled «Склады»
- `app.main` import of `warehouses` + `app.include_router(warehouses.router)`
</output>

---
phase: 08-warehouses
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models.py
  - alembic/versions/0007_warehouses.py
  - app/services/warehouses.py
  - tests/test_warehouses.py
autonomous: true
requirements: [WH-01]

must_haves:
  truths:
    - "A Warehouse row can be created with a required name and optional address (D-04)"
    - "Soft-deleting a warehouse sets deleted_at without removing the row; restoring clears deleted_at, and the row is never excluded from list_warehouses (D-05/D-09)"
    - "Soft-deleting the LAST remaining active warehouse is blocked with a warning until confirm=1 is passed; zero writes occur before that confirmation (D-06)"
    - "After migration, the database contains exactly one seeded default warehouse (id 00000000-0000-4000-8000-000000000010, name \"Склад по умолчанию\"), and all existing v1.0 tables/rows are untouched (D-01/D-02, ROADMAP Phase 8 success criterion 2)"
  artifacts:
    - path: "app/models.py"
      provides: "Warehouse model (id/name/address/created_at/updated_at/deleted_at)"
      contains: "class Warehouse"
    - path: "alembic/versions/0007_warehouses.py"
      provides: "warehouses table + seeded default row"
      contains: "revision = \"0007\""
    - path: "app/services/warehouses.py"
      provides: "CRUD + warn-but-allow service functions"
      contains: "def soft_delete_warehouse"
  key_links:
    - from: "app/services/warehouses.py"
      to: "app/models.py"
      via: "Warehouse ORM queries (select/session.get)"
      pattern: "select\\(Warehouse\\)"
    - from: "alembic/versions/0007_warehouses.py"
      to: "warehouses table"
      via: "op.bulk_insert seed row"
      pattern: "DEFAULT_WAREHOUSE_ID"
---

<objective>
Deliver the backend half of WH-01: a standalone `Warehouse` model and migration (D-01/D-02 — no FK wiring to `Product`/`Operation` yet), seeded with exactly one default row so v1.0 stock is conceptually attributed without any row-by-row change, plus a service layer covering create/edit/soft-delete/restore with the warn-but-allow guard on deleting the last remaining active warehouse (D-06/D-07). This plan has no HTTP-facing surface — Plan 08-02 wires it to `/warehouses`.

Purpose: `Batch.warehouse_id` (Phase 9) needs `Warehouse` to exist first (ROADMAP Phase 8 dependency note) — this plan builds that structural prerequisite and its full CRUD contract in isolation, testable via pytest without any route/template code.
Output: `Warehouse` model, migration `0007_warehouses.py` (frozen seed: id `00000000-0000-4000-8000-000000000010`, name `"Склад по умолчанию"`), `app/services/warehouses.py`, and `tests/test_warehouses.py` covering the migration and every service function.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-warehouses/08-CONTEXT.md
@.planning/phases/08-warehouses/08-RESEARCH.md
@.planning/phases/08-warehouses/08-PATTERNS.md
@.planning/phases/08-warehouses/08-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Warehouse model + migration 0007 (schema, frozen seed)</name>
  <files>app/models.py, alembic/versions/0007_warehouses.py, tests/test_warehouses.py</files>
  <read_first>
    E:\dev\myorishop\app\models.py (Dictionary class, lines 119-133 — exact structural shape to copy; Product class, lines 91-116 — deleted_at/created_at/updated_at soft-delete column convention)
    E:\dev\myorishop\alembic\versions\0001_initial_schema.py (DEMO_PRODUCT_ID frozen-seed / op.bulk_insert pattern to mirror for the default warehouse row)
    E:\dev\myorishop\alembic\versions\0002_catalog_dictionary.py (op.create_table shape for a new standalone table with PK named via op.f, no FK)
    E:\dev\myorishop\tests\test_catalog.py (test_migration_0006_adds_min_sale_cents_column, around line 886 — exact alembic upgrade-to-head migration test pattern: tmp_path db, monkeypatch settings.db_path, Config("alembic.ini"), command.upgrade)
    E:\dev\myorishop\.planning\phases\08-warehouses\08-RESEARCH.md (sections "Code Examples > Model" and "Code Examples > Migration" — ready-to-adapt reference implementations; Pitfall 4 and Pitfall 5)
    E:\dev\myorishop\.planning\phases\08-warehouses\08-CONTEXT.md (D-01 through D-05; D-03's cross-phase contract Phase 9 will rely on)
  </read_first>
  <behavior>
    - `Warehouse` is a new SQLAlchemy model: `id` (UUID str PK via `new_id`), `name` (required, non-null), `address` (optional, nullable), `created_at`/`updated_at` (via `utcnow_iso`), `deleted_at` (nullable, soft-delete only).
    - Migration 0007 creates a standalone `warehouses` table (no FK to/from `products`/`operations`, D-01) and seeds exactly one row: id `00000000-0000-4000-8000-000000000010`, name `"Склад по умолчанию"`, address NULL, deleted_at NULL.
    - `alembic upgrade head` applied to a fresh DB at revision 0006 creates the table cleanly; every existing table/row from the prior chain is untouched (no data loss, ROADMAP Phase 8 success criterion 2).
    - `downgrade()` drops the `warehouses` table cleanly.
  </behavior>
  <action>
    In app/models.py, add a new `Warehouse(Base)` class immediately after the `Dictionary` class and before `Operation`. `__tablename__ = "warehouses"`. Columns: `id: Mapped[str] = mapped_column(String(36), primary_key=True, default=new_id)`; `name: Mapped[str] = mapped_column(String(200), nullable=False)`; `address: Mapped[str | None] = mapped_column(String(300))`; `created_at: Mapped[str] = mapped_column(String(32), default=utcnow_iso)`; `updated_at: Mapped[str] = mapped_column(String(32), default=utcnow_iso, onupdate=utcnow_iso)`; `deleted_at: Mapped[str | None] = mapped_column(String(32))` — matches `Product.deleted_at`'s exact shape. Add a class docstring citing D-01/D-02 (standalone table, no FK yet — `Batch.warehouse_id` in Phase 9 is the real link) and D-03 (this table's seeded default row is the stable identity Phase 9's legacy-batch migration will point at). No unique constraint on `name` (D-04: no uniqueness enforced).

    Create alembic/versions/0007_warehouses.py: `revision = "0007"`, `down_revision = "0006"`, `branch_labels = None`, `depends_on = None`. Header docstring documents D-01 (standalone table, no FK from products/operations), D-02 (conceptual "no data loss" — nothing yet references warehouses to lose), D-03 (frozen seed identity Phase 9 will rely on), and restates the WR-06 immutability rule (this file must never import `app.models`/`app.core`; every value below is a frozen literal). Define module constants `DEFAULT_WAREHOUSE_ID = "00000000-0000-4000-8000-000000000010"` and `_SEED_CREATED_AT = "2026-07-11T00:00:00+00:00"`. In `upgrade()`: `op.create_table("warehouses", ...)` with columns `id` (`sa.String(36)`, not null), `name` (`sa.String(200)`, not null), `address` (`sa.String(300)`, nullable), `created_at`/`updated_at` (`sa.String(32)`, not null), `deleted_at` (`sa.String(32)`, nullable), and `sa.PrimaryKeyConstraint("id", name=op.f("pk_warehouses"))` — mirrors 0002's `op.create_table("dictionary", ...)` shape exactly. Then one `op.bulk_insert` via a `sa.table("warehouses", sa.column("id", sa.String), sa.column("name", sa.String), sa.column("address", sa.String), sa.column("created_at", sa.String), sa.column("updated_at", sa.String))` shim, inserting exactly one row: `{"id": DEFAULT_WAREHOUSE_ID, "name": "Склад по умолчанию", "address": None, "created_at": _SEED_CREATED_AT, "updated_at": _SEED_CREATED_AT}` — mirrors 0001's `DEMO_PRODUCT_ID` seed pattern. In `downgrade()`: `op.drop_table("warehouses")`.

    Create tests/test_warehouses.py (new file) with a module docstring stating the file's contract (mirrors test_dictionary.py's naming convention note: "route/e2e tests are prefixed test_web_, everything else is service/schema level"). Required imports at the top: `sqlite3`; `from contextlib import closing`; `from alembic.config import Config`; `from alembic import command`; `from app.config import settings` (mirrors test_catalog.py's migration-test imports). Add `test_migration_0007_creates_and_seeds_default_warehouse`, mirroring `test_migration_0006_adds_min_sale_cents_column`'s exact shape: `tmp_path` db file, `monkeypatch.setattr(settings, "db_path", db_file.as_posix())`, `cfg = Config("alembic.ini")`, `command.upgrade(cfg, "0006")`, then `command.upgrade(cfg, "head")`. Via a `sqlite3.connect` + `closing`: assert `PRAGMA table_info(warehouses)` columns include `{"id", "name", "address", "created_at", "updated_at", "deleted_at"}`; assert `SELECT id, name, address, deleted_at FROM warehouses` returns exactly one row equal to the literal tuple `("00000000-0000-4000-8000-000000000010", "Склад по умолчанию", None, None)` (hardcode the literal id/name strings directly in the test — mirrors how existing tests hardcode `DEMO_PRODUCT_ID`'s value rather than importing it from a migration module).
  </action>
  <acceptance_criteria>
    - `app/models.py` defines `class Warehouse(Base)` with `__tablename__ = "warehouses"`
    - `grep -c "class Warehouse" app/models.py` returns 1
    - `alembic/versions/0007_warehouses.py` exists with `revision = "0007"` and `down_revision = "0006"`
    - `grep -c "import app.models\|import app.core\|from app" alembic/versions/0007_warehouses.py` returns 0 (WR-06: migration never imports app modules)
    - `uv run pytest tests/test_warehouses.py -k migration_0007 -x` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_warehouses.py -k migration_0007 -x</automated>
  </verify>
  <done>Warehouse model exists with id/name/address/created_at/updated_at/deleted_at; migration 0007 creates the warehouses table and seeds exactly one default row (frozen id 00000000-0000-4000-8000-000000000010, name "Склад по умолчанию"); existing v1.0 data is untouched after upgrade.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Warehouse service layer (CRUD + warn-but-allow last-warehouse guard)</name>
  <files>app/services/warehouses.py, tests/test_warehouses.py</files>
  <read_first>
    E:\dev\myorishop\app\services\dictionary.py (add_entry/update_entry/list_entries — CRUD shape to mirror for add_warehouse/update_warehouse/list_warehouses)
    E:\dev\myorishop\app\services\catalog.py (soft_delete_product/restore_product, lines 295-310 — idempotent shape to mirror for restore_warehouse and the pre-write guard in soft_delete_warehouse)
    E:\dev\myorishop\app\services\sales.py (register_sale's oversell confirm-gate, lines 137-153 — the read-before-write warn-but-allow shape to transpose to a single-row delete)
    E:\dev\myorishop\.planning\phases\08-warehouses\08-RESEARCH.md ("Code Examples > Service" and "Architecture Patterns > Pattern 3" — verified, ready-to-use function bodies)
    E:\dev\myorishop\.planning\phases\08-warehouses\08-PATTERNS.md (section "app/services/warehouses.py")
    E:\dev\myorishop\tests\test_warehouses.py (as created by Task 1 in this plan — read current state before appending)
  </read_first>
  <behavior>
    - `list_warehouses(session)` returns ALL rows (active AND soft-deleted, D-09), sorted active-first then by name.
    - `add_warehouse(session, name=..., address=...)` strips both fields, requires non-blank `name` (error `"Укажите название склада."` otherwise), returns `(Warehouse, {})` on success or `(None, errors)` — no uniqueness check (D-04).
    - `update_warehouse(session, warehouse_id, name=..., address=...)` edits an existing row in place with the same validation; unknown id returns `(None, {"warehouse": "Склад не найден."})`.
    - `restore_warehouse(session, warehouse_id)` clears `deleted_at`; idempotent no-op if already active or unknown id.
    - `soft_delete_warehouse(session, warehouse_id, confirm=False)` sets `deleted_at` UNLESS the target is the last remaining ACTIVE warehouse and `confirm` is falsy — in that case ZERO writes occur and it returns `(False, {"warehouse": <Warehouse>})`; `confirm=True` always proceeds. Unknown id or already-deleted row returns `(False, {})`.
  </behavior>
  <action>
    Create app/services/warehouses.py. Imports: `from sqlalchemy import func, select`; `from sqlalchemy.orm import Session`; `from app.core import new_id, utcnow_iso`; `from app.models import Warehouse`. Define `NAME_REQUIRED_ERROR = "Укажите название склада."` and `WAREHOUSE_NOT_FOUND_ERROR = "Склад не найден."`.

    Define `list_warehouses(session: Session) -> list[Warehouse]`: `rows = list(session.scalars(select(Warehouse)))`; return `sorted(rows, key=lambda w: (w.deleted_at is not None, w.name))` — per D-09/Pitfall 2, this query MUST NOT filter by `deleted_at`.

    Define `add_warehouse(session: Session, *, name: str, address: str) -> tuple[Warehouse | None, dict[str, str]]`: strip both args, `errors: dict[str, str] = {}`, if not `name`: `errors["name"] = NAME_REQUIRED_ERROR`; if `errors`: return `(None, errors)`; else `warehouse = Warehouse(id=new_id(), name=name, address=address or None)`, `session.add(warehouse)`, `session.commit()`, return `(warehouse, {})`.

    Define `update_warehouse(session: Session, warehouse_id: str, *, name: str, address: str) -> tuple[Warehouse | None, dict[str, str]]`: `warehouse = session.get(Warehouse, warehouse_id)`; if `warehouse is None`: return `(None, {"warehouse": WAREHOUSE_NOT_FOUND_ERROR})`; strip+validate `name` exactly as `add_warehouse`; on error return `(None, errors)`; else `warehouse.name = name`, `warehouse.address = address or None`, `session.commit()`, return `(warehouse, {})`.

    Define `restore_warehouse(session: Session, warehouse_id: str) -> None`: mirrors `restore_product` exactly — `session.get`, early return if `None` or already active (`deleted_at is None`), else `warehouse.deleted_at = None; session.commit()`.

    Define `soft_delete_warehouse(session: Session, warehouse_id: str, *, confirm: bool = False) -> tuple[bool, dict]`: `warehouse = session.get(Warehouse, warehouse_id)`; if `warehouse is None or warehouse.deleted_at is not None`: return `(False, {})`; if not `confirm`: `active_count = session.scalar(select(func.count()).select_from(Warehouse).where(Warehouse.deleted_at.is_(None)))`; if `active_count <= 1`: return `(False, {"warehouse": warehouse})` with ZERO writes staged; else `warehouse.deleted_at = utcnow_iso()`, `session.commit()`, return `(True, {})`.

    Append tests to tests/test_warehouses.py: `test_add_warehouse_creates_row` (name+address round-trip, strips whitespace, 36-char UUID id), `test_add_warehouse_requires_name` (blank name -> `errors["name"] == NAME_REQUIRED_ERROR`, nothing written to `list_warehouses`), `test_add_warehouse_allows_duplicate_names` (two warehouses with the identical name both succeed — D-04, no uniqueness), `test_update_warehouse_edits_fields` (create then update name/address, assert persisted via `list_warehouses` or `session.get`), `test_update_warehouse_unknown_id_returns_error` (random UUID id -> `(None, {"warehouse": WAREHOUSE_NOT_FOUND_ERROR})`), `test_soft_delete_and_restore_roundtrip` (create TWO warehouses so neither is "last active", soft-delete one with `confirm=False`, assert `deleted_at` is a non-empty string AND the row is STILL returned by `list_warehouses` — D-09 applies even at service level — then `restore_warehouse` clears `deleted_at`), `test_delete_last_active_warehouse_warns_then_allows` (construct a session state with exactly ONE active warehouse — e.g. soft-delete the seeded default down to a single remaining active row, or use a fresh in-memory set built only from `add_warehouse` calls in the test — call `soft_delete_warehouse(session, only_id, confirm=False)`, assert it returns `(False, {"warehouse": <that Warehouse>})` and the row's `deleted_at` is still `None` afterward (zero writes); then call `soft_delete_warehouse(session, only_id, confirm=True)`, assert it returns `(True, {})` and `deleted_at` is now set), `test_restore_warehouse_is_idempotent_when_already_active` (calling restore on a never-deleted row is a no-op, no exception).
  </action>
  <acceptance_criteria>
    - `app/services/warehouses.py` defines `list_warehouses`, `add_warehouse`, `update_warehouse`, `soft_delete_warehouse`, `restore_warehouse`
    - `grep -c "def soft_delete_warehouse" app/services/warehouses.py` returns 1
    - `grep -c "deleted_at.is_(None)" app/services/warehouses.py` returns exactly 1 (the active-count guard only — confirms `list_warehouses` does NOT filter by `deleted_at`, per D-09/Pitfall 2)
    - `uv run pytest tests/test_warehouses.py -x -q` passes (full file, migration + service tests)
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_warehouses.py -x -q</automated>
  </verify>
  <done>Service layer supports create/edit/soft-delete/restore for warehouses; deleted rows are never filtered out of list_warehouses (D-09); soft-deleting the sole remaining active warehouse is blocked pending confirm=1, with zero writes on the blocked path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `app/services/warehouses.py` (called by not-yet-built routes) | Untrusted `name`/`address` strings will arrive from HTTP form input once Plan 08-02 wires this service to `/warehouses` — this plan's `add_warehouse`/`update_warehouse` are the enforcement point for that future boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | `add_warehouse`/`update_warehouse` (app/services/warehouses.py) | mitigate | Required `name` field validated non-blank after `.strip()`, mirroring the Dictionary/Product pattern; no unique constraint is required per D-04 so no duplicate-race handling is needed here (unlike Dictionary's `uq_dictionary_code`) |
| T-08-02 | Tampering | `alembic/versions/0007_warehouses.py` | mitigate | WR-06 compliance: file contains zero imports of `app.models`/`app.core`; `DEFAULT_WAREHOUSE_ID` and the seed name/timestamp are frozen literals so replaying the migration chain on a fresh DB is deterministic forever |
| T-08-03 | Repudiation | `soft_delete_warehouse`'s `confirm=True` bypass of the last-active-warehouse warning | accept | `confirm` is a warn-but-allow UX safeguard, not an authorization boundary — this app has no auth in v1 (CLAUDE.md constraint); soft-delete is fully reversible via `restore_warehouse`, so bypassing the warning causes no data loss |
</threat_model>

<verification>
`uv run pytest tests/test_warehouses.py -x -q` — new file green, covering migration 0007 and every service function (list/add/update/soft-delete/restore, including the warn-but-allow round trip). `uv run pytest -q` — full existing suite (247+ tests) unaffected. `uv run ruff check app/models.py app/services/warehouses.py alembic/versions/0007_warehouses.py` — no lint errors.
</verification>

<success_criteria>
`Warehouse` exists as a standalone table seeded with one default row (no FK wiring, D-01/D-02); the service layer supports create/edit/soft-delete/restore with the last-active-warehouse warn-but-allow guard (D-06/D-07); every behavior is covered by an automated test. This satisfies the backend half of WH-01 and unblocks Plan 08-02, which wires this service to `/warehouses`.
</success_criteria>

<output>
Create `.planning/phases/08-warehouses/08-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `app.models.Warehouse` — new SQLAlchemy model class (`id`, `name`, `address`, `created_at`, `updated_at`, `deleted_at`)
- `alembic/versions/0007_warehouses.py` — new migration (`revision = "0007"`, `down_revision = "0006"`), module constants `DEFAULT_WAREHOUSE_ID` and `_SEED_CREATED_AT`
- `app.services.warehouses.list_warehouses(session) -> list[Warehouse]`
- `app.services.warehouses.add_warehouse(session, *, name, address) -> tuple[Warehouse | None, dict[str, str]]`
- `app.services.warehouses.update_warehouse(session, warehouse_id, *, name, address) -> tuple[Warehouse | None, dict[str, str]]`
- `app.services.warehouses.soft_delete_warehouse(session, warehouse_id, *, confirm=False) -> tuple[bool, dict]`
- `app.services.warehouses.restore_warehouse(session, warehouse_id) -> None`
- `app.services.warehouses.NAME_REQUIRED_ERROR`, `app.services.warehouses.WAREHOUSE_NOT_FOUND_ERROR` — RU error message constants
- `tests/test_warehouses.py` — new test file (migration + service coverage; Plan 08-02 appends web-layer tests)
</output>

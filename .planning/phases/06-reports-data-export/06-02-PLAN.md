---
phase: 06-reports-data-export
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/core.py
  - app/services/reports.py
  - app/routes/reports.py
  - app/templates/pages/reports_landing.html
  - app/templates/pages/reports_sales.html
  - app/templates/partials/period_filter.html
  - app/templates/partials/sales_report_results.html
  - app/templates/base.html
  - app/main.py
  - app/static/style.css
  - tests/test_core.py
  - tests/test_reports.py
autonomous: true
requirements: [RPT-01]

must_haves:
  truths:
    - "Operator can view sales/profit totals and a per-product breakdown for today, this week, this month, or a custom с/по range"
    - "An evening sale near local midnight lands in the correct local day's report, never the neighboring UTC day"
    - "Profit never silently includes phantom profit from a sale line whose cost is unknown — the report shows a visible caveat instead"
    - "Reports landing page at /reports links to the sales/profit report; nav has an «Отчёты» entry"
  artifacts:
    - path: "app/core.py"
      provides: "local_day_bounds_utc(start_day, end_day, tz_name) -> (start_iso, end_iso)"
      contains: "def local_day_bounds_utc"
    - path: "app/services/reports.py"
      provides: "sales_profit_report(session, start_iso, end_iso) -> dict"
      contains: "def sales_profit_report"
    - path: "app/routes/reports.py"
      provides: "GET /reports, GET /reports/sales, shared _resolve_period period-parsing helper"
      contains: "def _resolve_period"
  key_links:
    - from: "app/routes/reports.py"
      to: "app/core.py"
      via: "local_day_bounds_utc call inside GET /reports/sales"
      pattern: "local_day_bounds_utc\\("
    - from: "app/templates/base.html"
      to: "app/routes/reports.py"
      via: "nav link href=\"/reports\""
      pattern: "href=\"/reports\""
    - from: "app/main.py"
      to: "app/routes/reports.py"
      via: "app.include_router(reports.router)"
      pattern: "include_router\\(reports\\.router\\)"
---

<objective>
Build the flagship report — sales & profit for a chosen period — and, alongside it, the shared reporting infrastructure the rest of Phase 6 reuses: the local-day boundary helper (D-02's mandatory correctness rule), the `/reports` landing page, nav entry, and the one-code-path period filter (D-01) that Plans 06-05 and 06-06 will also include unchanged.

Purpose: RPT-01 is the phase's primary success criterion ("operator can view sales and profit reports for a day, week, month, or custom period, with correct local-day boundaries"). Getting local-day boundary math right here, once, correctly, is the phase's core technical risk (RESEARCH: "the only genuinely new technical risk").
Output: `local_day_bounds_utc`, `sales_profit_report`, `/reports` + `/reports/sales`, `partials/period_filter.html` (reused by 06-05/06-06), nav + landing page.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-reports-data-export/06-CONTEXT.md
@.planning/phases/06-reports-data-export/06-RESEARCH.md
@.planning/phases/06-reports-data-export/06-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: local_day_bounds_utc — mandatory local-day boundary helper</name>
  <files>app/core.py, tests/test_core.py</files>
  <read_first>
    E:\dev\myorishop\app\core.py
    E:\dev\myorishop\tests\test_core.py
  </read_first>
  <behavior>
    - local_day_bounds_utc(date(2026, 7, 10), date(2026, 7, 10), "Europe/Moscow") returns a (start_iso, end_iso) tuple where start_iso is local midnight of 2026-07-10 converted to UTC ("2026-07-09T21:00:00+00:00", Moscow is UTC+3, no DST) and end_iso is local midnight of 2026-07-11 converted to UTC ("2026-07-10T21:00:00+00:00").
    - A sale timestamped "2026-07-10T20:30:00+00:00" (23:30 local time on 2026-07-10) satisfies start_iso <= created_at < end_iso for the single day (2026-07-10, 2026-07-10) — the exact evening-sale-near-midnight case D-02 exists to get right.
    - A sale timestamped "2026-07-10T21:30:00+00:00" (00:30 local time on 2026-07-11, the NEXT local day) does NOT satisfy that same range — it belongs to July 11's report, not July 10's.
    - local_day_bounds_utc(monday, sunday, tz) for a 7-day range returns end_iso = local midnight of the day AFTER sunday (never a closed <= range).
    - Both returned strings sort lexicographically consistent with utcnow_iso()'s existing format (timespec="seconds", +00:00 suffix) so they compare correctly against Operation.created_at with plain string >=/<.
  </behavior>
  <action>
    Extend the datetime import line in app/core.py (currently "from datetime import UTC, datetime") to "from datetime import UTC, date, datetime, time, timedelta". Add local_day_bounds_utc(start_day: date, end_day: date, tz_name: str) -> tuple[str, str] after iso_to_local, following its one-line-summary-plus-example docstring style. Implementation: tz = ZoneInfo(tz_name); start_local = datetime.combine(start_day, time.min, tzinfo=tz); end_local = datetime.combine(end_day, time.min, tzinfo=tz) + timedelta(days=1); return (start_local.astimezone(UTC).isoformat(timespec="seconds"), end_local.astimezone(UTC).isoformat(timespec="seconds")). Docstring states explicitly: half-open range, end_day is the LAST included local calendar day, callers filter created_at >= start AND created_at < end, never a closed range (this is D-02's exact contract — copy the reasoning, do not paraphrase it away).

    Add tests to tests/test_core.py: test_local_day_bounds_utc_single_day_moscow (asserts the exact ISO strings above), test_local_day_bounds_utc_evening_sale_within_local_day and test_local_day_bounds_utc_next_local_day_excluded (string-comparison assertions using the returned bounds directly against the two example timestamps above — together these cover RESEARCH.md's Validation Architecture "test_local_day_bounds_utc_dst_boundary" entry, named for the UTC-rollover edge case it guards, even though Europe/Moscow itself does not currently observe DST), test_local_day_bounds_utc_week_range_is_half_open (7-day Monday-Sunday range, asserts end_iso equals local midnight of the Monday after).
  </action>
  <acceptance_criteria>
    - app/core.py contains "def local_day_bounds_utc(start_day: date, end_day: date, tz_name: str) -> tuple[str, str]"
    - uv run pytest tests/test_core.py -x -q passes (existing money-helper tests plus the four new ones)
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_core.py -x -q</automated>
  </verify>
  <done>local_day_bounds_utc exists, is correctly documented as half-open, and its evening-sale/next-day boundary behavior is pinned by tests — this is the ONLY helper any report in this phase uses for period math.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: sales_profit_report — NULL-cost-safe aggregation service</name>
  <files>app/services/reports.py, tests/test_reports.py</files>
  <read_first>
    E:\dev\myorishop\app\services\operations.py
    E:\dev\myorishop\app\services\sales.py
    E:\dev\myorishop\app\models.py
    E:\dev\myorishop\app\services\ledger.py
  </read_first>
  <behavior>
    - Two sale lines in the period, same product, one with unit_cost_cents=1000 and one with unit_cost_cents=None (both unit_price_cents=1500, qty=1 each, i.e. qty_delta=-1): totals["units_sold"] == 2, totals["revenue_cents"] == 3000 (both lines counted), totals["cost_cents"] == 1000 (only the known-cost line), totals["profit_cents"] == 500 (only (1500-1000)*1 from the known-cost line — NOT 3000-1000=2000, which would silently inflate profit by the cost-unknown line's full revenue per RESEARCH Pitfall 2), totals["cost_unknown_count"] == 1.
    - by_product is a list sorted by qty descending; each entry has product, qty, revenue_cents, cost_cents, profit_cents computed with the same NULL-safe rule as the totals.
    - A sale operation with created_at outside [start_iso, end_iso) is excluded entirely (reuses local_day_bounds_utc's half-open contract from Task 1 — test by constructing bounds via that function and placing one sale just inside, one just outside).
    - A sale on a product that is later soft-deleted still appears in a report for a period BEFORE the deletion (RESEARCH Pitfall 5: sales/profit reports are historical, never filter Product.deleted_at).
  </behavior>
  <action>
    Create app/services/reports.py. Module docstring follows the app/services/operations.py read-only-service convention: states this file is 100% read-only (Phase 6 boundary — no operation types added, no ledger writes), references RPT-01/RPT-03/RPT-04, portable-ORM-only. Imports: "from sqlalchemy import select", "from sqlalchemy.orm import Session", "from app.models import Operation, Product".

    Implement sales_profit_report(session: Session, start_iso: str, end_iso: str) -> dict. Query: select(Operation, Product).join(Product, Operation.product_id == Product.id).where(Operation.type == "sale", Operation.created_at >= start_iso, Operation.created_at < end_iso) — deliberately NO Product.deleted_at.is_(None) filter (Pitfall 5). Aggregate in Python with a dict[str, dict] keyed by product.id (mirrors sales.py's requested_by_product accumulation style): for each (op, product) row, qty = -op.qty_delta; accumulate revenue = (op.unit_price_cents or 0) * qty into the product entry's revenue_cents unconditionally; when op.unit_cost_cents is not None, also accumulate op.unit_cost_cents * qty into cost_cents and (revenue - op.unit_cost_cents * qty) into profit_cents for that entry AND increment a separate line-level cost_unknown_count accumulator only when cost IS None. Build by_product as sorted(entries.values(), key=lambda e: e["qty"], reverse=True). Build totals by summing qty/revenue_cents/cost_cents/profit_cents across all entries, plus the standalone cost_unknown_count (store this same value under totals["cost_unknown_count"] too, so the template only ever reads from totals). Return {"totals": {...}, "by_product": by_product, "cost_unknown_count": cost_unknown_count}.

    Create tests/test_reports.py. Add a local test helper _record_sale_at(session, monkeypatch, iso, *, product, qty, price_cents, cost_cents=None): imports app.services.ledger as ledger_module inside the helper, calls monkeypatch.setattr(ledger_module, "utcnow_iso", lambda: iso), then calls record_operation(session, type_="sale", product_id=product.id, qty_delta=-qty, unit_price_cents=price_cents, unit_cost_cents=cost_cents) (import record_operation from app.services.ledger at module level). Add tests: test_sales_report_null_cost (the exact scenario in Behavior above), test_sales_report_by_product_sorted_by_qty_desc, test_sales_report_excludes_outside_period (uses local_day_bounds_utc from Task 1 to build real bounds), test_sales_report_includes_deleted_product_for_past_period (Pitfall 5, sets product.deleted_at after recording the sale, asserts the product still appears in by_product).
  </action>
  <acceptance_criteria>
    - app/services/reports.py defines "def sales_profit_report(session: Session, start_iso: str, end_iso: str) -> dict"
    - The function body contains no Product.deleted_at filter (read the function, do not blind-grep the whole file since later plans add other functions here that DO need such a filter)
    - uv run pytest tests/test_reports.py -x -q passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_reports.py -x -q</automated>
  </verify>
  <done>sales_profit_report returns mathematically correct, NULL-cost-safe totals and a sorted per-product breakdown, proven by tests including the exact Pitfall 2 scenario.</done>
</task>

<task type="auto">
  <name>Task 3: /reports landing + /reports/sales route, templates, nav, period filter partial</name>
  <files>app/routes/reports.py, app/templates/pages/reports_landing.html, app/templates/pages/reports_sales.html, app/templates/partials/period_filter.html, app/templates/partials/sales_report_results.html, app/templates/base.html, app/main.py, app/static/style.css, tests/test_reports.py</files>
  <read_first>
    E:\dev\myorishop\app\routes\history.py
    E:\dev\myorishop\app\templates\pages\history.html
    E:\dev\myorishop\app\templates\partials\history_filters.html
    E:\dev\myorishop\app\templates\pages\home.html
    E:\dev\myorishop\app\templates\base.html
    E:\dev\myorishop\app\main.py
    E:\dev\myorishop\app\static\style.css
    E:\dev\myorishop\.planning\phases\06-reports-data-export\06-UI-SPEC.md
  </read_first>
  <action>
    Create app/routes/reports.py. Module docstring: thin routes, read-only via app/services/reports.py, references RPT-01. Imports: date, datetime, timedelta from datetime; ZoneInfo from zoneinfo; APIRouter, Depends, Query, Request from fastapi; Session from sqlalchemy.orm; get_session from app.db; templates from app.routes; settings from app.config; local_day_bounds_utc from app.core; sales_profit_report from app.services.reports.

    Add a module-level helper _resolve_period(from_raw: str, to_raw: str, tz_name: str) -> dict. Computes today = datetime.now(ZoneInfo(tz_name)).date(); week_start = today - timedelta(days=today.weekday()) (Monday), week_end = week_start + timedelta(days=6); month_start = today.replace(day=1), month_end = last day of the current month (compute via (month_start.replace(day=28) + timedelta(days=4)).replace(day=1) - timedelta(days=1), stdlib only, no calendar dependency needed). Build presets = {"today": (today, today), "week": (week_start, week_end), "month": (month_start, month_end)}. If both from_raw and to_raw are blank, default from_date = to_date = today (matches D-01: no query params = today's preset, not an error). Otherwise parse each with date.fromisoformat inside try/except ValueError — on failure, set error = "Некорректная дата." and fall back to from_date = to_date = today. When both parse successfully, if from_date > to_date, set error = "Проверьте даты: «с» должно быть раньше или равно «по»." and ALSO fall back to today (never run a query against an inverted or unparsable range — V5). Determine active_preset: the key of the FIRST presets entry whose (start, end) tuple equals (from_date, to_date), else None (custom/hand-edited). Return {"from_date": from_date, "to_date": to_date, "active_preset": active_preset, "error": error, "presets": {key: {"from": d[0].isoformat(), "to": d[1].isoformat()} for key, d in presets.items()}}.

    Add @router.get("/reports") reports_landing(request: Request) returning templates.TemplateResponse(request, "pages/reports_landing.html", {}) — no context needed, the links are static hrefs (only the /reports/sales link exists after this plan; later plans append their own link to this same template).

    Add @router.get("/reports/sales") reports_sales_page(request, from_: str = Query("", alias="from"), to: str = Query("", alias="to"), session: Session = Depends(get_session)) — use FastAPI's Query(alias=...) since "from" is a Python keyword and cannot be a bare parameter name. Call period = _resolve_period(from_, to, settings.display_tz). If period["error"] is set, skip the report query entirely (report = None). Otherwise call start_iso, end_iso = local_day_bounds_utc(period["from_date"], period["to_date"], settings.display_tz) then report = sales_profit_report(session, start_iso, end_iso). Build context with from_date/to_date as .isoformat() strings, active_preset, presets, error, and report (or None). Branch on bool(request.headers.get("HX-Request")): HX request renders ONLY "partials/sales_report_results.html" targeting the results div; a plain/full request renders "pages/reports_sales.html" (which itself includes the period filter partial and, inside a div id="sales-results", the SAME results partial) — mirrors history.py's branching exactly, including the "a filtered top-level GET must still render full chrome" rule (CR-01 precedent — only the real HX-Request header decides, never query-param presence).

    Create app/templates/pages/reports_landing.html: extends base.html, "&lt;h1&gt;Отчёты&lt;/h1&gt;", then a single paragraph "&lt;p&gt;&lt;a href=\"/reports/sales\"&gt;Продажи и прибыль&lt;/a&gt;&lt;/p&gt;" in the home.html link-list style — later plans append " · &lt;a href=...&gt;...&lt;/a&gt;" for their own report inside this SAME paragraph, so leave it as one p element (not a list) to make that append trivial and diff-clean.

    Create app/templates/partials/period_filter.html. Purpose: shared by /reports/sales (this plan), /reports/writeoffs (Plan 06-05), and the top-selling half of /reports/products (Plan 06-06) — must stay generic. Expects context variables period_action (e.g. /reports/sales), period_target (e.g. #sales-results), from_date, to_date (ISO strings), active_preset, presets (dict of {key: {"from","to"}}), error. Renders a div class="preset-bar" with three a elements — Сегодня/Неделя/Месяц — each href="{{ period_action }}?from={{ presets.today.from }}&amp;to={{ presets.today.to }}" (etc per preset key) with hx-get set to the SAME href, hx-target="{{ period_target }}", hx-push-url="true"; each gets class="secondary" UNLESS active_preset equals that preset's key, in which case it gets no class (default accent button styling) — server-side toggle per D-01/UI-SPEC, no client JS. Below that, a form method="get" action="{{ period_action }}" hx-get="{{ period_action }}" hx-target="{{ period_target }}" hx-push-url="true" class="filter-bar" containing two div class="field" blocks (label for="from">с and input type="date" id="from" name="from" value="{{ from_date }}"; and the по/to equivalent) plus button type="submit">Показать. If error is truthy, render p class="error">{{ error }} under the по field, using whichever RU message the route already chose.

    Create app/templates/partials/sales_report_results.html. If report is none (the error path from the route), render a div class="error-block" containing p class="error">Не удалось загрузить отчёт. Проверьте даты и попробуйте ещё раз. Elif report.totals.units_sold == 0, render the empty state p class="empty-state muted">За выбранный период продаж не было. Else: h2>Итоги за период followed by a small table with 4 rows (no header row) — Продано, шт. / Выручка / Себестоимость / Прибыль, each row's value cell class="num" and the | cents filter for the three money rows (units_sold is a bare integer, no filter). If report.cost_unknown_count > 0, a muted caveat paragraph directly below the summary table using the exact copy "{{ report.cost_unknown_count }} продаж без указанной себестоимости — прибыль по ним не учтена." (autoescaped, no |safe). Then h2>По товарам and a full table with header row Товар / Продано, шт. / Выручка / Себестоимость / Прибыль, one tr per report.by_product entry, product cell as "{{ row.product.name }} ({{ row.product.code }})" (matches the /history product-cell convention), money cells via | cents, all numeric cells class="num". This partial itself has NO wrapping id — the page template supplies div id="sales-results" around the include, and the HX route renders this same partial as the innerHTML swap payload for that div (no oob needed, no pagination control here).

    Create app/templates/pages/reports_sales.html: extends base.html, h1>Продажи и прибыль, {% include "partials/period_filter.html" %} wrapped in a {% with period_action = "/reports/sales", period_target = "#sales-results" %}...{% endwith %} block (mirrors the {% with name = ... %} pattern already used in product_form.html for name_input.html), then div id="sales-results">{% include "partials/sales_report_results.html" %}&lt;/div&gt;.

    In app/templates/base.html, insert one new nav link between the /history link and the /dictionary link: a href="/reports" with the standard "{% if request.url.path.startswith(\"/reports\") %} class=\"active\"{% endif %}" active-state check, link text "Отчёты".

    In app/main.py, add reports to the "from app.routes import (...)" tuple, keeping the existing alphabetical order (backup, corrections, customers, dictionary, history, home, products, receipts, reports, returns, sales, writeoffs), and add app.include_router(reports.router) to the registration block, appended after app.include_router(history.router).

    In app/static/style.css, add a new .preset-bar rule directly below the existing .filter-bar block: display: flex; gap: 8px; align-items: center; margin-bottom: 16px;.

    Add web-level tests to tests/test_reports.py: test_web_reports_landing_links_to_sales, test_web_reports_sales_today_default (GET /reports/sales with no query params shows today's data and highlights the «Сегодня» preset — assert the Сегодня link's anchor tag lacks class="secondary" while Неделя/Месяц have it), test_web_reports_sales_invalid_date_shows_ru_error (GET with from=not-a-date renders "Некорректная дата." at 200, never a 500), test_web_reports_sales_hx_request_returns_partial_only (HX-Request header returns the results fragment without "&lt;html" / "&lt;nav", mirrors test_web_search_returns_partial_with_mark's assertion style), test_web_nav_has_reports_link.
  </action>
  <acceptance_criteria>
    - app/routes/reports.py defines _resolve_period, reports_landing, and reports_sales_page
    - app/main.py contains "include_router(reports.router)"
    - app/templates/base.html contains "href=\"/reports\""
    - uv run pytest tests/test_reports.py -x -q passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_reports.py -x -q</automated>
  </verify>
  <done>/reports lists the sales report; /reports/sales renders period totals plus per-product breakdown for the default (today) period and any valid preset or custom range; invalid dates degrade to a RU error, never a 500; nav shows «Отчёты».</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser -> GET /reports/sales | Untrusted query params from, to feeding date parsing and a SQL range filter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|------------------|
| T-06-04 | Denial of Service / Tampering | _resolve_period (app/routes/reports.py) | mitigate | date.fromisoformat wrapped in try/except ValueError; malformed or inverted ranges fall back to today's date with a RU error shown inline — never an uncaught exception reaching a 500 (V5) |
| T-06-05 | Tampering | local_day_bounds_utc / period math (D-02 core risk) | mitigate | Half-open [start, end) UTC range computed via ZoneInfo(settings.display_tz), never a raw UTC-string date slice; pinned by dedicated boundary tests in Task 1 |
| T-06-06 | Information Disclosure | sales_report_results.html | accept (already mitigated system-wide) | Product names render through Jinja's global autoescape (no |safe anywhere in this phase's templates); single local operator, no untrusted multi-tenant boundary |
</threat_model>

<verification>
uv run pytest tests/test_core.py tests/test_reports.py -x -q — all green, including the boundary tests, the aggregation tests, and the web tests. uv run ruff check app/core.py app/services/reports.py app/routes/reports.py app/main.py — no lint errors. Manual smoke (optional): uv run uvicorn app.main:app --port 8000 then visit /reports and /reports/sales.
</verification>

<success_criteria>
RPT-01 is fully satisfied: today/week/month/custom period sales & profit reporting with correct local-day boundaries and NULL-cost-safe profit math. local_day_bounds_utc and partials/period_filter.html exist as the shared contract Plans 06-05 and 06-06 build on without redefining period math.
</success_criteria>

<output>
Create `.planning/phases/06-reports-data-export/06-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `app.core.local_day_bounds_utc(start_day: date, end_day: date, tz_name: str) -> tuple[str, str]` — new function
- `app.services.reports` — new module
- `app.services.reports.sales_profit_report(session, start_iso, end_iso) -> dict` — new function
- `app.routes.reports` — new module
- `app.routes.reports._resolve_period(from_raw, to_raw, tz_name) -> dict` — new function (shared by 06-05, 06-06)
- `GET /reports` — new route
- `GET /reports/sales` — new route
- `app/templates/pages/reports_landing.html` — new template
- `app/templates/pages/reports_sales.html` — new template
- `app/templates/partials/period_filter.html` — new partial (shared by 06-05, 06-06)
- `app/templates/partials/sales_report_results.html` — new partial
- `.preset-bar` — new CSS class in `app/static/style.css`
- nav link `href="/reports"` (`Отчёты`) in `app/templates/base.html`

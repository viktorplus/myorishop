---
phase: 06-reports-data-export
plan: 05
type: execute
wave: 3
depends_on: ["06-02", "06-03"]
files_modified:
  - app/services/reports.py
  - app/routes/reports.py
  - app/templates/pages/reports_writeoffs.html
  - app/templates/partials/writeoffs_report_rows.html
  - app/templates/pages/reports_landing.html
  - tests/test_reports.py
autonomous: true
requirements: [RPT-03]

must_haves:
  truths:
    - "Operator can view write-offs for a chosen period (same preset/с-по filter and local-day boundary math as the sales report — D-01/D-02), grouped by reason, on a dedicated /reports/writeoffs page (D-03)"
    - "The reason grouping uses the exact WRITEOFF_REASONS category set and stable RU-label order established in Phase 5"
    - "A soft-deleted product's past write-offs still appear in a period report covering before its deletion"
  artifacts:
    - path: "app/services/reports.py"
      provides: "writeoff_report(session, start_iso, end_iso) -> dict, grouped by reason_code"
      contains: "def writeoff_report"
  key_links:
    - from: "app/routes/reports.py"
      to: "app/services/reports.py"
      via: "GET /reports/writeoffs calls writeoff_report"
      pattern: "writeoff_report\\("
    - from: "app/templates/partials/writeoffs_report_rows.html"
      to: "app/models.py"
      via: "WRITEOFF_REASONS RU label lookup (Jinja global)"
      pattern: "WRITEOFF_REASONS"
---

<objective>
Give the operator RPT-03: a period-filtered write-off report, grouped by the exact reason categories established in Phase 5 (`WRITEOFF_REASONS`), reusing the SAME period filter (D-01) and local-day boundary math (D-02) built in Plan 06-02 — no new period-handling code, only a new grouping and a new page.

Purpose: this is the phase's third success criterion ("operator can view write-off reports for a chosen period"). The reason categories were purpose-built in Phase 5 for exactly this kind of report (05-CONTEXT.md D-03), so this plan is pure read/aggregate — no new write paths, no new decisions about the category set.
Output: `writeoff_report` in `app/services/reports.py`, `GET /reports/writeoffs`, its template, a new landing-page link.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-reports-data-export/06-CONTEXT.md
@.planning/phases/06-reports-data-export/06-RESEARCH.md
@.planning/phases/06-reports-data-export/06-UI-SPEC.md
@.planning/phases/05-stock-operations-history/05-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: writeoff_report — Python-side grouping by reason_code</name>
  <files>app/services/reports.py, tests/test_reports.py</files>
  <read_first>
    E:\dev\myorishop\app\services\reports.py
    E:\dev\myorishop\app\services\sales.py
    E:\dev\myorishop\app\models.py
  </read_first>
  <behavior>
    - Write-offs in the period with reason_code "expired" (qty 3) and "damaged" (qty 2): writeoff_report returns an ordering that follows WRITEOFF_REASONS' own key order (damaged appears before expired in the dict, since "damaged" is listed first in app/models.py's WRITEOFF_REASONS), each entry carrying the correct summed qty and its RU label.
    - A reason_code present in WRITEOFF_REASONS but with ZERO write-offs in the period does NOT appear in the result (UI-SPEC: "one row per WRITEOFF_REASONS key present in the period").
    - A write-off on a product later soft-deleted still appears in a period report covering the time before deletion (Pitfall 5, same rule as sales_profit_report — no Product.deleted_at filter).
    - A write-off outside [start_iso, end_iso) is excluded.
  </behavior>
  <action>
    In app/services/reports.py, add "from collections import defaultdict" to the imports and "from app.models import WRITEOFF_REASONS" (extend the existing "from app.models import Operation, Product" line to include WRITEOFF_REASONS).

    Implement writeoff_report(session: Session, start_iso: str, end_iso: str) -> dict. Query: select(Operation, Product).join(Product, Operation.product_id == Product.id).where(Operation.type == "writeoff", Operation.created_at >= start_iso, Operation.created_at < end_iso) — no Product.deleted_at filter (Pitfall 5, same reasoning as sales_profit_report). Group in Python via a defaultdict(lambda: {"qty": 0, "lines": []}) keyed by reason_code read from "(op.payload or {}).get('reason_code', 'other')" (mirrors write-off's own payload shape from app/services/writeoffs.py): for each (op, product) row, qty = -op.qty_delta, accumulate into by_reason[reason_code]["qty"] and append {"op": op, "product": product} to by_reason[reason_code]["lines"]. Build the final ordered result by iterating WRITEOFF_REASONS.items() in its OWN declared order and including a reason ONLY if it is present in by_reason, producing a list of {"reason_code": code, "label": ru_label, "qty": ..., "lines": [...]} dicts (a list, not a dict, so the template can iterate it directly in the correct order without needing an OrderedDict). Return {"by_reason": that_list, "total_qty": sum of all qty}.

    Add tests to tests/test_reports.py: test_writeoff_report_groups_by_reason (the two-reason ordering scenario from Behavior above), test_writeoff_report_excludes_reason_with_zero_writeoffs_in_period, test_writeoff_report_includes_deleted_product_for_past_period, test_writeoff_report_excludes_outside_period.
  </action>
  <acceptance_criteria>
    - app/services/reports.py defines "def writeoff_report(session: Session, start_iso: str, end_iso: str) -> dict"
    - writeoff_report's result list order matches WRITEOFF_REASONS' declared key order, not insertion/discovery order (verify by reading the implementation — it must iterate WRITEOFF_REASONS.items(), not by_reason.items())
    - uv run pytest tests/test_reports.py -x -q -k writeoff passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_reports.py -x -q -k writeoff</automated>
  </verify>
  <done>writeoff_report groups period write-offs by reason_code in stable WRITEOFF_REASONS order, correctly excluding empty reasons and correctly including historical deleted-product data.</done>
</task>

<task type="auto">
  <name>Task 2: GET /reports/writeoffs route, templates, landing-page link</name>
  <files>app/routes/reports.py, app/templates/pages/reports_writeoffs.html, app/templates/partials/writeoffs_report_rows.html, app/templates/pages/reports_landing.html, tests/test_reports.py</files>
  <read_first>
    E:\dev\myorishop\app\routes\reports.py
    E:\dev\myorishop\app\templates\pages\reports_sales.html
    E:\dev\myorishop\app\templates\partials\sales_report_results.html
    E:\dev\myorishop\app\templates\pages\reports_landing.html
    E:\dev\myorishop\.planning\phases\06-reports-data-export\06-UI-SPEC.md
  </read_first>
  <action>
    In app/routes/reports.py, add "writeoff_report" to the "from app.services.reports import ..." line. Add @router.get("/reports/writeoffs") reports_writeoffs_page(request, from_: str = Query("", alias="from"), to: str = Query("", alias="to"), session: Session = Depends(get_session)) — same shape as reports_sales_page (Plan 06-02): call the SAME _resolve_period(from_, to, settings.display_tz) helper (do not duplicate period logic), then on success local_day_bounds_utc + writeoff_report; on period error skip the query (report = None). Same HX-Request branching: HX renders ONLY "partials/writeoffs_report_rows.html" (the results div content), full request renders "pages/reports_writeoffs.html".

    Create app/templates/partials/writeoffs_report_rows.html: mirrors sales_report_results.html's error/empty/data three-way branch. Error path (report is none): same error-block copy "Не удалось загрузить отчёт. Проверьте даты и попробуйте ещё раз." Empty path (report.by_reason is empty list): p class="empty-state muted">За выбранный период списаний не было. Data path: table header Причина / Кол-во, шт., one tr per report.by_reason entry showing entry.label and entry.qty (class="num").

    Create app/templates/pages/reports_writeoffs.html: extends base.html, h1>Списания, {% with period_action = "/reports/writeoffs", period_target = "#writeoffs-results" %}{% include "partials/period_filter.html" %}{% endwith %}, h2>Списания по причинам, div id="writeoffs-results">{% include "partials/writeoffs_report_rows.html" %}&lt;/div&gt; (the h2 belongs on the FULL page, not inside the swapped partial, since it never changes — only the table/empty-state below it needs to swap; keep the h2 OUTSIDE div id="writeoffs-results" so the HX partial response only contains the table/empty-state markup, mirroring how sales_report_results.html's OWN h2 elements sit inside ITS swap target since that page's swap target wraps the whole results section including headings — for consistency with 06-02's exact pattern, keep the h2 INSIDE the swapped div here too, i.e. writeoffs_report_rows.html itself starts with the h2, matching sales_report_results.html's own "h2 lives inside the swapped partial" structure exactly. Use this second approach — h2 inside the partial — for structural consistency with Plan 06-02, not the first).

    In app/templates/pages/reports_landing.html, append " · &lt;a href=\"/reports/writeoffs\"&gt;Списания&lt;/a&gt;" inside the same existing &lt;p&gt; element.

    Add web-level tests to tests/test_reports.py: test_web_reports_writeoffs_groups_by_reason, test_web_reports_writeoffs_empty_state, test_web_reports_writeoffs_hx_request_returns_partial_only, test_web_reports_landing_links_to_writeoffs.
  </action>
  <acceptance_criteria>
    - app/routes/reports.py defines reports_writeoffs_page and reuses _resolve_period (no second period-parsing implementation anywhere in the file — grep for "def _resolve_period" returns exactly 1 match)
    - app/templates/pages/reports_landing.html contains "href=\"/reports/writeoffs\"" inside the same &lt;p&gt; as the sales and stock links
    - uv run pytest tests/test_reports.py -x -q passes (full file — all prior plans' tests plus this plan's tests green)
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_reports.py -x -q</automated>
  </verify>
  <done>/reports/writeoffs shows period write-offs grouped by RU reason label in stable order, using the identical period-filter UX as /reports/sales; /reports links to it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser -> GET /reports/writeoffs | Untrusted query params from/to — SAME parsing path as Plan 06-02, no new surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|------------------|
| T-06-04 (inherited) | Denial of Service / Tampering | _resolve_period (shared, app/routes/reports.py) | mitigate | Already mitigated in Plan 06-02; this plan reuses the identical function unmodified — no new date-parsing code added |
| T-06-12 | Information Disclosure | writeoffs_report_rows.html rendering op.payload-derived reason labels | accept (already mitigated system-wide) | reason_code is looked up against the WRITEOFF_REASONS allow-list constant (same server-side allow-list write-offs already validate against at write time, per Phase 5 V5); rendered via Jinja autoescape, never |safe |
</threat_model>

<verification>
uv run pytest tests/test_reports.py -x -q — full file green across all plans landed so far. uv run ruff check app/services/reports.py app/routes/reports.py app/templates.
</verification>

<success_criteria>
RPT-03 is fully satisfied: period write-off reporting grouped by the Phase 5 reason categories, reusing Plan 06-02's period filter and boundary math verbatim.
</success_criteria>

<output>
Create `.planning/phases/06-reports-data-export/06-05-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `app.services.reports.writeoff_report(session, start_iso, end_iso) -> dict` — new function
- `GET /reports/writeoffs` — new route
- `app/templates/pages/reports_writeoffs.html` — new template
- `app/templates/partials/writeoffs_report_rows.html` — new partial

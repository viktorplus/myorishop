---
phase: 25-authentication-roles-user-attribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - app/config.py
  - app/device_id.py
  - .env.example
autonomous: false
requirements: [AUTH-02, AUTH-03]
user_setup: []
must_haves:
  truths:
    - "argon2-cffi and itsdangerous are installed and importable (argon2.PasswordHasher, itsdangerous)"
    - "settings.secret_key is a stable, non-empty value that survives an app restart (does not log everyone out)"
    - "settings.device_id is a per-install UUID persisted OUTSIDE the DB, not the static 'device-01', and is not cloned by copying myorishop.db"
  artifacts:
    - path: "app/config.py"
      provides: "secret_key + per-install device_id resolution"
      contains: "secret_key"
    - path: "app/device_id.py"
      provides: "get_or_create_local_id() file-persisted identity helper"
      min_lines: 15
    - path: "pyproject.toml"
      provides: "argon2-cffi + itsdangerous dependencies"
      contains: "argon2-cffi"
  key_links:
    - from: "app/config.py"
      to: "app/device_id.py"
      via: "device_id / secret_key resolved from a local persisted file"
      pattern: "get_or_create_local_id"
---

<objective>
Add the two new hard dependencies (argon2-cffi, itsdangerous), a persisted signing `secret_key`, and a per-install unique `device_id` — the offline-safe identity/crypto foundation every later auth plan builds on.

Purpose: AUTH-02 needs `argon2-cffi` for Argon2id hashing; AUTH-03 needs `itsdangerous` (behind Starlette `SessionMiddleware`) plus a STABLE `secret_key` that is persisted so a restart does not invalidate every session cookie (RESEARCH Pitfall 5). The roadmap pre-flight requires replacing the static `device_id="device-01"` with a per-install UUID stored OUTSIDE the synced DB so a copied `.db` backup does not clone device identity (RESEARCH Assumption A2 / Pitfall 6) — a prerequisite for all later sync phases.
Output: Two installed packages, `settings.secret_key`, `settings.device_id` resolved per-install, and `app/device_id.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-authentication-roles-user-attribution/25-RESEARCH.md
@.planning/phases/25-authentication-roles-user-attribution/25-PATTERNS.md
@app/config.py
@.gitignore
</context>

<artifacts>
This plan produces:
- Dependencies: `argon2-cffi` (25.1.*), `itsdangerous` (2.2.*) in `pyproject.toml` + `uv.lock`.
- Config fields: `Settings.secret_key: str` (persisted, stable), `Settings.device_id` resolution changed from static `"device-01"` to a per-install UUID.
- New module `app/device_id.py` with `get_or_create_local_id(path) -> str`.
- New tracked doc `.env.example` documenting `SECRET_KEY` / `DEVICE_ID` overrides (real `.env` stays gitignored).
- Persisted local files (gitignored, under `data/`): `data/secret_key`, `data/device_id` (generated at first run, never committed).
</artifacts>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify + install argon2-cffi and itsdangerous (SUS legitimacy gate)</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - .planning/phases/25-authentication-roles-user-attribution/25-RESEARCH.md § "Package Legitimacy Audit" (both packages verdict SUS — a data-availability false-positive, but the gate is mandatory) and § "Standard Stack" (exact versions).
    - CLAUDE.md § Technology Stack (uv workflow: `uv add`; offline/no-CDN constraints).
  </read_first>
  <what-built>
    Nothing installed yet — this checkpoint gates the install of two packages flagged `[SUS]` by the package-legitimacy audit (both flagged only for `unknown-downloads`; argon2-cffi additionally for `no-repository`, a PyPI `project_urls` gap). Per protocol the SUS verdict is retained and the install MUST be human-verified before `uv add`.
  </what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/argon2-cffi/ — confirm it is the Hynek Schlawack Argon2 binding, source github.com/hynek/argon2-cffi, latest release in the 25.1.x line.
    2. Open https://pypi.org/project/itsdangerous/ — confirm it is the Pallets project (github.com/pallets/itsdangerous), 2.2.0 line, and note it is already an indirect dependency of the installed Starlette.
    3. Approve to proceed with `uv add "argon2-cffi==25.1.*" "itsdangerous==2.2.*"`.
  </how-to-verify>
  <action>
    After approval, run `uv add "argon2-cffi==25.1.*" "itsdangerous==2.2.*"` (per RESEARCH Installation). Confirm `pyproject.toml` lists both under dependencies and `uv.lock` is updated. Do NOT add `passlib` (RESEARCH: unmaintained, avoided). No CDN, no vendored copy needed — these are pure Python/CFFI wheels installed into the uv venv.
  </action>
  <verify>
    <automated>uv run python -c "import argon2, itsdangerous; from argon2 import PasswordHasher; PasswordHasher()"</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` `[project].dependencies` (or the uv dependency table) contains `argon2-cffi` and `itsdangerous` with the pinned version specifiers.
    - `uv run python -c "import argon2, itsdangerous"` exits 0.
    - `passlib` is NOT added.
  </acceptance_criteria>
  <resume-signal>Type "approved" to install, or describe a concern.</resume-signal>
  <done>Both packages install cleanly and import; lockfile updated.</done>
</task>

<task type="auto">
  <name>Task 2: Add persisted secret_key + per-install device_id to config</name>
  <files>app/config.py, app/device_id.py, .env.example</files>
  <read_first>
    - app/config.py (the `Settings(BaseSettings)` class — `model_config = SettingsConfigDict(env_file=".env")`, existing `device_id: str = "device-01"`, `db_path: str = "data/myorishop.db"`).
    - .planning/phases/25-authentication-roles-user-attribution/25-PATTERNS.md § "app/config.py" (add `secret_key`, per-install `device_id`; keep `env_file=".env"`; never print secrets).
    - .planning/phases/25-authentication-roles-user-attribution/25-RESEARCH.md Pitfall 5 (secret_key must be PERSISTED, not regenerated each start) and Pitfall 6 / Assumption A2 (device_id must live OUTSIDE the synced DB).
    - .gitignore (confirm `.env` and `data/` are already ignored — the persisted key/id files live under `data/`).
  </read_first>
  <action>
    Create `app/device_id.py` exposing `get_or_create_local_id(path: str | Path) -> str`: if the file at `path` exists and is non-empty, return its stripped contents; otherwise generate a fresh value via `app.core.new_id()` (UUID4 String(36) — reuse the existing helper), create the parent dir, write it, and return it. Pure stdlib + `app.core.new_id`; no DB access.

    In `app/config.py`: add `secret_key: str` and change `device_id` so it is resolved per-install. Keep the existing `env_file=".env"` behavior so an explicit `SECRET_KEY` / `DEVICE_ID` in `.env` still wins. When not provided by env, resolve each from a persisted local file under the DB's directory: `secret_key` from `<db_path parent>/secret_key`, `device_id` from `<db_path parent>/device_id`, both via `get_or_create_local_id(...)`. Implement the fallback with a pydantic-settings `@model_validator(mode="after")` (or equivalent post-init hook) so: (a) an env-provided value is untouched, (b) the static sentinel `"device-01"` default is REPLACED by the persisted per-install id, (c) an empty `secret_key` is filled from the persisted key file. Result: `settings.secret_key` and `settings.device_id` are stable across restarts and unique per install. Never log or print either value (CLAUDE.md safety).

    Add `.env.example` (tracked) documenting `SECRET_KEY=` and `DEVICE_ID=` as optional overrides with a one-line comment each — the real `.env` remains gitignored. Confirm `data/` (which now holds `secret_key`/`device_id`) is already in `.gitignore` (it is) — no new ignore rule needed.
  </action>
  <verify>
    <automated>uv run python -c "from app.config import Settings; a=Settings(); b=Settings(); assert a.secret_key and a.secret_key==b.secret_key, 'secret_key unstable'; assert a.device_id and a.device_id!='device-01', 'device_id not per-install'; print('OK', a.device_id)"</automated>
  </verify>
  <acceptance_criteria>
    - `app/device_id.py::get_or_create_local_id` returns the same value on a second call with the same path (idempotent persistence).
    - Constructing `Settings()` twice yields the SAME non-empty `secret_key` (persisted, restart-stable — RESEARCH Pitfall 5).
    - `settings.device_id` is a UUID-shaped String(36), not `"device-01"`; a second `Settings()` returns the same id.
    - The persisted files are created under the DB directory (`data/`), which is already gitignored; `.env.example` is tracked and documents `SECRET_KEY`/`DEVICE_ID`.
    - No secret value is printed by config import.
  </acceptance_criteria>
  <done>secret_key and device_id resolve to stable, per-install, persisted values; env overrides still win; nothing secret is logged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → app config | Persisted `secret_key`/`device_id` files are read at startup; a leaked `secret_key` forges sessions |
| package registry → app | New third-party wheels enter the trust base |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01-SC | Tampering | `uv add argon2-cffi itsdangerous` | mitigate | Blocking human-verify checkpoint against pypi.org before install (SUS audit); keep this row (never auto-approve). |
| T-25-01-01 | Information Disclosure | `secret_key` | mitigate | Loaded from `.env`/persisted local file, never hardcoded, never logged; file lives under gitignored `data/` (CLAUDE.md safety). |
| T-25-01-02 | Spoofing | session cookie signing | mitigate | `secret_key` persisted + stable so cookies stay valid across restarts (Pitfall 5) and are not silently rotated to a weak per-start value. |
| T-25-01-03 | Spoofing | per-device identity | mitigate | `device_id` persisted OUTSIDE the DB so a copied `.db` backup cannot clone identity → prevents future per-device `seq` collisions (Pitfall 6 / A2). |
</threat_model>

<verification>
- `uv run python -c "import argon2, itsdangerous"` exits 0.
- `settings.secret_key` stable across two `Settings()` constructions; `settings.device_id` is per-install (not `device-01`).
- `uv run pytest` still green (this plan changes only config defaults + adds deps; existing tests that read `settings.device_id`/`settings.operator_name` remain valid because env/fixture overrides are untouched).
</verification>

<success_criteria>
- argon2-cffi + itsdangerous installed and importable.
- `secret_key` persisted and restart-stable.
- `device_id` per-install unique, stored outside the DB, no longer `"device-01"`.
</success_criteria>

<output>
Create `.planning/phases/25-authentication-roles-user-attribution/25-01-SUMMARY.md` when done.
</output>

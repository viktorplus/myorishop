---
phase: 25-authentication-roles-user-attribution
plan: 09
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - app/templates/base.html
  - tests/test_nav.py
autonomous: true
requirements: [ROLE-03]
must_haves:
  truths:
    - "On /finance/report the «Финансы» nav tab is active (class=\"active\") and the admin «Настройки» tab is NOT active."
    - "On /settings the «Настройки» nav tab is still active (class=\"active\")."
  artifacts:
    - path: "app/templates/base.html"
      provides: "nav active-state: «Финансы» owns the whole /finance subtree; «Настройки» owns only /settings"
      contains: "Финансы"
    - path: "tests/test_nav.py"
      provides: "render assertion that /finance/report activates «Финансы», not «Настройки», and /settings activates «Настройки»"
      contains: "finance/report"
  key_links:
    - from: "app/templates/base.html «Финансы» link"
      to: "the whole /finance subtree (incl. /finance/report)"
      via: 'request.url.path.startswith("/finance")'
      pattern: 'startswith\\("/finance"\\)'
---

<objective>
Fix the nav active-state so the operator-visible finance report (/finance/report) highlights the «Финансы» tab instead of the administrator-only «Настройки» tab.

Purpose: Closes the single cosmetic UAT gap (25-UAT.md § Gaps, test 1). The finance report was confirmed OPERATOR-VISIBLE by product decision during UAT, so it belongs under «Финансы». The current base.html hard-codes it under the admin «Настройки» tab (written under the earlier assumption it was administrator-only). Access control is already correct and is NOT touched — this is a two-line template active-state (CSS class) fix only. No route, gate, or CSV change.
Output: a corrected `app/templates/base.html` nav plus one focused render test in `tests/test_nav.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-authentication-roles-user-attribution/25-UAT.md
@app/templates/base.html
@tests/test_nav.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move /finance/report nav highlight from «Настройки» to «Финансы» + render test</name>
  <files>app/templates/base.html, tests/test_nav.py</files>
  <read_first>
    - app/templates/base.html lines 43-50: the «Финансы» link (line 44) and the admin-gated «Настройки» link (line 49) — these are the two lines to edit. Note ROLE-03: the `{% if current_user and current_user.role == "administrator" %}` wrapper around «Настройки» stays exactly as-is (that menu-hide is unrelated to this fix).
    - tests/test_nav.py in full: the `_seed(session, login=..., role=...)` helper and the `anon_client` + `login(...)` idiom that logs in and asserts on rendered chrome (e.g. `anon_client.get("/history").text`). The new test mirrors this exactly.
    - .planning/phases/25-authentication-roles-user-attribution/25-UAT.md § Gaps (test 1): the diagnosed fix — «Финансы» must own the whole /finance subtree; «Настройки» must own only /settings.
  </read_first>
  <action>
    In app/templates/base.html line 44, drop the ` and not request.url.path.startswith("/finance/report")` clause from the «Финансы» active-state condition so it reads `{% if request.url.path.startswith("/finance") %}` — «Финансы» now stays active for the entire /finance subtree, including /finance/report. In line 49, drop the ` or request.url.path.startswith("/finance/report")` clause from the «Настройки» active-state condition so it reads `{% if request.url.path.startswith("/settings") %}` — «Настройки» is active only for /settings. Do NOT touch the `{% if current_user and current_user.role == "administrator" %}` wrapper, the href values, the link text, any route, any require_role gate, or the CSV endpoint — access is already correct (the report is operator-visible by product decision); this is a CSS-class active-state change only.

    Then add ONE test to tests/test_nav.py that reuses the existing `_seed`/`login` idiom: seed an administrator (so both admin-visible pages render), log in via `login(anon_client, ...)`, then GET `/finance/report` and assert the rendered html contains the exact string `<a href="/finance" class="active">Финансы</a>` AND does NOT contain `<a href="/settings" class="active">Настройки</a>`; and GET `/settings` and assert the rendered html contains `<a href="/settings" class="active">Настройки</a>`. Keep it to this single nav-active-state test — no other assertions.
  </action>
  <verify>
    <automated>uv run pytest tests/test_nav.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - On `/finance/report` (as administrator) the rendered nav contains `<a href="/finance" class="active">Финансы</a>` and does NOT contain `<a href="/settings" class="active">Настройки</a>`.
    - On `/settings` the rendered nav contains `<a href="/settings" class="active">Настройки</a>`.
    - No route, require_role gate, href, link text, or CSV endpoint changed — diff is confined to the two `{% if %}` conditions on lines 44 and 49 (plus the new test).
    - `uv run pytest tests/test_nav.py` exits 0 (the three existing nav tests stay green alongside the new one).
  </acceptance_criteria>
  <done>The finance report activates «Финансы», not «Настройки»; /settings still activates «Настройки»; proven by a passing render test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Template active-state (CSS class) only; no new untrusted input crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-09-01 | Elevation of Privilege | /finance/report access | accept | No access change — the report is operator-visible by product decision (UAT test 1). The authoritative server-side boundaries (`require_role` on the admin routers) are untouched; this edits only the nav highlight, never a gate. |
</threat_model>

<verification>
- `uv run pytest tests/test_nav.py -x` green (existing three nav tests + the new active-state test).
- `git diff app/templates/base.html` shows only the two removed clauses on lines 44 and 49 — no href/route/text change.
</verification>

<success_criteria>
- The operator-visible finance report highlights «Финансы» (not the admin «Настройки» tab) — 25-UAT.md gap (test 1) closed.
- /settings still highlights «Настройки»; no access-control behavior changed.
</success_criteria>

<output>
Create `.planning/phases/25-authentication-roles-user-attribution/25-09-SUMMARY.md` when done.
</output>

---
phase: 16-manual-cash-movements-history
plan: 03
type: execute
wave: 3
depends_on: ["16-01", "16-02"]
files_modified:
  - app/routes/finance.py
  - app/templates/pages/finance.html
  - app/templates/partials/withdraw_form.html
  - app/templates/partials/deposit_form.html
  - app/templates/partials/cash_negative_balance.html
  - app/templates/partials/cash_balance.html
  - app/templates/partials/cash_history_rows.html
  - tests/test_finance.py
autonomous: true
requirements: [FIN-03, FIN-04, FIN-05, FIN-07]

must_haves:
  truths:
    - "Honors D-06 (two inline «Снять деньги» / «Внести деньги» forms below the balance) and D-07 («Тип»-filterable paginated history) on the desktop /finance page; D-04 (mandatory comment) and D-05 (negative-balance warn) enforced via the service"
    - "On /finance the operator can withdraw with a mandatory category + comment and the balance figure drops after submit"
    - "On /finance the operator can deposit (opening balance / correction) with a comment and the balance figure rises"
    - "A withdrawal that would go negative re-renders the снятие form (HTTP 200) with a warning + a «Снять всё равно» confirm control and writes nothing until confirmed"
    - "The /finance page shows a paginated, «Тип»-filterable history of every movement (sale, return, withdrawal, deposit) with date, label, comment, and signed amount"
  artifacts:
    - path: "app/routes/finance.py"
      provides: "POST /finance/withdraw, POST /finance/deposit, GET /finance/history + full-page GET /finance"
      contains: "/finance/withdraw"
    - path: "app/templates/partials/withdraw_form.html"
      provides: "Shared снятие form (finance_base-parameterised) with conditional negative-balance warn"
      contains: "finance_base"
    - path: "app/templates/partials/deposit_form.html"
      provides: "Shared внесение form (finance_base-parameterised)"
      contains: "finance_base"
    - path: "app/templates/partials/cash_history_rows.html"
      provides: "Desktop #cash-history-rows table + «Тип» bucket filter + numbered pagination"
      contains: "cash-history-rows"
    - path: "tests/test_finance.py"
      provides: "Desktop web tests for withdraw/deposit/negative-warn/history"
      contains: "/finance/withdraw"
  key_links:
    - from: "app/routes/finance.py"
      to: "finance.record_manual_movement"
      via: "POST withdraw/deposit call the single write path wrapper"
      pattern: "record_manual_movement\\("
    - from: "app/routes/finance.py GET /finance/history"
      to: "finance.cash_history_view + page_window"
      via: "read + numbered pagination, extra_qs preserves bucket"
      pattern: "cash_history_view\\("
    - from: "app/templates/partials/cash_history_rows.html"
      to: "partials/pagination.html"
      via: "shared numbered pagination include"
      pattern: "partials/pagination.html"
    - from: "app/templates/partials/cash_history_rows.html note/label"
      to: "Jinja autoescape"
      via: "no |safe on note or category label"
      pattern: "mv.note"
---

<objective>
Build the desktop «Финансы» write + read surface: POST `/finance/withdraw` and
POST `/finance/deposit` (thin routes over `record_manual_movement`), GET
`/finance/history` (numbered pagination over `cash_history_view`), and a
full-page GET `/finance` that renders the balance, the two inline forms
(«Снять деньги» / «Внести деньги»), and the history block below them (D-06/D-07a).
Forms are SHARED partials parameterised by a `finance_base` context var (UI-SPEC
Q2) so Plan 04 reuses them on mobile; the negative-balance warn is a conditional
include in the withdraw form (mirrors `writeoff_form` including `writeoff_oversell`).
Desktop history is the numbered `page_window` + `partials/pagination.html` path
(UI-SPEC Q1 — mobile's load-more is Plan 04).

Purpose: Delivers all four ROADMAP success criteria on the desktop surface. All
validation lives in the Plan 02 service (routes stay thin, mirroring
`routes/writeoffs.py`'s three-branch POST + `routes/history.py`'s GET).
Output: `app/routes/finance.py` (POST withdraw/deposit + GET history + full-page
GET), five new templates/partials, the wired `pages/finance.html`, and desktop
web tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-manual-cash-movements-history/16-CONTEXT.md
@.planning/phases/16-manual-cash-movements-history/16-PATTERNS.md
@.planning/phases/16-manual-cash-movements-history/16-UI-SPEC.md
@app/routes/finance.py
</context>

## Artifacts this phase produces (Plan 03 scope)

Newly-created symbols in this plan:
- Route handlers in `app/routes/finance.py`: `POST /finance/withdraw`,
  `POST /finance/deposit`, `GET /finance/history`, and an extended full-page
  `GET /finance` (now passes `finance_base="/finance"`, `balance_cents`, and the
  history context).
- Templates: `partials/withdraw_form.html`, `partials/deposit_form.html`,
  `partials/cash_negative_balance.html`, `partials/cash_balance.html` (id
  `cash-balance`, `oob` flag), `partials/cash_history_rows.html` (id
  `cash-history-rows`).
- Edited `pages/finance.html` (balance partial + two forms + history block).
- Desktop web tests (names begin `test_web_withdraw_` / `test_web_deposit_` /
  `test_web_negative_` / `test_web_cash_history_`) — `tests/test_finance.py`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared withdraw/deposit forms + POST routes on /finance</name>
  <files>app/routes/finance.py, app/templates/partials/withdraw_form.html, app/templates/partials/deposit_form.html, app/templates/partials/cash_negative_balance.html, app/templates/partials/cash_balance.html, app/templates/pages/finance.html, tests/test_finance.py</files>
  <read_first>
    - app/routes/writeoffs.py — `writeoff_create` (:99-189): the three-branch POST (warn 200 → :155-164, errors 422 → :166-176, success 200 → :181-189) and the defensive `except Exception: rollback; log; 422` guard (:135-150) to mirror; `Form("")` string fields (:100-109)
    - app/routes/finance.py — current read-only route (:16-19) to extend; note it imports `compute_balance` ONLY (:11) — ADD `record_manual_movement` (and, in Task 2, `cash_history_view`)
    - app/templates/partials/writeoff_form.html — the `#*-form-wrap` shell (:6), `form.stacked-form` + hx attrs (:17-24), the `<select>` iterating a labels dict (:65-70), the conditional oversell include (:10-12), and the `include_oob_rows` tail (:84-90)
    - app/templates/partials/writeoff_oversell.html — the `.error-block` + `button.danger` confirm + client-dismiss (:6-21) to copy for the negative-balance warn
    - app/templates/pages/finance.html — current balance-only page (:1-5) to extend
    - app/core.py — `format_cents` (`cents` filter); app/routes/mobile_history.py — the HTMLResponse concat pattern (:47-53) for composing a form response with an out-of-band sibling
    - .planning/phases/16-manual-cash-movements-history/16-UI-SPEC.md — §A form field contracts + copywriting; .planning/phases/16-manual-cash-movements-history/16-PATTERNS.md — «Templates» section
  </read_first>
  <behavior>
    - `POST /finance/withdraw` with `amount="15,00"`, `category="withdrawal_supplier"`, `note=""` (HX-Request) → 200; one -1500 cash row persists; response contains a fresh empty снятие form and an out-of-band balance element reflecting the new balance.
    - `POST /finance/withdraw` with blank/`"0"` amount → 422; response contains «Введите сумму больше нуля.»; zero new cash rows.
    - `POST /finance/withdraw` with `category="withdrawal_other"` and blank note → 422; «Укажите комментарий.»; zero writes.
    - `POST /finance/withdraw` that would go negative (balance 0, amount 50,00) without `confirm` → 200; response contains «Баланс уйдёт в минус» and a `hx-vals` `{"confirm":"1"}` «Снять всё равно» control targeting `#withdraw-form-wrap`; zero writes. The same POST with `confirm="1"` → 200 and the row persists.
    - `POST /finance/deposit` with `amount="100"`, `category="deposit_opening"` → 200; one +10000 row; NO negative-balance branch ever fires for deposits.
    - `GET /finance` renders «Снять деньги» and «Внести деньги» forms with category/reason options whose visible text is the RU labels from `CASH_CATEGORIES`.
  </behavior>
  <action>
    Create `partials/cash_balance.html`: `<p id="cash-balance" class="num"{% if oob %} hx-swap-oob="true"{% endif %}><strong>{{ balance_cents | cents }}</strong></p>`
    (keep the existing balance treatment; the `oob` flag lets a POST success refresh it in place).
    Create SHARED `partials/withdraw_form.html` by copying `writeoff_form.html`'s shell: an outer
    `<div id="withdraw-form-wrap">`, a conditional `{% if negative_balance %}{% include "partials/cash_negative_balance.html" %}{% endif %}` block, an `errors.form` block, then
    `<form id="withdraw-form" class="stacked-form" hx-post="{{ finance_base }}/withdraw" hx-target="#withdraw-form-wrap" hx-swap="outerHTML" hx-disabled-elt="find button">` with three `.field`s in UI-SPEC §A order:
    Сумма (`<input type="text" name="amount" inputmode="decimal" placeholder="0,00" value="{{ form.amount or '' }}">` + `errors.amount`),
    Категория (`<select name="category">` with an empty first `<option value="">`, then iterate `CASH_CATEGORIES` and render only keys starting with `withdrawal_` for the снятие form, using the dict value as the RU label — preserve the selected value; + `errors.category`),
    Комментарий (`<input type="text" name="note" value="{{ form.note or '' }}">` — neutral label «Комментарий», NO «(необязательно)» hint per UI-SPEC D-04 resolution; + `errors.note` shown on submit-error only), and a `.form-actions` `<button type="submit">Снять</button>`.
    Create `partials/deposit_form.html` the same way but: `id="deposit-form"`/`#deposit-form-wrap`, `hx-post="{{ finance_base }}/deposit"`, the select is «Основание» over the `deposit_*` keys, submit «Внести», and NO negative-balance include.
    Create `partials/cash_negative_balance.html` by copying `writeoff_oversell.html` retargeted (UI-SPEC §B copy):
    `<div class="error-block" id="withdraw-negative-warning">`, heading «Баланс уйдёт в минус», body «Текущий баланс {{ balance | cents }}, снимаете {{ amount | cents }}.», a `button.danger form="withdraw-form" hx-post="{{ finance_base }}/withdraw" hx-vals='{"confirm": "1"}' hx-target="#withdraw-form-wrap" hx-swap="outerHTML" hx-disabled-elt="this"` labelled «Снять всё равно», and a client-only `button.secondary` «Вернуться к форме» that removes `#withdraw-negative-warning`.
    In `app/routes/finance.py`, import `record_manual_movement`; add `POST /finance/withdraw` (fields `amount=Form("")`, `category=Form("")`, `note=Form("")`, `confirm=Form("")`) that calls `record_manual_movement(session, category=category, amount_raw=amount, note=note, confirm=confirm)` inside the defensive `try/except Exception: rollback; logger.exception; return 422 form` guard, then branches: `result and result.get("negative_balance")` → render `withdraw_form.html` with `negative_balance=result["negative_balance"]` + `finance_base="/finance"` at 200; `errors` → render withdraw form with errors + form-echo at 422; success → compose an HTMLResponse of the fresh withdraw form + an out-of-band `cash_balance.html` (`oob=True`, refreshed `balance_cents=compute_balance(session)`) (mirror mobile_history's concat). Add `POST /finance/deposit` the same way but only TWO branches (errors 422 / success 200 — no negative branch, D-05), rendering `deposit_form.html`; on a blank category surface «Выберите основание.» in `errors.category`. Wire `pages/finance.html`: `<h1>Баланс кассы</h1>` + `{% include "partials/cash_balance.html" %}`, then `<h2>Снять деньги</h2> {% include "partials/withdraw_form.html" %}`, `<h2>Внести деньги</h2> {% include "partials/deposit_form.html" %}` (history block added in Task 2), and ensure the full-page GET /finance context passes `finance_base="/finance"`, `balance_cents`, and empty `form`/`errors` defaults. Add the `<behavior>` web tests to `tests/test_finance.py` using the `client` fixture (POST with `data={...}` + `headers={"HX-Request":"true"}`; assert status_code, response text, and persisted rows via the `session` fixture / `_cash_count`).
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_finance.py -k "web_withdraw or web_deposit or web_negative" -x` exits 0.
    - A valid withdraw POST returns 200 and persists exactly one negative row; a valid deposit POST returns 200 and persists exactly one positive row.
    - A blank/zero amount and a blank mandatory comment each return 422 with the UI-SPEC RU error and zero new rows.
    - The negative-balance POST without confirm returns HTTP 200 (not 422), contains «Баланс уйдёт в минус» and a `confirm` control, and writes nothing; with `confirm="1"` it returns 200 and persists the row.
    - `grep -c "finance_base" app/templates/partials/withdraw_form.html app/templates/partials/deposit_form.html app/templates/partials/cash_negative_balance.html` shows each references `finance_base` (surface-parameterised, no hardcoded `/finance`).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_finance.py -k "web_withdraw or web_deposit or web_negative" -x</automated>
  </verify>
  <done>Desktop withdraw/deposit POST routes persist via the service with correct 200/422/warn branching; the shared forms + negative-balance warn render with finance_base parameterisation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GET /finance/history + desktop history rows partial + page history block</name>
  <files>app/routes/finance.py, app/templates/partials/cash_history_rows.html, app/templates/pages/finance.html, tests/test_finance.py</files>
  <read_first>
    - app/routes/history.py — the whole thin route (:16-62): HX detection (:34), `page_window` (:35), `extra_qs` re-serialisation (:36-45), full-page-vs-partial branch (:60-62)
    - app/services/pagination.py — `page_window` (:9) + the shared `partials/pagination.html` contract
    - app/templates/partials/history_rows.html — the swappable `#history-rows` block: header-row `<select>` filter with hx-get/hx-include/hx-target (:36-47), the empty-state row (:70-75), the row cells + `DICT.get(key, key)` label pattern (:79-114), and the `{% include "partials/pagination.html" %}` tail (:131). NOTE the header comment: autoescape only, NEVER `|safe` (:1-8)
    - app/templates/partials/pagination.html — reused UNCHANGED (ambient list_url/page/total_pages/page_window/rows_target_id/extra_qs)
    - app/routes/finance.py — Task 1's POST handlers (to add the history oob-refresh to the withdraw/deposit SUCCESS branch) + the full-page GET /finance (to add the history context)
    - .planning/phases/16-manual-cash-movements-history/16-UI-SPEC.md — §C history contract (4 columns, buckets, empty-state copy) + §D copywriting
  </read_first>
  <behavior>
    - `GET /finance` (non-HX) renders a `#cash-history-rows` block whose rows show date, RU category/bucket label, comment, and signed amount, and a «Тип» `<select name="bucket">` with «Все типы» + the 4 bucket labels (Продажа/Возврат/Снятие/Внесение).
    - `GET /finance/history?bucket=withdrawal` (HX-Request) returns ONLY the `cash_history_rows.html` partial (not the full page) listing only withdrawal-category rows.
    - With > 20 movements, `GET /finance/history?page=1` returns the second page; pagination links carry `&bucket=…` (extra_qs) so the active filter survives paging.
    - Empty ledger → «Движений пока нет.»; a filter with no matches → «Нет движений по выбранному типу.».
    - `note` is rendered escaped: a movement whose note contains `<script>` renders the literal text, not a live tag (autoescape; no `|safe`).
    - After a successful `POST /finance/withdraw`, the response also carries an out-of-band `#cash-history-rows` refresh showing the new row.
  </behavior>
  <action>
    Create `partials/cash_history_rows.html` by copying `history_rows.html` but SIMPLER: outer
    `<div id="cash-history-rows"{% if oob %} hx-swap-oob="true"{% endif %}>`, a 4-column table
    (Когда / Тип / Комментарий / Сумма — D-07b) with a header-row `<select name="bucket">`
    (`hx-get="{{ finance_base }}/history" hx-trigger="change" hx-include="#cash-history-rows select" hx-target="#cash-history-rows" hx-swap="outerHTML" hx-push-url="true"`) whose options are «Все типы» (empty) then a loop over the `CASH_BUCKET_LABELS` global (mirror the type `<select>` at history_rows.html:39-47). Rows iterate `rows` as bare `CashMovement` objects: `{{ mv.created_at | local_dt }}`, `{{ CASH_CATEGORIES.get(mv.category, mv.category) }}`, `{{ mv.note }}` (autoescape ONLY — never `|safe`; T-05-18), `{{ mv.amount_cents | cents }}` (signed). Empty-state `td colspan=4 class="empty-state muted"`: «Нет движений по выбранному типу.» when a bucket is active else «Движений пока нет.». End with `{% include "partials/pagination.html" %}` UNCHANGED.
    In `app/routes/finance.py`: import `cash_history_view` and `page_window` (+ `urlencode`). Add
    `GET /finance/history` (`bucket: str = ""`, `page: int = 0`) mirroring `history.py`: call
    `cash_history_view(session, bucket=bucket or None, page=page)`, build `pw = page_window(...)`,
    `qs_parts = {"bucket": result["bucket"]}` filtered to truthy, `extra_qs = ("&" + urlencode(qs_parts)) if qs_parts else ""`, context with `rows`, `page`, `total_pages`, `page_window=pw`, `bucket=result["bucket"]`, `list_url="/finance/history"`, `rows_target_id="cash-history-rows"`, `extra_qs`, `finance_base="/finance"`; on HX return `partials/cash_history_rows.html`, else return the full `pages/finance.html` (also passing `balance_cents`, empty `form`/`errors`). Extend the full-page `GET /finance` to build the same history context (bucket default none, page 0) so the page renders the history block. Update the withdraw/deposit SUCCESS branch (Task 1) to append an out-of-band `cash_history_rows.html` (rendered from a fresh `cash_history_view(session)`, `oob=True`) to the composed HTMLResponse, so a successful movement refreshes the history in place. Wire `pages/finance.html`: add `<h2>История движений</h2>` + `{% include "partials/cash_history_rows.html" %}` below the two forms. Add the `<behavior>` web tests to `tests/test_finance.py` (assert full-page vs HX partial, bucket narrowing, `extra_qs` on pagination links, escaped `<script>` note, and the post-write oob refresh).
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_finance.py -k "web_cash_history" -x` exits 0.
    - `GET /finance/history` with an `HX-Request` header returns the rows partial only (no `<html>`/base chrome); without it returns the full page.
    - A `bucket=withdrawal` request lists only withdrawal-category rows; pagination links contain `bucket=withdrawal`.
    - A movement note containing `<script>` renders escaped (the literal string appears; no executable tag) — asserted in a web test; `grep -c "|safe" app/templates/partials/cash_history_rows.html` returns 0.
    - After a successful withdraw POST, the response body contains a `#cash-history-rows` element with `hx-swap-oob` and the new movement.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_finance.py -k "web_cash_history" -x</automated>
  </verify>
  <done>Desktop history renders and HX-filters/paginates over cash_history_view with the numbered bar; the «Тип» filter survives paging via extra_qs; note/labels are autoescaped; successful writes oob-refresh the list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form → POST /finance/withdraw|/deposit | Untrusted amount/category/note/confirm strings; the route only parses `Form("")` and delegates ALL validation to `record_manual_movement` (Plan 02) — routes never write cash (D-00c). |
| stored `note` / category label → HTML history row | Operator-authored `note` is untrusted-at-rest text rendered into the history table; must rely on Jinja autoescape. |
| browser query `bucket`/`page` → GET history | Untrusted filter/page values; bucket flows through the service's `CASH_BUCKETS.get` (Plan 02) and page is clamped server-side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06 | Tampering (stored XSS) | `note` / category label in `cash_history_rows.html` | mitigate | Rendered via Jinja autoescape only — `{{ mv.note }}` / `{{ CASH_CATEGORIES.get(...) }}`, NEVER `|safe` (T-05-18 precedent). Task 2 asserts a `<script>` note renders escaped; grep asserts no `|safe`. |
| T-16-05 | Tampering | negative-balance warn re-render | mitigate | Warn returns HTTP **200** (htmx swaps 200; the `"422":{"swap":true}` config covers only true errors — Pitfall 6). The `confirm=="1"` re-POST re-runs the server-side gate in `record_manual_movement` (balance recomputed live); the route never trusts a client balance. Asserted by the negative web tests. |
| T-16-01 / T-16-02 / T-16-03 / T-16-04 | Tampering / Elevation | amount / category / sign / comment | mitigate (upstream) | Enforced in `record_manual_movement` (Plan 02); the desktop routes surface the resulting 422 errors and never bypass them. Route echoes user input back through autoescaped template values only. |
| T-16-07 | Tampering (SQLi) | bucket/page in GET /finance/history | mitigate (upstream) | Parameterised ORM in `cash_history_view`; unknown bucket ignored, page clamped. Route passes the raw string straight to the service, never into SQL. |
| T-16-SC | Tampering | supply chain (pip installs) | accept (N/A) | Zero packages installed. |
| V2/V3/V4 auth/session/access | — | — | accept (N/A) | Single local operator, no auth in v1 (CLAUDE.md). |
</threat_model>

<verification>
- `uv run pytest tests/test_finance.py -x` — all desktop web tests + prior tests green.
- `uv run ruff check app/routes/finance.py` — clean.
- `grep -c "|safe" app/templates/partials/cash_history_rows.html app/templates/partials/withdraw_form.html app/templates/partials/deposit_form.html` — all 0.
- <human-check> Run the app (`run.bat`), open `http://localhost:8000/finance`: withdraw «Оплата поставщику» 15,00 → balance drops and the row appears in history; try a withdrawal larger than the balance → «Баланс уйдёт в минус» warning with «Снять всё равно»; deposit «Начальный остаток» 100 → balance rises; filter «Тип» → Снятие shows only withdrawals; paging preserves the filter. </human-check>
</verification>

<success_criteria>
- Desktop `/finance` delivers withdraw (mandatory category + comment), deposit, negative-balance warn-but-allow, and a paginated «Тип»-filterable history — all four ROADMAP criteria on the desktop surface.
- Forms are shared + `finance_base`-parameterised (ready for Plan 04 mobile reuse); the warn is a conditional include.
- `note`/labels autoescaped; desktop history uses the numbered pagination bar.
</success_criteria>

<output>
Create `.planning/phases/16-manual-cash-movements-history/16-03-SUMMARY.md` when done.
</output>

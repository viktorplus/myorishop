---
phase: 31
slug: packaging-launcher-signed-release-pipeline
status: approved
nyquist_compliant: true
wave_0_complete: false
created: 2026-07-22
---

# Phase 31 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 9.1.x (already in `pyproject.toml` dev group) |
| **Config file** | `pyproject.toml` `[tool.pytest.ini_options]` (testpaths=`tests`, pythonpath=`.`) |
| **Quick run command** | `uv run pytest -x` |
| **Full suite command** | `uv run pytest` |
| **Estimated runtime** | ~40-60 seconds (full suite; current baseline ~1144 passed / 12 skipped) |

---

## Sampling Rate

- **After every task commit:** Run `uv run pytest -x` (or the task's targeted `-k` selector)
- **After every plan wave:** Run `uv run pytest` (full suite)
- **Before `/gsd-verify-work`:** Full suite must be green + CI release-workflow dry-run produces assets
- **Max feedback latency:** ~60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 31-01-01 | 01 | 0 | PKG-03 / PKG-01 / PKG-02 | T-31-03 | RED-by-design scaffold pins data-sibling + onedir + .iss contracts before impl | unit (scaffold) | `uv run pytest tests/test_packaging.py --co -q` | ✅ | ⬜ pending |
| 31-01-02 | 01 | 0 | PKG-04 | T-31-04 / T-31-05 / T-31-06 | Scaffold pins swap/rollback + path-traversal confinement | unit (scaffold) | `uv run pytest tests/test_launcher.py --co -q` | ✅ | ⬜ pending |
| 31-01-03 | 01 | 0 | PKG-05 | T-31-02 / T-31-03 | Scaffold pins manifest SHA-256 tamper-fail + minisign round-trip (skip-gated) | unit (scaffold) | `uv run pytest tests/test_release_verify.py --co -q` | ✅ | ⬜ pending |
| 31-02-01 | 02 | 1 | PKG-03 | T-31-05 / T-31-06 | All operator data paths root under absolute `MYORISHOP_DATA_DIR`; backup_dir never CWD-relative | unit | `uv run pytest tests/test_packaging.py -k "data_paths or backup_dir or swap_of_app_dir" -x` | ✅ W0 | ⬜ pending |
| 31-02-02 | 02 | 1 | PKG-03 | T-31-06 | Regression gate — dev behavior byte-identical, no CWD-relative backups | integration | `uv run pytest` | ✅ | ⬜ pending |
| 31-03-01 | 03 | 1 | PKG-04 | T-31-04 / T-31-05 / T-31-06 | Pure swap state machine + `parse_pending` rejects `..`/absolute escapes | unit | `uv run pytest tests/test_launcher.py -k "apply_update or parse_pending" -x` | ✅ W0 | ⬜ pending |
| 31-03-02 | 03 | 1 | PKG-04 | T-31-06 | PID-owned start/stop; backup_restore deletes -wal/-shm sidecars | unit | `uv run pytest tests/test_launcher.py -k "backup_restore or adapter or health" -x` | ✅ W0 | ⬜ pending |
| 31-03-03 | 03 | 1 | PKG-04 | T-31-04 | Launcher refuses swap without valid marker; drives one apply cycle | integration | `uv run pytest tests/test_launcher.py -x` | ✅ W0 | ⬜ pending |
| 31-04-01 | 04 | 2 | PKG-01 | T-31-01 / T-31-SC | Onedir `._pth` + vendored wheels + 22 alembic versions; pinned+hashed embeddable | unit | `uv run pytest tests/test_packaging.py -k "onedir_layout or alembic_versions" -x` | ✅ W0 | ⬜ pending |
| 31-04-02 | 04 | 2 | PKG-05 | T-31-03 / T-31-05 | manifest binds version+SHA-256; tamper detected; tag↔`__version__` asserted | unit | `uv run pytest tests/test_release_verify.py -k "manifest or tamper or tag_matches_version" -x` | ✅ W0 | ⬜ pending |
| 31-04-03 | 04 | 2 | PKG-02 | T-31-01 | Generated `.iss` = per-user `%LOCALAPPDATA%` + shortcut + uninstaller; no `data\` shipped | unit | `uv run pytest tests/test_packaging.py::test_generate_iss_is_per_user_with_shortcut_and_uninstaller -x` | ✅ W0 | ⬜ pending |
| 31-05-01 | 05 | 3 | PKG-05 / PKG-02 | T-31-02 | Tag-triggered build + DRAFT release; no repo secrets; minisign secret never in CI | config (YAML lint) | `uv run python -c "import yaml; d=yaml.safe_load(open('.github/workflows/release.yml')); ..."` | ✅ | ⬜ pending |
| 31-05-02 | 05 | 3 | PKG-05 / PKG-02 | T-31-02 | Offline-sign runbook; `.gitignore` blocks secret key; pubkey NOT generated by CI (operator-supplied) | config (guard) | `uv run python -c "... assert '*.key' in gitignore ... pubkey startswith RW if present ..."` | ✅ | ⬜ pending |
| 31-05-03 | 05 | 3 | PKG-05 | T-31-03 / T-31-SC | CI installs minisign so sign→verify round-trip + tamper-fail RUN (not skip) | config (YAML lint) | `uv run python -c "import yaml; d=yaml.safe_load(open('.github/workflows/ci.yml')); assert 'minisign' in ..."` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Wave 0 (Plan 31-01) creates three RED-by-design scaffold test files. Plans 02-05 turn them green by BUILDING the modules; **plans 02-05 must NOT edit these scaffold test files** (Plan 01 owns them).

- [ ] `tests/test_packaging.py` — PKG-03 data-dir-sibling gate + PKG-01 onedir `._pth`/site-packages/22-migration layout + PKG-02 `generate_iss` per-user `.iss` (`test_generate_iss_is_per_user_with_shortcut_and_uninstaller`)
- [ ] `tests/test_launcher.py` — PKG-04 `apply_update` happy-path + migrate-fail/health-fail matched-pair rollback + `parse_pending` path-traversal/marker rejection
- [ ] `tests/test_release_verify.py` — PKG-05 `manifest.txt` SHA-256 correctness/tamper-detect (pure Python, always runs) + tag↔version contract + minisign round-trip (skip-gated on `shutil.which("minisign")`) + vendored-pubkey (skip-gated on `app/minisign.pub` existence — operator-supplied offline)

*Existing infrastructure (`tests/conftest.py`, pytest config in `pyproject.toml`) covers fixtures; no new framework install needed.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Bare-Windows install | PKG-01 | Needs a clean Windows VM with no Python/uv/git — CI can prove the onedir *launches*, not that a *bare* machine has nothing preinstalled | On a clean Windows VM run `MyOriShop-Setup.exe`, launch from the Start-Menu shortcut, confirm the localhost login page opens with the bundled runtime, confirm the uninstaller is registered |
| SmartScreen "Run anyway" + shell UX | PKG-02 | SmartScreen reputation + Start-Menu/uninstaller shell integration are OS-interactive | Run the unsigned installer, clear SmartScreen via «Подробнее → Выполнить в любом случае», verify Start-Menu shortcut + uninstaller in Apps & Features (screenshots) |
| Offline sign round trip | PKG-05 | The signature is produced off-CI by design (secret key never enters GitHub Actions) | On the offline machine sign a real draft's `manifest.txt` with the OFFLINE minisign key, attach `manifest.txt.minisig`, publish; confirm the secret key never left the machine / never appears in any repo/CI log |
| Offline keygen + pubkey delivery | PKG-05 | The production keypair is generated by the human offline (`minisign -G`); CI/dev has no secret key and must never create one (threat T-31-02) | Operator runs `minisign -G` offline, stores `minisign.key` off-repo, commits ONLY `app/minisign.pub` (base64 line starts `RW`); confirm `.gitignore` blocks the secret key. Once present, `test_vendored_pubkey_present_and_bundled` runs green instead of skipping |
| Real pipeline dry run | PKG-05 | Requires a Windows GitHub runner + a pushed tag | Push a throwaway tag `v1.<N>` (with `__version__` bumped to match) and confirm `release.yml` produces archive + installer + `SHA256SUMS` + `manifest.txt` on a DRAFT release |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (`tests/test_packaging.py`, `tests/test_launcher.py`, `tests/test_release_verify.py`)
- [x] No watch-mode flags
- [x] Feedback latency < 60s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-07-22

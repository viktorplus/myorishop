---
phase: 31-packaging-launcher-signed-release-pipeline
plan: 07
type: execute
wave: 2
depends_on: [31-06]
files_modified:
  - launcher/swap.py
  - launcher/__main__.py
  - tests/test_launcher.py
  - app/__init__.py
autonomous: true
gap_closure: true
requirements: [PKG-04]

must_haves:
  truths:
    - "A failed update leaves the install runnable: after a failing apply, app\\ still exists with the previous code and the next launcher tick does not touch it"
    - "The pending marker is consumed on the failure path too — it is quarantined to data\\pending.failed.json instead of being replayed every 2 seconds"
    - "apply_update refuses to begin the cycle when the staged dir is missing — it raises BEFORE stop_app and before any rename, so app\\ is never renamed away with nothing to put back"
    - "Both os.replace renames live inside the rollback-guarded region, and the rollback undoes exactly what happened (no DB restore when no swap and no migration ever ran)"
  artifacts:
    - path: "launcher/swap.py"
      provides: "apply_update with a staged pre-flight guard, both renames inside the guarded region, and a proportional matched-pair rollback"
      exports: ["apply_update", "parse_pending", "Paths", "Pending"]
    - path: "launcher/__main__.py"
      provides: "run_once that quarantines the marker on every failure path; main() watch loop that survives a failed tick"
      exports: ["build_paths", "boot", "run_once", "main"]
    - path: "tests/test_launcher.py"
      provides: "two-tick regression + staged-missing refusal + marker-quarantine + no-DB-restore-without-swap gates"
      contains: "def test_two_ticks_with_one_failing_update_keep_app_dir"
  key_links:
    - from: "launcher/__main__.py run_once"
      to: "data/pending.failed.json"
      via: "os.replace of the marker on any failed apply (quarantine, not replay)"
      pattern: "pending\\.failed\\.json"
    - from: "launcher/swap.py apply_update"
      to: "paths.staged"
      via: "pre-flight existence check before stop_app and before the first rename"
      pattern: "staged"
---

<objective>
Close GAP-3 (blocker): one failed update bricks the installation. Two defects compound.

(a) `launcher/swap.py:86-88` — `stop_app()` and BOTH `os.replace` calls sit OUTSIDE the `try:` that
opens at line 89. Once `app\` has been renamed to `app.prev\`, a missing or already-consumed
`staged\` raises `FileNotFoundError` with no rollback handler in scope.
(b) `launcher/__main__.py:92` — `marker.unlink()` runs only after `apply_update` returns normally, so
a raised exception leaves `data\pending.json` in place and `main()`'s 2-second loop (lines 107-109)
replays the now-unsatisfiable cycle.

Observed end state after a real two-tick UAT run: `app=False, app.prev=True, app.failed=True,
staged=False, marker=True`, `app\python.exe` missing — the application directory no longer exists and
the marker is still there, so the cycle repeats on every relaunch.

Purpose: PKG-04's whole promise is that a failed apply leaves the operator running on the previous
version. Today the FIRST failure rolls back correctly and the SECOND tick destroys the install. This
path is load-bearing for Phase 32 (in-app self-update), which drives exactly this state machine.
Output: a guarded `apply_update`, a marker that is always consumed, a watch loop that survives a bad
tick, and the two-tick regression the UAT asked for.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-packaging-launcher-signed-release-pipeline/31-UAT.md
@launcher/swap.py
@launcher/__main__.py
@tests/test_launcher.py
</context>

<interfaces>
Contracts that MUST NOT change (Phase 32 `app/services/update.py` and the shipped tests depend on them):
- `apply_update(paths, pending, *, stop_app, start_app, migrate, health_ok, backup_restore) -> None`
  — same signature, same happy-path ordering (stop → app→app_prev → staged→app → migrate → start →
  health_ok → rmtree(app_prev)), same re-raise on failure.
- The rollback still moves the staged code aside to `app_failed`, restores `app_prev` → `app`, calls
  `backup_restore(pending.db_backup_path)` and restarts — `tests/test_launcher.py`
  `test_apply_update_rolls_back_on_migrate_failure`, `test_apply_update_rolls_back_on_failed_health_check`
  and `test_apply_rolls_back` (the Phase-32 UPD-04 anchor) assert exactly this and must stay green.
- `run_once(paths, app_process, *, migrate, health_ok, backup_restore) -> bool` — returns True iff a
  swap cycle ran; deletes the marker on success (`test_run_once_applies_app_written_marker`).
- `parse_pending` / `_confine` (swap.py:106-153) are untouched — the T-31-05 confinement stays exactly
  as shipped.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: apply_update — staged pre-flight guard, renames inside the guarded region, proportional rollback</name>
  <files>launcher/swap.py, tests/test_launcher.py</files>
  <read_first>
    - launcher/swap.py:60-104 (`apply_update` — the exact three lines 86-88 that sit outside the `try:` at line 89, and the rollback block at 94-101)
    - .planning/phases/31-packaging-launcher-signed-release-pipeline/31-UAT.md:80-97 (the observed replay that destroys the install; the three required properties)
    - tests/test_launcher.py:39-209 (the three shipped apply_update tests whose assertions must keep passing verbatim)
  </read_first>
  <behavior>
    New tests in tests/test_launcher.py (both RED against today's code):
    - test_apply_update_refuses_when_staged_dir_is_missing: build the fake install layout, delete
      `staged/`, call apply_update; it raises `FileNotFoundError`, `stop_app` was NEVER called, `app/`
      still holds the original "v1" marker, and neither `app.prev/` nor `app.failed/` exists. (Today
      this test leaves `app/` renamed away — the brick.)
    - test_rollback_leaves_db_untouched_when_no_swap_happened: monkeypatch `os.replace` to raise
      `OSError` on its FIRST call (simulating WinError 32 on the app→app.prev rename); apply_update
      re-raises, `backup_restore` was NOT called (nothing was swapped and no migration ran, so rolling
      the DB back would silently discard operator data written since the backup), `app/` still holds
      "v1", and `start_app` ran again so the install is left serving.
    Unchanged expectations (regression guard): the three shipped rollback/happy-path tests still pass
    with identical assertions.
  </behavior>
  <action>
    Rework `apply_update` in launcher/swap.py, keeping the signature and the happy-path ordering:

    1. Pre-flight, BEFORE `stop_app()` and before any rename: if `Path(paths.staged)` does not exist,
       raise `FileNotFoundError` naming the missing dir and stating that the cycle is refused so the
       app dir is never renamed away with nothing to put back. Nothing has been touched at this point,
       so the caller's install is exactly as it was.
    2. Call `stop_app()`, then open the `try:` and move BOTH `os.replace` calls inside it, tracking
       what actually completed with two local flags (app→app_prev done; staged→app done) and a third
       flag set immediately before `migrate()` is invoked.
    3. In the `except Exception:` block, undo exactly what happened, in reverse: `stop_app()`; if the
       staged dir was swapped in, `os.replace(paths.app, paths.app_failed)`; if the previous dir was
       renamed away, `os.replace(paths.app_prev, paths.app)`; call
       `backup_restore(pending.db_backup_path)` ONLY when the migration was actually attempted (a
       failure before that point cannot have changed the schema, and restoring an older DB there would
       destroy data written since the backup was taken); then `start_app()` as a BEST-EFFORT step
       wrapped in its own try/except that swallows a start failure, so the ORIGINAL exception is what
       propagates rather than a secondary one; finally re-raise.
    4. Keep the `else:` branch (`shutil.rmtree(paths.app_prev, ignore_errors=True)`) unchanged.

    Update the docstring to describe the pre-flight guard and the proportional rollback, and extend the
    module docstring's T-31-06 line to say that the rollback is now scoped to the steps that actually
    ran. Stdlib only; no `app.*` import; do not touch `parse_pending`/`_confine`. Cite PKG-04 and
    threats T-31-04/T-31-06.
  </action>
  <verify>
    <automated>uv run pytest tests/test_launcher.py -k "apply_update or apply_rolls_back or rollback" -x</automated>
  </verify>
  <acceptance_criteria>
    The two new tests pass and all three shipped apply_update tests plus the Phase-32
    `test_apply_rolls_back` anchor still pass unchanged. A missing `staged\` raises before `stop_app`
    and before any rename. A failure at the first rename performs no DB restore and leaves `app/`
    intact. `parse_pending` is byte-identical to what shipped.
  </acceptance_criteria>
  <done>All acceptance criteria are satisfied and independently verifiable via the task's automated check (RED-&gt;GREEN on the named selectors).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: run_once quarantines the marker on every failure path; the watch loop survives a bad tick</name>
  <files>launcher/__main__.py, tests/test_launcher.py, app/__init__.py</files>
  <read_first>
    - launcher/__main__.py:50-93 (`run_once` — the marker read, the invalid-marker discard, and line 92 where `marker.unlink()` runs only after a normal return)
    - launcher/__main__.py:101-117 (`main()` after Plan 31-06 — the 2-second watch loop that replays the cycle)
    - tests/test_launcher.py:347-413 (`test_run_once_applies_app_written_marker` — the success-path contract that must stay green: staged content live at app/, marker deleted)
  </read_first>
  <behavior>
    New tests in tests/test_launcher.py (all RED against today's code):
    - test_two_ticks_with_one_failing_update_keep_app_dir (THE UAT-mandated regression): fake install
      layout + a valid hand-written `data/pending.json`; TICK 1 calls `run_once` with a `migrate` that
      raises — it propagates, `app/` is restored to "v1", `app.failed/` holds "v2", `data/pending.json`
      is GONE and `data/pending.failed.json` exists. TICK 2 calls `run_once` again with the same
      arguments — it returns False, raises nothing, `app/` still exists holding "v1", and `app.prev/`
      does not exist. (Today tick 2 renames `app/` away and dies with FileNotFoundError.)
    - test_run_once_refuses_marker_whose_staged_dir_is_gone: a valid marker but `staged/` deleted →
      `run_once` returns False without raising, the marker is quarantined to `pending.failed.json`, and
      `app/` is untouched.
    - test_run_once_quarantines_marker_after_failed_apply: the quarantine overwrite is idempotent —
      running the failing tick twice (re-creating `pending.json` in between) leaves exactly one
      `pending.failed.json` and never raises from the quarantine itself.
    Unchanged: `test_run_once_applies_app_written_marker` still passes (success deletes the marker and
    writes no quarantine file).
  </behavior>
  <action>
    In launcher/__main__.py:

    - Add a module constant for the quarantine name (`pending.failed.json`) next to `_MARKER_NAME`, and
      a small private helper that moves the marker aside with `os.replace` (atomic overwrite of any
      previous quarantine) and never raises out of the caller — a quarantine failure must not mask the
      real error.
    - In `run_once`, after `parse_pending` succeeds, check that `pending.staged_dir` exists; if it does
      not, quarantine the marker and return False WITHOUT calling `apply_update` — this is the
      "refuse to begin the cycle" property, checked at the marker level before any side effect.
    - Wrap the `apply_update(...)` call in `try/except Exception`: on exception, quarantine the marker
      and re-raise (so the caller can log); on success, keep the existing `marker.unlink(missing_ok=True)`
      and `return True`. The unparseable-marker branch keeps today's behavior (discard with `unlink`)
      — do not change it, it is the documented T-31-04 gate.
    - Update the `run_once` docstring: the marker is ALWAYS consumed — deleted on success, moved to
      `pending.failed.json` on any failure — so a failed update can never be replayed by the watch loop.
    - In `main()`, wrap the `run_once(paths, app_process)` call inside the loop in `try/except Exception`
      that prints «Обновление не применено, приложение работает на прежней версии: {exc}» to
      `sys.stderr` and continues the loop. The rollback inside `apply_update` has already restarted the
      app, so the launcher must keep watching rather than exiting and stopping it.
    - Import `os` (needed for the quarantine rename); keep the module stdlib-only.

    Then bump `__version__` in app/__init__.py by one (project convention: every completed-task commit
    bumps the trailing counter, MEMORY "versioning-scheme").
  </action>
  <verify>
    <automated>uv run pytest tests/test_launcher.py -x</automated>
  </verify>
  <acceptance_criteria>
    tests/test_launcher.py is fully green including the two-tick regression. After a failed apply the
    marker is at `data/pending.failed.json`, `data/pending.json` is gone, and a second identical tick
    is a no-op that leaves `app\` intact. The success path still deletes the marker and returns True.
    `main()` survives a failed tick and keeps watching. `app/__init__.py.__version__` bumped by one.
  </acceptance_criteria>
  <done>All acceptance criteria are satisfied and independently verifiable via the task's automated check (RED-&gt;GREEN on the named selectors).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staged\ + pending.json → launcher | Untrusted marker/staged code the launcher acts on; Phase 32 gates staging behind signature + checksum verification |
| launcher → app\ / app.prev\ / app.failed\ | The four-directory rename swap; a partially-applied rename is the brick condition |
| launcher → data\ DB | The matched-pair rollback restores the pre-update DB; restoring it when nothing changed would itself be data loss |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-06 | DoS (data integrity) | half-applied update | mitigate | Both renames now sit inside the guarded region and the rollback undoes exactly the steps that ran; a missing `staged\` is refused BEFORE `stop_app` and before the first rename, so `app\` can never be renamed away with nothing to put back |
| T-31-06b | DoS (data integrity) | DB restore on a no-op failure | mitigate | `backup_restore` runs ONLY when the migration was actually attempted — a pre-swap failure leaves the operator's DB untouched instead of reverting it to an older backup and discarding intervening work |
| T-31-04 | Tampering / Elevation | replayed pending marker | mitigate | The marker is always consumed: deleted on success, moved to `pending.failed.json` on failure, and refused outright when its staged dir is gone — an unsatisfiable marker can no longer be replayed every 2 seconds |
| T-31-05 | Tampering | staged_dir / db_backup_path in marker | mitigate | `parse_pending`'s `..`/absolute/escape confinement (ASVS V12) is unchanged and still runs before the new existence check |
</threat_model>

<verification>
- `uv run pytest tests/test_launcher.py -x` — full PKG-04 gate green, including the two-tick regression.
- `uv run pytest` — full suite green. The 4 pre-existing `tests/test_sync_ui.py` failures are OUT OF
  SCOPE and pre-existing; do not fix or attribute them here.
- `uv run ruff check launcher tests` — clean.
- Deferred to UAT (re-run of Test 2 on a packaged install): inject a failing migration, watch the
  matched-pair rollback, then leave the launcher running for at least two more ticks and confirm
  `app\` is still present and the app still answers.
</verification>

<success_criteria>
- `apply_update` refuses a missing `staged\` before any side effect, and both renames are inside the rollback-guarded region.
- The rollback is proportional: it undoes only the steps that completed, and restores the DB only when a migration was attempted.
- The pending marker is always consumed — quarantined to `data\pending.failed.json` on failure.
- Two consecutive ticks with one failing update leave `app\` present and runnable.
- Every shipped launcher test, including the Phase-32 UPD-04 anchor, still passes unchanged.
</success_criteria>

<output>
Create `.planning/phases/31-packaging-launcher-signed-release-pipeline/31-07-SUMMARY.md` when done.
</output>

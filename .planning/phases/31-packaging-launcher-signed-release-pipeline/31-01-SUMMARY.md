---
phase: 31-packaging-launcher-signed-release-pipeline
plan: 01
subsystem: testing
tags: [packaging, embeddable-python, inno-setup, minisign, launcher, nyquist, red-scaffold]

# Dependency graph
requires:
  - phase: 30-offline-self-uploading-file
    provides: "Nyquist Wave-0 RED-scaffold convention (in-body imports of not-yet-built modules)"
provides:
  - "tests/test_packaging.py — PKG-03 sibling-data gate + PKG-01 onedir layout + PKG-02 per-user .iss assertions (RED)"
  - "tests/test_launcher.py — PKG-04 callback-injected swap/rollback state machine + parse_pending path-confinement (RED)"
  - "tests/test_release_verify.py — PKG-05 manifest sha256/version-contract + minisign roundtrip (RED, skip-gated)"
  - "Executable acceptance contracts Plans 02-05 read from without editing these files"
affects: [31-02 config seam, 31-03 launcher, 31-04 build_release, 31-05 release pipeline, phase-32 self-update]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Nyquist Wave-0 RED scaffold: not-yet-built modules (build_release, launcher.swap, launcher.adapters) imported INSIDE test bodies so collection stays green while execution is RED"
    - "importlib.reload(app.config) under monkeypatched MYORISHOP_DATA_DIR to observe the import-time data-dir seam (Plan 02)"
    - "shutil.which('minisign') skipif mirrors ci.yml's DATABASE_URL-absent PG auto-skip; app/minisign.pub skipif keeps the offline key out of CI/dev"

key-files:
  created:
    - tests/test_packaging.py
    - tests/test_launcher.py
    - tests/test_release_verify.py
  modified:
    - app/__init__.py

key-decisions:
  - "Data-path gates assert BOTH positive containment (under data dir) AND negative (not under app dir) so the swap-safety invariant is genuinely RED before Plan 02, not trivially green"
  - "parse_pending contract pinned to raise ValueError on both traversal and malformed/missing-field markers (json.JSONDecodeError is a ValueError subclass), giving Plan 03 a single clear rejection type"
  - "Sign the small manifest.txt (version + archive sha256), not the archive — verify_manifest re-hashes the archive against the signed manifest (RESEARCH Pattern 5, resolved)"
  - "minisign roundtrip uses a THROWAWAY tmp keypair with -W (unencrypted key, non-interactive); the production app/minisign.pub is never generated by the test (T-31-02)"

patterns-established:
  - "Pattern: build_release API surface pinned by tests — assemble_onedir(embeddable_zip, wheel_dir, dest, repo_root), generate_iss(dist_dir, version, dest), write_manifest(archive_path, version, dest), verify_manifest(archive, manifest), assert_tag_matches_version(tag), VENDORED_APP_ASSETS"
  - "Pattern: launcher API surface pinned by tests — swap.Paths(app, app_prev, staged, app_failed), swap.Pending(staged_dir, expected_version, db_backup_path), swap.apply_update(paths, pending, *, stop_app, start_app, migrate, health_ok, backup_restore), swap.parse_pending(raw_json, install_root), adapters.backup_restore(backup_path, db_path)"

requirements-completed: [PKG-01, PKG-02, PKG-03, PKG-04, PKG-05]

# Metrics
duration: 12min
completed: 2026-07-22
---

# Phase 31 Plan 01: Nyquist Wave-0 Test Scaffold Summary

**Three RED-by-design test files that pin PKG-01..05 (onedir layout, per-user Inno Setup installer, sibling data separation, launcher swap/rollback + path confinement, signed-release manifest+minisign verify) as executable acceptance contracts before any implementation module exists.**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-07-22
- **Completed:** 2026-07-22
- **Tasks:** 3
- **Files modified:** 4 (3 test files created + app/__init__.py version bumps)

## Accomplishments
- `tests/test_packaging.py` (6 tests): PKG-03 sibling-data gate via `importlib.reload(app.config)` under `MYORISHOP_DATA_DIR` (paths-are-siblings, backup_dir-absolute, swap-cannot-reach-data), plus PKG-01 `python313._pth` + 22-migration bundle and PKG-02 per-user `.iss` assertions.
- `tests/test_launcher.py` (6 tests): PKG-04 callback-injected, OS-agnostic `apply_update` happy-path rename ordering, migrate-fail and health-fail matched-pair rollback, `backup_restore` WAL-sidecar delete, and `parse_pending` traversal + malformed-marker rejection (ASVS V12, T-31-04/05/06).
- `tests/test_release_verify.py` (5 tests): PKG-05 pure-Python manifest version+sha256 bind, tamper detection, tag↔version contract (always run), plus skip-gated minisign sign→verify→tamper-fail roundtrip and vendored-pubkey `RW` + bundled check.
- Collection is green (17 new tests collect clean; full suite 1203 collected, 0 errors); execution is RED exactly as designed (15 fail on assertions / in-body imports, 2 skip for absent minisign binary + absent `app/minisign.pub`).

## Task Commits

Each task was committed atomically:

1. **Task 1: RED scaffold PKG-03 + PKG-01/02 (tests/test_packaging.py)** - `bc94dae` (test)
2. **Task 2: RED scaffold PKG-04 launcher swap/rollback (tests/test_launcher.py)** - `10cbe47` (test)
3. **Task 3: RED scaffold PKG-05 manifest + minisign verify (tests/test_release_verify.py)** - `36f81b8` (test)

## Files Created/Modified
- `tests/test_packaging.py` - PKG-03 data-separation gates + PKG-01 onedir + PKG-02 installer assertions (RED)
- `tests/test_launcher.py` - PKG-04 swap/rollback state machine + path-confinement assertions (RED)
- `tests/test_release_verify.py` - PKG-05 manifest sha256/version + minisign roundtrip assertions (RED, skip-gated)
- `app/__init__.py` - version bumped 1.1 → 1.4 (per-task-commit convention)

## Decisions Made
- Strengthened `test_swap_of_app_dir_cannot_reach_data` with a positive "under data dir" assertion so it is genuinely RED before Plan 02 (CWD-relative paths would otherwise pass the negative-only check trivially).
- Pinned `parse_pending` to raise `ValueError` for all rejection cases (traversal + malformed/missing marker), giving Plan 03 one unambiguous rejection contract.
- minisign roundtrip generates a throwaway `-W` (unencrypted) keypair in `tmp_path` so it is fully non-interactive and never touches the offline production key.

## Deviations from Plan

None - plan executed exactly as written. (Version bumps of `app/__init__.py` follow the established project per-task-commit versioning convention, not a plan deviation.)

## Issues Encountered
None. Collection verified green and execution confirmed RED/skip before each commit.

## User Setup Required
None - no external service configuration required. (The production `app/minisign.pub` is supplied OFFLINE by the operator with `minisign -G` in a later plan / Phase 32; its absence keeps the vendored-pubkey test skip-gated by design.)

## Next Phase Readiness
- Plan 02 can implement the `app/config.py` `MYORISHOP_DATA_DIR` seam and turn the three PKG-03 gates green.
- Plans 03/04/05 have their full API surface pinned (see `patterns-established`) — they build `launcher.swap`/`launcher.adapters`, `build_release`, and the release workflow to satisfy these tests without editing them.
- No blockers. minisign binary + `app/minisign.pub` remain absent on the dev box (both skip-gated); the release pipeline plans install/generate them.

## Self-Check: PASSED

- All three test files + SUMMARY.md exist on disk.
- All three task commits (bc94dae, 10cbe47, 36f81b8) present in git history.

---
*Phase: 31-packaging-launcher-signed-release-pipeline*
*Completed: 2026-07-22*

---
phase: 20-warehouses-batch-split-transfers
plan: 06
type: execute
wave: 2
depends_on: ["20-04"]
files_modified: [app/routes/mobile_transfers.py, app/templates/mobile_partials/transfers_step_dest.html, tests/test_mobile_transfers.py]
autonomous: true
requirements: [XFER-01]

must_haves:
  truths:
    - "The mobile transfer wizard's destination radio list includes the source batch's own warehouse (D-09 mirrored on mobile, RESEARCH Pitfall 3)"
    - "The mobile transfer wizard accepts new_expiry/new_comment and threads them into register_transfer, so a same-warehouse split is reachable from a phone, not just desktop (D-05/D-07 mirrored)"
    - "The mobile wizard's post-success confirmation message shows the actual transferred integer quantity, not the raw form string (D-11 parity — confirmed present in mobile's own saved dict, not just desktop's)"
  artifacts:
    - path: "app/routes/mobile_transfers.py"
      provides: "_dest_warehouses with no source-exclusion filter; transfers_create/_render_dest_step with override params"
    - path: "app/templates/mobile_partials/transfers_step_dest.html"
      provides: "override fields after Количество, before .mobile-actions"
  key_links:
    - from: "app/routes/mobile_transfers.py::transfers_create"
      to: "app/services/transfers.py::register_transfer"
      via: "new_expiry=new_expiry, new_comment=new_comment keyword args"
      pattern: "new_expiry=new_expiry"
---

<objective>
Mirror Plan 20-05's desktop D-09/D-05/D-07/D-11 changes onto the mobile transfer wizard
(`app/routes/mobile_transfers.py` + `app/templates/mobile_partials/transfers_step_dest.html`),
per RESEARCH Pitfall 3: mobile has its OWN duplicate `_dest_warehouses()` and calls
`register_transfer` with the identical signature as desktop — if this plan is skipped, the
same-warehouse split ships on desktop but stays unreachable from the mobile wizard.

Purpose: XFER-01 has no "desktop only" qualifier — CONTEXT.md's own research resolved the
open question here: mobile parity is in-scope, not optional.
Output: `/m/transfers` accepts a same-warehouse destination plus override fields end-to-end,
verified via the same three-step wizard flow, and the mobile-only D-11 bug (present in this
file's own `saved` dict, independently of desktop's) is fixed too.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-warehouses-batch-split-transfers/20-CONTEXT.md
@.planning/phases/20-warehouses-batch-split-transfers/20-RESEARCH.md
@.planning/phases/20-warehouses-batch-split-transfers/20-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: mobile_transfers.py — D-09/D-05/D-07/D-11 route wiring</name>
  <files>app/routes/mobile_transfers.py, tests/test_mobile_transfers.py</files>
  <read_first>
    app/routes/mobile_transfers.py (full file, current — `_dest_warehouses` lines 36-40,
    `_render_dest_step` lines 86-115, `transfers_step_dest` lines 168-181, `transfers_create`
    lines 184-259 [the `register_transfer(...)` call at lines 199-207, the D-11 bug in the
    success `saved={"name": ..., "qty": qty}` dict at line 257])
    app/services/transfers.py (post-20-04 state — `register_transfer`'s exact new signature)
    tests/test_mobile_transfers.py lines 127-142, 217-232 (the two CURRENT tests
    `test_transfers_batch_pick_dest_exclu_source_warehouse` and `test_transfers_dest_list_
    excludes_source_even_with_two_warehouses` — both assert the OLD exclusion behavior and
    must be rewritten by this task)
  </read_first>
  <action>
    In `_dest_warehouses(session, source)`: remove the `if w.id != source.warehouse_id` filter,
    identical fix to desktop's Plan 20-05 Task 1 — return `list(active_warehouses(session))`
    once `source` is not `None`. Update the docstring comment (currently claims to mirror D-02's
    exclusion; that claim becomes false).

    Add `new_expiry: str = ""` and `new_comment: str = ""` params to `_render_dest_step`'s
    signature (used to echo values back into the template on a retry — thread through as
    `new_expiry`/`new_comment` context keys, defaulting to `""`). Add the same two `Form("")`
    params to `transfers_step_dest` (POST `/m/transfers/step/dest`) and `transfers_create`
    (POST `/m/transfers`), forwarding them into their `_render_dest_step(...)` calls and, for
    `transfers_create`, into the `register_transfer(...)` call (`new_expiry=new_expiry,
    new_comment=new_comment`).

    Fix the mobile-side D-11 bug: in `transfers_create`'s final success return, change
    `saved={"name": result["product"].name, "qty": qty}` to `saved={"name": result["product"].
    name, "qty": result["qty"]}` — this is a MOBILE-ONLY instance of the same bug pattern
    (confirmed present here independently of the desktop fix in Plan 20-05, per RESEARCH's
    explicit note that this file's own `saved` dict was not yet verified clean).

    Rewrite `test_transfers_batch_pick_dest_exclu_source_warehouse` (rename to
    `test_transfers_batch_pick_dest_includes_source_warehouse`): keep the setup, change the
    assertion from `f'value="{source.warehouse_id}"' not in response.text` to
    `f'value="{source.warehouse_id}"' in response.text` (source warehouse now IS a selectable
    radio option).

    Rewrite `test_transfers_dest_list_excludes_source_even_with_two_warehouses` (rename to
    `test_transfers_dest_list_includes_source_even_with_two_warehouses`): same inversion —
    assert `f'value="{source.warehouse_id}"' in response.text`.

    Add new tests: `test_transfers_create_same_warehouse_with_override_succeeds` (POST
    `/m/transfers` with `dest_warehouse_id` equal to the source batch's own warehouse plus
    `new_expiry` set -> 200, "Перемещение сохранено" in body, a new batch exists in that
    warehouse via `open_batches` holding only the transferred qty), `test_transfers_create_
    same_warehouse_blank_overrides_shows_form_error` (same warehouse, both overrides blank ->
    422, the `SAME_WAREHOUSE_REQUIRES_OVERRIDE_ERROR` text in body, zero new transfer Operation
    rows), `test_transfers_create_qty_saved_matches_parsed_int` (a successful transfer with
    `qty="3"` -> success message shows "3 шт." — regression guard for the mobile-side D-11 fix,
    mirroring desktop's equivalent new test in Plan 20-05).
  </action>
  <acceptance_criteria>
    <test-command>uv run pytest tests/test_mobile_transfers.py -k "includes_source or same_warehouse or qty_saved" -x</test-command>
    <behavior>All 5 new/rewritten tests pass.</behavior>
    <test-command>uv run pytest tests/test_mobile_transfers.py -q</test-command>
    <behavior>Full file green.</behavior>
  </acceptance_criteria>
  <done>Mobile /m/transfers accepts a same-warehouse destination plus override fields
  end-to-end, and its own D-11 qty-echo bug is fixed.</done>
</task>

<task type="auto">
  <name>Task 2: transfers_step_dest.html — override fields (UI-SPEC decision 12)</name>
  <files>app/templates/mobile_partials/transfers_step_dest.html, tests/test_mobile_transfers.py</files>
  <read_first>
    app/templates/mobile_partials/transfers_step_dest.html (full file, current — the
    `<form id="transfer-dest-form">` block, specifically the "Количество" `.field` at lines
    50-54 and `.mobile-actions` at line 56)
    .planning/phases/20-warehouses-batch-split-transfers/20-UI-SPEC.md "Copywriting Contract"
    table rows for the expiry/comment override fields (EXACT label text, must match desktop
    verbatim — this is a locked UI-SPEC decision, not Claude's Discretion) and "Interaction &
    Layout Decisions" item 12 (placement: after Количество, before .mobile-actions; standard
    stacked-form fields, not .mobile-card radio style)
  </read_first>
  <action>
    In `transfers_step_dest.html`, insert two new `.field` blocks between the existing
    "Количество" `.field` (ends at line 54) and the `.mobile-actions` `<div>` (line 56):
    label "Новый срок годности <span class=\"muted\">(если отличается от партии-источника)
    </span>", `<input type="date" id="new_expiry" name="new_expiry" value="{{ new_expiry |
    default('') }}">`; label "Новое состояние или комментарий <span class=\"muted\">(например:
    повреждена упаковка, вскрыт образец)</span>", `<input type="text" id="new_comment"
    name="new_comment" value="{{ new_comment | default('') }}">`. Both fields are optional, no
    `required` attribute, no error-message slot of their own (the D-06 validation error renders
    via the existing `errors.form` block already present at line 21, unchanged).

    Add `test_transfers_step_dest_shows_override_fields` to `tests/test_mobile_transfers.py`:
    POST `/m/transfers/step/dest` with a valid code+batch_id, assert `'name="new_expiry"'` and
    `'name="new_comment"'` both appear in the response, and assert their position is AFTER the
    qty input and BEFORE the "Назад"/"Переместить" buttons (a simple substring-index comparison
    on `response.text`, mirroring the ordering-assertion style already used elsewhere in this
    test file, e.g. `test_web_warehouses_sort_name_desc`'s `.index(...)` comparisons in the
    sibling desktop test file).
  </action>
  <acceptance_criteria>
    <test-command>uv run pytest tests/test_mobile_transfers.py -k override_fields -x</test-command>
    <behavior>New test passes.</behavior>
    <test-command>uv run pytest tests/test_mobile_transfers.py -q</test-command>
    <behavior>Full file green.</behavior>
  </acceptance_criteria>
  <done>Mobile step 3 shows the same two override fields as desktop, correctly placed and
  optional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Operator phone -> `POST /m/transfers`, `POST /m/transfers/step/dest` `dest_warehouse_id` = source's own warehouse | Now a VALID input (D-05/D-09 mirrored), gated by D-06's override-required check inside `register_transfer` (Plan 20-04, unchanged here) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-20-08 | Tampering | `mobile_transfers.py::_pick_batch` | accept | Already correct (RESEARCH confirms this is the reference pattern other files copy FROM, per D-10's scoping) — not touched by this plan, verified unchanged via `read_first` |
</threat_model>

<verification>
`uv run pytest tests/test_mobile_transfers.py -q` green. `uv run pytest -q` (full suite) green.
</verification>

<success_criteria>
- [ ] Mobile destination radio list includes the source warehouse (D-09 mirrored)
- [ ] Mobile wizard accepts and forwards new_expiry/new_comment (D-05/D-07 mirrored)
- [ ] Mobile step 3 template shows both override fields with UI-SPEC-locked copy
- [ ] Mobile's independent D-11 qty-echo bug is fixed
- [ ] tests/test_mobile_transfers.py fully green
</success_criteria>

<output>
Create `.planning/phases/20-warehouses-batch-split-transfers/20-06-SUMMARY.md` when done
</output>

---
phase: 20-warehouses-batch-split-transfers
plan: 03
type: execute
wave: 3
depends_on: ["20-02"]
files_modified: [app/templates/pages/warehouses.html, app/templates/partials/warehouse_rows.html, app/routes/warehouses.py, tests/test_warehouses.py]
autonomous: true
requirements: [WH-01, WH-02, WH-03]

must_haves:
  truths:
    - "Warehouse list shows each warehouse's current item count and last goods-receipt date (WH-01)"
    - "/warehouses is a plain picker table: no inline <input> edit fields, no inline delete button — one 'Изменить' link per active row to the dedicated edit page (D-01)"
    - "The existing filter/sort/status-filter/pagination system (Phase 14, LIST-01..04) still works unchanged on the restructured table (RESEARCH Pitfall 1)"
    - "Soft-deleted rows keep their inline 'Восстановить' action (the one D-01 does not relocate)"
  artifacts:
    - path: "app/templates/partials/warehouse_rows.html"
      provides: "5-column picker table: Название, Адрес, Товаров, Последняя приёмка, Действия"
      contains: "Товаров"
  key_links:
    - from: "app/templates/partials/warehouse_rows.html"
      to: "/warehouses/{{ w.id }}/edit"
      via: "<a href> per active row"
      pattern: "warehouses/\\{\\{ w.id \\}\\}/edit"
---

<objective>
Restructure the `/warehouses` list page from its current inline-editable table into the plain
5-column picker D-01 specifies — adding WH-01's item-count/last-receipt columns (data already
computed by Plan 20-01) — while preserving every bit of Phase 14's filter/sort/status-filter/
pagination chrome (RESEARCH Pitfall 1).

Purpose: closes WH-01 (display) and the list-page half of WH-02/D-01. This plan is purely
ADDITIVE/cosmetic relative to Plan 20-02's route changes (which already redirect-after-POST) —
it does not change any route's request/response contract, so it cannot regress anything Plan
20-02 already made green; it only adds new coverage for the restructured markup.
Output: `warehouses.html` gains a "Добавить склад" page-actions link (mirrors
`customers_list.html`), replacing the inline add form; `warehouse_rows.html` becomes read-only
except for the still-inline Восстановить button on deleted rows.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-warehouses-batch-split-transfers/20-CONTEXT.md
@.planning/phases/20-warehouses-batch-split-transfers/20-RESEARCH.md
@.planning/phases/20-warehouses-batch-split-transfers/20-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure list templates + drop dead route context params</name>
  <files>app/templates/pages/warehouses.html, app/templates/partials/warehouse_rows.html, app/routes/warehouses.py, tests/test_warehouses.py</files>
  <read_first>
    app/templates/pages/warehouses.html (full file, current — the inline add `<form>` this
    task removes)
    app/templates/partials/warehouse_rows.html (full file, current — every line; this task
    rewrites the `<thead>`/`<tbody>` body while keeping the `.filter-bar` sort select and
    `.filter-row` name/address/status inputs byte-identical)
    app/templates/pages/customers_list.html lines 1-8 (the `page-actions` link pattern:
    `<p class="page-actions"><a class="button" href="/customers/new">Добавить
    покупателя</a></p>` — mirror exactly, substituting "Добавить склад" / "/warehouses/new")
    app/routes/warehouses.py `_warehouses_context` (current — confirm which params are STILL
    referenced by `warehouses_page`/`warehouse_restore` post-20-02: only
    name/address/status/sort/page; `warning_id`/`stock_blocked_id`/`stock_blocked_qty`/
    `errors`/`form`/`error_entry_id`/`error_form` are dead after 20-02's route rewrite)
    .planning/phases/20-warehouses-batch-split-transfers/20-UI-SPEC.md "Interaction & Layout
    Decisions" items 1-5 and "Copywriting Contract" table rows for "item count" / "last
    receipt" / empty-state / no-results copy (exact column labels and dash/muted treatment)
  </read_first>
  <action>
    In `app/templates/pages/warehouses.html`: delete the inline `<form hx-post="/warehouses"
    ...>` add-row block entirely; replace with `<p class="page-actions"><a class="button"
    href="/warehouses/new">Добавить склад</a></p>` directly under the `<h1>Склады</h1>` (mirrors
    `customers_list.html`).

    In `app/templates/partials/warehouse_rows.html`: keep the top-of-file `errors`/`form` block
    (lines 4-11) — DELETE it entirely, it only ever applied to the removed inline add form.
    Keep the `.filter-bar` sort `<select>` block UNCHANGED. In the `<thead>` first `<tr>`,
    change the 3 `<th>` (Название/Адрес/Действия) to 5: Название, Адрес, Товаров (num-aligned
    header, no special class needed on `<th>` itself), Последняя приёмка, Действия. In the
    `.filter-row` `<tr>`, keep the name-filter `<th>` and address-filter `<th>` as-is, insert
    TWO blank `<th></th>` cells (no filter control) for Товаров/Последняя приёмка, then keep the
    existing status `<select>` `<th>` last (5 `<th>` total, order matching the header row).
    Rewrite the `<tbody>` row loop: drop the `{% if error_entry_id == w.id and error_form %}`
    branch entirely (dead — error_entry_id no longer produced). Render Название and Адрес as
    plain text (`<td>{{ w.name }}</td>`, `<td>{{ w.address or '' }}</td>` — no `<input>`, no
    `form="edit-{{ w.id }}"` attribute anywhere). Add `<td class="num">{{ w.item_count
    }}</td>` for Товаров. Add a Последняя приёмка `<td>`: `{% if w.last_receipt %}{{
    w.last_receipt | ru_date }}{% else %}<span class="muted">—</span>{% endif %}` (mirrors the
    established null-date guard at `product_rows.html:88`). Действия `<td>`: `{% if not
    w.deleted_at %}<a href="/warehouses/{{ w.id }}/edit">Изменить</a>{% else %}<button
    type="button" class="secondary" hx-post="/warehouses/{{ w.id }}/restore"
    hx-target="#warehouse-rows" hx-swap="outerHTML">Восстановить</button>{% endif %}` — exactly
    one action per row, no Сохранить/Удалить button anywhere. DELETE the two `{% if
    stock_blocked_id == w.id %}`/`{% if warning_id == w.id %}` colspan warning `<tr>` blocks
    entirely (D-02: both states now live only on `warehouse_form.html`, relocated in 20-02).
    Update the empty-state text per UI-SPEC Copywriting Contract: `"Складов пока нет. Добавьте
    первый склад."` with an inline `<a href="/warehouses/new">Добавить склад</a>` link (was
    "...выше" — no longer accurate since the add form is not above this table). Leave the
    no-results-after-filter text ("Ничего не найдено по заданным фильтрам.") unchanged.

    In `app/routes/warehouses.py::_warehouses_context`: remove the now-unused parameters
    `warning_id`, `stock_blocked_id`, `stock_blocked_qty`, `errors`, `form`, `error_entry_id`,
    `error_form` from its signature and from the returned dict (confirm via grep that
    `warehouses_page` and `warehouse_restore` — the only two remaining callers post-20-02 — pass
    none of these before removing).

    Add new tests to `tests/test_warehouses.py`: `test_web_warehouses_page_shows_item_count_
    and_last_receipt_columns` (seed a warehouse + a receipted batch via the `stocked_product`-
    style fixture pattern already used elsewhere in this file, then GET /warehouses, assert
    "Товаров" and "Последняя приёмка" headers present and the row shows a numeric item count and
    a `ru_date`-formatted date, not "—"), `test_web_warehouses_row_action_is_edit_link_not_
    inline_buttons` (GET /warehouses with one active warehouse present, assert
    `'href="/warehouses/{id}/edit"'`-style link present and NO `'name="name"'` `<input>` and NO
    `>Удалить<` button text anywhere in the response for that active row).
  </action>
  <acceptance_criteria>
    <test-command>uv run pytest tests/test_warehouses.py -q</test-command>
    <behavior>Full file green, including the 2 new tests and every pre-existing unmodified test
    (test_web_warehouses_page_renders, test_web_warehouses_status_all_shows_active_and_deleted,
    test_web_warehouses_sort_name_desc, test_web_warehouses_filter_by_name,
    test_web_nav_has_warehouses_link, test_web_quick_deleted_warehouse_hidden_by_default_
    reachable_via_status_filter from Plan 20-02).</behavior>
  </acceptance_criteria>
  <done>The list table has 5 columns, zero inline edit/delete controls, and every Phase 14
  filter/sort/status control still round-trips correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Pitfall 1 regression guard + full-suite confirmation</name>
  <files>tests/test_warehouses.py</files>
  <read_first>
    tests/test_warehouses.py (current, post-Task-1 state — locate the filter/sort/status tests
    already present to model this new regression test's assertion style on)
    app/templates/partials/warehouse_rows.html (post-Task-1 state — confirm exact `.filter-bar`
    and `.filter-row` markup this test asserts against)
  </read_first>
  <action>
    Add `test_web_warehouses_page_preserves_filter_sort_status_bar_after_restructure`: GET
    `/warehouses` and assert `'class="filter-bar"'`, `'name="sort"'`, `'name="name"'` (the
    filter-row's name-filter input, distinct from a row-level edit input which no longer
    exists), `'name="address"'`, and `'name="status"'` are ALL still present in the response —
    a direct regression guard for RESEARCH Pitfall 1 ("D-01's restructure could accidentally
    strip the list-management chrome Phase 14 shipped"). Run the full suite and confirm
    everything the phase has touched so far stays green.
  </action>
  <acceptance_criteria>
    <test-command>uv run pytest tests/test_warehouses.py tests/test_transfers.py -q</test-command>
    <behavior>Green.</behavior>
    <test-command>uv run pytest -q</test-command>
    <behavior>Full suite green (baseline was 41/41 relevant tests passing before this phase per
    RESEARCH.md; this phase's warehouse work should net-add tests, not reduce coverage).</behavior>
  </acceptance_criteria>
  <done>A dedicated regression test proves the filter/sort/status bar survived the restructure;
  full suite confirmed green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Operator browser -> `/warehouses` filter/sort/status query params | Unchanged surface (Phase 14) — this plan only changes what the table's `<td>` cells render, not what query params the route accepts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-20-04 | Tampering (stored XSS) | `w.name`/`w.address` rendered in the new read-only `<td>` cells | accept | Jinja2 autoescape only, `|safe` never used anywhere in this template (unchanged discipline, verified by direct read of the file both before and after this task's edits) |
</threat_model>

<verification>
`uv run pytest tests/test_warehouses.py tests/test_transfers.py -q` and full `uv run pytest -q`
green. Manual: visit `/warehouses`, confirm 5 columns render, filter by name, sort by name,
switch status to "Все склады" and confirm a soft-deleted row still shows "Восстановить".
</verification>

<success_criteria>
- [ ] Warehouse list shows item count + last receipt date (WH-01)
- [ ] List is a plain picker: one "Изменить" link per active row, no inline edit/delete (D-01)
- [ ] Filter/sort/status bar and pagination unchanged (Pitfall 1 regression guard passes)
- [ ] Restore stays the one inline action for deleted rows
- [ ] Full test suite green
</success_criteria>

<output>
Create `.planning/phases/20-warehouses-batch-split-transfers/20-03-SUMMARY.md` when done
</output>

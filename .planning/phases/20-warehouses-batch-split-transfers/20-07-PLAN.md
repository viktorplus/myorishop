---
phase: 20-warehouses-batch-split-transfers
plan: 07
type: execute
wave: 3
depends_on: ["20-05"]
files_modified: [app/templates/partials/transfer_batch_wrap.html, app/templates/partials/transfer_form.html, tests/test_transfers.py]
autonomous: true
requirements: [XFER-01]

must_haves:
  truths:
    - "Desktop /transfers renders the two override fields (expiry, condition/comment) unconditionally once a source batch is selected, not gated on which destination is picked (RESEARCH Pitfall 5 / UI-SPEC decision 9)"
    - "The override fields use the exact UI-SPEC-locked Russian copy, matching the mobile wizard verbatim"
    - "On a 422 re-render, the operator's typed override values survive (echoed back into the inputs), matching the existing code/name/qty echo convention"
  artifacts:
    - path: "app/templates/partials/transfer_batch_wrap.html"
      provides: "override fields rendered once selected_batch_id is truthy"
      contains: "new_expiry"
  key_links:
    - from: "app/templates/partials/transfer_form.html"
      to: "app/templates/partials/transfer_batch_wrap.html"
      via: "new_expiry_value / new_comment_value passed through the existing {% with %} block"
      pattern: "new_expiry_value"
---

<objective>
Give desktop `/transfers` the browser-visible surface for the override fields Plan 20-05's
route already accepts: two new inputs in `transfer_batch_wrap.html` (expiry, condition/comment),
rendered unconditionally once a source batch is picked (Pitfall 5 / UI-SPEC decision 9), with
their typed values threaded through `transfer_form.html`'s existing echo mechanism so a 422
re-render never loses the operator's input.

Purpose: closes the last piece of XFER-01's desktop UI surface. Plans 20-04/20-05 already made
the same-warehouse split fully functional via raw form POST; this plan is what a human operator
actually sees and interacts with in the browser.
Output: `/transfers` shows "Новый срок годности"/"Новое состояние или комментарий" fields right
after the destination-warehouse select, and the D-06 validation error renders in the existing
`errors.form` slot with zero new error-rendering markup (UI-SPEC: explicitly no new markup
needed for this).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-warehouses-batch-split-transfers/20-CONTEXT.md
@.planning/phases/20-warehouses-batch-split-transfers/20-RESEARCH.md
@.planning/phases/20-warehouses-batch-split-transfers/20-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Override field templates (D-06 copy, Pitfall 5, UI-SPEC decision 9)</name>
  <files>app/templates/partials/transfer_batch_wrap.html, app/templates/partials/transfer_form.html</files>
  <read_first>
    app/templates/partials/transfer_batch_wrap.html (full file, current — the `{% set _wh_list
    = warehouses | default([]) %}` block at line 28 and the destination `<select>` at lines
    29-40; this task inserts new markup right after that `<select>`'s closing `</div>`, still
    inside the outer `<div class="field" id="batch-wrap-first">`)
    app/templates/partials/transfer_form.html (full file, current — the `{% with code =
    form.code or "", batches = ..., warehouses = warehouses | default([]) %}` block at lines
    56-63 that includes `transfer_batch_wrap.html`; this task adds two new keys to that `with`)
    app/templates/mobile_partials/transfers_step_dest.html (post-Plan-20-06 state — the exact
    label/span copy for both override fields, to confirm byte-identical text between desktop
    and mobile)
    .planning/phases/20-warehouses-batch-split-transfers/20-UI-SPEC.md "Copywriting Contract"
    table (expiry override field, comment override field, D-06 error message — locked text)
    and "Interaction & Layout Decisions" item 9 (unconditional render once `selected_batch_id`
    is truthy, independent of `_wh_list`)
  </read_first>
  <action>
    In `transfer_batch_wrap.html`: after the existing `{% if selected_batch_id and _wh_list %}
    ... {% endif %}` destination-select block, add a NEW `{% if selected_batch_id %}` block
    (separate condition — UI-SPEC decision 9 requires the override fields to render whenever a
    source batch is picked, independent of whether `_wh_list` is non-empty) containing two
    `.field` divs: (1) label `Новый срок годности <span class="muted">(если отличается от
    партии-источника)</span>`, `<input type="date" id="new_expiry" name="new_expiry"
    value="{{ new_expiry_value | default('') }}">`; (2) label `Новое состояние или комментарий
    <span class="muted">(например: повреждена упаковка, вскрыт образец)</span>`, `<input
    type="text" id="new_comment" name="new_comment" value="{{ new_comment_value |
    default('') }}">`. Neither field is `required`, neither has its own error slot (the D-06
    error renders via `errors.form` in the PARENT template, `transfer_form.html:10`, already
    unconditional).

    In `transfer_form.html`: extend the existing `{% with code = form.code or "", batches =
    [_sb] if _sb else [], selected_batch_id = _sb.id if _sb else none, batch_id_value = _sb.id
    if _sb else "", warehouses = warehouses | default([]) %}` block to also pass
    `new_expiry_value = form.new_expiry or ''` and `new_comment_value = form.new_comment or ''`
    — these read from the `form` dict Plan 20-05 already populates via `form_echo`'s
    `"new_expiry"`/`"new_comment"` keys on a 422/oversell re-render, and default to empty on a
    fresh page load (`form` is `{}` on `GET /transfers`).
  </action>
  <acceptance_criteria>
    <source>`app/templates/partials/transfer_batch_wrap.html` contains `name="new_expiry"` and
    `name="new_comment"` inside a `{% if selected_batch_id %}` block that is SEPARATE from the
    `{% if selected_batch_id and _wh_list %}` destination-select block (grep confirms two
    distinct `{% if %}` conditions, not one combined condition gating both).</source>
  </acceptance_criteria>
  <done>The override fields exist in the template, render whenever a source batch is picked
  (not gated on destination availability), and echo their typed values on error.</done>
</task>

<task type="auto">
  <name>Task 2: Web-level tests for the override UI + D-06 error rendering</name>
  <files>tests/test_transfers.py</files>
  <read_first>
    tests/test_transfers.py (current, post-Plan-20-05 state — the `test_transfer_batch_pick_
    dest_includes_source_warehouse` test Plan 20-05 already added/rewrote, and
    `test_transfer_post_same_warehouse_with_expiry_override_succeeds` /
    `test_transfer_post_same_warehouse_blank_overrides_shows_form_error` from that same plan —
    this task ADDS template-rendering coverage on top of those route-level tests, it does not
    duplicate them)
    app/templates/partials/transfer_batch_wrap.html (post-Task-1 state)
  </read_first>
  <action>
    Add `test_transfer_batch_pick_shows_override_fields`: `GET /transfers/batch-pick` with a
    valid `batch_id`+`code` (mirrors the setup already used by
    `test_transfer_batch_pick_dest_includes_source_warehouse`), assert `'name="new_expiry"'`
    and `'name="new_comment"'` both appear in the response body, and assert the UI-SPEC-locked
    label text "Новый срок годности" and "Новое состояние или комментарий" both appear.

    Add `test_transfer_post_same_warehouse_blank_overrides_error_visible_in_form`: POST
    `/transfers` with `dest_warehouse_id` equal to the source's own warehouse and both override
    fields blank -> 422, assert the FULL `SAME_WAREHOUSE_REQUIRES_OVERRIDE_ERROR` text renders
    inside an `error-block` div in the response (`'class="error-block"'` present alongside the
    error text — this is the template-level proof that `errors.form` correctly reaches
    `transfer_form.html:10`'s existing slot, complementing Plan 20-05's route-level assertion of
    the same error text without checking its rendering container).

    Add `test_transfer_post_422_echoes_typed_override_values`: POST `/transfers` with an
    INVALID quantity (e.g. `qty="abc"`, triggering the existing `QTY_ERROR` validation path) but
    valid `new_expiry`/`new_comment` values and a valid batch pick -> 422, assert the response
    echoes those exact `new_expiry`/`new_comment` values back into the `value="..."` attributes
    of the `new_expiry`/`new_comment` inputs (proves the echo wiring from Task 1 survives a
    validation error, not just a successful round trip).
  </action>
  <acceptance_criteria>
    <test-command>uv run pytest tests/test_transfers.py -k "override_fields or blank_overrides_error_visible or echoes_typed" -x</test-command>
    <behavior>All 3 new tests pass.</behavior>
    <test-command>uv run pytest tests/test_transfers.py -q</test-command>
    <behavior>Full file green.</behavior>
    <test-command>uv run pytest -q</test-command>
    <behavior>Full suite green — this is the final plan touching the transfer surface in this
    phase.</behavior>
  </acceptance_criteria>
  <done>The override fields' presence, copy, and error/echo behavior are all covered by passing
  automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Rendered `new_expiry`/`new_comment` echo values in `transfer_batch_wrap.html` | Operator-typed text rendered back into `value="..."` attributes — Jinja2 autoescape handles HTML-attribute escaping automatically (unchanged discipline, same as every other echoed form field in this template family) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-20-09 | Tampering (stored/reflected XSS) | `new_expiry_value`/`new_comment_value` echoed into `value="..."` attributes | accept | Jinja2 autoescape is ON by default for `.html` templates in this app (confirmed project-wide convention, no `|safe` filter used anywhere in `transfer_form.html`/`transfer_batch_wrap.html`) — attribute-context escaping is automatic and this plan introduces no new escaping mechanism |
</threat_model>

<verification>
`uv run pytest tests/test_transfers.py -q` green. `uv run pytest -q` (full suite) green.
Manual: open `/transfers`, look up a product with an open batch, pick the batch, confirm both
override fields appear immediately; select the batch's own warehouse as destination with both
override fields blank, submit, confirm the error text renders; fill the expiry field, resubmit,
confirm a new batch appears in the same warehouse with the new expiry.
</verification>

<success_criteria>
- [ ] Override fields render unconditionally once a source batch is picked (Pitfall 5)
- [ ] Override field copy matches UI-SPEC and mobile verbatim
- [ ] D-06 error renders in the existing errors.form slot, no new markup
- [ ] Typed override values survive a 422 re-render
- [ ] tests/test_transfers.py and the full suite are green
</success_criteria>

<output>
Create `.planning/phases/20-warehouses-batch-split-transfers/20-07-SUMMARY.md` when done
</output>

---
phase: 20-warehouses-batch-split-transfers
plan: 05
type: execute
wave: 2
depends_on: ["20-04"]
files_modified: [app/routes/transfers.py, app/routes/writeoffs.py, tests/test_transfers.py, tests/test_writeoffs.py]
autonomous: true
requirements: [XFER-01]

must_haves:
  truths:
    - "The destination-warehouse <select> on /transfers now includes the source batch's own warehouse as a selectable option (D-09)"
    - "POST /transfers accepts new_expiry/new_comment fields and threads them into register_transfer, so a same-warehouse split is reachable end-to-end without any new route (D-05)"
    - "A client-submitted batch_id naming another product's batch is never echoed back into the picker on POST /transfers or POST /writeoff's error/exception branches (D-10)"
    - "POST /transfers success message shows the actual transferred integer quantity, not the raw form string (D-11)"
  artifacts:
    - path: "app/routes/transfers.py"
      provides: "_dest_warehouses with no source-exclusion filter; transfers_create with ownership guard + new_expiry/new_comment Form params"
    - path: "app/routes/writeoffs.py"
      provides: "writeoff_create with the ported ownership guard"
  key_links:
    - from: "app/routes/transfers.py::transfers_create"
      to: "app/services/transfers.py::register_transfer"
      via: "new_expiry=new_expiry, new_comment=new_comment keyword args"
      pattern: "new_expiry=new_expiry"
---

<objective>
Wire the desktop `/transfers` route to the service-layer contract Plan 20-04 built: remove the
destination-warehouse source-exclusion filter (D-09), thread the two new override Form fields
through to `register_transfer` (D-05), port the proven batch-ownership guard into
`transfers_create` (D-10, closing the pre-flagged WR-01 debt) and identically into
`writeoffs.py::writeoff_create` (D-10, same debt in a sibling file), and fix the D-11 qty-echo
bug in the success message.

Purpose: D-09/D-10/D-11 are all route-layer fixes on lines this phase's XFER-01 work rewrites
anyway (per CONTEXT.md's debt-closure rationale) — doing them now avoids baking the same
unvalidated pattern into new code paths. This plan does NOT touch any template — it proves the
new route behavior via TestClient form-posts with raw field names, independent of whether the
rendered `<input>` elements exist yet (Plan 20-07 adds those).
Output: `/transfers` accepts `new_expiry`/`new_comment` and a same-warehouse destination;
`writeoff_create` no longer echoes a foreign batch.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-warehouses-batch-split-transfers/20-CONTEXT.md
@.planning/phases/20-warehouses-batch-split-transfers/20-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: transfers.py — D-09/D-10/D-11 + override Form params</name>
  <files>app/routes/transfers.py, tests/test_transfers.py</files>
  <read_first>
    app/routes/transfers.py (full file, current state — `_dest_warehouses` lines 26-30,
    `transfers_batch_pick` lines 78-107 [the CORRECT ownership-check pattern, lines 92-96],
    `transfers_create` lines 110-195 [the buggy `selected_batch` resolution at line 130, the
    `register_transfer(...)` call at lines 132-140, the qty-echo bug at line 190])
    app/services/transfers.py (post-20-04 state — confirm `register_transfer`'s exact new
    signature: `new_expiry: str = ""`, `new_comment: str = ""` positioned before `confirm`, and
    that the success dict now carries `"qty"`)
    tests/test_transfers.py lines 341-353 (current `test_transfer_batch_pick_dest_excludes_
    source` — this task rewrites its core assertion)
  </read_first>
  <action>
    In `_dest_warehouses(session, source)`: remove the `if w.id != source.warehouse_id` filter
    — return `list(active_warehouses(session))` unconditionally once `source` is not `None`
    (still return `[]` when `source is None`, unchanged). Update its docstring comment (it
    currently says "Active warehouses minus the source batch's own warehouse (D-02)" — that
    line is now false).

    In `transfers_create`: add two new `Form("")` params, `new_expiry: str = Form("")` and
    `new_comment: str = Form("")`, positioned after `dest_warehouse_id` and before `confirm`.
    Add both to `form_echo` dict (`"new_expiry": new_expiry, "new_comment": new_comment`) so a
    422/oversell re-render can echo the operator's typed override values back (Task 1 of Plan
    20-07 will consume these via the template — this task only needs them present in the
    context dict passed to `partials/transfer_form.html`, which already forwards `form` as a
    whole). Pass `new_expiry=new_expiry, new_comment=new_comment` into the `register_transfer(
    ...)` call (all three response branches: try block, oversell, errors — actually only the
    try block calls `register_transfer`, so this is one call site).

    Replace line 130's `selected_batch = session.get(Batch, batch_id.strip()) if
    batch_id.strip() else None` with the SAME 4-line ownership-check pattern already proven in
    `transfers_batch_pick` (lines 92-96 of this same file): first resolve `product` via the
    identical `select(Product).where(Product.code == code.strip(), Product.deleted_at.is_(
    None)).first()` lookup already used in `transfers_batch_pick`/`transfers_lookup`, then only
    accept `batch_id` as `selected_batch` if `candidate.product_id == product.id`. This
    resolution must happen BEFORE the `try:` block (it is used in all three response branches:
    exception, oversell, and validation-error), matching where `selected_batch` is currently
    computed.

    Fix the D-11 qty echo: in the success-response context (currently `"saved": {"name":
    result["product"].name, "qty": qty}` near the end of the function), change `"qty": qty` to
    `"qty": result["qty"]` (the parsed integer `register_transfer` now returns per Plan 20-04).

    Rewrite `test_transfer_batch_pick_dest_excludes_source` (rename to
    `test_transfer_batch_pick_dest_includes_source_warehouse`): same setup (source batch +
    second warehouse), but assert `f'value="{source.warehouse_id}"' in response.text` (D-09:
    the source warehouse IS now a selectable destination option) alongside the existing
    `f'value="{dest_wh.id}"' in response.text` assertion.

    Add new tests: `test_transfer_create_ownership_guard_does_not_echo_foreign_batch` (mirror
    `test_reject_tampered_ids`'s foreign-batch setup, but POST to `/transfers` with the foreign
    `batch_id` and a mismatched `code`/product so `register_transfer` itself rejects it with a
    `{"batch": BATCH_REQUIRED_ERROR}` error; assert the 422 response's picker does NOT echo the
    foreign batch's identifying data — e.g. assert the foreign batch's `id` does not appear as
    a `checked`/`selected` value in the response), `test_transfer_post_success_qty_echo_matches_
    parsed_int` (POST a valid transfer with `qty="3"`, assert the success message contains
    "3 шт." — same visible text as before, but now sourced from `result["qty"]`, added mainly
    as a locked-in regression guard for the source-code change), `test_transfer_post_same_
    warehouse_with_expiry_override_succeeds` (POST `/transfers` with `dest_warehouse_id` equal
    to the source batch's OWN warehouse plus `new_expiry` set to a future date and blank
    `new_comment` -> 200, "Перемещение сохранено" in body, then via `session`/`open_batches`
    confirm a NEW batch exists in that same warehouse holding only the transferred qty with the
    new expiry — this is the end-to-end route-level proof of D-05/D-09/D-07 working together),
    `test_transfer_post_same_warehouse_blank_overrides_shows_form_error` (POST `/transfers` with
    `dest_warehouse_id` equal to the source's own warehouse and both override fields blank ->
    422, the exact `SAME_WAREHOUSE_REQUIRES_OVERRIDE_ERROR` text appears in the response body,
    zero new `transfer`-type Operation rows written).
  </action>
  <acceptance_criteria>
    <test-command>uv run pytest tests/test_transfers.py -k "dest_includes_source or ownership or qty_echo or same_warehouse" -x</test-command>
    <behavior>All 5 new/rewritten tests pass.</behavior>
    <test-command>uv run pytest tests/test_transfers.py -q</test-command>
    <behavior>Full file green.</behavior>
  </acceptance_criteria>
  <done>Desktop /transfers accepts a same-warehouse destination plus override fields end-to-end,
  never leaks a foreign batch into the picker, and echoes the real transferred quantity.</done>
</task>

<task type="auto">
  <name>Task 2: writeoffs.py — D-10 ownership guard port</name>
  <files>app/routes/writeoffs.py, tests/test_writeoffs.py</files>
  <read_first>
    app/routes/writeoffs.py (full file, current — `writeoff_batch_pick` lines 70-96 [the
    CORRECT existing ownership-check pattern, lines 84-88] and `writeoff_create` lines 99-189
    [the buggy `selected_batch` resolution at line 123])
    tests/test_writeoffs.py lines 1-60 (existing fixtures/imports/style — `_writeoff_ops`,
    `_only_batch`, `_two_batch_product` helpers already defined in this file)
  </read_first>
  <action>
    In `writeoff_create`, replace line 123's `selected_batch = session.get(Batch,
    batch_id.strip()) if batch_id.strip() else None` with the same ownership-check pattern
    already proven in this file's own `writeoff_batch_pick` (lines 84-88): resolve `product` via
    the same `select(Product).where(Product.code == code.strip(), Product.deleted_at.is_(
    None)).first()` lookup used there, then only accept `batch_id` as `selected_batch` if
    `candidate.product_id == product.id`. This resolution must happen before the `try:` block
    (used in all three response branches). D-11 (qty echo) does NOT apply to this file —
    `writeoff_create`'s success echo already computes the real qty from `-result["operation"].
    qty_delta` (confirmed correct, do not change it).

    Add `test_writeoff_create_ownership_guard_does_not_echo_foreign_batch` to
    `tests/test_writeoffs.py`, mirroring the equivalent new test in `tests/test_transfers.py`
    from Task 1: create two products (use the `product`/`stocked_product` fixtures from
    `tests/conftest.py`, or this file's own `_two_batch_product` helper adapted), POST
    `/writeoff` with a `batch_id` belonging to a DIFFERENT product than the submitted `code`,
    assert the resulting 422/error response does not echo that foreign batch's data into the
    picker.
  </action>
  <acceptance_criteria>
    <test-command>uv run pytest tests/test_writeoffs.py -k ownership -x</test-command>
    <behavior>New test passes.</behavior>
    <test-command>uv run pytest tests/test_writeoffs.py -q</test-command>
    <behavior>Full file green — this is a pure security-hardening addition, no existing
    writeoff test asserts on a foreign batch being echoed (RESEARCH confirms no such test
    exists today).</behavior>
  </acceptance_criteria>
  <done>writeoff_create never echoes a foreign product's batch data on an error re-render.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Operator browser -> `POST /transfers`, `POST /writeoff` `batch_id` field | Client-submitted, untrusted — must be re-validated for product ownership before being echoed into any response, per the ALREADY-established pattern in each file's own GET batch-pick endpoint |
| Operator browser -> `POST /transfers` `dest_warehouse_id` = source's own warehouse | Now a VALID input (D-05/D-09), not tampering — the meaningful gate moved from "reject same warehouse" to "reject same warehouse WITHOUT an override" (D-06, enforced in the Plan 20-04 service layer, unchanged here) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-20-01 | Information Disclosure / Tampering | `transfers_create`'s `selected_batch` resolution (line ~130) | mitigate | Port the proven 4-line ownership guard from `transfers_batch_pick` (this same file, lines 92-96) — reject `candidate.product_id != product.id` before assigning `selected_batch`, closing the pre-flagged WR-01 debt (STATE.md, 2026-07-13) |
| T-20-07 | Information Disclosure / Tampering | `writeoff_create`'s `selected_batch` resolution (line 123) | mitigate | Port the identical guard from `writeoff_batch_pick` (this same file, lines 84-88) — same fix pattern, sibling file, closing the other half of the WR-01 debt |
</threat_model>

<verification>
`uv run pytest tests/test_transfers.py tests/test_writeoffs.py -q` green. `uv run pytest -q`
(full suite) green.
</verification>

<success_criteria>
- [ ] _dest_warehouses no longer excludes the source warehouse (D-09)
- [ ] POST /transfers accepts and forwards new_expiry/new_comment (D-05)
- [ ] transfers_create and writeoff_create never echo a foreign batch (D-10, both files)
- [ ] POST /transfers success message uses the real transferred int qty (D-11)
- [ ] Same-warehouse split reachable end-to-end via raw form POST (proves D-05/D-06/D-07/D-09 wired correctly, ahead of the template UI in Plan 20-07)
</success_criteria>

<output>
Create `.planning/phases/20-warehouses-batch-split-transfers/20-05-SUMMARY.md` when done
</output>

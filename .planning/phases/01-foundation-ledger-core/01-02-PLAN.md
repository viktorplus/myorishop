---
phase: 01-foundation-ledger-core
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - app/config.py
  - app/core.py
  - app/db.py
  - app/models.py
  - alembic.ini
  - alembic/env.py
  - alembic/script.py.mako
  - alembic/versions/0001_initial_schema.py
autonomous: true
requirements: [FND-01, FND-02, FND-03]

must_haves:
  truths:
    - "products and operations tables exist via Alembic migration 0001 with UUID4 TEXT PKs, integer _cents money, UTC ISO-8601 TEXT timestamps"
    - "UPDATE or DELETE on operations fails at the DATABASE level with an 'append-only' error (triggers, not app discipline)"
    - "Every pooled connection has journal_mode=WAL, foreign_keys=1, busy_timeout=5000"
    - "A demo product row exists in data/myorishop.db so the walking skeleton has something to correct"
  artifacts:
    - path: "app/config.py"
      provides: "pydantic-settings Settings (db_path, operator_name, device_id, display_tz) + module-level settings"
      contains: "BaseSettings"
    - path: "app/core.py"
      provides: "new_id, utcnow_iso, to_cents, format_cents, iso_to_local helpers"
      exports: ["new_id", "utcnow_iso", "to_cents", "format_cents", "iso_to_local"]
    - path: "app/db.py"
      provides: "build_engine with PRAGMA listener (autocommit gotcha handled), engine, SessionLocal, get_session, APPEND_ONLY_TRIGGERS"
      contains: "listens_for"
    - path: "app/models.py"
      provides: "Base with naming convention, Product, Operation, OPERATION_TYPES"
      contains: "UniqueConstraint"
    - path: "alembic/versions/0001_initial_schema.py"
      provides: "schema source of truth: tables + append-only triggers + demo product seed"
      contains: "RAISE(ABORT"
  key_links:
    - from: "alembic/env.py"
      to: "app/models.py"
      via: "target_metadata = Base.metadata + render_as_batch"
      pattern: "render_as_batch=True"
    - from: "alembic/versions/0001_initial_schema.py"
      to: "app/db.py"
      via: "imports APPEND_ONLY_TRIGGERS (single source for trigger DDL, shared with test fixtures)"
      pattern: "APPEND_ONLY_TRIGGERS"
    - from: "app/db.py"
      to: "app/config.py"
      via: "module engine built from settings.db_path"
      pattern: "settings\\.db_path"
---

## Phase Goal

**As a** shop operator, **I want to** start MyOriShop locally with run.bat and record a stock change in the browser as an immutable ledger entry, **so that** stock counts stay provably correct and no schema decision blocks future sync.

<objective>
Implement the sync-ready data foundation exactly as locked in CONTEXT.md: settings (D-17), convention helpers (D-05/D-06/D-07), SQLite engine with per-connection PRAGMAs (D-14), SQLAlchemy 2.0 models (D-05–D-10, D-12), and Alembic migration #1 (D-13) that creates the schema, installs DB-level append-only triggers on `operations`, and seeds one demo product.

Purpose: every later phase writes through this schema; FND-01/02/03 are decided here, at the database level. After this plan, `tests/test_pragmas.py` is GREEN and `tests/test_ledger.py` remains the only RED contract (service arrives in Plan 01-03).
Output: importable app core modules + applied migration producing data/myorishop.db.

Decision IDs referenced below are defined in the Decision Traceability table of 01-01-PLAN.md (D-01…D-17 from 01-CONTEXT.md). This plan implements D-05, D-06, D-07, D-08, D-09 (schema half), D-10, D-12 (sync engine/Session), D-13, D-14, D-17 (settings half).
</objective>

<execution_context>
@C:/Users/Admin/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-ledger-core/01-CONTEXT.md
@.planning/phases/01-foundation-ledger-core/01-RESEARCH.md
@.planning/phases/01-foundation-ledger-core/01-01-SUMMARY.md
@tests/conftest.py
@tests/test_pragmas.py
@tests/test_ledger.py
</context>

## Artifacts this phase produces (this plan's share)

- **app/config.py:** class `Settings(BaseSettings)` with fields `db_path: str = "data/myorishop.db"`, `operator_name: str = "operator"`, `device_id: str = "device-01"`, `display_tz: str = "Europe/Moscow"`; `model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8")`; module-level `settings = Settings()`
- **app/core.py:** `new_id() -> str`, `utcnow_iso() -> str`, `to_cents(value: str) -> int`, `format_cents(cents: int) -> str`, `iso_to_local(iso_str: str, tz_name: str) -> str`
- **app/db.py:** `APPEND_ONLY_TRIGGERS: tuple[str, str]`, `build_engine(db_path: str) -> Engine`, module `engine`, `SessionLocal`, `get_session()` generator dependency
- **app/models.py:** `NAMING_CONVENTION` dict, `Base(DeclarativeBase)`, `OPERATION_TYPES = ("receipt", "sale", "writeoff", "return", "correction")`, `Product`, `Operation`
- **alembic/:** env.py wired to Base.metadata with `render_as_batch=True`; migration `0001_initial_schema` (tables + triggers `operations_no_update`/`operations_no_delete` + demo product seed `DEMO_PRODUCT_ID = "00000000-0000-4000-8000-000000000001"`)
- **data/myorishop.db** (gitignored) with `alembic_version` at head

<tasks>

<task type="auto">
  <name>Task 1: Implement app core — config, helpers, engine with PRAGMA listener, models</name>
  <files>app/config.py, app/core.py, app/db.py, app/models.py</files>
  <read_first>
    - tests/conftest.py and tests/test_ledger.py (the contract: exact symbol names and assertion semantics these modules must satisfy)
    - .planning/phases/01-foundation-ledger-core/01-RESEARCH.md (Code Examples 1, 2, 4; Pattern 3; Pitfalls 1–4)
    - .planning/phases/01-foundation-ledger-core/01-CONTEXT.md (Data Model Conventions — column set is LOCKED)
  </read_first>
  <action>
    app/config.py (D-17): pydantic-settings `Settings(BaseSettings)` with the four fields and defaults listed in "Artifacts this plan's share" above. No secrets; `.env` optional override (encoding utf-8). Module-level singleton `settings`.

    app/core.py (D-05/D-06/D-07): `new_id()` returns `str(uuid.uuid4())`; `utcnow_iso()` returns `datetime.now(timezone.utc).isoformat(timespec="seconds")` (e.g. "2026-07-08T12:00:00+00:00" — sorts lexicographically = chronologically); `to_cents(value)` strips whitespace, replaces "," with "." (Russian locale input, per Don't Hand-Roll table), parses with `decimal.Decimal`, quantizes to 2 places, returns int cents, raises ValueError on garbage (never float — Pitfall 3); `format_cents(cents)` renders "1250" → "12,50" (comma separator for Russian UI); `iso_to_local(iso_str, tz_name)` parses `datetime.fromisoformat`, converts via `zoneinfo.ZoneInfo(tz_name)`, formats "%d.%m.%Y %H:%M" (D-07 display half).

    app/db.py (D-12/D-14): `build_engine(db_path)` — `Path(db_path).parent.mkdir(parents=True, exist_ok=True)`, then `create_engine(f"sqlite:///{db_path}")` (sync driver, NO aiosqlite); attach `@event.listens_for(engine, "connect")` listener executing `PRAGMA journal_mode=WAL`, `PRAGMA foreign_keys=ON`, `PRAGMA busy_timeout=5000` — with the autocommit save/set-True/restore dance from RESEARCH Code Example 1 (Pitfall 2: sqlite3 silently ignores foreign_keys inside an open transaction). Module level: `engine = build_engine(settings.db_path)`, `SessionLocal = sessionmaker(bind=engine)`, `get_session()` plain generator yielding a session from `with SessionLocal() as session`. Also define `APPEND_ONLY_TRIGGERS: tuple[str, str]` — the two CREATE TRIGGER statements `operations_no_update` (BEFORE UPDATE) and `operations_no_delete` (BEFORE DELETE) on `operations`, body `SELECT RAISE(ABORT, 'operations ledger is append-only')` (single source of trigger DDL — imported by migration 0001 AND tests/conftest.py; never duplicate the SQL).

    app/models.py (D-05/D-06/D-07/D-08/D-10): SQLAlchemy 2.0 style ONLY (`DeclarativeBase`, `Mapped[]`, `mapped_column()` — no legacy `declarative_base()`/`Column`). `NAMING_CONVENTION` exactly per RESEARCH Code Example 2 (Pitfall 4 — unnamed constraints dead-end future batch migrations); `Base` with `metadata = MetaData(naming_convention=NAMING_CONVENTION)`. `OPERATION_TYPES = ("receipt", "sale", "writeoff", "return", "correction")` module constant (Phase 1 uses only "correction").
    - `Product` (__tablename__ "products"): `id: Mapped[str] = mapped_column(String(36), primary_key=True, default=new_id)`; `code: Mapped[str | None] = mapped_column(String(20))`; `name: Mapped[str] = mapped_column(String(200))`; `quantity: Mapped[int] = mapped_column(Integer, nullable=False, default=0)` (cached projection, D-09); `created_at: Mapped[str] = mapped_column(String(32), default=utcnow_iso)`; `updated_at: Mapped[str] = mapped_column(String(32), default=utcnow_iso, onupdate=utcnow_iso)`; `deleted_at: Mapped[str | None] = mapped_column(String(32))` (D-10 soft delete). NO price columns yet — Phase 2 adds them via migration.
    - `Operation` (__tablename__ "operations", D-08 column set EXACTLY): per RESEARCH Code Example 4 — `id` String(36) PK; `type` String(20) NOT NULL; `product_id` ForeignKey("products.id") NOT NULL, `index=True`; `qty_delta` Integer NOT NULL (signed); `unit_cost_cents` / `unit_price_cents` Integer nullable (D-06 — Integer, NEVER Numeric/Float: test iterates all metadata columns); `payload` JSON nullable; `device_id` String(36) NOT NULL; `seq` Integer NOT NULL; `created_at` String(32) NOT NULL (D-07 — String, not DateTime: SQLite DateTime drops tz offset); `created_by` String(100) NOT NULL (FND-03); `synced_at` String(32) nullable (v2 sync cursor); `__table_args__ = (UniqueConstraint("device_id", "seq"),)` (Pitfall 6 guard).
    All code and comments in English.
  </action>
  <acceptance_criteria>
    - `uv run python -c "from app.core import to_cents, format_cents; assert to_cents('12,50') == 1250; assert to_cents('7') == 700; assert format_cents(1250) == '12,50'"` exits 0
    - `uv run python -c "from app.core import new_id, utcnow_iso; import uuid, datetime; assert uuid.UUID(new_id()).version == 4; d = datetime.datetime.fromisoformat(utcnow_iso()); assert d.utcoffset() == datetime.timedelta(0)"` exits 0
    - `uv run python -c "from app.db import build_engine, get_session, APPEND_ONLY_TRIGGERS; assert len(APPEND_ONLY_TRIGGERS) == 2 and all('RAISE(ABORT' in t for t in APPEND_ONLY_TRIGGERS)"` exits 0
    - `uv run python -c "from app.models import Base, Product, Operation, OPERATION_TYPES; import sqlalchemy as sa; cols = [c for t in Base.metadata.tables.values() for c in t.columns]; assert not any(isinstance(c.type, sa.Numeric) for c in cols)"` exits 0 (sa.Float subclasses sa.Numeric — covers both)
    - grep of app/models.py finds zero matches for `Numeric|Float|declarative_base|autoincrement`
    - `uv run python -c "from app.core import to_cents; err=0
try: to_cents('abc')
except ValueError: err=1
assert err==1"` — invalid money input raises ValueError
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "from app.config import settings; from app.core import new_id, utcnow_iso, to_cents, format_cents, iso_to_local; from app.db import build_engine, get_session, APPEND_ONLY_TRIGGERS; from app.models import Base, Product, Operation, OPERATION_TYPES; import sqlalchemy as sa; assert to_cents('12,50')==1250 and format_cents(1250)=='12,50'; assert len(APPEND_ONLY_TRIGGERS)==2; cols=[c for t in Base.metadata.tables.values() for c in t.columns]; assert not any(isinstance(c.type, sa.Numeric) for c in cols); assert all(isinstance(c.type, sa.Integer) for c in cols if c.name.endswith('_cents')); print('core OK')"</automated>
  </verify>
  <done>All four modules import cleanly; conventions provably match FND-02 (uuid4/36-char, integer cents, UTC ISO TEXT); trigger DDL lives in exactly one place (app.db.APPEND_ONLY_TRIGGERS).</done>
</task>

<task type="auto">
  <name>Task 2: Alembic setup + migration 0001 (schema, append-only triggers, demo seed) and apply</name>
  <files>alembic.ini, alembic/env.py, alembic/script.py.mako, alembic/versions/0001_initial_schema.py</files>
  <read_first>
    - app/models.py and app/db.py as written in Task 1 (column types in the migration must match the models byte-for-byte in semantics)
    - .planning/phases/01-foundation-ledger-core/01-RESEARCH.md (Code Example 3, Pattern 2 including the synced_at trigger caveat, Pitfall 4)
  </read_first>
  <action>
    1. `uv run alembic init alembic` (D-13). Edit alembic/env.py: `from app.models import Base`; `from app.config import settings`; `target_metadata = Base.metadata`; force URL from settings via `config.set_main_option("sqlalchemy.url", f"sqlite:///{settings.db_path}")`; add `render_as_batch=True` to BOTH offline and online `context.configure(...)` calls. Leave alembic.ini's `sqlalchemy.url` as a placeholder (env.py overrides it).
    2. `uv run alembic revision -m "initial schema"` then rename/author the file as `0001_initial_schema.py` (set `revision = "0001"`). Hand-write upgrade() — do NOT rely on autogenerate:
       - `op.create_table("products", ...)` and `op.create_table("operations", ...)` mirroring app/models.py exactly: sa.String(36) PKs, sa.Integer for qty/quantity and all *_cents, sa.JSON payload, sa.String(32) timestamps, sa.String(100) created_by, ForeignKey products.id on operations.product_id, UniqueConstraint("device_id", "seq") named per the naming convention, index on operations.product_id.
       - `from app.db import APPEND_ONLY_TRIGGERS` and `for stmt in APPEND_ONLY_TRIGGERS: op.execute(stmt)` (Pattern 2 — DB-level immutability; v1 blocks ALL updates including synced_at; v2 sync milestone relaxes with a WHEN clause in a new migration).
       - Seed demo product (walking-skeleton content, RESEARCH Open Question Q3 recommendation): `DEMO_PRODUCT_ID = "00000000-0000-4000-8000-000000000001"` (valid uuid4-shaped literal), code "DEMO-001", name "Демо-товар", quantity 0, created_at/updated_at via `app.core.utcnow_iso()` — use sa.table/sa.column + op.bulk_insert.
       - Migration docstring MUST record: "Batch (move-and-copy) migrations that recreate the operations table DROP its triggers — any future batch migration touching operations must re-execute APPEND_ONLY_TRIGGERS" (Alembic batch caveat, cited in RESEARCH).
       - downgrade(): drop both triggers, drop operations, drop products.
    3. Apply: `uv run alembic upgrade head` → creates data/myorishop.db (data/ is gitignored).
    4. Confirm Wave-0 progress: `uv run pytest tests/test_pragmas.py -q` must now be GREEN (fixtures import app.db/app.models which now exist). tests/test_ledger.py stays RED until Plan 01-03 (app.services.ledger missing) — expected, do not touch it.
  </action>
  <acceptance_criteria>
    - `uv run alembic upgrade head` exits 0; re-running it is a no-op (idempotent at head)
    - sqlite_master of data/myorishop.db contains triggers `operations_no_update` AND `operations_no_delete`, tables `products`, `operations`, `alembic_version`
    - `SELECT COUNT(*) FROM products` == 1 (demo product seeded, name "Демо-товар")
    - A raw `UPDATE operations`-style write is impossible: executing `UPDATE products SET id=id` succeeds but `INSERT` + `UPDATE` probe on operations raises with message containing "append-only" (probe in a throwaway copy or via transaction rollback)
    - `uv run pytest tests/test_pragmas.py -q` exits 0 (GREEN)
    - grep alembic/env.py finds `render_as_batch=True` (2 occurrences: offline + online)
  </acceptance_criteria>
  <verify>
    <automated>uv run alembic upgrade head && uv run python -c "import sqlite3; con = sqlite3.connect('data/myorishop.db'); trg = {r[0] for r in con.execute(\"SELECT name FROM sqlite_master WHERE type='trigger'\")}; assert {'operations_no_update','operations_no_delete'} <= trg, trg; n = con.execute('SELECT COUNT(*) FROM products').fetchone()[0]; assert n == 1, n; print('migration OK')" && uv run pytest tests/test_pragmas.py -q</automated>
  </verify>
  <done>Migration 0001 is the schema source of truth (no create_all outside test fixtures); operations is immutable at DB level; demo product exists; PRAGMA tests GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application code → SQLite file | all writes cross here; ledger integrity is the phase's core asset |
| future code paths → operations table | later phases (or bugs) could try to mutate history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-03 | Tampering / Repudiation | operations table (audit trail, FND-01/03) | mitigate | BEFORE UPDATE/DELETE triggers with RAISE(ABORT) created in migration 0001 — immutability enforced in the DB, not by convention; single trigger-DDL source in app.db |
| T-1-06 | Tampering | money/time correctness (FND-02) | mitigate | Integer _cents columns only (test iterates metadata rejecting Numeric/Float); UTC ISO TEXT via one helper; Decimal-based to_cents rejects garbage input |
| T-1-07 | Denial of Service | SQLite locking under pooled connections | mitigate | WAL + busy_timeout=5000 set per-connection via listener (autocommit gotcha handled per official SQLAlchemy docs); single uvicorn worker |
| T-1-SC | Tampering | pip/uv installs | mitigate | no new packages in this plan; uv.lock from 01-01 remains authoritative |
</threat_model>

<verification>
- `uv run pytest tests/test_pragmas.py -q` GREEN; full `uv run pytest -q` still RED only on test_ledger.py + test_smoke.py (missing service/UI — closed by Plan 01-03)
- data/myorishop.db inspectable: UUID TEXT PKs, Integer _cents, TEXT UTC timestamps, triggers present, 1 seeded product (ROADMAP Success Criterion 4)
</verification>

<success_criteria>
- FND-02 conventions locked in schema + helpers (D-05/D-06/D-07)
- FND-01 append-only enforcement live at DB level (D-08), stock projection column in place (D-09)
- Alembic (D-13) with render_as_batch + naming convention operational; migration applied on the real DB
</success_criteria>

<output>
Create `.planning/phases/01-foundation-ledger-core/01-02-SUMMARY.md` when done.
</output>

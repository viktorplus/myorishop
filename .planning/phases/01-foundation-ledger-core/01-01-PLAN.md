---
phase: 01-foundation-ledger-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - .python-version
  - .gitignore
  - .env.example
  - app/__init__.py
  - app/static/htmx.min.js
  - tests/conftest.py
  - tests/test_pragmas.py
  - tests/test_ledger.py
  - tests/test_smoke.py
autonomous: true
requirements: [FND-01, FND-02, FND-03]

must_haves:
  truths:
    - "uv-managed project imports fastapi, sqlalchemy, alembic, jinja2 under Python 3.13 (or documented 3.12.13 fallback)"
    - "htmx 2.0.10 is vendored at app/static/htmx.min.js — no CDN reference anywhere"
    - "Full pytest run exits non-zero (RED): Wave-0 tests define the FND-01/02/03 contract before any implementation exists"
  artifacts:
    - path: "pyproject.toml"
      provides: "uv project with pinned runtime + dev deps, pytest and ruff config"
      contains: "[tool.pytest.ini_options]"
    - path: "app/static/htmx.min.js"
      provides: "vendored htmx 2.0.10 (offline requirement)"
      min_lines: 1
    - path: "tests/conftest.py"
      provides: "tmp-path SQLite engine/session/client fixtures + demo product seed"
      contains: "build_engine"
    - path: "tests/test_ledger.py"
      provides: "FND-01/02/03 executable contract"
      contains: "record_operation"
    - path: "tests/test_pragmas.py"
      provides: "WAL/foreign_keys/busy_timeout assertions on live pooled connection"
      contains: "journal_mode"
    - path: "tests/test_smoke.py"
      provides: "end-to-end happy path: GET / and POST /ops"
      contains: "/ops"
  key_links:
    - from: "tests/conftest.py"
      to: "app/db.py"
      via: "imports build_engine + APPEND_ONLY_TRIGGERS (interface contract for Plan 01-02)"
      pattern: "from app\\.db import"
    - from: "tests/test_ledger.py"
      to: "app/services/ledger.py"
      via: "imports record_operation/compute_stock/rebuild_stock (interface contract for Plan 01-03)"
      pattern: "from app\\.services\\.ledger import"
---

## Phase Goal

**As a** shop operator, **I want to** start MyOriShop locally with run.bat and record a stock change in the browser as an immutable ledger entry, **so that** stock counts stay provably correct and no schema decision blocks future sync.

*(Story assembled verbatim from ROADMAP.md Phase 1 Goal + Success Criteria 1–4; ROADMAP Goal line is outcome prose, not canonical story format — flagged to orchestrator.)*

<objective>
Scaffold the uv-managed Python project (walking-skeleton prerequisite) and write the FAILING Wave-0 test suite that encodes FND-01 (append-only ledger + recomputable stock), FND-02 (integer cents / UTC ISO-8601 TEXT / UUID keys), and FND-03 (created_by + created_at audit trail) as executable contracts BEFORE any implementation exists.

Purpose: MVP/Walking-Skeleton rule — the first deliverable after scaffold is a failing end-to-end test. These tests are the interface contract that Plans 01-02 and 01-03 implement against (no scavenger hunt: module paths, function signatures, and fixture shapes are fixed here).
Output: runnable uv project (deps resolved, ruff + pytest configured), vendored htmx 2.0.10, and 4 RED test files.

### Decision Traceability (from 01-CONTEXT.md)

| ID | Locked decision |
|----|-----------------|
| D-01 | Python 3.13 via uv; deps in pyproject.toml |
| D-02 | run.bat → uvicorn single worker 127.0.0.1:8000 + browser open |
| D-03 | htmx 2.0.10 vendored in app/static/ (offline, no CDN) |
| D-04 | No PyInstaller/Docker; plain `uv run` via run.bat |
| D-05 | UUID4 TEXT primary keys on ALL tables |
| D-06 | Money as integer minor units; columns end `_cents` |
| D-07 | Timestamps UTC ISO-8601 TEXT; configurable display timezone |
| D-08 | `operations` append-only with exact column set (id, type, product_id, qty_delta, unit_cost_cents, unit_price_cents, payload, device_id, seq, created_at, created_by, synced_at) |
| D-09 | Stock = cached projection on product, recomputable from ledger; no direct edits |
| D-10 | Soft deletes (`deleted_at`); no hard deletes |
| D-11 | Thin routes / fat services: app/routes/, app/services/, app/models.py, app/templates/, app/static/ |
| D-12 | Sync SQLAlchemy 2.0 Session, plain `def` endpoints |
| D-13 | Alembic from day one, render_as_batch=True |
| D-14 | SQLite PRAGMAs per connection via event listener (WAL, foreign_keys=ON, busy_timeout) |
| D-15 | Jinja2 server-rendered + HTMX partials + python-multipart |
| D-16 | pytest for tests, Ruff for lint |
| D-17 | Operator name from settings recorded as created_by; no auth in v1 |

This plan implements D-01, D-03, D-04, D-15 (dependency set), D-16, and encodes D-05–D-09, D-14, D-17 as test assertions.
</objective>

<execution_context>
@C:/Users/Admin/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-ledger-core/01-CONTEXT.md
@.planning/phases/01-foundation-ledger-core/01-RESEARCH.md
</context>

## Artifacts this phase produces (full phase inventory)

Plans 01-01 → 01-03 together create these symbols; this plan creates the ones marked (01-01):

- **Files (01-01):** `pyproject.toml`, `uv.lock`, `.python-version`, `.gitignore`, `.env.example`, `app/__init__.py`, `app/static/htmx.min.js`, `tests/conftest.py`, `tests/test_pragmas.py`, `tests/test_ledger.py`, `tests/test_smoke.py`
- **Test fixtures (01-01):** `engine` (tmp-path SQLite w/ PRAGMA listener + triggers), `session`, `product` (seeded demo product), `client` (TestClient with `get_session` override)
- **Contracted symbols (implemented by 01-02/01-03, imported by these tests):** `app.config.settings` (fields `db_path`, `operator_name`, `device_id`, `display_tz`); `app.core.new_id() -> str`, `utcnow_iso() -> str`, `to_cents(value: str) -> int`, `format_cents(cents: int) -> str`, `iso_to_local(iso_str: str, tz_name: str) -> str`; `app.db.build_engine(db_path: str)`, `app.db.get_session()`, `app.db.APPEND_ONLY_TRIGGERS: tuple[str, str]`; `app.models.Base`, `Product`, `Operation`, `OPERATION_TYPES`; `app.services.ledger.record_operation(session, *, type_, product_id, qty_delta, unit_cost_cents=None, unit_price_cents=None, payload=None) -> Operation`, `next_seq(session, device_id) -> int`, `compute_stock(session, product_id) -> int`, `rebuild_stock(session) -> None`, `ledger_view(session) -> dict`; `app.main.app` (FastAPI); routes `GET /`, `POST /ops`; CLI entry `run.bat`

<tasks>

<task type="auto">
  <name>Task 1: Scaffold uv project, pin dependencies, vendor htmx 2.0.10</name>
  <files>pyproject.toml, uv.lock, .python-version, .gitignore, .env.example, app/__init__.py, app/static/htmx.min.js</files>
  <read_first>
    - .planning/phases/01-foundation-ledger-core/01-RESEARCH.md (sections "Standard Stack", "Package Legitimacy Audit", "Environment Availability")
    - .planning/phases/01-foundation-ledger-core/01-CONTEXT.md (Runtime & Startup decisions)
    - E:\dev\myorishop\.gitignore if it exists (extend, do not overwrite)
  </read_first>
  <action>
    Work at repo root E:\dev\myorishop (git repo ALREADY exists with commits — do NOT run git init).

    1. Runtime (D-01): run `uv python install 3.13` (one-time internet). If it fails offline, fall back to uv-managed Python 3.12.13 (already present) and record the deviation in the SUMMARY. Then `uv init --bare --python 3.13` (or `--python 3.12` on fallback) in-place at repo root — `--bare` creates only pyproject.toml, no sample main.py. Set project name `myorishop`, `requires-python = ">=3.13"` (fallback: ">=3.12").
    2. Dependencies (exact canonical names, all registry-verified in 01-RESEARCH.md Package Legitimacy Audit on 2026-07-08 — the SUS verdicts there are documented seam false-positives; uv.lock pins hashes as supply-chain mitigation T-1-SC):
       `uv add "fastapi==0.139.*" "uvicorn[standard]==0.51.*" "sqlalchemy==2.0.*" "alembic==1.18.*" "jinja2==3.1.*" "python-multipart==0.0.32" "pydantic-settings==2.14.*"`
       `uv add --dev "pytest==9.1.*" "httpx==0.28.*" "ruff==0.15.*"` (D-16)
    3. pyproject.toml additions: `[tool.pytest.ini_options]` with `testpaths = ["tests"]`; `[tool.ruff]` with `line-length = 100`, `target-version = "py313"` (fallback "py312"); `[tool.ruff.lint]` with `select = ["E", "F", "I", "UP", "B"]`.
    4. .gitignore — create or extend with: `.venv/`, `__pycache__/`, `*.pyc`, `.pytest_cache/`, `.ruff_cache/`, `data/`, `*.db`, `*.db-wal`, `*.db-shm`, `.env`.
    5. .env.example (committed; no secrets exist in v1, threat T-1-05): keys `DB_PATH=data/myorishop.db`, `OPERATOR_NAME=operator`, `DEVICE_ID=device-01`, `DISPLAY_TZ=Europe/Moscow`, each with an English comment. UTF-8 encoding.
    6. Create package dirs with `app/__init__.py` (empty) and empty dirs `app/static/`, `tests/`.
    7. Vendor htmx (D-03, offline requirement — never a CDN script tag): `curl.exe -L -o app/static/htmx.min.js https://unpkg.com/htmx.org@2.0.10/dist/htmx.min.js` (fallback URL: https://cdn.jsdelivr.net/npm/htmx.org@2.0.10/dist/htmx.min.js). Do NOT download any 4.x beta.
    8. No Docker/PyInstaller anywhere (D-04).
  </action>
  <acceptance_criteria>
    - `uv run python -c "import fastapi, sqlalchemy, alembic, jinja2, pydantic_settings, python_multipart"` exits 0
    - `uv run python -c "import sys; assert sys.version_info[:2] >= (3, 12), sys.version"` exits 0 (3.13 preferred; 3.12 only as documented fallback)
    - app/static/htmx.min.js exists, size > 20000 bytes, content contains the string "htmx"
    - pyproject.toml contains `[tool.pytest.ini_options]` and `[tool.ruff]`
    - .gitignore contains `data/` and `.env`
    - `git status` shows uv.lock created (hash-pinned deps committed later with the plan commit)
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "import fastapi, sqlalchemy, alembic, jinja2, pydantic_settings, python_multipart; import os; s=os.path.getsize('app/static/htmx.min.js'); assert s > 20000, s; print('scaffold OK')"</automated>
  </verify>
  <done>uv project resolves and imports the full locked stack; htmx 2.0.10 vendored locally; ruff + pytest configured; no git init performed.</done>
</task>

<task type="auto">
  <name>Task 2: Write the FAILING Wave-0 test suite (FND-01/02/03 contract + e2e happy path)</name>
  <files>tests/conftest.py, tests/test_pragmas.py, tests/test_ledger.py, tests/test_smoke.py</files>
  <read_first>
    - .planning/phases/01-foundation-ledger-core/01-RESEARCH.md (Code Examples 1–5, "Validation Architecture", Pitfall 7 — tmp-path file DB, never :memory:)
    - .planning/phases/01-foundation-ledger-core/01-VALIDATION.md (Wave 0 requirements, Per-Task Verification Map)
    - The "Artifacts this phase produces" section above — these tests must import EXACTLY those symbols
  </read_first>
  <action>
    Write four test files that import the contracted-but-not-yet-existing app modules. The whole suite MUST be RED (import/collection errors are the expected failure mode). This is the MVP-mode failing end-to-end test task; scaffold had to precede it only so pytest itself can run.

    tests/conftest.py — fixtures (Pitfall 7: file-based tmp_path SQLite, NOT :memory:):
    - `engine(tmp_path)`: `from app.db import build_engine, APPEND_ONLY_TRIGGERS`; engine = build_engine(str(tmp_path / "test.db")); create schema via `Base.metadata.create_all(engine)` (test-fixture-only exception to the Alembic rule); then execute both APPEND_ONLY_TRIGGERS statements on a connection (exec_driver_sql + commit).
    - `session(engine)`: sqlalchemy.orm.sessionmaker bound to engine; yield inside context manager.
    - `product(session)`: seed one Product via `from app.core import new_id` — Product(id=new_id(), code="TEST-001", name="Тестовый товар", quantity=0); commit; return it.
    - `client(engine, session, product)`: IMPORT `app.main` LAZILY INSIDE the fixture body (app/main.py arrives only in Plan 01-03 — lazy import keeps test_ledger/test_pragmas collectable in Wave 2): `from app.main import app; from app.db import get_session`; override `app.dependency_overrides[get_session]` with a generator function yielding the test session; yield `fastapi.testclient.TestClient(app)`; clear overrides on teardown.

    tests/test_pragmas.py (D-14, infra): using a pooled connection from the `engine` fixture, assert `PRAGMA journal_mode` == "wal", `PRAGMA foreign_keys` == 1, `PRAGMA busy_timeout` == 5000 (use connection.exec_driver_sql(...).scalar()).

    tests/test_ledger.py — imports `from app.services.ledger import record_operation, next_seq, compute_stock, rebuild_stock`, `from app.config import settings`, `from app.models import Base, Operation, Product`; tests:
    - test_record_operation_appends_and_updates_projection (FND-01, D-09): record correction +5 then -2 via `record_operation(session, type_="correction", product_id=product.id, qty_delta=...)`; assert product.quantity == 3, compute_stock(session, product.id) == 3, and exactly 2 operations rows exist.
    - test_operations_update_is_rejected (FND-01, D-08): `session.execute(text("UPDATE operations SET qty_delta = 99"))` inside `pytest.raises((OperationalError, IntegrityError))` and assert "append-only" appears in the exception string; rollback after.
    - test_operations_delete_is_rejected (FND-01, D-08): same for `DELETE FROM operations`.
    - test_rebuild_stock_repairs_tampered_cache (FND-01, D-09): after recording ops, set product.quantity = 999 directly and commit; call rebuild_stock(session); assert product.quantity == compute_stock(session, product.id).
    - test_conventions_uuid_cents_utc (FND-02, D-05/D-06/D-07): op = record_operation(...); assert len(op.id) == 36 and `uuid.UUID(op.id).version == 4`; assert `datetime.fromisoformat(op.created_at).utcoffset() == timedelta(0)`; iterate ALL columns of Base.metadata.tables and assert no column type isinstance of (sqlalchemy.Numeric, sqlalchemy.Float) — Pitfall 3 guard; assert every column whose name ends with `_cents` is sqlalchemy.Integer; assert both tables' PK column type is String with length 36.
    - test_audit_trail (FND-03, D-17): op.created_by == settings.operator_name and op.created_at is a non-empty string; two sequential ops get seq 1 and 2 via next_seq for settings.device_id.
    - test_seq_unique_per_device (D-08, Pitfall 6): manually add an Operation duplicating (device_id, seq) of an existing row → commit raises IntegrityError; rollback.

    tests/test_smoke.py — end-to-end happy path (walking-skeleton contract, D-15):
    - test_home_page_renders(client, product): GET "/" → 200; response.text contains "/static/htmx.min.js" and "Тестовый товар".
    - test_post_ops_records_correction(client, session, product): POST "/ops" with form data {"product_id": product.id, "qty_delta": "3"} → 200; response.text contains settings.operator_name (who — FND-03) and ">3<" or "3" as the updated stock; session.expire_all() then product.quantity == 3.

    All test code/comments in English; only Russian literals are UI-facing strings under test ("Тестовый товар"). Save files as UTF-8.
  </action>
  <acceptance_criteria>
    - All four test files exist under tests/ and are syntactically valid Python (`uv run python -m py_compile tests/*.py` exits 0 — compile check only, no imports executed)
    - `uv run pytest -q` exits NON-zero (RED) because app.db / app.services.ledger / app.main do not exist yet
    - tests/test_ledger.py contains the exact import line `from app.services.ledger import record_operation, next_seq, compute_stock, rebuild_stock`
    - tests/conftest.py contains `build_engine` and `APPEND_ONLY_TRIGGERS` and does NOT use ":memory:"
    - grep finds "append-only" assertion in tests/test_ledger.py
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m py_compile tests/conftest.py tests/test_pragmas.py tests/test_ledger.py tests/test_smoke.py && ! uv run pytest -q && echo "RED as expected"</automated>
  </verify>
  <done>Wave-0 suite exists, compiles, and fails as a whole (imports unresolved) — the executable contract for Plans 01-02 and 01-03 is locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI/unpkg → local machine | third-party code enters the project (installs, vendored JS) |
| filesystem → git | secrets/config files could leak into history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-SC | Tampering | uv installs (pypi), htmx download | mitigate | exact canonical package names from RESEARCH.md legitimacy audit (all 10 registry-verified 2026-07-08; SUS verdicts are documented seam false-positives — PyPI exposes no download counts to the seam); uv.lock hash-pins every dependency; htmx fetched only from unpkg/jsdelivr at the pinned 2.0.10 version |
| T-1-05 | Information Disclosure | .env / git history | mitigate | .env gitignored from the first commit; committed .env.example carries no secrets (none exist in v1) |
</threat_model>

<verification>
- `uv run python -c "import fastapi, sqlalchemy, alembic, jinja2, pydantic_settings, python_multipart"` → 0
- app/static/htmx.min.js > 20 KB and contains "htmx"
- `uv run pytest -q` → non-zero exit, 4 test files present (RED contract state)
- `git status` clean of .env / data/ (gitignore effective)
</verification>

<success_criteria>
- Locked stack installed at pinned versions under uv-managed Python 3.13 (or documented 3.12 fallback), D-01/D-16 satisfied
- htmx 2.0.10 vendored (D-03); no CDN reference exists in the repo
- Wave-0 tests encode FND-01, FND-02, FND-03 and the e2e happy path, and are RED
</success_criteria>

<output>
Create `.planning/phases/01-foundation-ledger-core/01-01-SUMMARY.md` when done (note Python 3.13 vs 3.12 outcome explicitly).
</output>

---
phase: 11-dedicated-mobile-flow
plan: 07
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - app/routes/mobile_transfers.py
  - app/templates/mobile_pages/transfers.html
  - app/templates/mobile_partials/transfers_step_batch.html
  - app/templates/mobile_partials/transfers_step_dest.html
  - app/templates/mobile_partials/transfers_warning.html
  - tests/test_mobile_transfers.py
autonomous: true
requirements: [UI-01]

must_haves:
  truths:
    - "Operator can complete a warehouse transfer (Перемещение) through 3 mobile wizard steps: Товар -> Партия (showing source warehouse) -> Куда и количество, ending with the SAME register_transfer() write as desktop"
    - "The destination warehouse list always excludes the source batch's own warehouse (D-02 desktop precedent)"
    - "The over-transfer warning is zero-write until confirm=1, identical guarantee to desktop"
  artifacts:
    - path: app/routes/mobile_transfers.py
      provides: "The full transfer wizard, calling app.services.transfers.register_transfer unchanged"
  key_links:
    - from: "app/routes/mobile_transfers.py"
      to: "app.services.transfers.register_transfer"
      via: "direct function call, same kwargs as app/routes/transfers.py::transfers_create"
      pattern: "register_transfer\\(\\s*session"
---

<objective>
Build the mobile Перемещение (warehouse transfer) wizard: 3 steps (Товар → Партия → Куда и количество) ending in the exact same `register_transfer()` write as the desktop form. The batch-selection step is the one place in Phase 11 that needs a field `batch_card_picker.html` does not show by default (the source warehouse), so this plan builds its own thin wrapper around the same batch data instead of reusing that partial verbatim.

Purpose: completes the fourth and last "single scalar batch_id" wizard, reusing `open_batches`/`active_warehouses` and the exact destination-exclusion rule from desktop.
Output: `app/routes/mobile_transfers.py`, 3 mobile templates, `tests/test_mobile_transfers.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-dedicated-mobile-flow/11-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route skeleton + steps Товар/Партия (with source warehouse shown)</name>
  <files>app/routes/mobile_transfers.py, app/templates/mobile_pages/transfers.html, app/templates/mobile_partials/transfers_step_batch.html</files>
  <read_first>
    app/routes/transfers.py (full file — `transfers_lookup`, `transfers_batch_pick`, `_dest_warehouses` are the exact patterns to replicate)
    app/services/receipts.py (`lookup_prefill` — transfer reuses the receipt lookup per desktop's D-04 comment)
    app/services/batches.py (`open_batches`, `active_warehouses`)
    app/models.py (confirm `Batch` has no ORM `warehouse` relationship — only a `warehouse_id` FK column; resolve the warehouse name via a `{w.id: w.name for w in active_warehouses(session)}` map or `session.get(Warehouse, b.warehouse_id)`, not `b.warehouse`)
    .planning/phases/11-dedicated-mobile-flow/11-UI-SPEC.md ("Transfer wizard specifics" table — batch card must show source warehouse alongside the four LOT-02 fields)
  </read_first>
  <action>
    Create `app/routes/mobile_transfers.py`: `router = APIRouter()`. Import `Warehouse` from `app.models` alongside `Batch`, `Product`. `GET /m/transfers` → step "Товар" (`mobile_pages/transfers.html`, step indicator "Шаг 1 из 3"), debounced `code` lookup via `lookup_prefill` from `app.services.receipts`.

    `POST /m/transfers/step/batch` (accepts `code`) → step "Партия" (`mobile_partials/transfers_step_batch.html`, step indicator "Шаг 2 из 3"): call `open_batches(session, product.id)` (ALL warehouses — a transfer's source may be any warehouse, matching desktop's comment). Build a `{w.id: w.name for w in active_warehouses(session)}` lookup map and pass it into the template alongside `batches`. This step does NOT include `batch_card_picker.html` verbatim — instead render its OWN card markup that starts from the same 4-line composition (`Цена:`/`Срок годности:`/`Остаток:`/location·comment, identical wording and `| cents`/`| ru_date` filters to `batch_card_picker.html`) plus a 5th line `Склад: {{ warehouse_names[b.warehouse_id] }}`, each card `hx-get`-ing to `/m/transfers/step/batch-pick` on tap. Zero open batches anywhere → `show_empty=True`, literal text "Нет партий с остатком.", no forward control.

    `GET /m/transfers/step/batch-pick` (accepts `batch_id`, `code`) → re-query `open_batches` fresh and re-validate `candidate.product_id == product.id` (T-09-08 precedent, copied from `transfers_batch_pick`) before echoing the selection back; also compute the destination-warehouse candidate list via a `_dest_warehouses(session, picked)` helper mirroring desktop's exactly: `[w for w in active_warehouses(session) if w.id != picked.warehouse_id]` (or `[]` when `picked is None`) — this list is empty/unpopulated until a source batch is picked, matching desktop.
  </action>
  <acceptance_criteria>
    `app/routes/mobile_transfers.py` imports `lookup_prefill` from `app.services.receipts` and `Warehouse` from `app.models`
    `mobile_partials/transfers_step_batch.html` renders `Склад:` as a 5th line per card, in addition to the four `batch_card_picker.html` fields
    A `_dest_warehouses` helper (or equivalent inline expression) excludes the picked batch's own `warehouse_id` from its result
    The batch ownership check `candidate.product_id == product.id` appears in the `/m/transfers/step/batch-pick` handler
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_mobile_transfers.py -x -q -k "batch_step or empty_batches or dest_exclu"</automated>
  </verify>
  <done>Steps Товар/Партия show source-warehouse-annotated batch cards and correctly compute the destination-warehouse candidate list, excluding the source's own warehouse.</done>
</task>

<task type="auto">
  <name>Task 2: Step Куда и количество + final write + guardrail + success + tests</name>
  <files>app/templates/mobile_partials/transfers_step_dest.html, app/templates/mobile_partials/transfers_warning.html, app/routes/mobile_transfers.py, tests/test_mobile_transfers.py</files>
  <read_first>
    app/routes/transfers.py (`transfers_create` — exact try/except + oversell branching to replicate verbatim)
    app/services/transfers.py (`register_transfer` full signature: session, code, name, qty_raw, batch_id, dest_warehouse_id, confirm)
    app/templates/partials/transfer_oversell.html (exact copy — `oversell` is a SCALAR dict: `oversell.product`, `oversell.available`, `oversell.requested`)
    .planning/phases/11-dedicated-mobile-flow/11-UI-SPEC.md ("Transfer wizard specifics" table: dest+qty combined on one screen per D-05's "one decision per screen" grouping rule, exact CTA copy)
  </read_first>
  <action>
    Add `POST /m/transfers/step/dest` (accepts `code`, `batch_id`) → step "Куда и количество" (`mobile_partials/transfers_step_dest.html`, step indicator "Шаг 3 из 3"): a list of large tappable `<label class="mobile-card">` rows, one per candidate destination warehouse from `_dest_warehouses`, wrapping a visually-hidden `<input type="radio" name="dest_warehouse_id">`, plus a `qty` text input on the SAME screen (D-05: destination+quantity is treated as one combined decision, mirroring how desktop already groups them on one form). Primary CTA "Переместить".

    `POST /m/transfers` (final write) → `register_transfer(session, code=code, name=name, qty_raw=qty, batch_id=batch_id, dest_warehouse_id=dest_warehouse_id, confirm=confirm)` — same kwargs as `transfers_create`. Replicate its exact branching: `try/except` + `session.rollback()` + `logger.exception("register_transfer failed")` on failure (`SAVE_FAILED_ERROR`, 422); `if result and result.get("oversell")` (scalar) → render `mobile_partials/transfers_warning.html` verbatim-copying `transfer_oversell.html`'s body ("Товара не хватает в партии" / "{{ oversell.product.name }}: в партии {{ oversell.available }}, перемещаете {{ oversell.requested }}." / danger button "Переместить всё равно" re-posting the same hidden fields plus `confirm=1` / secondary "Вернуться к форме" client-side dismiss); `if errors` → 422 re-render step "Куда и количество" with errors; success → post-success screen with the saved product/qty summary + "Добавить ещё"/"На главную".

    Create `tests/test_mobile_transfers.py` using `mobile_client_factory(mobile_transfers.router)`: (1) happy path transfers part of a batch's quantity to a different warehouse, writing TWO `transfer` Operation rows (source negative, destination positive) with the destination batch retaining the source's price/expiry/comment/location (WH-03 requirement); (2) the destination warehouse list never includes the source batch's own warehouse, even when only 2 warehouses exist; (3) a qty exceeding the source batch's remaining quantity triggers the over-transfer warning with zero rows written, then `confirm=1` completes it; (4) zero open batches blocks the batch step.
  </action>
  <acceptance_criteria>
    `app/routes/mobile_transfers.py` calls `register_transfer(session, code=` with `batch_id=`, `dest_warehouse_id=`, `confirm=` matching `app/services/transfers.py`'s signature exactly
    The oversell check uses scalar truthiness (`result.get("oversell")`)
    `uv run pytest tests/test_mobile_transfers.py -x -q` passes all 4+ scenarios, with scenario 3 asserting zero rows before the confirm re-post
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_mobile_transfers.py -x -q</automated>
  </verify>
  <done>The mobile transfer wizard produces the identical two-row register_transfer() write as desktop, preserving cost/price history at the destination, with identical zero-write-until-confirmed guardrail semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser → `POST /m/transfers/step/batch-pick` | Untrusted client-supplied `batch_id` must be re-validated against the looked-up product |
| Browser → `POST /m/transfers` | Untrusted `dest_warehouse_id` reaches `register_transfer` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-11-19 | Tampering / Elevation of Privilege | `/m/transfers/step/batch-pick` posted `batch_id` | mitigate | Re-validate `candidate.product_id == product.id` (T-09-08 precedent) |
| T-11-20 | Tampering | `dest_warehouse_id` posted value, including a value equal to the source's own warehouse | mitigate | `register_transfer`'s existing service-layer validation (unchanged) rejects a same-warehouse or inactive destination exactly as it does for the desktop form; the mobile card list is a UI-level convenience, not the enforcement boundary |
| T-11-21 | Repudiation | Over-transfer bypass via a client that never re-POSTs `confirm=1` | mitigate | Zero Operation rows are written until `register_transfer` returns without `oversell` set |
| T-11-CSRF | Tampering | CSRF | accept | Unchanged app-wide posture |

</threat_model>

<verification>
`uv run pytest tests/test_mobile_transfers.py -x -q` must pass. Full suite stays green.
</verification>

<success_criteria>
A mobile transfer produces byte-identical two-row Operation writes to an equivalent desktop transfer, with identical destination-exclusion and guardrail semantics, verified in isolation via mobile_client_factory.
</success_criteria>

<output>
Create `.planning/phases/11-dedicated-mobile-flow/11-07-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `app/routes/mobile_transfers.py` — `router`, `GET /m/transfers`, `POST /m/transfers/step/batch`, `GET /m/transfers/step/batch-pick`, `POST /m/transfers/step/dest`, `POST /m/transfers`, helper `_dest_warehouses`
- `app/templates/mobile_pages/transfers.html`, `app/templates/mobile_partials/transfers_step_batch.html`, `app/templates/mobile_partials/transfers_step_dest.html`, `app/templates/mobile_partials/transfers_warning.html`

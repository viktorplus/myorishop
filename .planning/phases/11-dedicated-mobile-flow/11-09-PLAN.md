---
phase: 11-dedicated-mobile-flow
plan: 09
type: execute
wave: 3
depends_on: ["11-01", "11-02", "11-03", "11-04", "11-05", "11-06", "11-07", "11-08"]
files_modified:
  - app/main.py
  - tests/test_mobile_wiring.py
autonomous: true
requirements: [UI-01]

must_haves:
  truths:
    - "Every /m/... route built in Plans 02-08 is reachable from the real running application, not just from isolated tests"
    - "All 8 home-screen tiles resolve to a real 200 response, not a 404"
    - "The viewport-width redirect script and every pre-existing desktop route still work after registration (ROADMAP criterion 4 — purely additive)"
  artifacts:
    - path: app/main.py
      provides: "All 10 mobile routers registered alongside the existing 16 desktop routers"
      contains: "mobile_home"
  key_links:
    - from: "app/main.py"
      to: "app/routes/mobile_home.py, mobile_search.py, mobile_receipts.py, mobile_sales.py, mobile_writeoff.py, mobile_corrections.py, mobile_transfers.py, mobile_returns.py, mobile_history.py, mobile_reports.py"
      via: "app.include_router(...) for each"
      pattern: "include_router\\(mobile_"
---

<objective>
Wire every mobile router built in Plans 02-08 into the real FastAPI application (`app/main.py`), and prove the whole `/m/...` surface plus every pre-existing desktop route still works together — this is the ONE plan in Phase 11 that touches `app/main.py`, kept last per the "wire last" pattern so the 7 feature plans in wave 2 could be built and tested in parallel without file conflicts.

Purpose: this is the final integration point; nothing here writes new business logic — it is pure registration plus a regression test proving reachability and the "purely additive" ROADMAP criterion.
Output: `app/main.py` with all 10 new routers registered, `tests/test_mobile_wiring.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-dedicated-mobile-flow/11-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register all 10 mobile routers in app/main.py</name>
  <files>app/main.py</files>
  <read_first>
    app/main.py (the file being edited — read the existing `from app.routes import (...)` alphabetized block and the `app.include_router(...)` call sequence in full before editing)
    app/routes/mobile_home.py, app/routes/mobile_search.py, app/routes/mobile_receipts.py, app/routes/mobile_sales.py, app/routes/mobile_writeoff.py, app/routes/mobile_corrections.py, app/routes/mobile_transfers.py, app/routes/mobile_returns.py, app/routes/mobile_history.py, app/routes/mobile_reports.py (each module's exported `router` — confirm every module actually defines a module-level `router = APIRouter()` before editing)
  </read_first>
  <action>
    Edit `app/main.py`: add `mobile_corrections`, `mobile_history`, `mobile_home`, `mobile_receipts`, `mobile_reports`, `mobile_returns`, `mobile_search`, `mobile_sales`, `mobile_transfers`, `mobile_writeoff` to the existing `from app.routes import (...)` block, keeping the WHOLE block alphabetically sorted (merge the 10 new names into the existing list of 16, do not append them as a separate second import statement). Add 10 new `app.include_router(mobile_X.router)` calls after the existing 16 `include_router` calls, in this order (matches the home-tile grid order for readability, not alphabetical): `mobile_home`, `mobile_sales`, `mobile_receipts`, `mobile_search`, `mobile_writeoff`, `mobile_corrections`, `mobile_transfers`, `mobile_returns`, `mobile_history`, `mobile_reports`. Do not reorder or modify any of the existing 16 `include_router` calls or the `lifespan`/`StaticFiles` setup above them.
  </action>
  <acceptance_criteria>
    `app/main.py`'s `from app.routes import (...)` block contains all 26 module names (16 existing + 10 new), alphabetically sorted as a single import statement
    `app/main.py` contains exactly 10 new `app.include_router(mobile_` lines
    `app/main.py`'s existing `lifespan`, `StaticFiles` mount, and pre-existing 16 `include_router` calls are byte-identical to before this edit except for their position relative to the new lines
    `python -c "from app.main import app"` succeeds with no ImportError
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "from app.main import app; print(len(app.routes))"</automated>
  </verify>
  <done>app/main.py imports and registers all 10 mobile routers without touching any pre-existing registration.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end reachability regression test + full suite verification</name>
  <files>tests/test_mobile_wiring.py</files>
  <read_first>
    tests/test_smoke.py (the existing convention for asserting on the real `client` fixture — e.g. `assert "/static/htmx.min.js" in response.text`)
    tests/conftest.py (the real `client` fixture — NOT `mobile_client_factory` — this task must exercise the actual registered app)
    app/templates/mobile_pages/home.html (the 8 literal tile hrefs to assert against, built in Plan 02)
  </read_first>
  <action>
    Create `tests/test_mobile_wiring.py` using the real `client` fixture (from `tests/conftest.py`, bound to `app.main.app`) — NOT `mobile_client_factory`, since the point of this test is proving registration in the REAL app. Assertions: (1) `GET /m/` returns 200 and its response text contains all 8 literal hrefs `/m/sales`, `/m/receipts`, `/m/search`, `/m/writeoff`, `/m/corrections`, `/m/transfers`, `/m/history`, `/m/reports/expiry`; (2) each of those 8 paths, plus `/m/` itself, returns a non-404 status via `client.get(path)` (200 for a GET-only screen; for wizard entry points this is their `GET` landing page, which must exist and return 200 even before any Form data is posted); (3) `GET /` (desktop home) still returns 200 and its response text contains the literal substring `matchMedia("(max-width: 599px)")` (the Pitfall-2 regression proxy — `TestClient` cannot execute JS, so this only confirms the script text is present, which is the documented limit of automated coverage for D-02 per RESEARCH.md's Validation Architecture section); (4) every pre-existing desktop nav-linked route still returns 200: `/products`, `/categories`, `/warehouses`, `/receipts/new`, `/sales/new`, `/writeoff`, `/transfers`, `/customers`, `/history`, `/reports`, `/export`, `/dictionary`, `/backup` (this is the automated proxy for ROADMAP criterion 4 — "desktop pages remain visually and functionally unchanged").

    After the test file is written and passing, run the FULL existing suite (`uv run pytest -q`) and confirm it is 100% green — this is the final proof that Phase 11 is purely additive, per RESEARCH.md's Sampling Rate / Phase gate guidance.
  </action>
  <acceptance_criteria>
    `tests/test_mobile_wiring.py` uses the `client` fixture, not `mobile_client_factory`
    `uv run pytest tests/test_mobile_wiring.py -x -q` passes all assertions
    `uv run pytest -q` (the complete existing + new suite) reports zero failures
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q</automated>
  </verify>
  <done>Every /m/... route is reachable in the real running app, every pre-existing desktop route is unaffected, and the complete test suite (existing + all of Phase 11's new tests) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Real running FastAPI app (`app/main.py`) → all 26 registered routers | No new trust boundary is introduced by registration itself — every individual router's own threat model (Plans 01-08) already covers its inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-11-25 | Denial of Service (route collision) | `app/main.py` router registration order | mitigate | Verified by `python -c "from app.main import app"` succeeding with no `ImportError`/route-collision at import time (FastAPI raises no error on path overlap by default, but this plan's acceptance criteria require every new path to be under the previously-unused `/m/...` prefix, so no collision with any of the 16 existing desktop routers is possible by construction) |
| T-11-26 | Tampering | Aggregate `/m/...` attack surface once fully wired | accept | Each individual endpoint's threats were already assessed and mitigated in its own plan (01-08); wiring introduces no new input-handling code |
| T-11-CSRF | Tampering | CSRF | accept | Unchanged app-wide posture, confirmed unaffected by this plan's full-suite regression run |

</threat_model>

<verification>
`uv run pytest -q` (the complete suite, existing + all Phase 11 tests) must report zero failures. This is the phase-gate check per RESEARCH.md's Validation Architecture.
</verification>

<success_criteria>
Phase 11's entire mobile flow is reachable end-to-end from the real application; the complete test suite is green; the 6 manual UAT gates listed in `11-UI-SPEC.md`'s Interaction Contract (viewport redirect behavior, desktop-width non-redirect, all 8 tiles reachable, no-truncation batch cards, guardrail copy/zero-write parity, pixel-for-pixel desktop unchanged) remain as the only manual verification step before `/gsd-verify-work`.
</success_criteria>

<output>
Create `.planning/phases/11-dedicated-mobile-flow/11-09-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `app/main.py` — 10 new `app.include_router(...)` calls; no new symbols, in-place registration only
- `tests/test_mobile_wiring.py` — new regression test module covering full-app reachability

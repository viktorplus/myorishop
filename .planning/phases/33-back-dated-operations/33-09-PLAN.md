---
phase: 33-back-dated-operations
plan: 09
type: execute
wave: 5
depends_on: ["33-07"]
files_modified:
  - app/services/export.py
  - tests/test_export.py
autonomous: true
requirements: [DATE-03, DATE-05]

must_haves:
  truths:
    - "cash_movements.csv selects its rows by the business date"
    - "Column 1 «Когда» carries the business date in both sales.csv and cash_movements.csv"
    - "A new last column «Внесено» carries the entry timestamp in both files"
    - "Column 1 is non-decreasing down each file"
    - "Column positions 1..N do not shift, so an existing spreadsheet formula over Код/Цена/Сумма keeps working"
    - "stream_products_csv and stream_customers_csv are byte-unchanged"
  artifacts:
    - path: "app/services/export.py"
      provides: "business-date row selection, «Когда» value change and the appended «Внесено» column"
      contains: "Внесено"
  key_links:
    - from: "app/services/export.py"
      to: "app/services/reports.py::business_date_expr"
      via: "the period predicate AND both ORDER BY clauses"
      pattern: "business_date_expr"
---

<objective>
D-23. In a single-operator Russian warehouse export, «Когда» reads as «когда это случилось» — so
column 1 becomes the business date and the entry timestamp is appended LAST as «Внесено».

The reason this is not a cosmetic preference: once back-dating ships, the row set of
`cash_movements.csv` is chosen by the business date (`app/services/export.py:211-212` is on the
LOCKED must-switch list). Under the additive alternative the file's headline date column would
routinely contradict the file's own period — exactly what DATE-03 exists to prevent.

Purpose: D-23 plus the CD-9 gap it left open.
Output: two CSV writers reshaped; the other two explicitly untouched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-back-dated-operations/33-CONTEXT.md
@.planning/phases/33-back-dated-operations/33-UI-SPEC.md
@.planning/phases/33-back-dated-operations/33-RESEARCH.md
@CLAUDE.md
</context>

<project_conventions>
- Portable ORM only; closed inclusive range per plan 33-06.
- Money cells are untouched — integer minor units rendered by the existing helpers (CLAUDE.md PC-3).
- Every value written into a CSV cell keeps going through the existing `_csv_safe` path; the two new
  date renders introduce no new escaping surface.
- Bump `app/__init__.py::__version__` by 0.1 per completed-task commit.
</project_conventions>

<artifacts_this_phase_produces>
`business_date_bounds`, `local_today_iso`, `business_date_expr`, `push_schema_ok`, `parse_op_date`,
`today_iso` (Jinja global), `OP_DATE_FORMAT_ERROR`, `OP_DATE_FUTURE_ERROR`, `SCHEMA_AHEAD_ERROR`,
`SyncResult(status="schema_mismatch")`, the four ledger columns `operations.business_date` /
`cash_movements.business_date` / `operations.reverses_op_id` / `cash_movements.reverses_movement_id`,
the `dated` filter parameter, the `business_day` and `is_backdated` row-dict keys, the
`.field.op-date` CSS class, the `op_date` form field, migration `0027`, the `alembic_engine` fixture
and `run_alembic` helper in `tests/conftest.py`, and the three new test files
`tests/test_migrations.py`, `tests/test_business_date.py`, `tests/test_sync_schema_gate.py`.
</artifacts_this_phase_produces>

<planner_decision_cd9>
**`export.py:214` DOES get D-23's `ORDER BY` treatment. Recorded here rather than left silent.**

D-23 enumerates ONE coupled `ORDER BY` edit — `export.py:135` for `sales.csv` — by the argument that
the dump would otherwise read as unsorted by its own first column. `stream_cash_movements_csv` has
the IDENTICAL construct at `export.py:214` (`.order_by(CashMovement.created_at)`) and its «Когда»
column at `:219` is already on D-23's rewrite list. Two independent research passes confirmed the
omission is real. Under D-23's own stated argument the edit is required; leaving it out is the
failure mode. Both `ORDER BY` clauses are switched.
</planner_decision_cd9>

<tasks>

<task type="auto">
  <name>Task 1: period predicate + both ORDER BY clauses</name>
  <files>app/services/export.py</files>
  <read_first>
    - app/services/export.py:34-36 (imports), :130-160 (`stream_sales_csv`: the `ORDER BY` at `:135`,
      the header at `:137-147`, the row build at `:156`)
    - app/services/export.py:205-225 (`stream_cash_movements_csv`: the period predicate at
      `:211-212`, the `ORDER BY` at **`:214`**, the header at `:216`, the row build at `:219`)
    - app/services/export.py:94-102 (`stream_products_csv` — no date column, OUT of scope) and
      :179,:185 (`stream_customers_csv`'s «Создан» is `Customer.created_at` on a table that gains no
      `business_date` — OUT of scope)
    - app/routes/finance.py:371-379 and app/routes/mobile_finance.py:374-380 — the two callers of
      `stream_cash_movements_csv`, whose bounds were switched to `business_date_bounds` by plan 33-07
    - app/routes/export.py:36-38 — `sales.csv` is a full dump with NO period filter
    - app/services/reports.py — `business_date_expr`
  </read_first>
  <action>
    Switch the period predicate in `stream_cash_movements_csv` (`:211-212`) to
    `business_date_expr(CashMovement)` with the closed contract (`>= start_day`, `<= end_day`). Its
    two callers already pass date-only bounds after plan 33-07 — confirm that before editing;
    if either still passes `local_day_bounds_utc` output, stop and report, because the mismatch is
    the Pitfall-14 bug.

    Switch BOTH `ORDER BY` clauses to a three-part order, business date first, with the technical
    timestamp and `seq` as tie-breakers so the dump is deterministic:
    - `:135` — `.order_by(Operation.created_at)` becomes
      `.order_by(business_date_expr(Operation), Operation.created_at, Operation.seq)`.
    - `:214` — `.order_by(CashMovement.created_at)` becomes the same three-part order over
      `CashMovement` (the CD-9 edit; see `<planner_decision_cd9>`).

    Add a one-line comment at each naming D-23 and the reason: otherwise the dump reads as unsorted
    by its own first column.

    `sales.csv` is a full dump with no period filter and still gets both changes — it is an
    `Operation` dump and will contain back-dated rows (DATE-05).
  </action>
  <verify>
    <automated>uv run pytest tests/test_export.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_export.py -q` green.
    - `grep -n "order_by" app/services/export.py` shows `business_date_expr` in BOTH the sales and
      the cash-movement query.
    - The cash-movement predicate uses `<=` against the upper bound (closed contract).
    - `git diff app/services/export.py` shows no change inside `stream_products_csv` or
      `stream_customers_csv`.
  </acceptance_criteria>
  <done>Both dumps are selected and ordered by the business date.</done>
</task>

<task type="auto">
  <name>Task 2: «Когда» becomes the business date, «Внесено» appended last</name>
  <files>app/services/export.py</files>
  <read_first>
    - app/services/export.py:137-147 and :156 (sales header + row), :216 and :219 (cash header + row)
    - app/core.py:89-99 (`format_ru_date`) and :102-105 (`iso_to_local`) — the two render paths
    - 33-UI-SPEC.md § Interaction Contract §8 — the column contract table
  </read_first>
  <action>
    In BOTH writers:
    - Column 1 keeps the header **«Когда»**; its VALUE becomes the business date, rendered as
      `format_ru_date(<business_date> or <created_at>[:10])` → `15.08.2026`, day only.
    - Append a NEW LAST column with the header **«Внесено»**, valued
      `iso_to_local(<created_at>, settings.display_tz)` → `04.09.2026 15:22`.

    **The NULL fallback is the UTC prefix, deliberately.** A DATE-08 row from a client that has not
    updated has `business_date IS NULL`; the CSV renders `created_at[:10]`, matching the
    `func.coalesce(business_date, substr(created_at,1,10))` expression the period-scoped row set was
    selected by. Column 1 must never contradict the file's own period. Backfilled rows are
    unaffected: their business date is the tz-correct local day, so the calendar date the operator
    sees is unchanged.

    **Column positions 1..N do not shift.** `Код` / `Цена` / `Сумма` keep their indexes, so an
    existing spreadsheet formula over them keeps working. Only column 1's VALUE TYPE changes
    (`dd.mm.yyyy HH:MM` → `dd.mm.yyyy`), and the `HH:MM` reappears verbatim in «Внесено». Write that
    accepted cost as a comment above each header list.

    **Out of scope entirely — do not touch:** `stream_products_csv` (`:94-102`, no date column at
    all) and `stream_customers_csv` (`:179`, `:185` — its «Создан» is `Customer.created_at` on a
    table that gains no `business_date`).
  </action>
  <verify>
    <automated>uv run pytest tests/test_export.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Внесено" app/services/export.py` returns 2 (one per writer).
    - The header list of each writer still starts with «Когда» and the new header is LAST.
    - `git diff --stat app/services/export.py` shows changes confined to `stream_sales_csv` and
      `stream_cash_movements_csv`.
    - Column 1 of each file matches `\d{2}\.\d{2}\.\d{4}` with no `:` time separator.
  </acceptance_criteria>
  <done>Both files state when it happened first and when it was entered last.</done>
</task>

<task type="auto">
  <name>Task 3: extend tests/test_export.py</name>
  <files>tests/test_export.py</files>
  <read_first>
    - tests/test_export.py — the whole file: how CSV output is captured and asserted, and the five
      `local_day_bounds_utc` fixture sites at `:111, :150, :164, :182, :333` (these BUILD `created_at`
      values and must stay as they are)
  </read_first>
  <action>
    Extend `tests/test_export.py` with:
    - `test_sales_csv_when_column_is_business_date` — a back-dated sale renders its business date in
      column 1 and its entry timestamp in the last column.
    - `test_cash_csv_when_column_is_business_date` — the same for `cash_movements.csv`.
    - `test_csv_vnesyeno_column_is_last` — the new header is the LAST field of each header row, and
      the indexes of `Код` / `Цена` / `Сумма` are unchanged from HEAD.
    - `test_csv_first_column_non_decreasing` — parse each file and assert column 1, converted back
      from `dd.mm.yyyy`, is non-decreasing down the file. This single cheap assertion covers BOTH
      `ORDER BY` edits, including the CD-9 one at `:214`.
    - `test_cash_csv_row_set_uses_business_date` (VA-13) — a movement back-dated INTO the exported
      period appears; one back-dated OUT of it does not.
    - `test_csv_null_business_date_falls_back_to_utc_prefix` (DATE-08) — a NULL-business-date row
      still appears and renders `created_at[:10]` in column 1.
    - `test_products_and_customers_csv_unchanged` — assert the header rows of `stream_products_csv`
      and `stream_customers_csv` are exactly what they are at HEAD.

    Do NOT rewrite the existing `local_day_bounds_utc` fixture calls in this file — they build
    `created_at` values and that remains the sanctioned way to do it.
  </action>
  <verify>
    <automated>uv run pytest tests/test_export.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All seven new tests pass and the pre-existing ones stay green.
    - `git diff tests/test_export.py` shows no modification to the five `local_day_bounds_utc` call
      sites.
    - `test_csv_first_column_non_decreasing` covers both files.
  </acceptance_criteria>
  <done>The CSV contract is pinned, including the ordering edit D-23 forgot to enumerate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored ledger text → CSV file opened in a spreadsheet | Stored operator text crosses into a formula-capable host |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-25 | Tampering | CSV formula injection through a cell | accept | Unchanged by this plan — the existing `_csv_safe` path still handles every cell, and the two columns this plan touches are machine-rendered dates (`format_ru_date` / `iso_to_local`), which cannot carry a leading `=`, `+`, `-` or `@` |
| T-33-26 | Repudiation | An export whose headline date column contradicts the period the file was selected for | mitigate | Column 1 and the row-set predicate both use the same `business_date_expr`; the NULL fallback in the render matches the COALESCE fallback in the query |
| T-33-27 | Tampering (silent wrong answer) | A dump that reads as unsorted by its own first column | mitigate | Both `ORDER BY` clauses switched (including CD-9's `:214`), pinned by `test_csv_first_column_non_decreasing` |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | No package installed; `pyproject.toml` untouched |
</threat_model>

<verification>
- `uv run pytest tests/test_export.py -q` green.
- `uv run pytest -q` green apart from the four known-red `tests/test_sync_ui.py` cases.
- Manual check B-7 (plan 33-15): export both files after one back-dated row exists and confirm one
  extra column, header «Внесено» last, column 1 non-decreasing, and `Код` / `Цена` / `Сумма` at their
  original positions.
</verification>

<success_criteria>
- `cash_movements.csv` selects rows by the business date and orders by it.
- Both files show the business date in «Когда» and the entry timestamp in a new last «Внесено».
- `stream_products_csv` and `stream_customers_csv` are byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/33-back-dated-operations/33-09-SUMMARY.md` when done.
</output>

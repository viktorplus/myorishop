---
phase: 33-back-dated-operations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/sync_client.py
  - app/main.py
  - tests/test_sync_client.py
  - tests/test_autosync.py
autonomous: true
requirements: [SYNC-10, SYNC-11]

must_haves:
  truths:
    - "A 409 from the server surfaces to the operator as «Сервер ещё не обновлён — синхронизация отложена» in #sync-status"
    - "The unsynced-count badge is NOT suppressed on a schema refusal — the growing count is the correct pressure signal"
    - "A 409 tick returns early: no pull is attempted"
    - "After a schema_mismatch tick the auto-sync loop sleeps MAX_INTERVAL_SECONDS (3600) instead of 300"
    - "A subsequent non-mismatch tick restores the configured interval with no manual reset"
  artifacts:
    - path: "app/services/sync_client.py"
      provides: "schema_mismatch SyncResult status + one format_sync_message branch"
      contains: "schema_mismatch"
    - path: "app/main.py"
      provides: "D-09 back-off in _auto_sync_iteration"
  key_links:
    - from: "app/services/sync_client.py"
      to: "app/templates/partials/sync_status.html"
      via: "format_sync_message return value rendered into #sync-status (no template change)"
      pattern: "format_sync_message"
---

<objective>
SYNC-10, operator-facing half. A 409 from the new push gate must read as a plain Russian sentence in
the header, not as a generic «Ошибка сервера», and must not turn into a self-inflicted retry storm.

Purpose: D-08 and D-09. Both reuse shipped machinery — one `elif` in the existing formatter chain,
one branch inside the existing `except httpx.HTTPStatusError`, and ~4 lines at the bottom of
`_auto_sync_iteration`. No new template, no new CSS token, no new column, no new setting.
Output: `SyncResult(status="schema_mismatch")`, its formatter branch, and the interval back-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-back-dated-operations/33-CONTEXT.md
@.planning/phases/33-back-dated-operations/33-PATTERNS.md
@.planning/phases/33-back-dated-operations/33-VALIDATION.md
@CLAUDE.md
</context>

<project_conventions>
- Russian for operator-facing strings, English for identifiers/comments/commits.
- Additive change only; reuse the shipped formatter and the shipped exception branch.
- Bump `app/__init__.py::__version__` by 0.1 on every completed-task commit (deliberately absent
  from `files_modified`; see plan 33-01's note).
</project_conventions>

<artifacts_this_phase_produces>
`business_date_bounds`, `local_today_iso`, `business_date_expr`, `push_schema_ok`, `parse_op_date`,
`today_iso` (Jinja global), `OP_DATE_FORMAT_ERROR`, `OP_DATE_FUTURE_ERROR`, `SCHEMA_AHEAD_ERROR`,
`SyncResult(status="schema_mismatch")`, the four ledger columns `operations.business_date` /
`cash_movements.business_date` / `operations.reverses_op_id` / `cash_movements.reverses_movement_id`,
the `dated` filter parameter, the `business_day` and `is_backdated` row-dict keys, the
`.field.op-date` CSS class, the `op_date` form field, migration `0027`, the `alembic_engine` fixture
and `run_alembic` helper in `tests/conftest.py`, and the three new test files
`tests/test_migrations.py`, `tests/test_business_date.py`, `tests/test_sync_schema_gate.py`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: RED cases for the refusal path</name>
  <files>tests/test_sync_client.py, tests/test_autosync.py</files>
  <read_first>
    - tests/test_sync_client.py — the whole file: how `SyncResult`, `format_sync_message` and the
      push driver are exercised today, and how the HTTP layer is stubbed
    - tests/test_autosync.py — the whole file: how `_auto_sync_iteration` is driven and how
      `read_autosync_config` is faked
    - app/services/sync_client.py:52-53 (`MAX_INTERVAL_SECONDS`, `DEFAULT_INTERVAL_SECONDS`),
      :186-209 (`format_sync_message`), :376-393 (the `raise_for_status` / stamp region)
    - app/main.py:71-103 — `_auto_sync_iteration`
  </read_first>
  <action>
    Extend the two existing test modules (do not create a new file — the shapes already live here).

    In `tests/test_sync_client.py` add:
    - `test_format_sync_message_schema_mismatch` — `format_sync_message` on a
      `SyncResult(status="schema_mismatch", ...)` returns exactly
      «Сервер ещё не обновлён — синхронизация отложена» (the string is not invented here; it is
      reused from `.planning/research/PITFALLS.md:130`).
    - `test_push_409_returns_schema_mismatch` — a stubbed 409 response makes the push driver return
      `status == "schema_mismatch"` (not `"error"`), with `pushed == 0`.
    - `test_schema_mismatch_skips_pull` — the tick returns early after the refusal; assert the pull
      endpoint was never called.
    - `test_schema_mismatch_leaves_rows_unsynced` — every local row still has `synced_at IS NULL`
      after the refusal (SYNC-11 restated at the client boundary).
    - `test_schema_mismatch_message_carries_no_server_bytes` — the formatted message is the fixed
      constant; assert the server's `detail` text does not appear in it (T-29-07 / V7: the client
      must keep ignoring `detail`, and it does by construction).

    In `tests/test_autosync.py` add:
    - `test_auto_sync_backs_off_on_schema_mismatch` — after a tick that leaves
      `sync_state.last_status == "schema_mismatch"`, `_auto_sync_iteration` returns
      `sync_client.MAX_INTERVAL_SECONDS` (3600), not the configured interval.
    - `test_auto_sync_interval_restored_after_recovery` — the next tick with a non-mismatch status
      returns the configured interval again, with no manual reset.

    Import any not-yet-existing symbol inside the test body so collection stays green while
    execution is red.
  </action>
  <verify>
    <automated>uv run pytest tests/test_sync_client.py tests/test_autosync.py -q --collect-only</automated>
  </verify>
  <acceptance_criteria>
    - Collection exits 0 with the 7 new test names present.
    - `uv run pytest tests/test_sync_client.py -k schema_mismatch -q` is RED (assertion/attribute
      failures, not collection errors).
    - The literal string «Сервер ещё не обновлён — синхронизация отложена» appears in
      `tests/test_sync_client.py`.
  </acceptance_criteria>
  <done>The D-08/D-09 contract is expressed as failing tests before any production edit.</done>
</task>

<task type="auto">
  <name>Task 2: schema_mismatch status + formatter branch (D-08)</name>
  <files>app/services/sync_client.py</files>
  <read_first>
    - app/services/sync_client.py:52-53, :182-209, :376-393, :597-599
    - app/templates/partials/sync_status.html:15-16 — `#sync-status` renders whatever the formatter
      returns and `#sync-badge` renders the unsynced count
    - 33-PATTERNS.md §B6
  </read_first>
  <action>
    Branch the EXISTING `except httpx.HTTPStatusError` block (`:377-379`) on
    `status_code == 409` and return `SyncResult(status="schema_mismatch", pushed=0,
    pushed_total=pushed_total)`; every other non-2xx keeps returning `status="error"` unchanged.
    Keep the early return — a client-ahead refusal means the server's reference data is OLDER, so
    pulling is pointless.

    Add ONE `elif status == "schema_mismatch"` branch to `format_sync_message` (`:186-209`), placed
    above the final `else`, returning the fixed string
    «Сервер ещё не обновлён — синхронизация отложена». Honour the docstring constraint at `:182-184`:
    only fixed strings and integer counts ever cross this boundary — never server bytes, never the
    token. Do not read or interpolate the response `detail` (T-29-07).

    Do NOT touch `app/templates/partials/sync_status.html` and do NOT suppress `#sync-badge` (D-08):
    the growing unsynced count is the correct visible pressure signal and is exactly what SYNC-11
    guarantees is recoverable. No new template, no new CSS token.

    Add a one-line comment naming D-08 and the reason the pull is skipped.
  </action>
  <verify>
    <automated>uv run pytest tests/test_sync_client.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All five new `tests/test_sync_client.py` cases pass.
    - `git diff --stat app/templates/` is empty for this task (no template change).
    - `grep -n "schema_mismatch" app/services/sync_client.py` shows exactly two occurrences: the
      `SyncResult` construction in the 409 branch and the formatter `elif`.
    - `grep -c "detail" app/services/sync_client.py` is unchanged from HEAD (the client still
      ignores the server's `detail`).
    - `grep -c "business_date" app/services/sync_client.py` returns 0 (Pitfall 16).
  </acceptance_criteria>
  <done>A 409 reads as a plain Russian sentence in the header and the badge keeps growing.</done>
</task>

<task type="auto">
  <name>Task 3: auto-sync back-off on schema_mismatch (D-09)</name>
  <files>app/main.py</files>
  <read_first>
    - app/main.py:71-103 — `_auto_sync_iteration`: the interval is read at the TOP (`:86-87`) and
      returned at the BOTTOM (`:103`)
    - app/services/sync_client.py:52-53 — `MAX_INTERVAL_SECONDS = 3600`,
      `DEFAULT_INTERVAL_SECONDS = 300`
    - app/routes/sync.py:84-86 — the push rate limit a retry storm would burn
    - app/templates/base.html:76 — the manual «Синхронизировать» link, which shares the `_run_lock`
      but not the loop's sleep
  </read_first>
  <action>
    In `_auto_sync_iteration`, after the tick and before `return interval`, re-read
    `sync_state.last_status` and return `sync_client.MAX_INTERVAL_SECONDS` when it is
    `"schema_mismatch"`. Roughly four lines; no new column, no new setting, self-clearing on the
    next non-mismatch tick.

    Why it is needed, and worth a code comment: the interval is read at the TOP of the iteration, so
    a permanent 409 would otherwise retry every 300 s forever, each time re-uploading the whole
    growing unsynced closure and burning a rate-limit token (`app/routes/sync.py:84-86`).

    Accepted cost, also worth the comment: recovery is delayed up to an hour after s1 is rebuilt —
    mitigated because the manual «Синхронизировать» link shares the `_run_lock` but not the loop's
    sleep, so the operator can resync instantly.

    Keep the existing broad `except Exception: pass` guard intact — the loop must never die.
  </action>
  <verify>
    <automated>uv run pytest tests/test_autosync.py tests/test_sync_client.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_auto_sync_backs_off_on_schema_mismatch` and
      `test_auto_sync_interval_restored_after_recovery` both pass.
    - `grep -n "MAX_INTERVAL_SECONDS" app/main.py` shows the new reference.
    - No new module-level constant and no new `sync_state` column were introduced
      (`git diff app/models.py` empty; `git diff alembic/` empty).
    - The broad `except Exception` guard in `_auto_sync_iteration` is still present.
  </acceptance_criteria>
  <done>A permanently refused client backs off to one attempt per hour and recovers by itself.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server response → local client | HTTP status and body cross from the server into client-side control flow and operator-visible text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-03 | Denial of service (self-inflicted) | `_auto_sync_iteration` retrying a permanent 409 every 300 s | mitigate | D-09: re-read `sync_state.last_status` after the tick and return `MAX_INTERVAL_SECONDS` on `schema_mismatch` |
| T-33-05 | Information disclosure | Server `detail` bytes reaching the operator's header | mitigate | `format_sync_message` returns a fixed RU constant only; the client never reads `detail` (T-29-07). Pinned by `test_schema_mismatch_message_carries_no_server_bytes` |
| T-33-06 | Repudiation | Suppressing `#sync-badge` on a refusal would hide the unsent backlog | mitigate | D-08 forbids it; the badge is untouched and its growth is the pressure signal SYNC-11 guarantees is recoverable |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | No package installed; `pyproject.toml` untouched (`33-RESEARCH.md` § Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `uv run pytest tests/test_sync_client.py tests/test_autosync.py tests/test_sync_schema_gate.py -q` green.
- `uv run pytest -q` green apart from the four known-red `tests/test_sync_ui.py` cases (pre-existing;
  do not attribute, do not fix).
</verification>

<success_criteria>
- A 409 produces `status="schema_mismatch"`, the RU sentence in `#sync-status`, an untouched
  `#sync-badge`, and no pull attempt.
- The next auto-sync tick after a mismatch sleeps 3600 s; the one after recovery sleeps the
  configured interval.
- No template, CSS token, model column or setting was added.
</success_criteria>

<output>
Create `.planning/phases/33-back-dated-operations/33-02-SUMMARY.md` when done.
</output>

---
phase: 33-back-dated-operations
plan: 13
type: execute
wave: 5
depends_on: ["33-07", "33-10", "33-11", "33-12"]
files_modified:
  - app/services/finance.py
  - app/routes/finance.py
  - app/routes/mobile_finance.py
  - app/templates/partials/withdraw_form.html
  - app/templates/partials/deposit_form.html
  - tests/test_finance.py
  - tests/test_business_date.py
autonomous: true
requirements: [DATE-01, DATE-02]

must_haves:
  truths:
    - "Снятие and внесение carry a visible «Дата операции» input pre-filled with today, on desktop and on mobile"
    - "The two cash forms render on the same page and each label focuses its OWN input"
    - "A future date is refused in Russian and no cash movement is written"
    - "All 14 write surfaces render name=\"op_date\" pre-filled with today"
  artifacts:
    - path: "app/templates/partials/withdraw_form.html"
      provides: "date field with the prefixed id withdraw-op-date"
      contains: "withdraw-op-date"
    - path: "app/templates/partials/deposit_form.html"
      provides: "date field with the prefixed id deposit-op-date"
      contains: "deposit-op-date"
    - path: "tests/test_business_date.py"
      provides: "VA-15 — every write surface renders op_date"
      contains: "test_every_write_surface_renders_op_date"
  key_links:
    - from: "app/templates/partials/withdraw_form.html"
      to: "app/services/finance.py::record_cash_movement"
      via: "op_date Form parameter through both the desktop and the mobile finance route"
      pattern: "op_date"
---

<objective>
DATE-01 / DATE-02 for the last two of the 14 write surfaces — the cash forms — and the single test
that proves the whole surface list is covered.

These two templates are SHARED between desktop and mobile, parameterised by `finance_base`
(`app/templates/pages/finance.html:30,33` and `app/templates/mobile_pages/finance.html:33,36`), so
one edit each covers both. And both render on the SAME page, which is why they are the only two
surfaces that do NOT use `id="op_date"`.

Rationale for including cash at all (D-16): DATE-03 puts the cash-flow report on the
bucket-by-business-date list and migration `0027` adds `cash_movements.business_date` — with no field
on the cash forms that column would only ever be populated by the backfill and by Phase 34
reversals, which is incoherent.
Output: two shared templates, both finance routes, and VA-15.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-back-dated-operations/33-UI-SPEC.md
@.planning/phases/33-back-dated-operations/33-CONTEXT.md
@.planning/phases/33-back-dated-operations/33-VALIDATION.md
@CLAUDE.md
</context>

<project_conventions>
- Label copy «Дата операции»; no `required`; no «(необязательно)» hint.
- Service-layer errors under `errors["op_date"]`, 422, redisplay what was typed, zero writes.
- No new CSS rule — `.field.op-date` already exists (plan 33-10).
- Bump `app/__init__.py::__version__` by 0.1 per completed-task commit.
</project_conventions>

<artifacts_this_phase_produces>
`business_date_bounds`, `local_today_iso`, `business_date_expr`, `push_schema_ok`, `parse_op_date`,
`today_iso` (Jinja global), `OP_DATE_FORMAT_ERROR`, `OP_DATE_FUTURE_ERROR`, `SCHEMA_AHEAD_ERROR`,
`SyncResult(status="schema_mismatch")`, the four ledger columns `operations.business_date` /
`cash_movements.business_date` / `operations.reverses_op_id` / `cash_movements.reverses_movement_id`,
the `dated` filter parameter, the `business_day` and `is_backdated` row-dict keys, the
`.field.op-date` CSS class, the `op_date` form field, migration `0027`, the `alembic_engine` fixture
and `run_alembic` helper in `tests/conftest.py`, and the three new test files
`tests/test_migrations.py`, `tests/test_business_date.py`, `tests/test_sync_schema_gate.py`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: finance service — op_date on the manual cash write path</name>
  <files>app/services/finance.py, tests/test_finance.py</files>
  <read_first>
    - app/services/finance.py:12-16 (imports), :48-57 (`record_cash_movement` with its
      `business_date` kwarg from plan 33-06), and :180-195 — the public entry function containing the
      `record_cash_movement` call at `:188`
    - app/services/ledger.py — `parse_op_date`, `OP_DATE_FORMAT_ERROR`, `OP_DATE_FUTURE_ERROR`
  </read_first>
  <action>
    Give the manual cash-movement entry function an `op_date: str = ""` keyword, parse it with
    `parse_op_date(op_date, errors)` inside the existing validation block before any write, and pass
    `business_date=<parsed>` to `record_cash_movement` at `app/services/finance.py:188`.

    Import `parse_op_date` from `app.services.ledger`. This creates no import cycle: `ledger.py`
    imports only `app.config`, `app.core`, `app.models` and `app.services.security`, and does not
    import `finance.py`. The other two `record_cash_movement` call sites (`sales.py:310`,
    `returns.py:174`) were threaded by plan 33-11 and are not touched here.

    Extend `tests/test_finance.py` (VA-14): a back-dated withdrawal lands its `business_date`; an
    empty value stamps today's local day; a future value sets
    `errors["op_date"] == OP_DATE_FUTURE_ERROR` with an unchanged `CashMovement` count; a malformed
    value sets `OP_DATE_FORMAT_ERROR`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_finance.py tests/test_finance_reports.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_finance.py tests/test_finance_reports.py -q` green.
    - `grep -n "business_date" app/services/finance.py` shows the kwarg at the `:188` call site.
    - The future-date test asserts an unchanged `CashMovement` count.
    - `python -c "import app.services.finance; print('OK')"` prints OK (no import cycle).
  </acceptance_criteria>
  <done>Manual cash movements accept and validate a business date.</done>
</task>

<task type="auto">
  <name>Task 2: the two shared cash templates + both finance routes</name>
  <files>app/routes/finance.py, app/routes/mobile_finance.py, app/templates/partials/withdraw_form.html, app/templates/partials/deposit_form.html</files>
  <read_first>
    - app/templates/partials/withdraw_form.html:16 (`<form id="withdraw-form" class="stacked-form">`),
      :21-26 (the canonical field shape with its per-key error at `:25`), :23 (`withdraw-amount`),
      :34 (`withdraw-currency`), :59 (`withdraw-note`) — the id-prefix convention, :63
      (`.form-actions`), :64 (the submit)
    - app/templates/partials/deposit_form.html:10 (`<form id="deposit-form">`), :49
      (`deposit-note`), :53 (`.form-actions`), :54 (the submit)
    - app/templates/pages/finance.html:30,33 and app/templates/mobile_pages/finance.html:33,36 —
      both forms rendered on ONE page, twice over, parameterised by `finance_base`
    - app/routes/finance.py:201 and :273 — the two render sites and their POST handlers
    - app/routes/mobile_finance.py — the mobile twins
  </read_first>
  <action>
    Add the date field to BOTH templates as the LAST `.field` before `.form-actions`, with
    `class="field op-date"` and the canonical label/input/error shape — but with **PREFIXED ids**:
    `id="withdraw-op-date"` / `for="withdraw-op-date"` in `withdraw_form.html`, and
    `id="deposit-op-date"` / `for="deposit-op-date"` in `deposit_form.html`. `name="op_date"` is
    identical on both; only the `id` / `for` pair is prefixed.

    **This is the reason, and it must be a comment in both files:** `pages/finance.html:30,33`
    renders withdraw and deposit on the SAME page, and so does `mobile_pages/finance.html:33,36`. A
    shared `id="op_date"` would produce a duplicate id, break `<label for>` association on the second
    form, and land the focus ring on the wrong control. The two forms already solve this exact
    problem for every other field (`withdraw-amount` / `deposit-amount`, `withdraw-currency`,
    `withdraw-note` / `deposit-note`); the date field copies that convention verbatim.

    Use `value="{{ form.op_date or today_iso() }}"` and `max="{{ today_iso() }}"`. Both are
    `<form hx-post>` surfaces with a native submit button, so `max` shows the browser's own bubble.

    In `app/routes/finance.py` and `app/routes/mobile_finance.py`, add `op_date: str = Form("")` to
    both the withdraw and the deposit POST handlers and pass it to the service; echo it back through
    the existing `form` dict on the 422 path.

    One edit per template covers both the desktop and the mobile surface — do not duplicate the field
    into `mobile_pages/finance.html`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_finance.py tests/test_mobile_foundation.py tests/test_smoke.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - GET `/finance` renders BOTH `id="withdraw-op-date"` and `id="deposit-op-date"`, and
      `grep -c 'id="op_date"'` against that response is 0 (no duplicate id).
    - GET `/m/finance` renders the same two ids.
    - Each `<label for>` matches its own input's `id` (assert both pairs on the response text).
    - A POST with a future date to either endpoint returns 422 with «Дата операции не может быть в
      будущем.» and still contains the submitted date in the input's `value=`.
    - `git diff app/templates/mobile_pages/finance.html` is EMPTY (the shared template covers it).
  </acceptance_criteria>
  <done>Both cash forms carry the field on desktop and mobile, with distinct label associations.</done>
</task>

<task type="auto">
  <name>Task 3: VA-15 — every one of the 14 write surfaces renders op_date</name>
  <files>tests/test_business_date.py</files>
  <read_first>
    - 33-UI-SPEC.md § Surface Inventory — the authoritative table of all 14 surfaces with their
      templates, `value=` expressions and `id=` values
    - tests/test_receipts.py:441-445 — the shipped markup-assertion idiom (plain substring assertions
      against `response.text`, one per fact, no HTML parser)
    - tests/conftest.py — the `client`, `login` and `mobile_client_factory` fixtures
  </read_first>
  <action>
    Add `test_every_write_surface_renders_op_date` to `tests/test_business_date.py` (VA-15, DATE-01).

    Drive a GET (or the minimum wizard navigation needed to reach the screen) for each of the 14
    surfaces from `33-UI-SPEC.md`'s Surface Inventory, and assert on `response.text` that it contains
    `name="op_date"` and `value="<today's ISO date>"`. Structure it as a parameterised test so a
    failure names the offending surface, and keep the surface list in ONE place in the test module so
    it reads as the coverage contract it is.

    The 14: приход, продажа, списание, корректировка, перемещение and возврат on desktop; снятие and
    внесение (the two shared cash forms, asserted with their prefixed ids); приход, продажа and
    списание mobile shells; корректировка and перемещение mobile final steps; and mobile возврат.

    Two surfaces assert `id="withdraw-op-date"` / `id="deposit-op-date"` instead of `id="op_date"`,
    and the two возврат surfaces carry `class="field"` without the `op-date` modifier — encode those
    three documented exceptions explicitly in the expectations table rather than special-casing them
    in the assertion body, so the exceptions stay visible.

    Add a docstring stating that this test IS the D-16 surface contract: DATE-01's requirement text
    says «6 desktop forms and 5 mobile wizards», which undercounts what the code actually has, and
    the locked list is 14.
  </action>
  <verify>
    <automated>uv run pytest tests/test_business_date.py::test_every_write_surface_renders_op_date -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The test passes for all 14 surfaces and a deliberate removal of the field from any one template
      makes it fail naming that surface.
    - The surface list in the test has exactly 14 entries.
    - The three documented exceptions (two prefixed ids, two `op-date`-less возврат surfaces) are
      expressed as data in the table, not as branches in the assertion.
  </acceptance_criteria>
  <done>DATE-01's surface coverage is a runnable assertion, not a checklist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form POST → service validation → cash_movements write | Untrusted `op_date` reaches a stored money-ledger column |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-16 | Tampering | `op_date` reaching SQL | mitigate | `parse_op_date` (`date.fromisoformat` + `.isoformat()`), bound ORM parameter only |
| T-33-17 | Tampering | XSS via the echoed value on a 422 | mitigate | Jinja autoescaping, never `\|safe` |
| T-33-33 | Tampering (usability-grade) | A duplicate `id="op_date"` on the finance page breaking `<label for>` and landing focus on the wrong control | mitigate | Prefixed ids `withdraw-op-date` / `deposit-op-date`, matching the shipped convention for every other field on that page; asserted directly |
| T-33-34 | Repudiation | A cash column populated only by the backfill and by Phase 34 reversals, never by the operator | mitigate | D-16 includes the cash forms in the locked surface list; VA-15 proves all 14 surfaces carry the field |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | No package installed; `pyproject.toml` untouched |
</threat_model>

<verification>
- `uv run pytest tests/test_finance.py tests/test_business_date.py tests/test_smoke.py -q` green.
- `uv run pytest -q` green apart from the four known-red `tests/test_sync_ui.py` cases.
- Browser check B-4 (`/finance` and `/m/finance`: two date inputs, each label focusing its own) is
  carried by plan 33-15.
</verification>

<success_criteria>
- Снятие and внесение carry the field on desktop and mobile from one template edit each.
- No duplicate DOM id on the finance page.
- VA-15 proves all 14 write surfaces render `name="op_date"` pre-filled with today.
</success_criteria>

<output>
Create `.planning/phases/33-back-dated-operations/33-13-SUMMARY.md` when done.
</output>

---
phase: 33-back-dated-operations
plan: 08
type: execute
wave: 4
depends_on: ["33-06"]
files_modified:
  - app/services/operations.py
  - app/routes/history.py
  - app/routes/mobile_history.py
  - app/services/customers.py
  - app/templates/partials/customer_insights.html
  - app/services/warehouses.py
  - app/templates/partials/warehouse_rows.html
  - tests/test_history.py
  - tests/test_customers.py
  - tests/test_warehouses.py
autonomous: true
requirements: [DATE-03, DATE-04]

must_haves:
  truths:
    - "История's date filter selects rows by the business date, and the row count agrees with the rows"
    - "Customer spend totals bucket by the business date"
    - "«Последний заказ» shows the latest PURCHASE, not the latest-entered row"
    - "«Последняя приёмка» per warehouse shows the business date, rendered with ru_date"
    - "_SORT_MAP and _DEFAULT_ORDER are unchanged and every «recent N» feed still orders by created_at desc, seq desc"
  artifacts:
    - path: "app/services/operations.py"
      provides: "history_view period predicate on business_date_expr, in BOTH stmt and count_stmt"
    - path: "app/services/customers.py"
      provides: "_spend_stmt switched + last_order_date recomputed as MAX(business_date_expr)"
    - path: "app/services/warehouses.py"
      provides: "«Последняя приёмка» on the business date"
  key_links:
    - from: "app/services/operations.py::history_view"
      to: "app/services/reports.py::business_date_expr"
      via: "the same expression applied to BOTH stmt and count_stmt"
      pattern: "business_date_expr"
---

<objective>
DATE-03 for История, customer spend and the warehouse list, plus the three borderline readers D-24
settles. And the negative half of DATE-04: prove that nothing about display ORDER moved.

The subtle one is `history_view`: its period predicate exists twice — once on `stmt` and once on
`count_stmt` (`app/services/operations.py:151-154`). Switching only one makes the pager's total
disagree with its own rows, and nothing but an explicit `len(rows) == total` assertion catches it.

Purpose: DATE-03, DATE-04, D-22 (`_SORT_MAP` NOT changed), D-24 (three borderline readers switched).
Output: four service modules, two templates and the VA-17 ordering tripwire.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-back-dated-operations/33-CONTEXT.md
@.planning/phases/33-back-dated-operations/33-RESEARCH.md
@.planning/phases/33-back-dated-operations/33-PATTERNS.md
@.planning/phases/33-back-dated-operations/33-VALIDATION.md
@CLAUDE.md
</context>

<project_conventions>
- Portable ORM only; closed inclusive range (`>= start_day`, `<= end_day`) per plan 33-06.
- Any cell switched from a timestamp to the `String(10)` business date MUST switch its Jinja filter
  from `| local_dt` to `| ru_date` — `iso_to_local("2026-09-01")` builds a naive datetime and renders
  a bogus time.
- Bump `app/__init__.py::__version__` by 0.1 per completed-task commit.
</project_conventions>

<artifacts_this_phase_produces>
`business_date_bounds`, `local_today_iso`, `business_date_expr`, `push_schema_ok`, `parse_op_date`,
`today_iso` (Jinja global), `OP_DATE_FORMAT_ERROR`, `OP_DATE_FUTURE_ERROR`, `SCHEMA_AHEAD_ERROR`,
`SyncResult(status="schema_mismatch")`, the four ledger columns `operations.business_date` /
`cash_movements.business_date` / `operations.reverses_op_id` / `cash_movements.reverses_movement_id`,
the `dated` filter parameter, the `business_day` and `is_backdated` row-dict keys, the
`.field.op-date` CSS class, the `op_date` form field, migration `0027`, the `alembic_engine` fixture
and `run_alembic` helper in `tests/conftest.py`, and the three new test files
`tests/test_migrations.py`, `tests/test_business_date.py`, `tests/test_sync_schema_gate.py`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: history_view period predicate (BOTH stmt and count_stmt) + VA-17 ordering tripwire</name>
  <files>app/services/operations.py, app/routes/history.py, app/routes/mobile_history.py, tests/test_history.py</files>
  <read_first>
    - app/services/operations.py:28-32 — `_SORT_MAP` (spans `:28-30`) and `_DEFAULT_ORDER` (`:32`).
      CONTEXT cites `:29-32`, which starts one line inside the dict literal (CD-6). **Neither is
      changed by this phase**
    - app/services/operations.py:96-160 — `history_view`'s query construction, the outer joins, and
      the period predicate at `:151-154` present on BOTH `stmt` and `count_stmt`
    - app/services/operations.py:170-196 — the row-dict construction and the result dict
      (`type_filter`, `product_id`, `author_id`, `sort`)
    - app/routes/history.py:11 (import) and :103 (the bounds call site); :117-126 (the comment block)
      and :129-142 (`qs_parts`), :143 (`extra_qs`) — CONTEXT cites `:117-141`, the real spans are
      these (CD-11)
    - app/routes/mobile_history.py:25 (import) and :88 (the bounds call site)
    - app/services/ledger.py:238 — the actual «recent N» feed
      (`.order_by(Operation.created_at.desc(), Operation.seq.desc())`). **CONTEXT cites `:234`, which
      is `.limit(1)`, and `:233` is an unrelated `Product` ordering (CD-5) — guard `:238`**
  </read_first>
  <action>
    In `app/services/operations.py::history_view`, switch the period predicate to
    `business_date_expr(Operation)` with the closed contract (`>= start_day`, `<= end_day`), **in
    BOTH `stmt` and `count_stmt`**. Add a one-line comment at the predicate saying the two must move
    together or the pager's total disagrees with its own rows.

    **Do NOT touch `_SORT_MAP` or `_DEFAULT_ORDER` (D-22)**, and add no business-date sort option
    this phase. DATE-04 settles the conflict between the two research passes: the technical timestamp
    keeps all three of its jobs INCLUDING display order, so Pitfall 15's «any sort entry must end in
    `created_at desc, seq desc`» is not triggered here. Add a one-line comment above `_SORT_MAP`
    naming D-22 so the next reader does not "finish the job".

    Switch the import and bounds call site in `app/routes/history.py` (`:11`, `:103`) and in
    `app/routes/mobile_history.py` (`:25`, `:88`) to `business_date_bounds(from_date, to_date)`.
    Leave `qs_parts` alone in this task — the new `dated` key is plan 33-14's.

    Extend `tests/test_history.py`:
    - the VA-13 pair for the История date filter (back-dated row IN under the business-date period,
      OUT under the entry-date period);
    - the assertion nothing else catches: for a filtered period,
      `len(result["rows"]) == result["total"]` — the count agreeing with its own rows;
    - a last-day-inclusive assertion (closed contract).

    Add `test_recent_feeds_still_order_by_created_at` to `tests/test_history.py` (VA-17,
    D-22/DATE-04): write a row back-dated far into the past and assert it is STILL first in the
    «recent N» feeds — these answer «what did I just enter?», and switching them would make a
    just-entered back-dated row vanish from the very list the operator uses to confirm the entry
    landed. Cover at least `app/services/ledger.py:238` plus two of
    `finance.py:21`, `dashboard.py:156`, `sales.py:374`, `receipts.py:309`, `writeoffs.py:127`,
    `transfers.py:210`, `catalog.py:351`, `customers.py:352`. Also assert `_SORT_MAP` and
    `_DEFAULT_ORDER` still hold exactly their HEAD values.

    **Deliberate deviation from `33-VALIDATION.md`, recorded so it is not silent:** the validation
    doc places VA-17 in `tests/test_business_date.py`. It goes in `tests/test_history.py` instead so
    that this plan and plan 33-07 (which owns `tests/test_business_date.py` in the same wave) keep
    disjoint `files_modified`. The VA-ID, the test name and the behaviour proven are unchanged — only
    the file is, which `33-CONTEXT.md` § Claude's Discretion explicitly allows. The VA-17 command
    becomes `uv run pytest tests/test_history.py::test_recent_feeds_still_order_by_created_at -x`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_history.py tests/test_mobile_history.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_history.py tests/test_mobile_history.py -q` green.
    - The `len(result["rows"]) == result["total"]` assertion exists and passes for a filtered period.
    - `git diff app/services/operations.py` shows NO change to `_SORT_MAP` or `_DEFAULT_ORDER`
      (comment lines above them are allowed).
    - `test_recent_feeds_still_order_by_created_at` (VA-17) passes and covers `ledger.py:238`.
    - `grep -c "local_day_bounds_utc" app/routes/history.py app/routes/mobile_history.py` returns 0
      for each.
  </acceptance_criteria>
  <done>История filters by the business date with an honest count, and display order provably did not move.</done>
</task>

<task type="auto">
  <name>Task 2: customer spend predicate + «Последний заказ» (D-24)</name>
  <files>app/services/customers.py, app/templates/partials/customer_insights.html, tests/test_customers.py</files>
  <read_first>
    - app/services/customers.py:17 (import), :407-420 (`_spend_stmt`, predicate at `:415-416`),
      :426-470 (`spend_totals` at `:443-446` and `spend_view` at `:465-468` — the two bounds call
      sites and the two inlined local-today expressions)
    - app/services/customers.py:340-360 — `purchase_history`'s `created_at DESC, seq DESC` ordering
      at `:352`. **This ordering is NOT changed** (D-22)
    - app/services/customers.py:528-540 — `last_order_date`, which currently returns
      `history[0]["op"].created_at` and relies on `purchase_history`'s ordering
    - app/templates/partials/customer_insights.html:12 — where «Последний заказ» renders
    - app/services/reports.py — `business_date_expr`
  </read_first>
  <action>
    Switch `_spend_stmt`'s period predicate (`:415-416`) to `business_date_expr(Operation)` with the
    closed contract. Switch the two bounds call sites (`:446`, `:468`) and the import (`:17`) to
    `business_date_bounds`. `_spend_stmt` needs no test of its own — `spend_totals` and `spend_view`
    are its only two callers and cover it transitively.

    **`last_order_date` is recomputed, not merely re-rendered (D-24, and this resolves a real
    coupling).** It currently returns `history[0]["op"].created_at`, relying on `purchase_history`'s
    `created_at DESC, seq DESC` ordering (`:352`) to make `history[0]` the latest. Since D-22 keeps
    that ordering, switching only the DISPLAYED field would show the business date of the
    latest-ENTERED row, not the latest purchase. Replace the body with a self-contained
    `select(func.max(business_date_expr(Operation)))` over that customer's sale rows, leaving
    `purchase_history`'s ordering completely untouched.

    In `app/templates/partials/customer_insights.html:12`, switch the «Последний заказ» filter from
    `| local_dt` to `| ru_date` — the value is now a `String(10)` date and `iso_to_local` on it would
    render a bogus time. One date is enough on this surface: it is an identification label, not a
    DATE-05 both-dates surface.

    Extend `tests/test_customers.py`: the VA-13 pair for spend totals; and a test that pins the
    coupling — a customer whose LATEST-ENTERED sale is back-dated to an old date must show the most
    recent PURCHASE as «Последний заказ», not that row's business date.
  </action>
  <verify>
    <automated>uv run pytest tests/test_customers.py tests/test_mobile_customers.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_customers.py tests/test_mobile_customers.py -q` green.
    - `git diff app/services/customers.py` shows NO change at `:352` (`purchase_history`'s ordering).
    - `last_order_date`'s body contains `func.max` and `business_date_expr` and no longer indexes
      `history[0]`.
    - `grep -n "local_dt" app/templates/partials/customer_insights.html` no longer matches line 12.
    - `grep -c "local_day_bounds_utc" app/services/customers.py` returns 0.
  </acceptance_criteria>
  <done>Customer spend buckets by the business date and «Последний заказ» means the latest purchase.</done>
</task>

<task type="auto">
  <name>Task 3: «Последняя приёмка» per warehouse (D-24) + its misleading comment</name>
  <files>app/services/warehouses.py, app/templates/partials/warehouse_rows.html, tests/test_warehouses.py</files>
  <read_first>
    - app/services/warehouses.py:13-15 (imports) and :100 (`func.max(Operation.created_at)`,
      receipts only)
    - app/templates/partials/warehouse_rows.html:29 (the «Последняя приёмка» column header), :71-74
      (the comment that explicitly instructs «this is a full ISO timestamp, use `local_dt` not
      `ru_date`»), :75 (the render)
    - app/services/reports.py — `business_date_expr`
  </read_first>
  <action>
    Switch `app/services/warehouses.py:100` from `func.max(Operation.created_at)` to
    `func.max(business_date_expr(Operation))`. «When did goods last arrive here» is a business fact,
    and `MAX()` over ISO date strings stays correct because ISO dates sort lexicographically.

    **This overrides `.planning/research/ARCHITECTURE.md:195`, which said to leave it** — that was
    open operator decision #5 and D-24 decided it the other way. Say so in a one-line comment naming
    D-24, so the next reader does not "restore" the older document's advice.

    Two coupled template edits, both mandatory:
    - `warehouse_rows.html:75` — `| local_dt` → `| ru_date`.
    - `warehouse_rows.html:71-74` — **rewrite the comment.** It currently instructs the exact
      opposite («this is a full ISO timestamp, use `local_dt` not `ru_date`») and becomes actively
      misleading the moment the value changes type. The new comment must say: this is
      `business_date_expr`'s `String(10)` date (D-24), so `ru_date` is correct and `local_dt` would
      build a naive datetime and print a bogus time.

    `app/services/warehouses.py` importing `app.services.reports` creates no cycle — `reports.py`
    imports only `app.config`, `app.core` and `app.models`.

    Extend `tests/test_warehouses.py`: a receipt back-dated to an older date updates «Последняя
    приёмка» to that business date, and the rendered cell contains `dd.mm.yyyy` with no time part.
  </action>
  <verify>
    <automated>uv run pytest tests/test_warehouses.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_warehouses.py -q` green.
    - The rendered «Последняя приёмка» cell matches `\d{2}\.\d{2}\.\d{4}` and contains no `:` time
      separator.
    - `grep -n "local_dt" app/templates/partials/warehouse_rows.html` no longer matches line 75.
    - The comment at `warehouse_rows.html:71-74` no longer instructs the use of `local_dt`, and names
      D-24.
  </acceptance_criteria>
  <done>The warehouse list answers a business question with a business date, and its comment agrees.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| История filter form (`from_date` / `to_date`) → SQL predicate + paginated count | Operator input drives both the row set and its total |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-16 | Tampering | SQL injection via a period bound | mitigate | Bounds are parsed `date` objects re-serialised with `.isoformat()` and bound as ORM parameters; never interpolated |
| T-33-22 | Tampering (silent wrong answer) | `stmt` switched, `count_stmt` not — pager total disagrees with its own rows | mitigate | Both predicates switched in one edit; pinned by the `len(rows) == total` assertion |
| T-33-23 | Repudiation | A just-entered back-dated row disappearing from the «recent N» feed the operator uses to confirm the entry landed | mitigate | VA-17 pins every «recent N» feed on `created_at desc, seq desc`; `_SORT_MAP` / `_DEFAULT_ORDER` unchanged (D-22) |
| T-33-24 | Information disclosure | A date-only string rendered through `local_dt` printing a fabricated time | mitigate | Every switched cell also switches to `\| ru_date`, and the misleading comment at `warehouse_rows.html:71-74` is rewritten rather than left to mislead |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | No package installed; `pyproject.toml` untouched |
</threat_model>

<verification>
- `uv run pytest tests/test_history.py tests/test_mobile_history.py tests/test_customers.py tests/test_warehouses.py -q` green.
- `uv run pytest -q` green apart from the four known-red `tests/test_sync_ui.py` cases.
- `grep -rn "local_day_bounds_utc" app/` returns NOTHING after this plan and plan 33-07 — all 14
  call sites have moved. The helper itself still exists in `app/core.py` and is still used by 36 test
  call sites across 6 files.
</verification>

<success_criteria>
- История's date filter, its count, customer spend, «Последний заказ» and «Последняя приёмка» all
  bucket by / display the business date.
- `_SORT_MAP`, `_DEFAULT_ORDER`, `purchase_history`'s ordering and every «recent N» feed are unchanged
  and pinned by VA-17.
- No `app/` module calls `local_day_bounds_utc` any more; `app/core.py` still defines it.
</success_criteria>

<output>
Create `.planning/phases/33-back-dated-operations/33-08-SUMMARY.md` when done.
</output>

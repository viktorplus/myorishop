---
phase: 33-back-dated-operations
plan: 11
type: execute
wave: 4
depends_on: ["33-06"]
files_modified:
  - app/services/sales.py
  - app/services/returns.py
  - app/routes/sales.py
  - app/routes/returns.py
  - app/routes/mobile_sales.py
  - app/routes/mobile_returns.py
  - app/templates/partials/sale_form.html
  - app/templates/partials/return_form.html
  - app/templates/mobile_pages/sales.html
  - app/templates/mobile_partials/sale_basket.html
  - app/templates/mobile_partials/return_confirm.html
  - tests/test_sales.py
  - tests/test_returns.py
  - tests/test_mobile_sales.py
  - tests/test_mobile_returns.py
autonomous: true
requirements: [DATE-01, DATE-02]

must_haves:
  truths:
    - "Продажа and возврат carry a visible «Дата операции» input pre-filled with today, on desktop and on mobile"
    - "A sale writes the same business date to its operation rows AND to its cash movement"
    - "The date typed on the mobile sale wizard survives the basket↔product round-trip and is never reset to today"
    - "«Возврат из продажи от …» names the origin sale's business date"
    - "Mobile возврат ships with the date field, at parity with its desktop twin"
  artifacts:
    - path: "app/services/sales.py"
      provides: "op_date threaded into record_operation AND record_cash_movement"
    - path: "app/templates/mobile_pages/sales.html"
      provides: "the date input inside the persistent sale-wizard shell"
  key_links:
    - from: "app/templates/mobile_pages/sales.html"
      to: "the persistent <form id=\"sale-wizard-form\"> shell"
      via: "placement above #wizard-step so the basket re-render from _acc_context cannot reset it"
      pattern: "op_date"
---

<objective>
DATE-01 / DATE-02 for продажа and возврат — including the one surface that would otherwise ship
dateless (mobile возврат, whose desktop twin has the field) and the one that would silently reset a
typed date (the mobile sale basket).

Purpose: D-10, D-11, D-13, D-14, D-16 (the возврат surfaces are explicitly in the locked list), D-17
(a sale writes through BOTH `record_operation` and `record_cash_movement`), D-24 (the «Возврат из
продажи от …» identification label).
Output: four write surfaces carrying `op_date`, and the return label switched to the business date.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-back-dated-operations/33-UI-SPEC.md
@.planning/phases/33-back-dated-operations/33-CONTEXT.md
@.planning/phases/33-back-dated-operations/33-PATTERNS.md
@CLAUDE.md
</context>

<project_conventions>
- Label copy «Дата операции»; no `required`; no «(необязательно)» hint.
- Service-layer errors under `errors["op_date"]`, 422, redisplay what was typed, zero writes.
- The `.field.op-date` full-row modifier already exists in `app/static/style.css` (plan 33-10). Do
  NOT add another CSS rule.
- Bump `app/__init__.py::__version__` by 0.1 per completed-task commit.
</project_conventions>

<artifacts_this_phase_produces>
`business_date_bounds`, `local_today_iso`, `business_date_expr`, `push_schema_ok`, `parse_op_date`,
`today_iso` (Jinja global), `OP_DATE_FORMAT_ERROR`, `OP_DATE_FUTURE_ERROR`, `SCHEMA_AHEAD_ERROR`,
`SyncResult(status="schema_mismatch")`, the four ledger columns `operations.business_date` /
`cash_movements.business_date` / `operations.reverses_op_id` / `cash_movements.reverses_movement_id`,
the `dated` filter parameter, the `business_day` and `is_backdated` row-dict keys, the
`.field.op-date` CSS class, the `op_date` form field, migration `0027`, the `alembic_engine` fixture
and `run_alembic` helper in `tests/conftest.py`, and the three new test files
`tests/test_migrations.py`, `tests/test_business_date.py`, `tests/test_sync_schema_gate.py`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: service layer — sales.py and returns.py, both write paths</name>
  <files>app/services/sales.py, app/services/returns.py, tests/test_sales.py, tests/test_returns.py</files>
  <read_first>
    - app/services/sales.py:33-38 (imports), the validation block that builds `errors`, :287 (the
      `record_operation` call) and :310 (the `record_cash_movement` call)
    - app/services/returns.py:28-32 (imports), :117-180 — `returnable_qty`, the validation block,
      :156 (`record_operation`) and :174 (`record_cash_movement`)
    - app/services/ledger.py — `parse_op_date` and the `business_date` kwarg (plan 33-06)
    - app/services/finance.py:48-57 — `record_cash_movement`'s signature with its own
      `business_date` kwarg (plan 33-06)
  </read_first>
  <action>
    Give each service's public entry function an `op_date: str = ""` keyword, parse it once with
    `parse_op_date(op_date, errors)` inside the existing validation block before any write, and
    thread the SAME parsed value into BOTH write paths:
    - `app/services/sales.py:287` (`record_operation`) and `:310` (`record_cash_movement`);
    - `app/services/returns.py:156` (`record_operation`) and `:174` (`record_cash_movement`).

    One parse, one value, both rows — a sale whose ledger rows and whose cash movement disagreed
    about the business date would make the cash-flow report and the sales-profit report disagree
    about the same day, which is precisely what DATE-03 exists to prevent. Add a one-line comment
    saying so at each pairing.

    A multi-line sale basket writes several `Operation` rows through the same call site; they all
    take the same business date.

    Extend `tests/test_sales.py` and `tests/test_returns.py` (VA-14):
    - a back-dated sale writes the SAME `business_date` on every ledger row AND on the cash movement;
    - an empty `op_date` stamps today's local day on both;
    - a future date sets `errors["op_date"] == OP_DATE_FUTURE_ERROR` and writes NOTHING — assert both
      the `Operation` count and the `CashMovement` count are unchanged;
    - a malformed date sets `OP_DATE_FORMAT_ERROR`;
    - a back-dated return still respects `returnable_qty` (the cap is unchanged by this phase).
  </action>
  <verify>
    <automated>uv run pytest tests/test_sales.py tests/test_returns.py tests/test_sales_total.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_sales.py tests/test_returns.py tests/test_sales_total.py -q` green.
    - The future-date tests assert BOTH an unchanged `Operation` count and an unchanged
      `CashMovement` count.
    - `grep -n "business_date" app/services/sales.py app/services/returns.py` shows the kwarg at all
      four call sites (`sales.py:287,310`; `returns.py:156,174`).
    - `parse_op_date` is called exactly once per service entry function.
  </acceptance_criteria>
  <done>A sale and a return date their ledger rows and their cash movement identically.</done>
</task>

<task type="auto">
  <name>Task 2: desktop sale + return forms, and the D-24 return label</name>
  <files>app/routes/sales.py, app/routes/returns.py, app/templates/partials/sale_form.html, app/templates/partials/return_form.html</files>
  <read_first>
    - app/templates/partials/sale_form.html:40-41 (`<form id="sale-form">` with `hx-post` at `:41`
      — note it does NOT carry `stacked-form`; it deliberately uses the flex form so the basket
      table, the running total and the hint flow together), :85 (the muted «Если товара с таким кодом
      ещё нет…» hint the date field must not sit beside), :87 (`.form-actions`), :89 (the submit)
    - app/templates/partials/return_form.html:11 (the «Возврат из продажи от …» label), :28 (the
      `<form>`, no class), :33-40 (the deliberate one-row compact layout: hidden input + qty field
      with `max` at `:35` + `.form-actions` at `:38`), :35 (`{{ remaining }}` — the FLAT context idiom
      this file uses, not `form.x`)
    - app/routes/sales.py — the POST handler and its 422 re-render
    - app/routes/returns.py:75 and :152 — where the origin sale's date is resolved for the label;
      :99, :102, :132, :142, :160 — the five render sites of `return_form.html`
    - 33-UI-SPEC.md § Surface Inventory rows 2 and 6, and the возврат layout exception
  </read_first>
  <action>
    **`sale_form.html`** — add the date field as the LAST `.field` before `.form-actions` (`:87`),
    with `class="field op-date"`, `id="op_date"`, `name="op_date"`,
    `value="{{ form.op_date or today_iso() }}"`, `max="{{ today_iso() }}"` and the per-key
    `{% if errors.op_date %}<p class="error">…</p>{% endif %}`. The `op-date` modifier is
    load-bearing here: `#sale-form` is a bare `<form>` inheriting `form { display:flex; flex-wrap:
    wrap }`, so without `flex-basis: 100%` the field would sit beside the muted hint at `:85`.

    **`return_form.html`** — add the field before `.form-actions` (`:38`) using
    `class="field"` **WITHOUT** the `op-date` modifier, and the flat-context value expression
    `{{ op_date | default(today_iso(), true) }}` mirroring `{{ remaining }}` at `:35`. This is the
    ONLY intentional layout divergence in the 14 surfaces: the form's existing one-row compact
    layout (hidden input + qty + actions) is the correct, denser reading for a two-field
    confirmation, so the date field MAY share the row with «Количество к возврату». Add a Jinja
    comment stating that this divergence is deliberate and pointing at the UI-SPEC exception, so a
    later reader does not "normalise" it.

    **D-24 — the return identification label.** At `app/routes/returns.py:75` and `:152`, resolve the
    origin sale's date as `origin.business_date or origin.created_at[:10]` and render it with
    `| ru_date` at `return_form.html:11`. One date is enough here: it is an identification label, not
    a DATE-05 both-dates surface. If the label currently renders with `| local_dt`, switch it —
    `iso_to_local` on a 10-char date builds a naive datetime and prints a bogus time.

    In both route handlers add `op_date: str = Form("")` and pass it to the service; echo it back on
    the 422 path (`form.op_date` for the sale, the flat `op_date` context key for возврат, matching
    each template's existing idiom).
  </action>
  <verify>
    <automated>uv run pytest tests/test_sales.py tests/test_returns.py tests/test_smoke.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - GET `/sales` and the возврат form render `name="op_date"` with today's ISO date in `value=` and
      in `max=`.
    - A POST with a future date returns 422, renders «Дата операции не может быть в будущем.» and
      still contains the submitted date in the input's `value=`.
    - `grep -n 'class="field op-date"' app/templates/partials/sale_form.html` returns one hit;
      `grep -c "op-date" app/templates/partials/return_form.html` returns 0 (the documented exception).
    - The «Возврат из продажи от …» label of a back-dated origin sale renders that sale's BUSINESS
      date, formatted `dd.mm.yyyy` with no time part.
    - `git diff app/static/style.css` is EMPTY for this plan.
  </acceptance_criteria>
  <done>Both desktop surfaces carry the field, with возврат keeping its deliberate compact layout.</done>
</task>

<task type="auto">
  <name>Task 3: mobile sale shell + mobile возврат, and the mobile D-24 label</name>
  <files>app/routes/mobile_sales.py, app/routes/mobile_returns.py, app/templates/mobile_pages/sales.html, app/templates/mobile_partials/sale_basket.html, app/templates/mobile_partials/return_confirm.html, tests/test_mobile_sales.py, tests/test_mobile_returns.py</files>
  <read_first>
    - app/templates/mobile_pages/sales.html:11-12 — `<form id="sale-wizard-form">` at `:11` wrapping
      `<div id="wizard-step">` at `:12`
    - app/routes/mobile_sales.py:39 — `_acc_context`, from which the basket is RE-RENDERED on every
      «Добавить товар». This is why the date cannot live on the basket screen
    - app/templates/mobile_partials/sale_basket.html:6 (the «Корзина» step indicator) and :9-11 (the
      `.error-block` that today carries only `errors.form` / `errors.basket`)
    - app/templates/mobile_partials/return_confirm.html:17 (the «Возврат из продажи от …» label),
      :33 (`<form hx-post="/m/returns">`), :38-42 (the «Количество к возврату» field, `max` at `:40`),
      :43 (`<div class="mobile-actions">`), :14 and :41 (the existing per-key error idiom)
    - app/routes/mobile_returns.py:80 and :156 (the origin date for the label); :104, :107, :135,
      :145, :162 (the five render sites)
    - 33-UI-SPEC.md § Surface Inventory rows 10 and 14, and § Interaction Contract §5
  </read_first>
  <action>
    **Mobile продажа (D-11).** Put the date field INSIDE `<form id="sale-wizard-form">` at
    `mobile_pages/sales.html:11`, immediately after it opens and BEFORE `<div id="wizard-step">`, with
    `class="field op-date"`, `value="{{ today_iso() }}"`, `max="{{ today_iso() }}"`.

    This placement is not a style preference — it is the whole reason D-11 rejected «date on the final
    screen of all five wizards». The sale basket is re-rendered from `_acc_context`
    (`app/routes/mobile_sales.py:39`) on every «Добавить товар», so a date typed on the basket screen
    silently resets to today the moment a second item is added. The shell is never swapped by htmx, so
    the value survives the entire basket↔product round-trip with ZERO hidden-field threading. Put that
    sentence in the Jinja comment.

    **Error placement on the sale wizard (D-14 + CF-UI-1).** Emit
    `{% if errors.op_date %}<p class="error">{{ errors.op_date }}</p>{% endif %}` as the FIRST element
    inside `#wizard-basket` in `sale_basket.html`, above the «Корзина» step indicator at `:6`. It must
    be a `<p class="error">`, NOT an `.error-block` — the message points at a specific field, not at
    the basket as a whole. Leave the existing `errors.form` / `errors.basket` block alone.

    **Mobile возврат (D-16).** Add the field as the LAST `.field` inside
    `<form hx-post="/m/returns">` in `return_confirm.html`, after the «Количество к возврату» field
    (`:38-42`) and before `<div class="mobile-actions">` (`:43`), using `class="field"` **without**
    the `op-date` modifier (the same deliberate compact-layout exception as its desktop twin) and the
    flat-context value `{{ op_date | default(today_iso(), true) }}`. Add the per-key
    `<p class="error">` branch under the input, copying this file's own existing per-key idiom at
    `:14` / `:41`. Without this surface, возврат ships dateless on mobile while its desktop twin has
    the field.

    Also add `aria-describedby` on the date input of the mobile shell wizard, associating it with the
    error `<p>` id, so the error is not merely adjacent in the DOM but associated in the accessibility
    tree. (Non-blocking UI-checker recommendation, folded in here because it costs one attribute.)

    **D-24 — the mobile return label.** At `app/routes/mobile_returns.py:80` and `:156`, resolve the
    origin sale's date as `origin.business_date or origin.created_at[:10]`, and render it with
    `| ru_date` at `return_confirm.html:17`.

    In both mobile route handlers add `op_date: str = Form("")` on the final POST and pass it to the
    service. Do not add a hidden field anywhere; do not add a wizard step; do not touch any
    «Шаг N из M» string.

    Extend `tests/test_mobile_sales.py` and `tests/test_mobile_returns.py`: the sale shell renders
    `name="op_date"` on step 1; the value survives adding a second basket item (drive the
    basket↔product round-trip and assert the date is still the one that was set, not today); the
    mobile возврат screen renders the field; a future date returns 422 with the RU message rendered
    as a per-key `<p class="error">`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_mobile_sales.py tests/test_mobile_returns.py tests/test_mobile_wiring.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_mobile_sales.py tests/test_mobile_returns.py tests/test_mobile_wiring.py -q` green.
    - `grep -n 'name="op_date"' app/templates/mobile_pages/sales.html` returns one hit BEFORE the
      `<div id="wizard-step">` line.
    - The basket round-trip test asserts the previously-set date is still present after a second item
      is added.
    - `grep -c 'type="hidden".*op_date' app/templates/mobile_partials/` returns 0.
    - `grep -n "aria-describedby" app/templates/mobile_pages/sales.html` returns a hit.
    - The mobile «Возврат из продажи от …» label renders the origin's business date as `dd.mm.yyyy`.
  </acceptance_criteria>
  <done>The mobile sale keeps its date across the basket round-trip and mobile возврат reaches desktop parity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form POST → service validation → ledger + cash write | Untrusted `op_date` reaches two stored columns and two templates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-16 | Tampering | `op_date` reaching SQL | mitigate | `parse_op_date` (`date.fromisoformat` + `.isoformat()`), bound ORM parameter only |
| T-33-17 | Tampering | XSS via the echoed value or the origin-sale label on a 422 | mitigate | Jinja autoescaping, never `\|safe`; the label renders a normalised 10-char date through `ru_date` |
| T-33-29 | Repudiation | A sale whose ledger rows and cash movement carry different business dates | mitigate | One parse, one value, threaded into both write paths; pinned by a test asserting equality across both rows |
| T-33-30 | Tampering (silent data loss) | The mobile sale basket re-render resetting a typed date to today | mitigate | D-11's shell placement — the node htmx never swaps; pinned by the basket round-trip test |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | No package installed; `pyproject.toml` untouched |
</threat_model>

<verification>
- `uv run pytest tests/test_sales.py tests/test_returns.py tests/test_mobile_sales.py tests/test_mobile_returns.py tests/test_smoke.py -q` green.
- `uv run pytest -q` green apart from the four known-red `tests/test_sync_ui.py` cases.
- Browser check B-3 (the mobile basket round-trip felt in a real browser) is carried by plan 33-15.
</verification>

<success_criteria>
- Four surfaces (desktop sale, desktop возврат, mobile sale shell, mobile возврат) render the field.
- A sale writes one business date to every ledger row and to its cash movement.
- The mobile sale's date survives the basket↔product round-trip.
- «Возврат из продажи от …» names the origin's business date on both surfaces.
</success_criteria>

<output>
Create `.planning/phases/33-back-dated-operations/33-11-SUMMARY.md` when done.
</output>

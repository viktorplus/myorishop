---
phase: 33-back-dated-operations
plan: 12
type: execute
wave: 4
depends_on: ["33-06"]
files_modified:
  - app/services/corrections.py
  - app/services/transfers.py
  - app/routes/corrections.py
  - app/routes/transfers.py
  - app/routes/mobile_corrections.py
  - app/routes/mobile_transfers.py
  - app/templates/partials/correction_form.html
  - app/templates/partials/transfer_form.html
  - app/templates/mobile_partials/corrections_step_value.html
  - app/templates/mobile_partials/transfers_step_dest.html
  - tests/test_corrections.py
  - tests/test_transfers.py
  - tests/test_mobile_corrections.py
  - tests/test_mobile_transfers.py
autonomous: true
requirements: [DATE-01, DATE-02]

must_haves:
  truths:
    - "Корректировка and перемещение carry a visible «Дата операции» input pre-filled with today, on desktop and on mobile"
    - "A transfer's TWO ledger rows carry the same business date"
    - "On the two shell-less mobile wizards the date lives on the FINAL step, which is terminal"
    - "The op_date error renders next to the field, not as a top-of-screen block"
    - "No wizard gains a step; all 17 «Шаг N из M» strings and the 3 step_label literals are unchanged"
  artifacts:
    - path: "app/services/transfers.py"
      provides: "one business date threaded into both record_operation calls"
    - path: "app/templates/mobile_partials/corrections_step_value.html"
      provides: "the date field plus op_date excluded from the loop-all .error-block"
  key_links:
    - from: "app/templates/mobile_partials/corrections_step_value.html"
      to: "errors['op_date']"
      via: "per-key <p class=\"error\"> under the input + exclusion from the loop-all block"
      pattern: "op_date"
---

<objective>
DATE-01 / DATE-02 for корректировка and перемещение — the two mobile wizards that have NO persistent
shell form and therefore take the other half of D-11's deliberate two-idiom split.

Purpose: D-10, D-11 (final-step placement for these two, and an explicit prohibition on manufacturing
a shell for them), D-13, D-14 (корректировка's loop-all `.error-block` is one of the two non-uniform
cases), D-16, D-17 (a transfer writes TWO ledger rows).
Output: four write surfaces carrying `op_date`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-back-dated-operations/33-UI-SPEC.md
@.planning/phases/33-back-dated-operations/33-CONTEXT.md
@.planning/phases/33-back-dated-operations/33-PATTERNS.md
@CLAUDE.md
</context>

<project_conventions>
- Label copy «Дата операции»; no `required`; no «(необязательно)» hint.
- Service-layer errors under `errors["op_date"]`, 422, redisplay what was typed, zero writes.
- The `.field.op-date` CSS rule already exists (plan 33-10). Do NOT add another CSS rule.
- Bump `app/__init__.py::__version__` by 0.1 per completed-task commit.
</project_conventions>

<artifacts_this_phase_produces>
`business_date_bounds`, `local_today_iso`, `business_date_expr`, `push_schema_ok`, `parse_op_date`,
`today_iso` (Jinja global), `OP_DATE_FORMAT_ERROR`, `OP_DATE_FUTURE_ERROR`, `SCHEMA_AHEAD_ERROR`,
`SyncResult(status="schema_mismatch")`, the four ledger columns `operations.business_date` /
`cash_movements.business_date` / `operations.reverses_op_id` / `cash_movements.reverses_movement_id`,
the `dated` filter parameter, the `business_day` and `is_backdated` row-dict keys, the
`.field.op-date` CSS class, the `op_date` form field, migration `0027`, the `alembic_engine` fixture
and `run_alembic` helper in `tests/conftest.py`, and the three new test files
`tests/test_migrations.py`, `tests/test_business_date.py`, `tests/test_sync_schema_gate.py`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: service layer — corrections.py and transfers.py</name>
  <files>app/services/corrections.py, app/services/transfers.py, tests/test_corrections.py, tests/test_transfers.py</files>
  <read_first>
    - app/services/corrections.py:15-21 (imports), the validation block, and :120 (the
      `record_operation` call)
    - app/services/transfers.py:14-22 (imports), the validation block including the destination-cost
      rule, and :176 and :184 — **the TWO `record_operation` calls that form one transfer**
    - app/services/ledger.py — `parse_op_date` and the `business_date` kwarg (plan 33-06)
  </read_first>
  <action>
    Give each service's public entry function an `op_date: str = ""` keyword, parse it once with
    `parse_op_date(op_date, errors)` inside the existing validation block before any write, and
    thread the parsed value into every `record_operation(...)` as `business_date=<parsed>`:
    `app/services/corrections.py:120`, `app/services/transfers.py:176` and `:184`.

    **A transfer writes TWO rows and they must carry the SAME business date.** Parse once and reuse
    the single value at both call sites — never re-parse or re-derive per row. A transfer whose two
    halves landed on different business dates would show as stock leaving one warehouse in one period
    and arriving in another, and Phase 34's reversal (which reverses a transfer as one unit or not at
    all) would inherit the inconsistency. Comment it at the pairing.

    Extend `tests/test_corrections.py` and `tests/test_transfers.py` (VA-14):
    - a past `op_date` lands on the ledger row(s);
    - for a transfer, BOTH rows carry the identical `business_date`;
    - an empty value stamps today's local day;
    - a future value sets `errors["op_date"] == OP_DATE_FUTURE_ERROR` with an unchanged `Operation`
      row count (zero writes);
    - a malformed value sets `OP_DATE_FORMAT_ERROR`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_corrections.py tests/test_transfers.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_corrections.py tests/test_transfers.py -q` green.
    - A dedicated test asserts a transfer's two rows share one `business_date`.
    - `grep -n "business_date" app/services/transfers.py` shows the kwarg at BOTH `:176` and `:184`.
    - The future-date tests assert an unchanged `Operation` count.
  </acceptance_criteria>
  <done>Both services date their rows from one parse, and a transfer's halves cannot drift apart.</done>
</task>

<task type="auto">
  <name>Task 2: desktop correction + transfer forms</name>
  <files>app/routes/corrections.py, app/routes/transfers.py, app/templates/partials/correction_form.html, app/templates/partials/transfer_form.html</files>
  <read_first>
    - app/templates/partials/correction_form.html:21-23 (`<form class="stacked-form">` + `hx-post`),
      :97 (the «(необязательно)» hint the date label must NOT copy), :101 (`.form-actions`), :102
      (the submit)
    - app/templates/partials/transfer_form.html:19-21 (form + `hx-post`), :71 (`.form-actions`), :72
      (the submit)
    - app/templates/partials/receipt_form.html:32-44 — the canonical `.field` shape
    - app/routes/corrections.py and app/routes/transfers.py — the POST handlers and their 422
      re-render paths
  </read_first>
  <action>
    In BOTH templates add the date field as the LAST `.field` before `.form-actions`, byte-identical
    to the canonical shape: `class="field op-date"`, `<label for="op_date">Дата операции</label>`,
    `<input type="date" id="op_date" name="op_date" value="{{ form.op_date or today_iso() }}"
    max="{{ today_iso() }}">`, then
    `{% if errors.op_date %}<p class="error">{{ errors.op_date }}</p>{% endif %}`.

    Both are `.stacked-form` surfaces, where the `op-date` modifier is inert; include it anyway so all
    the desktop surfaces stay identical.

    Add the standard Jinja comment naming DATE-01/D-10 and stating that `max=` is safe here because
    `hx-post` is on the `<form>` and the submit is a native button, so the browser validates first.
    Do NOT repeat the false «`<details>` appears nowhere» claim (CF-1).

    In both route handlers add `op_date: str = Form("")`, pass it to the service, and echo it back
    through the existing `form` dict on the 422 path.
  </action>
  <verify>
    <automated>uv run pytest tests/test_corrections.py tests/test_transfers.py tests/test_smoke.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - GET `/corrections` and `/transfers` render `name="op_date"` with today's ISO date in `value=`
      and `max=`.
    - A POST with a future date returns 422, renders «Дата операции не может быть в будущем.» and
      still contains the submitted date in `value=`.
    - `grep -c "необязательно" app/templates/partials/correction_form.html` is unchanged from HEAD.
    - `git diff app/static/style.css` is EMPTY for this plan.
  </acceptance_criteria>
  <done>Both desktop forms carry the field in the same position as every other desktop surface.</done>
</task>

<task type="auto">
  <name>Task 3: mobile FINAL-STEP placement + the loop-all error exclusion</name>
  <files>app/routes/mobile_corrections.py, app/routes/mobile_transfers.py, app/templates/mobile_partials/corrections_step_value.html, app/templates/mobile_partials/transfers_step_dest.html, tests/test_mobile_corrections.py, tests/test_mobile_transfers.py</files>
  <read_first>
    - app/templates/mobile_partials/corrections_step_value.html:13-17 (the single `.error-block`
      looping ALL messages — the D-14 non-uniform case), :21 (`<form id="corrections-value-form">`),
      :33 (the guarded value idiom `form.value if form is defined and form.value else ''` this
      field's `value=` mirrors), :36-39 (the «Примечание» field), :40 (`<div class="mobile-actions">`)
    - app/templates/mobile_partials/transfers_step_dest.html:21-23, :50, :56 (already per-key errors,
      using an `errors is defined` guard), :28 (`<form id="transfer-dest-form">`), :61
      (`{{ new_expiry | default('') }}` — this file's flat-context idiom), :72-79 (the
      «Себестоимость…» field), :81 (`<div class="mobile-actions">`)
    - app/templates/mobile_partials/corrections_step_product.html:14 and
      app/templates/mobile_partials/transfers_step_product.html:12 — the per-step forms that prove
      these two wizards have NO shell
    - app/routes/mobile_transfers.py:65, :106, :130 — the three `step_label` literals. **Untouched**
    - app/templates/mobile_partials/receipts_step_confirm.html:20 — the key-exclusion pattern to copy
  </read_first>
  <action>
    **Placement (D-11, the other half of the split).** These two wizards have no persistent shell —
    only per-step `<form>`s — so the date field goes on the FINAL step, whose screens are terminal
    with no accumulator round-trip:
    - `corrections_step_value.html` — LAST `.field` inside `<form id="corrections-value-form">`
      (`:21`), after the «Примечание» field (`:36-39`), before `<div class="mobile-actions">` (`:40`).
      Value expression uses this file's own guarded idiom:
      `{{ form.op_date if form is defined and form.op_date else today_iso() }}`.
    - `transfers_step_dest.html` — LAST `.field` inside `<form id="transfer-dest-form">` (`:28`),
      after the «Себестоимость…» field (`:72-79`), before `<div class="mobile-actions">` (`:81`).
      Value expression uses this file's flat-context idiom:
      `{{ op_date | default(today_iso(), true) }}`.

    **Do NOT manufacture a shell form for these two.** D-11 is explicit: accept the two-idiom split;
    that refactor has its own blast radius. And do NOT add a dedicated «Дата операции» wizard step —
    it would rewrite 17 hardcoded «Шаг N из M» strings across 13 files plus the three `step_label`
    literals at `app/routes/mobile_transfers.py:65,106,130`, and add a mandatory tap forever to a
    field that is correct by default about 95% of the time.

    **`max=` IS effective on these two** (unlike the three shell wizards): both carry `hx-post` on the
    `<form>` with a native `<button type="submit">`, so the browser's interactive validation fires
    first and shows its own bubble. Note that in the comment.

    **Error rendering, per file (D-14).**
    - `corrections_step_value.html` — add the per-key `<p class="error">{{ errors.op_date }}</p>`
      directly under the date input, AND exclude the key from the loop-all block at `:13-17` by
      changing its iteration from `errors.values()` to `errors.items()` filtered with
      `if key != "op_date"`. Same exclusion shape `receipts_step_confirm.html:20` already uses for
      `form`. Without this, the date error would surface as a detached top-of-screen block AND be
      duplicated.
    - `transfers_step_dest.html` — already per-key; add
      `{% if errors is defined and errors.op_date %}<p class="error">{{ errors.op_date }}</p>{% endif %}`
      under the date input, using this file's own `errors is defined` guard.

    Add `aria-describedby` on both date inputs, associating each with its error `<p>` id.

    In both mobile route handlers add `op_date: str = Form("")` on the final POST and pass it to the
    service.

    Extend `tests/test_mobile_corrections.py` and `tests/test_mobile_transfers.py`: the final step
    renders `name="op_date"` pre-filled with today; a future date returns 422 with the RU message
    rendered as a per-key `<p class="error">` under the input and NOT inside the `.error-block`; the
    «Шаг N из M» strings are unchanged.
  </action>
  <verify>
    <automated>uv run pytest tests/test_mobile_corrections.py tests/test_mobile_transfers.py tests/test_mobile_wiring.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_mobile_corrections.py tests/test_mobile_transfers.py tests/test_mobile_wiring.py -q` green.
    - `grep -n 'name="op_date"' app/templates/mobile_partials/corrections_step_value.html app/templates/mobile_partials/transfers_step_dest.html`
      returns one hit each, positioned before the `mobile-actions` div.
    - The 422 response for корректировка contains «Дата операции не может быть в будущем.» exactly
      ONCE (proving the loop exclusion works and the message is not duplicated).
    - `git diff app/routes/mobile_transfers.py` shows NO change at `:65`, `:106` or `:130`.
    - `grep -rn "Шаг" app/templates/mobile_partials/ | wc -l` is unchanged from HEAD.
    - `grep -c "aria-describedby" app/templates/mobile_partials/corrections_step_value.html app/templates/mobile_partials/transfers_step_dest.html`
      returns 1 for each.
  </acceptance_criteria>
  <done>Both shell-less wizards carry the date on their terminal step with the error beside it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form POST → service validation → ledger write | Untrusted `op_date` reaches a stored column and a template |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-16 | Tampering | `op_date` reaching SQL | mitigate | `parse_op_date` (`date.fromisoformat` + `.isoformat()`), bound ORM parameter only |
| T-33-17 | Tampering | XSS via the echoed value on a 422 | mitigate | Jinja autoescaping, never `\|safe`; the RU error is a fixed module constant |
| T-33-31 | Repudiation | A transfer's two ledger rows carrying different business dates | mitigate | One parse, one value, both call sites; pinned by a dedicated test |
| T-33-32 | Information disclosure (usability-grade) | A duplicated or detached date error confusing which field is wrong | mitigate | D-14 per-file handling: per-key `<p class="error">` under the input plus exclusion from the loop-all block; asserted by an exactly-once check |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | No package installed; `pyproject.toml` untouched |
</threat_model>

<verification>
- `uv run pytest tests/test_corrections.py tests/test_transfers.py tests/test_mobile_corrections.py tests/test_mobile_transfers.py tests/test_smoke.py -q` green.
- `uv run pytest -q` green apart from the four known-red `tests/test_sync_ui.py` cases.
</verification>

<success_criteria>
- Four surfaces (2 desktop, 2 mobile final steps) render a pre-filled, `max`-capped `op_date` input.
- A transfer's two rows always share one business date.
- The корректировка error renders once, beside the field.
- No wizard gained a step and no «Шаг N из M» string changed.
</success_criteria>

<output>
Create `.planning/phases/33-back-dated-operations/33-12-SUMMARY.md` when done.
</output>

---
phase: 33-back-dated-operations
plan: 15
type: execute
wave: 6
depends_on: ["33-09", "33-13", "33-14"]
files_modified:
  - .planning/phases/33-back-dated-operations/33-ROLLOUT.md
autonomous: false
requirements: [SYNC-10, SYNC-11, SYNC-12, SYNC-13, DATE-01, DATE-02, DATE-03, DATE-04, DATE-05, DATE-06, DATE-07, DATE-08]

must_haves:
  truths:
    - "The PostgreSQL half of the trigger rewrite and of the backfill is proven in CI, not assumed"
    - "The seven browser checks B-1..B-7 have a recorded observed result, never an assertion"
    - "The .filter-bar overflow observation at 1024 px is RECORDED, not fixed"
    - "The rollout runs in the LOCKED order: migrate + redeploy s1 → verify pull and a current-client push → only then cut the client release tag"
    - "Post-migration coverage on s1 shows total == filled on both ledger tables and exactly four triggers"
  artifacts:
    - path: ".planning/phases/33-back-dated-operations/33-ROLLOUT.md"
      provides: "the executed rollout log, the browser-check results and the CI parity result"
  key_links:
    - from: ".planning/phases/33-back-dated-operations/33-ROLLOUT.md"
      to: "the s1 deployment"
      via: "the executed checklist written before the migration existed"
      pattern: "cut the client release tag"
---

<objective>
Close the three things source reading and the local suite cannot settle: the PostgreSQL branch of
migration `0027`, the seven browser observations, and the cross-machine rollout order.

Purpose: none of these may be marked done by assertion. `33-RESEARCH.md` records the PostgreSQL half
of V4 as MEDIUM confidence because no PG instance exists on this machine and starting one is
forbidden; `33-UI-SPEC.md`'s B-1 … B-7 are browser-rendered behaviours with no server-side
observable; and LOCKED ordering constraint 5 spans two machines and a self-updating client fleet.
Output: an executed rollout log appended to `33-ROLLOUT.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-back-dated-operations/33-ROLLOUT.md
@.planning/phases/33-back-dated-operations/33-UI-SPEC.md
@.planning/phases/33-back-dated-operations/33-VALIDATION.md
@.planning/ROADMAP.md
@CLAUDE.md
</context>

<project_conventions>
- **Never start, stop, restart or kill a server, container or port.** The operator has a live
  instance running. Any check needing a running app is either run by the operator in their own
  instance, or run by Claude on a distinct unused port that Claude started and shuts down itself,
  naming the port and PID in the same reply.
- «не запускал» is an acceptable answer with the exact command for the operator; a fabricated PASS
  is not.
- Talk to the operator in Russian.
</project_conventions>

<artifacts_this_phase_produces>
`business_date_bounds`, `local_today_iso`, `business_date_expr`, `push_schema_ok`, `parse_op_date`,
`today_iso` (Jinja global), `OP_DATE_FORMAT_ERROR`, `OP_DATE_FUTURE_ERROR`, `SCHEMA_AHEAD_ERROR`,
`SyncResult(status="schema_mismatch")`, the four ledger columns `operations.business_date` /
`cash_movements.business_date` / `operations.reverses_op_id` / `cash_movements.reverses_movement_id`,
the `dated` filter parameter, the `business_day` and `is_backdated` row-dict keys, the
`.field.op-date` CSS class, the `op_date` form field, migration `0027`, the `alembic_engine` fixture
and `run_alembic` helper in `tests/conftest.py`, and the three new test files
`tests/test_migrations.py`, `tests/test_business_date.py`, `tests/test_sync_schema_gate.py`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: full local gate + the PostgreSQL parity run in CI</name>
  <files>.planning/phases/33-back-dated-operations/33-ROLLOUT.md</files>
  <read_first>
    - 33-VALIDATION.md § Sampling Rate and § Known pre-existing red
    - tests/test_pg_parity.py — the existing harness (module-level skip unless `DATABASE_URL` is a
      PostgreSQL URL)
    - .github/workflows/ — the existing pg-parity job on `postgres:17`
    - 33-RESEARCH.md § Assumptions Log A1 — the PostgreSQL assumption this task converts into evidence
  </read_first>
  <action>
    Run the full local gate and record the exact output:
    `uv run pytest -q`. Exactly four `tests/test_sync_ui.py` failures are expected and pre-existing
    (the lifespan auto-sync thread holds `sync_client._run_lock`, red since ≤ `49a53d2`). **Do not
    attribute them to this phase and do not "fix" them.** Anything else red blocks the phase.

    Then get the PostgreSQL branch proven. `33-RESEARCH.md` A1 assumes that PostgreSQL's enumerated
    `WHEN (...)` behaves like SQLite's for a backfill of an unnamed column; if PG fired on any UPDATE,
    `alembic upgrade head` would abort mid-upgrade **on the live server**. The mitigation is CI, not
    a local Postgres:
    - push the branch and let the existing pg-parity job run `uv run pytest tests/test_pg_parity.py -q`
      against the `postgres:17` service, or
    - if CI cannot be triggered from here, record the exact command and mark it as a pending human
      check with «не запускал».

    **Do NOT install, start or containerise a PostgreSQL instance on this machine to work around
    this.** Record the result — pass, fail, or not-run-with-the-command — in a new
    `## Executed verification` section of `33-ROLLOUT.md`.
  </action>
  <verify>
    <automated>uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - The recorded `uv run pytest -q` summary shows exactly four failures, all in
      `tests/test_sync_ui.py`.
    - `33-ROLLOUT.md` gains an `## Executed verification` section naming the PG parity outcome as one
      of PASS / FAIL / «не запускал + <exact command>» — never as an assumption.
    - No PostgreSQL server was installed or started locally.
  </acceptance_criteria>
  <done>The local suite is green modulo the known four, and the PostgreSQL branch has a recorded status.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: the seven browser observations (B-1 … B-7)</name>
  <files>—</files>
  <read_first>
    - 33-UI-SPEC.md § Verification the executor must run — the B-1 … B-7 table with expectations
    - 33-VALIDATION.md § Manual-Only Verifications
    - 33-CONTEXT.md § Deferred Ideas — the `.filter-bar { flex-wrap }` entry
  </read_first>
  <what-built>
    Все 14 форм получили поле «Дата операции» (предзаполнено сегодняшней датой, с `max`), История
    показывает обе даты и фильтр «Задним числом» на десктопе и на телефоне, CSV-выгрузки получили
    колонку «Внесено». Автотесты зелёные — кроме четырёх заранее известных падений
    `tests/test_sync_ui.py`, которые к этой фазе отношения не имеют.
  </what-built>
  <action>
    Present the seven checks below to the operator, one numbered block each, and wait for the observed
    results. Then append them verbatim to `33-ROLLOUT.md` under a `## Browser checks` heading, each
    marked OBSERVED or NOT RUN with the date. Never infer a result from source. Do not start, stop or
    restart the operator's instance; if you need a running app yourself, start one on a distinct
    unused port, name the port and PID in the same reply, and shut down only that one.
    If B-5 shows a horizontal scrollbar, RECORD it and stop there — the `flex-wrap` fix is a deferred
    decision touching every `.filter-bar` page and is explicitly not this phase's work.
    If B-1 shows no native bubble, that is a real defect: record it and raise D-13's fallback (drop
    `max`, rely on the server guard alone) rather than silently accepting it.
  </action>
  <verify>
    <automated>MISSING — browser-rendered constraint UI and htmx click behaviour have no server-side observable; results are recorded in 33-ROLLOUT.md § Browser checks</automated>
  </verify>
  <how-to-verify>
    Семь проверок. Каждую — в браузере, в вашем собственном экземпляре приложения. Ни одну нельзя
    закрыть «по логике кода».

    1. **B-1.** Откройте `/receipts`, поставьте «Дата операции» на завтра, нажмите «Сохранить приход».
       Ожидается: всплывающая подсказка самого браузера, и во вкладке Network запрос НЕ уходит.
    2. **B-2.** `/m/receipts`, шаг 4: поставьте дату в шапке на завтра, нажмите «Сохранить приход».
       Ожидается: запрос УХОДИТ, и в ответе первой строкой подменённого шага, прямо под всё ещё
       заполненным полем даты, появляется «Дата операции не может быть в будущем.»
       (на мобильных мастерах `max` намеренно неактивен — сервер и есть защита).
    3. **B-3.** `/m/sales`: поставьте дату, добавьте товар, вернитесь в корзину, добавьте второй.
       Ожидается: дата осталась той, что вы поставили, и НЕ сбросилась на сегодня.
    4. **B-4.** `/finance` и `/m/finance`. Ожидается: два поля даты; клик по каждой подписи ставит
       фокус в СВОЁ поле (id `withdraw-op-date` и `deposit-op-date`).
    5. **B-5.** `/history` при ширине окна 1024 px, все четыре фильтра на экране. Ожидается: нет
       горизонтальной полосы прокрутки. **Если полоса появилась — просто сообщите об этом.**
       Исправление (`flex-wrap: wrap` для `.filter-bar`) намеренно отложено: оно затрагивает все
       страницы с `.filter-bar` и не входит в эту фазу.
    6. **B-6.** `/history` и `/m/history` ДО того, как появится хоть одна операция задним числом.
       Ожидается: каждая ячейка «Когда» и каждая шапка мобильной карточки выглядят ровно как раньше —
       одна строка `дд.мм.гггг чч:мм`, без пометки.
    7. **B-7.** Выгрузите `sales.csv` и `cash_movements.csv` после того, как появится хотя бы одна
       операция задним числом. Ожидается: одна новая колонка, заголовок «Внесено» — последний;
       первая колонка не убывает сверху вниз; `Код` / `Цена` / `Сумма` на прежних местах.

    По каждому пункту напишите, что реально увидели.
  </how-to-verify>
  <resume-signal>Напишите результат по каждому из семи пунктов (или «B-N не проверял» — это тоже ответ)</resume-signal>
  <acceptance_criteria>
    - All seven results are recorded verbatim in `33-ROLLOUT.md` under `## Browser checks`, each as
      OBSERVED or NOT RUN — never as inferred.
    - If B-5 shows a scrollbar, it is recorded as a deferred observation with a pointer to
      `33-CONTEXT.md` § Deferred Ideas. **No `flex-wrap` fix is made in this phase.**
    - If B-1 shows NO native bubble (i.e. the submit is silently swallowed), that is a real defect:
      record it and raise it — D-13's fallback is to drop `max` and rely on the server guard alone.
  </acceptance_criteria>
  <done>The browser half of DATE-01/DATE-02/DATE-05/DATE-06/DATE-07 is observed, not assumed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: execute the rollout in the LOCKED order</name>
  <files>.planning/phases/33-back-dated-operations/33-ROLLOUT.md</files>
  <read_first>
    - .planning/phases/33-back-dated-operations/33-ROLLOUT.md — the checklist written by plan 33-04
      before the migration existed, including the post-migration smoke SQL
    - .planning/ROADMAP.md § Phase 33 LOCKED ordering constraint 5
    - 33-RESEARCH.md § Deployment-time checks — the rollout order with the checks slotted in
  </read_first>
  <what-built>
    Миграция `0027` и весь код фазы готовы и проверены локально. Осталась раскатка — она идёт в
    жёстко зафиксированном порядке, потому что клиенты обновляются сами.
  </what-built>
  <action>
    Walk the operator through the checklist below in the LOCKED order, one step at a time, and append
    the executed log to `33-ROLLOUT.md` under `## Executed rollout`: the date, the two coverage-query
    outputs, the four trigger names, and the pull/push verification result. Do not run anything on s1
    yourself and do not restart any container (CLAUDE.md PC-9) — the operator runs each command and
    pastes the output. Do not reorder the steps, and never edit `alembic/versions/0018_*.py` or
    `alembic/versions/0026_*.py`. If any coverage query shows `total != filled`, STOP before step 5
    and report rather than cutting the client release tag.
  </action>
  <verify>
    <automated>MISSING — cross-machine rollout against live production; verified by the operator's pasted coverage and pg_trigger query output, recorded in 33-ROLLOUT.md § Executed rollout</automated>
  </verify>
  <how-to-verify>
    Порядок из `33-ROLLOUT.md`, менять его нельзя:

    1. Обновить s1 и применить миграцию:
       `docker compose -f docker-compose.prod.yml up -d --build`
       (образ ЗАПЕКАЕТ код приложения — одного `git pull` недостаточно).
    2. Проверить покрытие бэкфилла (только чтение):
       `SELECT count(*) AS total, count(business_date) AS filled FROM operations;`
       и то же самое для `cash_movements`. Ожидается `total == filled` в обеих таблицах.
    3. Проверить, что все четыре триггера на месте (только чтение):
       `SELECT tgname FROM pg_trigger WHERE tgrelid IN ('operations'::regclass,
       'cash_movements'::regclass) AND NOT tgisinternal ORDER BY tgname;`
       Ожидается ровно четыре имени.
    4. Проверить, что синхронизация жива: `GET /api/sync/pull` отвечает 200, и push с ТЕКУЩЕГО,
       ещё не обновлённого клиента возвращает 200 и мержится (это и есть проверка того, что D-01
       принимает «отставшего» клиента).
    5. **И только после этого** выпускать клиентский релизный тег.

    Отдельно: миграции `0018` и `0026` НИКОГДА не редактируются задним числом — применённая
    миграция это исторический факт.

    Пришлите вывод шагов 2 и 3 и подтвердите шаг 4.
  </how-to-verify>
  <resume-signal>Пришлите вывод проверок покрытия и триггеров, либо напишите, что раскатка откладывается</resume-signal>
  <acceptance_criteria>
    - `33-ROLLOUT.md` gains an `## Executed rollout` section with the date, the coverage query output
      (`total == filled` on both tables), the four trigger names, and the pull/push verification
      result.
    - The client release tag was cut ONLY after step 4 passed — or the section records that the tag
      has not been cut yet.
    - No edit was made to `alembic/versions/0018_*.py` or `alembic/versions/0026_*.py`.
    - Advisory, recorded not blocking: whether `batch.schema_version` logging was enabled to answer
      the fleet-version question (is any deployed client below `0024`?).
  </acceptance_criteria>
  <done>The migration is live on s1 in the locked order, with coverage and triggers verified on the real data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local build → s1 production | A schema migration with a destructive `downgrade()` is applied to live operator data |
| s1 → self-updating client fleet | A client that updates ahead of the server would push a column the server does not have |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-11 | Denial of service | Cutting the client release tag before s1 is migrated | mitigate | The LOCKED rollout order is executed and its output recorded; SYNC-10's 409 gate plus D-09's back-off already bound the damage if the order slips |
| T-33-37 | Tampering (unproven dialect) | The PostgreSQL branch of the trigger rewrite and of the backfill never exercised before it runs on production | mitigate | CI `tests/test_pg_parity.py` against `postgres:17`; if it cannot be run, the status is recorded as «не запускал» with the exact command, never as a pass |
| T-33-38 | Tampering (data corruption) | An incomplete backfill leaving rows with a NULL business date on the server | mitigate | The post-migration coverage query asserts `total == filled` on both tables against the real data |
| T-33-39 | Denial of service | Restarting or killing the operator's live instance while verifying | mitigate | CLAUDE.md: nothing Claude did not start is stopped; browser checks run in the operator's own instance |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | No package installed by this phase; `pyproject.toml` untouched throughout |
</threat_model>

<verification>
- `uv run pytest -q` recorded, with exactly the four known `tests/test_sync_ui.py` failures.
- `tests/test_pg_parity.py` result recorded (PASS / FAIL / not-run-with-command).
- B-1 … B-7 recorded as observed results.
- The s1 coverage and trigger queries recorded with their real output.
</verification>

<success_criteria>
- No claim in this phase's artifacts rests on inference where a run was possible.
- The `.filter-bar` observation is recorded and explicitly NOT fixed.
- The rollout followed the locked order, or the deviation is written down.
</success_criteria>

<output>
Create `.planning/phases/33-back-dated-operations/33-15-SUMMARY.md` when done.
</output>

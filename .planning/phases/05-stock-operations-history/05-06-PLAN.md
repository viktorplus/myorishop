---
phase: 05-stock-operations-history
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - app/templates/base.html
  - app/templates/pages/home.html
  - tests/test_writeoffs.py
autonomous: true
gap_closure: true
requirements: [OPS-01]

must_haves:
  truths:
    - "Operator reaches /writeoff via a visible link in the app's nav bar and on the home page — not only by typing the URL into the browser"
  artifacts:
    - path: "app/templates/base.html"
      provides: "«Списание» nav link to /writeoff, active-state consistent with sibling links (Продажи, История, etc.)"
      contains: 'href="/writeoff"'
    - path: "app/templates/pages/home.html"
      provides: "Home-page contextual link to /writeoff alongside the existing /corrections and /history links"
      contains: 'href="/writeoff"'
    - path: "tests/test_writeoffs.py"
      provides: "Regression test asserting the rendered home page (base.html nav + home.html body) links to /writeoff"
      contains: 'href="/writeoff"'
  key_links:
    - from: "app/templates/base.html"
      to: "/writeoff"
      via: "<a href=\"/writeoff\">Списание</a> nav anchor rendered on every page (base.html is the layout for the whole app)"
      pattern: 'href="/writeoff"'
---

<objective>
Close the ONE blocking gap from `05-VERIFICATION.md` (score 3/4 must-haves): OPS-01's write-off feature is fully implemented and tested (`tests/test_writeoffs.py`, 4/4 GREEN) but has **no navigation entry point** anywhere in the rendered UI — `base.html`'s nav bar and `home.html` link to `/corrections` and `/history` but never to `/writeoff`. An operator using the running app (not typing a URL from memory) cannot discover or reach it. This was a known, deliberately-deferred gap noted in `05-02-SUMMARY.md` and independently confirmed as Critical (CR-01) by `05-REVIEW.md`.

Fix scope is intentionally minimal per the verifier's own assessment: this is a trivial, low-risk, template-only change with no service-layer impact — do not expand scope beyond wiring the two links plus a regression test proving they render.

Purpose: Make OPS-01 ("Operator can write off stock with a reason, and stock decreases accordingly") actually TRUE in practice, not just at the service/route level.
Output: `base.html` nav bar and `home.html` both link to `/writeoff`; a new regression test guards against this ever regressing silently again.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-stock-operations-history/05-VERIFICATION.md
@.planning/phases/05-stock-operations-history/05-REVIEW.md
@app/templates/base.html
@app/templates/pages/home.html
@tests/test_writeoffs.py
</context>

<tasks>

<task type="auto">
  <name>Task: Wire /writeoff into the nav bar and home page (CR-01 / OPS-01 gap)</name>
  <files>app/templates/base.html, app/templates/pages/home.html, tests/test_writeoffs.py</files>
  <read_first>
    app/templates/base.html (current nav block, lines 17-25 — the exact `<a href="..."{% if request.url.path.startswith(...) %} class="active"{% endif %}>Label</a>` pattern used for every sibling link, e.g. `/history`, `/customers`);
    app/templates/pages/home.html (current content block — the single `<p><a href="/corrections">...</a> · <a href="/history">...</a></p>` line, D-12/D-17 comment above it);
    tests/test_smoke.py (`test_home_page_renders` — shows the `client, product` fixture pattern used to render the home page for assertions).
  </read_first>
  <action>
    In `app/templates/base.html`: insert one new nav anchor between the existing `Продажи` (`/sales/new`) link and the `Покупатели` (`/customers`) link, following the exact same active-class pattern as every sibling: `<a href="/writeoff"{% if request.url.path.startswith("/writeoff") %} class="active"{% endif %}>Списание</a>`. Resulting nav order: Главная · Товары · Приход · Продажи · Списание · Покупатели · История · Справочник · Резервные копии — write-off sits next to the other stock-movement actions (Приход/Продажи).

    In `app/templates/pages/home.html`: extend the existing `{% if product %}` block's link line to also include `/writeoff`, matching the existing `·`-separated anchor style: `<p><a href="/writeoff">Списание</a> · <a href="/corrections">Корректировка остатка</a> · <a href="/history">История операций</a></p>`. Update the comment above it (currently referencing only D-12/D-17) to also note the write-off entry point closes the OPS-01 UI gap. Do not touch the `{% include "partials/ledger_rows.html" %}` line or the `{% else %}` empty-state branch.

    In `tests/test_writeoffs.py`: add one new test function after the existing web-slice tests (after `test_web_writeoff_oversell`), named `test_web_writeoff_reachable_from_nav`, using the `client, product` fixtures (mirrors `tests/test_smoke.py::test_home_page_renders`): `GET /` and assert `response.status_code == 200` and `'href="/writeoff"' in response.text`. Add a one-line docstring citing the gap-closure source: this guards against `05-VERIFICATION.md`'s Gap #1 (unreachable `/writeoff`) ever silently regressing.
  </action>
  <acceptance_criteria>
    - `grep -c 'href="/writeoff"' app/templates/base.html` >= 1
    - `grep -c 'href="/writeoff"' app/templates/pages/home.html` >= 1
    - `uv run pytest tests/test_writeoffs.py -q` exits 0, all tests pass (5/5, including the new one)
    - `uv run pytest -q` full suite exits 0 with no regressions (162 + 1 new = 163 passed, no other count decreases)
    - `uv run ruff check app/templates/../.. ` is not applicable to templates; run `uv run ruff check tests/test_writeoffs.py` and `uv run ruff format --check tests/test_writeoffs.py` — both exit 0
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_writeoffs.py -q</automated>
  </verify>
  <done>base.html's nav bar and home.html's content block both contain a working `<a href="/writeoff">` link; the new regression test and the full existing suite are green; an operator can reach the write-off form from the running app without typing the URL.</done>
</task>

</tasks>

## Artifacts This Phase Produces

- `app/templates/base.html` — modified: nav bar gains `<a href="/writeoff">Списание</a>` between `Продажи` and `Покупатели`, active-class consistent with siblings.
- `app/templates/pages/home.html` — modified: the `{% if product %}` link line gains a leading `<a href="/writeoff">Списание</a> ·` before the existing `/corrections`/`/history` links.
- `tests/test_writeoffs.py` — new test: `test_web_writeoff_reachable_from_nav` (regression guard for the OPS-01 UI gap).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This plan adds two static server-rendered anchor tags and one test; it introduces no new user input, no new route, and no new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-22 | N/A | Static nav/home template links | accept | No untrusted input crosses these anchors; `/writeoff`'s own route (GET/POST) already carries its Wave-2 STRIDE mitigations (server-side allow-list, oversell check, single write path) unaffected by this plan |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — not applicable |
</threat_model>

<verification>
- `uv run pytest tests/test_writeoffs.py -q` — 5/5 pass (4 existing + 1 new).
- `uv run pytest -q` — full suite green, no regressions.
- `grep -rn 'href="/writeoff"' app/templates/` — at least 2 matches (base.html nav + home.html), confirming Gap #1 is closed.
- `uv run ruff check .` and `uv run ruff format --check .` — clean (or only pre-existing, unrelated findings already noted in `05-VERIFICATION.md`).
</verification>

<success_criteria>
- OPS-01 truth "Operator can write off stock with a reason, and stock decreases accordingly" is achievable through the running app's own navigation, not only by typing `/writeoff` into the address bar.
- 05-VERIFICATION.md's Gap #1 is closed; a re-run of phase verification would find 4/4 must-haves verified.
</success_criteria>

<output>
Create `.planning/phases/05-stock-operations-history/05-06-SUMMARY.md` when done.
</output>

---
phase: 22-sales-page-rebuild
plan: 05
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - app/routes/sales.py
  - app/templates/partials/sale_customer.html
  - tests/test_sales.py
autonomous: true
requirements: [SALE-03, SALE-04, SALE-05, SALE-06]

must_haves:
  truths:
    - "Operator picks new / existing / anonymous from a radio group at the top of the sale form, each mode server-rendered on selection (D-01)"
    - "«Существующий» is the default selection when the form opens (D-02)"
    - "Switching the radio never loses data already typed in another mode (D-03)"
    - "Existing mode offers autocomplete by consultant number, name, or surname and fills the chip on pick (SALE-04)"
    - "New mode shows exactly 3 fields — Имя / Фамилия / Номер консультанта — and no Phase-21 profile fields (D-07)"
    - "Anonymous mode shows no extra fields and submits customer_id empty, so the sale is written with customer_id = NULL (D-05)"
    - "The customer is created only via the explicit «Добавить покупателя» button; register_sale's signature is untouched (D-10)"
    - "Filling the new-customer fields without pressing the button returns 422, never a silent walk-in sale (D-10)"
    - "A 422 or oversell re-render keeps the selected-customer chip visible, closing the verified mis-attribution defect (D-12)"
    - "An unknown customer_mode degrades to «Существующий» and is never echoed raw"
    - "Existing sale guardrails — oversell, batch selection, cash credit — still fire on the rebuilt form"
  artifacts:
    - path: "app/routes/sales.py"
      provides: "_CUSTOMER_MODES allow-list, _customer_context builder, GET /sales/customer-mode, D-10 guard"
      contains: "_customer_context"
    - path: "app/templates/partials/sale_customer.html"
      provides: "3-way radio-driven customer header with hidden-echo state preservation"
      contains: "customer_mode"
  key_links:
    - from: "app/templates/partials/sale_customer.html"
      to: "GET /sales/customer-mode"
      via: "fieldset hx-get with hx-include (MANDATORY — htmx GET sends only its own value)"
      pattern: "hx-include"
    - from: "app/routes/sales.py::_customer_context"
      to: "app.services.customers.get_customer"
      via: "server-side resolution of selected on EVERY render path"
      pattern: "get_customer"
---

<objective>
Restructure the sale form's customer header into an explicit 3-way radio selector (SALE-03), keep the
existing autocomplete (SALE-04) and the 3-field inline create (SALE-05), make walk-in an explicit
choice (SALE-06), and close two defects: the D-10 silent-walk-in and the D-12 chip loss.

Purpose: ROADMAP criterion 3. Today walk-in is the ABSENCE of a choice ("don't search, don't fill
anything"); this makes the choice explicit. Two verified defects sit on exactly this code and must
not re-ship.

Output: `_CUSTOMER_MODES`, `_customer_context()`, `GET /sales/customer-mode` in `app/routes/sales.py`;
`sale_customer.html` restructured into 3 radio-driven modes.

**⚠ THIS IS THE PHASE'S HIGHEST-RISK PLAN — give it your full attention.** It carries the two
highest-risk surfaces in Phase 22 at once: the D-03 round-trip and the D-12 five-path fix, plus the
D-10 guard. The D-03 round-trip is the single most likely defect: a radio with `hx-get` and NO
`hx-include` *appears* to work on the first switch and breaks silently only on the switch back. Do not
speed-run this plan.

**Test-file ownership (wave 2).** This plan removes the TEN xfail markers it owns from
`tests/test_sales.py` (7 in Task 2, 3 in Task 3). The remaining THREE — the `recent_sales` markers —
belong to **22-03, which runs in wave 3, after this plan**, because two plans must never write one
test file in the same wave. So this plan's criteria assert exactly 3 xfailed remaining, NOT zero;
22-03 owns the zero-marker end state.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-sales-page-rebuild/22-PATTERNS.md
@.planning/phases/22-sales-page-rebuild/22-UI-SPEC.md
@app/templates/partials/receipt_batch_chooser.html
</context>

## Artifacts this phase produces

This plan owns:

| Artifact | Kind |
|----------|------|
| `_CUSTOMER_MODES = ("new", "existing", "anon")` | module-level tuple allow-list, `app/routes/sales.py` |
| `_customer_context(session, mode, customer_id, form) -> dict` | function returning `mode`, `customer_id`, `selected`, `form` (NOT `errors` — see Task 1) |
| `GET /sales/customer-mode` | endpoint (literal path — must be declared before any parameterized `/sales/{...}`) |
| `errors.new_customer` | context key — the D-10 guard message slot, rendered under «Добавить покупателя» |
| `customer_id_keep`, `customer_q`, `name`, `surname`, `consultant_number` as hidden echo inputs | D-03 state-preservation mechanism |
| `customer_mode`, `name`, `surname`, `consultant_number` as new `Form("")` params on `POST /sales` | endpoint params |
| The TEN `customer_mode` / `new_customer` / `picker` / `chip_survives_422` xfail markers in `tests/test_sales.py` (removal only — bodies are 22-01's) | test-marker removal |

Preserved verbatim (hard contracts — renaming breaks confirm-to-write): `#customer-header`,
`#customer-id-input`, `#customer-selected`, `#customer-chip-text`, `#customer-default`, `#customer-q`,
`#customer-picker`, and `form="sale-form"` on `#customer-id-input`.
**Zero new CSS rules; zero new packages.**

<tasks>

<task type="auto">
  <name>Task 1: _CUSTOMER_MODES, _customer_context, and GET /sales/customer-mode</name>
  <files>app/routes/sales.py</files>
  <read_first>
    - `app/routes/sales.py:1-40` — the import block (note `get_customer` is NOT imported today — it
      lives at `app/services/customers.py:227` and is currently imported only by
      `app/routes/customers.py:16`; this is a genuinely NEW import), the route-order rule at :30-32,
      `SAVE_FAILED_ERROR` at :34, and `_ROW_ID_RE` at :36-40.
    - `app/routes/sales.py:343-386` — `sale_customer_create`, the shipped endpoint that renders
      `partials/sale_customer.html` from a hand-built context dict. Its `except Exception:
      # noqa: BLE001` + `logger.exception(...)` + `errors.quick_create` shape is the house error
      pattern to copy exactly.
    - `app/routes/sales.py:98-106` — `sale_new_page`'s context dict, one of the five D-12 bug sites.
    - `app/services/customers.py:227` — `get_customer`'s signature and return type.
    - `app/services/customers.py:262` — `_SORT_MAP`, the LIGHTER allow-list precedent to copy (a
      module-level tuple/dict, membership-checked, default on miss). Do NOT copy the heavier
      `_ROW_ID_RE` regex — that exists only because `row_id` is echoed into a JS-evaluated attribute.
    - `.planning/phases/22-sales-page-rebuild/22-RESEARCH.md` Pattern 1 — the reference
      implementation and the cited htmx parameter rule.
  </read_first>
  <action>
Add three things to `app/routes/sales.py`.

1. `_CUSTOMER_MODES = ("new", "existing", "anon")` at module level, near `_ROW_ID_RE`, with a comment
   explaining it is the proportionate guard: `customer_mode` reaches only `{% if mode == "new" %}`
   comparisons, never a JS-evaluated attribute, so a tuple membership check (the `_SORT_MAP` shape) is
   right where `_ROW_ID_RE`'s regex would be overkill. Compare, NEVER interpolate.

2. `_customer_context(session, mode, customer_id, form) -> dict` — the single shared builder every
   render of `sale_customer.html` goes through. It must:
   - allow-list `mode` against `_CUSTOMER_MODES`, defaulting to `"existing"` on a miss (D-02);
   - resolve `selected = get_customer(session, customer_id)` ONLY when mode is `existing` and
     `customer_id` is non-empty, else `None`. An id that no longer resolves degrades to "nothing
     selected" rather than rendering a chip for a deleted customer (mirroring `_build_lines`' batch
     rule);
   - return keys `mode`, `customer_id` (the RESOLVED id, i.e. `selected.id if selected else ""`),
     `selected`, and `form`.
   **Return NO `errors` key.** Callers own `errors` and merge this builder's keys into their own
   context dict. This matters: 22-RESEARCH.md's Pattern-1 sketch returns `"errors": {}`, which would
   CLOBBER the caller's real errors when merged into a `sale_create` error branch. Deviate from the
   sketch here deliberately and say so in the docstring.
   Add `get_customer` to the existing `from app.services.customers import ...` line.
   Docstring must state the D-03 contract (`form` carries ALL modes' values, active + echoed-inactive)
   and the D-12 contract (this builder is what keeps the 422/oversell re-render from dropping the chip).

3. `GET /sales/customer-mode` → `sale_customer_mode(request, customer_mode="existing", customer_id="",
   customer_id_keep="", customer_q="", name="", surname="", consultant_number="", session=Depends(get_session))`
   returning `templates.TemplateResponse(request, "partials/sale_customer.html", context)`.
   Build the context as `{**_customer_context(session, customer_mode, customer_id or customer_id_keep,
   {...}), "errors": {}}`, where the form dict carries `customer_q`, `name`, `surname`,
   `consultant_number`. Coalesce `customer_id or customer_id_keep` — do not pick one: whichever mode
   is LEAVING supplies one of them (the visible hidden input, or the inactive echo).
   Declare it as a LITERAL path BEFORE any parameterized `/sales/{...}` route — place it next to the
   other literal customer routes (`/sales/customer-search` at :336, `/sales/customer` at :343), per
   the file's own rule at :30-32.
   Wrap the body in the house error pattern: `except Exception:  # noqa: BLE001 — UI-SPEC: block
   error, never a raw 500`, `logger.exception("customer mode render failed")`, then a Russian block
   error at status 422. Never a raw 500.
  </action>
  <verify>
    <automated>uv run pytest tests/test_sales.py -k "customer_mode_allowlist" -rxX</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_CUSTOMER_MODES" app/routes/sales.py` shows a module-level tuple with exactly
      `"new"`, `"existing"`, `"anon"`.
    - `grep -n "from app.services.customers import" app/routes/sales.py` now includes `get_customer`.
    - `_customer_context` does NOT return an `errors` key:
      `grep -n -A 12 "def _customer_context" app/routes/sales.py` shows no `"errors"` in its return dict.
    - Route order respected: `GET /sales/customer-mode` is declared before `@router.post("/sales")` —
      verify by comparing line numbers from `grep -n '@router' app/routes/sales.py`.
    - `uv run pytest tests/test_sales.py -k "customer_mode" -rxX` still reports XFAIL for the markup
      tests (the template lands in Task 2) — the endpoint alone must not XPASS them.
    - `uv run ruff check app/routes/sales.py && uv run ruff format --check app/routes/sales.py` passes.
  </acceptance_criteria>
  <done>The allow-list, the shared context builder, and the mode endpoint exist, with the endpoint declared before any parameterized /sales route.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure sale_customer.html into 3 radio-driven modes</name>
  <files>app/templates/partials/sale_customer.html, tests/test_sales.py</files>
  <read_first>
    - `app/templates/partials/sale_customer.html` — the ENTIRE 65-line file being restructured. Read
      the header comment (:1-6) explaining why it lives OUTSIDE `<form id="sale-form">` and why every
      input associates via `form="sale-form"`; the chip block (:14-23); the search field (:28-29); the
      3-field block (:42-56) — reuse VERBATIM per D-07; the quick-create button (:57-62) — note it
      ships `hx-include="#customer-header input"` (element-scoped); and `:38`'s
      `<p class="muted">Без покупателя (розница)</p>`, the verbatim source of the anon label.
    - `app/templates/partials/receipt_batch_chooser.html:19-50` — the shipped `fieldset class="field"`
      + `<legend>` + wrapped-`<label>` radio group shape to copy. Do NOT copy its per-radio
      `hx-on:change` JS — this phase puts ONE `hx-get` on the `<fieldset>` itself.
    - `app/templates/partials/customer_picker.html:1-12` and `:21-28` — the `.dataset`-in /
      `.textContent`-out contract and the four hard-coded desktop ids the handler reaches.
    - `app/templates/partials/sale_form.html:10-14` — the bare `{% include %}`, which means
      `_customer_context`'s keys must be merged into every `sale_form.html` context dict, not nested.
    - `.planning/phases/22-sales-page-rebuild/22-UI-SPEC.md` Interaction 1, 2, 3, 5, 6, 8, 20 and the
      full Copywriting table.
  </read_first>
  <action>
Restructure `app/templates/partials/sale_customer.html` into the 3-way radio header.

Keep root `<div id="customer-header" class="customer-header">` as the outerHTML swap target
(unchanged contract), still OUTSIDE `<form id="sale-form">`. Default the mode with
`{% set mode = mode | default("existing") %}` (D-02).

**The radio group.** A `<fieldset class="customer-mode">` carrying
`hx-get="/sales/customer-mode"`, `hx-trigger="change"`, `hx-include="#customer-header input"`,
`hx-target="#customer-header"`, `hx-swap="outerHTML"`, `hx-sync="this:replace"`. Inside:
`<legend>Покупатель</legend>` and three `<label>`-wrapped radios named `customer_mode` with values
`new` / `existing` / `anon` and labels «Новый» / «Существующий» / «Без покупателя (розница)»,
`{% if mode == "..." %}checked{% endif %}`.

Use `hx-include="#customer-header input"` — element-scoped, matching the SHIPPED and TESTED
quick-create button at `:57-62`. 22-RESEARCH.md's sketch writes `#customer-header`; both work in
htmx, but 22-PATTERNS.md:268 says pick one and be consistent, so match the shipped one. Unchecked
radios do not contribute a value, so exactly one `customer_mode` is sent.

**`hx-include` is MANDATORY, not optional.** htmx sends only the triggering element's own value on a
GET; the enclosing form's inputs are auto-included ONLY on non-GET requests
[htmx.org/docs/#parameters]. Without it the server cannot know what the operator typed, re-renders an
empty block, and D-03 breaks silently — on the SECOND switch, not the first. `#customer-header` sits
outside `<form id="sale-form">` anyway, so there is no enclosing form to fall back on.

Use `<label>`-wrapped radios (not `for=`/`id=` pairing) — the shipped idiom, and it makes the whole
label text a click target. Do NOT substitute a `<select>` (SALE-03 and ROADMAP criterion 3 say
"radio"). Do NOT use `role="radiogroup"` on a `<div>` — native elements first. Phase 9's UAT found an
unlabeled batch chooser confusing and the fix was exactly `fieldset`+`legend`; re-deriving a bare
`<div>` of radios re-opens a defect this project already paid to close.

**`#customer-id-input`.** Render it ALWAYS, in every mode, outside the mode branches:
`<input type="hidden" id="customer-id-input" name="customer_id" form="sale-form" value="{{ customer_id if mode == 'existing' else '' }}">`.
Forcing it empty unless mode is `existing` is what makes `register_sale`'s `customer_id or None`
(`services/sales.py:254`) yield NULL for both `anon` and an un-created `new` (D-05 — no «Аноним» row,
no migration, no seed data). Keep `form="sale-form"` — it is what makes the oversell confirm re-POST
carry the customer.

**Mode blocks** (one visible at a time; the inactive modes persist as hidden VALUES, not hidden HTML):
- `existing` (active): the chip (`#customer-selected` / `#customer-chip-text`, `hidden` when not
  `selected`) with the «Убрать» button, and `#customer-default` holding the «Поиск покупателя»
  labelled `#customer-q` search input (placeholder «Поиск покупателя по имени…») with its existing
  `hx-get="/sales/customer-search"`, `hx-trigger="input changed delay:300ms"`,
  `hx-target="#customer-picker"`, `hx-sync="this:replace"`, plus `<div id="customer-picker">`. Reuse
  the shipped chip/search markup verbatim. Inactive echo: hidden `name`, `surname`,
  `consultant_number`.
- `new` (active): `<h2>Новый покупатель</h2>` + EXACTLY the 3 shipped fields reused verbatim (`Имя`;
  `Фамилия <span class="muted">(необязательно)</span>`; `Номер консультанта
  <span class="muted">(необязательно)</span>`) with their per-field `{% if errors.name %}` etc.
  blocks, then the «Добавить покупателя» button unchanged, then the D-10 error slot directly under it:
  `{% if errors.new_customer %}<p class="error">{{ errors.new_customer }}</p>{% endif %}`.
  Inactive echo: hidden `customer_id_keep` (valued `customer_id or ''`), hidden `customer_q`.
  **D-07 is a hard cap: exactly 3 fields, forever.** None of Phase 21's profile fields
  (phone/Telegram/email/social/address) appear here. Adding a 4th field is wrong.
- `anon` (active): the muted caption ONLY — «Продажа без покупателя. В списке продаж она будет
  отмечена как «Розница».» — and ZERO `<input>`/`<select>`/`<textarea>`. SALE-06's "no extra fields"
  constrains INPUTS; a caption is copy, not a field, and it is the only surface telling the operator
  what «Розница» in the recent-sales list means. Inactive echo: hidden `customer_id_keep`,
  `customer_q`, `name`, `surname`, `consultant_number`.

Guard field values as `{{ form.name if form else '' }}` (the SHIPPED guard at :44), not
`{{ form.name or '' }}` — this partial is rendered from `sale_form.html` include contexts that may not
define `form`.

Preserve `customer_picker.html`'s `.dataset`-in / `.textContent`-out handler contract exactly when the
picker sits inside the «Существующий» block — the four hard-coded ids must keep working. Never
`innerHTML`, never `|safe`. There is no legitimate `|safe` in this phase.

Do NOT give any new element an id starting with `name` or `price` — `sale_form.html:45-46`'s two swap
guards prefix-match those. Today's `id="customer-name"` is safe (it does not START with `name`); an
`id="name-mode"` would not be. Keep `#customer-header` OUTSIDE `<form id="sale-form">` so mode swaps
bypass those guards entirely — that is correct and must stay.

Add no CSS: `.field`, `.muted`, `.error`, `.customer-chip`, `.customer-picker` all exist.

Then remove the `@pytest.mark.xfail` markers from the five `customer_mode` tests and from
`test_web_sale_new_customer_field_set_is_exactly_three` and `test_web_sale_picker_data_attrs` in
`tests/test_sales.py` — marker lines only, never a test body. `tests/test_sales.py` is declared in
this plan's `files_modified`; no other wave-2 plan writes it.
  </action>
  <verify>
    <automated>uv run pytest tests/test_sales.py -k "customer_mode or new_customer_field_set or picker_data_attrs" -rxX</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_sales.py -k "customer_mode" -x` PASSES all five, markers removed —
      including `test_web_sale_customer_mode_roundtrip_preserves_both_modes`, the D-03 contract and
      the phase's highest-risk test.
    - `hx-include` present on the radio: `grep -c 'hx-include' app/templates/partials/sale_customer.html`
      >= 2 (the fieldset AND the shipped quick-create button). A radio `hx-get` WITHOUT `hx-include`
      is the phase's single most likely defect — it passes a first-switch manual test and fails only
      on the switch back.
    - D-07 cap holds: `grep -cE 'name="(phone|telegram|email|social|address)"' app/templates/partials/sale_customer.html`
      returns 0.
    - Anon label is «Без покупателя (розница)», not «Аноним»:
      `grep -c 'Аноним' app/templates/partials/sale_customer.html` returns 0.
    - Hard-contract ids intact: `grep -c 'id="customer-header"\|id="customer-id-input"\|id="customer-selected"\|id="customer-chip-text"\|id="customer-default"' app/templates/partials/sale_customer.html` >= 5.
    - `form="sale-form"` still on `#customer-id-input`: `grep -n 'id="customer-id-input"' -A1 app/templates/partials/sale_customer.html | grep -c 'form="sale-form"'` >= 1.
    - No `|safe`: `grep -c '| safe' app/templates/partials/sale_customer.html` returns 0.
    - Only markers changed in the test file: `git diff tests/test_sales.py` shows removed
      `@pytest.mark.xfail` lines and no edit to any test body.
    - No new CSS: `git diff --stat app/static/style.css` is empty.
    - `uv run ruff format --check .` passes.
  </acceptance_criteria>
  <done>The customer header renders as a 3-way radio group whose every switch round-trips all three modes' state through the server without loss.</done>
</task>

<task type="auto">
  <name>Task 3: Route every sale_create branch through _customer_context and add the D-10 guard</name>
  <files>app/routes/sales.py, tests/test_sales.py</files>
  <read_first>
    - `app/routes/sales.py:389-398` — `sale_create`'s signature. It reads ONLY `customer_id` today and
      never looks at `name`/`surname`/`consultant_number` — which is exactly why filling the 3 fields
      and pressing «Оформить продажу» currently writes a SILENT walk-in.
    - **The FIVE D-12 render paths** — every one must go through `_customer_context`:
      `:100` (`sale_new_page`) and `:465` (the success branch) both build `"customer_id": ""` as a
      LITERAL empty string; `:417`, `:435`, `:447` (the three `sale_create` error branches) build
      `"customer_id": customer_id`. Two different literals, five sites — a grep for one form will
      never find the other. Read all five before editing.
    - `app/routes/sales.py:425-430` — the comment explaining why a below-minimum-only basket must not
      fall through; `app/services/sales.py:431` checks BOTH `oversell` and `below_minimum`.
    - `app/templates/partials/sale_customer.html:17` — the chip renders on `{% if selected %}`, the
      key never set on the error path.
    - `.planning/phases/22-sales-page-rebuild/22-RESEARCH.md` Pitfall 7 (the verified defect with its
      probe output) and Pitfall 5 (the criterion-5 guardrails that must not regress).
  </read_first>
  <action>
Two changes to `sale_create` in `app/routes/sales.py`.

**A. The D-12 fix — one builder on ALL FIVE render paths.** Replace the hand-written `customer_id`
literal in every one of the five context dicts with a merge of `_customer_context(...)`, keeping each
branch's own `errors` / `lines` / `focus_code` / `include_oob_rows` keys intact:
- `:417`, `:435`, `:447` — `sale_create`'s three error branches, which pass `"customer_id": customer_id`;
- `:100` — `sale_new_page`, which passes the LITERAL `"customer_id": ""`;
- `:465` — the success branch, which also passes the LITERAL `"customer_id": ""` and resets to a fresh
  empty header (mode `existing`, customer_id `""`, form `{}`).
Also merge it into `sale_customer_create`'s paths.

**The two literal forms are why there are two tripwires below.** `"customer_id": customer_id` covers
only the three error branches; `"customer_id": ""` covers `sale_new_page` and the success branch. A
criterion asserting only the first would report success with two of the five paths unconverted — so
BOTH greps must return 0.

Because `_customer_context` returns no `errors` key, merge it FIRST and let the branch's own keys
follow, e.g. build the dict as `{**_customer_context(session, customer_mode, customer_id, form_dict),
"errors": {...}, "lines": ..., "focus_code": ..., "include_oob_rows": ...}`. Verify by reading that no
branch's `errors` is clobbered.

The defect this closes was verified empirically: a POST with `qty[]=0` and a valid `customer_id`
returns 422 with `customer_id` surviving in the hidden input but the chip text ABSENT and the chip
block `hidden` — the UI says "no customer" while the DB says otherwise. It is a silent
mis-attribution surface. **Warning sign for review: any surviving `"customer_id": customer_id,` OR
`"customer_id": "",` literal in a render path.**

**B. The D-10 guard.** Widen `sale_create`'s signature by exactly four `Form("")` params:
`customer_mode`, `name`, `surname`, `consultant_number`. Do NOT add `**kwargs` — FastAPI binds only
declared params, so the echoed hidden inputs are otherwise ignored, and that is the mass-assignment
control (T-22-06), not an oversight to "fix".

Before calling `register_sale`, add the guard: when the allow-listed mode is `"new"` AND
`customer_id.strip()` is empty AND ANY of `name` / `surname` / `consultant_number` is non-blank after
stripping, return a 422 rendering `partials/sale_form.html` with
`errors = {"new_customer": "Сначала нажмите «Добавить покупателя» — иначе продажа сохранится без покупателя."}`,
the operator's basket echoed via `_build_lines(session, code, qty, price, batch_id, {})`, and the
customer context merged in (mode `new`, the typed form values preserved). Use the `new_customer` key —
NOT `form` (which `sale_form.html:7-8` already renders, causing a double-render) and NOT
`quick_create` (whose `.error-block` treatment belongs to the create-failure path). The message
renders as `<p class="error">` directly under the «Добавить покупателя» button: problem + solution,
and the solution names the exact button on screen.

The guard fires ONLY when a field is non-blank. All three blank must still write a walk-in — the
anon/walk-in path (D-05) must not regress.

Keep `POST /sales/customer` (`:343`) as the SOLE customer-creation path and leave `register_sale`'s
signature untouched (D-10). Do NOT create the customer inside `sale_create` — that was explicitly
rejected: it changes the endpoint's contract and pulls in duplicate-handling and basket-error-rollback
logic for no operator-visible gain.

Touch NOTHING in `register_sale`. Oversell, batch-required and cash-credit are criterion-5 regression
guards that live there and must keep firing unchanged.

Then remove the `@pytest.mark.xfail` markers from `test_web_sale_chip_survives_422_rerender`,
`test_web_sale_new_customer_requires_button_returns_422`, and
`test_web_sale_new_customer_blank_fields_still_walks_in` in `tests/test_sales.py` (marker lines only).

**Do NOT remove the three `recent_sales` markers** — they belong to 22-03, which runs in wave 3 after
this plan. `tests/test_sales.py` therefore still carries exactly 3 xfail markers when this plan
finishes, and that is the correct end state for wave 2.
  </action>
  <verify>
    <automated>uv run pytest tests/test_sales.py tests/test_finance.py tests/test_sales_search.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_sales.py -x` PASSES with the TEN markers this plan owns removed.
      **Scoped end state for wave 2:** `uv run pytest tests/test_sales.py -q` reports 0 failed,
      **exactly 3 xfailed** (the `recent_sales` trio owned by 22-03, wave 3) and **0 xpassed**. Zero
      xfailed is 22-03's criterion, not this plan's — this plan cannot satisfy it and must not claim it.
    - D-12 closed on all five paths — **both tripwires must return 0**:
      `grep -c '"customer_id": customer_id' app/routes/sales.py` returns 0 (the three error branches), AND
      `grep -c '"customer_id": ""' app/routes/sales.py` returns 0 (`sale_new_page` + the success branch,
      which use the empty-string literal the first grep can never match).
    - `grep -n "def sale_create" -A 12 app/routes/sales.py` shows exactly four new `Form("")` params
      and NO `**kwargs`.
    - `app/services/sales.py` is unmodified by this plan: `git diff --stat app/services/sales.py`
      shows no change attributable to 22-05 (`register_sale`'s signature is untouched per D-10).
    - Criterion-5 tripwires green and UNTOUCHED:
      `uv run pytest tests/test_sales.py -k "oversell or below_minimum or both_warnings or missing_batch_pick or re_echoes_picked_batch" -q` passes,
      and `git diff tests/test_sales.py` shows no edit to those five test bodies.
    - Cash credit unregressed: `uv run pytest tests/test_finance.py -q` passes.
    - Anon/walk-in unregressed:
      `uv run pytest tests/test_sales.py::test_customer_link_walkin_customer_id_null -x` passes.
    - Full suite: `uv run pytest -q` → 0 failed, ≥808 passed.
    - `uv run ruff check . && uv run ruff format --check .` passes.
  </acceptance_criteria>
  <done>Every one of the five render paths resolves `selected` server-side so the chip survives a 422, and a new-customer sale can no longer be silently written as a walk-in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `GET /sales/customer-mode` | untrusted `customer_mode`, `customer_id`, `customer_id_keep`, and all typed field values cross here |
| browser → `POST /sales` | untrusted `customer_id` + the four new echoed fields cross here |
| stored customer name → chip / picker HTML | operator-supplied text re-enters the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | `customer_mode` param on `GET /sales/customer-mode` and `POST /sales` | mitigate | `_CUSTOMER_MODES` tuple allow-list in `_customer_context`; unknown values degrade to `"existing"`. The value is only ever COMPARED (`{% if mode == "new" %}`), never interpolated into an attribute — mirroring `customers.py:262`'s `_SORT_MAP`. Asserted by `test_web_sale_customer_mode_allowlist_rejects_unknown`. |
| T-22-02 | Tampering (stored XSS) | customer name in the chip and picker rows | mitigate | Jinja autoescape; `.dataset` in / `.textContent` out; `<mark>` stays literal template HTML. **No `|safe` anywhere** — asserted by grep in Task 2. Preserves the shipped `customer_picker.html:1-12` contract. |
| T-22-04 | Spoofing / Repudiation | `customer_id` forcing attribution to an arbitrary customer | mitigate | Resolved via `get_customer`; an unresolvable id degrades to "nothing selected" rather than rendering a chip for a deleted customer. FK `fk_sales_customer_id_customers` + `PRAGMA foreign_keys=ON` is the DB backstop. Low severity: single-operator local app — the operator can already reach every customer. |
| T-22-10 | Repudiation | silent mis-attribution (D-12) — UI shows no customer while the DB records one | mitigate | The `_customer_context` builder resolves `selected` on ALL FIVE render paths including all error branches; `test_web_sale_chip_survives_422_rerender` asserts both chip presence and non-hidden state, and the two literal-form greps assert no path was missed. This is a verified live defect, not a hypothetical. |
| T-22-11 | Repudiation | silent walk-in (D-10) — operator believes a customer was recorded, DB says NULL | mitigate | The D-10 422 guard blocks the write and names the exact button to press; `test_web_sale_new_customer_requires_button_returns_422` asserts NO `Sale` row is written. |
| T-22-06 | Tampering | mass assignment via the extra echoed hidden inputs on `POST /sales` | mitigate | FastAPI binds only declared `Form(...)` params; `customer_id_keep` / `customer_q` are simply ignored there. Explicitly do NOT add `**kwargs` — asserted in Task 3's criteria. |
| T-22-07 | Error handling | a raw 500 leaking a stack trace from the new endpoint | mitigate | House pattern: `except Exception:  # noqa: BLE001` + `logger.exception(...)` + a Russian block error at 422 — never a raw 500 (`sales.py:358-361` precedent). |
| T-22-SC | Tampering | package installs | accept | No install occurs; `pyproject.toml` untouched. |
</threat_model>

<verification>
- `uv run pytest tests/test_sales.py tests/test_finance.py tests/test_sales_search.py -q` → 0 failed,
  exactly 3 xfailed (22-03's `recent_sales` trio, removed in wave 3), 0 xpassed.
- `uv run pytest -q` → 0 failed, ≥808 passed.
- `uv run ruff check . && uv run ruff format --check .` → clean.
- `grep -c '"customer_id": customer_id' app/routes/sales.py` → 0.
- `grep -c '"customer_id": ""' app/routes/sales.py` → 0.

**Manual-only (22-VALIDATION.md):** type into «Новый» fields → switch to «Существующий» → pick a
customer → switch back to «Новый»; confirm the typed values are still there and nothing was silently
reset. This is D-03 felt end-to-end (focus, in-flight typing, swap guards interacting) and the server
round-trip test cannot cover the perceived behavior.
</verification>

<success_criteria>
- ROADMAP criterion 3 is met: new / existing / anonymous is an explicit radio choice at the top of the form.
- D-03 holds: no radio switch loses data, on any number of switches.
- The two verified defects (D-10 silent walk-in, D-12 chip loss) are closed and regression-tested, with
  all five render paths converted.
- Criterion-5 guardrails (oversell, batch selection, cash credit) still fire, untouched.
</success_criteria>

<output>
Create `.planning/phases/22-sales-page-rebuild/22-05-SUMMARY.md` when done.
</output>

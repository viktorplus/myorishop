---
phase: 22-sales-page-rebuild
plan: 03
type: execute
wave: 3
depends_on: ["22-01", "22-05"]
files_modified:
  - app/services/sales.py
  - app/templates/partials/recent_sales.html
  - tests/test_sales.py
autonomous: true
requirements: [SALE-07]

must_haves:
  truths:
    - "Recent-sales list shows each sale's customer first + last name (SALE-07)"
    - "A walk-in sale renders a muted «Розница» label, never a blank, an em-dash, or the literal None (D-06)"
    - "The customer join is a double outerjoin, so no walk-in sale is dropped from the list (D-06)"
    - "Walk-in stays customer_id = NULL — no «Аноним» customer row is created (D-05)"
    - "The /returns page, the second caller of recent_sales, still renders"
    - "tests/test_sales.py carries no Phase-22 xfail marker once this plan lands — it is the last plan to touch that file"
  artifacts:
    - path: "app/services/sales.py"
      provides: "recent_sales() returning {op, product, customer}"
      contains: "outerjoin"
    - path: "app/templates/partials/recent_sales.html"
      provides: "Покупатель column with D-06 muted «Розница» fallback"
      contains: "Розница"
  key_links:
    - from: "app/services/sales.py::recent_sales"
      to: "app.models.Customer"
      via: "double outerjoin Operation -> Sale -> Customer"
      pattern: "outerjoin\\(Customer"
    - from: "app/templates/partials/recent_sales.html"
      to: "r.customer"
      via: "Jinja conditional cell"
      pattern: "r\\.customer"
---

<objective>
Add the buyer's name to the recent-sales list (SALE-07), with D-06's muted «Розница» label for
walk-in sales.

Purpose: ROADMAP criterion 4. This is the phase's most independent slice in PRODUCTION code — it
touches neither the customer header nor the total, so it conflicts with nothing 22-04/22-05/22-06
write under `app/`.

**Why it is wave 3 and not wave 2.** This plan and 22-05 both edit `tests/test_sales.py` (each
removes its own xfail markers, both introduced by 22-01). Two plans writing one file in the same wave
risks a lost update, so the pair is serialized: 22-05 lands first, this plan follows. That is the
ONLY reason for the `22-05` dependency — no code in this plan reads anything 22-05 writes. In wave 3
it runs in parallel with 22-06, which shares no file with it.

**This plan is the LAST to touch `tests/test_sales.py`**, so it owns the file's end-state assertion:
zero xfail markers remaining.

Output: `recent_sales()` returns a `customer` key via a portable double outerjoin;
`recent_sales.html` gains a «Покупатель» column.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-sales-page-rebuild/22-PATTERNS.md
@app/services/export.py
</context>

## Artifacts this phase produces

This plan owns:

| Artifact | Kind |
|----------|------|
| `recent_sales(session, limit=10)` returns `list[dict]` with keys `op`, `product`, **`customer`** (new, may be `None`) | service shape change |
| `<th>Покупатель</th>` + its `<td>` in `partials/recent_sales.html` | template column |
| `<span class="muted">Розница</span>` walk-in cell | copy + existing class |
| The three `recent_sales` xfail markers in `tests/test_sales.py` (removal only — the test bodies are 22-01's) | test-marker removal |

Consumed but NOT created here: `app.models.Customer` (existing), `.muted` (`style.css:230`, existing).
**Zero new CSS rules; zero new packages.**

<tasks>

<task type="auto">
  <name>Task 1: recent_sales gains the customer outerjoin</name>
  <files>app/services/sales.py, tests/test_sales.py</files>
  <read_first>
    - `app/services/sales.py:333-342` — `recent_sales`, the 10-line function being extended. Read its
      `.order_by(Operation.created_at.desc(), Operation.seq.desc())` and `.limit(limit)` — these
      DIFFER from the export analog on purpose (newest-first UI listing vs chronological dump) and
      must be preserved.
    - `app/services/export.py:117-125` — the shipped double-outerjoin, the verbatim analog to copy.
    - `app/models.py:409` — `Sale.customer_id` is `Mapped[str | None]` with the comment "walk-in sale
      is valid"; `app/models.py:318` — `Operation.sale_id` is nullable for legacy rows. These two
      nullables are exactly why the join must be `outerjoin`, twice.
    - `app/models.py:381-383` — the house rule: zero `relationship()`/`back_populates` in `app/`; FKs
      are joined manually in the service layer.
  </read_first>
  <action>
Extend `recent_sales` in `app/services/sales.py` to return the buyer alongside the product.

Add `Customer` to this module's existing `from app.models import ...` line (it is not currently
imported here). Change the `select(Operation, Product)` to `select(Operation, Product, Customer)`,
keep the existing `.join(Product, Operation.product_id == Product.id)`, and add
`.outerjoin(Sale, Operation.sale_id == Sale.id)` followed by
`.outerjoin(Customer, Sale.customer_id == Customer.id)` — the exact shape `export.py:117-125` ships.
Keep the existing `.where(Operation.type == "sale")`, the two-key `.order_by(...desc())` and
`.limit(limit)` unchanged. Return `{"op": op, "product": product, "customer": customer}` per row.

`outerjoin` (NOT `join`) on BOTH hops is load-bearing and is the single thing to get right here: an
inner join on `Customer` would silently DROP every walk-in sale from the list instead of labelling it
«Розница», inverting D-06. Adding the `"customer"` key is backward-compatible for both callers.

Update the docstring to state the SALE-07/D-06 contract and why the join must not become an inner
join, mirroring the tone of `export.py:114-116`'s note about its own deliberate divergence.

Portable SQLAlchemy ORM constructs only — no raw SQL, no `relationship()` (CLAUDE.md §What NOT to
Use; house rule at `models.py:381-383`).

Then remove the `@pytest.mark.xfail` marker from `test_recent_sales_includes_walkin` in
`tests/test_sales.py`. The test BODY is 22-01's and must not be edited — remove the marker line only.
A strict XPASS is reported as a FAILURE, so leaving the marker breaks the suite. This plan declares
`tests/test_sales.py` in `files_modified` and runs in wave 3, after 22-05 (the only other plan that
edits that file) — the two never write it concurrently.
  </action>
  <verify>
    <automated>uv run pytest tests/test_sales.py -k "recent_sales_includes_walkin" -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_sales.py -k "recent_sales_includes_walkin" -x` PASSES with the xfail
      marker removed.
    - `grep -c "outerjoin" app/services/sales.py` returns at least 2 (both hops present).
    - `grep -n "\.join(Customer" app/services/sales.py` returns NOTHING — an inner join on Customer is
      the anti-pattern this task exists to avoid.
    - `recent_sales` still orders newest-first: `grep -n "Operation.created_at.desc()" app/services/sales.py`
      returns a hit inside the function.
    - Only the marker changed in the test file: `git diff tests/test_sales.py` shows no edit to
      `test_recent_sales_includes_walkin`'s body — a removed `@pytest.mark.xfail` line and nothing else.
    - `uv run pytest tests/test_returns.py -q` passes — `/returns` is the second caller
      (`routes/returns.py:157`) and the dict-shape change must be backward-compatible.
    - `uv run ruff check app/services/sales.py && uv run ruff format --check app/services/sales.py` passes.
  </acceptance_criteria>
  <done>`recent_sales` returns a `customer` key for every row, `None` for walk-ins, with no row dropped.</done>
</task>

<task type="auto">
  <name>Task 2: recent_sales.html gains the Покупатель column</name>
  <files>app/templates/partials/recent_sales.html, tests/test_sales.py</files>
  <read_first>
    - `app/templates/partials/recent_sales.html` — the file being edited. Read the header comment
      (:1-6) explaining the `hx-swap-oob` contract and the "autoescape only — stored product names
      are untrusted (T-4-01)" rule, and the current 7 columns (:11-36).
    - `app/templates/partials/customer_picker.html:30` — the muted-fallback idiom to copy:
      `{% if customer.surname %}...{% else %}<span class="muted">—</span>{% endif %}`. D-06
      substitutes «Розница» for the em-dash.
    - `.planning/phases/22-sales-page-rebuild/22-UI-SPEC.md` Interaction 16, 17, 18 and the
      Copywriting rows for «Покупатель» / «Розница».
  </read_first>
  <action>
Add the buyer column to `app/templates/partials/recent_sales.html`.

Insert `<th>Покупатель</th>` immediately after the existing `<th>Название</th>`, and its matching
`<td>` at the SAME position in the body row — in the SAME edit. A `<th>`-only change yields a ragged
table (22-RESEARCH.md Pitfall 4).

The cell renders `{{ r.customer.name }} {{ r.customer.surname or '' }}` when `r.customer` is truthy,
and `<span class="muted">Розница</span>` otherwise. `surname or ''` guards the nullable surname
(mirroring `sale_customer.html:18`) — the cell must NEVER render the literal `None`. This is the same
bug class as the standing `/finance` «Комментарий» `None` advisory recorded in STATE.md; do not add a
second instance.

`.muted` already exists at `style.css:230` — **add no CSS**. D-06's «Розница» is deliberately a word,
not a blank or an em-dash, so the operator can tell "no customer recorded" apart from "data missing";
and it is `.muted` grey rather than `.error` red because a walk-in is a valid, deliberate outcome
(22-UI-SPEC.md Color rule 2).

Customer names are untrusted operator-supplied stored text: Jinja autoescape only, **never `|safe`**
(T-22-02). There is no legitimate `|safe` anywhere in this phase.

This partial is included from THREE sites — `pages/sale_form.html:8`, `partials/sale_form.html:84`
(the oob refresh), and `partials/return_form.html:51`. The column therefore also lands on `/returns`.
That is deliberate and accepted (22-UI-SPEC.md Interaction 17: knowing who bought the item is more
relevant on a returns screen, not less); do NOT add an `{% if show_customer %}` flag to suppress it.

Then remove the `@pytest.mark.xfail` markers from `test_web_recent_sales_customer_column` and
`test_web_recent_sales_retail_label_for_walkin` in `tests/test_sales.py` (marker lines only — the test
bodies are 22-01's).

**These are the last two Phase-22 markers in `tests/test_sales.py`.** 22-05 (wave 2) already removed
the ten it owns; this plan removes the remaining three. After this task the file must carry zero
xfail markers — that end-state is asserted below because this plan is the last to write the file.
  </action>
  <verify>
    <automated>uv run pytest tests/test_sales.py -k "recent_sales" tests/test_returns.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_sales.py -k "recent_sales" -x` PASSES with all three xfail markers removed.
    - **File end-state (this plan is the last to touch it):** `uv run pytest tests/test_sales.py -q`
      reports 0 failed, **0 xfailed and 0 xpassed** — every Phase-22 marker from 22-01 is now gone
      (ten removed by 22-05 in wave 2, three by this plan).
    - `uv run pytest tests/test_returns.py -q` passes — confirms no test asserts an exact column count
      and the third include site renders (22-RESEARCH.md Pitfall 4).
    - `<th>` count equals `<td>` count in the table: verify the rendered `/sales/new` page has no
      ragged row. `grep -c "<th>" app/templates/partials/recent_sales.html` returns 8.
    - `grep -c "| safe\|@safe" app/templates/partials/recent_sales.html` returns 0.
    - `grep -c "Розница" app/templates/partials/recent_sales.html` returns at least 1, and the label
      is wrapped in `class="muted"`, not `class="error"`.
    - Only markers changed in the test file: `git diff tests/test_sales.py` shows removed
      `@pytest.mark.xfail` lines and no edit to any test body.
    - No CSS file changed: `git diff --stat app/static/style.css` is empty.
    - Full suite: `uv run pytest -q` → 0 failed, ≥808 passed.
    - `uv run ruff check . && uv run ruff format --check .` passes.
  </acceptance_criteria>
  <done>The recent-sales table shows the buyer's first + last name, with muted «Розница» for walk-ins, on both /sales/new and /returns — and tests/test_sales.py carries no xfail marker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored customer name → rendered HTML | operator-supplied text re-enters the page in a new table cell |
| DB → `recent_sales` | no untrusted input; `limit` is server-supplied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-02 | Tampering (stored XSS) | new «Покупатель» cell in `recent_sales.html` | mitigate | Jinja autoescape only; **no `|safe`**, no `innerHTML`. The cell renders `{{ r.customer.name }}` directly — acceptance criteria assert zero `|safe` in the file (T-4-01 precedent, the same rule the existing product-name cells follow). |
| T-22-06 | Tampering | SQL injection via the new join | mitigate | SQLAlchemy Core/ORM bound params only; no raw SQL, no f-string SQL. The join adds no user-controlled predicate — it joins on FK equality alone. |
| T-22-07 | Information disclosure | buyer name now visible on `/returns` | accept | Single local operator with no auth and no per-record ownership (CLAUDE.md §Constraints: "1 operator in year one"). The operator can already open every customer card; surfacing the name on a page they already own discloses nothing new. Deliberate per 22-UI-SPEC.md Interaction 17. |
| T-22-SC | Tampering | package installs | accept | No install occurs; `pyproject.toml` untouched. |
</threat_model>

<verification>
- `uv run pytest tests/test_sales.py tests/test_returns.py -q` → 0 failed, 0 xfailed, 0 xpassed.
- `uv run pytest -q` → 0 failed, ≥808 passed.
- `uv run ruff check . && uv run ruff format --check .` → clean.
- `git diff --stat app/static/style.css` → empty (zero new CSS this phase).
</verification>

<success_criteria>
- ROADMAP criterion 4 is met: the recent-sales list shows each sale's customer name.
- No walk-in sale is dropped from the list, and none renders as blank or `None`.
- `/returns` is unregressed.
- `tests/test_sales.py` is marker-free: every xfail 22-01 introduced there is removed.
</success_criteria>

<output>
Create `.planning/phases/22-sales-page-rebuild/22-03-SUMMARY.md` when done.
</output>

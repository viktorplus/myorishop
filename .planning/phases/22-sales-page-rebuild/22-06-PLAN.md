---
phase: 22-sales-page-rebuild
plan: 06
type: execute
wave: 3
depends_on: ["22-02", "22-05"]
files_modified:
  - app/routes/mobile_sales.py
  - app/templates/mobile_partials/sale_customer.html
  - app/templates/mobile_partials/customer_picker.html
autonomous: true
requirements: [SALE-03, SALE-04, SALE-05, SALE-06]

must_haves:
  truths:
    - "The mobile sale wizard gets the SAME 3-way customer selector as desktop — full parity, not a reduced set (D-04)"
    - "The mobile selector is its own partial with its own ids, not a reuse of the desktop picker (D-04)"
    - "The mobile mode radio carries hx-include, because an htmx GET sends only its own value (D-01, D-03)"
    - "The mobile mode swap targets #m-customer-header and never #wizard-step, so the code_acc[] basket is never wiped (D-04)"
    - "«Существующий» is the mobile default too (D-02)"
    - "Mobile new-customer keeps exactly the same 3 fields — Имя / Фамилия / Номер консультанта (D-07)"
    - "Mobile anonymous mode shows no extra fields (D-05)"
    - "Mobile customer creation goes through the same explicit button path; register_sale is untouched (D-10)"
  artifacts:
    - path: "app/templates/mobile_partials/sale_customer.html"
      provides: "Mobile 3-way customer selector rooted at #m-customer-header"
      contains: "m-customer-header"
    - path: "app/templates/mobile_partials/customer_picker.html"
      provides: "Mobile customer search-result card list"
      contains: "mobile-card"
    - path: "app/routes/mobile_sales.py"
      provides: "GET /m/sales/customer-mode, GET /m/sales/customer-search, POST /m/sales/customer"
      contains: "m/sales/customer-mode"
  key_links:
    - from: "app/templates/mobile_partials/sale_customer.html"
      to: "GET /m/sales/customer-mode"
      via: "fieldset hx-get with hx-include, targeting #m-customer-header"
      pattern: "m-customer-header"
    - from: "app/routes/mobile_sales.py"
      to: "app.routes.sales._CUSTOMER_MODES"
      via: "import of the shared allow-list"
      pattern: "_CUSTOMER_MODES"
---

<objective>
Give the mobile sale wizard the same 3-way customer selector the desktop form has (D-04) — its own
partials, its own ids, its own endpoints.

Purpose: mobile currently has NO customer picker at all (`mobile_sales.py:346` hardcodes
`customer_id=""`). D-04 is a confirmed scope EXPANSION beyond SALE-03..07's literal wording — the
operator explicitly asked for parity, so this is in-scope, not a gap to flag later.

Output: `mobile_partials/customer_picker.html` and `mobile_partials/sale_customer.html` (both new);
three new endpoints on `app/routes/mobile_sales.py`.

The wiring into the basket screen and the `POST /m/sales` write path land in **22-07**.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-sales-page-rebuild/22-PATTERNS.md
@.planning/phases/22-sales-page-rebuild/22-UI-SPEC.md
@app/templates/partials/sale_customer.html
</context>

## Artifacts this phase produces

This plan owns:

| Artifact | Kind |
|----------|------|
| `app/templates/mobile_partials/sale_customer.html` | new template, root `#m-customer-header` |
| `app/templates/mobile_partials/customer_picker.html` | new template, `.mobile-card` result list |
| `GET /m/sales/customer-mode`, `GET /m/sales/customer-search`, `POST /m/sales/customer` | endpoints |
| `#m-customer-header`, `#m-customer-id-input`, `#m-customer-selected`, `#m-customer-chip-text`, `#m-customer-default`, `#m-customer-q`, `#m-customer-picker` | element ids (mobile-scoped — must NOT collide with the desktop ids) |
| `_m_customer_context(...)` | function, `app/routes/mobile_sales.py` |

Reused unchanged: `search_customers` / `customer_search_view` / `create_customer` / `get_customer`
(`app/services/customers.py`), `_CUSTOMER_MODES` (imported from `app.routes.sales`).
**Zero new CSS rules; zero new packages.**

**No-analog warning (22-PATTERNS.md §No Analog Found):** `mobile_partials/customer_picker.html` has
NO single analog — no mobile search-picker exists in any wizard. Compose it from
`partials/customer_picker.html` (the `.dataset`/`.textContent` handler + `<mark>` highlighting) and
`mobile_partials/receipts_step_batch.html:30-51` + `batch_card_picker.html:46-64` (the `.mobile-card`
list shape). Do not expect one file to copy.

<tasks>

<task type="auto">
  <name>Task 1: New mobile customer picker partial</name>
  <files>app/templates/mobile_partials/customer_picker.html</files>
  <read_first>
    - `app/templates/partials/customer_picker.html` — the ENTIRE file. Read the header comment (:1-12)
      stating the T-4-05 contract, the `data-id`/`data-name`/`data-surname` attributes and the
      `hx-on:click` handler (:21-28), the `<mark>` highlighting via `row.name_seg` (:27), the surname
      cell's muted em-dash fallback (:30), and the `.empty-state` «Ничего не найдено…» block (:37-38).
      **Its handler hard-codes FOUR desktop ids** — that is exactly why mobile needs its own partial.
    - `app/templates/mobile_partials/batch_card_picker.html:46-64` — the `.mobile-card` button list
      shape (44px touch target via `style.css:329`).
    - `app/services/customers.py:246` — `customer_search_view`, which already returns `q` plus rows of
      `{customer, name_seg, consultant_seg}`. The search BACKEND needs zero changes.
    - `.planning/phases/22-sales-page-rebuild/22-UI-SPEC.md` Interaction 8, 9 and the Copywriting rows
      for the search placeholder / empty state / chip.
  </read_first>
  <action>
Create `app/templates/mobile_partials/customer_picker.html` — the mobile search-result list.

Root `<div id="m-customer-picker" class="customer-picker">`, consuming the SAME context
`customer_search_view` already returns (`q` + rows of `{customer, name_seg, consultant_seg}`) — the
search backend is reused unchanged.

Each result is a `<button type="button" class="mobile-card">` carrying `data-id="{{ customer.id }}"`,
`data-name="{{ customer.name }}"`, `data-surname="{{ customer.surname or '' }}"`, and an
`hx-on:click` that (a) sets `#m-customer-id-input`'s value from `this.dataset.id`, (b) sets
`#m-customer-chip-text`'s `.textContent` to `'Покупатель: ' + this.dataset.name + (this.dataset.surname ? (' ' + this.dataset.surname) : '')`,
(c) unhides `#m-customer-selected`, and (d) hides `#m-customer-default`. This is the desktop handler's
exact shape with the four ids swapped to their `m-` prefixed twins — the `m-` prefix is the whole
reason this is a separate file (reusing the desktop partial would collide ids across the two trees).

`.mobile-card` supplies the 44px touch target (`style.css:329-330`) — the standing mobile minimum
(Phase 11, `style.css:313-315`). Add no CSS.

Render the match highlighting the desktop way: `{% set pre, match, post = row.name_seg %}{{ pre }}{% if match %}<mark>{{ match }}</mark>{% endif %}{{ post }}`.
`<mark>` is LITERAL template HTML wrapped around an autoescaped segment — never `|safe`. Do the same
for `consultant_seg`.

Empty state: reuse the desktop copy verbatim — «Ничего не найдено по запросу „{{ q }}“. Проверьте имя
или номер консультанта.» in the existing `.empty-state` + `.muted` treatment. It is already
problem + next step; do not reword it.

Header comment must state: the `.dataset`-in / `.textContent`-out contract (customer names are
untrusted operator-supplied stored text, T-22-02 — never build HTML/JS strings from them, never
`|safe`), and WHY this file exists rather than reusing `partials/customer_picker.html` (four
hard-coded desktop ids).
  </action>
  <verify>
    <automated>uv run pytest tests/test_mobile_sales.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/templates/mobile_partials/customer_picker.html` exists.
    - Mobile-scoped ids only: `grep -cE 'customer-id-input|customer-chip-text|customer-selected|customer-default' app/templates/mobile_partials/customer_picker.html`
      matches ONLY `m-` prefixed forms — verify `grep -cE "getElementById\('customer-" app/templates/mobile_partials/customer_picker.html` returns 0 (no desktop id reached).
    - No `|safe`: `grep -c '| safe' app/templates/mobile_partials/customer_picker.html` returns 0.
    - No `innerHTML`: `grep -c 'innerHTML' app/templates/mobile_partials/customer_picker.html` returns 0.
    - 44px target reused: `grep -c 'mobile-card' app/templates/mobile_partials/customer_picker.html` >= 1.
    - No new CSS: `git diff --stat app/static/style.css` is empty.
    - `uv run pytest -q` → 0 failed (the partial is not yet routed; this asserts no collateral break).
  </acceptance_criteria>
  <done>A mobile-styled customer result list exists with its own ids and the untrusted-text contract preserved.</done>
</task>

<task type="auto">
  <name>Task 2: New mobile customer selector partial</name>
  <files>app/templates/mobile_partials/sale_customer.html</files>
  <read_first>
    - `app/templates/partials/sale_customer.html` — the DESKTOP partial as restructured by 22-05. This
      is the behavioral contract to mirror: the 3 modes, the hidden-echo mechanism, the forced-empty
      `customer_id` outside `existing` mode.
    - `app/templates/mobile_partials/receipts_step_batch.html:30-51` — the shipped MOBILE
      `fieldset class="field"` + `<legend>` + `<label class="mobile-card">` radio group. This is the
      mobile radio idiom to copy.
    - `app/templates/mobile_pages/sales.html` — the wizard shell: `<form id="sale-wizard-form">` wraps
      a `#wizard-step` that EVERY step response innerHTML-swaps. Read its header comment on the swap
      contract and the batch-pick card's self-replacing `outerHTML` root — that is the documented
      precedent `#m-customer-header` follows.
    - `.planning/phases/22-sales-page-rebuild/22-UI-SPEC.md` Interaction 9 (the full mobile contract)
      and the Copywriting table.
  </read_first>
  <action>
Create `app/templates/mobile_partials/sale_customer.html` — the mobile 3-way selector.

Root `<div id="m-customer-header" class="customer-header">`. Default
`{% set mode = mode | default("existing") %}` (D-02 parity).

Radio group: `<fieldset class="field">` with `hx-get="/m/sales/customer-mode"`,
`hx-trigger="change"`, `hx-include="#m-customer-header input"`,
**`hx-target="#m-customer-header"`, `hx-swap="outerHTML"`**, `hx-sync="this:replace"`. Inside,
`<legend>Покупатель</legend>` and three `<label class="mobile-card">`-wrapped radios named
`customer_mode`, values `new` / `existing` / `anon`, labels «Новый» / «Существующий» /
«Без покупателя (розница)», with `{% if mode == "..." %}checked{% endif %}`.

**Targeting `#m-customer-header` and NEVER `#wizard-step` is load-bearing.** `#wizard-step` is where
the basket's `code_acc[]` / `qty_acc[]` / `price_acc[]` / `batch_acc[]` hidden inputs live; swapping it
from the mode radio would WIPE the basket. The batch-pick card's self-replacing `#batch-wrap` root is
the documented in-repo precedent for a scoped `outerHTML` root inside `#wizard-step` — follow it.

`hx-include` is mandatory for the same reason as desktop: htmx sends only the triggering element's
own value on a GET; the enclosing form is auto-included only on non-GET requests.

`#m-customer-id-input`: render ALWAYS, in every mode, outside the mode branches —
`<input type="hidden" id="m-customer-id-input" name="customer_id" value="{{ customer_id if mode == 'existing' else '' }}">`.
It needs **no `form=` attribute**: unlike desktop's `#customer-header`, this partial sits INSIDE
`#sale-wizard-form`, and «Оформить продажу» is an `hx-post` — a non-GET — so htmx auto-includes the
whole enclosing form. That is exactly how `code_acc[]` reaches `mobile_sale_create` today, and
`customer_id` rides the same mechanism for free.

Mode blocks, mirroring desktop exactly (D-04 = full parity, NOT a reduced set):
- `existing`: chip (`#m-customer-selected` / `#m-customer-chip-text`, `hidden` when not `selected`)
  with an «Убрать» button that clears `#m-customer-id-input` and re-hides/unhides the two blocks;
  `#m-customer-default` holding the «Поиск покупателя»-labelled `#m-customer-q` search input
  (placeholder «Поиск покупателя по имени…») with `hx-get="/m/sales/customer-search"`,
  `hx-trigger="input changed delay:300ms"`, `hx-target="#m-customer-picker"`, `hx-swap="outerHTML"`,
  `hx-sync="this:replace"`, plus `{% include "mobile_partials/customer_picker.html" %}`.
  Inactive echo: hidden `name`, `surname`, `consultant_number`.
- `new`: `<h2>Новый покупатель</h2>` + EXACTLY the 3 D-07 fields («Имя»; «Фамилия
  <span class="muted">(необязательно)</span>»; «Номер консультанта <span class="muted">(необязательно)</span>»)
  with their per-field error slots, then a «Добавить покупателя» button
  (`hx-post="/m/sales/customer"`, `hx-include="#m-customer-header input"`,
  `hx-target="#m-customer-header"`, `hx-swap="outerHTML"`, `hx-disabled-elt="this"`), then the D-10
  error slot `{% if errors.new_customer %}<p class="error">{{ errors.new_customer }}</p>{% endif %}`.
  Inactive echo: hidden `customer_id_keep`, `customer_q`.
  **D-07 is a hard cap: exactly 3 fields on mobile too.** None of Phase 21's profile fields.
- `anon`: the muted caption ONLY — «Продажа без покупателя. В списке продаж она будет отмечена как
  «Розница».» — and ZERO input/select/textarea. Inactive echo: hidden `customer_id_keep`,
  `customer_q`, `name`, `surname`, `consultant_number`.

Guard field values as `{{ form.name if form else '' }}` (the desktop-shipped guard shape).

`.field`, `.mobile-card`, `.muted`, `.error`, `.customer-chip` all exist — add no CSS. `.mobile-card`
labels supply the 44px touch target.

Header comment must record: the D-04 parity contract, WHY the mode radio must target
`#m-customer-header` and never `#wizard-step`, and why `form=` is unnecessary here (the non-GET
auto-include rule) — the inverse of desktop's situation, and a reader coming from the desktop partial
will otherwise assume it was forgotten.
  </action>
  <verify>
    <automated>uv run pytest tests/test_mobile_sales.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/templates/mobile_partials/sale_customer.html` exists with root `id="m-customer-header"`.
    - The mode radio never targets the wizard step:
      `grep -c 'hx-target="#wizard-step"' app/templates/mobile_partials/sale_customer.html` returns 0,
      and `grep -c 'hx-target="#m-customer-header"' ...` >= 1.
    - `hx-include` present: `grep -c 'hx-include' app/templates/mobile_partials/sale_customer.html` >= 2
      (the fieldset AND the create button).
    - D-07 cap: `grep -cE 'name="(phone|telegram|email|social|address)"' app/templates/mobile_partials/sale_customer.html` returns 0.
    - Anon label is «Без покупателя (розница)»: `grep -c 'Аноним' app/templates/mobile_partials/sale_customer.html` returns 0.
    - No desktop id collision: `grep -cE 'id="customer-(id-input|selected|chip-text|default|q|picker)"' app/templates/mobile_partials/sale_customer.html` returns 0.
    - No new CSS: `git diff --stat app/static/style.css` is empty.
    - `uv run pytest -q` → 0 failed.
  </acceptance_criteria>
  <done>The mobile selector partial exists with full desktop parity, mobile-scoped ids, and a swap root that cannot wipe the basket.</done>
</task>

<task type="auto">
  <name>Task 3: Mobile customer endpoints</name>
  <files>app/routes/mobile_sales.py</files>
  <read_first>
    - `app/routes/mobile_sales.py:1-30` — the module docstring and import block. Note `SAVE_FAILED_ERROR`
      is DUPLICATED here (:30) rather than imported from `routes/sales.py` — a shipped house choice.
    - `app/routes/mobile_finance.py:26` — `from app.routes.reports import _resolve_period`. This is the
      in-repo PRECEDENT for a mobile route importing a private helper from a desktop route module, and
      it is what licenses importing `_CUSTOMER_MODES` from `app.routes.sales`.
    - `app/routes/sales.py` — `_CUSTOMER_MODES`, `_customer_context`, `sale_customer_mode`,
      `sale_customer_search` (:336) and `sale_customer_create` (:343) as landed by 22-05. These are the
      three endpoints being mirrored for mobile.
    - `app/routes/mobile_sales.py:353-359` — the mobile-specific `session.rollback()`-FIRST error
      pattern (WR-01), required whenever a query follows a failure in the same branch.
    - `app/services/customers.py:121` — `create_customer` (maintains `search_lc` at :166 — skip it and
      the new customer is invisible to autocomplete forever; its `address`/`contacts` params default
      precisely to keep this call site 3-field).
  </read_first>
  <action>
Add three endpoints plus a context builder to `app/routes/mobile_sales.py`.

Import `_CUSTOMER_MODES` from `app.routes.sales` (the `mobile_finance.py:26` precedent licenses the
cross-module import of a private helper). Import `create_customer`, `customer_search_view`, and
`get_customer` from `app.services.customers`.

1. `_m_customer_context(session, mode, customer_id, form) -> dict` — the mobile twin of
   `_customer_context`. Same contract: allow-list `mode` against the imported `_CUSTOMER_MODES`
   (default `"existing"`), resolve `selected` via `get_customer` only when mode is `existing` and the
   id is non-empty, degrade an unresolvable id to "nothing selected", and return `mode`,
   `customer_id`, `selected`, `form` — **and no `errors` key** (callers own errors; returning one
   would clobber a caller's real errors on merge).
   Do NOT import `_customer_context` itself: it is bound to the desktop template's context shape, and
   the mobile partial's echo field set differs. Mirror the LOGIC, not the object — and say so in the
   docstring so a future reader does not "de-duplicate" them into a shared helper that then has to
   serve two templates.

2. `GET /m/sales/customer-mode` → `mobile_sale_customer_mode(request, customer_mode="existing",
   customer_id="", customer_id_keep="", customer_q="", name="", surname="", consultant_number="",
   session=Depends(get_session))`, rendering `mobile_partials/sale_customer.html` with
   `{**_m_customer_context(session, customer_mode, customer_id or customer_id_keep, {...}), "errors": {}}`.
   Coalesce `customer_id or customer_id_keep` — whichever mode is leaving supplies one of them.

3. `GET /m/sales/customer-search` → `mobile_sale_customer_search(request, q="", session=...)`,
   rendering `mobile_partials/customer_picker.html` from `customer_search_view(session, q)`. The
   search backend needs ZERO changes — `search_customers` already matches name, surname AND
   consultant number via the `search_lc` shadow column, and Cyrillic folding CANNOT happen in SQLite
   (`customers.py:5-7`), so never reimplement it in SQL.

4. `POST /m/sales/customer` → `mobile_sale_customer_create(request, name=Form(""), surname=Form(""),
   consultant_number=Form(""), customer_q=Form(""), session=...)`, calling `create_customer` and
   re-rendering `mobile_partials/sale_customer.html` in `existing` mode with the new customer selected
   (so the chip appears), mirroring the desktop endpoint's behavior. Use `create_customer` — never a
   direct `session.add(Customer(...))`, which would skip the `search_lc` maintenance and make the new
   customer permanently invisible to the autocomplete.
   On validation errors, re-render in `new` mode at 422 with the per-field `errors` and the typed
   `form` preserved.
   Wrap in the house error pattern: `except Exception:  # noqa: BLE001 — UI-SPEC: block error, never a
   raw 500`, `session.rollback()` FIRST (WR-01 — mobile-specific; an unexpected failure may leave the
   session needing rollback and a following query would otherwise raise PendingRollbackError instead of
   this graceful 422), then `logger.exception(...)`, then `errors.quick_create = SAVE_FAILED_ERROR` at 422.

Declare all three as literal paths BEFORE any parameterized `/m/sales/{...}` route.

Do NOT touch `mobile_sale_create` in this plan — the `customer_id` write path is 22-07's task.

Then remove the `@pytest.mark.xfail` marker from `test_mobile_customer_selector_renders_on_basket`
only if the selector is already reachable; if the basket does not yet include the partial (22-07's
task), LEAVE that marker in place and note it in the summary.
  </action>
  <verify>
    <automated>uv run pytest tests/test_mobile_sales.py -k "acc_survives" -rxX</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_mobile_sales.py -k "acc_survives" -x` PASSES with its xfail marker
      removed — the mode endpoint exists and its response root is `#m-customer-header`, containing no
      `id="wizard-step"`.
    - `grep -n "_CUSTOMER_MODES" app/routes/mobile_sales.py` shows the import from `app.routes.sales`.
    - `_m_customer_context` returns no `errors` key:
      `grep -n -A 12 "def _m_customer_context" app/routes/mobile_sales.py` shows no `"errors"` in the return dict.
    - `grep -c "session.add(Customer" app/routes/mobile_sales.py` returns 0 — creation goes through
      `create_customer` so `search_lc` is maintained.
    - Route order: the three literal `/m/sales/customer*` paths are declared before any parameterized
      `/m/sales/{...}` route — verify via `grep -n '@router' app/routes/mobile_sales.py`.
    - `mobile_sale_create` untouched by this plan: `git diff app/routes/mobile_sales.py` shows no edit
      inside `def mobile_sale_create`.
    - Full suite: `uv run pytest -q` → 0 failed, ≥808 passed.
    - `uv run ruff check . && uv run ruff format --check .` passes.
  </acceptance_criteria>
  <done>Three mobile customer endpoints exist, mirroring the desktop contracts against mobile templates and mobile ids.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `GET /m/sales/customer-mode` | untrusted `customer_mode` / `customer_id` / typed values cross here |
| browser → `GET /m/sales/customer-search` | untrusted `q` crosses here |
| browser → `POST /m/sales/customer` | untrusted 3 profile fields cross here |
| stored customer name → mobile chip / picker HTML | operator-supplied text re-enters the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | `customer_mode` on the mobile endpoints | mitigate | `_m_customer_context` allow-lists against the `_CUSTOMER_MODES` tuple imported from `app.routes.sales`; unknown values degrade to `"existing"`. Compared, never interpolated. |
| T-22-02 | Tampering (stored XSS) | mobile chip + picker rendering customer names | mitigate | Jinja autoescape; `.dataset` in / `.textContent` out; `<mark>` literal template HTML. Acceptance criteria assert zero `|safe` and zero `innerHTML` in both new partials. |
| T-22-12 | Tampering | SQL injection / LIKE-wildcard abuse via `q` | mitigate | Reuses `search_customers` unchanged, which uses `.contains(q_lc, autoescape=True)` (`customers.py:241`) — `autoescape=True` neutralises `%`/`_` wildcards. No raw SQL added; never reimplement the search in SQL (SQLite cannot fold Cyrillic). |
| T-22-13 | Tampering | invalid/oversized new-customer field values | mitigate | Reuses `create_customer`, which enforces the WR-05 max-length guards mirroring the declared column widths and maintains `search_lc` (`customers.py:166`). Direct `session.add(Customer(...))` is forbidden — asserted by grep. |
| T-22-07 | Error handling | a raw 500 from a new mobile endpoint | mitigate | House pattern with the mobile-specific `session.rollback()` FIRST (WR-01), then `logger.exception(...)`, then a Russian block error at 422. |
| T-22-14 | Tampering | mode swap wiping the accumulated basket | mitigate | The mode radio targets `#m-customer-header` with `outerHTML`, never `#wizard-step`; asserted by `test_mobile_selector_swap_acc_survives`. A wiped basket is data loss, which is the project's stated core value ("without losing any data"). |
| T-22-SC | Tampering | package installs | accept | No install occurs; `pyproject.toml` untouched. |
</threat_model>

<verification>
- `uv run pytest tests/test_mobile_sales.py -q` → 0 failed.
- `uv run pytest -q` → 0 failed, ≥808 passed.
- `uv run ruff check . && uv run ruff format --check .` → clean.
- `git diff --stat app/static/style.css` → empty.
</verification>

<success_criteria>
- The mobile wizard has a 3-way customer selector at full desktop parity (D-04).
- The selector's swap root cannot wipe the `code_acc[]` basket.
- Mobile ids are namespaced and collide with no desktop id.
</success_criteria>

<output>
Create `.planning/phases/22-sales-page-rebuild/22-06-SUMMARY.md` when done.
</output>

---
phase: 22-sales-page-rebuild
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_sales.py
autonomous: true
requirements: [SALE-01, SALE-03, SALE-04, SALE-05, SALE-06, SALE-07]

must_haves:
  truths:
    - "A test pins that GET /sales/new renders three radios with «Существующий» pre-checked (D-01, D-02)"
    - "A test pins that switching mode to «Новый» and back preserves both modes' typed values (D-03)"
    - "A test pins that an unknown customer_mode degrades to «Существующий» and is never echoed raw (D-01)"
    - "A test pins that «Аноним» mode renders zero input elements (D-05)"
    - "A test pins that a 422 re-render keeps the selected-customer chip visible (D-12)"
    - "A test pins that «Новый» mode with filled fields and no created customer returns 422, never a silent walk-in (D-10)"
    - "A test pins that the inline new-customer block has exactly 3 fields and no Phase-21 profile fields (D-07)"
    - "A test pins that recent sales renders a «Покупатель» column with muted «Розница» for walk-in rows (D-06)"
    - "The full suite stays green after this plan — new tests land as strict xfail, not as red failures"
  artifacts:
    - path: "tests/test_sales.py"
      provides: "Wave-0 red-side coverage for SALE-03/04/05/06/07 + SALE-01 regression guard"
      contains: "customer_mode_roundtrip"
  key_links:
    - from: "tests/test_sales.py"
      to: "GET /sales/customer-mode"
      via: "TestClient GET assertions"
      pattern: "sales/customer-mode"
---

<objective>
Land the desktop red-side test coverage for the customer selector (SALE-03/04/05/06) and the
recent-sales customer column (SALE-07), plus a SALE-01 regression guard on the already-shipped
basket table.

Purpose: 22-VALIDATION.md marks 11 of these tests as `❌ W0` — they do not exist. Wave 2 cannot be
verified without them. Writing them first also pins the exact markup contract Wave 2 must satisfy,
so the implementer builds against an assertion instead of a prose description.

Output: `tests/test_sales.py` extended with 13 tests. New-behavior tests land as strict xfail so the
suite stays green (baseline 808 passed); Wave 2 removes each marker as it turns the test green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-sales-page-rebuild/22-VALIDATION.md
@.planning/phases/22-sales-page-rebuild/22-UI-SPEC.md
</context>

## Artifacts this phase produces

Phase-wide contract. This plan asserts against it; the owning plan builds it.

| Artifact | Kind | Owner |
|----------|------|-------|
| `_CUSTOMER_MODES = ("new", "existing", "anon")` | tuple allow-list, `app/routes/sales.py` | 22-05 |
| `_customer_context(session, mode, customer_id, form) -> dict` | function, `app/routes/sales.py` | 22-05 |
| `GET /sales/customer-mode` | endpoint | 22-05 |
| `#customer-header`, `#customer-id-input`, `#customer-selected`, `#customer-chip-text`, `#customer-default`, `#customer-q`, `#customer-picker` | element ids (existing ids preserved) | 22-05 |
| `errors.new_customer` | context key, D-10 guard message slot | 22-05 |
| `app/static/sale-total.js` (`recalcSaleTotal`, `moneyToCents`, `qtyToInt`, `formatCents`) | JS file + `window.recalcSaleTotal` | 22-04 |
| `#sale-total`, `#sale-total-amount`, `#sale-total-units`, `#sale-total-warning` | element ids | 22-04 |
| `recent_sales()` returns `{"op", "product", "customer"}` | service shape change | 22-03 |
| `<th>Покупатель</th>` column | `partials/recent_sales.html` | 22-03 |
| `app/templates/mobile_partials/sale_customer.html` | new template | 22-06 |
| `app/templates/mobile_partials/customer_picker.html` | new template | 22-06 |
| `GET /m/sales/customer-mode`, `GET /m/sales/customer-search`, `POST /m/sales/customer` | endpoints | 22-06 |
| `#m-customer-header`, `#m-customer-id-input`, `#m-customer-selected`, `#m-customer-chip-text`, `#m-customer-default`, `#m-customer-q`, `#m-customer-picker` | element ids | 22-06 |
| `customer_id` form param on `POST /m/sales` | endpoint param | 22-07 |
| `hx-include="closest form"` on the shared batch card | attribute, `batch_card_picker.html` | 22-07 |
| `tests/test_sales_total.py` | new test file | 22-02 |

**Zero new CSS rules and zero new packages this phase** (22-UI-SPEC.md preamble; 22-RESEARCH.md
§Package Legitimacy Audit). A plan proposing either is a scope deviation.

<tasks>

<task type="auto">
  <name>Task 1: SALE-03/SALE-06 mode-selector tests</name>
  <files>tests/test_sales.py</files>
  <read_first>
    - `tests/test_sales.py` — the suite being extended. Read `test_web_sale_page_renders_form` (:570)
      and `test_web_customer_search_returns_rows` (:749) for the house web-test idiom.
    - `tests/conftest.py` — the `client`, `session`, `customer`, `stocked_product` fixtures. The
      `customer` fixture already seeds «Анна Иванова» / consultant `12345` with `search_lc` set; do
      NOT create a new fixture (22-VALIDATION.md §Wave 0: "Shared fixtures: none needed").
    - `.planning/phases/22-sales-page-rebuild/22-UI-SPEC.md` Interaction 1, 3, 5, 7 — the mode-block
      content contract and the exact Russian copy these tests assert on.
  </read_first>
  <action>
Add five tests to `tests/test_sales.py`, each decorated
`@pytest.mark.xfail(strict=True, reason="SALE-03: customer mode selector lands in 22-05")`.
Strict xfail keeps the suite green now and forces 22-05 to remove the marker (a strict XPASS is
reported as a failure), so the marker cannot be forgotten.

1. `test_web_sale_customer_mode_default_renders_three_radios` — `GET /sales/new` returns 200; body
   contains `name="customer_mode"` three times, with `value="new"`, `value="existing"`,
   `value="anon"`; the `existing` radio carries `checked` and the other two do not (D-02). Assert the
   legend «Покупатель» and the three labels «Новый», «Существующий», «Без покупателя (розница)»
   (22-UI-SPEC.md Copywriting — note the anon label is NOT «Аноним»).
2. `test_web_sale_customer_mode_new_renders_three_fields` — `GET /sales/customer-mode?customer_mode=new`
   returns 200; body contains `name="name"`, `name="surname"`, `name="consultant_number"`, the
   heading «Новый покупатель», and the button «Добавить покупателя».
3. `test_web_sale_customer_mode_roundtrip_preserves_both_modes` — the D-03 contract and the phase's
   highest-risk test. Step 1: `GET /sales/customer-mode?customer_mode=new&customer_id=<customer.id>&customer_q=Анна`
   → assert the response still carries the existing-mode state as hidden echo (an input named
   `customer_id_keep` whose value is `customer.id`, and an input named `customer_q` valued `Анна`).
   Step 2: feed that echoed state back as the switch-back request
   `GET /sales/customer-mode?customer_mode=existing&customer_id_keep=<customer.id>&customer_q=Анна&name=Пётр&surname=Петров&consultant_number=999`
   → assert the chip is rendered (body contains «Покупатель: Анна») AND the new-mode values survive
   as hidden echo (inputs named `name`/`surname`/`consultant_number` valued `Пётр`/`Петров`/`999`).
   Assert on rendered `value="..."` attributes, not on substring presence alone.
4. `test_web_sale_customer_mode_allowlist_rejects_unknown` — `GET /sales/customer-mode?customer_mode=<script>alert(1)</script>`
   returns 200, the `existing` radio is `checked` (allow-list fallback to D-02's default), and the raw
   payload string `<script>alert(1)</script>` does NOT appear anywhere in the body (T-22-01).
5. `test_web_sale_customer_mode_anon_renders_no_inputs` — `GET /sales/customer-mode?customer_mode=anon`
   returns 200; the anon block renders the muted caption «Продажа без покупателя.» and contains no
   `name="name"` / `name="surname"` / `name="consultant_number"` VISIBLE field. Scope the "no fields"
   assertion to exclude `type="hidden"` — SALE-06 constrains visible inputs, and the hidden echoes
   plus `#customer-id-input` are required by D-03/D-05 (22-UI-SPEC.md Interaction 5 + rationale 4).
   Assert `#customer-id-input` renders `value=""` in anon mode.

Import `pytest` if the module does not already import it.
  </action>
  <verify>
    <automated>uv run pytest tests/test_sales.py -k "customer_mode" -rxX</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_sales.py -k "customer_mode" -rxX` reports exactly 5 XFAIL, 0 XPASS,
      0 ERROR. An ERROR means a collection/import bug, not a pending feature — fix it now.
    - `uv run pytest tests/test_sales.py -q` reports 0 failed (xfail is not a failure).
    - `grep -n 'value="anon"' tests/test_sales.py` returns at least one hit and
      `grep -c 'Аноним' tests/test_sales.py` returns 0 — the anon label is «Без покупателя (розница)»
      per 22-UI-SPEC.md Copywriting rationale 1.
    - Task 1 adds zero fixtures to `tests/conftest.py` (file is not in `files_modified`).
  </acceptance_criteria>
  <done>Five strict-xfail mode-selector tests exist and fail for the right reason (missing endpoint/markup), and the suite is green.</done>
</task>

<task type="auto">
  <name>Task 2: SALE-04/SALE-05 chip, picker and new-customer guard tests</name>
  <files>tests/test_sales.py</files>
  <read_first>
    - `tests/test_sales.py` — `test_web_customer_search_returns_rows` (:749),
      `test_web_customer_quick_create_returns_chip` (:756), and
      `test_web_sale_422_re_echoes_picked_batch` (:1041) for the 422-re-render POST idiom this task
      copies (how the existing suite POSTs a bad basket and asserts on the re-rendered body).
    - `app/templates/partials/customer_picker.html` — the `data-id`/`data-name`/`data-surname`
      attribute contract Task 2 asserts on.
    - `.planning/phases/22-sales-page-rebuild/22-RESEARCH.md` Pitfall 7 — the verified chip-loss
      defect, including the exact probe output this test reproduces.
    - `.planning/phases/22-sales-page-rebuild/22-UI-SPEC.md` Interaction 4, 6 and the Copywriting
      row for the D-10 guard message.
  </read_first>
  <action>
Add five tests to `tests/test_sales.py`.

Strict-xfail (`@pytest.mark.xfail(strict=True, reason="SALE-04/05: lands in 22-05")`):

1. `test_web_sale_chip_survives_422_rerender` — the D-12 regression test for a live defect. POST
   `/sales` with a valid `customer_id=<customer.id>` and a basket that 422s (`qty[]=0`, per the
   existing `test_web_sale_missing_batch_pick_returns_422` shape). Assert status 422 AND that the
   response body contains «Покупатель: Анна» (chip text present) AND that the element
   `id="customer-selected"` is NOT `hidden`. Today's code returns the id in the hidden input while
   hiding the chip — assert BOTH halves so the test cannot pass on the hidden input alone.
2. `test_web_sale_picker_data_attrs` — `GET /sales/customer-search?q=Анна` returns 200 and each row
   button carries `data-id`, `data-name`, `data-surname` (the `.dataset`-in/`.textContent`-out
   contract, T-22-02). Assert `|safe` is not required: the response must HTML-escape a customer whose
   name contains `<b>`. Seed that customer inline via the `session` fixture + `create_customer`.
3. `test_web_sale_new_customer_field_set_is_exactly_three` — `GET /sales/customer-mode?customer_mode=new`
   renders exactly the 3 D-07 fields. Assert `name="name"`, `name="surname"`,
   `name="consultant_number"` are present AND that none of `phone`, `telegram`, `email`, `social`,
   `address` appear as a `name=` attribute anywhere in the block (D-07 is a hard cap — 22-UI-SPEC.md
   Interaction 6: "A plan task adding a 4th field to this block is wrong").
4. `test_web_sale_new_customer_requires_button_returns_422` — the D-10 guard. POST `/sales` with
   `customer_mode=new`, `customer_id=""`, `name=Пётр` and an OTHERWISE VALID basket (a real
   `stocked_product` code, qty 1, a valid price, a picked `batch_id[]`) so the 422 can only come from
   the D-10 guard and not from basket validation. Assert status 422, the body contains the exact
   message «Сначала нажмите «Добавить покупателя»», and — critically — that NO `Sale` row was written
   (`session.scalars(select(Sale)).all() == []`). The silent-walk-in bug is precisely a 200 + a
   written walk-in Sale, so the DB assertion is the real contract.
5. `test_web_sale_new_customer_blank_fields_still_walks_in` — the guard's negative case. POST `/sales`
   with `customer_mode=new`, `customer_id=""` and ALL THREE fields blank plus a valid basket → the
   sale still succeeds as a walk-in with `Sale.customer_id IS NULL` (D-05). This pins that the guard
   fires only when a field is non-blank, so it cannot regress the anon/walk-in path.

Test 5 asserts current behavior plus the new `customer_mode` param; keep it strict-xfail too because
`customer_mode` is not a declared `Form` param until 22-05 (FastAPI ignores it today, so the test
would pass for the wrong reason — the xfail marker is removed in 22-05 once the param exists).
  </action>
  <verify>
    <automated>uv run pytest tests/test_sales.py -k "chip_survives_422 or picker_data_attrs or new_customer" -rxX</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_sales.py -k "chip_survives_422 or picker_data_attrs or new_customer" -rxX`
      reports exactly 5 XFAIL, 0 XPASS, 0 ERROR.
    - `uv run pytest tests/test_sales.py -q` reports 0 failed.
    - `test_web_sale_new_customer_requires_button_returns_422` asserts on the DB (`select(Sale)`), not
      only on the response body — verify by `grep -n "select(Sale)" tests/test_sales.py` returning a
      hit inside that test.
    - `test_web_sale_chip_survives_422_rerender` asserts both chip-text presence AND the
      non-`hidden` state of `#customer-selected`.
  </acceptance_criteria>
  <done>Five strict-xfail tests pin the D-12 chip fix, the picker escape contract, D-07's 3-field cap, and both sides of the D-10 guard.</done>
</task>

<task type="auto">
  <name>Task 3: SALE-07 recent-sales tests + SALE-01 regression guard</name>
  <files>tests/test_sales.py</files>
  <read_first>
    - `tests/test_sales.py` — `test_web_sale_page_renders_form` (:570), the test being EXTENDED (not
      duplicated) for SALE-01; and `test_customer_link_walkin_customer_id_null` (:255) for the
      walk-in seeding idiom.
    - `app/templates/partials/recent_sales.html` — current columns
      (Когда/Код/Название/Кол-во/Цена/Сумма/Действие); the new «Покупатель» column lands after «Название».
    - `app/services/sales.py::recent_sales` (:333) — the function whose return shape gains `"customer"`.
    - `.planning/phases/22-sales-page-rebuild/22-UI-SPEC.md` Interaction 16, 18 and the Copywriting
      rows for «Покупатель» / «Розница».
  </read_first>
  <action>
Add three strict-xfail tests (`reason="SALE-07: customer column lands in 22-03"`) and one
non-xfail extension.

Strict-xfail:
1. `test_web_recent_sales_customer_column` — register a sale linked to the `customer` fixture, then
   `GET /sales/new`; assert the body contains `<th>Покупатель</th>` and the cell text «Анна Иванова».
2. `test_web_recent_sales_retail_label_for_walkin` — register a walk-in sale (`customer_id=""` →
   NULL), then `GET /sales/new`; assert the cell renders «Розница» inside a `class="muted"` span and
   that the literal string `None` does NOT appear in the recent-sales block (D-06; this is the same
   bug class as the standing `/finance` «Комментарий» `None` advisory in STATE.md).
3. `test_recent_sales_includes_walkin` — a SERVICE-level test, not web. Seed one walk-in sale and one
   customer-linked sale, call `recent_sales(session)` directly, and assert BOTH rows come back and
   that the walk-in row's `"customer"` key is `None`. This is the outerjoin guard: an inner join
   would silently drop the walk-in row and this test is the only thing that catches it
   (22-RESEARCH.md §Anti-Patterns).

Non-xfail (asserts already-shipped behavior — SALE-01 is a regression guard, NOT a build task, per
22-RESEARCH.md Pitfall 1):
4. Extend the EXISTING `test_web_sale_page_renders_form` (:570) in place — do not add a new test.
   Add assertions that the basket table renders the four headers «Код», «Название», «Кол-во»,
   «Цена продажи» and that the input names `code[]`, `qty[]`, `price[]`, `batch_id[]` are present.
   This test must pass immediately and stay green through every later wave; it is the tripwire
   against a well-meaning executor "rebuilding" the already-shipped SALE-01 table.
  </action>
  <verify>
    <automated>uv run pytest tests/test_sales.py -k "recent_sales or renders_form" -rxX</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_sales.py -k "recent_sales" -rxX` reports exactly 3 XFAIL, 0 XPASS, 0 ERROR.
    - `uv run pytest tests/test_sales.py::test_web_sale_page_renders_form -x` PASSES now (SALE-01 is
      already shipped) and is NOT marked xfail.
    - `test_recent_sales_includes_walkin` calls `recent_sales(session)` directly rather than going
      through the web layer — verify by `grep -n "recent_sales(session)" tests/test_sales.py`.
    - `uv run pytest tests/test_sales.py -q` reports 0 failed.
    - Full suite unregressed: `uv run pytest -q` reports 0 failed and ≥808 passed.
    - `uv run ruff check tests/test_sales.py && uv run ruff format --check tests/test_sales.py` passes.
  </acceptance_criteria>
  <done>Three strict-xfail SALE-07 tests exist, and the SALE-01 regression guard passes green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `GET /sales/customer-mode` | untrusted `customer_mode` / `customer_id` / typed field values cross here |
| browser → `POST /sales` | untrusted basket + customer fields cross here |
| stored customer name → rendered HTML | operator-supplied text re-enters the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | `customer_mode` param | mitigate | This plan's `test_web_sale_customer_mode_allowlist_rejects_unknown` asserts an unknown mode degrades to `existing` and is never echoed raw; 22-05 implements `_CUSTOMER_MODES`. |
| T-22-02 | Tampering (stored XSS) | customer name in chip / picker / recent-sales cell | mitigate | This plan's `test_web_sale_picker_data_attrs` seeds a customer named with `<b>` and asserts the response HTML-escapes it (Jinja autoescape; no `|safe` anywhere this phase). |
| T-22-04 | Spoofing / Repudiation | `customer_id` attribution | mitigate | `test_web_sale_new_customer_requires_button_returns_422` asserts no `Sale` row is written when the customer was never created — closing the silent mis-attribution surface (D-10). |
| T-22-SC | Tampering | package installs | accept | No package-manager install occurs in this phase; `pyproject.toml` is untouched (22-RESEARCH.md §Package Legitimacy Audit: "Not applicable"). No slopsquatting surface exists. |
</threat_model>

<verification>
- `uv run pytest tests/test_sales.py -q` → 0 failed, 13 new tests present, 11 xfailed.
- `uv run pytest -q` → 0 failed, ≥808 passed (baseline preserved; xfail does not reduce the count).
- `uv run ruff check . && uv run ruff format --check .` → clean.
</verification>

<success_criteria>
- 11 strict-xfail tests pin the Wave-2/3/4 contracts for SALE-03/04/05/06/07.
- The SALE-01 regression guard passes green and will fail loudly if the basket table is restructured.
- The repository suite is green — no red wave.
</success_criteria>

<output>
Create `.planning/phases/22-sales-page-rebuild/22-01-SUMMARY.md` when done.
</output>


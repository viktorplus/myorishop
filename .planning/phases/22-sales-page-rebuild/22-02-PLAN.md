---
phase: 22-sales-page-rebuild
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_sales_total.py
  - tests/test_mobile_sales.py
  - tests/test_core.py
autonomous: true
requirements: [SALE-01, SALE-02, SALE-03, SALE-06]

must_haves:
  truths:
    - "A test pins that #sale-total renders under the basket table and carries no name= attribute (D-08)"
    - "A test pins that sale-total.js is loaded on BOTH the desktop and mobile shells (D-08)"
    - "A test pins that a 422 re-render still carries #sale-total, the path that fires no input event (D-08)"
    - "A test pins that both delete buttons carry the recalcSaleTotal hook (D-08)"
    - "A test pins the «итог неполный» marker element exists and is hidden by default (D-09)"
    - "A test pins that the mobile Корзина renders the 3-way customer selector (D-04)"
    - "A test pins that POST /m/sales with a customer_id links the sale, and without one still writes a walk-in (D-04, D-05)"
    - "A test pins that the mobile selector swap does not wipe the code_acc[] basket (D-04)"
    - "A test pins that a batch-card tap preserves the accumulated basket (D-11)"
    - "to_cents accept-set boundaries the JS mirrors are pinned as executable characterization tests (D-08)"
    - "The full suite stays green after this plan — new tests land as strict xfail, not as red failures"
  artifacts:
    - path: "tests/test_sales_total.py"
      provides: "SALE-02 markup and wiring coverage"
      min_lines: 40
    - path: "tests/test_mobile_sales.py"
      provides: "D-04 mobile selector + D-11 batch-card coverage"
      contains: "batch_card_preserves_basket"
    - path: "tests/test_core.py"
      provides: "to_cents boundary pins that the JS parser mirrors"
      contains: "to_cents"
  key_links:
    - from: "tests/test_sales_total.py"
      to: "app/static/sale-total.js"
      via: "script tag presence assertion"
      pattern: "static/sale-total.js"
---

<objective>
Land the red-side coverage for the live running total (SALE-02), the mobile customer selector (D-04),
the mobile batch-card basket-preservation fix (D-11), and the `to_cents` accept-set boundaries the
client parser must mirror.

Purpose: 22-VALIDATION.md marks 9 of these as `❌ W0`. SALE-02 has NO JS runtime in this suite (no
jsdom/Playwright — CLAUDE.md forbids an npm toolchain), so these tests assert **markup and wiring
presence**, never arithmetic; the arithmetic is explicitly manual-only (22-VALIDATION.md
§Manual-Only Verifications). The `to_cents` pins are the closest executable proxy: they fix the
server's accept-set in place so a future change to it cannot silently desync the JS mirror.

Output: new `tests/test_sales_total.py`; `tests/test_mobile_sales.py` and `tests/test_core.py`
extended. New-behavior tests land as strict xfail so the suite stays green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-sales-page-rebuild/22-VALIDATION.md
@.planning/phases/22-sales-page-rebuild/22-UI-SPEC.md
</context>

## Artifacts this phase produces

Phase-wide contract. This plan asserts against it; the owning plan builds it. See 22-01-PLAN.md
"Artifacts this phase produces" for the full table. This plan's direct targets:

| Artifact | Kind | Owner |
|----------|------|-------|
| `app/static/sale-total.js` — `recalcSaleTotal`, `moneyToCents`, `qtyToInt`, `formatCents`, `window.recalcSaleTotal` | JS file | 22-04 |
| `#sale-total` (no `name=`, `data-rows="mobile"` on the mobile twin), `#sale-total-amount`, `#sale-total-units`, `#sale-total-warning` | element ids | 22-04 / 22-07 |
| `<script src="/static/sale-total.js" defer>` on `base.html` AND `mobile_base.html` | script tags | 22-04 |
| `#m-customer-header` + mobile selector partials | template | 22-06 |
| `customer_id` form param on `POST /m/sales` | endpoint param | 22-07 |
| `hx-include="closest form"` on the shared batch card | attribute | 22-07 |

<tasks>

<task type="auto">
  <name>Task 1: tests/test_sales_total.py — SALE-02 markup and wiring</name>
  <files>tests/test_sales_total.py</files>
  <read_first>
    - `tests/test_sales.py` — the house web-test idiom this new file mirrors; specifically
      `test_web_sale_page_renders_form` (:570) for the `GET /sales/new` shape and
      `test_web_sale_422_re_echoes_picked_batch` (:1041) for the 422-re-render POST shape.
    - `tests/conftest.py` — `client`, `stocked_product`, `mobile_client_factory` fixtures. Add none.
    - `app/templates/partials/sale_form.html` — the insertion point (between `</table>` and the hint
      «Если товара с таким кодом ещё нет…») and the `#basket-rows` / `#sale-form` / `#sale-form-wrap`
      ids that must NOT be renamed.
    - `.planning/phases/22-sales-page-rebuild/22-UI-SPEC.md` Interaction 11, 13 — the exact markup
      and the three-trigger contract.
  </read_first>
  <action>
Create `tests/test_sales_total.py`. Every test is decorated
`@pytest.mark.xfail(strict=True, reason="SALE-02: live total lands in 22-04")` except where noted.

1. `test_sale_total_element_renders_under_basket` — `GET /sales/new` returns 200 and the body
   contains `id="sale-total"`, `id="sale-total-amount"`, `id="sale-total-units"`,
   `id="sale-total-warning"`. Assert the empty-basket defaults render as `0,00` and `0`, and that
   the copy «Предварительный итог:» and «шт.» are present (22-UI-SPEC.md Copywriting — NOT a bare
   «Итого»). Assert `#sale-total` appears BEFORE the hint «Если товара с таким кодом ещё нет» in the
   body (SALE-02: "directly under the table", above the hint) by comparing `body.index(...)` offsets.
2. `test_sale_total_has_no_name_attribute` — a SECURITY test (T-22-03), not a style check. Extract
   the `<p id="sale-total" ...>` element's own tag text from the `GET /sales/new` body and assert it
   contains no `name=` attribute, and that the `#sale-total` element's inner HTML contains no
   `<input`, `<select`, or `<textarea>`. The browser's advisory money math must be structurally
   incapable of reaching the server; `register_sale` stays the sole authority (22-UI-SPEC.md
   Interaction 11: "Verify at review that no plan ever adds a `name=` to this element").
3. `test_sale_total_script_loaded_on_both_shells` — `GET /sales/new` AND `GET /m/sales` (via
   `mobile_client_factory`) both return a body containing `/static/sale-total.js` with a `defer`
   attribute. `mobile_base.html` does not inherit from `base.html`, so this catches the classic
   "wired desktop, forgot mobile" gap (22-PATTERNS.md Shared Pattern 7).
4. `test_sale_total_survives_422_rerender` — POST `/sales` with a basket that 422s (`qty[]=0`) and
   assert the re-rendered `#sale-form-wrap` body still contains `id="sale-total"`. This is
   Pitfall 2a: the re-render fires no `input` event, so if the element were dropped or the listener
   not re-triggered the total would silently go stale.
5. `test_sale_row_delete_hook_present` — `GET /sales/row` (the row partial endpoint) returns a body
   whose «Удалить строку» button's `hx-on:click` contains `recalcSaleTotal`. This is Pitfall 2b: a
   plain DOM `.remove()` fires neither an `input` nor an htmx event, so the hook is the only trigger.
   Assert the call is guarded (`window.recalcSaleTotal` appears, i.e. not a bare `recalcSaleTotal()`)
   so the button still works if the script has not loaded.
6. `test_sale_total_warning_hidden_by_default` — `GET /sales/new`: the `#sale-total-warning` span
   carries `hidden` and the `muted` class, and contains the copy «итог неполный». Assert it does NOT
   carry the `error` class — an incomplete total is a typing-in-progress state, not a fault
   (22-UI-SPEC.md Color rule 1; D-09).

Name the file's module docstring after SALE-02/D-08/D-09 and state plainly that this file asserts
markup + wiring ONLY, never arithmetic (no JS runtime in this suite), pointing at
22-VALIDATION.md §Manual-Only Verifications. That comment prevents a future reader from "fixing"
the perceived coverage gap by adding an npm test runner, which CLAUDE.md forbids.
  </action>
  <verify>
    <automated>uv run pytest tests/test_sales_total.py -rxX</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_sales_total.py -rxX` reports exactly 6 XFAIL, 0 XPASS, 0 ERROR.
    - `uv run pytest -q` reports 0 failed.
    - `grep -c 'name=' tests/test_sales_total.py` confirms `test_sale_total_has_no_name_attribute`
      asserts on the element's own tag text, not on the whole page body (a whole-body `name=` search
      would always match the basket inputs and the test would be vacuous).
    - `uv run ruff check tests/test_sales_total.py && uv run ruff format --check tests/test_sales_total.py` passes.
  </acceptance_criteria>
  <done>Six strict-xfail tests pin the SALE-02 markup, the no-`name=` security control, both-shell script wiring, and the two stale-total trigger paths.</done>
</task>

<task type="auto">
  <name>Task 2: tests/test_mobile_sales.py — D-04 selector + D-11 batch-card basket preservation</name>
  <files>tests/test_mobile_sales.py</files>
  <read_first>
    - `tests/test_mobile_sales.py` — the suite being extended; read the existing wizard-step tests for
      the `mobile_client_factory` idiom and how the `*_acc[]` arrays are posted between steps.
    - `app/routes/mobile_sales.py` — `mobile_sale_step_batch` (:155, note it ALREADY declares
      `code_acc`/`qty_acc`/`price_acc`/`batch_acc` as `Query` params) and `mobile_sale_create` (:333,
      the `customer_id=""` hardcode at :346).
    - `app/templates/mobile_partials/sale_step_batch.html` — `#batch-wrap` (:5) CONTAINS the four
      `*_acc[]` hidden inputs (:7-10), which is exactly why the card tap's `outerHTML` swap drops them
      when they are not re-sent.
    - `app/templates/mobile_partials/batch_card_picker.html` — the card `hx-get` at :46-54 with
      `hx-vals` and no `hx-include`.
    - `.planning/phases/22-sales-page-rebuild/22-UI-SPEC.md` Interaction 9, 10.
  </read_first>
  <action>
Add five strict-xfail tests to `tests/test_mobile_sales.py`.

D-04 (`reason="D-04: mobile customer selector lands in 22-06/22-07"`):
1. `test_mobile_customer_selector_renders_on_basket` — drive the wizard to the Корзина screen and
   assert the response contains `id="m-customer-header"`, three radios named `customer_mode` with
   values `new`/`existing`/`anon`, and `existing` checked (D-02 parity). Assert the selector markup
   appears BEFORE the first basket card in the body (22-UI-SPEC.md Interaction 9: selector renders
   above the basket cards, mirroring desktop).
2. `test_mobile_links_customer` — POST `/m/sales` with a full valid `*_acc[]` basket AND
   `customer_id=<customer.id>`; assert 200 and that the written `Sale.customer_id == customer.id`.
   Today `mobile_sales.py:346` hardcodes `customer_id=""`, so this is the test that retires the
   hardcode.
3. `test_mobile_walkin` — POST `/m/sales` with the same basket and NO `customer_id`; assert the sale
   is still written with `Sale.customer_id IS NULL` (D-05 — the mobile walk-in path must not regress).
4. `test_mobile_selector_swap_acc_survives` — `GET /m/sales/customer-mode?customer_mode=new` with the
   `*_acc[]` arrays supplied as query params; assert the response's swap unit is scoped to
   `#m-customer-header` and does NOT contain `id="wizard-step"`. Targeting `#wizard-step` from the
   mode radio would wipe the basket's `code_acc[]` hidden inputs — 22-UI-SPEC.md Interaction 9 calls
   this out explicitly. Assert on the RESPONSE ROOT, since that is what the `outerHTML` swap replaces.

D-11 (`reason="D-11: batch-card hx-include lands in 22-07"`):
5. `test_batch_card_preserves_basket` — reproduce the empirically-verified defect
   (22-RESEARCH.md Pitfall 3). Two assertions, both required:
   (a) MARKUP: render the sale wizard's batch step and assert the card button carries
       `hx-include="closest form"`. This is the actual fix, and it is the only part a server-side
       suite can prove — htmx's GET-excludes-the-form behavior happens in the browser, which this
       suite has no runtime for.
   (b) ENDPOINT: `GET /m/sales/step/batch` WITH the `code_acc[]`/`qty_acc[]`/`price_acc[]`/`batch_acc[]`
       params present (simulating what the browser will send once `hx-include` is added) returns a
       body that re-renders the `code_acc[]` hidden inputs. Note assertion (b) may already pass today
       because the endpoint declares those `Query` params — that is expected and fine; it pins the
       endpoint's half of the contract so a later refactor cannot drop it. Put (b) in its own
       NON-xfail test named `test_batch_step_echoes_acc_when_supplied` so the strict-xfail marker on
       (a) stays honest.

Use the seeded `customer` fixture; add no new fixtures.
  </action>
  <verify>
    <automated>uv run pytest tests/test_mobile_sales.py -k "customer_selector or mobile_links_customer or mobile_walkin or acc_survives or batch_card_preserves_basket or batch_step_echoes_acc" -rxX</automated>
  </verify>
  <acceptance_criteria>
    - The verify command reports exactly 5 XFAIL, 1 PASSED (`test_batch_step_echoes_acc_when_supplied`),
      0 XPASS, 0 ERROR.
    - `uv run pytest tests/test_mobile_sales.py -q` reports 0 failed.
    - `test_mobile_links_customer` and `test_mobile_walkin` assert against the DB (`select(Sale)`),
      not against response text — verify by `grep -n "select(Sale)" tests/test_mobile_sales.py`.
    - `test_mobile_selector_swap_acc_survives` asserts `id="wizard-step"` is ABSENT from the response.
    - `uv run pytest -q` reports 0 failed and ≥808 passed.
  </acceptance_criteria>
  <done>Five strict-xfail tests pin mobile customer parity and the D-11 card fix; one passing test pins the batch-step endpoint's echo contract.</done>
</task>

<task type="auto">
  <name>Task 3: tests/test_core.py — pin the to_cents accept-set the JS mirrors</name>
  <files>tests/test_core.py</files>
  <read_first>
    - `tests/test_core.py` — the existing `to_cents` coverage being extended.
    - `app/core.py:28-53` — `to_cents` (ROUND_HALF_UP, rejects inf/nan) and `format_cents` (comma
      separator). These are the two functions `sale-total.js` mirrors.
    - `.planning/phases/22-sales-page-rebuild/22-RESEARCH.md` Pattern 2 — the 20-case accept-set
      table, produced by EXECUTING `to_cents` on 2026-07-17. Use it as the expected values; do not
      re-derive them by reasoning.
    - `.planning/phases/22-sales-page-rebuild/22-UI-SPEC.md` Interaction 15 — the deliberate,
      bounded divergence between the server and JS accept-sets.
  </read_first>
  <action>
Extend `tests/test_core.py` with characterization tests for `to_cents`. These are NOT xfail — they
assert already-shipped behavior and must pass immediately. Their job is to freeze the server's
accept-set so a future change to `to_cents` breaks a test instead of silently desyncing
`sale-total.js`, which has no runtime coverage of its own.

Add `test_to_cents_accept_set_boundaries` as a `pytest.mark.parametrize` over the verified table:
- Accepted by BOTH server and the JS mirror: `"7"` → 700, `"12,50"` → 1250, `"12.50"` → 1250,
  `".5"` → 50, `"5."` → 500, `" 12,5 "` → 1250.
- The ROUND_HALF_UP tie rule the JS must mirror: `"12.505"` → 1251 (ties away from zero, NOT
  banker's rounding — `app/core.py` WR-03).
- Rejected by BOTH (assert `pytest.raises(ValueError)`): `"1 000"` (space thousands), `"12abc"`,
  `""`, `"inf"`, `"nan"`, `"Infinity"`, `"12,5,0"`.

Add a second test `test_to_cents_accepts_more_than_the_js_mirror` documenting the deliberate
divergence (22-UI-SPEC.md Interaction 15): assert the server ACCEPTS `"1e3"` → 100000,
`"1_000"` → 100000, `"+12.5"` → 1250, and the fullwidth `"１２"` → 1200 — inputs the JS regex
deliberately rejects into D-09's «итог неполный» bucket. Give this test a docstring stating that a
false «итог неполный» on these is accepted (advisory display; no operator types an exponent into a
price field) whereas a WRONG NUMBER would not be, and that byte-exact JS parity is not achievable in
a regex and must not be attempted.

Add `test_format_cents_mirror_contract` asserting `format_cents(1250) == "12,50"`,
`format_cents(0) == "0,00"`, `format_cents(5) == "0,05"`, `format_cents(-1250) == "-12,50"` — the
exact output shape the JS `formatCents` must reproduce.

If any expected value here disagrees with the running code, STOP and report it rather than editing
`app/core.py` — `to_cents` is out of this phase's scope and a disagreement means the research table
is stale.
  </action>
  <verify>
    <automated>uv run pytest tests/test_core.py -k "to_cents or format_cents" -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_core.py -k "to_cents or format_cents" -x` PASSES (0 failed, 0 xfail) —
      these characterize shipped behavior.
    - `app/core.py` is unmodified: `git diff --stat app/core.py` is empty.
    - `uv run pytest -q` reports 0 failed and ≥808 passed.
    - `uv run ruff check . && uv run ruff format --check .` passes.
  </acceptance_criteria>
  <done>The server's money accept-set and render shape are pinned as executable tests, giving the untestable JS parser a stable contract to mirror.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `POST /m/sales` | untrusted `customer_id` + `*_acc[]` basket arrays cross here |
| browser → `GET /m/sales/step/batch` | untrusted `batch_id` / `code` cross here |
| client-computed total → server | MUST NOT exist — the structural control under test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | Tampering | `#sale-total` client money math | mitigate | `test_sale_total_has_no_name_attribute` asserts the element carries no `name=` and contains no form control, so the browser's arithmetic is structurally incapable of reaching the server. `register_sale` (`services/sales.py:282`) recomputes from `price[]`. |
| T-22-04 | Spoofing | `customer_id` on `POST /m/sales` | mitigate | `test_mobile_links_customer` / `test_mobile_walkin` assert the server-side `customer_id or None` coercion decides attribution; the browser never asserts NULL-ness itself. |
| T-22-05 | Tampering | untrusted `batch_id` on the card tap | accept | Unchanged by this phase — `mobile_sale_step_batch` already re-validates product ownership server-side on every tap (T-09-08/T-11-10 precedent). D-11 adds an `hx-include`; it does not widen this surface. |
| T-22-SC | Tampering | package installs | accept | No package-manager install occurs; `pyproject.toml` untouched. Any proposal to add jsdom/Playwright/npm to close the SALE-02 arithmetic gap is a CLAUDE.md scope deviation and must be escalated, not installed. |
</threat_model>

<verification>
- `uv run pytest tests/test_sales_total.py tests/test_mobile_sales.py tests/test_core.py -q` → 0 failed.
- `uv run pytest -q` → 0 failed, ≥808 passed.
- `uv run ruff check . && uv run ruff format --check .` → clean.
</verification>

<success_criteria>
- 11 strict-xfail tests pin the SALE-02 wiring, D-04 mobile parity, and the D-11 card fix.
- The `to_cents` accept-set and `format_cents` output shape are frozen as passing tests.
- The repository suite is green — no red wave.
</success_criteria>

<output>
Create `.planning/phases/22-sales-page-rebuild/22-02-SUMMARY.md` when done.
</output>

---
phase: 13-mobile-wizard-context-navigation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - app/templates/mobile_partials/sale_basket.html
  - app/routes/mobile_sales.py
  - tests/test_mobile_sales.py
  - app/templates/mobile_partials/search_product_detail.html
  - tests/test_mobile_search.py
autonomous: true
requirements: [UI-04, UI-05]

must_haves:
  truths:
    - "The mobile sale wizard's Корзина (basket) screen shows a step-indicator line consistent with the rest of the wizard, reading Корзина instead of a numbered step (D-07)"
    - "From the mobile search product-detail screen, the operator can tap Продать or Принять to land directly on the sale or receipt wizard's step 1 with the product code already filled in (D-08/D-09)"
    - "The Продать quick-action link always renders, even for a product with zero stock across every warehouse (D-09)"
  artifacts:
    - path: "app/templates/mobile_partials/sale_basket.html"
      provides: "Корзина step-indicator line"
      contains: "mobile-step-indicator"
    - path: "app/templates/mobile_partials/search_product_detail.html"
      provides: "Продать/Принять quick-action links to /m/sales?code= and /m/receipts?code="
      contains: "mobile-actions"
  key_links:
    - from: "app/templates/mobile_partials/search_product_detail.html"
      to: "/m/sales?code="
      via: "plain <a> link"
      pattern: "href=\"/m/sales\\?code="
    - from: "app/templates/mobile_partials/search_product_detail.html"
      to: "/m/receipts?code="
      via: "plain <a> link"
      pattern: "href=\"/m/receipts\\?code="
---

<objective>
Two small, independent template-level fixes that close out Phase 13's remaining requirements:
(1) add a "Корзина" step-indicator line to the mobile sale wizard's basket/review screen (D-07,
UI-04), and (2) add "Продать"/"Принять" quick-action links to the mobile search product-detail
screen that jump straight into the sale/receipt wizard's step 1 with the code pre-filled (D-08/
D-09, UI-05). This plan also adds the `?code=` GET query-param pre-fill to `GET /m/sales` (the
sale-side half of D-08 — the receipts-side half is added by Plan 13-03, since that touches
`mobile_receipts.py` which this plan does not).

Purpose: Phase 13 Success Criteria #3 (basket step indicator) and #4 (search quick actions).
Output: `sale_basket.html` with a step-indicator line; `mobile_sales.py`'s `mobile_sales_page`
accepting an optional `code` query param; `search_product_detail.html` with two new quick-action
links.

**Cross-plan note:** The "Принять" link's target (`GET /m/receipts?code=`) is wired up by Plan
13-03 (same wave, no file overlap with this plan). This plan's own tests verify the link's href
markup and the `/m/sales?code=` round trip in full; the `/m/receipts?code=` round trip is verified
by Plan 13-03's own tests. Both land in the same execution wave.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-mobile-wizard-context-navigation/13-CONTEXT.md
@.planning/phases/13-mobile-wizard-context-navigation/13-UI-SPEC.md
@app/templates/mobile_partials/sale_basket.html
@app/routes/mobile_sales.py
@app/templates/mobile_partials/search_product_detail.html
@app/routes/mobile_search.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sale basket step indicator + /m/sales code query param</name>
  <files>app/templates/mobile_partials/sale_basket.html, app/routes/mobile_sales.py, tests/test_mobile_sales.py</files>
  <read_first>
    app/templates/mobile_partials/sale_basket.html (current content, lines 5-6: `<div
    id="wizard-basket"><h2>Корзина</h2>` — the insertion point)
    app/templates/mobile_partials/transfers_step_dest.html (line 9: `<p class="mobile-step-indicator">{{
    step_label }}</p>` — confirms the CSS class/position convention, though sale_basket.html uses a
    literal string not a variable)
    app/routes/mobile_sales.py (full file — `mobile_sales_page`'s current signature/context)
  </read_first>
  <action>
    In `sale_basket.html`, insert `<p class="mobile-step-indicator">Корзина</p>` immediately after
    the opening `<div id="wizard-basket">` tag and before `<h2>Корзина</h2>` (per 13-UI-SPEC.md
    Contract C — same CSS class as every numbered step, text reads "Корзина" instead of "Шаг N из
    3"; do not attempt to compute a numeric step for this screen).

    In `mobile_sales.py`, change `mobile_sales_page`'s signature from `(request: Request)` to
    `(request: Request, code: str = "")`. Change the context dict's `"code": ""` to `"code": code`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_mobile_sales.py -x</automated>
  </verify>
  <acceptance_criteria>
    A basket response (`POST /m/sales/step/basket-add`) contains
    `&lt;p class="mobile-step-indicator"&gt;Корзина&lt;/p&gt;` (adjust for literal HTML). GET
    /m/sales?code=TEST-001 renders a page containing `value="TEST-001"` in the code input.
  </acceptance_criteria>
  <done>sale_basket.html shows a Корзина step-indicator line; GET /m/sales accepts and echoes an
  optional code query param; existing tests/test_mobile_sales.py suite still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Search product-detail quick actions (Продать/Принять)</name>
  <files>app/templates/mobile_partials/search_product_detail.html, tests/test_mobile_search.py</files>
  <read_first>
    app/templates/mobile_partials/search_product_detail.html (current content — the file's own
    header comment confirms plain top-level link navigation, not htmx, is the established
    convention for this screen)
    .planning/phases/13-mobile-wizard-context-navigation/13-UI-SPEC.md (Contract D — the exact
    markup and copy to use, verbatim)
  </read_first>
  <action>
    In `search_product_detail.html`, add (after the existing stock-rows loop, at the end of the
    template body) exactly the markup from 13-UI-SPEC.md Contract D:
    `<div class="mobile-actions"><a class="button" href="/m/sales?code={{ product.code
    }}">Продать</a><a class="button" href="/m/receipts?code={{ product.code
    }}">Принять</a></div>`. Both links use plain `class="button"` (primary styling, NOT
    `.secondary`) per the approved UI-SPEC — do not conditionally wrap either link on
    `stock_rows` being empty; both must always render regardless of stock level (D-09 — the app
    already allows selling into negative stock with an oversell warning, so a zero-stock product
    must stay reachable via this quick action).

    Add new pytest tests to `tests/test_mobile_search.py` asserting: (1) the product-detail
    response for a stocked product contains `href="/m/sales?code={{ product.code }}"` and
    `href="/m/receipts?code={{ product.code }}"` (substitute the actual code); (2) the response for
    a product with ZERO stock (no batches at all — use the plain `product` fixture, not
    `stocked_product`) still contains both links unconditionally.
  </action>
  <verify>
    <automated>uv run pytest tests/test_mobile_search.py -x</automated>
  </verify>
  <acceptance_criteria>
    `uv run pytest tests/test_mobile_search.py -v` shows both new tests passing — the zero-stock
    test in particular must NOT be skipped or conditioned on stock existing.
  </acceptance_criteria>
  <done>search_product_detail.html always renders both quick-action links regardless of stock
  level; full pytest file green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Operator's browser → GET /m/sales | Untrusted query param: `code` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-09 | Tampering | `?code=` query-param pre-fill on GET /m/sales | accept | `code` only pre-fills a text input; it is re-validated identically to manual entry by the existing `lookup_prefill` call in `mobile_sale_step_product` downstream — no new trust boundary (13-RESEARCH.md Security Domain, "Known Threat Patterns"). |
| T-13-10 | Information Disclosure | `search_product_detail.html`'s quick-action links echo `product.code` into an `href` | accept | `product.code` is already rendered unescaped-safe via Jinja2 autoescape elsewhere on this same page (`<h1>{{ product.name }} ({{ product.code }})</h1>`); no new rendering surface, same trust level as existing content. |
</threat_model>

<verification>
Run `uv run pytest tests/test_mobile_sales.py -x` and `uv run pytest tests/test_mobile_search.py -x`
after each task's relevant file changes.
</verification>

<success_criteria>
- sale_basket.html contains `<p class="mobile-step-indicator">Корзина</p>`.
- search_product_detail.html always renders both "Продать" and "Принять" links, regardless of
  stock.
- GET /m/sales accepts and echoes an optional `?code=` query param.
- `uv run pytest tests/test_mobile_sales.py tests/test_mobile_search.py` is green with zero
  regressions.
</success_criteria>

<output>
Create `.planning/phases/13-mobile-wizard-context-navigation/13-05-SUMMARY.md` when done.
</output>

---
phase: 12-code-name-autofill
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - app/routes/mobile_receipts.py
  - app/templates/mobile_partials/receipts_step_batch.html
  - app/templates/mobile_partials/receipts_step_details.html
  - tests/test_mobile_receipts.py
autonomous: true
requirements: [PRICE-04]

must_haves:
  truths:
    - "On mobile goods receipt, when a code unknown to Product resolves via Dictionary/CatalogPrice, the suggested cost/catalog prices pre-fill on step 3 without extra operator action"
    - "Mobile receipt step 3 always shows the product code, and the name when known, as visible text"
    - "CatalogPrice data never reaches the receipt's sale price field on mobile, matching desktop's D-02 boundary"
  artifacts:
    - path: app/routes/mobile_receipts.py
      provides: "mobile_receipt_step_batch resolving and forwarding cost/sale/catalog via lookup_prefill"
    - path: app/templates/mobile_partials/receipts_step_batch.html
      provides: "hidden cost/sale/catalog carry-forward fields"
    - path: app/templates/mobile_partials/receipts_step_details.html
      provides: "visible code/name readout line"
  key_links:
    - from: app/routes/mobile_receipts.py
      to: "app/services/receipts.py::lookup_prefill"
      via: "function call"
      pattern: "lookup_prefill\\(session, code_clean\\)"
    - from: app/templates/mobile_partials/receipts_step_batch.html
      to: app/templates/mobile_partials/receipts_step_details.html
      via: "hidden field carry-forward through hx-include=\"closest form\""
      pattern: "name=\"(cost|sale|catalog)\""
---

<objective>
Extend the mobile goods-receipt wizard's existing "resolve once per step-submit" pattern so the price data `mobile_receipt_step_batch` already fetches (and today discards) reaches step 3's price inputs as pre-filled values (D-06), and add the minimal visible code/name readout D-12 requires on step 3 to keep an autofilled number anchored to a visible product.

Purpose: PRICE-04's mobile half — a code unknown to the product catalog must still surface catalog/consultant price and name suggestions on the mobile receipt flow, without adding a new debounced live-lookup interaction surface (explicitly rejected by D-06/D-07, which belongs to Phase 13's navigation rework).
Output: mobile receipt step 2 -> step 3 transition carries cost/sale/catalog forward; step 3 shows a visible `код — название` line.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-code-name-autofill/12-CONTEXT.md
@.planning/phases/12-code-name-autofill/12-RESEARCH.md
@.planning/phases/12-code-name-autofill/12-PATTERNS.md
@.planning/phases/12-code-name-autofill/12-UI-SPEC.md
@app/routes/mobile_receipts.py
@app/services/receipts.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread resolved cost/sale/catalog from step 2 into step 3 (D-06)</name>
  <files>app/routes/mobile_receipts.py, app/templates/mobile_partials/receipts_step_batch.html, tests/test_mobile_receipts.py</files>
  <read_first>
    app/routes/mobile_receipts.py (full file — `mobile_receipt_step_batch`, `_lookup_name`)
    app/core.py (`format_cents`/`to_cents` round trip — "12,50" string shape)
    app/services/receipts.py (`lookup_prefill()` contract — now includes the source=="catalog" branch from Plan 12-01; this plan depends on 12-01)
    app/templates/mobile_partials/receipts_step_batch.html (full file)
    app/templates/mobile_partials/receipts_step_details.html (full file — `cost`/`sale`/`catalog` are read via `Form("")` and echoed as `value="{{ cost }}"` etc., already string-shaped, requires NO handler change)
    .planning/phases/12-code-name-autofill/12-CONTEXT.md (D-06, D-07)
    .planning/phases/12-code-name-autofill/12-RESEARCH.md (Common Pitfall #4)
    tests/test_mobile_receipts.py lines 1-80 (existing step_batch test block, fixtures, "carries_name" test)
  </read_first>
  <behavior>
    - code resolves to an existing Product with cost_cents=1250, sale_cents/catalog_cents unset -> step 2's response includes a hidden `cost` input with value "12,50" and hidden `sale`/`catalog` inputs with value ""
    - code unknown to Product but CatalogPrice has consultant_cents=900, consumer_cents=1500 -> response's hidden `cost` input value is "9,00" and hidden `catalog` input value is "15,00"; the hidden `sale` input is NEVER non-empty for this source (D-02 regression at the mobile layer)
    - code resolves to nothing anywhere -> all three hidden price inputs render value=""
  </behavior>
  <action>
    In app/routes/mobile_receipts.py add `from app.core import format_cents` to the imports. Inside `mobile_receipt_step_batch`, replace `resolved_name = _lookup_name(session, code)` with a single direct call: compute `code_clean = code.strip()`, then `result = lookup_prefill(session, code_clean) if code_clean else None`, then `resolved_name = (result["name"] or "") if result else ""` (None-guard — `result["name"]` can be `None` for a catalog-source match with a CatalogPrice but no Dictionary entry). Delete the now-unused `_lookup_name` helper function entirely (its logic is inlined above). Build `prices = result["prices"] if result and result["prices"] else {}` and format each of "cost", "sale", "catalog" as `format_cents(prices[field])` when `prices.get(field) is not None`, else `""`; add these three formatted strings to the context dict already being built as `"cost"`, `"sale"`, `"catalog"` alongside the existing `code`/`warehouse_id`/`name`/`name_known` keys. This forwards prices for BOTH `source=="product"` and `source=="catalog"` matches — a superset of the literal D-06 wording that keeps desktop and mobile behavior symmetric, since desktop's `/receipts/lookup` already fills product-card prices too. In app/templates/mobile_partials/receipts_step_batch.html add three hidden inputs immediately after the existing `<input type="hidden" name="warehouse_id" value="{{ warehouse_id }}">` line (still outside the `{% if zero_warehouses %}` block, mirroring the existing code/warehouse_id placement): `<input type="hidden" name="cost" value="{{ cost }}">`, `<input type="hidden" name="sale" value="{{ sale }}">`, `<input type="hidden" name="catalog" value="{{ catalog }}">`. Do not modify `mobile_receipt_step_details` (the step-3 POST handler) — it already declares `cost`/`sale`/`catalog` as `Form("")` and echoes them into context unchanged, so the new hidden fields flow through automatically via step 2's existing `hx-include="closest form"` submit. In tests/test_mobile_receipts.py, extend the models/services imports as needed (`CatalogPrice` from `app.models`, `add_entry` from `app.services.dictionary`) and add 3 new tests in the step-batch test block covering the three behaviors above, asserting on the exact `value="..."` substrings for the cost/sale/catalog hidden inputs.
  </action>
  <acceptance_criteria>
    <source>app/routes/mobile_receipts.py no longer defines `_lookup_name`</source>
    <source>app/routes/mobile_receipts.py imports `format_cents` from `app.core`</source>
    <test>uv run pytest tests/test_mobile_receipts.py -k step_batch -x passes including the 3 new price-forwarding cases</test>
    <behavior>for every catalog-source test case, the step-2 response never contains a non-empty value on the `sale` hidden input</behavior>
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_mobile_receipts.py -k step_batch -x</automated>
  </verify>
  <done>mobile receipt step 2 resolves cost/sale/catalog once (product or catalog source) via a single lookup_prefill call and forwards them as hidden fields that pre-fill step 3's price inputs, with CatalogPrice never reaching the sale field.</done>
</task>

<task type="auto">
  <name>Task 2: Visible code/name readout on step 3 (D-12)</name>
  <files>app/templates/mobile_partials/receipts_step_details.html, tests/test_mobile_receipts.py</files>
  <read_first>
    app/templates/mobile_partials/receipts_step_details.html (full file — hidden inputs at lines 7-10, qty field starts line 11)
    .planning/phases/12-code-name-autofill/12-UI-SPEC.md (Copywriting Contract row "Mobile receipt step 3 — visible code/name readout")
    .planning/phases/12-code-name-autofill/12-CONTEXT.md (D-12, and the D-07 boundary it narrowly exempts)
    tests/test_mobile_receipts.py (existing step 3 / step_details test block)
  </read_first>
  <action>
    In app/templates/mobile_partials/receipts_step_details.html insert one new markup line: a paragraph with the code wrapped in a strong tag, followed by an optional em-dash and the name — exact content: an opening p tag, then an opening strong tag, then the code expression, then a closing strong tag, then (only when name is truthy) a space-emdash-space and the name expression, then a closing p tag. This line goes immediately after the last hidden input (the batch_choice hidden field) and before the qty field's opening div — this is the ONLY change to this file (no new CSS class, no step-indicator/back-button change, per D-07's boundary — D-12 is a deliberately narrow exception to it). The existing hidden code/name inputs on lines 7 and 9 are untouched (still required for form submission). In tests/test_mobile_receipts.py add a test asserting the step-3 response (reached via POST /m/receipts/step/details) contains a strong-tag-wrapped code value plus the name text when a name is known, and a second test for the unknown-code/no-name case asserting the code alone renders with no trailing " — " when name is empty.
  </action>
  <acceptance_criteria>
    <source>app/templates/mobile_partials/receipts_step_details.html contains a paragraph line opening with a strong tag immediately wrapping the code template expression (an opening p tag directly followed by an opening strong tag, the code variable, and a closing strong tag)</source>
    <test>uv run pytest tests/test_mobile_receipts.py -k step_details -x passes including both new readout assertions</test>
    <behavior>when name is empty, the rendered readout paragraph contains no trailing "—" character</behavior>
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_mobile_receipts.py -k step_details -x</automated>
  </verify>
  <done>mobile receipt step 3 always shows a visible, bolded product code (plus name when known) above the price fields, satisfying D-12's narrow scope with no other wizard changes.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (Plan 12-02)

- `mobile_receipt_step_batch` (app/routes/mobile_receipts.py) rewritten to call `lookup_prefill` directly and forward `cost`/`sale`/`catalog` in its render context
- `_lookup_name` helper (app/routes/mobile_receipts.py) removed — inlined into `mobile_receipt_step_batch`
- 3 new hidden inputs (`cost`, `sale`, `catalog`) in `app/templates/mobile_partials/receipts_step_batch.html`
- New visible readout paragraph (bolded code, optional em-dash + name) in `app/templates/mobile_partials/receipts_step_details.html`
- 5 new test functions in `tests/test_mobile_receipts.py` (3 for price forwarding, 2 for the visible readout) — exact names left to the executor
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Operator device -> `/m/receipts/step/batch` -> `/m/receipts/step/details` | Untrusted `code`/`warehouse_id`/`name` form fields (existing boundary, unchanged shape); price fields are now server-derived (not operator-posted) at the step-2 hop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-12-04 | Information Disclosure | `receipts_step_details.html` rendering `code`/`name` | accept (mitigated) | Jinja2 autoescape default, no `\|safe` used |
| T-12-05 | Tampering (of user intent) | Server-computed prices arriving as literal "None" text | mitigate | `format_cents()` applied server-side before the value reaches the template — never a raw `int \| None` interpolated directly into a value attribute (RESEARCH Pitfall 4) |
</threat_model>

<verification>
Run `uv run pytest tests/test_mobile_receipts.py -x` — all green, including the 5 new tests.
</verification>

<success_criteria>
- Mobile receipt step 3 arrives pre-filled with cost/catalog (never sale) whenever step 2 resolved a product or catalog-sourced match.
- Mobile receipt step 3 always shows a visible product code (plus name when known) above the price fields.
</success_criteria>

<output>
Create `.planning/phases/12-code-name-autofill/12-02-SUMMARY.md` when done
</output>

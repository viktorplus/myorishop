---
phase: 09-batch-tracking-ledger-integration
plan: 05
type: execute
wave: 4
depends_on: [09-02, 09-03, 09-04]
files_modified:
  - app/services/returns.py
  - app/routes/returns.py
  - app/templates/partials/return_form.html
  - app/services/operations.py
  - app/routes/history.py
  - app/templates/partials/history_rows.html
  - app/services/ledger.py
  - tests/test_returns.py
  - tests/test_history.py
autonomous: true
requirements: [LOT-05]
must_haves:
  truths:
    - "A return does NOT re-ask for a batch: it restores stock to the origin sale op's batch_id, and the return form shows that batch read-only (D-08)"
    - "When the origin sale op has a NULL batch_id (pre-Phase-9 sale), the return targets the product's legacy batch, lazily creating it with the frozen D-14 field values (is_legacy=1, quantity 0) inside register_return's transaction when the product had no legacy batch seeded (D-08, RESEARCH Open Q1)"
    - "The record_operation D-12 guard is now MANDATORY: stock-affecting types (receipt, sale, writeoff, return, correction) raise ValueError without a batch_id, and audit types (price_change, product_created, product_edited) raise if given one (D-12)"
    - "/history renders a NULL batch_id on a stock-affecting op as the muted legacy label «До внедрения партий» and a batched op as «Партия: {expiry|«без срока»}{ — comment}», as a muted second line in the existing «Товар» cell; audit rows show no second line; the ledger is never rewritten (D-15)"
  artifacts:
    - path: "app/services/returns.py"
      provides: "register_return batch inheritance + legacy lazy-create"
      contains: "batch_id"
    - path: "app/services/ledger.py"
      provides: "D-12 mandatory batch_id guard for stock-affecting types"
      contains: "STOCK_AFFECTING_TYPES"
    - path: "app/services/operations.py"
      provides: "history_view LEFT OUTER JOIN Batch"
      contains: "outerjoin"
  key_links:
    - from: "app/services/returns.py register_return"
      to: "origin.batch_id / legacy_batch"
      via: "record_operation(type_='return', batch_id=origin.batch_id or legacy_batch(...).id)"
      pattern: "origin\\.batch_id"
    - from: "app/services/operations.py history_view"
      to: "Batch"
      via: "select(Operation, Product, Batch).outerjoin(Batch, Operation.batch_id == Batch.id)"
      pattern: "outerjoin\\(Batch"
---

<objective>
Close the phase: make returns inherit the origin sale line's batch (D-08, with a legacy lazy-create for pre-Phase-9 sales), render batch attribution read-time on /history (D-15), and flip the `record_operation` D-12 guard to MANDATORY now that every stock-affecting service already supplies a `batch_id`.

Purpose: Complete LOT-05 for the return path, deliver criterion 5's display side (legacy operations show as belonging to a default legacy batch without any ledger rewrite), and enforce the batch-required invariant at the single write path — the enforcement backstop for the whole phase. This is deliberately the last plan so the guard flip lands after Plans 02/03/04 already thread `batch_id` through receipts, sales, write-offs, and corrections, keeping the suite green at every prior wave boundary (RESEARCH Pitfall 1).
Output: A batch-inheriting return, a legacy-labelled /history, and a mandatory batch guard; full suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-batch-tracking-ledger-integration/09-CONTEXT.md
@.planning/phases/09-batch-tracking-ledger-integration/09-RESEARCH.md
@.planning/phases/09-batch-tracking-ledger-integration/09-PATTERNS.md
@.planning/phases/09-batch-tracking-ledger-integration/09-UI-SPEC.md
@.planning/phases/09-batch-tracking-ledger-integration/09-02-SUMMARY.md
@.planning/phases/09-batch-tracking-ledger-integration/09-03-SUMMARY.md
@.planning/phases/09-batch-tracking-ledger-integration/09-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Return batch inheritance + legacy lazy-create + read-only origin line</name>
  <files>app/services/returns.py, app/routes/returns.py, app/templates/partials/return_form.html, tests/test_returns.py</files>
  <read_first>
    E:\dev\myorishop\app\services\returns.py (whole file — the origin-op validation gate 72-74, the frozen-copy record_operation call 91-102)
    E:\dev\myorishop\app\services\batches.py (from Plan 01 — `legacy_batch`)
    E:\dev\myorishop\.planning\phases\09-batch-tracking-ledger-integration\09-RESEARCH.md ("Pattern 6: Return batch inheritance", Open Question 1 — lazy-create sign-off APPROVED as the deliberate third batch birth path; Assumptions A3)
    E:\dev\myorishop\.planning\phases\09-batch-tracking-ledger-integration\09-UI-SPEC.md ("Return form (D-08)" copy)
    E:\dev\myorishop\.planning\phases\09-batch-tracking-ledger-integration\09-PATTERNS.md ("app/services/returns.py — batch inheritance")
  </read_first>
  <behavior>
    - A return of a batched sale op increments the origin op's batch quantity; the return form shows the read-only origin batch line and has NO picker.
    - A return of a pre-Phase-9 (NULL batch_id) sale for a product WITH a seeded legacy batch targets that legacy batch.
    - A return of a NULL-batch sale for a product with NO legacy batch lazily creates one (`is_legacy == 1`, comment «Остаток до внедрения партий») and completes without error.
  </behavior>
  <action>
    In `register_return`, after the origin-op validation gate, resolve the return's target batch: if `origin.batch_id is not None` use it directly; else resolve `batch = legacy_batch(session, origin.product_id)` and, when that is None (the origin's product had ledger stock ≤ 0 at migration so no legacy batch was seeded — RESEARCH Open Q1), LAZILY create the legacy batch inside this same transaction using the frozen D-14 contract (`warehouse_id = DEFAULT_WAREHOUSE_ID`, expiry/price_cents/location NULL, `comment = "Остаток до внедрения партий"`, `is_legacy = 1`, `quantity = 0`) via a module-level frozen constant (do NOT import the migration). Add `batch_id=<resolved batch id>` to the existing `record_operation(type_="return", ...)` call — the single write path then increments that batch's quantity. This is the deliberate third batch birth path (migration + receipts + legacy-return fallback); note it in the module docstring.
    Route/template: the return form displays the target batch READ-ONLY — no picker, no batch input (D-08). return_form.html adds a muted line under the existing origin summary: batched origin «Возврат в партию: {ru_date(expiry)|«без срока»}{ — comment}»; legacy/NULL origin «Возврат в партию: Остаток до внедрения партий». The route passes the resolved (or to-be-resolved) origin batch info into the form context.
    Update tests/test_returns.py: a return of a batched sale restores stock to the origin's batch (batch quantity increases); a return of a pre-Phase-9 (NULL batch_id) sale for a product WITH a seeded legacy batch targets that legacy batch; a return of a pre-Phase-9 sale for a product with NO legacy batch lazily creates one (is_legacy=1) and the return succeeds.
  </action>
  <acceptance_criteria>
    - A return of a batched sale op increments the origin op's batch quantity, and the form shows the read-only origin batch line (no picker) (D-08).
    - A return of a NULL-batch sale for a product with no legacy batch creates a legacy batch (`is_legacy == 1`, comment «Остаток до внедрения партий») and completes without error (Open Q1).
    - `uv run pytest tests/test_returns.py -x -q` passes.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_returns.py -x -q</automated>
  </verify>
  <done>register_return inherits the origin op's batch (or the product's legacy batch, lazily creating it when absent) and passes batch_id to the return op; the return form shows the target batch read-only with no picker.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /history batch attribution (read-time, D-15)</name>
  <files>app/services/operations.py, app/routes/history.py, app/templates/partials/history_rows.html, tests/test_history.py</files>
  <read_first>
    E:\dev\myorishop\app\services\operations.py (whole file — `history_view` select/join 29-48)
    E:\dev\myorishop\app\routes\history.py (the HX-Request branching that renders history_rows — stays untouched except context passthrough)
    E:\dev\myorishop\app\templates\partials\history_rows.html (the 8-column table, the «Товар» cell — the muted second line is added here, no 9th column)
    E:\dev\myorishop\app\services\sales.py (lines 254-263 `recent_sales` — the outer-join shape to mirror)
    E:\dev\myorishop\.planning\phases\09-batch-tracking-ledger-integration\09-RESEARCH.md (Open Question 3 — display shape recommendation; D-15)
    E:\dev\myorishop\.planning\phases\09-batch-tracking-ledger-integration\09-UI-SPEC.md ("Return form (D-08) and /history (D-15)" copy)
    E:\dev\myorishop\.planning\phases\09-batch-tracking-ledger-integration\09-PATTERNS.md ("app/routes/history.py + operations.py + history_rows.html")
  </read_first>
  <behavior>
    - A stock op with NULL batch_id renders «До внедрения партий»; the underlying operation row is not mutated (no UPDATE issued — D-15).
    - A batched op renders «Партия: {expiry|«без срока»}{ — comment}»; a `price_change`/`product_created`/`product_edited` op renders no batch second line.
    - The /history table stays 8 columns.
  </behavior>
  <action>
    Change `history_view` to `select(Operation, Product, Batch).join(Product, ...).outerjoin(Batch, Operation.batch_id == Batch.id)` and add a `"batch"` key per row dict (may be None). Keep pagination, filters, and the `page_size + 1` sentinel unchanged. In history_rows.html, inside the existing «Товар» cell, add a muted SECOND line (do NOT add a column — the table stays 8 columns): for a stock-affecting op with a batch → «Партия: {ru_date(expiry)|«без срока»}{ — comment}»; for a stock-affecting op with NULL batch_id → «До внедрения партий» (D-15 legacy label, read-time only — the ledger is never rewritten); for audit types (price_change/product_created/product_edited) → no second line. Route context passthrough only.
    Update tests/test_history.py: a pre-migration (NULL batch_id) stock op renders «До внедрения партий»; a batched op renders its «Партия: …» line; a price_change row renders no batch second line.
  </action>
  <acceptance_criteria>
    - A stock op with NULL batch_id renders «До внедрения партий»; the operation row is not mutated (no UPDATE issued) (D-15).
    - A batched op renders «Партия: …»; a `price_change` op renders no batch line.
    - `history_view` uses `outerjoin(Batch, Operation.batch_id == Batch.id)` and history_rows.html still renders 8 columns.
    - `uv run pytest tests/test_history.py -x -q` passes.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_history.py -x -q</automated>
  </verify>
  <done>history_view outer-joins Batch and /history shows a muted batch line (legacy label for NULL batch_id) at read time without rewriting the ledger; the table stays 8 columns.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Flip the D-12 mandatory batch guard + full-suite sweep</name>
  <files>app/services/ledger.py, tests/test_ledger.py</files>
  <read_first>
    E:\dev\myorishop\app\services\ledger.py (the `record_operation` batch block added in Plan 01 — extend it with the mandatory/audit branches)
    E:\dev\myorishop\app\models.py (`OPERATION_TYPES` at lines 34-43 — the source list for `STOCK_AFFECTING_TYPES`)
    E:\dev\myorishop\.planning\phases\09-batch-tracking-ledger-integration\09-RESEARCH.md (D-12, "Pattern 1: Dual cached projection" — the full guard shape, Pitfall 1)
    E:\dev\myorishop\.planning\phases\09-batch-tracking-ledger-integration\09-VALIDATION.md (Wave 0 requirements — the signature-change sweep list)
  </read_first>
  <behavior>
    - `record_operation(type_="sale", product_id=..., qty_delta=-1)` with no `batch_id` raises ValueError (D-12); same for receipt/writeoff/return/correction.
    - `record_operation(type_="price_change", ..., qty_delta=0, batch_id=<id>)` raises ValueError (audit types are batch-less).
    - Audit ops (`product_created`, `price_change`) still succeed with `batch_id=None`.
    - `uv run pytest -q` is green after the flip (every stock-affecting caller already supplies a batch).
  </behavior>
  <action>
    Add `STOCK_AFFECTING_TYPES = frozenset({"receipt", "sale", "writeoff", "return", "correction"})` to app/services/ledger.py. Extend the `record_operation` batch block to the full D-12 guard: for `type_ in STOCK_AFFECTING_TYPES`, `batch_id is None` now raises `ValueError(f"batch_id is required for {type_!r} operations")`; keep the existing unknown-batch and product-ownership ValueErrors; for a non-stock-affecting (audit) type, `batch_id is not None` raises `ValueError(f"{type_!r} operations are batch-less")`. This is the LOT-05 enforcement backstop and the phase's write-path invariant.
    Because Plans 02-04 already thread `batch_id` through receipts/sales/writeoffs/corrections and Task 1 threads it through returns, the flip should leave those green. Run the FULL suite (`uv run pytest -q`) and fix any straggler test that stages a stock-affecting op without a batch (the signature-change sweep from 09-VALIDATION Wave 0: test_ledger, test_smoke, and any direct `record_operation` caller). Add ledger tests: a `receipt`/`sale`/etc. without `batch_id` raises ValueError; a `price_change` WITH a batch_id raises ValueError; the qty_delta==0 audit types still write with `batch_id=None`.
  </action>
  <acceptance_criteria>
    - `record_operation(type_="sale", product_id=..., qty_delta=-1)` with no `batch_id` raises ValueError; `record_operation(type_="price_change", ..., qty_delta=0, batch_id=<id>)` raises ValueError (D-12).
    - Audit ops (`product_created`, `price_change`) still succeed with `batch_id=None`.
    - `uv run pytest -q` green (the whole D-12 blast radius resolved — every stock-affecting caller supplies a batch).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_ledger.py -x -q && uv run pytest -q</automated>
  </verify>
  <done>record_operation enforces the mandatory D-12 batch guard for stock-affecting types and rejects batch_id on audit types; the full suite is green with every stock-affecting caller supplying a batch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| return form → register_return | `origin_op_id`/`qty` untrusted; a forged origin must be rejected (existing gate) — the batch is server-resolved, never client-supplied |
| any service caller → record_operation | after the flip, the write path is the mandatory batch-integrity choke point |
| stored batch text → /history + return form | comment/expiry rendered into HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-16 | Tampering / integrity | stock-affecting op without a batch | mitigate | D-12 mandatory guard in `record_operation` raises ValueError — the write-path backstop for LOT-05 across all current and future callers |
| T-09-17 | Tampering | return batch attribution | mitigate | batch is server-resolved from the validated origin op (or the legacy batch), never accepted from the return form (D-08) |
| T-09-18 | Info-integrity | legacy /history attribution | mitigate | NULL batch_id resolved to the legacy label at READ time (LEFT OUTER JOIN); the append-only ledger is never rewritten (D-15) — criterion 5 display side |
| T-09-19 | Tampering (XSS) | batch text in /history + return form | mitigate | Jinja autoescape only; never `|safe` |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | zero packages installed this phase (vacuous gate) |
</threat_model>

<artifacts_produced>
## Artifacts this plan produces

- `app/services/returns.py`: `register_return` batch inheritance (`origin.batch_id` or `legacy_batch`, lazy-create for the no-legacy-batch case); frozen legacy-batch constant.
- `app/routes/returns.py` + `app/templates/partials/return_form.html`: read-only origin-batch line (no picker).
- `app/services/operations.py`: `history_view` LEFT OUTER JOIN Batch + `"batch"` row key.
- `app/templates/partials/history_rows.html`: muted batch second line in the «Товар» cell (D-15 legacy label).
- `app/services/ledger.py`: `STOCK_AFFECTING_TYPES` + D-12 mandatory guard.
</artifacts_produced>

<verification>
- `uv run pytest -q` — full suite green (the D-12 flip's blast radius fully resolved).
- `ruff check .` clean.
- Manual (UAT gates 7,8): /history shows «До внедрения партий» on pre-migration stock ops and nothing extra on price-change rows; a return of a legacy sale shows «Возврат в партию: Остаток до внедрения партий».
</verification>

<success_criteria>
- ROADMAP criterion 3 completed: the return path is batch-attributed (via inheritance, no re-ask) — LOT-05 fully covered.
- ROADMAP criterion 5: v1.0 stock and sales history remain intact after migration; legacy operations show as belonging to a default legacy batch; totals and reports still balance (display side + the migration seed from Plan 01) — D-15.
- D-12 mandatory guard enforced at the single write path.
- Full pytest suite green; phase ready for `/gsd-verify-work`.
</success_criteria>

<output>
Create `.planning/phases/09-batch-tracking-ledger-integration/09-05-SUMMARY.md` when done.
</output>

---
phase: 09-batch-tracking-ledger-integration
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - app/templates/partials/history_rows.html
  - app/templates/pages/history.html
  - tests/test_history.py
autonomous: true
gap_closure: true
requirements: [LOT-05]
must_haves:
  truths:
    - "/history shows a dedicated «Код» column with the product code (not buried inside the «Товар» cell)."
    - "A sale row on /history has a «Вернуть» link that opens the existing return flow; a #return-slot receives it."
  artifacts:
    - path: "app/templates/partials/history_rows.html"
      provides: "«Код» <td> + «Действие» <td> with the «Вернуть» hx-get link on sale rows"
      contains: "/returns?sale_id="
    - path: "app/templates/pages/history.html"
      provides: "matching «Код» and «Действие» <th> headers + a #return-slot div"
      contains: "return-slot"
    - path: "tests/test_history.py"
      provides: "regression asserting the code column + «Вернуть» link render and the 8→10 column update"
  key_links:
    - from: "app/templates/partials/history_rows.html"
      to: "GET /returns"
      via: "hx-get link on r.op.type == 'sale' rows"
      pattern: "hx-get=\"/returns\\?sale_id=.*origin_op_id="
    - from: "app/templates/pages/history.html"
      to: "#return-slot"
      via: "hx-target of the «Вернуть» link"
      pattern: "id=\"return-slot\""
---

<objective>
Fix Phase 9 UAT test 6 (major): /history has no discoverable «Вернуть» (return) action, and the product code is buried inside the «Товар» cell as `Name (CODE)` instead of a scannable column. Because /sales' recent list is capped at 10, /history is the only view where an old legacy sale can be found and returned — so this omission blocked verifying the legacy-return display.

Root cause is already diagnosed (do NOT re-diagnose — see .planning/debug/history-return-button-and-code-column.md): this is a TEMPLATE-ONLY omission. The two sibling sale-listing templates — partials/recent_sales.html (/sales) and partials/purchase_history.html (customer detail) — already carry a dedicated «Код» column, a «Действие» column with a «Вернуть» `hx-get="/returns?sale_id=…&product_id=…&origin_op_id=…"` link, and a `<div id="return-slot"></div>`. history_rows.html never carried them over. All row data (`r.op.sale_id`, `r.op.id`, `r.product.id`, `r.product.code`) and the entry-point-agnostic GET/POST /returns routes already exist — NO route or service changes.

Purpose: make legacy-sale returns reachable from /history and surface the code column the operator asked for.
Output: two edited templates (mirroring the existing sibling pattern) + updated regression tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/09-batch-tracking-ledger-integration/09-UAT.md
@.planning/debug/history-return-button-and-code-column.md

@app/templates/partials/history_rows.html
@app/templates/pages/history.html
@app/templates/partials/recent_sales.html
@app/templates/partials/purchase_history.html
@tests/test_history.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add «Код» column, «Вернуть» action column, and #return-slot to /history</name>
  <files>app/templates/pages/history.html, app/templates/partials/history_rows.html</files>
  <action>
Mirror the established pattern in partials/recent_sales.html / partials/purchase_history.html — do not invent new markup shapes.

In app/templates/partials/history_rows.html:
  1. Add a dedicated code cell as a NEW `<td>{{ r.product.code }}</td>` placed BEFORE the «Товар» cell, and REMOVE the inline ` ({{ r.product.code }})` from the «Товар» cell so the code lives in exactly one place (the user asked for the code column «вместо» searching inside «Товар»; recent_sales.html/purchase_history.html both keep code and name in separate columns). The «Товар» cell keeps `{{ r.product.name }}` plus its existing D-15 muted batch second line (the `{% if r.op.type not in ("price_change", "product_created", "product_edited") %}` block) unchanged. CAUTION (W3): edit ONLY the inline `({{ r.product.code }})` fragment on line 29 — do NOT strip or reflow the `<br><span class="muted">…</span>` D-15 batch-attribution second line that lives in the same cell (lines 30-38); it must render byte-for-byte as before.
  2. Add a trailing «Действие» cell as the LAST `<td>` of each data row: for sale rows only (`{% if r.op.type == "sale" %}`) render the same neutral text link shape as recent_sales.html — `<a href="#" hx-get="/returns?sale_id={{ r.op.sale_id }}&product_id={{ r.product.id }}&origin_op_id={{ r.op.id }}" hx-target="#return-slot" hx-swap="innerHTML">Вернуть</a>`; for non-sale rows render `<span class="muted">—</span>` so the column stays aligned. Keep autoescape (product name is untrusted-at-rest, T-05-18 — never |safe).
  3. Update the empty-state row `colspan` from `8` to `10` (two columns were added).

In app/templates/pages/history.html `<thead>`: add a `<th>Код</th>` header positioned to match the new code cell order (between «Тип» and «Товар»), and a trailing `<th>Действие</th>` header (last). Add a `<div id="return-slot"></div>` AFTER the `</table>` (mirroring where recent_sales.html/purchase_history.html place their return-slot — outside the rows so an OOB tbody/filter swap never wipes an in-progress return form). Respect the existing `#history-tbody` innerHTML-swap and `<tfoot>` `#load-more` boundaries noted in history_rows.html's CR-01 comment — the return-slot lives outside the table, not inside tbody/tfoot.

Verify the header column count now matches the data row column count (10 each): Когда, Тип, Код, Товар, Кол-во, Цена, Себестоимость, Причина, Кто, Действие (the «Код» column sits between «Тип» and «Товар», matching the `<th>Код</th>` placement and the `<td>{{ r.product.code }}</td>` cell added before the «Товар» cell above) — confirm the header order and the `<td>` order in history_rows.html line up cell-for-cell.
  </action>
  <verify>
    <automated>cd "E:/dev/myorishop" && uv run pytest tests/test_history.py -x -q</automated>
  </verify>
  <done>/history renders a «Код» column and a sale-row «Вернуть» link whose hx-get matches the recent_sales.html URL shape; a #return-slot exists outside the table; header and data rows are both 10 columns; empty-state colspan is 10.</done>
</task>

<task type="auto">
  <name>Task 2: Update history regression tests for the new columns and return link</name>
  <files>tests/test_history.py</files>
  <action>
Two existing tests encode the pre-fix layout and MUST be updated (they will otherwise fail):
  1. `test_web_history_table_stays_8_columns` (asserts `count("<th", …) == 8`): update the assertion to `== 10`, and rename the function + docstring to reflect that /history now has 10 columns (added «Код» and «Действие»). Keep the same `<thead>`/`</thead>` slice technique.
  2. `test_web_history_filters` — its assertions `f"<td>{stocked_product.name} ({stocked_product.code})"` and `f"<td>{product.name} ({product.code})"` reference the now-removed inline `(code)`. Update both to drop the ` ({code})` suffix (assert `f"<td>{stocked_product.name}"` present and `f"<td>{product.name}"` absent). Update the docstring note about the «Товар» cell scope accordingly (code moved to its own column; the batch second line still sits between name and the closing tag).

Add one new regression test `test_web_history_has_code_column_and_return_link` (using the existing `client`/`session`/`stocked_product` fixtures and the `_seed_mixed_ops` helper): GET `/history`, assert status 200 and that `response.text` contains a return link for the seeded sale op — assert the substring `/returns?sale_id=` and `origin_op_id=` are present, that `>Вернуть<` is present, and that `id="return-slot"` is present. Also assert the product code renders in its own cell (`f"<td>{stocked_product.code}</td>"`).

Scan the rest of tests/test_history.py for any other assertion that depends on the 8-column count or the inline `(code)` shape and update it the same way; do not weaken unrelated assertions.
  </action>
  <verify>
    <automated>cd "E:/dev/myorishop" && uv run pytest tests/test_history.py -q</automated>
  </verify>
  <done>All history tests pass, including the renamed 10-column test, the updated filter-scope assertions, and the new code-column/return-link test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /history link → GET /returns | sale_id/product_id/origin_op_id become query params; returns.py `_resolve_origin()` already validates them entry-point-agnostically |
| stored product name → template | untrusted-at-rest (T-05-18), rendered in the «Товар» and «Код» cells |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07-01 | Tampering | «Вернуть» link params | accept | GET /returns already re-resolves and validates the origin sale op regardless of caller (see debug session); no new trust surface — same URL shape as the two shipping siblings |
| T-09-07-02 | Information disclosure | product name/code rendering | mitigate | keep Jinja autoescape on the new «Код»/«Товар» cells — never \|safe |
</threat_model>

<verification>
- `uv run pytest tests/test_history.py -q` passes.
- `uv run ruff check tests/test_history.py` clean.
- Manual UAT re-test (browser, 09-UAT.md test 6): /history shows a «Код» column; a legacy sale row shows «Вернуть»; clicking it renders the return form into #return-slot and the «Возврат в партию: Остаток до внедрения партий» line is verifiable.
</verification>

<success_criteria>
- /history data rows and header are both 10 columns with a dedicated «Код» column.
- Sale rows expose a «Вернуть» link with the same `/returns?…` shape as recent_sales.html, targeting #return-slot.
- No route or service file was modified (template + test only).
</success_criteria>

<output>
Create `.planning/phases/09-batch-tracking-ledger-integration/09-07-SUMMARY.md` when done.
</output>

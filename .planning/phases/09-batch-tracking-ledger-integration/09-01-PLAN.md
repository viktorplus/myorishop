---
phase: 09-batch-tracking-ledger-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models.py
  - alembic/versions/0008_batches.py
  - app/services/batches.py
  - app/services/ledger.py
  - app/core.py
  - app/routes/__init__.py
  - tests/conftest.py
  - tests/test_batches.py
  - tests/test_ledger.py
autonomous: true
requirements: [LOT-01, LOT-03]
must_haves:
  truths:
    - "Batch rows hold product_id, warehouse_id, optional expiry, price_cents, location, comment, cached quantity, and is_legacy; a batch has NO deleted_at and no standalone CRUD (D-03, LOT-01)"
    - "record_operation accepts an optional batch_id and, when supplied, increments Batch.quantity with a SQL-side increment in the SAME transaction as Product.quantity (D-11)"
    - "A supplied batch_id whose Batch.product_id differs from the operation's product_id raises ValueError before any write (D-12 ownership backstop)"
    - "operations.batch_id is added by native op.add_column with no inline FK, and both operations_no_update and operations_no_delete triggers still exist after alembic upgrade head (D-10)"
    - "Migration 0008 seeds exactly one legacy batch per product whose ledger SUM(qty_delta) > 0, with quantity read from the ledger SUM in plain SQL and is_legacy=1, warehouse_id = the frozen default-warehouse UUID, expiry/price_cents NULL, comment 'Остаток до внедрения партий' (D-13, D-14)"
    - "open_batches returns quantity>0 batches ordered earliest expiry first, NULL expiry last, tie-broken by oldest created_at (D-07)"
    - "rebuild_stock recomputes each batch quantity from the ledger and raises when Product.quantity != SUM(batch quantities) + the uncaptured NULL bucket for products with no legacy batch (D-11)"
  artifacts:
    - path: "app/models.py"
      provides: "Batch model + Operation.batch_id nullable FK column"
      contains: "class Batch"
    - path: "alembic/versions/0008_batches.py"
      provides: "batches table, operations.batch_id, per-product legacy-batch seed"
      contains: "revision"
    - path: "app/services/batches.py"
      provides: "open_batches (D-07 order), legacy_batch, active_warehouses read helpers"
      exports: ["open_batches", "legacy_batch", "active_warehouses"]
    - path: "app/services/ledger.py"
      provides: "record_operation batch_id param + dual projection, compute_batch_stock, rebuild_stock invariant"
      contains: "batch_id"
    - path: "tests/test_batches.py"
      provides: "model, ordering, rebuild-invariant, and migration-replay tests"
      contains: "def test_"
  key_links:
    - from: "app/services/ledger.py record_operation"
      to: "Batch.quantity"
      via: "SQL-side increment in the same transaction"
      pattern: "Batch\\.quantity \\+ qty_delta"
    - from: "alembic/versions/0008_batches.py legacy seed"
      to: "SUM(operations.qty_delta)"
      via: "plain SQL GROUP BY product_id HAVING SUM > 0"
      pattern: "SUM\\(qty_delta\\)"
---

<objective>
Lay the batch write-path foundation: the `Batch` model, migration `0008_batches` (batches table + `operations.batch_id` + per-product legacy-batch seed), the `app/services/batches.py` read helpers, and the `record_operation` / `rebuild_stock` extensions that thread a per-batch quantity projection through the single write path. Also add the shared `ru_date` display filter used by every downstream batch surface.

This plan is the riskiest change in the milestone. It touches the ledger write path and the live data migration, so it must land with the full pytest suite green.

Purpose: Establish the stock-holding unit (`Batch`) and make the single write path batch-aware WITHOUT yet forcing every caller to supply a batch. `batch_id` stays OPTIONAL here (default `None`); the mandatory D-12 guard is flipped ON in Plan 05 after every operation service already passes a batch. This keeps the suite green at every wave boundary (RESEARCH Pitfall 1: never split the guard from its callers into a red wave) while still delivering D-11's dual projection and the D-13/D-14 legacy migration now.
Output: A migrated schema at Alembic head `0008`, a batch-aware `record_operation`, ledger-derivable batch quantities, and Wave-0 test coverage (model, ordering, rebuild invariant, migration replay).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-batch-tracking-ledger-integration/09-CONTEXT.md
@.planning/phases/09-batch-tracking-ledger-integration/09-RESEARCH.md
@.planning/phases/09-batch-tracking-ledger-integration/09-PATTERNS.md
@.planning/phases/09-batch-tracking-ledger-integration/09-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Batch model, Operation.batch_id, and batches.py read helpers</name>
  <files>app/models.py, app/services/batches.py, app/core.py, app/routes/__init__.py</files>
  <read_first>
    E:\dev\myorishop\app\models.py (add `Batch`; mirror the `Warehouse` class at lines 119-137 and the `Operation.sale_id` nullable-FK column at lines 176-178; copy the `Product.quantity` cached-projection column at line 112)
    E:\dev\myorishop\app\services\warehouses.py (module shape + `list_warehouses` — the analog for batches.py; note it deliberately INCLUDES deleted rows, so `active_warehouses` must filter `deleted_at IS NULL`)
    E:\dev\myorishop\app\core.py (`format_cents` at lines 49-52 — the analog for the new `format_ru_date` helper)
    E:\dev\myorishop\app\routes\__init__.py (filter registration; add `ru_date` next to `cents`/`local_dt`)
    E:\dev\myorishop\.planning\phases\09-batch-tracking-ledger-integration\09-RESEARCH.md ("Batch model" and "Open-batch query with D-07 ordering" code drafts — verified consistent with the analogs; use them verbatim for column names and function bodies)
    E:\dev\myorishop\.planning\phases\09-batch-tracking-ledger-integration\09-PATTERNS.md (sections "app/models.py — Batch model" and "app/services/batches.py")
  </read_first>
  <behavior>
    - `Batch` is a new SQLAlchemy 2.0 model: UUID str PK; product_id/warehouse_id named FKs; expiry String(10) nullable; price_cents Integer nullable; location String(100) nullable; comment String(200) nullable; quantity Integer not-null default 0; is_legacy Integer not-null default 0; created_at/updated_at via utcnow_iso. It has NO deleted_at column (D-03).
    - `Operation` gains a nullable `batch_id` String(36) ORM FK named `fk_operations_batch_id_batches`, indexed (mirrors sale_id).
    - `open_batches(session, product_id, warehouse_id=None)` returns quantity>0 batches ordered earliest-expiry-first, NULL-expiry-last, tie-broken by oldest created_at; an optional warehouse filter narrows it; a zero-quantity batch is excluded.
    - `legacy_batch(session, product_id)` returns the is_legacy=1 batch for a product or None; `active_warehouses(session)` returns only deleted_at IS NULL warehouses, name-ordered.
    - `format_ru_date("2026-07-12")` == "12.07.2026"; `format_ru_date(None)` == "" and `format_ru_date("")` == "".
  </behavior>
  <action>
    Add class `Batch(Base)` to app/models.py with columns: `id` String(36) PK default `new_id`; `product_id` FK→`products.id` (named FK, `index=True`); `warehouse_id` FK→`warehouses.id` (named FK); `expiry` String(10) nullable (ISO yyyy-mm-dd, LOT-03); `price_cents` Integer nullable (sale-price snapshot); `location` String(100) nullable (WH-02); `comment` String(200) nullable (LOT-04); `quantity` Integer not-null default 0 (cached projection, mirror `Product.quantity`); `is_legacy` Integer not-null default 0 (set to 1 only by migration 0008); `created_at`/`updated_at` String(32) with `utcnow_iso` default/onupdate. The class docstring MUST cite D-03 (no `deleted_at`, no standalone CRUD) and D-13/D-14 (is_legacy marks the migration-seeded per-product legacy batch). Add to `Operation` a nullable `batch_id: Mapped[str | None]` = `mapped_column(ForeignKey("batches.id", name="fk_operations_batch_id_batches"), index=True)` — copy the exact shape of `sale_id` (lines 176-178); docstring note: set at INSERT time only, NULL = legacy (D-10/D-15).
    Create app/services/batches.py mirroring the warehouses.py module shape (module docstring citing D-07/D-08/D-13, RU-free read helpers taking `session` first): `open_batches(session, product_id, warehouse_id=None) -> list[Batch]` selecting `Batch.quantity > 0`, optional warehouse filter, ordered by `nullslast(Batch.expiry.asc()), Batch.created_at.asc()` (import `nullslast` from sqlalchemy — D-07, verified on SQLite 3.50.4); `legacy_batch(session, product_id) -> Batch | None` selecting `is_legacy == 1` (D-08 fallback target); `active_warehouses(session) -> list[Warehouse]` selecting `Warehouse.deleted_at.is_(None)` ordered by name (the receipt select needs active-only, unlike `list_warehouses`).
    Add `format_ru_date(iso: str | None) -> str` to app/core.py: return `""` for `None`/empty; otherwise reformat the stored ISO `yyyy-mm-dd` to `dd.mm.yyyy` (string slicing or `date.fromisoformat` — no locale dependency). Register it in app/routes/__init__.py as `templates.env.filters["ru_date"] = format_ru_date`.
  </action>
  <acceptance_criteria>
    - `app/models.py` contains `class Batch` and a `batch_id` mapped_column referencing `fk_operations_batch_id_batches`; `Batch` has NO `deleted_at` column.
    - `open_batches` on three batches (expiry 2026-01-01, NULL, 2025-06-01) returns them in order [2025-06-01, 2026-01-01, NULL]; a zero-quantity batch is excluded.
    - `format_ru_date("2026-07-12")` == "12.07.2026"; `format_ru_date(None)` == "".
    - `uv run pytest tests/test_batches.py -k "model or ordering or ru_date" -x -q` passes.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_batches.py -k "model or ordering or ru_date" -x -q</automated>
  </verify>
  <done>Batch model exists (no deleted_at); Operation.batch_id nullable FK added; batches.py provides open_batches (D-07 order), legacy_batch, active_warehouses; ru_date filter registered and formats ISO to dd.mm.yyyy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migration 0008 (batches table, operations.batch_id, legacy seed) + replay test</name>
  <files>alembic/versions/0008_batches.py, tests/test_batches.py</files>
  <read_first>
    E:\dev\myorishop\alembic\versions\0004_sales_customers.py (native `op.add_column` with a BARE column + `op.create_index`, and the docstring explaining Alembic's SQLite inline-FK NotImplementedError; downgrade drops index then column)
    E:\dev\myorishop\alembic\versions\0007_warehouses.py (frozen `DEFAULT_WAREHOUSE_ID = "00000000-0000-4000-8000-000000000010"`, `_SEED_CREATED_AT`, "never import app modules" rule)
    E:\dev\myorishop\alembic\versions\0001_initial_schema.py (the `operations_no_update`/`operations_no_delete` trigger DDL and the frozen warning that batch-mode migrations DROP them)
    E:\dev\myorishop\tests\test_catalog.py (the `command.upgrade(cfg, "head")` migration-test pattern with tmp_path db + monkeypatch settings.db_path — the analog for the replay test)
    E:\dev\myorishop\.planning\phases\09-batch-tracking-ledger-integration\09-RESEARCH.md ("Migration 0008 skeleton" — precedent-consistent, use verbatim; Pitfall 6 verification SQL)
    E:\dev\myorishop\.planning\phases\09-batch-tracking-ledger-integration\09-PATTERNS.md ("alembic/versions/0008_batches.py" Analogs A/B/C; "No Analog Found" migration-replay test spec)
  </read_first>
  <behavior>
    - `alembic upgrade head` on a DB seeded with pre-batch operations (incl. one product with ledger SUM ≤ 0 and one with SUM > 0) creates the `batches` table and adds `operations.batch_id` without dropping any table/row.
    - Exactly one legacy batch is created per SUM>0 product, with quantity == that product's `SUM(operations.qty_delta)` (read from the ledger, not the cache) and is_legacy=1; the ≤0-stock product gets NO legacy batch.
    - After migration `SELECT count(*) FROM sqlite_master WHERE type='trigger' AND name LIKE 'operations_no_%'` returns 2 and an `UPDATE operations` still ABORTs.
    - `downgrade()` drops `ix_operations_batch_id`, `operations.batch_id`, `ix_batches_product_id`, then `batches` cleanly.
  </behavior>
  <action>
    Create alembic/versions/0008_batches.py with `revision = "0008"`, `down_revision = "0007"`. Module docstring cites D-10/D-13/D-14, the batch-mode trigger warning, and "never import app modules". Re-declare frozen literals `DEFAULT_WAREHOUSE_ID = "00000000-0000-4000-8000-000000000010"`, `LEGACY_COMMENT = "Остаток до внедрения партий"`, `_SEED_CREATED_AT`, and a frozen `_LEGACY_NS` uuid namespace (stdlib `uuid` import IS allowed; app-module imports are NOT).
    upgrade(): `op.create_table("batches", ...)` with named PK/FK constraints via `op.f(...)` (product_id→products.id, warehouse_id→warehouses.id) and columns exactly matching the Batch model (id, product_id, warehouse_id, expiry String(10), price_cents Integer, location String(100), comment String(200), quantity Integer not-null, is_legacy Integer not-null, created_at/updated_at String(32)); `op.create_index(op.f("ix_batches_product_id"), ...)`. Then NATIVE `op.add_column("operations", sa.Column("batch_id", sa.String(36), nullable=True))` — BARE, no inline FK (the 0004 precedent) — followed by `op.create_index(op.f("ix_operations_batch_id"), "operations", ["batch_id"])`. NEVER use `batch_alter_table("operations")`.
    Legacy seed (D-13): `conn = op.get_bind()`; select `product_id, SUM(qty_delta) AS qty FROM operations GROUP BY product_id HAVING SUM(qty_delta) > 0` via `sa.text` (plain SQL, NEVER the products.quantity cache — Pitfall 5); for each row INSERT a batches row with `id = str(uuid.uuid5(_LEGACY_NS, product_id))`, `warehouse_id = DEFAULT_WAREHOUSE_ID`, expiry/price_cents/location NULL, `comment = LEGACY_COMMENT`, `quantity = qty`, `is_legacy = 1`, timestamps = `_SEED_CREATED_AT`. downgrade(): drop `ix_operations_batch_id`, drop `operations.batch_id`, drop `ix_batches_product_id`, drop table `batches` (0004 mirror order).
    Add the migration-replay test to tests/test_batches.py (no in-repo analog — build it fresh, mirroring test_catalog.py's `command.upgrade` shape): create a temp SQLite DB, install the 0001 triggers, seed pre-batch operations INCLUDING at least one product whose ledger SUM is ≤ 0 and one with SUM > 0, run the Alembic upgrade to head against the temp DB, then assert: (a) exactly one legacy batch per SUM>0 product with `quantity == ledger SUM` and `is_legacy == 1`; (b) NO legacy batch for the ≤0-stock product; (c) `SELECT count(*) FROM sqlite_master WHERE type='trigger' AND name LIKE 'operations_no_%'` returns 2; (d) an `UPDATE operations SET qty_delta = qty_delta` still ABORTs (triggers intact — criterion 5).
  </action>
  <acceptance_criteria>
    - After upgrade, legacy-batch quantity for a SUM>0 product equals `SUM(operations.qty_delta)` for that product (not the cache); the ≤0-stock product has no legacy batch.
    - Trigger count is 2 after migration and an UPDATE on operations raises/aborts.
    - `grep -c "batch_alter_table" alembic/versions/0008_batches.py` returns 0; the migration uses `op.add_column("operations", ...)`.
    - `grep -c "import app\.\|from app" alembic/versions/0008_batches.py` returns 0 (never import app modules).
    - `uv run pytest tests/test_batches.py -k migration -x -q` passes.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_batches.py -k migration -x -q</automated>
  </verify>
  <done>Migration 0008 creates batches + operations.batch_id via native add_column, seeds one legacy batch per SUM>0 product from the ledger SUM, and preserves both append-only triggers (criterion 5 foundation); replay test asserts all of it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: record_operation dual projection + rebuild_stock invariant + conftest/test sweep</name>
  <files>app/services/ledger.py, tests/conftest.py, tests/test_batches.py, tests/test_ledger.py</files>
  <read_first>
    E:\dev\myorishop\app\services\ledger.py (the whole file — extend `record_operation` guard block at lines 60-90, the `sale_id` optional-param precedent at lines 38/78, and `compute_stock`/`rebuild_stock` at lines 93-107)
    E:\dev\myorishop\tests\conftest.py (fixtures `product` 38-49, `stocked_product` 52-71, `customer` 74-86 — the shapes to copy for `warehouse`/`batch`)
    E:\dev\myorishop\.planning\phases\09-batch-tracking-ledger-integration\09-RESEARCH.md ("Pattern 1: Dual cached projection", "rebuild_stock per-batch pass + invariant" code draft, Pitfall 1)
    E:\dev\myorishop\.planning\phases\09-batch-tracking-ledger-integration\09-PATTERNS.md ("app/services/ledger.py — record_operation extension" and "tests/conftest.py" sections)
  </read_first>
  <behavior>
    - `record_operation(..., type_="receipt", batch_id=b.id, qty_delta=5)` increases BOTH `product.quantity` and `batch.quantity` by 5 in one transaction.
    - `record_operation` with a `batch_id` whose Batch belongs to a different product raises `ValueError`; an unknown batch_id raises `ValueError`.
    - `record_operation` WITHOUT a batch_id still succeeds for every type (batch_id remains optional this plan — no mandatory guard yet).
    - `rebuild_stock` recomputes each batch quantity from the ledger and does not raise for consistent data (legacy batch absorbs the NULL bucket; a product with no legacy batch keeps its NULL-bucket ops accounted for); it raises on a deliberately corrupted cache.
  </behavior>
  <action>
    Extend `record_operation` in app/services/ledger.py: add a keyword-only `batch_id: str | None = None` parameter (mirror `sale_id`), pass it into the `Operation(...)` constructor. After the existing product guards (lines 60-68) and BEFORE `session.add(op)`, add a batch resolution block that runs ONLY when `batch_id is not None`: `batch = session.get(Batch, batch_id)`; raise `ValueError(f"unknown batch: {batch_id!r}")` if None; raise `ValueError("batch does not belong to product")` if `batch.product_id != product_id` (security: untrusted client id — the T-09 tampering mitigation). Keep the existing `product.quantity = Product.quantity + qty_delta` line UNCHANGED; add, only when a batch was resolved, `batch.quantity = Batch.quantity + qty_delta` (SQL-side increment, same transaction, D-11). Do NOT add the mandatory-when-missing guard or the audit-type rejection here — those are Plan 05's D-12 flip; keeping `batch_id` optional now is what keeps the suite green (RESEARCH Pitfall 1).
    Add `compute_batch_stock(session, batch) -> int` and extend `rebuild_stock`: after the existing product pass, add a per-batch pass that sets `batch.quantity = compute_batch_stock(...)` (a legacy batch also absorbs `SUM(qty_delta WHERE product_id = b.product_id AND batch_id IS NULL)`), accumulate per-product batch totals and the legacy-product set, then assert per product `Product.quantity == batch_total + (NULL-bucket SUM only when the product has NO legacy batch)`; raise `ValueError` on mismatch (D-11 invariant). Use the RESEARCH `rebuild_stock` draft.
    conftest.py: add a `warehouse` fixture (mirror `customer`) that creates a Warehouse, plus an option/fixture to create the seeded-default-id warehouse when a test needs it; add a `batch` fixture creating a `Batch(product_id=..., warehouse_id=..., quantity=0)`; update `stocked_product` to create its batch and pass `batch_id=batch.id` into its `record_operation` receipt call so its product and batch quantities agree. `Base.metadata.create_all` already covers the new table. Run the FULL suite and fix any straggler that now trips the rebuild invariant; the suite MUST stay green (batch_id is still optional, so batch-less call sites keep working).
    Add unit tests to tests/test_batches.py: dual projection (a receipt with batch_id bumps both Product.quantity and Batch.quantity); ownership guard (a batch_id from a different product raises ValueError); `rebuild_stock` invariant holds for a mixed product (batch ops + NULL-bucket) and for a legacy batch that absorbs the NULL bucket.
  </action>
  <acceptance_criteria>
    - `record_operation(..., type_="receipt", batch_id=b.id, qty_delta=5)` results in both `product.quantity` and `batch.quantity` increased by 5.
    - Calling `record_operation` with a `batch_id` whose Batch belongs to a different product raises `ValueError`.
    - `rebuild_stock` on seeded data with a legacy batch + NULL-bucket ops leaves caches consistent and does not raise.
    - `uv run pytest -q` is green (D-12 blast radius neutralized by keeping batch_id optional this plan).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_batches.py tests/test_ledger.py -x -q && uv run pytest -q</automated>
  </verify>
  <done>record_operation maintains both quantity projections in one transaction and rejects a mismatched batch; rebuild_stock enforces the per-batch invariant; conftest has warehouse/batch fixtures and a batch-consistent stocked_product; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service caller → record_operation | a `batch_id` reaching the write path is untrusted (may name another product's batch, a forged id, or an exhausted batch) |
| migration → live ledger | migration 0008 must add data without mutating or corrupting the append-only ledger |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | record_operation batch_id | mitigate | `session.get(Batch)` + `batch.product_id == product_id` ValueError guard active whenever batch_id is supplied (mirrors the IN-01 deleted-product guard) |
| T-09-02 | Tampering / Info-integrity | migration 0008 on operations | mitigate | native `op.add_column` only (no batch mode); replay test asserts both `operations_no_%` triggers survive and an UPDATE still ABORTs |
| T-09-03 | Tampering | legacy-batch quantity seed | mitigate | seed reads `SUM(operations.qty_delta)` in plain SQL, never `products.quantity`, so criterion 5 balances against a stale cache (D-13) |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | this phase installs ZERO packages (RESEARCH Package Legitimacy Gate satisfied vacuously); no install task exists |
</threat_model>

<artifacts_produced>
## Artifacts this plan produces

- `Batch` model (`app/models.py`): `batches` table — columns `id, product_id, warehouse_id, expiry, price_cents, location, comment, quantity, is_legacy, created_at, updated_at`; named FKs `fk_batches_product_id_products`, `fk_batches_warehouse_id_warehouses`; index `ix_batches_product_id`. NO `deleted_at` (D-03).
- `Operation.batch_id` (`app/models.py`): nullable `String(36)` ORM FK `fk_operations_batch_id_batches`, indexed.
- Alembic migration revision `0008` (`alembic/versions/0008_batches.py`): batches table, `operations.batch_id` (+ `ix_operations_batch_id`), per-product legacy-batch seed. Frozen constants `DEFAULT_WAREHOUSE_ID`, `LEGACY_COMMENT`, `_SEED_CREATED_AT`, `_LEGACY_NS`.
- `app/services/batches.py`: `open_batches(session, product_id, warehouse_id=None)`, `legacy_batch(session, product_id)`, `active_warehouses(session)`.
- `app/services/ledger.py`: `record_operation(..., batch_id=None)` param + dual projection + ownership guard; `compute_batch_stock(session, batch)`; `rebuild_stock` invariant.
- `app/core.py`: `format_ru_date(iso)`; registered Jinja filter `ru_date` (`app/routes/__init__.py`).
- Test fixtures `warehouse`, `batch` and updated `stocked_product` (`tests/conftest.py`); `tests/test_batches.py`.
</artifacts_produced>

<verification>
- `uv run pytest -q` — full suite green (mandatory: D-12 blast radius is neutralized by keeping batch_id optional).
- `uv run alembic upgrade head` then downgrade one step then upgrade again on a scratch copy of a seeded DB — reversible, triggers intact.
- `ruff check .` clean.
</verification>

<success_criteria>
- Schema at Alembic head `0008` with a `batches` table and `operations.batch_id`; both append-only triggers survive (ROADMAP criterion 5 foundation).
- `record_operation` maintains both quantity projections in one transaction (D-11); a mismatched batch_id is rejected (D-12 ownership backstop).
- Legacy batches seeded per SUM>0 product from the ledger, not the cache (D-13/D-14).
- Full pytest suite green.
</success_criteria>

<output>
Create `.planning/phases/09-batch-tracking-ledger-integration/09-01-SUMMARY.md` when done.
</output>

---
phase: 23-dashboard-history-rebuild
plan: 05
type: execute
wave: 2
depends_on: ["23-02"]
files_modified:
  - app/routes/mobile_history.py
  - app/templates/mobile_pages/history.html
  - app/templates/mobile_partials/history_cards.html
  - app/templates/mobile_partials/history_pagination.html
  - tests/test_mobile_history.py
autonomous: true
requirements: [HIST-01, HIST-02, HIST-03, HIST-04]

must_haves:
  truths:
    - "Mobile История has full data parity with desktop: same type-first column narrowing, same 4 filter dimensions, same numbered pagination (D-10)"
    - "Mobile История migrates off the legacy load-more control onto page_window/paginate — the same mechanism desktop already uses (D-10 closes this known gap)"
    - "#history-cards and the pagination bar remain DOM siblings, never nested — a filter change can never destroy the pagination control (CR-01 precedent)"
    - "The dashboard feed's deep link (/m/history?type=X&product=Y) correctly narrows results even though mobile has no visible Товар filter control"
  artifacts:
    - path: "app/routes/mobile_history.py"
      provides: "GET /m/history with type/product/sort/page + category/customer/from/to, numbered pagination"
      contains: "def mobile_history_page"
    - path: "app/templates/mobile_partials/history_pagination.html"
      provides: "OOB-swappable pagination sibling, replaces history_load_more.html"
      contains: "history-pagination"
  key_links:
    - from: "app/templates/mobile_partials/history_cards.html"
      to: "GET /m/history"
      via: "hx-get + hx-include=\"#history-filters input, #history-filters select\""
      pattern: "hx-target=\"#history-cards\" hx-swap=\"outerHTML\""
    - from: "app/routes/mobile_history.py"
      to: "app/services/pagination.py::page_window"
      via: "direct call, replaces the has_next sentinel"
      pattern: "page_window\\("
---

<objective>
Bring mobile `/m/history` to full parity with desktop (D-10): the same
type-first per-type column narrowing (HIST-01), the same 4 filter
dimensions (HIST-02), and — closing the known Phase-14 generation gap —
numbered pagination instead of the legacy "Показать ещё" load-more control
(HIST-04). Rendered in mobile's own card layout, not a squeezed copy of the
desktop table.

Purpose: mobile gets identical underlying data/filters to desktop, presented
as cards, per the established "same data, own simpler layout" idiom
(Phase 12-22 precedent).
Output: `/m/history` fully paginated + filterable; `history_load_more.html`
retired.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-dashboard-history-rebuild/23-CONTEXT.md
@.planning/phases/23-dashboard-history-rebuild/23-UI-SPEC.md
@.planning/phases/23-dashboard-history-rebuild/23-02-SUMMARY.md

<interfaces>
From Plan 02's `app/services/operations.py`: same extended `history_view`
signature as Plan 04 (`customer`, `category`, `start_iso`, `end_iso`,
returns `"columns"`, rows carry `"warehouse"`).

From `app/routes/mobile_history.py` (current, to extend — note the CURRENT
signature deliberately has NO `product` param, per a Phase-11 UI-SPEC
decision this phase's D-10 supersedes): `mobile_history_page(request,
type="", page=0, session=...)`.

From `app/services/pagination.py`: `page_window(page, total_pages, spread=2)
-> list[int|str]`, `LIST_PAGE_SIZE = 20`.

From `app/templates/partials/pagination.html` (reuse UNMODIFIED — expects
`list_url`, `page`, `total_pages`, `page_window`, `rows_target_id`,
`extra_qs`; every button uses `hx-target="#{{ rows_target_id }}"
hx-swap="outerHTML"`).

From `app/routes/mobile_finance.py::_movement_success` (the established
"concatenate a main fragment + one or more OOB fragments" HTMLResponse
pattern this plan's HX-Request branch mirrors).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: mobile_history.py — full filter set + numbered pagination</name>
  <files>app/routes/mobile_history.py, tests/test_mobile_history.py</files>
  <read_first>app/routes/mobile_history.py (full file), app/routes/history.py (Plan 04's desktop version, for the _resolve_history_period pattern and extra_qs shape to mirror), app/services/pagination.py (page_window)</read_first>
  <action>
    Add `product: str = "", category: str = "", customer: str = "", from_:
    str = Query("", alias="from"), to: str = Query("", alias="to")` to
    `mobile_history_page` (reinstating `product` — UI-SPEC's dashboard feed
    deep-link passes `&product=...` to `/m/history`; there is still no
    visible Товар control on mobile, this param exists for the deep-link
    only, per 23-UI-SPEC.md's Copywriting Contract). Reuse the SAME local
    `_resolve_history_period`-equivalent date resolution as Plan 04's
    history.py (either duplicate the small helper here or factor it — this
    codebase has no shared route-helper module today, so duplicate it with
    a comment cross-referencing history.py, matching how `_metrics_context`
    is already independently duplicated between finance.py and
    mobile_finance.py). Call `history_view(session, type_filter=type or
    None, product_id=product or None, page=page, customer=customer or None,
    category=category or None, start_iso=start_iso, end_iso=end_iso)`.

    Replace the `has_next` sentinel entirely with `page_window(result["page"],
    result["total_pages"])` and an `extra_qs` re-serialization mirroring
    history.py's Task 1 shape (type/product/category/customer/from/to, all
    conditionally included when non-blank). On an HX-Request, render TWO
    fragments concatenated into one `HTMLResponse` (mirrors
    `_movement_success`'s pattern): `mobile_partials/history_cards.html`
    (main, self-wrapping `<div id="history-cards">`, includes `"columns"`
    in its context) as the PRIMARY fragment, and
    `mobile_partials/history_pagination.html` rendered with `oob=True` as
    the SECOND (out-of-band) fragment — both ALWAYS returned together on
    every HX-Request (filter change OR pagination click), so a pagination
    click also refreshes correctly and a filter change also refreshes the
    page count. On a plain GET, render the full `mobile_pages/history.html`
    chrome with both pieces in context.
  </action>
  <verify>
    <automated>cd "E:\dev\myorishop" &amp;&amp; uv run pytest tests/test_mobile_history.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `GET /m/history?type=writeoff&page=1` (with &gt;20 writeoff rows seeded) returns the second page's rows AND a rendered pagination bar showing "Страница 2 из N" (mirrors desktop's `test_web_history_pagination_bar_reflects_filtered_total`).
    - `GET /m/history?product=<id>` (no other filter) narrows results to that product — the reinstated deep-link param works even with no visible UI control for it.
    - The existing `test_no_product_filter_param_on_route_signature` test is UPDATED (not silently deleted) to reflect that `product` is now an intentional, D-10-driven parameter — replace its assertion with one confirming `product` IS present, with a docstring noting this supersedes the Phase-11 decision per 23-CONTEXT.md D-10.
    - `test_paging_returns_additional_cards_when_has_next` is updated: the old "page=1" load-more-link substring assertion is replaced with an assertion on the numbered pagination bar's presence/content (`'class="pagination"'` or a `"Страница"` substring), matching desktop's equivalent test shape.
  </acceptance_criteria>
  <done>mobile_history_page supports the same 4 filter dimensions as desktop plus product, and returns numbered pagination instead of has_next/load-more.</done>
</task>

<task type="auto">
  <name>Task 2: mobile templates — stacked filters, self-wrapping cards, OOB pagination sibling</name>
  <files>app/templates/mobile_pages/history.html, app/templates/mobile_partials/history_cards.html, app/templates/mobile_partials/history_pagination.html</files>
  <read_first>app/templates/mobile_pages/history.html (full file), app/templates/mobile_partials/history_cards.html (full file), app/templates/mobile_partials/history_load_more.html (the OOB-sibling pattern being replaced — read for the exact `hx-swap-oob` idiom to mirror, then this file should be deleted), app/templates/partials/pagination.html (reused unmodified), 23-UI-SPEC.md Interaction 14</read_first>
  <action>
    In mobile_pages/history.html: wrap the Тип/Категория/Покупатель
    (conditional)/от/по controls in a new `<div id="history-filters">`
    (stacked `.field` divs, one per control, mirroring the existing single-
    field layout). Every control gets `hx-get="/m/history" hx-trigger=
    "change"` (or `"input changed delay:300ms"` for the two text inputs,
    matching desktop's debounce) `hx-include="#history-filters input,
    #history-filters select" hx-target="#history-cards" hx-swap="outerHTML"
    hx-push-url="true"`. The Покупатель field is wrapped in `{% if
    type_filter in ("sale", "return") %}...{% endif %}`, hidden entirely
    otherwise (D-05, mirrors desktop). Date fields are two plain `.field`
    divs (`<input type="date" name="from">` / `name="to">`) — no preset-bar
    reuse on mobile (screen-space discretion, D-10's "own simpler layout").
    Below `#history-filters`: `<div id="history-cards">{% include
    "mobile_partials/history_cards.html" %}</div>` becomes just `{% include
    "mobile_partials/history_cards.html" %}` (the id moves INTO the partial
    itself in this task, so it can self-wrap for outerHTML swaps — see
    below). Replace the old `{% include "mobile_partials/history_load_more.html"
    %}` line with `{% include "mobile_partials/history_pagination.html" %}`.
    `#return-slot` stays exactly where it is (outside both, CR-01 precedent
    unchanged).

    In history_cards.html: add the self-wrapping `<div id="history-cards"
    {% if oob %} hx-swap-oob="true"{% endif %}>` around the existing content
    (closing `</div>` at the end) — mirrors `recent_sales.html`'s/
    `finance_tiles.html`'s self-wrapping-partial convention, needed so
    `partials/pagination.html`'s outerHTML-swap buttons (targeting
    `#history-cards`) receive a response that includes the wrapper itself.
    Make each card's field lines conditional on `columns` (the SAME
    per-type narrowing as desktop, mirroring 23-UI-SPEC.md Interaction 8's
    column list — reuse Plan 04's exact per-field Jinja expressions
    verbatim, e.g. profit computed inline the same way, «Розница» for a
    None customer): when `columns` is `None`, keep TODAY'S card markup
    completely unchanged (everything visible, "Кол-во · Цена ·
    Себестоимость" line + created_by line); when `columns` is a tuple,
    render only the lines for keys present in it (expiry/qty/price/cost/
    profit/customer/reason/warehouse), same "Срок"/"Склад" copy and muted-
    dash fallbacks as desktop.

    Create mobile_partials/history_pagination.html:
    `<div id="history-pagination"{% if oob %} hx-swap-oob="true"{% endif
    %}>{% include "partials/pagination.html" %}</div>` — expects the SAME
    `list_url` ("/m/history"), `rows_target_id` ("history-cards"),
    `page`/`total_pages`/`page_window`/`extra_qs` context vars the route
    (Task 1) already supplies. Delete
    app/templates/mobile_partials/history_load_more.html (fully superseded
    — verified nothing else in the codebase references it; do not confuse
    with the separate `mobile_partials/cash_history_load_more.html`, which
    stays untouched, out of scope).
  </action>
  <verify>
    <automated>cd "E:\dev\myorishop" &amp;&amp; uv run pytest tests/test_mobile_history.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `GET /m/history` (HX-Request, e.g. a type-select change) returns a response containing BOTH `id="history-cards"` and `id="history-pagination"` (two sibling fragments, never nested — grep-verifiable: the `history-pagination` div is not inside the `history-cards` div's closing tag).
    - `GET /m/history?type=sale` cards show «Прибыль»/«Покупатель» lines; `GET /m/history?type=receipt` cards do not.
    - `GET /m/history` (no type) cards render identically to today's unchanged shape (columns=None fallback).
    - `mobile_partials/history_load_more.html` no longer exists; `mobile_partials/cash_history_load_more.html` is untouched and still used by mobile_finance.py.
  </acceptance_criteria>
  <done>Mobile История has stacked filters, per-type-narrowed cards, and numbered pagination as a DOM sibling of the cards — never nested, never destroyed by a filter swap.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 05)

- `app/routes/mobile_history.py`: `mobile_history_page` gains `product`, `category`, `customer`, `from`/`from_`, `to`; numbered pagination replaces `has_next`
- `app/templates/mobile_partials/history_pagination.html` — new, OOB-swappable pagination sibling
- `app/templates/mobile_partials/history_cards.html`: self-wrapping `#history-cards` id, per-type field narrowing
- `app/templates/mobile_pages/history.html`: new `#history-filters` wrapper, stacked filter fields
- `app/templates/mobile_partials/history_load_more.html` — deleted (superseded)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser -> GET /m/history query string | Untrusted `product`, `category`, `customer`, `from`, `to` params (mirrors Plan 04's desktop boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-23-14 | Tampering | `from`/`to`/`category`/`customer` on mobile | mitigate | Same Plan 02 service-layer allow-list/bounded-candidate discipline as desktop; this plan adds no new SQL construction, only param plumbing |
| T-23-15 | Denial of Service (minor) | OOB dual-fragment response construction | mitigate | Both fragments are always rendered from the same already-validated `history_view` result — no additional query/parse step that could raise independently |
</threat_model>

<verification>
- `uv run pytest tests/test_mobile_history.py -x` green.
- `uv run pytest tests/test_history.py tests/test_mobile_history.py -x` (cross-check desktop unaffected).
</verification>

<success_criteria>
- Mobile История matches desktop's filter/column/sort capability (HIST-01/02/03), fully paginated via `page_window` (HIST-04), rendered as cards.
</success_criteria>

<output>
Create `.planning/phases/23-dashboard-history-rebuild/23-05-SUMMARY.md` when done.
</output>

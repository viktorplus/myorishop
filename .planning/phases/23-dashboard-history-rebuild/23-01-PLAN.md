---
phase: 23-dashboard-history-rebuild
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models.py
  - alembic/versions/0016_active_catalog.py
  - app/services/active_catalog.py
  - app/routes/catalogs.py
  - app/templates/pages/catalogs.html
  - app/templates/partials/active_catalog_form.html
  - tests/test_active_catalog.py
autonomous: true
requirements: [DASH-02]

must_haves:
  truths:
    - "Operator can set the active catalog's number and close date on the existing /catalogs page (D-01, D-02)"
    - "A missing/never-configured active catalog does not error — it is a placeholder state other pages can render independently around"
    - "A malformed close date shows an inline RU error and does not save"
  artifacts:
    - path: "app/models.py"
      provides: "ActiveCatalog model (singleton row: id, number, close_date, created_at, updated_at)"
      contains: "class ActiveCatalog(Base)"
    - path: "alembic/versions/0016_active_catalog.py"
      provides: "active_catalog table migration, down_revision = 0015"
      contains: "down_revision = \"0015\""
    - path: "app/services/active_catalog.py"
      provides: "get_active_catalog / set_active_catalog"
      exports: ["get_active_catalog", "set_active_catalog"]
  key_links:
    - from: "app/routes/catalogs.py"
      to: "app/services/active_catalog.py"
      via: "get_active_catalog / set_active_catalog calls"
      pattern: "active_catalog\\.(get|set)_active_catalog"
    - from: "app/templates/partials/active_catalog_form.html"
      to: "POST /catalogs/active"
      via: "hx-post"
      pattern: "hx-post=\"/catalogs/active\""
---

<objective>
Add the active-catalog number + close-date fields (DASH-02) as a brand-new
`ActiveCatalog` table, service, and a small form on the existing `/catalogs`
page. Per D-01, both fields are fully manual (never derived from the PDF
filename scan). Per D-02, this lives on `/catalogs` — no new route, no
Настройки page (that doesn't exist until Phase 24).

Purpose: give Phase 23's dashboard (Plan 03) a source of truth for the
catalog-countdown line, without touching `app/services/catalogs.py`'s
existing pure-filesystem-scan behavior at all.
Output: `ActiveCatalog` table + migration, `app/services/active_catalog.py`,
and a working save form on `/catalogs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-dashboard-history-rebuild/23-CONTEXT.md
@.planning/phases/23-dashboard-history-rebuild/23-RESEARCH.md
@.planning/phases/23-dashboard-history-rebuild/23-UI-SPEC.md

<interfaces>
From `alembic/versions/0015_customer_contacts.py` (exact migration shape to
mirror — native `op.create_table`, no batch mode, no app-module imports):
revision = "0016"; down_revision = "0015"; branch_labels = None; depends_on = None.

From `app/models.py` (existing `Warehouse` — minimal-table shape to mirror
for `ActiveCatalog`: UUID String(36) PK via `default=new_id`, `created_at`/
`updated_at` via `default=utcnow_iso`/`onupdate=utcnow_iso`).

From `app/routes/catalogs.py` (existing, unmodified elsewhere this plan):
`catalogs_page(request, year="", sort="", page=0, session=Depends(get_session))`
renders `pages/catalogs.html` via `_catalogs_context(session, ...)`.

From `app/services/catalog.py::parse_optional_cents` / `create_customer`
(error-dict convention to mirror): return `(value_or_None, dict[str,str])`,
nothing written on any error key present.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ActiveCatalog model + Alembic migration 0016</name>
  <files>app/models.py, alembic/versions/0016_active_catalog.py</files>
  <read_first>app/models.py (Warehouse class, NAMING_CONVENTION), alembic/versions/0015_customer_contacts.py (exact migration shape/style to mirror)</read_first>
  <behavior>
    - `ActiveCatalog.__tablename__ == "active_catalog"`; columns: id (String(36) PK, default=new_id), number (String(20), nullable), close_date (String(10), nullable), created_at (String(32), default=utcnow_iso), updated_at (String(32), default=utcnow_iso, onupdate=utcnow_iso).
    - Migration 0016: revision="0016", down_revision="0015"; upgrade() creates the table natively (op.create_table, sa.PrimaryKeyConstraint("id", name=op.f("pk_active_catalog"))); downgrade() drops it. No FK, no index, no CHECK constraint, no seed row (empty table = "no active catalog configured" state).
  </behavior>
  <action>
    Add `class ActiveCatalog(Base)` to app/models.py, placed near `Warehouse`
    (both are minimal standalone tables with no FK wiring). Mirror
    `Warehouse`'s exact `id`/`created_at`/`updated_at` column declarations.
    Add a short docstring: singleton row, no unique constraint (cardinality
    enforced by the service layer's get-or-create in Task 2, mirroring how
    `Batch` has no standalone CRUD lifecycle by convention rather than a DB
    constraint). Create alembic/versions/0016_active_catalog.py following
    0015_customer_contacts.py's exact file header/docstring/import style
    (WR-06: never import application modules from a migration file).
  </action>
  <verify>
    <automated>cd "E:\dev\myorishop" &amp;&amp; uv run alembic upgrade head &amp;&amp; uv run alembic downgrade -1 &amp;&amp; uv run alembic upgrade head</automated>
  </verify>
  <acceptance_criteria>
    - `uv run alembic upgrade head` succeeds with 0016 as the new head (no error).
    - `uv run alembic downgrade -1` then `upgrade head` round-trips cleanly (table drop/recreate both succeed).
    - `app/models.py` conventions test (money-as-integer-cents / naming) still passes: `uv run pytest tests/test_conventions.py -x` if that file exists, else `uv run pytest -k conventions -x`.
  </acceptance_criteria>
  <done>ActiveCatalog table exists via migration 0016; model class importable from app.models.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: active_catalog service — get/set the singleton row</name>
  <files>app/services/active_catalog.py, tests/test_active_catalog.py</files>
  <read_first>app/services/catalog.py (parse_optional_cents / create_product error-dict convention), app/core.py (new_id, utcnow_iso, date.fromisoformat usage precedent in format_ru_date)</read_first>
  <behavior>
    - `get_active_catalog(session) -> ActiveCatalog | None`: the single row (order_by created_at, limit 1), or None if the table is empty (never-configured state).
    - `set_active_catalog(session, *, number: str, close_date: str) -> tuple[ActiveCatalog | None, dict[str, str]]`:
      - Both fields independently optional (stripped-empty -> stored NULL); no cross-field validation.
      - `number` longer than 20 chars (String(20) column) -> `{"number": "Слишком длинный номер каталога."}`, nothing written.
      - Non-blank `close_date` that fails `date.fromisoformat` -> `{"close_date": "Проверьте дату закрытия каталога."}` (exact UI-SPEC copy), nothing written.
      - On success: get-or-create the singleton row (get_active_catalog; if None, create with new_id()+utcnow_iso()), set number/close_date, commit, return (row, {}).
    - Two round-trip tests: create then read back; update overwrites in place (still exactly one row in the table after two set_active_catalog calls).
  </behavior>
  <action>
    Create app/services/active_catalog.py with the two functions above.
    Module-level constants: `NUMBER_TOO_LONG_ERROR = "Слишком длинный номер каталога."`
    and `CLOSE_DATE_ERROR = "Проверьте дату закрытия каталога."` (exact copy
    from 23-UI-SPEC.md's Copywriting Contract — «Error state — catalog form
    (D-02)»). `_NUMBER_MAX_LEN = 20` mirrors the column width (WR-05 style
    guard, matches app/services/customers.py's `_ADDRESS_MAX_LEN` pattern).
    Write tests/test_active_catalog.py covering: empty-table get returns
    None; set then get round-trips both fields; set with blank fields stores
    NULL for both; overlong number rejected with zero writes; malformed
    close_date rejected with zero writes (assert the table row count is
    still 0, or unchanged, via a second get_active_catalog call); a second
    set_active_catalog call updates the SAME row (table still has exactly
    one row — query `select(func.count()).select_from(ActiveCatalog)`).
  </action>
  <verify>
    <automated>cd "E:\dev\myorishop" &amp;&amp; uv run pytest tests/test_active_catalog.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_active_catalog.py -x` passes, covering every behavior bullet above.
    - Calling `set_active_catalog` twice never creates a second row (singleton invariant, service-enforced).
  </acceptance_criteria>
  <done>app/services/active_catalog.py exports get_active_catalog/set_active_catalog; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: /catalogs active-catalog form (route + template)</name>
  <files>app/routes/catalogs.py, app/templates/pages/catalogs.html, app/templates/partials/active_catalog_form.html, tests/test_active_catalog.py</files>
  <read_first>app/routes/catalogs.py (catalogs_page, full file), app/templates/pages/catalogs.html, app/routes/finance.py (finance_withdraw — the "always re-render the form partial, no is_hx branching" convention for a simple POST), 23-UI-SPEC.md Interaction 6 (the exact form markup and copy contract)</read_first>
  <action>
    In app/routes/catalogs.py: import get_active_catalog/set_active_catalog
    from app.services.active_catalog. In `catalogs_page`, add
    `"active": get_active_catalog(session), "error": None` to the context
    dict passed to `pages/catalogs.html` (the HX-partial branch for
    `partials/catalog_rows.html` is untouched — it never needed `active`).
    Add a new `POST /catalogs/active` route: `Form(number: str = "")`,
    `Form(close_date: str = "")`, calls `set_active_catalog`; on error,
    render `partials/active_catalog_form.html` with
    `active={"number": number, "close_date": close_date}` (echo raw
    submitted values) and `error=` the single error message (first value
    from the errors dict); on success, render the same partial with
    `active=<the saved ActiveCatalog row>` and `error=None`. Always return
    this same partial response regardless of HX-Request header (mirrors
    `finance_withdraw`'s convention — this endpoint is only ever reached via
    the form's own hx-post, so no is_hx branching is needed).

    Create app/templates/partials/active_catalog_form.html with the exact
    markup from 23-UI-SPEC.md Interaction 6: `<form hx-post="/catalogs/active"
    hx-target="this" hx-swap="outerHTML" method="post" action="/catalogs/active">`
    (method/action added for no-JS progressive enhancement, matching
    period_filter.html's pattern of pairing native method/action with
    hx-get/hx-post), two `.field` divs (Номер каталога / Дата закрытия,
    `type="date"` for the second), submit button «Сохранить», and
    `{% if error %}<p class="error">{{ error }}</p>{% endif %}`. Field
    values: `value="{{ active.number or '' }}"` / `value="{{ active.close_date or '' }}"`
    (works whether `active` is the ORM row or the plain echo-dict — Jinja
    attribute access resolves both).

    In app/templates/pages/catalogs.html: after the existing `<h1>Каталоги</h1>`,
    add `<h2>Активный каталог</h2>` followed by
    `{% include "partials/active_catalog_form.html" %}`, then the existing
    `{% include "partials/catalog_rows.html" %}` unchanged below it.
  </action>
  <verify>
    <automated>cd "E:\dev\myorishop" &amp;&amp; uv run pytest tests/test_active_catalog.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `GET /catalogs` (no active row yet) renders 200 with the empty form (blank number/close_date inputs) and the rest of the page (catalog list) unaffected.
    - `POST /catalogs/active` with `number=05`, `close_date=2026-08-31` returns 200, and a subsequent `GET /catalogs` shows those values pre-filled in the form.
    - `POST /catalogs/active` with `close_date=not-a-date` returns the form re-rendered with «Проверьте дату закрытия каталога.» and does NOT change the previously saved row (verify via a follow-up GET still showing the last valid saved value, or None if never saved).
  </acceptance_criteria>
  <done>Operator can save/see the active catalog number+close date from /catalogs; malformed input never writes and shows the RU error inline.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

- `app.models.ActiveCatalog` — SQLAlchemy model class (fields: id, number, close_date, created_at, updated_at)
- `alembic/versions/0016_active_catalog.py` — migration (revision "0016", down_revision "0015")
- `app.services.active_catalog.get_active_catalog(session) -> ActiveCatalog | None`
- `app.services.active_catalog.set_active_catalog(session, *, number: str, close_date: str) -> tuple[ActiveCatalog | None, dict[str, str]]`
- `app.services.active_catalog.NUMBER_TOO_LONG_ERROR`, `app.services.active_catalog.CLOSE_DATE_ERROR` (RU error copy constants)
- `POST /catalogs/active` route (app/routes/catalogs.py)
- `app/templates/partials/active_catalog_form.html` — new partial
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser -> POST /catalogs/active | Untrusted operator-entered `number` (free text) and `close_date` (date string) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-23-01 | Tampering | POST /catalogs/active `close_date` | mitigate | `date.fromisoformat` validation in `set_active_catalog`; malformed value never reaches the DB, RU error shown inline (ASVS V5) |
| T-23-02 | Tampering | POST /catalogs/active `number` | mitigate | Server-side max-length guard (20 chars, mirrors the column width) before any write |
| T-23-03 | Information Disclosure / Tampering | `active.number` free-text render on `/catalogs` and (later, Plan 06/07) `/` and `/m/` | mitigate | Jinja autoescape only, never `\|safe` on this field, consistent with every other untrusted-text field in this codebase |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan (Package Legitimacy Audit: not applicable per 23-RESEARCH.md) |
</threat_model>

<verification>
- `uv run alembic upgrade head` then `downgrade -1` then `upgrade head` round-trips cleanly.
- `uv run pytest tests/test_active_catalog.py -x` green.
</verification>

<success_criteria>
- ActiveCatalog table exists (migration 0016), reachable via `app.services.active_catalog`.
- `/catalogs` shows a working save form for the active catalog number + close date; malformed close dates are rejected with an inline RU error and zero writes; no active row is a placeholder state, not an error, on every consumer.
</success_criteria>

<output>
Create `.planning/phases/23-dashboard-history-rebuild/23-01-SUMMARY.md` when done.
</output>

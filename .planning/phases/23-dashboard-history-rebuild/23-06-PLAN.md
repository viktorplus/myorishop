---
phase: 23-dashboard-history-rebuild
plan: 06
type: execute
wave: 3
depends_on: ["23-03"]
files_modified:
  - app/routes/home.py
  - app/templates/pages/home.html
  - app/templates/partials/dashboard_tiles.html
  - tests/test_home.py
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, DASH-05]

must_haves:
  truths:
    - "Desktop Главная shows current date/weekday/time, the active-catalog countdown (or its empty-state link), and a 4-tile metric grid (Сегодня/Неделя/Месяц/Склад)"
    - "Desktop Главная shows a 10-column recent-operations feed, columns populated per operation type with muted dashes for irrelevant cells"
    - "Every feed row links into /history pre-filtered to that row's type + product"
    - "Money is never sign-colored; a closed catalog shows the word «закрыт», never a red tint or a bare negative number"
  artifacts:
    - path: "app/routes/home.py"
      provides: "GET / rebuilt, calls dashboard_context"
      contains: "dashboard_context"
    - path: "app/templates/pages/home.html"
      provides: "the rebuilt Главная page"
      contains: "Последние операции"
    - path: "app/templates/partials/dashboard_tiles.html"
      provides: "4-tile metric grid (Сегодня/Неделя/Месяц/Склад)"
      contains: "metric-grid"
  key_links:
    - from: "app/routes/home.py"
      to: "app/services/dashboard.py::dashboard_context"
      via: "direct call"
      pattern: "dashboard_context\\(session"
    - from: "app/templates/pages/home.html"
      to: "/history"
      via: "«Подробнее» link per feed row"
      pattern: "/history\\?type="
---

<objective>
Replace the Phase-1 walking-skeleton `pages/home.html` (currently: oldest
active product + last 50 raw operations, no dashboard content) with the
real DASH-01..05 dashboard, built entirely from Plan 03's
`dashboard_context()`. Thin route, all composition already done in the
service layer.

Purpose: Главная answers "what is the state of the business right now" at a
glance (ROADMAP Phase 23 goal, desktop half).
Output: `GET /` renders date/time, catalog countdown, 4 metric tiles, and a
10-column recent-operations feed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-dashboard-history-rebuild/23-CONTEXT.md
@.planning/phases/23-dashboard-history-rebuild/23-UI-SPEC.md
@.planning/phases/23-dashboard-history-rebuild/23-03-SUMMARY.md

<interfaces>
From Plan 03's `app/services/dashboard.py`:
`dashboard_context(session, tz_name: str) -> dict` returns
`{"weekday", "date", "time", "catalog": dict|None, "metrics": {"today",
"week", "month"}, "stock": {"product_count","cost_value_cents",
"sale_value_cents","cost_unknown_count","sale_unknown_count"}, "feed":
list[{"op","product","batch","customer"}]}`. `catalog` (when not None):
`{"number","close_date","days_left","closed"}`. Each `metrics.<period>`:
`{"revenue_cents","profit_cents","expense_cents"}`.

From `app/services/ledger.py`: `STOCK_AFFECTING_TYPES` (for template
reference if needed — feed rows are already filtered server-side, the
template does not need to re-filter).

From `app/templates/partials/finance_tiles.html` (styling precedent for
`.metric-grid`/`.metric-tile`/`.tile-label` markup — reuse the class names,
not the content).

From `app/templates/partials/history_rows.html` (Plan 04's per-type feed
row rendering — the SAME Jinja expressions for signed qty, muted-dash
price/cost, inline profit computation, and «Розница» customer fallback
apply here for DASH-05's feed table; copy them verbatim rather than
re-deriving).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: home.py route rebuild</name>
  <files>app/routes/home.py</files>
  <read_first>app/routes/home.py (current 17-line stub), app/routes/finance.py (finance_page — the thin-route composition pattern to mirror), app/config.py (settings.display_tz)</read_first>
  <action>
    Replace the entire file: `GET /` now calls `dashboard_context(session,
    settings.display_tz)` and passes the result straight into
    `pages/home.html`. Remove the `ledger_view` import (the walking-skeleton
    correction form and "oldest active product" concept are fully retired
    by this plan — the last read of `app.services.ledger.ledger_view` from
    a route disappears; leave `ledger_view` itself in `app/services/ledger.py`
    untouched, it may still be imported by tests). No new query params, no
    HX-Request branching (Главная has no swappable regions in this phase —
    every reload gets a fresh timestamp, matching the established
    no-client-side-clock convention).
  </action>
  <verify>
    <automated>cd "E:\dev\myorishop" &amp;&amp; uv run pytest tests/test_home.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `GET /` returns 200 and the response context passed to the template contains every one of `dashboard_context`'s top-level keys.
    - No route in this file calls `ledger_view` anymore.
  </acceptance_criteria>
  <done>home.py is a 2-statement thin route delegating entirely to dashboard_context.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pages/home.html rebuild + dashboard_tiles.html partial</name>
  <files>app/templates/pages/home.html, app/templates/partials/dashboard_tiles.html, tests/test_home.py</files>
  <read_first>app/templates/pages/home.html (current stub, to be replaced), app/templates/partials/finance_tiles.html (metric-grid markup precedent), app/templates/partials/history_rows.html (per-type row rendering to copy verbatim into the feed table), app/templates/partials/recent_sales.html (the «Розница» customer fallback), 23-UI-SPEC.md Interaction 1/2/3/4 (exact page order, copy, and the 4-tile composition)</read_first>
  <behavior>
    - Page order top-to-bottom (Interaction 1): `<h1>Главная</h1>`, date/weekday/time line, catalog line (or empty state), 4-tile metric grid, `<h2>Последние операции</h2>`, the feed table.
    - DASH-02 empty state never blocks the rest of the page (Interaction 2) — `dashboard_context`'s other keys always render regardless of `catalog`.
    - Money is never sign-colored (plain `#222` text via `| cents`, including a negative net-profit figure) — Color rule 1.
    - A closed catalog renders the word «закрыт» via `<span class="muted">`, never a bare negative day count or a red tint (WCAG 1.4.1).
  </behavior>
  <action>
    Rewrite app/templates/pages/home.html per 23-UI-SPEC.md Interaction 1
    exactly: `<h1>Главная</h1>`; `<p class="muted">{{ weekday }}, {{ date }}
    · {{ time }}</p>`; the catalog line — empty state «Активный каталог не
    задан. &lt;a href="/catalogs"&gt;Указать номер и дату закрытия&lt;/a&gt;.»
    when `not catalog`, else «Каталог &lt;strong&gt;{{ catalog.number }}
    &lt;/strong&gt; — &lt;span class="muted"&gt;закрыт&lt;/span&gt;» when
    `catalog.closed`, else «Каталог &lt;strong&gt;{{ catalog.number }}
    &lt;/strong&gt; — осталось дней: &lt;strong&gt;{{ catalog.days_left }}
    &lt;/strong&gt;» when `catalog.close_date`, else just «Каталог &lt;strong&gt;
    {{ catalog.number }}&lt;/strong&gt;» (number-only, no close_date set);
    `{% include "partials/dashboard_tiles.html" %}`; `&lt;h2&gt;Последние
    операции&lt;/h2&gt;`; a 10-column table (Когда · Тип · Код · Товар ·
    Срок · Кол-во · Себестоимость · Прибыль · Покупатель · Действие) over
    `feed`, one row per entry — reuse `history_rows.html`'s exact per-field
    Jinja (signed qty, muted-dash cost, inline profit, «Розница» fallback),
    empty state «Операций пока нет.» when `not feed`; each row's «Действие»
    cell is always «Подробнее» linking to
    `/history?type={{ r.op.type }}&amp;product={{ r.product.id }}`.

    Create app/templates/partials/dashboard_tiles.html: `.metric-grid` with
    4 `.metric-tile` divs — «Сегодня»/«Неделя»/«Месяц» each rendering 3
    `.num` lines (Выручка/Прибыль/Расход via `metrics.today|week|month
    .revenue_cents|profit_cents|expense_cents`, all through the `| cents`
    filter, no sign stripping); «Склад» rendering «Товаров в наличии: {{
    stock.product_count }}», «Стоимость по закупке: {{ stock.cost_value_cents
    | cents }}», «Стоимость по продаже: {{ stock.sale_value_cents | cents
    }}», a `<p class="muted">на текущий момент</p>` caption, and the
    existing `cost_unknown_count`/`sale_unknown_count` caveat lines copied
    verbatim from `finance_tiles.html`.

    Write tests/test_home.py: `GET /` renders 200 with «Главная» and
    «Последние операции» headings; an empty-catalog state renders the
    «Указать номер и дату закрытия» link and the rest of the page (4 tiles)
    still renders; a closed catalog renders «закрыт» not a negative number;
    a feed row for each of receipt/sale/writeoff/transfer shows the
    type-appropriate populated cells and muted dashes elsewhere (mirrors
    desktop history's per-type column tests, applied to the always-10-column
    feed shape); each feed row's «Подробнее» link targets
    `/history?type=...&amp;product=...`.
  </action>
  <verify>
    <automated>cd "E:\dev\myorishop" &amp;&amp; uv run pytest tests/test_home.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_home.py -x` green, covering every behavior bullet.
    - No `<button class="danger">` or any destructive control anywhere on this page (this phase's read-only rebuild, per 23-UI-SPEC.md's Copywriting Contract).
    - `dashboard_tiles.html` introduces zero new CSS rules (reuses `.metric-grid`/`.metric-tile`/`.tile-label` verbatim from `style.css`, per 23-UI-SPEC.md's "zero new CSS" contract).
  </acceptance_criteria>
  <done>Desktop Главная is the full DASH-01..05 dashboard; empty-catalog and closed-catalog states render correctly; feed links into /history.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 06)

- `app/routes/home.py` — rebuilt, thin, calls `dashboard_context`
- `app/templates/pages/home.html` — rebuilt Главная (date/time, catalog line, tiles include, feed table)
- `app/templates/partials/dashboard_tiles.html` — new, 4-tile `.metric-grid` (Сегодня/Неделя/Месяц/Склад)
- `tests/test_home.py` — new test file
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new — this plan is 100% read-only rendering of already-validated `dashboard_context` output | No new untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-23-16 | Information Disclosure / Tampering | `catalog.number` / feed product names rendered on the home page | mitigate | Jinja autoescape only, never `\|safe`, consistent with every other untrusted-text field already rendered elsewhere in this codebase |
| T-23-17 | Tampering | feed row «Подробнее» link construction | accept | `op.type`/`product.id` are server-controlled values (not user input) at render time; the link target `/history` itself independently validates/allow-lists its own `type` param (Plan 02/04) |
</threat_model>

<verification>
- `uv run pytest tests/test_home.py -x` green.
- Manual smoke: `GET /` with and without an active-catalog row configured, both render without error.
</verification>

<success_criteria>
- DASH-01..05 fully satisfied on desktop: date/time, catalog countdown/empty-state, 4-tile metrics, 10-column feed linking into History.
</success_criteria>

<output>
Create `.planning/phases/23-dashboard-history-rebuild/23-06-SUMMARY.md` when done.
</output>

---
phase: 23-dashboard-history-rebuild
plan: 04
type: execute
wave: 2
depends_on: ["23-02"]
files_modified:
  - app/routes/history.py
  - app/templates/partials/history_rows.html
  - app/templates/partials/period_filter.html
autonomous: true
requirements: [HIST-01, HIST-02, HIST-03, HIST-04]

must_haves:
  truths:
    - "Selecting an operation type on /history narrows both the row set AND the column set in one swap (D-03)"
    - "Before any type is picked, /history shows today's unchanged generic 10-column view (D-04)"
    - "All 9 operation types remain selectable in the Тип dropdown; the 3 audit types keep the generic column view (Pitfall 5)"
    - "The customer filter only appears for sale/return; category and date-range filters apply to every type (D-05)"
    - "Numbered pagination (page_window/paginate) keeps working, reflecting the filtered total (HIST-04, unchanged mechanism)"
  artifacts:
    - path: "app/routes/history.py"
      provides: "GET /history with type/product/sort/page + NEW category/customer/from/to params"
      contains: "def history_page"
    - path: "app/templates/partials/history_rows.html"
      provides: "top filter-bar (Тип + Сортировать по + period_filter.html), per-type <thead>/<tbody> narrowing"
      contains: "HISTORY_TYPE_COLUMNS"
  key_links:
    - from: "app/templates/partials/history_rows.html"
      to: "GET /history"
      via: "hx-get + hx-include=\"#history-rows input, #history-rows select\""
      pattern: "hx-target=\"#history-rows\" hx-swap=\"outerHTML\""
    - from: "app/routes/history.py"
      to: "app/services/operations.py::history_view"
      via: "customer=, category=, start_iso=, end_iso= kwargs"
      pattern: "history_view\\(session"
---

<objective>
Wire Plan 02's extended `history_view` into the desktop `/history` page:
relocate the «Тип» select into a new top filter-bar (per UI-SPEC Interaction
7, "type-first" reads as the type control sitting above and outside the
per-type filters it narrows), add category/customer/date-range filter
controls, and make the `<thead>`/`<tbody>` render only the active type's
column set (server-side only — D-03 explicitly rules out client-side column
hiding). Same `/history` route and `#history-rows` swap target throughout —
no new routes, no renamed ids (ROADMAP criterion 5 regression guard).

Purpose: HIST-01/02/03/04 on desktop.
Output: `/history` supports type-first narrowed columns plus all 4 filter
dimensions, still fully paginated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-dashboard-history-rebuild/23-CONTEXT.md
@.planning/phases/23-dashboard-history-rebuild/23-UI-SPEC.md
@.planning/phases/23-dashboard-history-rebuild/23-02-SUMMARY.md

<interfaces>
From Plan 02's `app/services/operations.py`:
`history_view(session, *, type_filter=None, product_id=None, sort="", page=0,
page_size=LIST_PAGE_SIZE, customer=None, category=None, start_iso=None,
end_iso=None) -> dict` — return dict gains `"columns"` (per-type tuple or
`None`) and every row dict gains `"warehouse"`.
`HISTORY_TYPE_COLUMNS: dict[str, tuple[str, ...]]` (exported from the same module).

From `app/routes/history.py` (current, unmodified structure to extend):
`history_page(request, type="", product="", sort="", page=0, session=...)`
builds `context` then branches `is_hx` between `partials/history_rows.html`
and `pages/history.html`; `extra_qs` is built from a `qs_parts` dict of the
currently-active filter values.

From `app/services/customers.py::search_customers` and
`app/services/catalog.py::category_options` — already consumed inside
`history_view` itself (Plan 02); the route does NOT call these directly for
filtering, only `category_options(session)` for the dropdown-independent
"Категория" input has no dropdown (free-text, per UI-SPEC) so no extra route
import is needed beyond what Plan 02 already added service-side.

From `app/templates/partials/period_filter.html` (reused verbatim structurally,
ONE additive change — see Task 2): expects `period_action`, `period_target`,
`from_date`, `to_date`, `active_preset`, `presets` (dict of
`{key: {"from","to"}}`), `error`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: history.py route — new filters, local date-range resolution, extra_qs</name>
  <files>app/routes/history.py</files>
  <read_first>app/routes/history.py (full file), app/routes/reports.py (_resolve_period — read for the week/month preset math and the malformed/inverted-range fallback pattern; do NOT import it, see action), app/core.py (local_day_bounds_utc), app/config.py (settings.display_tz)</read_first>
  <action>
    Add query params `category: str = "", customer: str = "", from_: str =
    Query("", alias="from"), to: str = Query("", alias="to")` to
    `history_page`. Implement a LOCAL `_resolve_history_period(from_raw,
    to_raw, tz_name)` helper in this file (do not import `_resolve_period`
    from reports.py — its "blank params = today" default would silently
    restrict the unfiltered default view to today, breaking D-04). This
    local helper differs from `_resolve_period` in exactly one respect:
    when BOTH from_raw and to_raw are blank, return `from_date=None,
    to_date=None, error=None` (no date filter at all — the generic
    default view stays fully unfiltered). When either is non-blank,
    reuse `_resolve_period`'s exact parse/validate/preset-detection logic
    (malformed or inverted range -> `error` set and fall back to TODAY as
    the effective date, matching 23-UI-SPEC.md's explicit "degrades exactly
    like `_resolve_period` — fall back to today" instruction) — including
    the same `presets` dict (`today`/`week`/`month`, Monday-start-week/
    calendar-month math copied from `_resolve_period`) so the preset links
    still work. Convert non-None `from_date`/`to_date` via
    `local_day_bounds_utc` into `start_iso`/`end_iso` before calling
    `history_view`; pass `None`/`None` when there is no date filter.

    Pass `customer=customer or None, category=category or None,
    start_iso=start_iso, end_iso=end_iso` into `history_view`. Extend the
    `qs_parts` dict (extra_qs re-serialization) with `category`, `customer`
    (only when non-blank), and `from`/`to` (only when the resolved
    `from_date`/`to_date` are set — i.e. re-serialize the RESOLVED dates,
    not the raw possibly-malformed input, mirroring how `type`/`sort` are
    already re-serialized from `result["..."]`, not from the raw query
    string). Add `"columns": result["columns"]`, `"from_date":
    from_date.isoformat() if from_date else "", "to_date": to_date.isoformat()
    if to_date else "", "active_preset": ..., "presets": ..., "error": ...,
    "category": category, "customer": customer` to the template context (the
    template needs `from_date`/`to_date`/`active_preset`/`presets`/`error`
    verbatim for `period_filter.html`'s own contract).
  </action>
  <verify>
    <automated>cd "E:\dev\myorishop" &amp;&amp; uv run pytest tests/test_history.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `GET /history` with no query params returns the SAME unfiltered row count as before this plan (D-04 regression guard — no default date restriction).
    - `GET /history?from=not-a-date&to=` returns 200 with an inline `.error` message and the row set falls back to today's operations (matches `_resolve_period`'s malformed-input convention).
    - `GET /history?type=sale&category=X` and `GET /history?type=receipt&customer=Y` both return 200 (customer ignored/inert for the non-sale/return type, per D-05 — verified by Plan 02's service-level guard, this task just confirms the route passes the param through untouched).
  </acceptance_criteria>
  <done>history_page accepts and correctly resolves category/customer/date-range params; extra_qs re-serializes every active filter; existing type/product/sort/page behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: history_rows.html — top filter-bar relocation + per-type columns</name>
  <files>app/templates/partials/history_rows.html, app/templates/partials/period_filter.html</files>
  <read_first>app/templates/partials/history_rows.html (full file), app/templates/partials/period_filter.html (full file), app/templates/partials/product_rows.html (the debounced category-filter-input idiom to mirror), 23-UI-SPEC.md Interaction 7/8/9/10/11 (the authoritative layout, column table, and copy)</read_first>
  <action>
    In period_filter.html: add `hx-include="{{ period_target }} input, {{
    period_target }} select"` to the `<form>` tag AND to each of the 3
    preset `<a>` tags (alongside their existing `hx-get`/`hx-target`
    attributes). This is additive and safe for every existing consumer
    (Reports/Finance pages' `period_target` container has no other
    inputs/selects beyond the от/по fields themselves, so this is a no-op
    there); on `/history` it makes every preset click and от/по submit also
    carry the other 5 active filter values (type/sort/product/category/
    customer) instead of dropping them.

    In history_rows.html: move the existing «Тип» `<select name="type">`
    out of the header `<tr class="filter-row">` and into the top
    `.filter-bar` div (alongside «Сортировать по»), giving it
    `id="type"` and `<label for="type">Тип операции</label>` (matches the
    label style already used for «Сортировать по»; keeps ALL 9
    `OPERATION_TYPE_LABELS` options, per Pitfall 5/D-06 — do not narrow the
    dropdown to the 6 stock-affecting types). Immediately below the
    `.filter-bar` div, `{% include "partials/period_filter.html" %}` with
    `period_action="/history"`, `period_target="#history-rows"`,
    `from_date=from_date`, `to_date=to_date`, `active_preset=active_preset`,
    `presets=presets`, `error=error` (D-05: date range stays visible for
    every type, universal).

    Add a new `<input type="text" name="category" placeholder="Фильтр…"
    value="{{ category }}" hx-get="/history" hx-trigger="input changed
    delay:300ms" hx-include="#history-rows input, #history-rows select"
    hx-target="#history-rows" hx-swap="outerHTML" hx-push-url="true">` to
    the existing header-row filter-row (mirrors `product_rows.html`'s
    category-filter idiom exactly, always visible per Interaction 10). Add
    a same-shaped `<input name="customer">` («Покупатель», same debounced
    idiom), wrapped in `{% if type_filter in ("sale", "return") %}...{%
    endif %}` — hidden entirely (not disabled) for every other type, per
    Interaction 11.

    Make the `<thead>`'s two `<tr>`s (10-column header + filter-row) and
    the `<tbody>`'s row markup conditional on `columns`: when `columns` is
    `None` (no type filter, or one of the 3 audit types), render the
    EXISTING unchanged generic 10-column table body exactly as it is today.
    When `columns` is a tuple (one of the 6 STOCK_AFFECTING_TYPES), render
    a narrower table instead, columns in this exact order: Когда, Код,
    Товар (name only — no batch-line here, since expiry now has its own
    column), then one `<th>`/`<td>` per key present in `columns` in
    UI-SPEC's declared order (`expiry` -> «Срок», rendered
    `{% if r.batch and r.batch.expiry %}{{ r.batch.expiry | ru_date }}{%
    elif r.batch %}<span class="muted">без срока</span>{% else %}<span
    class="muted">—</span>{% endif %}`; `qty` -> «Кол-во», reuse the
    existing signed `+`/no-sign rendering verbatim (Pitfall 7); `price` ->
    «Цена», reuse the existing `unit_price_cents | cents` / muted-dash
    rendering verbatim; `cost` -> «Себестоимость», same reuse for
    `unit_cost_cents`; `profit` -> «Прибыль», computed inline as `{% if
    r.op.unit_price_cents is not none and r.op.unit_cost_cents is not none
    %}{{ ((r.op.unit_price_cents - r.op.unit_cost_cents) * -r.op.qty_delta)
    | cents }}{% else %}<span class="muted">—</span>{% endif %}`;
    `customer` -> «Покупатель», reuse `recent_sales.html`'s exact
    "«Розница» for None, else name+surname" rendering; `reason` -> «Причина»,
    reuse the existing writeoff-reason/correction-mode-or-note rendering
    verbatim; `warehouse` -> «Склад», `{% if r.warehouse %}{{ r.warehouse.name
    }}{% else %}<span class="muted">—</span>{% endif %}`), then a final
    «Действие» column identical to today's (Вернуть link for sale rows,
    muted dash otherwise). Keep the empty-state `<tr>`'s `colspan` correct
    for whichever column count is active (10 for the generic view; `3 +
    len(columns) + 1` for a narrowed view). `{% include "partials/pagination.html"
    %}` stays inside `#history-rows`, unchanged, after `</table>`.
  </action>
  <verify>
    <automated>cd "E:\dev\myorishop" &amp;&amp; uv run pytest tests/test_history.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `GET /history` (no filter) still renders the full 10-column generic table byte-shape-identical to before this plan (existing `test_web_history_table_has_10_columns` passes unmodified).
    - `GET /history?type=sale` renders a narrower table whose header includes «Прибыль» and «Покупатель» but NOT «Причина»; `GET /history?type=writeoff` includes «Причина» but NOT «Прибыль»/«Покупатель»/«Цена».
    - `GET /history?type=price_change` still renders the generic 10-column view (Pitfall 5 — audit type falls back, not narrowed).
    - `GET /history?type=receipt` does NOT render a «Покупатель» filter input anywhere in the page (D-05 — hidden, not disabled).
    - Clicking the «Неделя» preset while `type=sale&category=X` is active preserves `type`/`category` in the resulting request (verify via the rendered pagination/filter links carrying all params, or a route-level test asserting `extra_qs` contains all of them together).
    - `partials/period_filter.html`'s reuse on `/reports/sales`, `/reports/writeoffs`, `/finance/report` still renders and functions unchanged (spot-check one, e.g. `uv run pytest tests/test_reports.py -k sales -x`).
  </acceptance_criteria>
  <done>Desktop /history is type-first: Тип sits in the top filter-bar, columns narrow per the 6 stock-affecting types, category/date filters are universal, customer is sale/return-only, all 9 types stay selectable, pagination and #history-rows/#return-slot ids are untouched.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)

- `app/routes/history.py`: `history_page` gains `category`, `customer`, `from`/`from_`, `to` query params and a local `_resolve_history_period` helper
- `app/templates/partials/history_rows.html`: relocated Тип select, new Категория/Покупатель filter inputs, per-type `<thead>`/`<tbody>` narrowing driven by `columns`
- `app/templates/partials/period_filter.html`: additive `hx-include` on its form and 3 preset links (safe for every existing consumer)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser -> GET /history query string | Untrusted `category`, `customer`, `from`, `to` params |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-23-11 | Tampering | `from`/`to` malformed or inverted | mitigate | Local `_resolve_history_period` bounded-exception handling (never a raw 500), falls back to today only on genuine malformed input — never on blank/no-params (ASVS V5) |
| T-23-12 | Tampering | `category`/`customer` query params | mitigate | Passed through as opaque strings to Plan 02's `history_view`, which resolves them against bounded candidate lists before any `.in_()` filter — never string-interpolated (already covered by Plan 02's threat register; this plan does not weaken it) |
| T-23-13 | Information Disclosure / Tampering | reflected `category`/`customer` values echoed back into filter-row `value="..."` attributes | mitigate | Jinja autoescape only, never `\|safe`, consistent with every other filter value already echoed in this template |
</threat_model>

<verification>
- `uv run pytest tests/test_history.py -x` green.
- Manual smoke: `/history`, `/history?type=sale`, `/history?type=transfer`, `/history?type=price_change` each render without error.
</verification>

<success_criteria>
- HIST-01/02/03/04 fully satisfied on desktop: type-first column narrowing, 4 filter dimensions (product unchanged, category/customer/date new), sort unchanged, numbered pagination unchanged.
</success_criteria>

<output>
Create `.planning/phases/23-dashboard-history-rebuild/23-04-SUMMARY.md` when done.
</output>

---
phase: 23-dashboard-history-rebuild
plan: 07
type: execute
wave: 3
depends_on: ["23-03"]
files_modified:
  - app/routes/mobile_home.py
  - app/templates/mobile_pages/home.html
  - tests/test_mobile_home.py
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, DASH-05]

must_haves:
  truths:
    - "Mobile Главная (/m/) keeps its existing 10-tile navigation grid FIRST and UNTOUCHED — it is the only way to reach /m/sales, /m/receipts, etc. until Phase 24 ships a tab bar"
    - "Dashboard content (date/time, catalog countdown, 4 metric tiles, recent-operations feed) is ADDED below the nav grid, never replacing it (Pitfall 1)"
    - "Mobile dashboard shows the exact same data as desktop — same dashboard_context call, own card/2-column layout"
  artifacts:
    - path: "app/routes/mobile_home.py"
      provides: "GET /m/ calls dashboard_context (same as desktop)"
      contains: "dashboard_context"
    - path: "app/templates/mobile_pages/home.html"
      provides: "10-tile nav grid (unchanged) + dashboard content below it"
      contains: "mobile-tile-grid"
  key_links:
    - from: "app/routes/mobile_home.py"
      to: "app/services/dashboard.py::dashboard_context"
      via: "direct call, identical to app/routes/home.py"
      pattern: "dashboard_context\\(session"
    - from: "app/templates/mobile_pages/home.html"
      to: "/m/history"
      via: "«Подробнее» link per feed card"
      pattern: "/m/history\\?type="
---

<objective>
Rebuild `/m/` (currently a static 13-line stub with zero data) into the
same dashboard as desktop, calling the identical `dashboard_context()` from
Plan 03 — same data, mobile's own card/2-column layout (D-10). Critically,
per 23-RESEARCH.md Pitfall 1, the existing 10-tile navigation grid is the
ONLY way to reach `/m/sales`, `/m/receipts`, `/m/writeoff`, etc. — Phase
24's MOB-01 tab bar does not exist yet, so this rebuild ADDS dashboard
content below the grid, never replaces it.

Purpose: DASH-01..05 on mobile, with zero mobile-navigation regression.
Output: `/m/` shows the nav grid first (unchanged), then the dashboard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-dashboard-history-rebuild/23-CONTEXT.md
@.planning/phases/23-dashboard-history-rebuild/23-UI-SPEC.md
@.planning/phases/23-dashboard-history-rebuild/23-03-SUMMARY.md

<interfaces>
From Plan 03's `app/services/dashboard.py::dashboard_context(session,
tz_name) -> dict` — identical shape to Plan 06's desktop consumption (see
23-06-PLAN.md's interfaces block for the full field list).

From `app/templates/mobile_pages/home.html` (current, EXACT content to
preserve unchanged as the page's first element):
`<h1>MyOriShop</h1>` + `<div class="mobile-tile-grid">` with its 10 `<a
class="mobile-tile">` links (Продажа/Приход/Поиск/Списание/Корректировка/
Перемещение/История/Сроки годности/Финансы/Экспорт кассы).

From `app/static/style.css`: `.metric-grid` already has a `.mobile-shell
.metric-grid { grid-template-columns: 1fr 1fr; }` override (Phase 17) —
applies automatically, no new CSS needed.

From `app/templates/mobile_partials/history_cards.html` (Plan 05's per-type
card field narrowing — the SAME per-field Jinja to reuse for the feed
cards here, since the feed is also a per-operation-type row set).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: mobile_home.py route rebuild</name>
  <files>app/routes/mobile_home.py</files>
  <read_first>app/routes/mobile_home.py (current 13-line stub), app/routes/home.py (Plan 06's desktop version, for the exact dashboard_context call shape)</read_first>
  <action>
    Replace the entire file: add `Depends(get_session)` (currently absent
    — this route has never touched the database) and call
    `dashboard_context(session, settings.display_tz)`, passing the result
    into `mobile_pages/home.html`. Same call as `app/routes/home.py` — no
    mobile-specific service function, per D-10 ("same data").
  </action>
  <verify>
    <automated>cd "E:\dev\myorishop" &amp;&amp; uv run pytest tests/test_mobile_home.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `GET /m/` returns 200 and the template context contains every one of `dashboard_context`'s top-level keys — byte-identical composition to `app/routes/home.py`'s Task 1.
  </acceptance_criteria>
  <done>mobile_home.py calls the same dashboard_context as desktop; route now has a session dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: mobile_pages/home.html — dashboard content below the untouched nav grid</name>
  <files>app/templates/mobile_pages/home.html, tests/test_mobile_home.py</files>
  <read_first>app/templates/mobile_pages/home.html (current, full file — the nav grid to preserve verbatim), app/templates/mobile_partials/history_cards.html (per-type card field narrowing to reuse for the feed cards), app/templates/partials/dashboard_tiles.html (Plan 06's tile markup — mobile reuses the SAME `.metric-grid`/`.metric-tile` classes but its own inline markup, not a shared partial, per 23-PATTERNS.md's "no analog found / build fresh" note), 23-UI-SPEC.md Interaction 5 (the exact mobile page order and the Pitfall-1 guardrail), tests/test_mobile_home.py (existing test, EXPECTED_HREFS list — must keep passing unmodified)</read_first>
  <behavior>
    - Page order (Interaction 5, locked — must not be reordered): `<h1>MyOriShop</h1>`, the existing 10-tile `.mobile-tile-grid` UNCHANGED first, then `<h2>Показатели</h2>`, date/weekday/time line, catalog line (same empty/closed/open states as desktop), a `.metric-grid` (2-column via the existing `.mobile-shell` CSS override, no new CSS), `<h2>Последние операции</h2>`, one `.mobile-card` per feed row.
    - Existing `test_mobile_home_renders_all_tiles_in_order` must keep passing byte-for-byte unmodified (the 8 checked hrefs' relative order and the absence of `<nav>` are both untouched).
  </behavior>
  <action>
    Extend app/templates/mobile_pages/home.html: keep `<h1>MyOriShop</h1>`
    and the entire `.mobile-tile-grid` block EXACTLY as they are today, at
    the top, untouched. Below it, add: `<h2>Показатели</h2>`; the same
    date/weekday/time `<p class="muted">` line as desktop; the same catalog
    line (empty-state link / closed / countdown / number-only) as desktop
    Task 2 — copy the exact same 4-branch Jinja verbatim (both templates
    render identical copy per 23-UI-SPEC.md, only the surrounding layout
    differs); a `<div class="metric-grid">` with the same 4 tiles as
    desktop's `dashboard_tiles.html` (Сегодня/Неделя/Месяц/Склад, identical
    field references and copy — inline here rather than via
    `{% include %}`, since this is mobile's "own layout" per D-10, not a
    shared partial); `<h2>Последние операции</h2>`; one `.mobile-card` per
    `feed` entry, fields narrowed the SAME way as Plan 05's
    `history_cards.html` per-type narrowing would apply if this row set
    were type-filtered — but the feed is NOT type-filtered (it mixes all 6
    types), so render it the same way desktop's Task-2 feed table does:
    every field present for that op's type, muted dash otherwise (mirror
    the per-type branches from 23-UI-SPEC.md Interaction 4's column list,
    restacked as card lines instead of table cells). Each card ends with a
    «Подробнее» link to `/m/history?type={{ r.op.type }}&amp;product={{
    r.product.id }}`. Empty state «Операций пока нет.» when `not feed`.
  </action>
  <verify>
    <automated>cd "E:\dev\myorishop" &amp;&amp; uv run pytest tests/test_mobile_home.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `test_mobile_home_renders_all_tiles_in_order` (existing test) passes unmodified — all 8 nav hrefs still present, in order, before any dashboard content, and `<nav>` still absent from the page.
    - New test: the nav grid's 10th tile («Экспорт кассы») textually precedes the «Показатели» heading in the response body (structural regression guard for Pitfall 1 — dashboard content is provably BELOW the grid, never replacing it).
    - New test: an empty-catalog state renders the same «Указать номер и дату закрытия» link as desktop, and the metric tiles still render.
    - New test: a feed card for a sale shows «Прибыль»/customer info; a feed card for a receipt does not.
  </acceptance_criteria>
  <done>Mobile Главная keeps 100% of its existing navigation surface, with the full DASH-01..05 dashboard appended below it in mobile's own card/2-column layout.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 07)

- `app/routes/mobile_home.py` — rebuilt, calls `dashboard_context` (same as desktop), now has a session dependency
- `app/templates/mobile_pages/home.html` — nav grid preserved unchanged + dashboard content (date/time, catalog line, metric tiles, feed cards) appended below it
- `tests/test_mobile_home.py` — extended with dashboard-content + Pitfall-1 nav-grid-order regression tests
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new — 100% read-only rendering of already-validated `dashboard_context` output | No new untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-23-18 | Information Disclosure / Tampering | `catalog.number` / feed product names rendered on `/m/` | mitigate | Jinja autoescape only, never `\|safe` — same discipline as Plan 06 |
| T-23-19 | Availability (navigation regression) | Replacing the 10-tile nav grid instead of supplementing it | mitigate | Structural regression test asserts the nav grid's last tile textually precedes the new «Показатели» heading — a future edit that reorders/removes the grid fails this test immediately |
</threat_model>

<verification>
- `uv run pytest tests/test_mobile_home.py -x` green.
- Manual smoke: `GET /m/` shows all 10 nav tiles first, dashboard content below, no console/template errors.
</verification>

<success_criteria>
- DASH-01..05 fully satisfied on mobile, with zero regression to the existing mobile navigation surface (Pitfall 1 closed).
</success_criteria>

<output>
Create `.planning/phases/23-dashboard-history-rebuild/23-07-SUMMARY.md` when done.
</output>

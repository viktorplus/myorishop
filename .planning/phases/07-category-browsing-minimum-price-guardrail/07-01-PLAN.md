---
phase: 07-category-browsing-minimum-price-guardrail
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/catalog.py
  - app/routes/categories.py
  - app/templates/pages/categories.html
  - app/templates/base.html
  - app/main.py
  - tests/test_catalog.py
autonomous: true
requirements: [CAT-01]

must_haves:
  truths:
    - "Operator can open /categories and see all active products grouped under their category heading, alphabetically ordered"
    - "Products with no category (NULL or empty string) appear in a visible 'Без категории' group, sorted last, never hidden (D-04)"
    - "A soft-deleted product never appears on /categories"
    - "Each product row links to its edit page at /products/{id}/edit"
    - "The nav bar exposes a 'Категории' link to /categories from every page"
  artifacts:
    - path: "app/services/catalog.py"
      provides: "products_by_category() query function grouping active products by category"
      contains: "def products_by_category"
    - path: "app/routes/categories.py"
      provides: "GET /categories thin route"
      contains: "@router.get(\"/categories\")"
    - path: "app/templates/pages/categories.html"
      provides: "category-grouped product listing page"
      contains: "Товары на складе"
  key_links:
    - from: "app/routes/categories.py"
      to: "app/services/catalog.py"
      via: "products_by_category(session) call feeding the page context"
      pattern: "products_by_category\\("
    - from: "app/templates/base.html"
      to: "app/routes/categories.py"
      via: "nav link href=\"/categories\""
      pattern: "href=\"/categories\""
    - from: "app/main.py"
      to: "app/routes/categories.py"
      via: "app.include_router(categories.router)"
      pattern: "categories\\.router"
---

<objective>
Deliver CAT-01: a new read-only "Товары на складе" page at /categories where the operator sees all active products grouped by category, alphabetically ordered, with a visible "Без категории" group sorted last. This is the first of two independent, additive capabilities in Phase 7 (the other, PRICE-01, is delivered by plans 07-02/07-03) — it has no schema dependency and touches only the existing Product read path.

Purpose: give the operator a fast way to browse the whole catalog by category without hunting through the flat /products search list — a pure browsing view, not a new CRUD surface (D-01).
Output: app/services/catalog.py::products_by_category(), a new GET /categories route, a new categories.html page template, one new nav link, and app.main router registration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-category-browsing-minimum-price-guardrail/07-CONTEXT.md
@.planning/phases/07-category-browsing-minimum-price-guardrail/07-RESEARCH.md
@.planning/phases/07-category-browsing-minimum-price-guardrail/07-PATTERNS.md
@.planning/phases/07-category-browsing-minimum-price-guardrail/07-UI-SPEC.md
@.planning/phases/07-category-browsing-minimum-price-guardrail/07-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: products_by_category() service query</name>
  <files>app/services/catalog.py, tests/test_catalog.py</files>
  <read_first>
    E:\dev\myorishop\app\services\catalog.py (category_options at the end of the file — direct analog to extend; list_products for the active-only filter convention)
    E:\dev\myorishop\tests\test_catalog.py (test_category_options_distinct_active_only for the exact fixture/assertion shape to mirror)
    E:\dev\myorishop\.planning\phases\07-category-browsing-minimum-price-guardrail\07-PATTERNS.md (section "app/services/catalog.py — products_by_category()")
  </read_first>
  <behavior>
    - products_by_category(session) returns a list of dicts shaped {"label": str, "products": list[Product]}.
    - Groups for named categories are sorted alphabetically by category name.
    - A group labeled "Без категории" (covering products whose category is NULL or "") is ALWAYS the last element of the returned list when any such products exist — never hidden, never sorted among the named groups (D-03/D-04).
    - Soft-deleted products (deleted_at IS NOT NULL) never appear in any group.
    - Calling it against a catalog with zero active products returns [].
  </behavior>
  <action>
    In app/services/catalog.py, add a new function `products_by_category(session: Session) -> list[dict]` after `category_options()` at the end of the file. Query all `Product` rows where `Product.deleted_at.is_(None)`, ordered by `Product.name_lc` (mirrors `list_products`'s active-only filter and the existing Cyrillic-safe name ordering convention — never `Product.name` for display ordering where `name_lc` is available). Materialize the results into a plain dict keyed by `p.category or ""` (treats NULL and empty-string category identically), appending each product to its bucket's list in query order. Compute `named = sorted(k for k in by_category if k)`. Build `groups = [{"label": k, "products": by_category[k]} for k in named]`. If `""` is a key in `by_category`, append one final `{"label": "Без категории", "products": by_category[""]}` entry after the named groups — this explicit-append ordering (not a SQL NULL-ordering trick) guarantees the uncategorized bucket sorts last regardless of dict iteration order, per RESEARCH.md's Open Question #2 recommendation (Python-side grouping, not the unverified SQL `ORDER BY .is_(None)` trick). Return `groups`.

    Add tests to tests/test_catalog.py (add `products_by_category` to the existing `from app.services.catalog import (...)` block): `test_products_by_category_groups_alphabetically_uncategorized_last` (create products across 2-3 named categories via `create_product`, plus two products created with `category=""` — both normalize to NULL on the model via `create_product`'s own `category or None` — assert the returned group labels equal the named categories in alphabetical order followed by exactly one `"Без категории"` entry last, and that both uncategorized products appear together in that one bucket), `test_products_by_category_excludes_deleted` (create two products, soft-delete one via `soft_delete_product`, assert the deleted product's id is absent from every group's product list), `test_products_by_category_empty_catalog_returns_empty_list` (no products created at all -> `products_by_category(session) == []`).
  </action>
  <acceptance_criteria>
    - `app/services/catalog.py` defines `products_by_category(session: Session) -> list[dict]`
    - `grep -c "def products_by_category" app/services/catalog.py` returns 1
    - `uv run pytest tests/test_catalog.py -k products_by_category -x` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_catalog.py -k products_by_category -x</automated>
  </verify>
  <done>products_by_category() returns active products grouped by category, alphabetically sorted, with a "Без категории" bucket always last and never hidden; soft-deleted products are excluded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /categories route, page template, nav link</name>
  <files>app/routes/categories.py, app/templates/pages/categories.html, app/templates/base.html, app/main.py, tests/test_catalog.py</files>
  <read_first>
    E:\dev\myorishop\app\routes\reports.py (reports_stock_page — the exact thin-route shape to mirror: plain full-page GET, no HX-Request branching)
    E:\dev\myorishop\app\templates\pages\reports_stock.html (page structure precedent: extends base.html, h1, table-per-section)
    E:\dev\myorishop\app\templates\partials\product_rows.html (exact empty-state markup and null-money-cell "muted em-dash" pattern to reuse verbatim)
    E:\dev\myorishop\app\templates\base.html (nav <a> list and active-class pattern)
    E:\dev\myorishop\app\main.py (router import tuple and include_router call order)
    E:\dev\myorishop\.planning\phases\07-category-browsing-minimum-price-guardrail\07-UI-SPEC.md (Screens & Components row "Товары на складе"; Copywriting Contract rows for nav entry, page title, group headings, empty state, row action link)
  </read_first>
  <behavior>
    - GET /categories returns 200 and renders h1 "Товары на складе".
    - The response contains one h2 per category group, in the exact order returned by products_by_category(session), including "Без категории" last when uncategorized products exist.
    - Each product row shows an "Изменить" link to /products/{id}/edit.
    - A soft-deleted product's name never appears in the response body.
    - When there are zero active products, the response shows the "Товаров пока нет" empty-state block (heading + body text + "Добавить товар" button to /products/new) instead of any table.
    - Every page's nav bar includes a "Категории" link to /categories, marked active (class="active") when the current request path starts with /categories.
  </behavior>
  <action>
    Create app/routes/categories.py: an `APIRouter()` with one route, `GET /categories`, function `categories_page(request: Request, session: Session = Depends(get_session))`, building `context = {"groups": products_by_category(session)}` and returning `templates.TemplateResponse(request, "pages/categories.html", context)` — mirrors `reports_stock_page`'s shape exactly: a plain full-page GET with no `HX-Request` header branching (D-01/UI-SPEC: this page has no search/filter this phase, unlike the period-filtered reports routes). Import `templates` from `app.routes`, `get_session` from `app.db`, `products_by_category` from `app.services.catalog`.

    Create app/templates/pages/categories.html, `{% extends "base.html" %}` / `{% block content %}`. Render `<h1>Товары на складе</h1>` (verbatim CAT-01 title). When `groups` is empty (falsy), render the empty state exactly matching `partials/product_rows.html`'s existing zero-products markup: a `div.empty-state` containing `<h2>Товаров пока нет</h2>`, `<p class="muted">Добавьте первый товар, чтобы начать вести каталог.</p>`, and `<p><a class="button" href="/products/new">Добавить товар</a></p>`. When `groups` is non-empty, loop over `groups` and for each group render an `<h2>{{ group.label }}</h2>` followed by a `<table>` with header row Код / Название / Остаток / Закупочная / Продажа / Каталог / Действия (D-02 — no separate Категория column, redundant under a group heading) and one body `<tr>` per product in `group.products`: `<td>{{ product.code or "" }}</td>`, `<td>{{ product.name }}</td>` (autoescape only — product names are untrusted stored input, never `|safe`, same convention as `sale_oversell.html`), `<td class="num">{{ product.quantity }}</td>`, then three `class="num"` money cells for `cost_cents`/`sale_cents`/`catalog_cents` each rendered via the `cents` filter when `is not none`, else `<span class="muted">—</span>` (mirrors `partials/product_rows.html`'s exact null-money-cell pattern), and a final `<td><a href="/products/{{ product.id }}/edit">Изменить</a></td>`.

    In app/templates/base.html, add one new nav `<a>` immediately after the existing `/products` link and before the `/receipts/new` link: `href="/categories"`, `class="active"` applied when `request.url.path.startswith("/categories")`, label «Категории» — matches UI-SPEC's nav order (Главная · Товары · Категории · Приход · ...).

    In app/main.py, add `categories` to the `from app.routes import (...)` tuple, alphabetically between `backup` and `corrections`. Add `app.include_router(categories.router)` immediately after `app.include_router(products.router)`.

    Add tests to tests/test_catalog.py: `test_web_categories_page_lists_groups_with_edit_link` (create 2-3 products spanning named categories plus one uncategorized via the `client`/`session` fixtures, GET /categories, assert 200, assert "Товары на складе" and every category label including "Без категории" appear in the response in the correct order, assert an `/products/{id}/edit` link is present for a known product id), `test_web_categories_page_hides_deleted_products` (soft-delete a product, assert its name is absent from the response), `test_web_categories_page_empty_state` (no products created, assert "Товаров пока нет" is shown and no `<table>` is present), `test_web_nav_has_categories_link` (GET any existing page via `client`, assert `href="/categories"` and "Категории" appear in the nav).
  </action>
  <acceptance_criteria>
    - `app/routes/categories.py` defines a `GET /categories` route
    - `app/templates/pages/categories.html` contains "Товары на складе" and "Изменить"
    - `app/templates/base.html` contains `href="/categories"`
    - `app/main.py` imports `categories` and calls `app.include_router(categories.router)`
    - `uv run pytest tests/test_catalog.py -k "web_categories or web_nav_has_categories_link" -x` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_catalog.py -k "web_categories or web_nav_has_categories_link" -x</automated>
  </verify>
  <done>/categories renders all active products grouped by category with "Без категории" last and never hidden, soft-deleted products excluded, every row links to its edit page, the empty state shows when the catalog is empty, and the nav exposes the new page from every screen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser -> GET /categories | Read-only page; no user-supplied input this phase (no search/filter per CAT-01 scope) beyond the implicit session-less request itself |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|------------------|
| T-07-01 | Tampering / Information Disclosure | `categories.html` rendering `product.name` | mitigate | Jinja2 autoescape only, never `\|safe` on `product.name` — matches the existing `sale_oversell.html` convention (T-4-01); product names are untrusted stored input and this is a NEW template rendering that same field |
| T-07-02 | Elevation of Privilege | `GET /categories` unauthenticated | accept | No auth exists anywhere in this v1 single-local-operator app (CLAUDE.md/PROJECT.md decision) — this phase introduces no new risk, every existing route (including `/products`, `/reports/stock`) is equally unauthenticated |
</threat_model>

<verification>
`uv run pytest tests/test_catalog.py -x -q` — full file green, including the four new `products_by_category`/`web_categories`/`web_nav_has_categories_link` tests and the pre-existing suite unaffected. `uv run ruff check app/services/catalog.py app/routes/categories.py app/templates/base.html app/main.py` — no lint errors.
</verification>

<success_criteria>
Operator can open /categories and see every active product grouped under its category heading, alphabetically ordered, with uncategorized products visible under "Без категории" sorted last; soft-deleted products never appear; every row links to its edit page; the nav bar exposes the new page from anywhere in the app. This satisfies CAT-01 and ROADMAP Phase 7 success criterion 1 in full — independent of plans 07-02/07-03 (PRICE-01).
</success_criteria>

<output>
Create `.planning/phases/07-category-browsing-minimum-price-guardrail/07-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `app.services.catalog.products_by_category(session) -> list[dict]` — new function, groups shaped `{"label": str, "products": list[Product]}`
- `app/routes/categories.py` — new router module, `GET /categories`
- `app/templates/pages/categories.html` — new page template
- `base.html` nav link `href="/categories"` labeled «Категории»
- `app.main` import of `categories` + `app.include_router(categories.router)`

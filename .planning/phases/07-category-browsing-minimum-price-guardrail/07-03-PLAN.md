---
phase: 07-category-browsing-minimum-price-guardrail
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - app/services/sales.py
  - app/routes/sales.py
  - app/templates/partials/sale_price_warning.html
  - app/templates/partials/sale_form.html
  - tests/test_sales.py
autonomous: true
requirements: [PRICE-01]

must_haves:
  truths:
    - "Selling a basket line below its product's minimum sale price shows a warning and records zero writes until the operator confirms"
    - "A price exactly equal to the minimum passes silently (strict less-than boundary, D-10)"
    - "A product with no minimum price configured never triggers the warning, even at an entered price of 0 (D-06/success criterion 4)"
    - "A basket that trips both the oversell check and the price-floor check shows both warnings stacked in one response, resolved by a single 'Продать всё равно' confirm (D-11)"
  artifacts:
    - path: "app/services/sales.py"
      provides: "below_minimum per-line price-floor check inside register_sale, sharing the confirm=1 gate with the existing oversell check"
      contains: "below_minimum"
    - path: "app/templates/partials/sale_price_warning.html"
      provides: "price-floor warning partial mirroring sale_oversell.html"
      contains: "sale-price-warning"
  key_links:
    - from: "app/templates/partials/sale_form.html"
      to: "app/templates/partials/sale_price_warning.html"
      via: "{% if below_minimum %} include"
      pattern: "below_minimum"
    - from: "app/routes/sales.py"
      to: "app/services/sales.py"
      via: "sale_create reads result.get(\"below_minimum\") from register_sale's return"
      pattern: "below_minimum"
---

<objective>
Deliver the sale-time enforcement half of PRICE-01: extend `register_sale`'s existing warn-but-allow oversell check with a parallel per-line minimum-price check, sharing the exact same `confirm=1` gate and zero-writes-until-confirmed contract, and wire it through the route and a new warning partial so a basket tripping both checks shows both warnings stacked in one response. Depends on Plan 07-02, which adds `Product.min_sale_cents`.

Purpose: this is the observable guardrail behavior ROADMAP Phase 7 success criteria 3 and 4 require — selling below a configured minimum must warn but not silently block, mirroring the oversell pattern the operator already knows.
Output: `register_sale`'s new `below_minimum` check, `app/routes/sales.py`'s widened result guard, a new `sale_price_warning.html` partial, and its inclusion in `sale_form.html`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-category-browsing-minimum-price-guardrail/07-CONTEXT.md
@.planning/phases/07-category-browsing-minimum-price-guardrail/07-RESEARCH.md
@.planning/phases/07-category-browsing-minimum-price-guardrail/07-PATTERNS.md
@.planning/phases/07-category-browsing-minimum-price-guardrail/07-UI-SPEC.md
@.planning/phases/07-category-browsing-minimum-price-guardrail/07-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: register_sale() per-line price-floor check</name>
  <files>app/services/sales.py, tests/test_sales.py</files>
  <read_first>
    E:\dev\myorishop\app\services\sales.py (register_sale — the existing oversell block this task extends, lines building `oversold` and its early return)
    E:\dev\myorishop\tests\test_sales.py (test_oversell_blocks_without_confirm / test_oversell_confirm_writes_negative_stock / test_oversell_aggregates_duplicate_lines — exact fixture/assertion shape to mirror)
    E:\dev\myorishop\.planning\phases\07-category-browsing-minimum-price-guardrail\07-RESEARCH.md (Pattern 2 "Warn-but-allow guardrail" and Pitfall 2 "Short-circuiting on the first failed check")
    E:\dev\myorishop\.planning\phases\07-category-browsing-minimum-price-guardrail\07-CONTEXT.md (D-08 through D-11)
  </read_first>
  <behavior>
    - `register_sale` with a line whose entered price is strictly below `product.min_sale_cents`, `confirm` not `"1"` -> returns `(result, {})` where `result["below_minimum"]` is a non-empty list and zero sale operations are written.
    - The same call with `confirm="1"` -> writes the sale operation at the entered (below-minimum) price; `result` has no truthy `"below_minimum"` key.
    - A line whose entered price exactly equals `product.min_sale_cents` never appears in `below_minimum` (strict less-than, D-10).
    - A product whose `min_sale_cents` is `None` never triggers `below_minimum` at any entered price, including `0` (D-06/success criterion 4).
    - A basket that trips BOTH the existing oversell check AND the new below-minimum check, with `confirm` not `"1"`, returns a SINGLE result dict containing BOTH `"oversell"` and `"below_minimum"` keys in the same call (Pitfall 2 — neither check may short-circuit the other).
    - The below-minimum check is per-LINE (D-09): it is not aggregated/summed like the oversell quantity check.
  </behavior>
  <action>
    In app/services/sales.py, inside `register_sale`'s existing `if confirm != "1":` block, after the existing `oversold` list comprehension, add a new `below_minimum` list comprehension: for each `line` in `resolved`, include `{"product": line["product"], "entered": line["price_cents"], "minimum": line["product"].min_sale_cents}` when `line["product"].min_sale_cents is not None and line["price_cents"] < line["product"].min_sale_cents` (D-09: per-line, NOT aggregated like `requested_by_product`; D-10: strict `<`; D-06: `is not None` guard, never a bare `or`).

    Replace the existing `if oversold: oversold.sort(...); return {"oversell": oversold}, {}` block with: `if oversold or below_minimum:` — inside, build `result: dict = {}`; when `oversold` is truthy, sort it (`key=lambda entry: entry["product"].name`, unchanged) and set `result["oversell"] = oversold`; when `below_minimum` is truthy, sort it the same way and set `result["below_minimum"] = below_minimum`; then `return result, {}`. This is Pitfall 2's fix — both checks are computed before any return, so a basket tripping both surfaces both warnings in the SAME round trip instead of the price-floor check being silently skipped.

    Add tests to tests/test_sales.py, positioned after the existing oversell test group (after `test_oversell_aggregates_duplicate_lines`): `test_below_minimum_blocks_without_confirm` (set `stocked_product.min_sale_cents` via direct attribute assignment + `session.commit()`, sell at a lower price with no confirm, assert `result.get("below_minimum")` truthy and `_sale_ops(session) == []`), `test_below_minimum_confirm_writes` (same setup with `confirm="1"`, assert the written sale op's `unit_price_cents` equals the entered below-minimum price and `result.get("below_minimum")` is falsy), `test_below_minimum_boundary_equal_price_passes_silently` (entered price exactly equals `min_sale_cents`, assert `result.get("below_minimum")` falsy and exactly one sale op is written), `test_min_sale_unset_never_warns_even_at_zero_entered_price` (leave `stocked_product.min_sale_cents` at its default `None`, sell at price `"0,00"`, assert `result.get("below_minimum")` falsy — success criterion 4), `test_oversell_and_below_minimum_both_reported_together` (set `min_sale_cents` AND request more than the available stock in one line, assert `result` contains BOTH truthy `"oversell"` and `"below_minimum"` keys with `_sale_ops(session) == []` — the highest-value test in this plan, per CONTEXT D-11 and Pitfall 2).
  </action>
  <acceptance_criteria>
    - `app/services/sales.py`'s `register_sale` contains a `below_minimum` list comprehension guarded by `is not None`
    - `grep -v '^#' app/services/sales.py | grep -c below_minimum` returns at least 3 (the comprehension, the sort call, the result key assignment)
    - `uv run pytest tests/test_sales.py -k "below_minimum or both_reported" -x` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_sales.py -k "below_minimum or both_reported" -x</automated>
  </verify>
  <done>register_sale enforces the per-line minimum-price guardrail with the exact same confirm=1/zero-writes contract as the oversell check, and a basket tripping both checks surfaces both warnings in one response.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route wiring, warning partial, sale_form include</name>
  <files>app/routes/sales.py, app/templates/partials/sale_price_warning.html, app/templates/partials/sale_form.html, tests/test_sales.py</files>
  <read_first>
    E:\dev\myorishop\app\routes\sales.py (sale_create — the `if result and result.get("oversell"):` guard this task widens, and the context dict it builds)
    E:\dev\myorishop\app\templates\partials\sale_oversell.html (exact structure to mirror: error-block wrapper, heading, per-line paragraph, two-button form-actions block, confirm=1 hx-vals)
    E:\dev\myorishop\app\templates\partials\sale_form.html (existing `{% if oversell %}` include block — insertion point)
    E:\dev\myorishop\tests\test_sales.py (test_web_sale_oversell_shows_warning_and_confirm_writes — exact web-test shape to mirror)
  </read_first>
  <behavior>
    - `POST /sales` with a below-minimum line and no confirm returns 200 with the basket form intact, showing a "Цена ниже минимальной" warning block listing the offending line(s); the "Продать всё равно" button re-POSTs the SAME basket with `confirm=1` via form association (no new confirm parameter, D-11).
    - Re-POSTing with `confirm=1` completes the sale and shows the existing "Продажа оформлена:" success line.
    - A basket tripping both the oversell AND price-floor checks shows BOTH "Товара не хватает на складе" and "Цена ниже минимальной" blocks stacked in the same response, and a single `confirm=1` re-POST resolves both.
    - Product names rendered in the new warning block are autoescaped, never `\|safe` (same convention as `sale_oversell.html`).
  </behavior>
  <action>
    In app/routes/sales.py's `sale_create` route, change the condition `if result and result.get("oversell"):` to `if result and (result.get("oversell") or result.get("below_minimum")):` — this is the critical wiring fix: without it, a basket that trips ONLY `below_minimum` (no oversell) falls through this guard entirely and reaches the success-write branch below, silently defeating the warn-but-allow contract. Inside that branch's context dict, replace the single `"oversell": result["oversell"]` line with two lines: `"oversell": result.get("oversell")` and `"below_minimum": result.get("below_minimum")` — both keys are always present in the context (falsy/`None` when absent), so the template's `{% if %}` guards behave correctly either way.

    Create app/templates/partials/sale_price_warning.html, mirroring `partials/sale_oversell.html`'s full structure: a `div.error-block` with `id="sale-price-warning"`, a `<p><strong>Цена ниже минимальной</strong></p>` heading, one `<p>` per entry in `below_minimum` reading `{{ e.product.name }}: цена {{ e.entered | cents }}, минимальная {{ e.minimum | cents }}.` (autoescape only — product names are untrusted stored input, never `\|safe`, same convention as `sale_oversell.html`), and the identical two-button form-actions block: a submit `button.danger` with `form="sale-form"`, `hx-post="/sales"`, `hx-vals='{"confirm": "1"}'`, `hx-target="#sale-form-wrap"`, `hx-swap="outerHTML"`, `hx-disabled-elt="this"`, labeled "Продать всё равно" (the SAME `confirm=1` flag as oversell — D-11, no new confirm parameter), and a secondary dismiss `button` that removes `#sale-price-warning` via `hx-on:click`, labeled "Вернуться к корзине".

    In app/templates/partials/sale_form.html, add `{% if below_minimum %}{% include "partials/sale_price_warning.html" %}{% endif %}` immediately after the existing `{% if oversell %}...{% endif %}` block (established-pattern-first ordering per UI-SPEC: oversell first, price-floor second) — both blocks render together in the same response when both keys are present.

    Add tests to tests/test_sales.py: `test_web_sale_below_minimum_shows_warning_and_confirm_writes` (mirrors `test_web_sale_oversell_shows_warning_and_confirm_writes`: set `stocked_product.min_sale_cents`, POST with a below-minimum price and no confirm, assert "Цена ниже минимальной" and "Продать всё равно" in the 200 response; re-POST with `confirm="1"`, assert "Продажа оформлена:"), `test_web_sale_both_warnings_stack_and_single_confirm_resolves_both` (POST a basket that both oversells AND is below minimum, assert BOTH "Товара не хватает на складе" and "Цена ниже минимальной" appear in the same response; re-POST with `confirm="1"`, assert "Продажа оформлена:" — this is the CONTEXT D-11 manual UAT gate made automated).
  </action>
  <acceptance_criteria>
    - `app/routes/sales.py`'s `sale_create` contains `result.get("below_minimum")` in its guard condition
    - `app/templates/partials/sale_price_warning.html` exists and contains `sale-price-warning` and `confirm": "1"`
    - `app/templates/partials/sale_form.html` contains `{% if below_minimum %}`
    - `uv run pytest tests/test_sales.py -k "web_sale_below_minimum or web_sale_both_warnings" -x` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_sales.py -k "web_sale_below_minimum or web_sale_both_warnings" -x</automated>
  </verify>
  <done>POST /sales surfaces the price-floor warning exactly like the oversell warning (zero writes, same confirm=1 re-POST, intact basket), and a basket tripping both checks shows both blocks stacked, resolved by one confirm click.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser -> POST /sales | Untrusted form input: `price[]` array, `confirm` bypass flag |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|------------------|
| T-07-06 | Tampering | crafted `confirm=1` bypassing the price-floor warning | accept | Not a new risk — `confirm` is a server-validated flag re-checked against fresh DB state on the SAME request; the server recomputes `below_minimum` from the submitted basket + current `Product.min_sale_cents`, never trusts a client-supplied warning state. Identical mechanism to the already-shipped oversell check. |
| T-07-07 | Repudiation / Logic error | short-circuit dropping the price-floor check when oversell also fires | mitigate | Both `oversold` and `below_minimum` are computed BEFORE any early return (Pitfall 2), and `app/routes/sales.py`'s guard checks `result.get("oversell") or result.get("below_minimum")` so neither warning can be silently swallowed by the other |
| T-07-08 | Information Disclosure | `sale_price_warning.html` rendering `product.name` | mitigate | Jinja2 autoescape only, never `\|safe` — same T-4-01 convention as `sale_oversell.html` |
</threat_model>

<verification>
`uv run pytest tests/test_sales.py -x -q` — full file green, including all new `below_minimum`/`web_sale_below_minimum`/`web_sale_both_warnings` tests and the pre-existing oversell suite unaffected. `uv run ruff check app/services/sales.py app/routes/sales.py app/templates/partials/sale_price_warning.html app/templates/partials/sale_form.html` — no lint errors. Full-suite gate before phase close: `uv run pytest`.
</verification>

<success_criteria>
A basket line priced below its product's minimum sale price is blocked with a warn-but-allow guardrail identical in mechanism to the existing oversell check (same confirm=1 flag, same zero-writes contract, same visual treatment), and both checks can fire together in one response. This satisfies PRICE-01 in full and ROADMAP Phase 7 success criteria 3 and 4.
</success_criteria>

<output>
Create `.planning/phases/07-category-browsing-minimum-price-guardrail/07-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `app.services.sales.register_sale`'s `below_minimum` check — new list comprehension + `result["below_minimum"]` key
- `app/templates/partials/sale_price_warning.html` — new partial
- `sale_form.html`'s `{% if below_minimum %}` include block
- `app/routes/sales.py`'s widened `result.get("oversell") or result.get("below_minimum")` guard in `sale_create`

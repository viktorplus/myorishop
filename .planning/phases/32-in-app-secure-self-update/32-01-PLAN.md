---
phase: 32-in-app-secure-self-update
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_update.py
  - tests/test_launcher.py
autonomous: true
requirements: [UPD-01, UPD-02, UPD-03, UPD-04, UPD-05, UPD-06, UPD-07]
must_haves:
  truths:
    - "tests/test_update.py collects green (RED-by-design: imports the update service INSIDE test bodies)"
    - "Every UPD-01..07 behaviour has a named, executable RED test that later waves flip GREEN"
    - "The real-minisign round-trip test skip-gates on the minisign binary (mirrors test_release_verify.py), so CI runs it and the dev box skips"
    - "tests/test_launcher.py gains the app-writes-marker -> launcher-applies integration + the /health version-match health contract"
  artifacts:
    - path: "tests/test_update.py"
      provides: "RED Nyquist scaffold for UPD-01..07 + fixtures (fake GitHub JSON, throwaway keypair, synthetic zip+manifest+.minisig)"
      min_lines: 120
      contains: "def test_anti_downgrade"
    - path: "tests/test_launcher.py"
      provides: "app-writes-marker -> launcher run_once integration + test_apply_rolls_back + /health version-match contract"
      contains: "expected_version"
  key_links:
    - from: "tests/test_update.py"
      to: "app.services.update"
      via: "in-body import inside each test (collection stays green while red)"
      pattern: "import app.services.update|from app.services import update"
    - from: "tests/test_update.py"
      to: "app.services.minisign_verify"
      via: "in-body import for the verify-gate tests"
      pattern: "minisign_verify"
---

<objective>
Wave 0 of Phase 32: lay the executable RED test contract for the in-app secure
self-update BEFORE any implementation, following the project convention
(phases 30/31 Wave-0 RED scaffold, MEMORY `execute-phase-sequential-mode`).

Every UPD-01..07 behaviour gets a named test in `tests/test_update.py` (plus the
UPD-04 matched-pair rollback in `tests/test_launcher.py`). The update service and
the minisign verifier do not exist yet, so — exactly like `tests/test_release_verify.py`
and `tests/test_packaging.py` — the service is imported INSIDE each test body and
collection stays green while execution is RED. Later waves (02-05) flip each test
GREEN.

Purpose: pin the contract (function names, signatures, fixture shapes) so the
implementation waves have an unambiguous target and the security gates are
test-first, not bolted on.
Output: `tests/test_update.py` (new) + an extension to `tests/test_launcher.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-in-app-secure-self-update/32-RESEARCH.md
@.planning/phases/32-in-app-secure-self-update/32-VALIDATION.md
@tests/test_release_verify.py
@tests/test_launcher.py
</context>

<artifacts>
## Artifacts this phase produces (Plan 01 contributes)

NEW files created by this plan:
- `tests/test_update.py` — RED Nyquist scaffold: the 7 UPD tests + fixtures.

NEW test functions (contract names the later waves must satisfy):
- `tests/test_update.py::test_check_detects_newer` (UPD-01)
- `tests/test_update.py::test_server_noop` (UPD-06)
- `tests/test_update.py::test_verify_gate` (UPD-02)
- `tests/test_update.py::test_minisign_pure_python_verify` (UPD-02, skip-gated on the `minisign` binary)
- `tests/test_update.py::test_anti_downgrade` (UPD-05)
- `tests/test_update.py::test_confirm_and_defer` (UPD-03)
- `tests/test_update.py::test_manual_check` (UPD-07)
- `tests/test_launcher.py::test_apply_rolls_back` (UPD-04)
- `tests/test_launcher.py::test_run_once_applies_app_written_marker` (UPD-04 integration)
- `tests/test_launcher.py::test_health_ok_requires_version_match` (UPD-04 stronger health contract)

Symbols these tests REFERENCE (built in later waves, imported in-body):
- `app.services.update` — `check_for_update`, `fetch_latest_release`, `apply`,
  `stage_pending`, `is_strictly_newer`, `get_cached_status`, `UpdateStatus`
- `app.services.minisign_verify` — `verify_minisign`, `sha256_matches`
- `launcher.adapters.health_ok(expected_version=...)`, `launcher.__main__.run_once`
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: RED scaffold tests/test_update.py for UPD-01/02/03/05/06/07 + fixtures</name>
  <files>tests/test_update.py</files>
  <read_first>
    - tests/test_release_verify.py (the in-body-import + `shutil.which("minisign")` skip-gate + throwaway-keypair pattern to mirror exactly — file:1-157)
    - tests/conftest.py (the authenticated TestClient + engine fixtures available to reuse)
    - .planning/phases/32-in-app-secure-self-update/32-VALIDATION.md (§ Per-Task Verification Map — the exact test names + requirement mapping)
    - .planning/phases/32-in-app-secure-self-update/32-RESEARCH.md (Pattern 1 verify skeleton, Pattern 4 anti-downgrade, Code Examples GitHub JSON shape lines 277-308)
    - build_release.py:300-345 (write_manifest / verify_manifest — the manifest.txt schema `version=…\narchive=…\nsha256=…\n` the synthetic fixtures must reproduce)
  </read_first>
  <action>
    Create `tests/test_update.py` as a RED-by-design Nyquist scaffold (module docstring states it is RED until Waves 02-05, mirroring `tests/test_release_verify.py`). Import `app.services.update` and `app.services.minisign_verify` INSIDE each test body (never at module top) so collection stays green while the modules do not yet exist.

    Fixtures (pytest fixtures or module helpers):
    - `fake_release_json()` — a dict shaped like GitHub `/repos/viktorplus/myorishop/releases/latest`: `tag_name="v1.16"`, `body="release notes text"`, `assets=[{name:"manifest.txt",browser_download_url:"https://github.com/…/manifest.txt"},{name:"manifest.txt.minisig",browser_download_url:"https://objects.githubusercontent.com/…/manifest.txt.minisig"},{name:"MyOriShop-1.16.zip",browser_download_url:"https://github.com/…/MyOriShop-1.16.zip"}]`.
    - `synthetic_release(tmp_path)` — builds a real tiny onedir zip + a `manifest.txt` whose `sha256=` is the real `hashlib.sha256` of that zip (reuse the `build_release.write_manifest` schema literally), returning paths for the archive/manifest.
    - `throwaway_keypair(tmp_path)` — skip-gated on `shutil.which("minisign")`; runs `minisign -G -W` to make a tmp keypair + `minisign -S` to sign `manifest.txt`, exactly as `tests/test_release_verify.py` does; NEVER touches `app/minisign.pub`.

    Tests (each named per 32-VALIDATION.md, RED until its wave):
    - `test_check_detects_newer` (UPD-01): monkeypatch `update.fetch_latest_release` to return `fake_release_json()` and the manifest fetch/verify to yield a signature-verified `version="1.16"`; assert `update.check_for_update()` returns an `UpdateStatus` with `state=="available"`, `latest=="1.16"`, notes carried; then monkeypatch fetch to return `None` (offline) and assert `state=="offline"` and NOTHING raises (silent no-op).
    - `test_server_noop` (UPD-06): monkeypatch the dialect seam so `engine.dialect.name=="postgresql"`; assert `update.check_for_update()` returns the no-op status and never performs a network fetch (patch fetch to fail the test if called).
    - `test_verify_gate` (UPD-02): using `synthetic_release`, assert `minisign_verify.sha256_matches(archive, good_sha)` is True and flipping one archive byte makes it False; assert `update.apply(...)` ABORTS (raises / returns a failed result) and stages NOTHING when the SHA-256 mismatches OR the Ed25519 signature is invalid (bad-signature bytes).
    - `test_minisign_pure_python_verify` (UPD-02, `@pytest.mark.skipif(not minisign_available)`): sign `manifest.txt` with the throwaway key in BOTH modes if producible, then assert `minisign_verify.verify_minisign(manifest_bytes, sig_text, pubkey_text)` returns True for the valid signature and False for a tampered manifest — proving the pure-Python verify matches the real minisign binary.
    - `test_anti_downgrade` (UPD-05): assert `update.is_strictly_newer("1.16","1.15") is True`, `is_strictly_newer("1.10","1.9") is True` (the 9->10 boundary STATE.md flags), `is_strictly_newer("1.15","1.15") is False`, `is_strictly_newer("1.14","1.15") is False`; and that a `check_for_update` whose signed-manifest version is not strictly newer returns `state=="up_to_date"`.
    - `test_confirm_and_defer` (UPD-03): via the authenticated admin TestClient, `POST /settings/update/apply` returns 200 with the applying partial (release notes autoescaped, never `|safe`); `POST /settings/update/dismiss` returns 200 and the notice is gone. Assert the notes text is HTML-escaped when it contains `<b>`.
    - `test_manual_check` (UPD-07): `POST /settings/update/check` returns 200 with a `#update-panel` partial (up-to-date OR available OR offline caption) — never a 5xx even when the fetch fails.

    Do NOT implement any production code here. Do NOT put fenced production code in the plan; the tests are the deliverable.
  </action>
  <verify>
    <automated>uv run pytest tests/test_update.py --co -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_update.py --co -q` lists all 7 test functions (collection succeeds — no ImportError at module top).
    - `uv run pytest tests/test_update.py -q` runs RED (failures/errors from the missing `app.services.update` / `app.services.minisign_verify`), NOT collection errors.
    - `test_minisign_pure_python_verify` reports SKIPPED on a box without the `minisign` binary (never fails collection).
    - `grep -n "minisign_verify" tests/test_update.py` shows the verifier is imported inside test bodies, not at module scope.
  </acceptance_criteria>
  <done>tests/test_update.py exists, collects green, runs RED for UPD-01/02/03/05/06/07 with the exact function names from 32-VALIDATION.md.</done>
</task>

<task type="auto">
  <name>Task 2: Extend tests/test_launcher.py — app-marker->launcher integration, UPD-04 rollback, /health version-match</name>
  <files>tests/test_launcher.py</files>
  <read_first>
    - tests/test_launcher.py (the existing fake-callback swap/rollback tests + `_install_layout` helper to reuse — file:1-275)
    - launcher/__main__.py:50-91 (run_once — marker read + apply_update drive; the integration surface to exercise)
    - launcher/swap.py:60-153 (apply_update matched-pair rollback + parse_pending path confinement)
    - launcher/adapters.py:103-126 (health_ok current "any status alive" — the contract Wave 3 strengthens to version-match)
    - .planning/phases/32-in-app-secure-self-update/32-RESEARCH.md (Pattern 5 pending.json schema + OQ-3 health-check strength + Pitfall 6 expected_version)
  </read_first>
  <action>
    Extend `tests/test_launcher.py` (do NOT rewrite existing tests) with three RED tests, importing `app.services.update` and the extended `launcher.adapters` symbols INSIDE the bodies:

    - `test_apply_rolls_back` (UPD-04): reuse `_install_layout` + the injected fake callbacks; assert that when `migrate` raises, `apply_update` restores the original `app/` content, moves staged to `app.failed/`, invokes `backup_restore(pending.db_backup_path)` (matched pair), restarts, and re-raises — this mirrors the existing rollback tests but is the named UPD-04 anchor referenced by 32-VALIDATION.md (may delegate to / assert the same invariants as `test_apply_update_rolls_back_on_migrate_failure`).
    - `test_run_once_applies_app_written_marker` (UPD-04 integration): call `app.services.update.stage_pending(install_root, "staged", "1.16", "data/backups/b.db")` to WRITE `data/pending.json`, hand-place a fake `staged/` dir, then call `launcher.__main__.run_once(paths, fake_app_process, migrate=…, health_ok=lambda *_a, **_k: True, backup_restore=…)` and assert exactly one real `os.replace` swap ran and the marker was deleted — proving the app-writes / launcher-consumes contract end to end (the app side is RED until Wave 3 builds `stage_pending`).
    - `test_health_ok_requires_version_match` (UPD-04 stronger health): assert the extended `launcher.adapters.health_ok(expected_version="1.16")` returns True only when a stub HTTP server on 127.0.0.1:8000 answers `GET /health` with `{"version":"1.16"}`, and False when it answers `{"version":"1.15"}` (stale swap) or refuses the connection; and that `health_ok(expected_version=None)` keeps the legacy "any status = alive" behaviour (backward-compatible for the Phase 31 hand-placed-marker path). Skip or use a tiny `http.server` stub — no `app.*` import (launcher is stdlib-only).

    All three are RED until Waves 03/04 build `stage_pending` and the `health_ok(expected_version=...)` param.
  </action>
  <verify>
    <automated>uv run pytest tests/test_launcher.py --co -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_launcher.py --co -q` lists the three new tests plus all pre-existing launcher tests (collection stays green).
    - The three new tests run RED (missing `update.stage_pending` / `health_ok(expected_version=...)`), not collection errors.
    - `grep -n "expected_version" tests/test_launcher.py` shows the version-match health contract is pinned.
    - No `import app` / `from app` at module top of test_launcher.py (launcher stays stdlib-only in its own suite context; app-side symbols imported in-body).
  </acceptance_criteria>
  <done>tests/test_launcher.py carries the UPD-04 rollback anchor, the app-marker->launcher integration, and the /health version-match contract, all RED, collection green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> update service | The scaffold defines the executable contract for every trust gate the later waves implement; a missing/loose test here is a silent hole in the security gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering | verify-before-unpack gate | mitigate | `test_verify_gate` + `test_minisign_pure_python_verify` pin "bad SHA-256 OR bad Ed25519 => abort, nothing staged" as the RED contract Wave 03/04 must satisfy |
| T-32-02 | Tampering | anti-downgrade compare | mitigate | `test_anti_downgrade` pins integer compare incl. the 9->10 boundary and "not strictly newer => up_to_date" |
| T-32-05 | Tampering | zip-slip on unpack | mitigate | `test_verify_gate` asserts NOTHING is staged on a failed gate; Wave 04 adds the member-path guard the scaffold references |
| T-32-08 | Denial of Service | half-applied migration / data loss | mitigate | `test_apply_rolls_back` + `test_run_once_applies_app_written_marker` pin the pre-update backup + matched-pair rollback contract |
| T-32-10 | Denial of Service | offline startup check | mitigate | `test_check_detects_newer` pins "offline => silent no-op, never raises" |
| T-32-SC | Tampering | pip installs | accept | No package install in this plan (only test files); `cryptography` is vetted behind a blocking-human checkpoint in Plan 02 |
</threat_model>

<verification>
- `uv run pytest tests/test_update.py --co -q` and `uv run pytest tests/test_launcher.py --co -q` both collect with zero errors.
- Running each new file RED shows behavioural failures (missing service), not import/collection failures.
- The full suite is NOT expected green yet (this is Wave 0) — but no PRE-EXISTING test regresses: `uv run pytest -q` shows only the new RED update/launcher tests failing plus the 4 known pre-existing `tests/test_sync_ui.py` failures (OUT OF SCOPE, MEMORY `preexisting-sync-ui-test-failures`).
</verification>

<success_criteria>
- `tests/test_update.py` exists with the 7 named UPD tests + the three fixtures; collection green, execution RED.
- `tests/test_launcher.py` gains the UPD-04 rollback anchor, the app-marker->launcher integration, and the `/health` version-match contract.
- Every UPD-01..07 requirement has at least one executable RED test targeting it.
</success_criteria>

<output>
Create `.planning/phases/32-in-app-secure-self-update/32-01-SUMMARY.md` when done.
</output>

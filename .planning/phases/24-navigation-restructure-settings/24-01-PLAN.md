---
phase: 24-navigation-restructure-settings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/templates/base.html
  - tests/test_smoke.py
  - app/templates/partials/products_toolbar.html
  - app/templates/pages/products_list.html
  - app/static/style.css
  - tests/test_dictionary.py
  - tests/test_writeoffs.py
  - tests/test_receipts.py
  - tests/test_catalog.py
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03, NAV-08]

must_haves:
  truths:
    - "Top-level desktop nav shows exactly 8 items (Главная, Товары, Продажи, Покупатели, История, Отчёты, Финансы, Настройки) and no others (NAV-08)"
    - "Приход, Списание, Справочник are reachable as always-visible buttons on the /products page, grouped into two labeled clusters, never behind a click (D-01, D-04, D-05)"
    - "Категории and Каталоги are reachable from the same /products toolbar rather than the top nav (D-02, D-03)"
  artifacts:
    - path: "app/templates/partials/products_toolbar.html"
      provides: "Always-visible two-group action toolbar for the Товары page (D-01/D-04)"
      contains: "toolbar-group"
    - path: "app/templates/base.html"
      provides: "8-item top-level nav"
      contains: "Настройки"
  key_links:
    - from: "app/templates/pages/products_list.html"
      to: "app/templates/partials/products_toolbar.html"
      via: "{% include %}"
      pattern: "include \"partials/products_toolbar.html\""
    - from: "app/static/style.css"
      to: "app/templates/partials/products_toolbar.html"
      via: ".toolbar/.toolbar-group CSS classes"
      pattern: "\\.toolbar"
---

<objective>
Reduce the desktop top-level nav in `app/templates/base.html` from 17 items to exactly 8 (Главная, Товары, Продажи, Покупатели, История, Отчёты, Финансы, Настройки — NAV-08), and give the Товары page (`/products`) an always-visible two-group action toolbar that re-homes Приход, Списание, Справочник (NAV-01/NAV-02/NAV-03), Категории (D-02) and Каталоги (D-03) — per CONTEXT.md D-01/D-04/D-05: plain `<div>` toolbar, no JS, no click-to-reveal.

Purpose: This is the trunk of the phase's IA restructure — every other plan in this phase (Настройки hub, mobile tab bar, mobile Товары page) either points AT the destinations this plan creates space for, or reuses the `.toolbar`/`.toolbar-group` CSS this plan introduces.
Output: 8-item `base.html` nav; new `partials/products_toolbar.html` included on `/products`; 2 new CSS rules; 3 existing tests updated to assert the new toolbar reachability path instead of the old top-nav one.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-navigation-restructure-settings/24-CONTEXT.md
@.planning/phases/24-navigation-restructure-settings/24-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reduce desktop top nav to 8 items (NAV-08)</name>
  <files>app/templates/base.html, tests/test_smoke.py</files>
  <read_first>app/templates/base.html (full file — the `<nav>` block is lines 34-52, one `<a>` per current item, active-state via `request.url.path.startswith(...)`)</read_first>
  <action>
    In `app/templates/base.html`, replace the entire `<nav>...</nav>` block (lines 34-52) with exactly 8 `<a>` elements, same active-state idiom (`{% if request.url.path == "/" %}`/`{% if request.url.path.startswith("/X") %}`) as the existing items, in this order: Главная (`href="/"`, exact-match active check), Товары (`href="/products"`), Продажи (`href="/sales/new"`, active check `startswith("/sales")` — unchanged from today), Покупатели (`href="/customers"`), История (`href="/history"`), Отчёты (`href="/reports"`), Финансы (`href="/finance"`, active check simplified to plain `startswith("/finance")` — the old carve-out excluding `/finance/report` is no longer needed since Экспорт кассы is leaving the nav entirely in plan 24-02), Настройки (`href="/settings"`, active check `startswith("/settings")`, new item, target page does not exist until plan 24-02 — that is fine, this task only adds the link text/href, it does not require `/settings` to resolve).
    Remove all other current items outright: Категории, Склады, Приход, Списание, Перемещение, Экспорт, Экспорт кассы, Справочник, Каталоги, Резервные копии — every one of these keeps its route registered and its page fully working (no route file is touched by this task), only its top-nav `<a>` is deleted.
    Then append one new test to `tests/test_smoke.py`: `test_web_top_nav_has_exactly_eight_items(client)` — GET `/`, assert 200, slice `response.text` between the literal `"<nav>"` and `"</nav>"` markers, assert the slice contains exactly 8 occurrences of the substring `"<a "`, and assert each of the 8 `href="..."` values (`/`, `/products`, `/sales/new`, `/customers`, `/history`, `/reports`, `/finance`, `/settings`) plus each of the 8 visible labels (Главная, Товары, Продажи, Покупатели, История, Отчёты, Финансы, Настройки) is present inside that slice.
  </action>
  <verify>
    <automated>uv run pytest tests/test_smoke.py tests/test_mobile_wiring.py::test_every_preexisting_desktop_nav_route_still_returns_200 -x</automated>
  </verify>
  <acceptance_criteria>
    - `app/templates/base.html`'s `<nav>` block contains exactly 8 `<a>` elements.
    - `uv run pytest tests/test_smoke.py::test_web_top_nav_has_exactly_eight_items -x` passes.
    - `uv run pytest tests/test_mobile_wiring.py::test_every_preexisting_desktop_nav_route_still_returns_200 -x` still passes (proves every removed-from-nav route, e.g. `/categories`, `/warehouses`, `/dictionary`, `/backup`, `/export`, still returns 200 by direct URL).
  </acceptance_criteria>
  <done>base.html renders exactly 8 nav items; the new test proves it; every relocated route stays reachable by direct URL.</done>
</task>

<task type="auto">
  <name>Task 2: Товары toolbar partial + CSS (D-01/D-02/D-03/D-04/D-05)</name>
  <files>app/templates/partials/products_toolbar.html, app/templates/pages/products_list.html, app/static/style.css</files>
  <read_first>
    app/templates/pages/products_list.html (3-line content block: `<h1>Товары</h1>` then `{% include "partials/product_rows.html" %}`)
    app/templates/pages/catalog_detail.html (lines 6-8, the existing `.form-actions` group usage pattern)
    app/static/style.css (lines 20-36, existing `nav`/`nav a.active` rules — new `.toolbar`/`.toolbar-group` rules go immediately after this block, before line 38's `a { color: #2563eb; }`)
    .planning/phases/24-navigation-restructure-settings/24-UI-SPEC.md (section "New Components > Товары toolbar" for the exact markup/CSS values to use verbatim)
  </read_first>
  <action>
    Create `app/templates/partials/products_toolbar.html`: a `<div class="toolbar">` (never `<nav>` — `base.html` already owns the page's one `<nav>` landmark, and `tests/test_reports.py:316,459` assert `"<nav" not in response.text` on chrome-less HX-partial responses elsewhere in the app) containing two `<div class="toolbar-group">` children. Group 1: `<span class="muted">Действия</span>` followed by a `<div class="form-actions">` with two `<a class="button">` links — `href="/receipts/new"` labeled `Приход`, `href="/writeoff"` labeled `Списание` (NAV-01/NAV-02). Group 2: `<span class="muted">Справочники</span>` followed by a `<div class="form-actions">` with three `<a class="button">` links — `href="/categories"` labeled `Категории` (D-02), `href="/dictionary"` labeled `Справочник` (NAV-03), `href="/catalogs"` labeled `Каталоги` (D-03). No Jinja logic, no context variables needed — every href/label is static.
    Edit `app/templates/pages/products_list.html`: insert `{% include "partials/products_toolbar.html" %}` immediately after `<h1>Товары</h1>` and before `{% include "partials/product_rows.html" %}` — the toolbar sits below the title and above the list (D-05: always visible, never inside the `#product-rows` HTMX swap target, so filter/sort/pagination swaps never touch it).
    Append 2 new CSS rules to `app/static/style.css` right after the `nav a.active { font-weight: 600; }` rule (line 36) and before `a { color: #2563eb; }` (line 38): `.toolbar { display: flex; gap: 32px; margin-bottom: 24px; flex-wrap: wrap; }` and `.toolbar-group { display: flex; flex-direction: column; gap: 8px; }`. Do not modify `.form-actions`, `.button`, or `.muted` — the toolbar reuses all three verbatim.
  </action>
  <verify>
    <automated>uv run pytest tests/test_catalog.py -k "products_list or nav_has_categories" -x</automated>
  </verify>
  <acceptance_criteria>
    - `GET /products` response contains `<div class="toolbar">`, two `<div class="toolbar-group">` blocks, and all 5 hrefs (`/receipts/new`, `/writeoff`, `/categories`, `/dictionary`, `/catalogs`).
    - `GET /products` response does NOT render the toolbar as a bare `<nav` element.
    - `app/static/style.css` contains exactly one `.toolbar {` rule and one `.toolbar-group {` rule (no duplicates).
    - `uv run pytest tests/test_catalog.py::test_web_nav_has_categories_link -x` still passes unmodified (it already targets `GET /products`, so it now incidentally verifies the toolbar).
  </acceptance_criteria>
  <done>The Товары page renders an always-visible, two-group toolbar reusing `.form-actions`/`.button`/`.muted`; only 2 new CSS rules were added.</done>
</task>

<task type="auto">
  <name>Task 3: Update the 3 toolbar-relocated nav-presence tests</name>
  <files>tests/test_dictionary.py, tests/test_writeoffs.py, tests/test_receipts.py</files>
  <read_first>
    tests/test_dictionary.py (`test_web_nav_has_dictionary_link`, line 321)
    tests/test_writeoffs.py (`test_web_writeoff_reachable_from_nav`, line 286)
    tests/test_receipts.py (`test_web_nav_has_receipts_link`, line 584)
  </read_first>
  <action>
    In `tests/test_dictionary.py`, change `test_web_nav_has_dictionary_link`'s `client.get("/")` call to `client.get("/products")`; update the docstring from "Nav gains the third entry" to reflect the item now lives in the Товары toolbar, not the top nav. Keep the two existing assertions (`'href="/dictionary"' in response.text` and `"Справочник" in response.text`) unchanged — they now verify the toolbar instead of the old nav.
    In `tests/test_writeoffs.py`, change `test_web_writeoff_reachable_from_nav`'s `client.get("/")` to `client.get("/products")`; update the docstring to note the gap-closure guard now points at the Товары toolbar (D-01/D-04) instead of the home nav. Keep the `'href="/writeoff"' in response.text` assertion unchanged.
    In `tests/test_receipts.py`, change `test_web_nav_has_receipts_link`'s `client.get("/")` to `client.get("/products")`; keep both assertions (`'href="/receipts/new"'` and `"Приход"`) unchanged.
  </action>
  <verify>
    <automated>uv run pytest tests/test_dictionary.py::test_web_nav_has_dictionary_link tests/test_writeoffs.py::test_web_writeoff_reachable_from_nav tests/test_receipts.py::test_web_nav_has_receipts_link -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_dictionary.py::test_web_nav_has_dictionary_link tests/test_writeoffs.py::test_web_writeoff_reachable_from_nav tests/test_receipts.py::test_web_nav_has_receipts_link -x` all pass.
    - None of the 3 updated tests call `client.get("/")` anymore — each calls `client.get("/products")`.
  </acceptance_criteria>
  <done>All 3 nav-presence tests assert reachability from `/products` (the Товары toolbar), matching the new IA.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Operator browser -> `GET /products`, `GET /` | Same-origin, no auth boundary (single local operator, v1 has no auth per CLAUDE.md) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01a | Information Disclosure | Routes removed from `base.html`'s `<nav>` (e.g. `/categories`, `/dictionary`, `/warehouses`) but left registered | accept | No new exposure — these pages showed the same data before this phase too (already noted LOW/accepted in 24-RESEARCH.md Security Domain); removing a nav link does not change access control, only discoverability |
| T-24-01b | Tampering | `products_toolbar.html` new partial | accept | 100% static markup, zero user input, zero Jinja logic — no injectable surface |

No V5 input-validation surface is introduced by this plan (no new query/form params). The `?code=` V5 case belongs to plan 24-04.
</threat_model>

<verification>
- `uv run pytest tests/test_smoke.py tests/test_catalog.py tests/test_dictionary.py tests/test_writeoffs.py tests/test_receipts.py tests/test_mobile_wiring.py -x` — full touched-module sweep.
- `uv run pytest` — full suite, confirming no other test still depends on the old 17-item nav shape.
</verification>

<success_criteria>
1. `GET /` renders a `<nav>` with exactly 8 `<a>` items matching NAV-08's list.
2. `GET /products` renders the always-visible two-group toolbar with all 5 relocated links (D-01/D-02/D-03/D-04/D-05).
3. `tests/test_dictionary.py`, `tests/test_writeoffs.py`, `tests/test_receipts.py` assert reachability via `/products`, not `/`.
4. Every route removed from the nav (`/categories`, `/warehouses`, `/dictionary`, `/backup`, `/export`, `/finance/report`) still returns 200 by direct URL.
</success_criteria>

<output>
Create `.planning/phases/24-navigation-restructure-settings/24-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan)

- `app/templates/partials/products_toolbar.html` (NEW) — static two-group action toolbar, included by `pages/products_list.html`
- `.toolbar` / `.toolbar-group` CSS classes (NEW, in `app/static/style.css`)
- `app/templates/base.html` — nav reduced to 8 `<a>` items (Главная, Товары, Продажи, Покупатели, История, Отчёты, Финансы, Настройки)
- `tests/test_smoke.py::test_web_top_nav_has_exactly_eight_items` (NEW)

---
phase: 24-navigation-restructure-settings
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/templates/pages/reports_sales.html
  - app/templates/pages/reports_writeoffs.html
  - app/templates/pages/reports_stock.html
  - app/templates/pages/reports_expiry.html
  - app/templates/pages/reports_products.html
  - tests/test_reports.py
autonomous: true
requirements: [RPT-01]

must_haves:
  truths:
    - "Every one of the 5 report detail pages (sales, writeoffs, stock, expiry, products) shows a «← Назад к отчётам» link back to /reports, placed immediately above the page's own <h1> (RPT-01)"
  artifacts:
    - path: "app/templates/pages/reports_sales.html"
      provides: "Back-link to /reports"
      contains: "← Назад к отчётам"
    - path: "app/templates/pages/reports_writeoffs.html"
      provides: "Back-link to /reports"
      contains: "← Назад к отчётам"
    - path: "app/templates/pages/reports_stock.html"
      provides: "Back-link to /reports"
      contains: "← Назад к отчётам"
    - path: "app/templates/pages/reports_expiry.html"
      provides: "Back-link to /reports"
      contains: "← Назад к отчётам"
    - path: "app/templates/pages/reports_products.html"
      provides: "Back-link to /reports"
      contains: "← Назад к отчётам"
  key_links:
    - from: "app/templates/pages/reports_sales.html"
      to: "/reports"
      via: "static <a href>"
      pattern: "href=\"/reports\">.*Назад"
---

<objective>
Add a "← Назад к отчётам" link to every report detail page (`/reports/sales`, `/reports/writeoffs`, `/reports/stock`, `/reports/expiry`, `/reports/products`) so the operator can always get back to the `/reports` landing page in one click (RPT-01).

Purpose: Closes the same class of gap UAT found in Phase 17 (report pages shipped without entry/exit navigation) — a small, mechanical, self-contained fix.
Output: 5 templates each gain one static `<p><a href="/reports">← Назад к отчётам</a></p>` line; `tests/test_reports.py` gains 5 new assertions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-navigation-restructure-settings/24-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert the back-link into all 5 report detail templates</name>
  <files>app/templates/pages/reports_sales.html, app/templates/pages/reports_writeoffs.html, app/templates/pages/reports_stock.html, app/templates/pages/reports_expiry.html, app/templates/pages/reports_products.html</files>
  <read_first>
    app/templates/pages/catalog_detail.html (lines 1-4 — the exact precedent: `{% extends "base.html" %}` / `{% block content %}` / `<p><a href="/catalogs">← Все каталоги</a></p>` / `<h1>...` — CONTEXT.md mandates following this shape verbatim for RPT-01)
    app/templates/pages/reports_sales.html (lines 1-4 — `{% block content %}` then `<h1>Продажи и прибыль</h1>`)
    app/templates/pages/reports_writeoffs.html (lines 1-4 — `<h1>Списания</h1>`)
    app/templates/pages/reports_stock.html (lines 1-6 — `<h1>Остатки склада</h1>`)
    app/templates/pages/reports_expiry.html (lines 1-6 — `<h1>Сроки годности</h1>`)
    app/templates/pages/reports_products.html (lines 1-4 — `<h1>Топ и залежавшиеся товары</h1>`)
    app/templates/pages/reports_landing.html (do NOT modify — this is the `/reports` index itself, the destination of the back-link, not a detail page)
  </read_first>
  <action>
    In each of the 5 files, insert exactly one line — `<p><a href="/reports">← Назад к отчётам</a></p>` — immediately before that file's own `<h1>` line (line 3 in `reports_sales.html`/`reports_writeoffs.html`/`reports_products.html`; line 3 in `reports_stock.html`/`reports_expiry.html`). Use this exact text (`← Назад к отчётам`, em-dash arrow + text — CONTEXT.md mandates this exact wording), matching `catalog_detail.html:3`'s shape byte-for-byte except for the href/label text. Do not touch `partials/sales_report_results.html`, `partials/writeoffs_report_rows.html`, or `partials/top_selling_rows.html` — those are the chrome-less HX-partial fragments swapped by the period filter, and must stay exactly as they are (adding the back-link there would duplicate it on every filter swap and risk it being mistaken for swappable content).
  </action>
  <verify>
    <automated>uv run pytest tests/test_reports.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 5 report detail templates contains the exact string `<p><a href="/reports">← Назад к отчётам</a></p>` immediately before its `<h1>`.
    - `partials/sales_report_results.html`, `partials/writeoffs_report_rows.html`, `partials/top_selling_rows.html` are unmodified (still 0 occurrences of "Назад к отчётам").
    - `uv run pytest tests/test_reports.py::test_web_reports_sales_hx_request_returns_partial_only tests/test_reports.py::test_web_reports_writeoffs_hx_request_returns_partial_only -x` still pass (proves the back-link, being outside the HX swap target, does not leak into the chrome-less partial responses).
  </acceptance_criteria>
  <done>All 5 report detail pages show the back-link above their title; the HX-partial fragments remain chrome-less.</done>
</task>

<task type="auto">
  <name>Task 2: Add 5 new back-link assertions to tests/test_reports.py</name>
  <files>tests/test_reports.py</files>
  <read_first>tests/test_reports.py (lines 305-323, 412-465 — existing per-report test style/naming convention to match: `test_web_reports_{name}_...`)</read_first>
  <action>
    Append 5 new test functions to `tests/test_reports.py`, one per report detail page, each following the existing file's plain `def test_web_..._(client):` style (no new fixtures needed): `test_web_reports_sales_has_back_link` — `client.get("/reports/sales")` returns 200 and contains `<p><a href="/reports">← Назад к отчётам</a></p>`; `test_web_reports_writeoffs_has_back_link` — same for `client.get("/reports/writeoffs")`; `test_web_reports_stock_has_back_link` — same for `client.get("/reports/stock")`; `test_web_reports_expiry_has_back_link` — same for `client.get("/reports/expiry")`; `test_web_reports_products_has_back_link` — same for `client.get("/reports/products")`. Place them near the other nav/reachability tests in the file (e.g. near `test_web_nav_has_reports_link` at line 319) rather than scattered.
  </action>
  <verify>
    <automated>uv run pytest tests/test_reports.py -k back_link -x</automated>
  </verify>
  <acceptance_criteria>
    - All 5 new tests pass.
    - `uv run pytest tests/test_reports.py -k back_link -x` collects exactly 5 tests.
  </acceptance_criteria>
  <done>RPT-01 has explicit, per-page automated coverage — one assertion per report detail page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Operator browser -> `GET /reports/{sales,writeoffs,stock,expiry,products}` | Same-origin, no auth boundary, no new query/form params |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03 | Tampering | 5 static `<a href="/reports">` insertions | accept | Zero user input, zero Jinja logic — purely static markup, no injectable surface |

No V5 input-validation surface is introduced by this plan.
</threat_model>

<verification>
- `uv run pytest tests/test_reports.py -x` — full module sweep, including the untouched HX-partial chrome-less assertions.
- `uv run pytest` — full suite.
</verification>

<success_criteria>
1. Every report detail page (`/reports/sales`, `/reports/writeoffs`, `/reports/stock`, `/reports/expiry`, `/reports/products`) shows the exact "← Назад к отчётам" link back to `/reports`.
2. `/reports` itself (the landing page) is unmodified.
3. HX-partial fragments remain chrome-less (no back-link leakage).
4. 5 new automated tests cover this, one per page.
</success_criteria>

<output>
Create `.planning/phases/24-navigation-restructure-settings/24-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan)

- `app/templates/pages/reports_sales.html` — back-link added
- `app/templates/pages/reports_writeoffs.html` — back-link added
- `app/templates/pages/reports_stock.html` — back-link added
- `app/templates/pages/reports_expiry.html` — back-link added
- `app/templates/pages/reports_products.html` — back-link added
- `tests/test_reports.py` — 5 new `test_web_reports_*_has_back_link` tests

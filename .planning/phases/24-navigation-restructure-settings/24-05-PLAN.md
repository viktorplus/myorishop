---
phase: 24-navigation-restructure-settings
plan: 05
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - app/templates/mobile_base.html
  - app/static/style.css
  - app/templates/mobile_pages/home.html
  - app/templates/mobile_pages/finance.html
  - tests/test_mobile_home.py
  - tests/test_mobile_wiring.py
  - tests/test_finance_reports.py
autonomous: true
requirements: [MOB-01]

must_haves:
  truths:
    - "Every /m/* page renders a persistent tab bar docked at the TOP of the viewport, not the bottom (D-09, deliberate deviation from mobile convention)"
    - "The tab bar shows exactly 7 tabs — Главная, Товары, Продажи, Покупатели, История, Отчёты, Финансы — and excludes Настройки (MOB-01)"
    - "The old 10-tile mobile home grid is gone entirely; dashboard content on /m/ renders unchanged below the new tab bar (D-10)"
    - "Экспорт кассы is unreachable from every /m/* page — no home-grid tile, no in-page /m/finance CTA (D-12)"
  artifacts:
    - path: "app/templates/mobile_base.html"
      provides: "{% block tabbar %} persistent 7-tab nav, sibling to {% block back %}"
      contains: "mobile-tabbar"
  key_links:
    - from: "app/templates/mobile_base.html"
      to: "/m/products"
      via: "static <a> in the tabbar block"
      pattern: "mobile-tabbar"
---

<objective>
Give every mobile page a persistent, top-docked 7-tab navigation bar (Главная, Товары, Продажи, Покупатели, История, Отчёты, Финансы — MOB-01, D-09), remove the old 10-tile `/m/` home grid it replaces (D-10), and remove the one remaining in-page path to Экспорт кассы on `/m/finance` so Настройки-hosted destinations stay fully unreachable from mobile (D-12).

Purpose: This is the mobile-chrome half of the phase's restructure — every mobile page from this point on renders through this new tab bar, mirroring the desktop 8-item nav from plan 24-01 (minus Настройки).
Output: New `{% block tabbar %}` in `mobile_base.html`; 2 new CSS rules; tile grid removed from `mobile_pages/home.html`; the in-page report-CTA removed from `mobile_pages/finance.html`; 5 existing mobile tests updated to match.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-navigation-restructure-settings/24-CONTEXT.md
@.planning/phases/24-navigation-restructure-settings/24-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persistent top-docked mobile tab bar (D-09, MOB-01)</name>
  <files>app/templates/mobile_base.html, app/static/style.css</files>
  <read_first>
    app/templates/mobile_base.html (full file, 37 lines — `<body>` at line 29, `<main class="mobile-shell">` at line 30, `{% block back %}` at line 31)
    app/templates/base.html (lines 34-52, POST plan-24-01 shape — the `request.url.path.startswith(...)`/`class="active"` idiom to mirror, not reinvent)
    app/routes/mobile_reports.py (full file — confirms ONLY `GET /m/reports/expiry` is registered; there is NO bare `/m/reports` route today. The UI-SPEC's literal tab markup uses `href="/m/reports"` for the Отчёты tab — that route does not exist and would 404. Use `href="/m/reports/expiry"` instead, the one mobile reports route verified to exist; keep the active-state check as `request.url.path.startswith("/m/reports")` since that prefix still correctly covers `/m/reports/expiry`.)
    .planning/phases/24-navigation-restructure-settings/24-UI-SPEC.md (section "New Components > Mobile top tab bar" for the CSS values — apply the href correction above on top of it)
  </read_first>
  <action>
    In `app/templates/mobile_base.html`, insert a new `{% block tabbar %}...{% endblock %}` between `<body>` (line 29) and `<main class="mobile-shell">` (line 30) — a sibling of `{% block back %}`, never nested inside `.mobile-shell` (the UI-SPEC requires it full-viewport-width, not constrained to the 480px shell). Inside the block, render `<nav class="mobile-tabbar">` containing exactly 7 `<a>` elements, each following the `{% if request.url.path... %} class="active"{% endif %}` idiom: Главная (`href="/m/"`, active check `request.url.path == "/m/"`), Товары (`href="/m/products"`, active check `startswith("/m/products")`), Продажи (`href="/m/sales"`, active check `startswith("/m/sales")`), Покупатели (`href="/m/customers"`, active check `startswith("/m/customers")`), История (`href="/m/history"`, active check `startswith("/m/history")`), Отчёты (`href="/m/reports/expiry"`, active check `startswith("/m/reports")` — see read_first note on why this differs from the UI-SPEC's literal href), Финансы (`href="/m/finance"`, active check `startswith("/m/finance")`). No icons, text-only labels. Do not add an 8th "Настройки"/"Ещё" tab (D-12).
    Append 2 new CSS rules to `app/static/style.css`, after the `.toolbar-group` rule added in plan 24-01 (this plan runs after 24-01 for exactly this reason — same file): `nav.mobile-tabbar { display: flex; background: #ffffff; border-bottom: 1px solid #d9d9d9; position: sticky; top: 0; z-index: 1; }` and `nav.mobile-tabbar a { flex: 1; min-height: 44px; display: flex; align-items: center; justify-content: center; font-size: 14px; text-align: center; padding: 4px; }` plus the active-state rule `nav.mobile-tabbar a.active { font-weight: 600; border-bottom: 3px solid #2563eb; }` (weight + border shape, never color alone — WCAG 1.4.1, matches the project's existing `a.link-danger` underline convention).
  </action>
  <verify>
    <automated>uv run pytest tests/test_mobile_wiring.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Every `/m/*` page (proven via `/m/`, `/m/sales`, `/m/finance`) renders `<nav class="mobile-tabbar">` with exactly 7 `<a>` children.
    - The Отчёты tab's `href` resolves to a real, registered route (`/m/reports/expiry`, not the non-existent `/m/reports`).
    - No tab labeled "Настройки" or "Ещё" is present anywhere in the tab bar.
    - `app/static/style.css` contains exactly one `nav.mobile-tabbar {` rule block (no duplicates).
  </acceptance_criteria>
  <done>Every mobile page renders a working, top-docked, 7-tab bar with correct hrefs and active-state highlighting.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the old home tile grid (D-10) and the /m/finance report CTA (D-12)</name>
  <files>app/templates/mobile_pages/home.html, app/templates/mobile_pages/finance.html</files>
  <read_first>
    app/templates/mobile_pages/home.html (lines 1-22 — the `.mobile-tile-grid` block at lines 5-16 plus its own anticipatory "temporary until Phase 24" comment at lines 18-22, both being removed; the dashboard content starting at line 23 `<h2>Показатели</h2>` stays untouched)
    app/templates/mobile_pages/finance.html (line 15 — `<p><a class="button" href="/m/finance/report">Отчёт и экспорт CSV</a></p>`, the in-page CTA independent of the home-grid tile, per RESEARCH.md Pitfall 2)
  </read_first>
  <action>
    In `app/templates/mobile_pages/home.html`, delete the entire `<div class="mobile-tile-grid">...</div>` block (lines 5-16) and the Jinja comment block immediately after it that anticipates this exact removal (lines 18-22). Leave everything from `<h2>Показатели</h2>` (line 23) onward completely untouched — dashboard content must render exactly as before, just with nothing above it but the new tab bar (added in Task 1's `mobile_base.html` edit, which `home.html` inherits automatically via `{% extends "mobile_base.html" %}`).
    In `app/templates/mobile_pages/finance.html`, delete line 15 (`<p><a class="button" href="/m/finance/report">Отчёт и экспорт CSV</a></p>`) outright. Leave `app/routes/mobile_finance.py`'s `/m/finance/report` route registered and untouched (Open Question 1 in 24-RESEARCH.md: leave it reachable-but-unlinked, no operator-visible difference, zero risk) — do not delete the route, only this one in-page link.
  </action>
  <verify>
    <automated>uv run pytest tests/test_mobile_home.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `GET /m/` response contains no `class="mobile-tile-grid"` and no `href="/m/finance/report"` anywhere.
    - `GET /m/` still renders `<h2>Показатели</h2>` and the 4 metric tiles unchanged.
    - `GET /m/finance` response contains no `href="/m/finance/report"`.
    - `GET /m/finance/report` (direct URL) still returns 200 — the route itself stays registered.
  </acceptance_criteria>
  <done>The old tile grid is gone, dashboard content is intact, and Экспорт кассы has zero remaining path from any /m/* page (D-12).</done>
</task>

<task type="auto">
  <name>Task 3: Update the 5 mobile tests broken by the grid/link removal</name>
  <files>tests/test_mobile_home.py, tests/test_mobile_wiring.py, tests/test_finance_reports.py</files>
  <read_first>
    tests/test_mobile_home.py (lines 1-51 — `EXPECTED_HREFS`, `test_mobile_home_renders_all_tiles_in_order` at line 28, `test_mobile_home_dashboard_content_is_below_the_untouched_nav_grid` at line 41)
    tests/test_mobile_wiring.py (lines 1-43 — `MOBILE_TILE_PATHS`, `test_mobile_home_lists_all_eight_tile_hrefs` at line 38; note `test_mobile_home_itself_is_reachable`, `test_every_mobile_tile_path_is_reachable`, `test_every_preexisting_desktop_nav_route_still_returns_200` all stay UNCHANGED — they hit routes directly by URL, which stay registered regardless of nav visibility)
    tests/test_finance_reports.py (lines 606-623 — `test_web_mobile_home_tile_links_to_finance_report`, `test_web_mobile_finance_page_report_link_is_button_styled`)
  </read_first>
  <action>
    In `tests/test_mobile_home.py`: replace `EXPECTED_HREFS` (the old 8-tile list) with a new module-level constant `EXPECTED_TABBAR_HREFS = ["/m/", "/m/products", "/m/sales", "/m/customers", "/m/history", "/m/reports/expiry", "/m/finance"]` (7 entries, matches Task 1's tab bar exactly). Rename `test_mobile_home_renders_all_tiles_in_order` to `test_mobile_home_renders_tabbar_hrefs_in_order`; keep the same `mobile_client_factory(mobile_home.router)` setup and the `assert "<nav>" not in body` line (still valid — the tab bar always renders with a `class` attribute, so the exact 6-character substring `"<nav>"` never appears); replace `EXPECTED_HREFS` with `EXPECTED_TABBAR_HREFS` in the `positions = [...]` comprehension. Rename `test_mobile_home_dashboard_content_is_below_the_untouched_nav_grid` to `test_mobile_home_dashboard_content_renders_below_tabbar`; replace its body's `body.index('href="/m/finance/report"')` (now absent, would raise) with `body.index('mobile-tabbar')`, keep the `< body.index("<h2>Показатели</h2>")` comparison, and add two new assertions: `"mobile-tile-grid" not in body` and `"/m/finance/report" not in body` (folds in the D-12 regression guard so a separate test is not needed).
    In `tests/test_mobile_wiring.py`: add a new module-level constant `MOBILE_TABBAR_PATHS = ["/m/", "/m/products", "/m/sales", "/m/customers", "/m/history", "/m/reports/expiry", "/m/finance"]` (leave the existing `MOBILE_TILE_PATHS` constant and the 2 tests that use it — `test_mobile_home_itself_is_reachable`, `test_every_mobile_tile_path_is_reachable` — completely unchanged). Rename `test_mobile_home_lists_all_eight_tile_hrefs` to `test_mobile_home_lists_seven_tabbar_hrefs`; change its body to iterate `MOBILE_TABBAR_PATHS` instead of `MOBILE_TILE_PATHS`, and add `assert "Настройки" not in response.text` (MOB-01: tab bar excludes Настройки).
    In `tests/test_finance_reports.py`: rename `test_web_mobile_home_tile_links_to_finance_report` to `test_web_mobile_home_has_no_finance_report_link`; replace its body's assertions with `assert 'href="/m/finance/report"' not in response.text` (inverts the old "prove reachable" intent to "prove unreachable", per D-12) and update the docstring accordingly. Rename `test_web_mobile_finance_page_report_link_is_button_styled` to `test_web_mobile_finance_page_has_no_report_link`; replace its body with `assert 'href="/m/finance/report"' not in response.text` against `client.get("/m/finance")`, update the docstring to note this now closes the D-12 gap instead of proving button styling.
  </action>
  <verify>
    <automated>uv run pytest tests/test_mobile_home.py tests/test_mobile_wiring.py tests/test_finance_reports.py -x</automated>
  </verify>
  <acceptance_criteria>
    - All renamed/updated tests pass.
    - `MOBILE_TILE_PATHS`/`test_mobile_home_itself_is_reachable`/`test_every_mobile_tile_path_is_reachable` in `tests/test_mobile_wiring.py` are byte-identical to before this plan (unaffected — routes stay registered).
    - No test anywhere in the suite still asserts `href="/m/finance/report"` IS present on `/m/` or `/m/finance`.
  </acceptance_criteria>
  <done>All 5 broken mobile tests are updated (not deleted) to assert the new tab-bar/no-finance-report-link reality, preserving each test's original regression-guard intent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Operator browser -> every `/m/*` page | Same-origin, no auth boundary; the tab bar is 100% static chrome, zero new query/form params |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05a | Information Disclosure | `/m/finance/report` route left registered but unlinked | accept | Same data exposure as before this phase (no auth boundary change); the operator-facing regression this plan closes is discoverability (D-12), not access control |
| T-24-05b | Tampering | New `{% block tabbar %}` chrome | accept | 100% static markup, zero user input, zero Jinja logic beyond the existing `request.url.path` active-state idiom already used 17 times in `base.html` |

No V5 input-validation surface is introduced by this plan (no new query/form params).
</threat_model>

<verification>
- `uv run pytest tests/test_mobile_home.py tests/test_mobile_wiring.py tests/test_finance_reports.py tests/test_mobile_finance.py -x` — full touched-module sweep (also re-runs `mobile_finance` tests since `finance.html` was edited).
- `uv run pytest` — full suite.
</verification>

<success_criteria>
1. Every `/m/*` page renders the 7-tab, top-docked bar with correct active-state highlighting.
2. The old 10-tile grid is fully gone from `/m/`; dashboard content renders unchanged below the tab bar.
3. Экспорт кассы has zero remaining path from any `/m/*` page.
4. All 5 previously-breaking tests are updated (not deleted) and pass.
</success_criteria>

<output>
Create `.planning/phases/24-navigation-restructure-settings/24-05-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan)

- `app/templates/mobile_base.html` — new `{% block tabbar %}` (7-tab `<nav class="mobile-tabbar">`)
- `nav.mobile-tabbar` / `nav.mobile-tabbar a` / `nav.mobile-tabbar a.active` CSS classes (NEW, in `app/static/style.css`)
- `app/templates/mobile_pages/home.html` — 10-tile grid removed
- `app/templates/mobile_pages/finance.html` — in-page Экспорт-кассы CTA removed

---
phase: 24-navigation-restructure-settings
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/routes/transfers.py
  - app/templates/partials/transfer_form.html
  - app/templates/partials/product_rows.html
  - tests/test_transfers.py
  - tests/test_catalog.py
autonomous: true
requirements: [NAV-07]

must_haves:
  truths:
    - "The product list (/products) has a «Переместить» text link on every row, next to «Изменить»/«Удалить», linking to /transfers?code={product.code} (D-13)"
    - "Opening /transfers?code=X pre-fills the product name and its open-batch picker automatically, with no re-selection step — the operator only has to pick a batch and a destination (D-14)"
    - "An unmatched code in ?code= silently renders an empty, unprefilled form — never a 500, never an echoed error (V5 Security Domain)"
  artifacts:
    - path: "app/routes/transfers.py"
      provides: "_resolve_transfer_lookup shared helper + GET /transfers?code= prefill branch"
      exports: ["_resolve_transfer_lookup"]
    - path: "app/templates/partials/product_rows.html"
      provides: "Переместить row action"
      contains: "/transfers?code="
  key_links:
    - from: "app/templates/partials/product_rows.html"
      to: "app/routes/transfers.py"
      via: "GET /transfers?code={{ product.code }}"
      pattern: "/transfers\\?code="
    - from: "app/routes/transfers.py"
      to: "app/templates/partials/transfer_form.html"
      via: "prefill_batches / prefill_show_empty context keys"
      pattern: "prefill_batches"
---

<objective>
Add a "Переместить" per-row action to the product list (`/products`) that opens `/transfers?code={code}` with the product's name and open-batch picker already resolved server-side — no client-side trigger, no re-selection step (NAV-07, D-13, D-14).

Purpose: Перемещение is being removed from the top nav entirely (handled in plan 24-01) — this plan builds its replacement reachability path, and is the only V5-input-validation-relevant surface in this phase (the new `?code=` query param).
Output: `app/routes/transfers.py` gains a shared `_resolve_transfer_lookup` helper and a `?code=` prefill branch on `GET /transfers`; `partials/transfer_form.html` gains a `prefill_batches`/`prefill_show_empty` fallback path; `product_rows.html` gains the row action; new/updated tests in `test_transfers.py` and `test_catalog.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-navigation-restructure-settings/24-CONTEXT.md
@.planning/phases/24-navigation-restructure-settings/24-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract shared lookup helper + GET /transfers?code= prefill (D-14, V5)</name>
  <files>app/routes/transfers.py, app/templates/partials/transfer_form.html</files>
  <read_first>
    app/routes/transfers.py (full file — `transfers_page` lines 33-41, `transfers_lookup` lines 44-75; the exact product+batches resolution logic to extract)
    app/routes/products.py (`product_new`, lines 174-203 — the resolve-then-prefill pattern this task mirrors: `.strip()` the code, parameterized `select()`, redirect-or-prefill, never `|safe`)
    app/templates/partials/transfer_form.html (full file — the inline `{% with %}` block at lines 56-66 that currently only ever populates the batch picker from `selected_batch`; this task adds a `prefill_batches`/`prefill_show_empty` fallback path used only when no batch has been explicitly selected yet)
    app/templates/partials/transfer_batch_wrap.html (full file — confirms the picker already reads `batches`/`selected_batch_id`/`show_empty` generically from whatever the caller passes, so no change is needed in this file)
  </read_first>
  <behavior>
    - `GET /transfers?code=EXISTING_CODE` (matching an active product with 1 open batch): response contains `value="EXISTING_CODE"` in the code field, the product's name pre-filled, and the batch picker renders that one open batch (not the "Нет партий с остатком." empty state).
    - `GET /transfers?code=UNKNOWN_CODE` (no matching active product): response is 200, code field shows `value="UNKNOWN_CODE"`, no product name filled, no 500, no error text rendered.
    - `GET /transfers` (no `code` param, existing behavior): response unchanged — empty form, empty batch picker, `focus_code=False`.
    - `GET /transfers/lookup?code=UNKNOWN_CODE` (existing endpoint, must stay byte-identical after the refactor): still returns 204.
  </behavior>
  <action>
    In `app/routes/transfers.py`, extract a new private function `_resolve_transfer_lookup(session: Session, code: str) -> dict | None`: move the body currently inline in `transfers_lookup` (lines 55-74 — the `lookup_prefill(session, code)` call, the `None` check, `code.strip()`, the parameterized `select(Product).where(Product.code == code_clean, Product.deleted_at.is_(None))` lookup, `open_batches(session, product.id)`) into this helper. Return `None` when `lookup_prefill` finds nothing (preserves the existing silent-no-match behavior — V5 Security Domain: never a 500, never echoes unsanitized input). On a match, return `{"name": result["name"], "code": code_clean, "batches": batches, "show_empty": product is not None and not batches}`.
    Rewrite `transfers_lookup` to call `_resolve_transfer_lookup(session, code)` and build its existing `partials/transfer_lookup.html` context from the returned dict (`selected_batch_id=None`, `batch_id_value=""` stay hardcoded exactly as today) — the endpoint's request/response contract must not change.
    Add a `code: str = ""` parameter to `transfers_page`. `.strip()` it. When non-empty, call `_resolve_transfer_lookup(session, code_clean)`. On a match, set `form = {"code": resolved["code"], "name": resolved["name"]}` and pass two new context keys — `prefill_batches = resolved["batches"]`, `prefill_show_empty = resolved["show_empty"]` — into the `pages/transfer_form.html` render context. On no match (or empty `code`), set `form = {"code": code_clean}` (or `{}` when `code_clean` is empty, matching today's default) with no `prefill_batches` key (silent no-prefill).
    In `app/templates/partials/transfer_form.html`, generalize the inline `{% with %}` block (lines 56-66): add `{% set _prefill = prefill_batches | default([]) %}` before it, then change `batches = [_sb] if _sb else []` to `batches = [_sb] if _sb else _prefill`, and add a new `show_empty = (not _sb) and (prefill_show_empty | default(false))` key to the same `{% with %}` list (this key does not exist in the block today — `transfer_batch_wrap.html` already reads `show_empty | default(false)` from it, so adding it is additive and safe). Leave every other key in that block (`code`, `selected_batch_id`, `batch_id_value`, `warehouses`, `dest_warehouse_id_value`, `new_expiry_value`, `new_comment_value`) unchanged.
  </action>
  <verify>
    <automated>uv run pytest tests/test_transfers.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `_resolve_transfer_lookup` exists in `app/routes/transfers.py` and is called from both `transfers_lookup` and `transfers_page`.
    - `GET /transfers/lookup?code=` for a code with no matching product/no open batches still returns 204 (byte-identical pre/post-refactor behavior).
    - `GET /transfers?code={valid_code}` response contains the resolved product name and its open batch, with zero client-side JS trigger involved (no new `hx-trigger="load"` added anywhere).
    - `GET /transfers?code={unmatched_code}` returns 200 with the code echoed via the existing autoescaped `value="{{ form.code }}"` attribute only — never `|safe`, never a raw 500.
  </acceptance_criteria>
  <done>Opening /transfers?code=X resolves the product and its batches server-side on the very first render — no re-selection step.</done>
</task>

<task type="auto">
  <name>Task 2: Add the «Переместить» row action to product_rows.html (D-13)</name>
  <files>app/templates/partials/product_rows.html</files>
  <read_first>
    app/templates/partials/product_rows.html (lines 61-64 — the existing actions `<td>` containing `Изменить`/`Удалить`)
    app/templates/pages/catalog_detail.html (line 37 — the closest existing precedent for a plain, non-htmx `?code=`-carrying link: `<a href="/products/new?code={{ entry.code }}">изменить цену</a>`)
  </read_first>
  <action>
    In `app/templates/partials/product_rows.html`, add a third link inside the existing actions `<td>` (immediately after the existing `Удалить` `<a>`, same `<td>`, same row): `<a href="/transfers?code={{ product.code }}">Переместить</a>` — a plain page-navigation link, no `hx-*` attributes (this is a full-page GET, matching the project-wide convention that all navigation is plain browser GETs, confirmed by `grep -r hx-boost app/templates` returning zero matches).
  </action>
  <verify>
    <automated>uv run pytest tests/test_catalog.py -k transfer_link -x</automated>
  </verify>
  <acceptance_criteria>
    - `GET /products` response contains `href="/transfers?code={product.code}"` and the text `Переместить` inside the same `<td>` as `Изменить`/`Удалить`, for every rendered product row.
  </acceptance_criteria>
  <done>Every row in the product list offers a direct, pre-filled path into the transfer form.</done>
</task>

<task type="auto">
  <name>Task 3: New/updated tests — prefill behavior + row action presence</name>
  <files>tests/test_transfers.py, tests/test_catalog.py</files>
  <read_first>
    tests/test_transfers.py (lines 1-30 — this file currently has ONLY service-layer tests calling `register_transfer`/`recent_transfers` directly; it has no web-level `client`-based tests yet, so this task adds the file's first `def test_web_...` functions, using the `client`/`stocked_product`/`warehouse` fixtures already provided globally by `tests/conftest.py`)
    tests/test_catalog.py (lines 1258-1266 — `test_web_products_delete_control_is_link_not_button`, the existing convention for asserting `product_rows.html` action-cell markup)
  </read_first>
  <action>
    Append to `tests/test_transfers.py`: `test_web_transfers_page_prefills_code_and_batches(client, stocked_product)` — GET `/transfers?code={stocked_product.code}` (use `stocked_product` fixture, which already has one open batch with 8 units per `tests/conftest.py`), assert 200, assert `f'value="{stocked_product.code}"'` in the response, assert `stocked_product.name` in the response, assert the empty-state text `"Нет партий с остатком."` is NOT in the response (proves the batch picker resolved a real batch, not the empty state). `test_web_transfers_page_unmatched_code_renders_empty_form(client)` — GET `/transfers?code=NOPE-NOPE` (no such product exists), assert 200, assert `'value="NOPE-NOPE"'` in the response, assert no 500/exception, assert the response contains no bare `|safe`-rendered raw echo (i.e. `NOPE-NOPE` only ever appears inside the escaped `value="..."` attribute, not as freestanding unescaped text). `test_web_transfers_lookup_unmatched_code_still_returns_204(client)` — GET `/transfers/lookup?code=NOPE-NOPE`, assert `response.status_code == 204` (regression guard: the helper extraction in Task 1 must not change this endpoint's contract).
    Append to `tests/test_catalog.py`: `test_web_products_list_row_has_transfer_link(client, product)` — GET `/products`, assert 200, assert `f'href="/transfers?code={product.code}"'` in the response and `"Переместить"` in the response.
  </action>
  <verify>
    <automated>uv run pytest tests/test_transfers.py tests/test_catalog.py -k "transfer" -x</automated>
  </verify>
  <acceptance_criteria>
    - All 4 new tests pass.
    - `uv run pytest tests/test_transfers.py -k "prefill or unmatched or lookup_unmatched" -x` collects and passes 3 tests.
  </acceptance_criteria>
  <done>NAV-07's prefill behavior and row-action presence both have explicit automated coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Operator browser -> `GET /transfers?code=` | Untrusted query-string input crosses into a database lookup and a rendered HTML attribute |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04 | Tampering / Information Disclosure | `GET /transfers?code=` new query param | mitigate | `.strip()` the raw string, resolve via the parameterized ORM `select(Product).where(Product.code == code_clean, ...)` (never string-interpolated SQL — the file's own existing convention at lines 63-65/88-90/139-141), treat "not found" as a silent no-prefill (empty form, 200), never a 500, never an error message that echoes unsanitized input. The value is only ever rendered inside the existing autoescaped `value="{{ form.code }}"` Jinja attribute — no `|safe` filter is added anywhere in this plan. |

This is the only V5 Input Validation surface in the entire Phase 24 plan set (per 24-RESEARCH.md Security Domain) — no other plan in this phase accepts new user input.
</threat_model>

<verification>
- `uv run pytest tests/test_transfers.py tests/test_catalog.py -x` — full touched-module sweep.
- `uv run pytest` — full suite (transfers.py is shared infrastructure; a regression here would also affect the desktop `/transfers/lookup` and `/transfers/batch-pick` endpoints).
</verification>

<success_criteria>
1. `/products` shows a "Переместить" link on every row, targeting `/transfers?code={code}`.
2. `GET /transfers?code=X` resolves the product name and open batches server-side, with zero client-side trigger.
3. An unmatched code renders an empty, unprefilled form — never a 500, never unsanitized echo.
4. `GET /transfers/lookup` keeps its exact pre-existing 204/200 contract after the helper extraction.
</success_criteria>

<output>
Create `.planning/phases/24-navigation-restructure-settings/24-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan)

- `app/routes/transfers.py` — `_resolve_transfer_lookup(session, code) -> dict | None` (NEW private helper), `GET /transfers` gains `code: str = ""` query param
- `app/templates/partials/transfer_form.html` — `prefill_batches` / `prefill_show_empty` context keys (NEW, additive)
- `app/templates/partials/product_rows.html` — «Переместить» row action linking to `/transfers?code={{ product.code }}`

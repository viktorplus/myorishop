---
phase: 24-navigation-restructure-settings
plan: 06
type: execute
wave: 2
depends_on: ["24-01", "24-02"]
files_modified:
  - app/routes/mobile_products.py
  - app/routes/mobile_customers.py
  - app/templates/mobile_pages/products.html
  - app/templates/mobile_pages/customers.html
  - app/templates/mobile_partials/products_toolbar.html
  - app/main.py
  - tests/test_mobile_products.py
  - tests/test_mobile_customers.py
autonomous: true
requirements: [MOB-01]

must_haves:
  truths:
    - "Tapping the Товары tab opens a real, working mobile product list (/m/products) reusing the existing desktop product-listing service — not a 404, not a repurposed search page (MOB-01, Pitfall 1)"
    - "Tapping the Покупатели tab opens a real, working mobile customer list (/m/customers) reusing the existing desktop customer-listing service (MOB-01, Pitfall 1)"
    - "The mobile Товары tab hosts the same two-group action toolbar shape as desktop, adapted for touch — Действия (Приход, Списание) and Справочники (Категории, Справочник, Каталоги) (D-11)"
  artifacts:
    - path: "app/routes/mobile_products.py"
      provides: "GET /m/products"
      exports: ["router"]
    - path: "app/routes/mobile_customers.py"
      provides: "GET /m/customers"
      exports: ["router"]
    - path: "app/templates/mobile_partials/products_toolbar.html"
      provides: "D-11 mobile mirror of the desktop Товары toolbar"
      contains: "toolbar-group"
  key_links:
    - from: "app/routes/mobile_products.py"
      to: "app/services/catalog.py"
      via: "list_products_view(session, code=code, name=name, page=page)"
      pattern: "list_products_view\\("
    - from: "app/routes/mobile_customers.py"
      to: "app/services/customers.py"
      via: "list_customers_view(session, page=page)"
      pattern: "list_customers_view\\("
    - from: "app/main.py"
      to: "app/routes/mobile_products.py"
      via: "app.include_router(mobile_products.router)"
      pattern: "include_router\\(mobile_products\\.router\\)"
---

<objective>
Create the two mobile pages the new tab bar (plan 24-05) points at but that do not exist yet: `GET /m/products` and `GET /m/customers` (MOB-01's Pitfall 1 gap) — thin routes reusing the exact same desktop list services (`list_products_view`, `list_customers_view`), new mobile-only card templates, and the mobile Товары tab additionally hosts the D-11 toolbar mirror (Приход/Списание/Категории/Справочник/Каталоги, adapted for touch).

Purpose: Without this plan, tapping "Товары" or "Покупатели" on the new mobile tab bar 404s — this is not optional chrome, it is the last functional gap MOB-01 requires closed.
Output: New `app/routes/mobile_products.py`, `app/routes/mobile_customers.py`, `mobile_pages/products.html`, `mobile_pages/customers.html`, `mobile_partials/products_toolbar.html`; both routers registered in `app/main.py`; 2 new test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-navigation-restructure-settings/24-CONTEXT.md
@.planning/phases/24-navigation-restructure-settings/24-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mobile Товары page — route, template, D-11 toolbar mirror</name>
  <files>app/routes/mobile_products.py, app/templates/mobile_pages/products.html, app/templates/mobile_partials/products_toolbar.html</files>
  <read_first>
    app/routes/mobile_search.py (full file, 59 lines — the exact thin-route shape to mirror: `router = APIRouter()`, one `@router.get(...)` calling an existing service function and rendering a mobile-only template, no HX-partial branch needed for this simpler page)
    app/services/catalog.py (`list_products_view`, lines 368+ — signature `(session, *, code="", name="", category="", sort="", page=0) -> dict` returning `{rows, total, total_pages, page, code, name, category, sort}`; `rows` are `Product` ORM objects with `.code`/`.name`/`.category`/`.quantity`/`.cost_cents`/`.sale_cents` attributes, already confirmed by `app/templates/partials/product_rows.html`)
    app/services/pagination.py (`LIST_PAGE_SIZE = 20`, `paginate()` — confirms `page`/`total_pages` semantics: 0-based, never raises on an out-of-range page)
    app/templates/mobile_pages/home.html (lines 1-3 — `{% extends "mobile_base.html" %}` shell shape to mirror)
    app/templates/partials/products_toolbar.html (created in plan 24-01 — the desktop toolbar this task's mobile version mirrors with different hrefs; read it for the two-group `.toolbar`/`.toolbar-group` shape, reusing the SAME CSS classes, already defined in `app/static/style.css` — no new CSS needed in this plan)
    app/static/style.css (`.mobile-card`, `.mobile-shell` rules — the mobile card-list visual convention to reuse for each product row)
  </read_first>
  <action>
    Create `app/templates/mobile_partials/products_toolbar.html` (D-11 mirror of the desktop toolbar, adapted for touch — reuses `.toolbar`/`.toolbar-group`/`.form-actions`/`.button`/`.muted` verbatim, zero new CSS): same two-group `<div class="toolbar">` shape as `partials/products_toolbar.html`, but group 1 ("Действия") links to the existing MOBILE routes — `href="/m/receipts"` labeled `Приход`, `href="/m/writeoff"` labeled `Списание` — and group 2 ("Справочники") links to the existing DESKTOP routes (no mobile equivalent exists for these three, and creating one is out of this phase's scope per CONTEXT.md's "no new capability" framing; `base.html`'s viewport-redirect script only fires on the root `/` path per its own D-02 comment, so these desktop pages remain reachable and functional from a phone-width browser) — `href="/categories"` labeled `Категории`, `href="/dictionary"` labeled `Справочник`, `href="/catalogs"` labeled `Каталоги`.
    Create `app/routes/mobile_products.py`: `router = APIRouter()`, one `@router.get("/m/products")` function `mobile_products(request: Request, code: str = "", name: str = "", page: int = 0, session: Session = Depends(get_session))` that calls `list_products_view(session, code=code, name=name, page=page)` and returns `templates.TemplateResponse(request, "mobile_pages/products.html", context)` where `context` is that dict unchanged (no HX-partial branch — this is a plain full-page GET, matching the project's all-navigation-is-full-page-GETs convention). Import `list_products_view` from `app.services.catalog`.
    Create `app/templates/mobile_pages/products.html`: `{% extends "mobile_base.html" %}`, `{% block step_indicator %}{% endblock %}`, `{% block content %}` containing `<h1>Товары</h1>`, then `{% include "mobile_partials/products_toolbar.html" %}`, then either an `<div class="empty-state"><p class="muted">Товаров пока нет.</p></div>` when `rows` is empty, or one `<div class="mobile-card">` per row in `rows` showing the product name (`<p><strong>{{ product.name }}</strong></p>`), a muted line with code and category (`Код: {{ product.code }}{% if product.category %} · {{ product.category }}{% endif %}`), and a quantity line (`<p class="num">Кол-во: {{ product.quantity }}</p>`). After the loop, when `page + 1 < total_pages`, render `<p><a href="/m/products?page={{ page + 1 }}">Показать ещё →</a></p>`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_mobile_products.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `GET /m/products` returns 200 (via `mobile_client_factory(mobile_products.router)` — no `app.main` dependency needed to prove the route itself works).
    - Response contains `<h1>Товары</h1>`, the toolbar's 5 hrefs (`/m/receipts`, `/m/writeoff`, `/categories`, `/dictionary`, `/catalogs`), and a `.mobile-card` per seeded product.
    - `list_products_view` is called with the request's `code`/`name`/`page` params, unmodified from `app.services.catalog` — no new business logic in the route.
  </acceptance_criteria>
  <done>Tapping the Товары tab lands on a working mobile product list with the D-11 toolbar.</done>
</task>

<task type="auto">
  <name>Task 2: Mobile Покупатели page — route, template</name>
  <files>app/routes/mobile_customers.py, app/templates/mobile_pages/customers.html</files>
  <read_first>
    app/routes/mobile_search.py (same thin-route shape as Task 1)
    app/services/customers.py (`list_customers_view`, lines 268-311 — signature `(session, *, name="", surname="", consultant_number="", sort="", page=0) -> dict` returning `{rows, total, total_pages, page, name, surname, consultant_number, sort}`; `rows` are `Customer` ORM objects with `.name`/`.surname`/`.consultant_number` attributes)
    app/routes/customers.py (`customer_detail`, lines 188-204 — confirms the existing desktop detail route is `GET /customers/{customer_id}`, the reuse target for each mobile card's link; no new mobile customer-detail page is created in this plan, per "no new capability")
    app/templates/mobile_pages/products.html (created in Task 1 of this same plan — mirror its exact shell/card/pagination shape)
  </read_first>
  <action>
    Create `app/routes/mobile_customers.py`: `router = APIRouter()`, one `@router.get("/m/customers")` function `mobile_customers(request: Request, page: int = 0, session: Session = Depends(get_session))` that calls `list_customers_view(session, page=page)` and returns `templates.TemplateResponse(request, "mobile_pages/customers.html", context)` where `context` is that dict unchanged. Import `list_customers_view` from `app.services.customers`.
    Create `app/templates/mobile_pages/customers.html`: `{% extends "mobile_base.html" %}`, `{% block step_indicator %}{% endblock %}`, `{% block content %}` containing `<h1>Покупатели</h1>`, then either an empty-state block (`Покупателей пока нет.`) when `rows` is empty, or one `<a class="mobile-card" href="/customers/{{ customer.id }}">` per row (the whole card is the link, reusing the existing desktop detail page — no new mobile detail route) showing `<p><strong>{{ customer.name }} {{ customer.surname or '' }}</strong></p>` and, when `customer.consultant_number` is set, a muted line `Номер консультанта: {{ customer.consultant_number }}`. After the loop, when `page + 1 < total_pages`, render `<p><a href="/m/customers?page={{ page + 1 }}">Показать ещё →</a></p>`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_mobile_customers.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `GET /m/customers` returns 200 (via `mobile_client_factory`).
    - Response contains `<h1>Покупатели</h1>` and one `.mobile-card` per seeded customer, each linking to `/customers/{id}` (the existing desktop detail page).
  </acceptance_criteria>
  <done>Tapping the Покупатели tab lands on a working mobile customer list.</done>
</task>

<task type="auto">
  <name>Task 3: Register both routers in app.main + integration tests</name>
  <files>app/main.py, tests/test_mobile_products.py, tests/test_mobile_customers.py</files>
  <read_first>
    app/main.py (full file — alphabetized `from app.routes import (...)` block and matching `app.include_router(...)` call order; `settings` was already inserted here by plan 24-02, so `mobile_customers`/`mobile_products` slot alphabetically into the existing `mobile_*` group between `mobile_corrections` and `mobile_finance`)
    tests/test_mobile_wiring.py (lines 21-35, `DESKTOP_NAV_PATHS`/`MOBILE_TILE_PATHS` — the existing convention for a real-`client`-fixture reachability list, for reference only; this task does not edit this file)
  </read_first>
  <action>
    In `app/main.py`, add `mobile_customers` and `mobile_products` to the existing alphabetized `from app.routes import (...)` block (they slot between `mobile_corrections` and `mobile_finance`), and add `app.include_router(mobile_products.router)` and `app.include_router(mobile_customers.router)` in the matching position among the other `mobile_*` `include_router` calls.
    Write `tests/test_mobile_products.py` (new file, mirror `tests/test_mobile_home.py`'s `mobile_client_factory` style for isolated-router tests, plus one real-`client` reachability test): `test_mobile_products_page_renders(mobile_client_factory, product)` — build a client via `mobile_client_factory(mobile_products.router)`, GET `/m/products`, assert 200, assert `product.code` and `product.name` in the response. `test_mobile_products_empty_state(mobile_client_factory)` — with no products seeded beyond the `client` fixture's own... note this file has no `client` fixture dependency, only `mobile_client_factory` + `session`; seed nothing extra, assert the empty-state text renders when `list_products_view` returns zero rows (use a fresh `session` with no products — do NOT rely on the global `product` fixture for this one). `test_mobile_products_registered_in_real_app(client)` — using the real `client` fixture (proves `app.main` registration from this task), GET `/m/products`, assert 200.
    Write `tests/test_mobile_customers.py` mirroring the same 3-test shape: `test_mobile_customers_page_renders(mobile_client_factory, customer)` (uses the `customer` fixture from `conftest.py`), `test_mobile_customers_empty_state(mobile_client_factory, session)`, `test_mobile_customers_registered_in_real_app(client)`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_mobile_products.py tests/test_mobile_customers.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `app/main.py` includes both `mobile_products.router` and `mobile_customers.router` exactly once each.
    - `uv run pytest tests/test_mobile_products.py::test_mobile_products_registered_in_real_app tests/test_mobile_customers.py::test_mobile_customers_registered_in_real_app -x` pass (proves real end-to-end registration, not just isolated-router tests).
  </acceptance_criteria>
  <done>Both new mobile routers are live in the real application and covered by isolated + integration tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Operator browser -> `GET /m/products`, `GET /m/customers` | Same-origin, no auth boundary; both routes reuse existing, already-tested desktop filter/sort/paginate service functions unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-06a | Tampering | `code`/`name`/`page` query params on `GET /m/products` | accept | Identical params, identical validation, as the already-shipped desktop `GET /products` route — `list_products_view` does its own internal `.strip()`/`.lower()` filtering (Phase 14 pattern), no new surface introduced by exposing it via a second thin route |
| T-24-06b | Information Disclosure | Mobile customer cards link to the existing desktop `/customers/{id}` detail page | accept | Same page, same data, same lack of auth as before this phase — only the entry point (mobile tab) is new, not the destination or its access level |

No V5 input-validation surface is introduced by this plan beyond what the already-shipped desktop equivalents already validate.
</threat_model>

<verification>
- `uv run pytest tests/test_mobile_products.py tests/test_mobile_customers.py tests/test_mobile_wiring.py -x` — full touched-module sweep.
- `uv run pytest` — full suite.
</verification>

<success_criteria>
1. `GET /m/products` and `GET /m/customers` both return 200 and render seeded data via a mobile card layout.
2. The mobile Товары tab hosts the D-11 toolbar with the same 5 links as desktop, adapted for touch.
3. Both routers are registered in `app/main.py` and reachable through the real app, not just isolated test apps.
4. MOB-01 is now fully satisfied — every one of the 7 mobile tabs lands on a real, working page.
</success_criteria>

<output>
Create `.planning/phases/24-navigation-restructure-settings/24-06-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan)

- `app/routes/mobile_products.py` (NEW) — `GET /m/products`
- `app/routes/mobile_customers.py` (NEW) — `GET /m/customers`
- `app/templates/mobile_pages/products.html` (NEW)
- `app/templates/mobile_pages/customers.html` (NEW)
- `app/templates/mobile_partials/products_toolbar.html` (NEW) — D-11 mobile toolbar mirror
- `app/main.py` — registers `mobile_products.router`, `mobile_customers.router`
- `tests/test_mobile_products.py` (NEW), `tests/test_mobile_customers.py` (NEW)

---
phase: 21-customer-profiles-purchase-insights
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - app/models.py
  - alembic/versions/0015_customer_contacts.py
  - tests/conftest.py
  - tests/test_customers.py
autonomous: true
requirements: [CUST-01, CUST-02, CUST-03, CUST-04, CUST-05, CUST-07]

must_haves:
  truths:
    - "Importing app.models does not raise — the whole test suite still collects (754 tests)"
    - "A fresh database migrated to head has a customer_contacts table and a customers.address column"
    - "A test can seed a sale at an explicit past UTC timestamp without touching the append-only ledger triggers"
  artifacts:
    - path: "app/models.py"
      provides: "CONTACT_KINDS allow-list + CustomerContact model + Customer.address column (D-01, D-02)"
      contains: "class CustomerContact"
    - path: "alembic/versions/0015_customer_contacts.py"
      provides: "create_table customer_contacts + add_column customers.address"
      contains: "revision = \"0015\""
    - path: "tests/conftest.py"
      provides: "past_sale fixture — backdated Sale + Operation seeding (Wave 0 gap)"
      contains: "def past_sale"
  key_links:
    - from: "app/models.py CustomerContact.customer_id"
      to: "customers.id"
      via: "bare mapped_column(ForeignKey(...)) — NO relationship()"
      pattern: "ForeignKey\\(\"customers.id\""
    - from: "alembic/versions/0015_customer_contacts.py"
      to: "0014_drop_product_catalog_cents.py"
      via: "down_revision pointer"
      pattern: "down_revision = \"0014\""
---

<objective>
Lay the Phase 21 foundation: the `CustomerContact` child table (D-01), the `Customer.address`
column (D-02), their Alembic migration, and the past-dated-sale test fixture that every CUST-07
spend-window test needs.

Purpose: Nothing else in this phase can start until the schema exists and tests can place a sale
at a controlled past date. `21-VALIDATION.md` §Wave 0 names the fixture gap "the single most
likely thing to block Wave 1".

Output: `CONTACT_KINDS`, `CustomerContact`, `Customer.address`, migration `0015`, `past_sale` fixture.

**Scope notes carried from `21-RESEARCH.md` Open Questions — decided, do not revisit:**
- **`CustomerContact.label` ships as a nullable column but is NOT surfaced in any UI this phase.**
  CUST-01..04 ask only for multiple values per kind. A second migration later for a field we
  already suspect is wanted is churn; a nullable column costs nothing.
- **CSV export drift is OUT OF SCOPE.** `app/services/export.py::stream_customers_csv` calls itself
  a "Full customer profile dump" and that claim becomes stale once address/contacts exist.
  **Do not touch `app/services/export.py` in this phase** — contacts are 1-to-many and do not fit
  the flat CSV shape. This is recorded as a deferred item, not an oversight.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-customer-profiles-purchase-insights/21-CONTEXT.md
@.planning/phases/21-customer-profiles-purchase-insights/21-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CONTACT_KINDS, CustomerContact model, and Customer.address</name>

  <files>app/models.py</files>

  <read_first>
    - `app/models.py` — the file being modified. Read in full. Note especially:
      `NAMING_CONVENTION` (lines 24-30), the `# no CHECK constraint on operations.type` decision
      comment (lines 32-33), the `WRITEOFF_REASONS` allow-list dict shape (lines 49-56),
      `Warehouse.address` (line 190), `Operation` (295-331), `Customer` (333-351),
      `Sale` (354-370) — `Sale.customer_id` is the exact FK+index spelling to copy.
    - `.planning/phases/21-customer-profiles-purchase-insights/21-PATTERNS.md` — §"No Analog Found"
      gives the exact `CheckConstraint` form. This is the project's FIRST CheckConstraint; there is
      nothing in the repo to copy from.
    - `app/core.py` — `new_id` and `utcnow_iso`, the only sanctioned id/time factories.
  </read_first>

  <action>
    Add three things to `app/models.py`.

    **1. `CONTACT_KINDS` module-level dict**, placed with the other allow-list dicts (near
    `WRITEOFF_REASONS` / `CASH_CATEGORIES`), mapping latin key → RU label:
    `"phone"` → `"Телефон"`, `"telegram"` → `"Telegram"`, `"email"` → `"Email"`,
    `"social"` → `"Соцсеть"`. Give it a docstring-style comment in the house voice noting it is the
    exact server-side allow-list the customer service validates `kind` against (per D-01, and
    matching the `WRITEOFF_REASONS` precedent at lines 49-56). Keys are latin and internal; the RU
    values are operator-facing labels used by the detail page.

    **2. `class CustomerContact(Base)`** with `__tablename__ = "customer_contacts"`, placed
    immediately after the `Customer` class. Columns, copying `Sale`/`Operation` spellings verbatim:
    - `id` — `Mapped[str]`, `mapped_column(String(36), primary_key=True, default=new_id)`
    - `customer_id` — `Mapped[str]`, `mapped_column(ForeignKey("customers.id", name="fk_customer_contacts_customer_id_customers"), nullable=False, index=True)`.
      Pass the `name=` explicitly — `Sale.customer_id` (line 366) and `Operation.sale_id` (line 315)
      both do. **No `relationship()`, no `back_populates`** — there are ZERO in `app/` and the FK is
      joined manually in the service (house rule, verified by PATTERNS.md).
    - `kind` — `Mapped[str]`, `mapped_column(String(20), nullable=False)`
    - `value` — `Mapped[str]`, `mapped_column(String(300), nullable=False)`
    - `label` — `Mapped[str | None]`, `mapped_column(String(100))`. **Nullable, ships unused this
      phase** — no form field, no template renders it (RESEARCH Open Question 1, decided). Add a
      comment saying so.
    - `created_at` — `Mapped[str]`, `mapped_column(String(32), default=utcnow_iso)`
    - `updated_at` — `Mapped[str]`, `mapped_column(String(32), default=utcnow_iso, onupdate=utcnow_iso)`

    `__table_args__` is a tuple containing exactly one `CheckConstraint` whose condition is
    `kind IN ('phone', 'telegram', 'email', 'social')` and whose `name` is the **short token**
    `"kind_valid"` — NOT the expanded name. `NAMING_CONVENTION["ck"]` is
    `ck_%(table_name)s_%(constraint_name)s`, so it expands to `ck_customer_contacts_kind_valid`.
    **The `name=` is MANDATORY**: an unnamed `CheckConstraint` raises `InvalidRequestError` at
    *import* of `app.models`, which breaks collection of the entire 754-test suite, not just new
    tests (reproduced live — RESEARCH Pitfall 1). Add a comment on the constraint saying exactly
    this so nobody "cleans it up" later.

    Add `CheckConstraint` to the existing `from sqlalchemy import (...)` block at lines 8-17
    (currently `JSON, ForeignKey, Index, Integer, MetaData, String, UniqueConstraint, text`) in
    alphabetical position.

    Also add a class docstring noting: D-01 one generic table with a `kind` discriminator, all four
    kinds share an identical label+value shape; the CHECK is defence-in-depth only — the PRIMARY
    gate is the `CONTACT_KINDS` Python allow-list validated in the service, matching the
    `models.py:32-33` decision not to CHECK `operations.type`.

    **3. `Customer.address`** — add `address: Mapped[str | None] = mapped_column(String(300))` to
    the `Customer` class, after `consultant_number` and before `search_lc`. This is a byte-identical
    copy of `Warehouse.address` (line 190), the in-repo precedent for a physical address (D-02:
    singular, so it is a plain column, NOT a `CustomerContact` row). Do **not** add `address` to
    `_search_lc` — search stays "name surname consultant".
  </action>

  <verify>
    <automated>uv run python -c "import app.models; from app.models import CONTACT_KINDS, CustomerContact, Customer; print(sorted(CONTACT_KINDS)); print(CustomerContact.__table__.constraints)"</automated>
    <automated>uv run pytest -q</automated>
    <automated>uv run ruff check app/models.py &amp;&amp; uv run ruff format --check app/models.py</automated>
  </verify>

  <acceptance_criteria>
    - `uv run python -c "import app.models"` exits 0 (proves the CheckConstraint is named; an
      unnamed one raises `InvalidRequestError` here)
    - `app/models.py` contains the literal `name="kind_valid"` (short token) and does **not**
      contain `name="ck_customer_contacts_kind_valid"` (expanded form belongs only in the migration)
    - The rendered constraint name is `ck_customer_contacts_kind_valid`: the printed
      `CustomerContact.__table__.constraints` includes a `CheckConstraint` whose `.name` equals
      `ck_customer_contacts_kind_valid`
    - `sorted(CONTACT_KINDS)` prints `['email', 'phone', 'social', 'telegram']`
    - `grep -n "relationship\|back_populates" app/models.py` returns no matches
    - `app/models.py` contains `ForeignKey("customers.id", name="fk_customer_contacts_customer_id_customers")`
    - `Customer.__table__.c.address.type.length == 300` and `Customer.__table__.c.address.nullable is True`
    - `uv run pytest -q` exits 0 with 754 tests passing (the pre-Phase-21 baseline — `app/models.py`
      is imported by everything, so any red here is this task's fault)
    - `uv run ruff check` and `uv run ruff format --check` both exit 0
  </acceptance_criteria>

  <done>
    `app.models` imports cleanly, `CustomerContact` carries a correctly-expanded named CHECK
    constraint, `Customer.address` exists as a nullable `String(300)`, and the full 754-test
    baseline is still green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Alembic migration 0015 — customer_contacts table + customers.address column</name>

  <files>alembic/versions/0015_customer_contacts.py</files>

  <read_first>
    - `alembic/versions/0013_cash_movements.py` — **the create_table analog**. Copy its
      `op.create_table` shape: every constraint name wrapped in `op.f(...)` and fully expanded
      (`pk_cash_movements`, `fk_cash_movements_sale_id_sales`), plus the separate
      `op.create_index(op.f("ix_..."), ...)` call. Also copy its WR-06 immutability docstring and
      its frozen-copy comment style (lines 34-37).
    - `alembic/versions/0005_product_thresholds.py` — **the add_column analog**. Its docstring
      states the rule verbatim: "Native ADD COLUMN, no batch mode … never batch-alter a table whose
      migrations must stay replayable forever."
    - `alembic/versions/0014_drop_product_catalog_cents.py` — the current HEAD. Confirm
      `revision = "0014"` before pointing `down_revision` at it. **This file is the wrong shape to
      copy** (it is a drop_column) — it is relevant only as the head pointer.
    - `app/models.py` (as written in Task 1) — the column definitions this migration must mirror.
      **Read it for reference only; NEVER import it.**
  </read_first>

  <action>
    Create `alembic/versions/0015_customer_contacts.py` with `revision = "0015"`,
    `down_revision = "0014"`, `branch_labels = None`, `depends_on = None`.

    Module docstring in the 0013/0005 voice: state that this adds the CUST-01..05 profile schema
    (`customer_contacts` child table per D-01, `customers.address` per D-02), that both ops are
    **native — no batch mode** (a brand-new table needs `op.create_table`; a nullable column needs
    `op.add_column`; both are natively supported by SQLite, and `render_as_batch=True` in
    `alembic/env.py` only affects autogenerate rendering of ops that genuinely need table rebuilds),
    and restate the **WR-06 immutability rule**: this file must never import application modules;
    the `kind` literals below are a FROZEN copy of `app.models.CONTACT_KINDS`' keys.

    `upgrade()` does exactly three things, in this order:
    1. `op.create_table("customer_contacts", ...)` with columns mirroring the model:
       `id` `sa.String(36)` not null; `customer_id` `sa.String(36)` not null; `kind` `sa.String(20)`
       not null; `value` `sa.String(300)` not null; `label` `sa.String(100)` nullable;
       `created_at` `sa.String(32)` not null; `updated_at` `sa.String(32)` not null.
       Constraints, each name wrapped in `op.f(...)` and **fully expanded**:
       - `sa.PrimaryKeyConstraint("id", name=op.f("pk_customer_contacts"))`
       - `sa.ForeignKeyConstraint(["customer_id"], ["customers.id"], name=op.f("fk_customer_contacts_customer_id_customers"))`
       - `sa.CheckConstraint("kind IN ('phone', 'telegram', 'email', 'social')", name=op.f("ck_customer_contacts_kind_valid"))`
         — **note the expanded name here**, unlike the model's short `"kind_valid"`. `op.f` marks a
         name as already-conventionalized so Alembic does not re-apply the convention; mixing the two
         spellings silently yields `ck_customer_contacts_ck_customer_contacts_kind_valid` or a bare
         unexpanded name. Precede it with a comment: frozen copy of the CONTACT_KINDS keys (WR-06 —
         migrations may duplicate app constants, they must not reference them).
    2. `op.create_index(op.f("ix_customer_contacts_customer_id"), "customer_contacts", ["customer_id"], unique=False)`
    3. `op.add_column("customers", sa.Column("address", sa.String(300), nullable=True))`

    `downgrade()` reverses in exact inverse order: `op.drop_column("customers", "address")`, then
    `op.drop_index(op.f("ix_customer_contacts_customer_id"), table_name="customer_contacts")`, then
    `op.drop_table("customer_contacts")`.

    **Do NOT copy 0013's append-only triggers.** `customer_contacts` is an ordinary mutable table —
    the append-only guarantee is scoped to `operations` and `cash_movements` only (verified:
    `app.db.APPEND_ONLY_TRIGGERS`). The contacts save path is delete-all-and-reinsert (Plan 02) and
    triggers would break it.
  </action>

  <verify>
    <automated>rm -f data/_migcheck.db* &amp;&amp; DB_PATH=data/_migcheck.db uv run alembic upgrade head &amp;&amp; DB_PATH=data/_migcheck.db uv run alembic downgrade -1 &amp;&amp; DB_PATH=data/_migcheck.db uv run alembic upgrade head</automated>
    <automated>DB_PATH=data/_migcheck.db uv run python -c "import sqlite3; c=sqlite3.connect('data/_migcheck.db'); print([r[1] for r in c.execute('PRAGMA table_info(customer_contacts)')]); print([r[1] for r in c.execute('PRAGMA table_info(customers)')]); print([r[0] for r in c.execute(\"SELECT name FROM sqlite_master WHERE type='index' AND tbl_name='customer_contacts'\")]); print(c.execute(\"SELECT sql FROM sqlite_master WHERE name='customer_contacts'\").fetchone()[0])"</automated>
    <automated>rm -f data/_migcheck.db*</automated>
    <automated>uv run ruff check alembic/versions/0015_customer_contacts.py &amp;&amp; uv run ruff format --check alembic/versions/0015_customer_contacts.py</automated>
    <human-check>Copy a real 0014-era `data/myorishop.db`, run `uv run alembic upgrade head` against the copy, confirm exit 0 and that existing customer rows survive with the new `address` column present and NULL. (`21-VALIDATION.md` §Manual-Only Verifications — no migration test harness exists in this repo.)</human-check>
  </verify>

  <acceptance_criteria>
    - The full chain replays on a throwaway DB: `DB_PATH=data/_migcheck.db uv run alembic upgrade head`
      exits 0 from an empty file (replays 0001..0015)
    - `alembic downgrade -1` then `alembic upgrade head` both exit 0 (0015 is reversible)
    - `PRAGMA table_info(customer_contacts)` prints exactly
      `['id', 'customer_id', 'kind', 'value', 'label', 'created_at', 'updated_at']`
    - `PRAGMA table_info(customers)` includes `address`
    - The index list for `customer_contacts` includes `ix_customer_contacts_customer_id`
    - The `sqlite_master` DDL for `customer_contacts` contains the exact string
      `CONSTRAINT ck_customer_contacts_kind_valid CHECK` — and does **not** contain
      `ck_customer_contacts_ck_customer_contacts_kind_valid`
    - `grep -n "from app\|import app" alembic/versions/0015_customer_contacts.py` returns no matches
      (WR-06)
    - `grep -c "batch_alter_table" alembic/versions/0015_customer_contacts.py` returns 0
    - `grep -c "TRIGGER" alembic/versions/0015_customer_contacts.py` returns 0
    - `alembic/versions/0015_customer_contacts.py` contains `revision = "0015"` and
      `down_revision = "0014"`
    - `data/_migcheck.db*` is deleted afterwards; the operator's real `data/myorishop.db` was never
      migrated by this task's automated checks
  </acceptance_criteria>

  <done>
    Migration 0015 replays cleanly from an empty database through the whole chain, is reversible,
    creates `customer_contacts` with the correctly-named CHECK constraint and FK index, adds
    `customers.address`, imports no app modules, and uses no batch mode and no triggers.
  </done>
</task>

<task type="auto">
  <name>Task 3: past_sale fixture — seed a sale at a controlled past date (Wave 0 gap)</name>

  <files>tests/conftest.py, tests/test_customers.py</files>

  <read_first>
    - `tests/conftest.py` — the file being modified. Note the existing `session`, `product`,
      `warehouse`, `batch`, `stocked_product`, `customer` fixtures and the `mobile_client_factory`
      **factory-fixture pattern** (returns an inner `_make` callable) — that is the shape to copy.
    - `app/services/ledger.py` — `record_operation` stamps `created_at=utcnow_iso()` itself and
      cannot be overridden; `next_seq(session, device_id)` is the existing per-device counter helper.
    - `app/db.py` — `APPEND_ONLY_TRIGGERS`: the triggers fire on `BEFORE UPDATE` / `BEFORE DELETE`
      of `operations`, **not on INSERT**. This is why a direct INSERT with an explicit `created_at`
      is the sanctioned way to backdate, and why monkeypatching `utcnow_iso` globally is not needed.
    - `app/models.py` — `Operation` (295-331) and `Sale` (354-370): the exact non-nullable columns a
      direct INSERT must supply (`device_id`, `seq`, `created_at`, `created_by` on both).
    - `app/config.py` — `settings.device_id`, `settings.operator_name`.
    - `tests/test_customers.py` — module docstring conventions (lines 1-14) and the `_only_batch`
      helper.
  </read_first>

  <action>
    `21-VALIDATION.md` §Wave 0 Requirements: no existing fixture seeds a sale at a controlled **past**
    date, and every CUST-07 window test needs one (a sale 2 months ago must fall outside the month
    window but inside the year window). Close that gap now.

    Add a **factory fixture** `past_sale` to `tests/conftest.py`, modeled on `mobile_client_factory`'s
    inner-`_make` shape. Signature of the inner callable:

    `_make(customer, product, *, created_at, qty=1, unit_price_cents=1000, type_="sale", sale=None, batch_id=None) -> tuple[Sale, Operation]`

    Behavior:
    - `created_at` is a UTC ISO-8601 string in `utcnow_iso()`'s exact format
      (`"2026-05-17T12:00:00+00:00"`). It is written verbatim to BOTH the `Sale` and the `Operation`.
      Callers own the value; the fixture never calls `utcnow_iso()`.
    - When `sale is None`, construct a `Sale` with `id=new_id()`, `customer_id=customer.id`,
      `created_at=created_at`, `created_by=settings.operator_name`, `device_id=settings.device_id`;
      `session.add(sale)` then `session.flush()` **before** adding the Operation — `PRAGMA
      foreign_keys=ON` is active, so the header must exist first. When `sale` is passed, reuse it
      (this is how a `return` op reaches the same `Sale`, and how a batch-split purchase writes two
      ops under one `Sale`).
    - Construct an `Operation` with `id=new_id()`, `type=type_`, `product_id=product.id`,
      `qty_delta=-qty` when `type_ == "sale"` and `+qty` when `type_ == "return"`,
      `unit_price_cents=unit_price_cents`, `unit_cost_cents=None`, `sale_id=sale.id`,
      `batch_id=batch_id`, `device_id=settings.device_id`,
      `seq=next_seq(session, settings.device_id)`, `created_at=created_at`,
      `created_by=settings.operator_name`. Then `session.commit()` and return `(sale, op)`.
    - `unit_price_cents=None` must be passable through untouched — the `spend_null_price` test
      (Plan 03) depends on it.

    Import `next_seq` from `app.services.ledger`, `settings` from `app.config`, and add `Operation`,
    `Sale` to the existing `from app.models import ...` line.

    Fixture docstring must state, in the house voice:
    - Why it bypasses `record_operation`: `record_operation` stamps `created_at=utcnow_iso()` and the
      `operations_no_update` trigger ABORTs any later UPDATE, so backdating is only possible at
      INSERT time. The append-only triggers block UPDATE/DELETE, never INSERT — this is safe.
    - **The limitation, stated loudly:** this helper does NOT update the `Product.quantity` /
      `Batch.quantity` projections, because it does not go through the single write path. It is for
      **read-only insight tests** (CUST-06/07/08) only. Never combine it with `rebuild_stock` or any
      stock-invariant assertion, and never use it to test stock.

    Then add ONE smoke test to `tests/test_customers.py` named
    `test_past_sale_fixture_seeds_backdated_op` proving the fixture works: seed a sale at
    `"2026-01-15T10:00:00+00:00"`, assert the returned op's `created_at` equals that string exactly,
    its `qty_delta` is negative, its `sale_id` matches the returned sale, and that
    `purchase_history(session, customer.id)` returns it. Extend the module docstring's selector list
    (lines 10-14) with the Phase 21 selectors rather than adding a second docstring.
  </action>

  <verify>
    <automated>uv run pytest tests/test_customers.py -k past_sale_fixture -x</automated>
    <automated>uv run pytest -q</automated>
    <automated>uv run ruff check tests/ &amp;&amp; uv run ruff format --check tests/</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tests/test_customers.py -k past_sale_fixture -x` exits 0
    - `uv run pytest -q` exits 0 with 755 tests passing (754 baseline + this smoke test); adding a
      fixture must not perturb any existing test
    - `tests/conftest.py` contains `def past_sale` and the returned inner callable accepts the
      keyword `created_at`
    - The seeded `Operation.created_at` equals the string passed in, byte for byte (no
      `utcnow_iso()` call inside the fixture): `grep -c "utcnow_iso" tests/conftest.py` returns 0
    - Calling the factory with `type_="return"` produces an `Operation` with positive `qty_delta`;
      calling it with `type_="sale"` produces negative `qty_delta`
    - Passing `unit_price_cents=None` stores NULL rather than raising
    - Passing an existing `sale` reuses it — two calls with the same `sale` produce two Operations
      sharing one `sale_id` (the batch-split shape Plan 03's `favorites_batch_split` test needs)
    - The `past_sale` docstring states that it does not update `Product.quantity` and is read-only-test-only
    - `uv run ruff check tests/` and `uv run ruff format --check tests/` both exit 0
  </acceptance_criteria>

  <done>
    `tests/conftest.py` exposes a `past_sale` factory fixture that seeds a `Sale` + `Operation` pair
    at any caller-supplied UTC timestamp, supports `sale`/`return` types, shared sale headers, and
    NULL prices, and a smoke test proves it. `21-VALIDATION.md`'s only Wave 0 gap is closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → FastAPI route | Untrusted form/query input crosses here. **Not exercised by this plan** — Plan 01 is schema + test infrastructure only, zero routes. |
| application → SQLite file | All DDL crosses here via Alembic. The operator's real `data/myorishop.db` is a trust asset (irreplaceable local data, no server copy). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | Tampering | `customer_contacts.value` / `label`, `customers.address` column widths | mitigate | Declare `String(300)` / `String(100)` widths here so the service-layer WR-05 guards (Plan 02) have a column to mirror. SQLite silently truncates an overlong value; PostgreSQL hard-errors — the width is the contract the guard enforces. |
| T-21-09 | Tampering | `customer_contacts.kind` | mitigate | Named `CheckConstraint` `ck_customer_contacts_kind_valid` as DB-level defence-in-depth (D-01). The PRIMARY gate is the `CONTACT_KINDS` Python allow-list validated in the service (Plan 02) — matching the house convention recorded at `models.py:32-33` (no CHECK on `operations.type`). |
| T-21-10 | Tampering | `operations` append-only ledger vs. the new `past_sale` fixture | mitigate | The fixture INSERTs only; it never UPDATEs or DELETEs an `operations` row, so `operations_no_update`/`operations_no_delete` stay intact. Its docstring states the read-only-tests-only limitation so it can never be mistaken for a stock-write path. |
| T-21-11 | Denial of Service | Operator's real `data/myorishop.db` during migration verification | mitigate | All automated migration checks run against a throwaway `DB_PATH=data/_migcheck.db`, never the real DB. The real-DB upgrade is a separate `<human-check>` on a **copy**, per `21-VALIDATION.md` §Manual-Only Verifications. |
| T-21-12 | Tampering | Migration 0015 replayability | mitigate | WR-06: the file imports zero app modules; the `kind` literals are a frozen copy. Enforced by the `grep -n "from app\|import app"` acceptance criterion. |
| T-21-05 | Information Disclosure | Contact PII at rest (`customer_contacts` is the most PII-dense table this app will have) | accept | Single local operator, no auth in v1 by explicit project decision (CLAUDE.md: "Do not add auth machinery in v1"). Protected by the machine's own login. **Flagged forward:** when v2 multi-operator sync lands, this is the row set that most needs an access-control story. |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | **This phase installs zero external packages** (`21-RESEARCH.md` §Package Legitimacy Audit: not applicable — no slopsquatting surface). Every library used is an already-pinned, shipped dependency. No `uv add` appears in any task. |
</threat_model>

<verification>
- `uv run pytest -q` → 755 passing (754 baseline + the fixture smoke test)
- `uv run python -c "import app.models"` → exits 0
- Migration chain replays from empty on a throwaway DB, and is reversible
- `uv run ruff check` + `uv run ruff format --check` → clean
</verification>

<success_criteria>
- `CONTACT_KINDS`, `CustomerContact`, and `Customer.address` exist in `app/models.py` and the full
  suite still collects and passes
- The CHECK constraint renders as `ck_customer_contacts_kind_valid` in the emitted DDL
- Migration 0015 (`down_revision = "0014"`) creates the table + index + column natively, with no
  batch mode, no triggers, and no app imports
- `tests/conftest.py` exposes a working `past_sale` factory fixture
- `app/services/export.py` is untouched (CSV drift deferred, by decision)
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

**New symbols in `app/models.py`:**
- `CONTACT_KINDS: dict[str, str]` — `{"phone": "Телефон", "telegram": "Telegram", "email": "Email", "social": "Соцсеть"}`
- `class CustomerContact(Base)` — `__tablename__ = "customer_contacts"`
- `CustomerContact.id: Mapped[str]` — `String(36)` PK, `default=new_id`
- `CustomerContact.customer_id: Mapped[str]` — FK `fk_customer_contacts_customer_id_customers`, indexed
- `CustomerContact.kind: Mapped[str]` — `String(20)`
- `CustomerContact.value: Mapped[str]` — `String(300)`
- `CustomerContact.label: Mapped[str | None]` — `String(100)`, ships unused (no UI this phase)
- `CustomerContact.created_at: Mapped[str]` — `String(32)`
- `CustomerContact.updated_at: Mapped[str]` — `String(32)`
- Constraint `ck_customer_contacts_kind_valid` (model spelling: `name="kind_valid"`)
- Index `ix_customer_contacts_customer_id`
- `Customer.address: Mapped[str | None]` — `String(300)`

**New file:** `alembic/versions/0015_customer_contacts.py` — migration revision id **`0015`**, `down_revision = "0014"`

**New DB objects:** table `customer_contacts`; column `customers.address`; index `ix_customer_contacts_customer_id`; constraints `pk_customer_contacts`, `fk_customer_contacts_customer_id_customers`, `ck_customer_contacts_kind_valid`

**New test fixture in `tests/conftest.py`:** `past_sale` — factory fixture returning
`_make(customer, product, *, created_at, qty=1, unit_price_cents=1000, type_="sale", sale=None, batch_id=None) -> tuple[Sale, Operation]`

**New test in `tests/test_customers.py`:** `test_past_sale_fixture_seeds_backdated_op`

**New routes:** none. **New templates:** none. **New CSS rules:** none. **New CLI flags:** none.
</artifacts_produced>

<validation_binding>
Rows bound from `21-VALIDATION.md` §Per-Task Verification Map:

| Task ID | Requirement | Test Type | Automated Command |
|---------|-------------|-----------|-------------------|
| 21-01-02 | CUST-01..05 (migration smoke) | smoke | `DB_PATH=data/_migcheck.db uv run alembic upgrade head` (+ `<human-check>` on a real 0014-era DB copy — VALIDATION §Manual-Only) |
| 21-01-03 | CUST-07 (Wave 0 fixture gap) | infra | `uv run pytest tests/test_customers.py -k past_sale_fixture -x` |
</validation_binding>

<output>
Create `.planning/phases/21-customer-profiles-purchase-insights/21-01-SUMMARY.md` when done
</output>

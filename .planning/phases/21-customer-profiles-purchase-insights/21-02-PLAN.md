---
phase: 21-customer-profiles-purchase-insights
plan: 02
type: execute
wave: 1
depends_on: ["21-01"]
files_modified:
  - app/services/customers.py
  - tests/test_customers.py
autonomous: true
requirements: [CUST-01, CUST-02, CUST-03, CUST-04, CUST-05]

must_haves:
  truths:
    - "Saving a customer with three phone numbers stores three CustomerContact rows with kind='phone'"
    - "Saving a customer with values for all four kinds stores all four kinds independently"
    - "A physical address saved on a customer profile persists and reads back"
    - "Re-saving a customer replaces its contacts rather than duplicating them"
    - "A blank contact row never creates an empty CustomerContact"
    - "An overlong contact value or address is rejected with a Russian error, nothing is written"
  artifacts:
    - path: "app/services/customers.py"
      provides: "contacts + address write/read path (D-01, D-02)"
      exports: ["create_customer", "update_customer", "contacts_by_kind", "CONTACT_VALUE_TOO_LONG_ERROR", "ADDRESS_TOO_LONG_ERROR"]
      contains: "def contacts_by_kind"
  key_links:
    - from: "app/services/customers.py _replace_contacts"
      to: "app.models.CustomerContact"
      via: "delete-all-then-reinsert inside the caller's transaction"
      pattern: "CustomerContact"
    - from: "app/services/customers.py _validate_contacts"
      to: "app.models.CONTACT_KINDS"
      via: "allow-list membership check before any write"
      pattern: "CONTACT_KINDS"
---

<objective>
Build the CUST-01..05 write and read path in the service layer: contact rows (D-01) and the
physical address (D-02), validated and persisted inside the existing `create_customer` /
`update_customer` transaction.

Purpose: `app/routes/customers.py:1` states the house rule — "thin routes, all writes in
app/services/customers.py". Everything the form (Plan 04) and the detail page (Plan 05) do with
contacts has to exist here first, with its own service-level tests, before any HTML is written.

Output: `contacts` and `address` keyword arguments on both write functions, `_validate_contacts`,
`_replace_contacts`, `contacts_by_kind`, two new RU error constants, and the CUST-01..05
service-level test contract.

**Out of scope, by decision — do not touch:** `app/services/export.py`. Its
`stream_customers_csv` docstring calls itself a "Full customer profile dump" and that claim goes
stale here, but contacts are 1-to-many and do not fit its flat CSV shape (`21-RESEARCH.md` Open
Question 2). Recorded as a deferred item.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-customer-profiles-purchase-insights/21-CONTEXT.md
@.planning/phases/21-customer-profiles-purchase-insights/21-PATTERNS.md
@.planning/phases/21-customer-profiles-purchase-insights/21-VALIDATION.md
@.planning/phases/21-customer-profiles-purchase-insights/21-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Address column wiring + WR-05 length guards (CUST-05)</name>

  <files>app/services/customers.py, tests/test_customers.py</files>

  <read_first>
    - `app/services/customers.py` — the file being modified. Read in full. Note: the RU error
      constants at lines 18-21 (module-level, UPPER_SNAKE, `_ERROR` suffix, copy ends in a period);
      the WR-05 comment + `_NAME_MAX_LEN` / `_SURNAME_MAX_LEN` / `_CONSULTANT_NUMBER_MAX_LEN`
      constants at lines 23-29; `_validate_lengths` (36-45); `create_customer` (48-78) and
      `update_customer` (81-112) — the `(obj | None, dict[str, str])` return contract, the
      `.strip()`-everything habit, the `x or None` before assign, and the single `session.commit()`
      at the end.
    - `app/models.py` — `Customer.address` as landed by Plan 01: `String(300)`, nullable.
    - `.planning/phases/21-customer-profiles-purchase-insights/21-UI-SPEC.md` §Copywriting Contract
      — the exact RU error copy this task must use, verbatim.
    - `app/routes/sales.py` around line 355 — the OTHER `create_customer` caller (the sale form's
      quick-create). It must keep working untouched, which is why the new arguments are
      keyword-only with defaults.
  </read_first>

  <action>
    Wire `address` (CUST-05 / D-02) through the customer write path.

    Add the module-level constant `_ADDRESS_MAX_LEN = 300` next to the existing WR-05 constants at
    lines 27-29, mirroring the declared `Customer.address` column width. Extend the existing WR-05
    comment block rather than writing a second one.

    Add the module-level RU error constant `ADDRESS_TOO_LONG_ERROR`, placed with the others at lines
    18-21, with the copy taken **verbatim** from `21-UI-SPEC.md`:
    `"Адрес слишком длинный — не больше 300 символов."`

    Extend `_validate_lengths` to take `address: str` and set `errors["address"] = ADDRESS_TOO_LONG_ERROR`
    when `len(address) > _ADDRESS_MAX_LEN`. Keep it a shared guard for both write functions — do not
    fork it.

    Add `address: str = ""` as a **keyword-only argument with a default** to both `create_customer`
    and `update_customer`. The default is load-bearing: `app/routes/sales.py:355` calls
    `create_customer` for the sale form's quick-create and must keep working with no change (Phase 22
    owns that surface). In both functions: `address = address.strip()` alongside the existing strips,
    pass it to `_validate_lengths`, and assign `customer.address = address or None` following the
    existing `surname or None` habit. Do **not** add `address` to `_search_lc` — search stays
    "name surname consultant" (LIST/CST convention, unchanged).

    Add service-level tests to `tests/test_customers.py` (bare names — these are not route tests):
    - `test_create_customer_stores_address` — CUST-05: create with an address, assert
      `customer.address` reads back exactly.
    - `test_update_customer_changes_address` — CUST-05: update an existing customer's address.
    - `test_customer_address_blank_stores_none` — CUST-05: `address=""` and `address="   "` both
      store `None`, not `""` (the `or None` contract).
    - `test_customer_address_too_long_rejected` — WR-05: a 301-char address returns
      `(None, {"address": ADDRESS_TOO_LONG_ERROR})` and writes nothing (assert the `Customer` row
      count is unchanged, mirroring `test_create_customer_requires_name`'s before/after shape).
    All four names contain `address`, so they bind to `-k address`.
  </action>

  <verify>
    <automated>uv run pytest tests/test_customers.py -k address -x</automated>
    <automated>uv run pytest tests/test_customers.py -q</automated>
    <automated>uv run ruff check app/services/customers.py tests/test_customers.py &amp;&amp; uv run ruff format --check app/services/customers.py tests/test_customers.py</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tests/test_customers.py -k address -x` exits 0 and collects 4 tests
    - `uv run pytest tests/test_customers.py -q` exits 0 — every pre-existing customer test still
      passes unchanged
    - `app/services/customers.py` contains the exact literal
      `ADDRESS_TOO_LONG_ERROR = "Адрес слишком длинный — не больше 300 символов."`
    - `app/services/customers.py` contains `_ADDRESS_MAX_LEN = 300`
    - `create_customer` and `update_customer` both accept `address` as a keyword argument defaulting
      to `""`: `uv run python -c "import inspect; from app.services.customers import create_customer, update_customer; print(inspect.signature(create_customer)); print(inspect.signature(update_customer))"` shows `address: str = ''` on both
    - `app/routes/sales.py` is NOT modified: `git diff --name-only` does not list it
    - `grep -n "_search_lc" app/services/customers.py` shows `address` is not passed into it
    - Behavior: creating a customer with `address="  "` yields `customer.address is None`
    - Behavior: creating a customer with a 301-character address returns `(None, errors)` with
      `errors["address"] == ADDRESS_TOO_LONG_ERROR` and adds zero `Customer` rows
    - `uv run ruff check` and `uv run ruff format --check` both exit 0
  </acceptance_criteria>

  <done>
    `address` round-trips through `create_customer`/`update_customer`, blanks normalize to `None`,
    an overlong address is rejected in the service with the exact UI-SPEC Russian copy before any
    write, and the sale form's existing `create_customer` call site is untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Contact validation + full-replace persistence (CUST-01..04)</name>

  <files>app/services/customers.py, tests/test_customers.py</files>

  <read_first>
    - `app/services/customers.py` — as modified by Task 1. Re-read `create_customer` /
      `update_customer` for the transaction shape: **one** `session.commit()` at the end. The new
      contact write must slot INSIDE that transaction and issue no commit of its own.
    - `app/models.py` — `CustomerContact` and `CONTACT_KINDS` as landed by Plan 01.
    - `app/services/ledger.py` lines 74-75 — `raise ValueError(f"unknown operation type: {type_!r}")`:
      the house precedent for rejecting a value that is NOT operator-reachable (programmer error, not
      a form error).
    - `app/services/customers.py` lines 148-153 — the `_SORT_MAP` allow-list comment citing T-14-18:
      the in-file precedent for gating a closed value set.
    - `.planning/phases/21-customer-profiles-purchase-insights/21-UI-SPEC.md` §Copywriting Contract
      (the exact RU error copy) and §"Interaction & Layout Decisions" item 6 (blank rows are
      discarded server-side — the form always renders one always-present blank row, which must never
      create an empty `CustomerContact`).
    - `app/db.py` — `APPEND_ONLY_TRIGGERS` cover `operations` and `cash_movements` only.
      `customer_contacts` is an ordinary mutable table, which is what makes delete-all-and-reinsert
      legal here.
  </read_first>

  <action>
    Implement contact validation and persistence (D-01), full-replace semantics.

    **Constants.** Add `_CONTACT_VALUE_MAX_LEN = 300` next to the other WR-05 constants (mirrors the
    `CustomerContact.value` `String(300)` column width). Add the RU error constant
    `CONTACT_VALUE_TOO_LONG_ERROR` with the copy taken **verbatim** from `21-UI-SPEC.md`:
    `"Значение слишком длинное — не больше 300 символов."`

    **The `contacts` argument shape.** Both write functions gain a keyword-only
    `contacts: dict[str, list[str]] | None = None`, mapping a `CONTACT_KINDS` key to that kind's
    submitted raw values. Document the two-state contract explicitly in both docstrings:
    - `contacts is None` → **contacts are left untouched** (existing rows preserved). This is what
      keeps `app/routes/sales.py:355`'s quick-create call working unchanged.
    - `contacts` is a dict → **full replace**: every existing `CustomerContact` for that customer is
      deleted and the supplied non-blank values are inserted. A dict that omits a kind therefore
      clears that kind. The form (Plan 04) always posts all four keys, so this is correct there;
      the docstring must say so, since it is the sharp edge.

    **`_validate_contacts(contacts, errors) -> None`** — private, runs BEFORE any write:
    - For each key: if the key is not in `CONTACT_KINDS`, `raise ValueError(f"unknown contact kind: {kind!r}")`.
      This is NOT an operator-reachable path (the route binds four fixed `Form` params), so it is a
      programmer error and gets the `record_operation` treatment (ledger.py:74-75 precedent), not an
      errors-dict entry.
    - For each value: `.strip()` it; **skip blanks entirely** (they are discarded, never an error —
      UI-SPEC Interaction 6's always-present blank row depends on this). If a stripped value is
      longer than `_CONTACT_VALUE_MAX_LEN`, set `errors[kind] = CONTACT_VALUE_TOO_LONG_ERROR`.
      Key the error by **kind**, not by row index — UI-SPEC's markup has one `{% if errors.phone %}`
      slot per section, not per row.

    **`_replace_contacts(session, customer_id, contacts) -> None`** — private, `commit=False`
    semantics (never commits; the caller owns the transaction, per the PATTERNS Analog F write-path
    shape). Delete every existing `CustomerContact` where `customer_id == customer_id` via a
    portable ORM/Core construct, then insert one `CustomerContact(id=new_id(), customer_id=...,
    kind=kind, value=stripped_value, label=None)` per non-blank stripped value, preserving the
    submitted order within each kind. **`label` is always `None` this phase** — the column ships
    unused (Plan 01, RESEARCH Open Question 1). Deletion is legal because `customer_contacts` is an
    ordinary mutable table; the append-only triggers cover only `operations` and `cash_movements`.

    **Wiring.** In both `create_customer` and `update_customer`: call `_validate_contacts` in the
    same validation block as `_validate_lengths`, and return `(None, errors)` before any write if
    errors accumulated — the existing early-return shape. On the success path, call
    `_replace_contacts` **after** the customer row is added/updated and **before** the single
    `session.commit()`, only when `contacts is not None`. In `create_customer` the customer must be
    added and flushed before contacts are inserted (`PRAGMA foreign_keys=ON` is active, so the parent
    row must exist).

    Import `CONTACT_KINDS` and `CustomerContact` from `app.models` into the existing import line, and
    `delete` from `sqlalchemy` if you use a Core delete.

    **Tests** in `tests/test_customers.py`, service level (bare names):
    - `test_contacts_validation_discards_blank_rows` — CUST-01..04: `contacts={"phone": ["+7900", "", "   "]}`
      creates exactly ONE `CustomerContact`.
    - `test_contacts_validation_rejects_unknown_kind` — `contacts={"fax": ["123"]}` raises `ValueError`;
      assert with `pytest.raises(ValueError)`.
    - `test_contacts_validation_value_too_long` — a 301-char value returns
      `(None, {"phone": CONTACT_VALUE_TOO_LONG_ERROR})` and writes zero `CustomerContact` rows.
    - `test_contacts_replace_does_not_duplicate` — save two phones, re-save with one phone; assert
      exactly one `CustomerContact` row remains for that customer (full-replace, not append).
    - `test_contacts_replace_none_leaves_contacts_untouched` — save two phones, then
      `update_customer(..., contacts=None)`; assert both rows survive.
    Names contain `contacts_validation` / `contacts_replace`, binding to those `-k` selectors.
  </action>

  <verify>
    <automated>uv run pytest tests/test_customers.py -k contacts_validation -x</automated>
    <automated>uv run pytest tests/test_customers.py -k contacts_replace -x</automated>
    <automated>uv run pytest tests/test_customers.py -q</automated>
    <automated>uv run ruff check app/services/customers.py tests/test_customers.py &amp;&amp; uv run ruff format --check app/services/customers.py tests/test_customers.py</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tests/test_customers.py -k contacts_validation -x` exits 0 and collects 3 tests
    - `uv run pytest tests/test_customers.py -k contacts_replace -x` exits 0 and collects 2 tests
    - `uv run pytest tests/test_customers.py -q` exits 0 — the whole existing customer suite is
      still green
    - `app/services/customers.py` contains the exact literal
      `CONTACT_VALUE_TOO_LONG_ERROR = "Значение слишком длинное — не больше 300 символов."`
      and `_CONTACT_VALUE_MAX_LEN = 300`
    - `app/services/customers.py` contains `def _validate_contacts` and `def _replace_contacts`
    - Behavior: `contacts={"phone": ["+7900", "", "   "]}` → exactly 1 `CustomerContact` row
    - Behavior: `contacts={"fax": ["1"]}` raises `ValueError` (not an errors-dict entry)
    - Behavior: a 301-char value returns `(None, errors)` with `errors["phone"]` set and adds zero
      `CustomerContact` rows
    - Behavior: saving `["a", "b"]` then re-saving `["a"]` leaves exactly 1 row for that customer
    - Behavior: `update_customer(..., contacts=None)` after saving 2 phones leaves 2 rows
    - Behavior: every inserted `CustomerContact.label` is `None`
    - `_replace_contacts` issues no commit of its own: `grep -n "session.commit" app/services/customers.py`
      shows commits only inside `create_customer` and `update_customer`, one each
    - `uv run ruff check` and `uv run ruff format --check` both exit 0
  </acceptance_criteria>

  <done>
    Contacts validate against the `CONTACT_KINDS` allow-list before any write, blanks are silently
    discarded, an overlong value is rejected per-kind with the exact UI-SPEC copy, and saving
    replaces rather than duplicates — all inside the existing single-commit transaction.
  </done>
</task>

<task type="auto">
  <name>Task 3: contacts_by_kind read + per-kind persistence contract (CUST-01..04)</name>

  <files>app/services/customers.py, tests/test_customers.py</files>

  <read_first>
    - `app/services/customers.py` — as modified by Tasks 1-2.
    - `app/services/customers.py` lines 202-214 — `purchase_history`: the house read shape
      (`select(...)` + explicit manual join, returning a plain `list[dict]`, deterministic
      `order_by`). `contacts_by_kind` follows the same conventions minus the join.
    - `app/models.py` — `CustomerContact`, `CONTACT_KINDS`. There is no `relationship()` anywhere in
      `app/`; the FK is queried explicitly.
    - `.planning/phases/21-customer-profiles-purchase-insights/21-UI-SPEC.md` §"Interaction & Layout
      Decisions" items 2, 6 and 12 — the two consumers of this read: the form (Plan 04) needs every
      kind present so it can render one blank row for empty kinds; the detail page (Plan 05) omits
      kinds with zero values entirely.
  </read_first>

  <action>
    Add the public read `contacts_by_kind(session, customer_id) -> dict[str, list[CustomerContact]]`.

    Contract, spelled out in the docstring:
    - **Every `CONTACT_KINDS` key is always present** in the returned dict, mapping to a possibly-empty
      list. Both consumers depend on this: the form renders one blank row for an empty kind
      (UI-SPEC Interaction 6), the detail page omits an empty kind (UI-SPEC Interaction 12) — neither
      should have to guard for a missing key.
    - Key insertion order follows `CONTACT_KINDS` order (phone, telegram, email, social) so the
      detail page's section order is stable without the template sorting.
    - Within each kind, rows are ordered by `(CustomerContact.created_at, CustomerContact.id)` — a
      deterministic, reproducible order. `created_at` alone is not sufficient: `utcnow_iso()` has
      **one-second** resolution (`timespec="seconds"`), and a full-replace inserts every row of a
      save inside the same second, so ties are routine. `id` is the stable tie-break.
    - **One query, not one per kind.** Issue a single `select(CustomerContact).where(customer_id == ...)`
      ordered as above, then bucket in Python. A loop issuing a query per kind is the N+1 shape this
      codebase avoids — and with no `relationship()` there is no lazy loader to trigger one, so
      keeping it flat is entirely on this function.

    Portability: all values bound, no SQLite-specific SQL, no `strftime`.

    **Tests** in `tests/test_customers.py`, service level (bare names) — the CUST-01..04 persistence
    contract, one per requirement:
    - `test_contacts_phone_multiple_values_persist` — CUST-01: three phones save and read back in
      submitted order under `kind="phone"`.
    - `test_contacts_telegram_multiple_values_persist` — CUST-02: two Telegram handles.
    - `test_contacts_email_multiple_values_persist` — CUST-03: two emails.
    - `test_contacts_social_multiple_values_persist` — CUST-04: two free-form social links,
      including one with a URL containing a query string, to prove no normalization/mangling occurs
      (values are stored verbatim — no format validation, per the roadmap's Out of Scope note).
    - `test_contacts_by_kind_returns_all_kinds_for_bare_customer` — a customer with zero contacts
      returns a dict with all four keys, each an empty list (never a `KeyError` for a consumer).
    - `test_contacts_all_kinds_are_independent` — save values for all four kinds at once; assert each
      kind reads back exactly its own values and no cross-contamination.
    Names contain `contacts_phone` / `contacts_telegram` / `contacts_email` / `contacts_social`,
    binding to those `-k` selectors.
  </action>

  <verify>
    <automated>uv run pytest tests/test_customers.py -k contacts_phone -x</automated>
    <automated>uv run pytest tests/test_customers.py -k contacts_telegram -x</automated>
    <automated>uv run pytest tests/test_customers.py -k contacts_email -x</automated>
    <automated>uv run pytest tests/test_customers.py -k contacts_social -x</automated>
    <automated>uv run pytest -q</automated>
    <automated>uv run ruff check app/services/customers.py tests/test_customers.py &amp;&amp; uv run ruff format --check app/services/customers.py tests/test_customers.py</automated>
  </verify>

  <acceptance_criteria>
    - Each of `-k contacts_phone`, `-k contacts_telegram`, `-k contacts_email`, `-k contacts_social`
      exits 0 and collects at least 1 test
    - `uv run pytest -q` exits 0 (full suite — this is the Wave 1 merge gate; `app/models.py` is
      imported by everything and a schema mistake must not survive the wave)
    - `app/services/customers.py` contains `def contacts_by_kind`
    - Behavior: `contacts_by_kind(session, <customer with no contacts>)` returns a dict whose keys
      are exactly `["phone", "telegram", "email", "social"]` in that order, each mapping to `[]`
    - Behavior: saving 3 phones then reading returns 3 `CustomerContact` objects in submitted order
    - Behavior: saving values for all 4 kinds returns each kind's own values only
    - Behavior: a social link containing `?utm_source=x&y=1` reads back byte-identical (stored
      verbatim, no normalization)
    - `contacts_by_kind` issues exactly ONE SELECT against `customer_contacts` per call — assert by
      inspecting the function body: it contains exactly one `select(CustomerContact)` and no
      `for` loop wrapping a `session.execute` / `session.scalars` call
    - `grep -c "strftime" app/services/customers.py` returns 0
    - `uv run ruff check` and `uv run ruff format --check` both exit 0
  </acceptance_criteria>

  <done>
    `contacts_by_kind` returns every kind (empty lists included) in a stable, deterministic order
    from a single query, and CUST-01..04 each have a passing service-level persistence test. The
    full 754+ suite is green at the Wave 1 merge.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route → service (`app/services/customers.py`) | Operator-supplied form values (`contacts` arrays, `address`) cross here. **This is the phase's validation boundary** — `app/routes/customers.py:1` fixes the house rule that all validation lives in the service, never the route. |
| service → SQLite | All contact/address writes cross here as bound parameters via SQLAlchemy Core constructs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03 | Tampering | SQL injection via `contacts` values / `address` | mitigate | SQLAlchemy Core/ORM constructs only — every value renders as a bound parameter. **Never** f-string a value into a query. Enforced by the "no `strftime`, no interpolation" criterion and by Plan 03's compiled-SQL portability guard. |
| T-21-04 | Tampering | Overlong `value` / `address` (V5 Input Validation) | mitigate | Service-layer guards against `_CONTACT_VALUE_MAX_LEN = 300` / `_ADDRESS_MAX_LEN = 300`, module constants mirroring the declared column widths (WR-05 precedent, `customers.py:23-29`). SQLite silently truncates an overlong value; PostgreSQL hard-errors — rejecting here keeps the future migration safe. Errors are RU copy, keyed per kind. |
| T-21-09 | Tampering | Unknown `kind` reaching a write (V5 Input Validation) | mitigate | `_validate_contacts` rejects any key not in the `CONTACT_KINDS` allow-list with `ValueError` **before any write** — the `_SORT_MAP` / T-14-18 precedent (`customers.py:150-153`), and the primary gate the `ck_customer_contacts_kind_valid` CHECK backstops. `ValueError` (not an errors-dict entry) is correct because the route binds four fixed `Form` params, so an unknown kind is a programmer error, not operator input — matching `record_operation`'s treatment (`ledger.py:74-75`). |
| T-21-13 | Tampering | Partial write on validation failure (integrity) | mitigate | Validation runs entirely before any write; both functions return `(None, errors)` on the early-return path and `_replace_contacts` never commits — the caller owns the single transaction. Tests assert zero rows written on every rejection path. |
| T-21-14 | Tampering | Orphaned / dangling `customer_contacts` rows | mitigate | `PRAGMA foreign_keys=ON` is enforced (`app/db.py`) and guarded by `tests/test_pragmas.py`; the FK makes orphans impossible. `create_customer` flushes the parent before inserting children. |
| T-21-01 | Tampering / Elevation | Stored XSS via the free-form social link (CUST-04) | transfer | **The phase's one real threat surface.** Values are stored verbatim here by design — no format validation (roadmap Out of Scope). The control is at the render boundary and is owned by Plan 05: autoescaped plain text, **no `<a href>`**, no `| safe`. Autoescape does NOT stop `javascript:` inside an href, so plain-text rendering is the control, not a cosmetic choice (`21-UI-SPEC.md` §Security Decision). This plan's `test_contacts_social_multiple_values_persist` deliberately proves storage is verbatim, so the render-side test in Plan 05 is the one that closes it. |
| T-21-05 | Information Disclosure | Contact PII at rest | accept | Single local operator, no auth in v1 by explicit project decision. Flagged forward for the v2 sync milestone. |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed — no `uv add` in any task (`21-RESEARCH.md`: "this phase installs zero external packages"). |
</threat_model>

<verification>
- `uv run pytest tests/test_customers.py -k address -x` → green (4 tests)
- `uv run pytest tests/test_customers.py -k contacts_validation -x` → green (3 tests)
- `uv run pytest tests/test_customers.py -k contacts_replace -x` → green (2 tests)
- `uv run pytest tests/test_customers.py -k contacts_phone -x` / `contacts_telegram` / `contacts_email` / `contacts_social` → all green
- `uv run pytest -q` → full suite green (Wave 1 merge gate)
- `uv run ruff check` + `uv run ruff format --check` → clean
</verification>

<success_criteria>
- CUST-01..04: multiple values per kind persist independently and read back in a stable order
- CUST-05: an address persists, blanks normalize to `None`, overlong is rejected with RU copy
- Re-saving replaces contacts; blank rows never become rows; unknown kinds raise
- `app/routes/sales.py` and `app/services/export.py` are untouched
- The full suite is green
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

**New symbols in `app/services/customers.py`:**
- `ADDRESS_TOO_LONG_ERROR: str` — `"Адрес слишком длинный — не больше 300 символов."`
- `CONTACT_VALUE_TOO_LONG_ERROR: str` — `"Значение слишком длинное — не больше 300 символов."`
- `_ADDRESS_MAX_LEN: int` = 300
- `_CONTACT_VALUE_MAX_LEN: int` = 300
- `_validate_contacts(contacts: dict[str, list[str]], errors: dict[str, str]) -> None` — private
- `_replace_contacts(session: Session, customer_id: str, contacts: dict[str, list[str]]) -> None` — private, never commits
- `contacts_by_kind(session: Session, customer_id: str) -> dict[str, list[CustomerContact]]` — public read, all 4 kinds always present

**Changed signatures in `app/services/customers.py`:**
- `create_customer(session, *, name, surname, consultant_number, address: str = "", contacts: dict[str, list[str]] | None = None) -> tuple[Customer | None, dict[str, str]]`
- `update_customer(session, customer_id, *, name, surname, consultant_number, address: str = "", contacts: dict[str, list[str]] | None = None) -> tuple[Customer | None, dict[str, str]]`
- `_validate_lengths(name, surname, consultant_number, address, errors) -> None` — gains `address`

**New error dict keys:** `errors["address"]`, `errors["phone"]`, `errors["telegram"]`, `errors["email"]`, `errors["social"]`

**New tests in `tests/test_customers.py`:** `test_create_customer_stores_address`, `test_update_customer_changes_address`, `test_customer_address_blank_stores_none`, `test_customer_address_too_long_rejected`, `test_contacts_validation_discards_blank_rows`, `test_contacts_validation_rejects_unknown_kind`, `test_contacts_validation_value_too_long`, `test_contacts_replace_does_not_duplicate`, `test_contacts_replace_none_leaves_contacts_untouched`, `test_contacts_phone_multiple_values_persist`, `test_contacts_telegram_multiple_values_persist`, `test_contacts_email_multiple_values_persist`, `test_contacts_social_multiple_values_persist`, `test_contacts_by_kind_returns_all_kinds_for_bare_customer`, `test_contacts_all_kinds_are_independent`

**New routes:** none. **New templates:** none. **New CSS rules:** none. **New migration:** none.
</artifacts_produced>

<validation_binding>
Rows bound from `21-VALIDATION.md` §Per-Task Verification Map:

| Task ID | Requirement | Threat Ref | Test Type | Automated Command |
|---------|-------------|------------|-----------|-------------------|
| 21-02-01 | CUST-05 | T-21-04 (V5) | integration | `uv run pytest tests/test_customers.py -k address -x` |
| 21-02-02 | CUST-01..04 (blank rows discarded; kind allow-list) | T-21-09 (V5) | unit | `uv run pytest tests/test_customers.py -k contacts_validation -x` |
| 21-02-02 | CUST-01..04 (re-saving replaces, not duplicates) | — | integration | `uv run pytest tests/test_customers.py -k contacts_replace -x` |
| 21-02-03 | CUST-01 | — | integration | `uv run pytest tests/test_customers.py -k contacts_phone -x` |
| 21-02-03 | CUST-02 | — | integration | `uv run pytest tests/test_customers.py -k contacts_telegram -x` |
| 21-02-03 | CUST-03 | — | integration | `uv run pytest tests/test_customers.py -k contacts_email -x` |
| 21-02-03 | CUST-04 (storage half; render half is 21-05-03) | T-21-01 | integration | `uv run pytest tests/test_customers.py -k contacts_social -x` |
</validation_binding>

<output>
Create `.planning/phases/21-customer-profiles-purchase-insights/21-02-SUMMARY.md` when done
</output>

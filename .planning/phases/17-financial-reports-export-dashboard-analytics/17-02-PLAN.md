---
phase: 17-financial-reports-export-dashboard-analytics
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - app/routes/finance.py
  - app/templates/pages/finance.html
  - app/templates/partials/finance_tiles.html
  - app/static/style.css
  - tests/test_finance_reports.py
autonomous: true
requirements: [FIN-10, FIN-11, FIN-12]

must_haves:
  truths:
    - "Operator sees gross profit for the selected period as a tile on /finance"
    - "Operator sees net profit for the same period, with an always-visible cash-outflow caveat line"
    - "Operator sees stock value at purchase cost and at sale price, marked point-in-time, with unknown-price caveats"
    - "Changing the light period selector refreshes only the gross/net tiles; the stock tile stays point-in-time; balance/forms/history are untouched"
  artifacts:
    - path: "app/routes/finance.py"
      provides: "/finance/metrics tiles endpoint + tiles context on /finance"
      contains: "/finance/metrics"
    - path: "app/templates/partials/finance_tiles.html"
      provides: "gross/net/stock metric tiles with caveats"
      contains: "metric-tile"
    - path: "app/static/style.css"
      provides: ".metric-grid / .metric-tile structural CSS"
      contains: ".metric-tile"
  key_links:
    - from: "app/templates/pages/finance.html"
      to: "/finance/metrics"
      via: "period_filter light selector targeting #finance-metrics"
      pattern: "finance-metrics"
    - from: "app/routes/finance.py::finance_metrics"
      to: "sales_profit_report + cash_expense_total + stock_valuation"
      via: "period → gross, net = gross + expense, point-in-time valuation"
      pattern: "cash_expense_total"
---

<objective>
Add the three dashboard metric tiles (gross profit FIN-10, net profit FIN-11, stock valuation FIN-12) plus a light period selector to the existing desktop `/finance` page, backed by a new `/finance/metrics` HX-refresh endpoint. The Phase 15-16 balance, withdraw/deposit forms, and movement history on `/finance` stay untouched (D-04 regression guard).

Purpose: Deliver the "business-health at a glance" dashboard (D-04) reusing the verified `_resolve_period` → `local_day_bounds_utc` → service → HX-branch pattern, with the two period controls (this light one vs the report page's) kept independent (D-04b).
Output: `/finance/metrics` route + tiles context on `/finance`, `partials/finance_tiles.html`, `.metric-grid`/`.metric-tile` CSS, modified `pages/finance.html`, web tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-financial-reports-export-dashboard-analytics/17-CONTEXT.md
@.planning/phases/17-financial-reports-export-dashboard-analytics/17-UI-SPEC.md
@.planning/phases/17-financial-reports-export-dashboard-analytics/17-PATTERNS.md
@.planning/phases/17-financial-reports-export-dashboard-analytics/17-01-SUMMARY.md
</context>

<artifacts_produced>
See 17-01-PLAN.md `<artifacts_produced>` for the full phase symbol list. This plan creates: GET `/finance/metrics` (+ tiles context on `/finance`), `partials/finance_tiles.html`, and `.metric-grid` / `.metric-tile` in `style.css`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: /finance/metrics endpoint + tiles context on /finance</name>
  <files>app/routes/finance.py</files>
  <read_first>
    - app/routes/finance.py:1-93 (module head, FINANCE_BASE, `_history_context`, `finance_page`, `finance_history` HX-branch pattern to mirror)
    - app/routes/reports.py:32-119 (`_resolve_period` to import; `reports_sales_page` HX full/partial branch; `reports_products_page` :181-220 shows an UNCONDITIONAL point-in-time call — the pattern for stock_valuation)
    - app/core.py (local_day_bounds_utc signature), app/config.py settings.display_tz usage
    - app/services/reports.py:20-82 (sales_profit_report return shape: totals.profit_cents, totals.cost_unknown_count)
    - app/services/finance_reports.py (Task from 17-01: cash_expense_total, stock_valuation)
  </read_first>
  <action>
    In `app/routes/finance.py` add imports: `from fastapi import Query`; `from app.config import settings`; `from app.core import local_day_bounds_utc`; `from app.routes.reports import _resolve_period`; `from app.services.reports import sales_profit_report`; `from app.services.finance_reports import cash_expense_total, stock_valuation`.
    Add `_metrics_context(session, from_: str, to: str) -> dict`: call `period = _resolve_period(from_, to, settings.display_tz)`; if `not period["error"]` compute `start_iso, end_iso = local_day_bounds_utc(period["from_date"], period["to_date"], settings.display_tz)`, `gross = sales_profit_report(session, start_iso, end_iso)`, `expense = cash_expense_total(session, start_iso, end_iso)`, and build `metrics = {"gross_profit_cents": gross["totals"]["profit_cents"], "cost_unknown_count": gross["totals"]["cost_unknown_count"], "net_profit_cents": gross["totals"]["profit_cents"] + expense}` (net = gross PLUS the already-negative expense — plain addition, D-01a); else `metrics = None`. ALWAYS compute `valuation = stock_valuation(session)` UNCONDITIONALLY (point-in-time, period-independent — mirror how `reports_products_page` calls `stale_products` outside the period guard, D-02b). Return the period echo keys (`from_date`/`to_date` isoformat, `active_preset`, `presets`, `error`), `metrics`, `valuation`, and `finance_base=FINANCE_BASE`.
    Add `@router.get("/finance/metrics")` `finance_metrics(request, from_: str = Query("", alias="from"), to: str = Query("", alias="to"), session=Depends(get_session))`: build `_metrics_context`; on `request.headers.get("HX-Request")` return `partials/finance_tiles.html`; else return the full `pages/finance.html` merging balance/forms/history context (spread `_history_context(session)`, `balance_cents=compute_balance(session)`, `form={}`, `errors={}`) plus the metrics context.
    Modify `finance_page` (the existing `/finance` GET) to merge `**_metrics_context(session, "", "")` into its context so the tiles render on first load with today's default period. Do NOT alter the existing balance/form/history keys or the `_history_context` call.
  </action>
  <acceptance_criteria>
    - `app/routes/finance.py` contains `@router.get("/finance/metrics")` and a `_metrics_context` helper.
    - `finance_metrics` returns `partials/finance_tiles.html` when the `HX-Request` header is present, else `pages/finance.html`.
    - net profit is computed as `gross profit_cents + cash_expense_total(...)` (a `+`, never `-`).
    - `stock_valuation(session)` is called unconditionally (outside the `if not period["error"]` guard).
    - `GET /finance` (plain) still returns 200 with the existing balance/forms/history AND a `metrics`/`valuation` context.
    - No call to any write function (`record_manual_movement`/`record_cash_movement`) is added.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_finance_reports.py -x -k "metrics or tiles"</automated>
  </verify>
  <done>/finance/metrics returns tiles on HX and the full page otherwise; /finance renders tiles on first load; net = gross + expense; valuation is point-in-time.</done>
</task>

<task type="auto">
  <name>Task 2: finance_tiles.html partial + .metric-grid/.metric-tile CSS + finance.html section</name>
  <files>app/templates/partials/finance_tiles.html, app/static/style.css, app/templates/pages/finance.html</files>
  <read_first>
    - app/templates/pages/finance.html:1-14 (existing structure to preserve byte-for-byte; add the «Показатели» section ABOVE the balance include)
    - app/templates/pages/reports_sales.html:1-12 (the `{% with period_action, period_target %}{% include period_filter %}{% endwith %}` + results-div wrapping pattern to mirror)
    - app/templates/partials/period_filter.html:1-36 (shared selector; expects period_action, period_target, from_date, to_date, active_preset, presets, error)
    - app/templates/partials/sales_report_results.html:9-23 (`.num` money cells, `.muted` caveat line, `{{ x | cents }}` treatment)
    - app/static/style.css:263-309 (`.customer-chip`, `.mobile-tile-grid`, `.mobile-tile` token values to reuse: #ffffff, 1px solid #d9d9d9, 4px radius, 16px padding, 16px grid gap)
    - .planning/phases/17-financial-reports-export-dashboard-analytics/17-UI-SPEC.md §A + Copywriting Contract (exact tile headings, the MANDATORY net caveat copy, the «на текущий момент» cue, unknown-price caveat copy)
  </read_first>
  <action>
    Create `app/templates/partials/finance_tiles.html`: a `.metric-grid` containing three `.metric-tile` blocks. Each tile: an `h2`/`.tile-label` heading, the figure `<p class="num"><strong>{{ value | cents }}</strong></p>`, then optional `.muted` caveat lines. Copy exact RU strings from UI-SPEC Copywriting Contract:
    - Gross tile — heading «Валовая прибыль»; value `metrics.gross_profit_cents | cents`; if `metrics.cost_unknown_count > 0` a `.muted` line «{{ metrics.cost_unknown_count }} продаж без себестоимости — не учтены.».
    - Net tile — heading «Чистая прибыль»; value `metrics.net_profit_cents | cents`; ALWAYS render the `.muted` caveat line «Денежный поток: валовая прибыль минус снятия и возвраты за период. Это не бухгалтерская прибыль.» (MANDATORY, D-01b — a visible line, NOT a title= tooltip; Pitfall 2).
    - Stock tile — heading «Стоимость склада»; two lines «По закупке: {{ valuation.cost_value_cents | cents }}» and «По продаже: {{ valuation.sale_value_cents | cents }}»; a `.muted` «на текущий момент» cue (marks it period-independent, D-04b); if `valuation.cost_unknown_count > 0` add `.muted` «{{ valuation.cost_unknown_count }} товаров без цены закупки — не учтены.» and symmetrically for `sale_unknown_count` «…без цены продажи…».
    - When `metrics` is None (bad dates), render a `.muted` «Проверьте даты.» in place of the gross/net figures; the stock tile always renders. Numbers are NEVER sign-colored (UI-SPEC Q4) — plain `format_cents` in default color.
    Add `.metric-grid` (`display: grid; grid-template-columns: repeat(3, 1fr); gap: 16px;`) and `.metric-tile` (`background: #ffffff; border: 1px solid #d9d9d9; border-radius: 4px; padding: 16px;`) to `app/static/style.css`, reusing ONLY existing token values (mirror `.mobile-tile`). Add a mobile-narrow fallback consistent with `.mobile-tile-grid` (single/2-col) if a media query is already the file's convention; introduce no new color/size tokens.
    Modify `app/templates/pages/finance.html`: insert a «Показатели» section ABOVE the existing `<h1>Баланс кассы</h1>`/balance include — an `<h2>Показатели</h2>`, then `{% with period_action = "/finance/metrics", period_target = "#finance-metrics" %}{% include "partials/period_filter.html" %}{% endwith %}`, then `<div id="finance-metrics">{% include "partials/finance_tiles.html" %}</div>`. The period_filter MUST sit OUTSIDE `#finance-metrics` (so an HX swap never resets the inputs). Leave the balance/withdraw/deposit/history includes byte-for-byte unchanged.
  </action>
  <acceptance_criteria>
    - `partials/finance_tiles.html` contains three `.metric-tile` inside a `.metric-grid`; net tile ALWAYS emits the D-01b caveat line (grep: `не бухгалтерская прибыль`).
    - Stock tile renders «По закупке» and «По продаже» lines plus «на текущий момент»; unknown-price caveats are conditional on the `*_unknown_count` values.
    - Category/label/caveat copy matches the UI-SPEC Copywriting Contract exactly; money via `| cents` with no currency symbol and no sign coloring.
    - `style.css` contains `.metric-grid` and `.metric-tile` using only existing token values (#ffffff, #d9d9d9, 4px, 16px); no new hex/size token introduced.
    - `pages/finance.html` still `{% include %}`s cash_balance, withdraw_form, deposit_form, cash_history_rows unchanged, and the period_filter targets `#finance-metrics` from `/finance/metrics`.
    - `uv run pytest tests/test_finance_reports.py -x -k "tiles or caveat"` passes (assert the net caveat text is present in the /finance HTML and in the /finance/metrics HX partial).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_finance_reports.py -x -k "web and (finance or tiles or caveat)"</automated>
  </verify>
  <done>Three tiles render on /finance with the mandatory net caveat and stock point-in-time cue; CSS reuses existing tokens; the Phase 15-16 finance surface is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /finance/metrics (from/to) | Untrusted date params cross here |
| stored note/category → rendered tile HTML | Stored strings are not rendered in tiles (tiles show only numbers + fixed RU copy), so no stored-text sink here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | `/finance/metrics` from/to params | mitigate | Flow ONLY through `_resolve_period` (clamps malformed/inverted → today + RU error, never reaches SQL) then `local_day_bounds_utc`; consumed solely as ORM `.where` bounds inside the 17-01 services |
| T-17-07 | Information Disclosure | net-profit misread as accounting profit | mitigate | Always-visible `.muted` cash-outflow caveat on the net tile (D-01b), not a tooltip; understated-on-return behavior surfaced, never hidden |
| T-17-05 | Elevation | new write path / auth | accept | Tiles + `/finance/metrics` are GET-only reads; no write, no auth surface (single local operator, v1). Existing balance/forms/history untouched |
| T-17-08 | DoS | tile recompute cost | accept | Single-operator scale; point-in-time valuation is two SUM + two COUNT queries; gross/net reuse existing bounded period reports |
</threat_model>

<verification>
- `uv run pytest tests/test_finance_reports.py -x` green (metrics + tiles + caveat web tests).
- `uv run pytest` full suite green.
- `<human-check>` (end-of-phase, human_verify_mode=end-of-phase — deferred to phase UAT): open http://127.0.0.1:8000/finance — the «Показатели» section shows three tiles (Валовая прибыль / Чистая прибыль / Стоимость склада); the net tile shows the cash-outflow caveat line; the stock tile shows «на текущий момент»; changing the period preset updates gross/net but not stock; balance, withdraw/deposit forms, and history look identical to before.
</verification>

<success_criteria>
- `/finance` shows gross, net (with mandatory caveat), and stock (point-in-time, unknown caveats) tiles driven by an independent light period selector.
- The Phase 15-16 finance surface is byte-for-byte unchanged.
- CSS introduces only structural `.metric-grid`/`.metric-tile` on existing tokens.
- Web tests green; full suite green.
</success_criteria>

<output>
Create `.planning/phases/17-financial-reports-export-dashboard-analytics/17-02-SUMMARY.md` when done.
</output>

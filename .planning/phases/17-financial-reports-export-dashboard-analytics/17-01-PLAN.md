---
phase: 17-financial-reports-export-dashboard-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/finance_reports.py
  - app/services/export.py
  - tests/test_finance_reports.py
  - tests/test_export.py
autonomous: true
requirements: [FIN-08, FIN-09, FIN-11, FIN-12]

must_haves:
  truths:
    - "cash_expense_total returns the signed SUM of withdrawal+return rows in a period (net = gross + this, a plain addition)"
    - "stock_valuation returns cost/sale value over active products, excluding NULL-price rows, with unknown counts"
    - "cash_flow_report groups a period's movements into income vs expense whose expense subtotal equals cash_expense_total"
    - "stream_cash_movements_csv streams only in-period rows as one-BOM, ;-delimited, formula-escaped CSV"
    - "Implements CONTEXT decisions D-01, D-02, D-03, D-05 (net-profit expense set, product-level stock valuation, period-scoped CSV export, cash-flow report income-vs-expense structure)"
  artifacts:
    - path: "app/services/finance_reports.py"
      provides: "cash_expense_total, stock_valuation, cash_flow_report read-only aggregations"
      contains: "def cash_expense_total"
    - path: "app/services/export.py"
      provides: "stream_cash_movements_csv period-scoped CSV export + updated T-06-09 docstring"
      contains: "def stream_cash_movements_csv"
    - path: "tests/test_finance_reports.py"
      provides: "service-level tests for the three aggregations"
      contains: "def test_"
  key_links:
    - from: "app/services/finance_reports.py::cash_expense_total"
      to: "app.models.CASH_BUCKETS"
      via: "cats = CASH_BUCKETS['withdrawal'] + CASH_BUCKETS['return']"
      pattern: "CASH_BUCKETS\\[.withdrawal.\\]"
    - from: "app/services/export.py::stream_cash_movements_csv"
      to: "_encode_once / _csv_rows / _csv_safe"
      via: "verbatim reuse of the existing CSV stack"
      pattern: "_encode_once\\(_csv_rows"
---

<objective>
Build the read-only aggregation + CSV-export service layer that every Phase 17 route consumes. Three new SELECT-only functions in a new `app/services/finance_reports.py` (cash_expense_total for FIN-11, stock_valuation for FIN-12, cash_flow_report for FIN-08) plus one new streaming export `stream_cash_movements_csv` for FIN-09 in the existing `app/services/export.py`. No route, no template, no schema, no write path.

Purpose: Isolate the analytics math + CSV bytes behind tested service functions so the Wave 2-4 route/template plans are thin wiring. This is 100% READ-ONLY — every function only SELECTs (mirrors `app/services/reports.py` discipline).
Output: `finance_reports.py` (3 fns), `export.py` (+1 fn, +docstring edit per D-03b), service tests in `tests/test_finance_reports.py` and CSV tests in `tests/test_export.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-financial-reports-export-dashboard-analytics/17-CONTEXT.md
@.planning/phases/17-financial-reports-export-dashboard-analytics/17-RESEARCH.md
@.planning/phases/17-financial-reports-export-dashboard-analytics/17-PATTERNS.md
</context>

<artifacts_produced>
Symbols CREATED by Phase 17 (new — exclude from drift/source-grounding):
- Services: `app/services/finance_reports.py` → `cash_expense_total(session, start_iso, end_iso) -> int`; `stock_valuation(session) -> dict{cost_value_cents, sale_value_cents, cost_unknown_count, sale_unknown_count}`; `cash_flow_report(session, start_iso, end_iso) -> dict{income[], income_total_cents, expense[], expense_total_cents, movement_count}`
- Export: `app/services/export.py` → `stream_cash_movements_csv(session, start_iso, end_iso) -> StreamingResponse`
- Desktop routes (app/routes/finance.py): GET `/finance/metrics`, GET `/finance/report`, GET `/finance/report.csv`
- Mobile routes (app/routes/mobile_finance.py): GET `/m/finance/metrics`, GET `/m/finance/report`, GET `/m/finance/report.csv`
- Templates: `partials/finance_tiles.html`, `partials/cash_flow_report.html`, `pages/finance_report.html`, `mobile_pages/finance_report.html`
- CSS: `.metric-grid`, `.metric-tile` in `app/static/style.css`
- Tests: `tests/test_finance_reports.py` (new); new cash-movement cases in `tests/test_export.py`
- NO new label-dict entries (CASH_CATEGORIES/CASH_BUCKETS unchanged), NO schema/migration, NO new packages.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cash_expense_total + stock_valuation aggregations</name>
  <files>app/services/finance_reports.py, tests/test_finance_reports.py</files>
  <read_first>
    - app/services/reports.py:1-82 (module docstring read-only discipline + sales_profit_report NULL-cost pattern to mirror)
    - app/services/finance.py:165-211 (compute_balance is unfiltered whole-till — NOT reusable here; cash_history_view shows the `CASH_BUCKETS.get` → `category.in_(cats)` filter pattern)
    - app/models.py:69-99 (CASH_CATEGORIES keys; CASH_BUCKETS: `withdrawal`=5 keys, `return`=`("return",)`), :129-170 (Product.quantity/cost_cents/sale_cents nullable, deleted_at)
    - tests/test_reports.py:1-110 (`_ensure_batch`, `_record_sale_at` helpers, `local_day_bounds_utc` usage, TZ constant)
    - tests/conftest.py:1-125 (session/product fixtures; how record_cash_movement-style seeding works)
    - app/services/finance.py:46-84 (record_cash_movement signature — tests use it to seed cash rows; DO NOT call it from the new service)
  </read_first>
  <behavior>
    - cash_expense_total: a period with one -1000 withdrawal_supplier + one -500 return → returns -1500; a deposit_opening (+) and a sale (+) in the same period are EXCLUDED. Empty period → 0. Net reconciliation: for gross profit G, `G + cash_expense_total(...)` equals gross minus the outflow magnitude (plain addition — D-01a, Pitfall 1: never subtract).
    - cash_expense_total: half-open bounds — a row stamped exactly at end_iso is EXCLUDED; a row at start_iso is INCLUDED.
    - stock_valuation: two active products qty=2 cost=1000 sale=1500 and qty=3 cost=500 sale=800 → cost_value_cents=3500, sale_value_cents=5400.
    - stock_valuation: a product with NULL cost_cents is EXCLUDED from cost_value (never zero-filled) and counted in cost_unknown_count (restrict the count to quantity>0); NULL sale_cents handled symmetrically in sale_unknown_count.
    - stock_valuation: a soft-deleted product (deleted_at set) is excluded from both sums and both counts.
    - stock_valuation: point-in-time — it takes NO period argument and ignores any period.
  </behavior>
  <action>
    Create `app/services/finance_reports.py` with the read-only module docstring copied from `reports.py` (100% SELECT, portable ORM only, no SQLite-specific SQL). Import `func, select` from sqlalchemy, `Session` from sqlalchemy.orm, and `CASH_BUCKETS, CashMovement, Product` from `app.models`.
    Define `cash_expense_total(session: Session, start_iso: str, end_iso: str) -> int` (FIN-11): `cats = CASH_BUCKETS["withdrawal"] + CASH_BUCKETS["return"]` (compose from the map, do NOT hardcode the six category strings — D-01a); return `session.scalar(select(func.coalesce(func.sum(CashMovement.amount_cents), 0)).where(CashMovement.category.in_(cats), CashMovement.created_at >= start_iso, CashMovement.created_at < end_iso))`. Rows are already negative → the caller ADDS this to gross profit.
    Define `stock_valuation(session: Session) -> dict` (D-02, product-level valuation over ACTIVE products — batch-level cost is NOT used because Batch has no cost column): `active = Product.deleted_at.is_(None)`; `cost_value_cents = coalesce(sum(Product.quantity * Product.cost_cents), 0)` over active rows (a NULL cost_cents makes the term NULL which SUM skips — reproduces "exclude, never zero", D-02a); `sale_value_cents` symmetric on `sale_cents`; `cost_unknown_count = count()` where `active AND Product.cost_cents.is_(None) AND Product.quantity > 0`; `sale_unknown_count` symmetric. Return dict keys `cost_value_cents, sale_value_cents, cost_unknown_count, sale_unknown_count`. NO period argument (D-02b).
    Add service-level tests to a new `tests/test_finance_reports.py` (no `test_web_` prefix — service tier). Seed cash rows via `app.services.finance.record_cash_movement` and products directly; reuse `local_day_bounds_utc` + `TZ` from the `test_reports.py` pattern. Assert every bullet in <behavior>.
  </action>
  <acceptance_criteria>
    - `app/services/finance_reports.py` contains `def cash_expense_total(session` and `def stock_valuation(session` and its module docstring states read-only / portable ORM.
    - `cash_expense_total` composes its category set from `CASH_BUCKETS["withdrawal"] + CASH_BUCKETS["return"]` (grep: `CASH_BUCKETS\["withdrawal"\]`), with NO hardcoded six-string list.
    - `stock_valuation` filters `Product.deleted_at.is_(None)` and uses `Product.cost_cents.is_(None)` / `.quantity > 0` for the unknown counts (grep: `is_(None)`), never a bare `or` (D-02/D-02a).
    - `uv run pytest tests/test_finance_reports.py -x` passes (RED→GREEN); tests assert the -1000/-500→-1500 addition, deposit/sale exclusion, half-open boundary, NULL-price exclusion + unknown counts, deleted-product exclusion, and period-independence.
    - No call to `compute_balance`, `record_cash_movement`, or `record_manual_movement` inside `finance_reports.py`.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_finance_reports.py -x -k "expense or valuation"</automated>
  </verify>
  <done>cash_expense_total and stock_valuation exist, are SELECT-only, compose from CASH_BUCKETS, exclude NULL prices and deleted products, and pass their service tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cash_flow_report income-vs-expense grouping (FIN-08)</name>
  <files>app/services/finance_reports.py, tests/test_finance_reports.py</files>
  <read_first>
    - app/services/reports.py:85-140 (writeoff_report — the "group by a fixed key set, stable key order" analog for grouping movements by category)
    - app/models.py:69-99 (CASH_CATEGORIES key order for stable ordering; CASH_BUCKETS income set = sale+deposit, expense set = withdrawal+return)
    - app/services/finance_reports.py (Task 1 output — reuse the same imports and half-open predicate)
  </read_first>
  <behavior>
    - A period with sales (+3000), a deposit_opening (+1000), a withdrawal_rent (-800), a withdrawal_salary (-1200), and a return (-500) yields: income rows for `sale` (+3000) and `deposit_opening` (+1000) with income_total_cents=4000; expense rows for `withdrawal_rent`, `withdrawal_salary`, `return` with expense_total_cents = -2500; movement_count = 5 (= len(income)=2 + len(expense)=3, i.e. the bucketed-row count, never a raw un-bucketed count).
    - RECONCILIATION (hard): `cash_flow_report(session, s, e)["expense_total_cents"] == cash_expense_total(session, s, e)` for the same period (report and net-profit tile can never disagree — D-01/D-05).
    - Empty period → income=[], expense=[], income_total_cents=0, expense_total_cents=0, movement_count=0.
    - Half-open: a movement stamped at end_iso is excluded.
    - Category rows appear in CASH_CATEGORIES key order (stable across reports), one row per category PRESENT in the period.
  </behavior>
  <action>
    Add `cash_flow_report(session: Session, start_iso: str, end_iso: str) -> dict` to `finance_reports.py` (FIN-08). Query `select(CashMovement.category, func.sum(CashMovement.amount_cents)).where(CashMovement.created_at >= start_iso, CashMovement.created_at < end_iso).group_by(CashMovement.category)`. Split each grouped category into income (membership in `CASH_BUCKETS["sale"] + CASH_BUCKETS["deposit"]`) vs expense (membership in `CASH_BUCKETS["withdrawal"] + CASH_BUCKETS["return"]`). Emit `income` and `expense` as lists of dicts `{"category": key, "total_cents": int}` iterated in `CASH_CATEGORIES` key order so rows are stable. Compute `income_total_cents`, `expense_total_cents` (the expense list summed — MUST equal cash_expense_total for the same bounds, D-05), and `movement_count = len(income) + len(expense)` (the count of rows ACTUALLY placed in a bucket, NOT the raw grouped-row count — so a `CashMovement.category` that falls outside all four `CASH_BUCKETS` sets can never be counted while being silently dropped from both tables; deriving it this way keeps the empty-state-vs-tables branch and the D-05 reconciliation exact). Do NOT put RU labels in the service — the template renders labels via the CASH_CATEGORIES Jinja global. Extend `tests/test_finance_reports.py` with cash_flow_report tests asserting every <behavior> bullet, including the reconciliation equality against cash_expense_total.
  </action>
  <acceptance_criteria>
    - `app/services/finance_reports.py` contains `def cash_flow_report(session`.
    - Return dict has keys `income, income_total_cents, expense, expense_total_cents, movement_count`; income/expense are lists of `{"category", "total_cents"}` dicts (no RU label strings in the service).
    - Category membership is decided via CASH_BUCKETS sets, not hardcoded category-string comparisons.
    - Test asserts `cash_flow_report(...)["expense_total_cents"] == cash_expense_total(...)` for an identical period (D-05 reconciliation).
    - `movement_count` equals `len(income) + len(expense)` (bucketed rows only): a test asserts it is 0 iff both tables are empty and that every in-period category lands in income XOR expense, so an un-bucketed category can never be counted while being dropped from both tables.
    - `uv run pytest tests/test_finance_reports.py -x -k flow` passes.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_finance_reports.py -x -k "flow or reconcile"</automated>
  </verify>
  <done>cash_flow_report groups a period into income/expense by CASH_BUCKETS, reconciles its expense subtotal with cash_expense_total, and passes its tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: stream_cash_movements_csv + T-06-09 docstring exception (FIN-09)</name>
  <files>app/services/export.py, tests/test_export.py</files>
  <read_first>
    - app/services/export.py:1-141 (module docstring T-06-09 lines 12-18 to EDIT; `_csv_safe` 35-39, `_csv_rows` 42-54, `_encode_once` 57-69, `stream_sales_csv` 105-141 shape to mirror)
    - app/models.py:69-81 (CASH_CATEGORIES key→RU label for the category column), :370-401 area (CashMovement: category, signed amount_cents, nullable note, created_at ISO text)
    - app/core.py (format_cents, iso_to_local signatures — already used by export.py line 29)
    - tests/test_export.py:1-70 (test_csv_bom_appears_once, test_money_field_not_split_by_delimiter, `_ensure_batch`, utf-8-sig decode roundtrip assertions to mirror)
  </read_first>
  <behavior>
    - stream_cash_movements_csv over a period with a withdrawal_supplier (-1200, note "Оплата") and a sale (+3000, note NULL) → the streamed bytes start with exactly one UTF-8 BOM (b"\xef\xbb\xbf" count == 1), are `;`-delimited, header `["Когда","Категория","Комментарий","Сумма"]`.
    - Category cell renders the RU label from CASH_CATEGORIES ("Оплата поставщику", "Продажа"), amount via format_cents ("-12,00", "30,00"), datetime via iso_to_local.
    - A NULL note renders as "" (never the literal "None") — Pitfall 4 / Discrepancy 6.
    - A note beginning with `=`, `+`, `-`, or `@` is prefixed with a leading apostrophe by `_csv_safe` (formula-injection escape) — assert on a crafted `=CMD()` note.
    - Only rows in `[start_iso, end_iso)` appear (a row at end_iso is excluded); rows are oldest-first (order_by created_at).
  </behavior>
  <action>
    Add `stream_cash_movements_csv(session, start_iso, end_iso) -> StreamingResponse` to `app/services/export.py` (FIN-09, D-03 period-filtered), mirroring `stream_sales_csv`. Add `CashMovement` and `CASH_CATEGORIES` to the existing `from app.models import ...` line. Query `session.scalars(select(CashMovement).where(CashMovement.created_at >= start_iso, CashMovement.created_at < end_iso).order_by(CashMovement.created_at)).all()`. Build `header = ["Когда", "Категория", "Комментарий", "Сумма"]` and rows `[iso_to_local(m.created_at, settings.display_tz), _csv_safe(CASH_CATEGORIES.get(m.category, m.category)), _csv_safe(m.note or ""), format_cents(m.amount_cents)]` (D-03c minimum column set). Keep EVERY existing export convention intact (D-03a): reuse `_encode_once`/`_csv_rows`/`_csv_safe` verbatim — return `StreamingResponse(_encode_once(_csv_rows(header, rows)), media_type="text/csv", headers={"Content-Disposition": "attachment; filename=cash_movements.csv"})` (one UTF-8 BOM, `;` delimiter, formula-escape on every free-text cell).
    Edit the module docstring T-06-09 paragraph (lines 12-18): keep the T-06-10 `_csv_safe` sentence intact, and record the documented, bounded exception — `stream_cash_movements_csv` accepts a VALIDATED calendar `from`/`to` range (clamped upstream by `_resolve_period`, consumed ONLY as an ORM `.where(created_at …)` bound; it is not a filename/path/arbitrary string). State this is a bounded relaxation, not "exports may take arbitrary params" (D-03b).
    Add cash-movement CSV tests to `tests/test_export.py` (service-level, no route needed): consume the StreamingResponse `body_iterator`, decode utf-8-sig, and assert every <behavior> bullet including single BOM, `;` delimiter, RU label, signed format_cents, NULL note → "", `=CMD()` apostrophe escape, and half-open period boundary.
  </action>
  <acceptance_criteria>
    - `app/services/export.py` contains `def stream_cash_movements_csv(session` and reuses `_encode_once(_csv_rows(...))` verbatim (grep: `_encode_once\(_csv_rows`) — D-03a conventions intact.
    - The import line adds `CashMovement` and `CASH_CATEGORIES` from `app.models`.
    - Module docstring now documents the validated `from`/`to` exception (grep: `_resolve_period` present in the docstring region) while retaining the `_csv_safe` formula-injection line (D-03b).
    - Free-text cells (note, category label) pass through `_csv_safe`; `m.note or ""` guards NULL; columns match the D-03c minimum set.
    - `uv run pytest tests/test_export.py -x -k cash` passes; the existing param-less export tests (`test_web_export_*`) still pass.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_export.py -x</automated>
  </verify>
  <done>stream_cash_movements_csv streams period-scoped, one-BOM, ;-delimited, formula-escaped CSV; the T-06-09 docstring records the bounded date-range exception; all export tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route → service (start_iso/end_iso) | The only untrusted input reaching these functions is the already-validated UTC ISO period bound; category filters are server-side constants |
| service → DB | Parameterised ORM only; no string-built SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | period bounds reaching `cash_expense_total`/`cash_flow_report`/`stream_cash_movements_csv` | mitigate | Consume start_iso/end_iso ONLY as ORM `.where(CashMovement.created_at …)` bounds — never string-concatenated; upstream `_resolve_period` (Wave 2-4) clamps/validates before these are called |
| T-17-02 | Tampering/Exec | CSV free-text cells (note, category label) | mitigate | `_csv_safe` on every free-text cell in `stream_cash_movements_csv` (leading `= + - @` → apostrophe); tested with a crafted `=CMD()` note |
| T-17-03 | Integrity | CSV BOM correctness | mitigate | `_encode_once` yields the BOM on the first chunk only; test asserts BOM count == 1 |
| T-17-04 | Tampering | outflow/income category selection | mitigate | Category sets composed from the server-side `CASH_BUCKETS` allow-list via `.in_()`, never operator-supplied |
| T-17-05 | Elevation | new write path | accept | Phase 17 is 100% READ-ONLY — these functions only SELECT; the single cash write path (`finance.record_cash_movement`) is untouched. No auth surface (single local operator, v1) — considered and intentionally unchanged |
| T-17-SC | Tampering | supply chain | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit: N/A); all code reuses in-repo modules |
</threat_model>

<verification>
- `uv run pytest tests/test_finance_reports.py tests/test_export.py -x` green.
- `uv run pytest` full suite green (no regression in existing export/report tests).
- `uv run ruff check app/services/finance_reports.py app/services/export.py` clean.
</verification>

<success_criteria>
- Three read-only aggregations exist in `finance_reports.py`, all SELECT-only, composing category sets from CASH_BUCKETS and excluding NULL prices / deleted products / out-of-period rows.
- `cash_flow_report` expense subtotal reconciles exactly with `cash_expense_total`.
- `stream_cash_movements_csv` reuses the CSV safety stack verbatim and the T-06-09 docstring records the bounded date-range exception (D-03b).
- All new service/CSV tests pass; full suite green.
</success_criteria>

<output>
Create `.planning/phases/17-financial-reports-export-dashboard-analytics/17-01-SUMMARY.md` when done.
</output>
</content>
</invoke>

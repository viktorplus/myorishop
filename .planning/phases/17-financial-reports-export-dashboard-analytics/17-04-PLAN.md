---
phase: 17-financial-reports-export-dashboard-analytics
plan: 04
type: execute
wave: 4
depends_on: [17-01, 17-02, 17-03]
files_modified:
  - app/routes/mobile_finance.py
  - app/templates/mobile_pages/finance.html
  - app/templates/mobile_pages/finance_report.html
  - tests/test_finance_reports.py
autonomous: true
requirements: [FIN-08, FIN-09, FIN-10, FIN-11, FIN-12]

must_haves:
  truths:
    - "Operator sees the same gross/net/stock tiles + light period selector on /m/finance"
    - "Operator can open /m/finance/report, pick a period, and see Приход vs Расход"
    - "Operator can download the period cash-movements CSV from the mobile report page"
    - "Mobile balance, forms, and card history from Phases 15-16 stay untouched"
  artifacts:
    - path: "app/routes/mobile_finance.py"
      provides: "/m/finance/metrics, /m/finance/report, /m/finance/report.csv routes"
      contains: "/m/finance/report"
    - path: "app/templates/mobile_pages/finance_report.html"
      provides: "mobile report page shell reusing the shared results partial"
      contains: "cashflow-results"
    - path: "app/templates/mobile_pages/finance.html"
      provides: "Показатели tiles section added above the untouched balance/forms/history"
      contains: "finance-metrics"
  key_links:
    - from: "app/templates/mobile_pages/finance.html"
      to: "/m/finance/metrics"
      via: "period_filter light selector targeting #finance-metrics"
      pattern: "finance-metrics"
    - from: "app/routes/mobile_finance.py::mobile_finance_report"
      to: "cash_flow_report + shared partials/cash_flow_report.html"
      via: "_resolve_period → local_day_bounds_utc → cash_flow_report → HX branch, finance_base=/m/finance"
      pattern: "cash_flow_report"
---

<objective>
Bring the desktop Phase 17 analytics to the mobile «Финансы» surface at parity (D-04c): the three metric tiles + light period selector on `/m/finance` (via a new `/m/finance/metrics` endpoint), and a new `/m/finance/report` sub-page + `/m/finance/report.csv` download. This is NEW construction assembled from the shared seams — there is NO `/m/reports/sales` precedent to clone (RESEARCH Discrepancy 1). Reuse the shared `partials/finance_tiles.html` (17-02) and `partials/cash_flow_report.html` (17-03) via the `finance_base` prefix convention.

Purpose: Mobile/desktop parity for the dashboard + report, matching how FIN-03..07 achieved parity (shared partials parameterised by `finance_base`). The Phase 15-16 mobile balance, forms, and card history stay untouched.
Output: `/m/finance/metrics`, `/m/finance/report`, `/m/finance/report.csv` routes, modified `mobile_pages/finance.html`, new `mobile_pages/finance_report.html`, web tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-financial-reports-export-dashboard-analytics/17-CONTEXT.md
@.planning/phases/17-financial-reports-export-dashboard-analytics/17-UI-SPEC.md
@.planning/phases/17-financial-reports-export-dashboard-analytics/17-PATTERNS.md
@.planning/phases/17-financial-reports-export-dashboard-analytics/17-01-SUMMARY.md
@.planning/phases/17-financial-reports-export-dashboard-analytics/17-02-SUMMARY.md
@.planning/phases/17-financial-reports-export-dashboard-analytics/17-03-SUMMARY.md
</context>

<artifacts_produced>
See 17-01-PLAN.md `<artifacts_produced>` for the full phase symbol list. This plan creates: GET `/m/finance/metrics`, GET `/m/finance/report`, GET `/m/finance/report.csv`, `mobile_pages/finance_report.html`, and the «Показатели» section on `mobile_pages/finance.html`. It REUSES the shared `partials/finance_tiles.html` and `partials/cash_flow_report.html`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: /m/finance/metrics + /m/finance/report + /m/finance/report.csv routes</name>
  <files>app/routes/mobile_finance.py</files>
  <read_first>
    - app/routes/mobile_finance.py:1-118 (module head, `FINANCE_BASE = "/m/finance"`, `_history_context`, `mobile_finance_page`, `mobile_finance_history` HX branch — the parity mechanics to mirror)
    - app/routes/finance.py (17-02/17-03 output: `_metrics_context`, `finance_metrics`, `finance_report_page`, `finance_report_csv` — the desktop shapes to replicate with the mobile prefix)
    - app/routes/reports.py:32-119 (`_resolve_period` + HX branch)
    - app/services/finance_reports.py, app/services/reports.py, app/services/export.py (the shared read/CSV services — identical calls, no mobile-specific service)
  </read_first>
  <action>
    In `app/routes/mobile_finance.py` add imports mirroring the desktop route: `from fastapi import Query`; `from app.config import settings`; `from app.core import local_day_bounds_utc`; `from app.routes.reports import _resolve_period`; `from app.services.reports import sales_profit_report`; `from app.services.finance_reports import cash_expense_total, stock_valuation, cash_flow_report`; `from app.services import export as export_service`.
    Add a `_metrics_context(session, from_, to)` helper identical to the desktop one but with `finance_base=FINANCE_BASE` (= `/m/finance`): `_resolve_period` → (if no error) `local_day_bounds_utc` → `sales_profit_report`/`cash_expense_total` → `metrics` dict with `net_profit_cents = gross profit_cents + expense` (plain addition); ALWAYS compute `valuation = stock_valuation(session)` unconditionally (point-in-time, D-02b).
    Add `@router.get("/m/finance/metrics")` `mobile_finance_metrics(...)` with `from`/`to` Query aliases: on `HX-Request` return `partials/finance_tiles.html` (the SHARED partial), else the full `mobile_pages/finance.html` merging balance/forms/history + metrics context.
    Modify `mobile_finance_page` to merge `**_metrics_context(session, "", "")` so tiles render on first load.
    Add `@router.get("/m/finance/report")` `mobile_finance_report(...)`: clone `finance_report_page` — `_resolve_period` → `local_day_bounds_utc` → `cash_flow_report`; context includes `finance_base=FINANCE_BASE`; on `HX-Request` return the SHARED `partials/cash_flow_report.html`, else `mobile_pages/finance_report.html`.
    Add `@router.get("/m/finance/report.csv")` `mobile_finance_report_csv(...)`: `_resolve_period` → `local_day_bounds_utc` → `return export_service.stream_cash_movements_csv(session, start_iso, end_iso)` (same service as desktop; from/to only).
    Keep every route THIN — no cash writes, no new service (D-00c). The router is already registered in `app/main.py`; no main.py change.
  </action>
  <acceptance_criteria>
    - `app/routes/mobile_finance.py` contains `@router.get("/m/finance/metrics")`, `@router.get("/m/finance/report")`, and `@router.get("/m/finance/report.csv")`.
    - `mobile_finance_metrics` returns the SHARED `partials/finance_tiles.html` on `HX-Request`; `mobile_finance_report` returns the SHARED `partials/cash_flow_report.html` on `HX-Request`.
    - net profit uses `gross profit_cents + cash_expense_total(...)` (a `+`); `stock_valuation(session)` is called unconditionally.
    - `mobile_finance_report_csv` delegates to `export_service.stream_cash_movements_csv` (from/to only, no filename/path param).
    - `GET /m/finance` still returns 200 with balance/forms/card-history AND the tiles context; no write function is added.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_finance_reports.py -x -k "mobile"</automated>
  </verify>
  <done>Mobile metrics/report/CSV routes exist, reuse the shared partials + services with finance_base=/m/finance, and add no write path.</done>
</task>

<task type="auto">
  <name>Task 2: mobile finance.html Показатели section + mobile finance_report.html page</name>
  <files>app/templates/mobile_pages/finance.html, app/templates/mobile_pages/finance_report.html</files>
  <read_first>
    - app/templates/mobile_pages/finance.html:1-33 (existing structure to preserve byte-for-byte; add «Показатели» ABOVE `<h1>Баланс кассы</h1>`)
    - app/templates/pages/finance_report.html (17-03 desktop page — the shell to mirror with mobile action/base)
    - app/templates/partials/period_filter.html:1-36 (shared selector), app/templates/partials/finance_tiles.html + partials/cash_flow_report.html (shared partials from 17-02/17-03 — reuse, do NOT fork)
    - app/templates/mobile_base.html (mobile chrome the report page extends), app/templates/mobile_pages/reports_expiry.html (mobile page-extends-mobile_base pattern)
    - .planning/phases/17-financial-reports-export-dashboard-analytics/17-UI-SPEC.md §A/§B/§D (mobile parity + the untouched-surface regression guard)
  </read_first>
  <action>
    Modify `app/templates/mobile_pages/finance.html`: insert a «Показатели» section ABOVE the existing `<h1>Баланс кассы</h1>` — `<h2>Показатели</h2>`, then `{% with period_action = "/m/finance/metrics", period_target = "#finance-metrics" %}{% include "partials/period_filter.html" %}{% endwith %}`, then `<div id="finance-metrics">{% include "partials/finance_tiles.html" %}</div>` (the SHARED tiles partial; `.metric-grid` falls back to the mobile column rhythm via the 17-02 CSS). The period_filter sits OUTSIDE `#finance-metrics`. Leave the balance include, the two shared forms, the «Тип» bucket select, `#cash-history-cards`, and the load-more control byte-for-byte unchanged.
    Create `app/templates/mobile_pages/finance_report.html` extending `mobile_base.html` (clear `step_indicator` block as `reports_expiry` / the mobile finance page do): `<h1>Движения кассы за период</h1>`; `{% with period_action = "/m/finance/report", period_target = "#cashflow-results" %}{% include "partials/period_filter.html" %}{% endwith %}`; a plain `<a class="button" href="/m/finance/report.csv?from={{ from_date }}&to={{ to_date }}">Скачать CSV</a>` (plain download, NEVER `hx-get`); `<div id="cashflow-results">{% include "partials/cash_flow_report.html" %}</div>` (the SHARED results partial). Use `finance_base = "/m/finance"` where the shell needs the prefix.
  </action>
  <acceptance_criteria>
    - `mobile_pages/finance.html` has the «Показатели» section with the shared `partials/finance_tiles.html` inside `#finance-metrics`, the period_filter targeting `/m/finance/metrics` OUTSIDE that div, and the Phase 15-16 balance/forms/«Тип»-select/`#cash-history-cards`/load-more includes unchanged.
    - `mobile_pages/finance_report.html` extends `mobile_base.html`, includes the shared `partials/period_filter.html` (action `/m/finance/report`, target `#cashflow-results`) and the shared `partials/cash_flow_report.html`, and a plain `<a class="button" href="/m/finance/report.csv?...">Скачать CSV</a>` with NO `hx-get`.
    - No new mobile-specific tiles or report partial is created (shared partials reused, D-04c).
    - `uv run pytest tests/test_finance_reports.py -x -k "mobile"` passes: `/m/finance` shows tiles + net caveat; `/m/finance/report` renders full/HX; `/m/finance/report.csv` returns text/csv.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_finance_reports.py -x -k "web and mobile"</automated>
  </verify>
  <done>Mobile /m/finance shows the tiles at parity and /m/finance/report + CSV work, all reusing the shared partials; the Phase 15-16 mobile surface is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /m/finance/{metrics,report,report.csv} (from/to) | Untrusted date params cross here (same surface as desktop, distinct prefix) |
| stored category → mobile report HTML | Category keys resolve to fixed CASH_CATEGORIES labels via the shared partial |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | mobile from/to params on all three routes | mitigate | Same `_resolve_period` clamp + `local_day_bounds_utc` + ORM `.where` bounds as desktop; never string-built, never a path/filename |
| T-17-02 | Tampering/Exec | CSV free-text cells | mitigate | Reuses 17-01 `stream_cash_movements_csv` (`_csv_safe` on note + label); mobile route only passes validated bounds |
| T-17-05c | Tampering (XSS) | category labels in the shared report partial | mitigate | Shared `partials/cash_flow_report.html` renders via Jinja autoescape + `CASH_CATEGORIES`; never `| safe` |
| T-17-07 | Information Disclosure | net-profit caveat on mobile | mitigate | The shared `finance_tiles.html` always-visible `.muted` caveat is discoverable on touch/mobile (D-01b / UI-SPEC Q3 — reason a visible line, not a tooltip, was chosen) |
| T-17-05 | Elevation | new write path / auth | accept | All three mobile routes are GET-only reads; no write, no auth (single local operator, v1); existing mobile balance/forms/history untouched |
</threat_model>

<verification>
- `uv run pytest tests/test_finance_reports.py -x` green (mobile metrics/report/CSV web tests).
- `uv run pytest` full suite green.
- `uv run ruff check app/routes/mobile_finance.py` clean.
- `<human-check>` (end-of-phase, human_verify_mode=end-of-phase — phase UAT): on a phone-width browser open /m/finance — the three tiles render (single/2-col), the net tile shows the cash-outflow caveat, the stock tile shows «на текущий момент»; open /m/finance/report, pick «Неделя» — Приход/Расход swap in place; tap «Скачать CSV» — a cash_movements.csv downloads; the mobile balance, withdraw/deposit forms, and card history behave exactly as before.
</verification>

<success_criteria>
- Mobile `/m/finance` shows the gross/net/stock tiles at desktop parity via shared partials + `finance_base`.
- `/m/finance/report` + `/m/finance/report.csv` work (full/HX branch, plain-link CSV).
- Phase 15-16 mobile finance surface unchanged.
- Web tests green; full suite green.
</success_criteria>

<output>
Create `.planning/phases/17-financial-reports-export-dashboard-analytics/17-04-SUMMARY.md` when done.
</output>

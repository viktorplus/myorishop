---
phase: 19-products-page-rebuild
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/batches.py
  - app/routes/products.py
  - app/templates/partials/product_rows.html
  - app/templates/pages/products_list.html
  - app/static/style.css
  - tests/test_batches.py
  - tests/test_catalog.py
autonomous: true
requirements: [PROD-01, PROD-02, PROD-03, PROD-04, PROD-08]

must_haves:
  truths:
    - "Product list shows one row per product code carrying the total quantity summed across all of that code's batches"
    - "Operator can see each code's individual batches, each with its own expiry date and batch name"
    - "Product list shows each product's category and can be filtered by category (unchanged, regression-guarded)"
    - "The Добавить товар button is gone from the product list (page CTA and empty-state), and delete renders as a text link rather than a button"
    - "Existing pagination, filtering, and sorting on the product list keep working against the new grouped rows"
  artifacts:
    - path: "app/services/batches.py"
      provides: "batches_for_products(session, product_ids) — batched, non-N+1 open-batch query grouped by product_id"
      contains: "def batches_for_products"
    - path: "app/routes/products.py"
      provides: "_products_context() context dict now carries batches_by_id"
      contains: "batches_by_id"
    - path: "app/templates/partials/product_rows.html"
      provides: "Кол-во column, collapsed batch breakout <details>, text-link delete"
      contains: "link-danger"
    - path: "app/templates/pages/products_list.html"
      provides: "Add-product CTA removed from the products page"
    - path: "app/static/style.css"
      provides: ".link-danger destructive text-link rule (reuses #b91c1c, no new color role)"
      contains: "a.link-danger"
  key_links:
    - from: "app/routes/products.py"
      to: "app/services/batches.py"
      via: "batches_for_products(session, product_ids) called inside _products_context()"
      pattern: "batches_for_products\\("
    - from: "app/templates/partials/product_rows.html"
      to: "batches_by_id context key"
      via: "batches_by_id.get(product.id, [])"
      pattern: "batches_by_id\\.get"
    - from: "app/templates/partials/product_rows.html"
      to: "POST /products/{id}/quick-delete"
      via: "hx-post on the new <a class=\"link-danger\"> element (never a real href)"
      pattern: "hx-post=\"/products/\\{\\{ product\\.id \\}\\}/quick-delete"
---

<objective>
Rebuild `/products` from a flat, batch-blind list into a stock list the operator can scan by
code: add a quantity column, surface each code's open batches in a collapsed per-row expander,
remove the redundant "Добавить товар" entry point, and turn the delete action from a button into
an accessible text link — while leaving the existing filter/sort/pagination contract and the
already-working category filter completely untouched.

Purpose: Phase 19 goal — the products page reads as a stock list grouped by code, not a flat
per-batch dump with a redundant add path (PROD-01, PROD-02, PROD-03, PROD-04, PROD-08).

Output: `app/services/batches.py::batches_for_products`, an extended `_products_context()`,
a rebuilt `product_rows.html` + `products_list.html`, a new `.link-danger` CSS rule, and
~9 new automated tests covering every Wave 0 gap in `19-VALIDATION.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-products-page-rebuild/19-RESEARCH.md
@.planning/phases/19-products-page-rebuild/19-PATTERNS.md
@.planning/phases/19-products-page-rebuild/19-UI-SPEC.md
@.planning/phases/19-products-page-rebuild/19-VALIDATION.md
@app/services/batches.py
@app/routes/products.py
@app/templates/partials/product_rows.html
@app/templates/pages/products_list.html
@app/static/style.css
@tests/conftest.py
@tests/test_batches.py

<interfaces>
<!-- Existing contract this plan extends. Executor should use these directly. -->

From app/services/batches.py (existing, mirror this ordering exactly):
```python
def open_batches(
    session: Session, product_id: str, warehouse_id: str | None = None
) -> list[Batch]:
    stmt = select(Batch).where(Batch.product_id == product_id, Batch.quantity > 0)
    if warehouse_id is not None:
        stmt = stmt.where(Batch.warehouse_id == warehouse_id)
    return list(
        session.scalars(
            stmt.order_by(nullslast(Batch.expiry.asc()), Batch.created_at.asc())
        )
    )
```
NEW function to add, same file, same ordering, batched over a page of products:
```python
def batches_for_products(session: Session, product_ids: list[str]) -> dict[str, list[Batch]]:
    """Open batches (quantity > 0) for a PAGE of products, grouped by product_id.

    Same ordering as open_batches (earliest expiry first, NULL last, then
    oldest receipt) but ONE query for the whole page instead of N+1.
    """
    if not product_ids:
        return {}
    rows = session.scalars(
        select(Batch)
        .where(Batch.product_id.in_(product_ids), Batch.quantity > 0)
        .order_by(nullslast(Batch.expiry.asc()), Batch.created_at.asc())
    )
    grouped: dict[str, list[Batch]] = defaultdict(list)
    for batch in rows:
        grouped[batch.product_id].append(batch)
    return dict(grouped)
```
`nullslast, select` are already imported at the top of `app/services/batches.py`; add
`from collections import defaultdict` as a new import line.

From app/routes/products.py (existing, extend — do not replace):
```python
def _products_context(
    session: Session, *, code: str = "", name: str = "", category: str = "",
    sort: str = "", page: int = 0, blocked_id: str | None = None,
    blocked_qty: int | None = None,
) -> dict:
    result = list_products_view(session, code=code, name=name, category=category, sort=sort, page=page)
    pw = page_window(result["page"], result["total_pages"])
    ...
    return {
        "rows": result["rows"], "page": result["page"], "total": result["total"],
        "total_pages": result["total_pages"], "page_window": pw, "code": result["code"],
        "name": result["name"], "category": result["category"], "sort": result["sort"],
        "list_url": "/products", "rows_target_id": "product-rows", "extra_qs": extra_qs,
        "blocked_id": blocked_id, "blocked_qty": blocked_qty,
    }
```
Every existing key in this dict MUST stay unchanged (success criterion 5) — only add one new
key, `"batches_by_id"`, computed from `result["rows"]` after `list_products_view` runs.

From tests/test_batches.py (existing helpers, reuse verbatim):
```python
def _make_warehouse(session, name="Основной склад"): ...
def _make_batch(session, *, product_id, warehouse_id, expiry, quantity, is_legacy=0): ...
```

From tests/conftest.py (existing fixtures, reuse — do not redefine):
- `product` — zero-quantity product, zero batches (`code="TEST-001"`)
- `stocked_product` — quantity=8 via one ledger receipt on one batch with `expiry=None`,
  `name=None` (legacy-shaped: exercises the "—" null-guard, Pitfall 4)
- `client` — TestClient wired to `session`/`get_session` override; always seeds `product`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Batched batch-grouping service + route wiring (PROD-04 data contract)</name>
  <files>app/services/batches.py, app/routes/products.py, tests/test_batches.py</files>
  <read_first>
    app/services/batches.py:1-33 (open_batches — ordering to mirror exactly)
    app/routes/products.py:35-78 (_products_context — the dict to extend)
    tests/test_batches.py:1-51 (imports/helpers), :86-134 (existing ordering tests to mirror in style)
  </read_first>
  <behavior>
    - `batches_for_products(session, [])` returns `{}` (no query issued)
    - `batches_for_products(session, [p1.id, p2.id])` returns a dict keyed by product_id, each
      value a list of that product's open (quantity > 0) batches only — a quantity-0 batch for
      either product must never appear in any list
    - Ordering within each product's list matches `open_batches`: earliest expiry first, NULL
      expiry last, tie-broken by oldest `created_at`
    - `_products_context()`'s returned dict gains exactly one new key, `"batches_by_id"`, mapping
      each row currently on the page to its open batches (via `batches_for_products` on
      `[p.id for p in result["rows"]]`); every other existing key is unchanged
  </behavior>
  <action>
    In `app/services/batches.py`: add `from collections import defaultdict` to the imports, then
    add `batches_for_products(session, product_ids)` exactly as given in `<interfaces>` above —
    same `nullslast(Batch.expiry.asc()), Batch.created_at.asc()` ordering as `open_batches`,
    `quantity > 0` filter, one `IN (...)` query, Python-side `defaultdict(list)` grouping (RESEARCH
    Pattern 1 / PATTERNS.md). Give it a short docstring in the same one-paragraph style as
    `open_batches`. In `app/routes/products.py`: add `batches_for_products` to a new
    `from app.services.batches import batches_for_products` import line (this file currently has
    no import from `app.services.batches`). Inside `_products_context()`, after the
    `list_products_view(...)` call, compute `product_ids = [p.id for p in result["rows"]]`, call
    `batches_by_id = batches_for_products(session, product_ids)`, and add
    `"batches_by_id": batches_by_id` to the returned dict — do not reorder, rename, or remove any
    existing key (success criterion 5's filter/sort/pagination contract must not move).
  </action>
  <verify>
    <automated>uv run pytest tests/test_batches.py -q -k batches_for_products</automated>
  </verify>
  <done>
    Three new tests pass (empty-list guard, multi-product grouping excluding quantity-0 batches,
    ordering parity with `open_batches`); `_products_context()`'s dict carries `batches_by_id`
    alongside every pre-existing key; `uv run pytest tests/test_batches.py -q` fully green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Quantity column + collapsed batch breakout (PROD-03, PROD-04)</name>
  <files>app/templates/partials/product_rows.html, tests/test_catalog.py</files>
  <read_first>
    app/templates/partials/product_rows.html (full file, current state — this task extends it)
    app/templates/partials/batch_picker.html:41-64 (null-guard idiom + nested batch-table shape to
    copy: `{% if b.expiry %}{{ b.expiry | ru_date }}{% else %}<span class="muted">—</span>{% endif %}`)
    19-RESEARCH.md "Pattern 2: Zero-JS expand/collapse via native `<details>`"
    tests/conftest.py:38-49 (`product` — zero batches), :75-110 (`stocked_product` — one batch,
    quantity 8, `expiry=None`/`name=None`)
  </read_first>
  <behavior>
    - `/products` renders a `Кол-во` column showing `product.quantity` for every row (no null
      guard needed — `Product.quantity` is a non-nullable `Integer`, D-09)
    - A product with open batches renders a collapsed `<details><summary>Партии (N)</summary>`
      block listing each batch's expiry/name/quantity, with NULL expiry/name rendered as `—`
      (never blank)
    - A product with zero open batches renders no batch-breakout row at all (Pitfall 5 regression
      guard — the bare `product`/`client` fixtures must keep passing unmodified)
    - Zero `<input>`/`<select>` elements exist anywhere inside the new batch block (Pitfall 1 —
      would otherwise be swept into every `hx-include="#product-rows input, #product-rows select"`
      filter/sort/pagination request)
  </behavior>
  <action>
    In the header `<tr>`, insert `<th class="num">Кол-во</th>` between the `Категория` and
    `Закупочная` headers (matches the existing `Кол-во` convention used in
    `receipt_rows.html`/`transfer_rows.html`/`writeoff_rows.html`). In the filter `<tr
    class="filter-row">`, insert one matching empty `<th></th>` right after the category filter
    `<th>` and before the three existing empty `<th></th>` cells — no filter control for this
    column (Pitfall 1). In the row body, insert `<td class="num">{{ product.quantity }}</td>`
    between the category `<td>` and the cost (`Закупочная`) `<td>`. Update the blocked-delete
    error row's `colspan="6"` to `colspan="7"` (6 existing columns + 1 new = 7). Immediately after
    each product's `<tr>` (and its optional blocked-delete `<tr>`), add a new conditional sibling
    row: `{% set product_batches = batches_by_id.get(product.id, []) %}{% if product_batches %}`
    then a `<tr><td colspan="7"><details><summary>Партии ({{ product_batches | length }})</summary>`
    containing a `<table class="batch-picker">` with a `<thead>` of `Срок годности` / `Партия` /
    `Остаток` and a `<tbody>` looping `product_batches`, rendering `b.expiry` (via the `ru_date`
    filter) and `b.name` each guarded exactly like `batch_picker.html:52`
    (`{% if b.expiry %}...{% else %}<span class="muted">—</span>{% endif %}`, same shape for
    `b.name`) and `b.quantity` unguarded (Integer, never NULL) — close with
    `</details></td></tr>{% endif %}`. Render `<details>` WITHOUT the `open` attribute (collapsed
    by default, UI-SPEC Interaction Decision 1). Do not add any `<input>`/`<select>` inside this
    block.

    Add to `tests/test_catalog.py`:
    - `test_web_products_list_shows_quantity_column(client, stocked_product)` — GET `/products`,
      assert the response renders `stocked_product.quantity` (8) inside a `class="num"` cell for
      that row.
    - `test_web_products_list_shows_batch_breakdown_when_batches_exist(client, stocked_product)` —
      GET `/products`, assert `"Партии (1)"` appears in the response, and that the batch's NULL
      `expiry`/`name` render as `—` (not blank).
    - `test_web_products_list_no_batch_breakdown_when_no_batches(client, product)` — GET
      `/products`, assert `"Партии"` does not appear in the response text (the bare zero-batch
      `product` fixture must render no expand affordance).
  </action>
  <verify>
    <automated>uv run pytest tests/test_catalog.py -q -k "quantity_column or batch_breakdown"</automated>
  </verify>
  <done>
    New quantity column renders `product.quantity` on every row; batch breakout appears only for
    products with open batches, collapsed by default, with legacy NULL fields shown as `—`;
    zero-batch products render nothing extra; `uv run pytest tests/test_catalog.py -q` — all
    pre-existing 61 tests plus the 3 new ones pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Remove add-button, delete-as-text-link, CSS (PROD-01, PROD-02, PROD-08 regression)</name>
  <files>app/templates/pages/products_list.html, app/templates/partials/product_rows.html, app/static/style.css, tests/test_catalog.py</files>
  <read_first>
    app/templates/pages/products_list.html (full file, 9 lines)
    app/templates/partials/product_rows.html:58-64 (button being replaced), :79-85 (empty-state
    block being edited) — post-Task-2 state
    app/static/style.css:194-197 (`button.danger` — token being reused), :278-300 (`.batch-picker`
    + price-cue block — insertion point for the new rule, right after `.price-above` at line 300,
    before the "Mobile flow (Phase 11)" comment)
    19-UI-SPEC.md "Copywriting Contract" (empty-state body copy) and "Interaction Decisions #4"
    (delete-link accessibility contract, exact CSS)
  </read_first>
  <behavior>
    - `/products` response no longer contains `"Добавить товар"` anywhere (page-level CTA AND
      empty-state CTA); `GET /products/new` still returns 200 (PROD-01 explicitly keeps the route
      alive as the receipt/catalog-detail entry point — regression guard)
    - The delete control for each product row is an `<a>` carrying `hx-post=".../quick-delete"`,
      never a `<button>`, and its `href` is exactly `"#"` — never a real delete URL (WCAG 1.4.1 +
      htmx click-interception contract, Pitfall 2)
    - `PROD-08`'s existing category display + free-text filter are untouched by this plan — the
      pre-existing `test_list_products_view_filters_by_category` and
      `test_web_products_filter_row_narrows_results` tests stay green with zero code changes
  </behavior>
  <action>
    In `app/templates/pages/products_list.html`, delete the
    `<p class="page-actions"><a class="button" href="/products/new">Добавить товар</a></p>` line
    entirely (PROD-01) — leave `<h1>Товары</h1>` and the `{% include %}` untouched. Do NOT touch
    `/products/new` (GET) or `POST /products` in `app/routes/products.py` — both stay live as the
    catalog "изменить цену" deep-link and receipt-flow redirect target (RESEARCH Anti-Patterns).

    In `app/templates/partials/product_rows.html`: replace the
    `<button type="button" class="danger" hx-post=... hx-confirm=... hx-target=... hx-swap=...>Удалить</button>`
    with `<a href="#" class="link-danger" hx-post="/products/{{ product.id }}/quick-delete?page={{ page }}{{ extra_qs }}" hx-confirm="Удалить товар „{{ product.name }}“? Он будет скрыт из каталога и поиска, история операций сохранится." hx-target="#product-rows" hx-swap="outerHTML">Удалить</a>` —
    copy every `hx-*` attribute and the confirm text verbatim from the button being replaced; only
    the tag and class change. Keep the browser's default underline (do not set
    `text-decoration: none` anywhere for this element or its class). In the empty-state block
    (`<h2>Товаров пока нет</h2>...`), delete the
    `<p><a class="button" href="/products/new">Добавить товар</a></p>` line and replace the muted
    body paragraph with: `<p class="muted">Товары появляются в каталоге автоматически при
    оформлении приёмки. <a href="/receipts/new">Оформить приход</a></p>` (UI-SPEC Copywriting
    Contract — `/receipts/new` already exists as a route, confirmed).

    In `app/static/style.css`, insert immediately after the `.price-above` rule (line 300) and
    before the "Mobile flow (Phase 11)" comment block:
    ```css
    /* Text-link delete action (Phase 19, PROD-02): reuses the destructive token
       (#b91c1c) already named at button.danger above — no new color role. Keeps
       the browser's default underline (no text-decoration:none) so the link
       reads as interactive by shape, not color alone (WCAG 1.4.1). */
    a.link-danger {
      color: #b91c1c;
    }
    a.link-danger:hover {
      color: #7f1414;
    }
    ```

    Add to `tests/test_catalog.py`:
    - `test_web_products_list_has_no_add_button(client)` — GET `/products`, assert
      `"Добавить товар"` not in `response.text`.
    - `test_web_products_new_still_reachable_after_button_removal(client)` — GET `/products/new`
      returns 200 (PROD-01 scope-boundary regression guard).
    - `test_web_products_delete_control_is_link_not_button(client, product)` — GET `/products`,
      assert the response contains `hx-post="/products/{{ product.id }}/quick-delete` on an `<a`
      element and does not contain a `<button` carrying that same `hx-post` target.

    Finish with the full regression gate: run `uv run pytest -q` and `uv run ruff check`; the full
    suite must report **no fewer than 711 passed** (the pre-phase-19 baseline) plus this plan's 9
    new tests, with zero failures — this is the canary for `PROD-08` and every other untouched
    behavior (pagination, sort, category filter) staying intact per success criterion 5.
  </action>
  <verify>
    <automated>uv run pytest -q</automated>
  </verify>
  <done>
    "Добавить товар" absent from `/products` (both CTA locations); `/products/new` still returns
    200; delete control is an `<a href="#" class="link-danger">`, never a `<button>`; new
    `.link-danger` CSS rule present and reuses the existing destructive token; full suite green at
    ≥ 711 passed + 9 new tests; `ruff check` clean.
  </done>
</task>

</tasks>

## Artifacts This Phase Produces

New symbols introduced by this plan (excluded from drift verification — cited here for the
source-grounding pass):

- `app/services/batches.py::batches_for_products(session, product_ids) -> dict[str, list[Batch]]`
  — new function
- `app/routes/products.py::_products_context()` — new return-dict key `"batches_by_id"`
- `app/templates/partials/product_rows.html` — new `<th class="num">Кол-во</th>` column, new
  `<td class="num">{{ product.quantity }}</td>` cell, new conditional batch-breakout `<tr>` /
  `<details>` / `<summary>Партии (N)</summary>` block, new `<a href="#" class="link-danger">`
  delete control (replaces the removed `<button class="danger">`), new empty-state copy
  ("Товары появляются в каталоге автоматически при оформлении приёмки." + "Оформить приход" link)
- `app/templates/pages/products_list.html` — removal of the `<p class="page-actions">` CTA (no
  new symbol; a deletion)
- `app/static/style.css` — new CSS rule `a.link-danger` / `a.link-danger:hover`
- `tests/test_batches.py` — new test functions: `test_batches_for_products_empty_list_returns_empty_dict`,
  `test_batches_for_products_groups_by_product_excludes_zero_quantity`,
  `test_batches_for_products_ordering_matches_open_batches` (exact names at executor's
  discretion; behavior contract above is binding)
- `tests/test_catalog.py` — new test functions: `test_web_products_list_shows_quantity_column`,
  `test_web_products_list_shows_batch_breakdown_when_batches_exist`,
  `test_web_products_list_no_batch_breakdown_when_no_batches`,
  `test_web_products_list_has_no_add_button`,
  `test_web_products_new_still_reachable_after_button_removal`,
  `test_web_products_delete_control_is_link_not_button` (exact names at executor's discretion)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Operator browser → `GET`/`POST /products*` | Local-only, unauthenticated FastAPI routes (single-operator app, no auth in v1 per CLAUDE.md); filter/sort/page/delete inputs cross here — unchanged surface, no new input field added by this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-19-01 | Tampering / Information Disclosure | New batch-breakdown table in `product_rows.html` rendering `Batch.name`/`Batch.expiry` (operator-entered, stored text) | mitigate | Jinja2 autoescape only, never `\| safe` — identical rule already enforced at `batch_picker.html:22-23`; the new `<details>` batch table renders the same fields the same way, no new escaping surface |
| T-19-02 | Tampering | New `<a class="link-danger">` delete control replacing `<button class="danger">` (PROD-02) | mitigate | `href="#"` only, never the real delete URL; `POST /products/{id}/quick-delete` stays POST-only via FastAPI's route decorator (unchanged); htmx's `hx-post` triggers the AJAX request via click interception, so no GET-triggerable delete path exists even with JS/htmx disabled or on slow first paint |
| T-19-03 | Information Disclosure | `code`/`name`/`category` filter substrings on `/products` (unchanged input surface) | accept | Already mitigated pre-phase: `list_products_view` filters `Product.deleted_at IS NULL` before any substring match; this phase adds zero new filter inputs |
| T-19-SC | Tampering | npm/pip/cargo installs | not applicable | Zero new packages installed this phase — RESEARCH.md Package Legitimacy Audit: "Not applicable. This phase installs zero external packages." |
</threat_model>

<verification>
Per `19-VALIDATION.md` sampling contract:
- After each task commit: `uv run pytest tests/test_catalog.py -q` (~12s) + `uv run ruff check`
- After this plan (all 3 tasks): `uv run pytest tests/test_catalog.py tests/test_batches.py tests/test_pagination.py -q`
- Phase gate (before `/gsd-verify-work`): `uv run pytest -q` must report ≥ 711 passed (pre-phase-19
  baseline) plus this plan's 9 new tests, zero regressions
- PROD-08 regression: `uv run pytest tests/test_catalog.py -q -k category` stays green with no
  code changes to the category filter path
</verification>

<success_criteria>
1. `/products` shows one row per product code with a `Кол-во` column summing `Product.quantity`
   (PROD-03).
2. Each code's open batches are reachable via a collapsed per-row `<details>` showing expiry and
   batch name, `—` for NULL legacy fields (PROD-04).
3. Category still displays and filters correctly, unchanged (PROD-08).
4. "Добавить товар" is gone from both the page CTA and the empty state; delete renders as an
   accessible `<a class="link-danger">` text link, never a `<button>` (PROD-01, PROD-02).
5. Existing pagination/filter/sort keep working — full suite ≥ 711 passed + 9 new tests, zero
   regressions; `ruff check` clean.
</success_criteria>

<output>
Create `.planning/phases/19-products-page-rebuild/19-01-SUMMARY.md` when done
</output>

---
phase: 15-cash-ledger-foundation
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - app/services/finance.py
  - tests/test_finance.py
autonomous: true
requirements: [FIN-01, FIN-02, FIN-06]

must_haves:
  truths:
    - "cash_movements rows can be written ONLY through finance.record_cash_movement (single write path)"
    - "compute_balance returns the live SUM of amount_cents (0 on an empty ledger, correct signed sum otherwise)"
    - "record_cash_movement rejects a category outside CASH_CATEGORIES"
  artifacts:
    - path: "app/services/finance.py"
      provides: "next_seq, record_cash_movement (single write path), compute_balance (live SUM)"
      exports: ["next_seq", "record_cash_movement", "compute_balance"]
      min_lines: 30
    - path: "tests/test_finance.py"
      provides: "balance + write-path contract tests"
      contains: "compute_balance"
  key_links:
    - from: "finance.record_cash_movement"
      to: "settings.device_id / settings.operator_name"
      via: "audit-field stamping (mirrors ledger.py:114-117)"
      pattern: "settings\\.(device_id|operator_name)"
    - from: "finance.compute_balance"
      to: "cash_movements"
      via: "SELECT COALESCE(SUM(amount_cents), 0)"
      pattern: "func\\.sum\\(CashMovement\\.amount_cents\\)"
---

<objective>
Create `app/services/finance.py` — the sibling of `ledger.py` and the SINGLE
sanctioned write path for `cash_movements`. It stamps audit fields from
`settings`, computes a per-device `seq`, enforces the `CASH_CATEGORIES`
allow-list, and exposes a live-`SUM()` balance with no cache.

Purpose: Implements D-00b (single write path, live-SUM balance, Integer cents).
Every later hook (sale credit, return debit) and the balance page consume this
service — never the model or a raw insert.
Output: `next_seq`, `record_cash_movement`, `compute_balance`, and their unit tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-cash-ledger-foundation/15-CONTEXT.md
@.planning/phases/15-cash-ledger-foundation/15-RESEARCH.md
@.planning/phases/15-cash-ledger-foundation/15-PATTERNS.md
@.planning/phases/15-cash-ledger-foundation/15-01-SUMMARY.md
</context>

## Artifacts this phase produces (Plan 02 scope)

Newly-created symbols in this plan (exclude from source-drift verification):
- `app/services/finance.py` module — NEW
- `next_seq(session, device_id) -> int` — finance.py
- `record_cash_movement(session, *, category, amount_cents, sale_id=None, note=None, commit=True) -> CashMovement` — finance.py
- `compute_balance(session) -> int` — finance.py
- balance + contract tests in `tests/test_finance.py`

Consumes from Plan 01: `CashMovement`, `CASH_CATEGORIES` (app/models.py).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create app/services/finance.py (single write path + live-SUM balance)</name>
  <files>app/services/finance.py</files>
  <read_first>
    - app/services/ledger.py — `next_seq` (:23-33, the exact per-device max+1 pattern), `record_operation` audit stamping + `commit` flag (:104-129, the `session.add` + `if commit: session.commit()` core and the `settings.device_id`/`settings.operator_name`/`utcnow_iso()` stamps at :114-117), `compute_stock` live-SUM (:132-138) — CLONE these, DROP the product/batch guards (:74-102) and the cached-projection increments (:122-126)
    - app/models.py — `CashMovement`, `CASH_CATEGORIES` (from Plan 01)
    - app/core.py — `new_id` (:15), `utcnow_iso` (:20)
  </read_first>
  <behavior>
    - record_cash_movement stamps created_by = settings.operator_name, device_id = settings.device_id, and a monotonically increasing per-device seq.
    - record_cash_movement(category not in CASH_CATEGORIES) raises ValueError and stages no row.
    - compute_balance(empty session) == 0; after +12500 and -5000 it == 7500.
    - commit=False stages the row without committing (caller closes the transaction).
  </behavior>
  <action>
    Create `app/services/finance.py` as a sibling module of `ledger.py` (NOT a submodule),
    the SINGLE write path for cash_movements (D-00b). Imports: `from sqlalchemy import func, select`;
    `from sqlalchemy.orm import Session`; `from app.config import settings`; `from app.core import
    new_id, utcnow_iso`; `from app.models import CASH_CATEGORIES, CashMovement`. Define
    `next_seq(session: Session, device_id: str) -> int` mirroring ledger.next_seq but selecting
    `func.max(CashMovement.seq)` where device_id matches, returning `(current or 0) + 1`. Define
    `record_cash_movement(session: Session, *, category: str, amount_cents: int, sale_id: str | None = None,
    note: str | None = None, commit: bool = True) -> CashMovement`: first guard
    `if category not in CASH_CATEGORIES: raise ValueError(f"unknown cash category: {category!r}")`
    (the server-side allow-list, mirrors the ledger type guard at ledger.py:74-75 and the writeoffs
    reason allow-list); then build `CashMovement(id=new_id(), category=category, amount_cents=amount_cents,
    sale_id=sale_id, note=note, device_id=settings.device_id, seq=next_seq(session, settings.device_id),
    created_at=utcnow_iso(), created_by=settings.operator_name)`; `session.add(mv)`; `if commit:
    session.commit()`; `return mv`. amount_cents is passed through as-is (SIGNED Integer cents — callers
    pass a positive credit or a negative debit); NEVER coerce to float/Decimal (D-00b). The `commit`
    flag is the WR-03 staging contract. Define `compute_balance(session: Session) -> int` returning
    `session.scalar(select(func.coalesce(func.sum(CashMovement.amount_cents), 0)))` — whole-till live
    SUM, NO WHERE clause, NO cache (D-00b, Pitfall 4). Do NOT import or touch Product/Batch/Operation.
  </action>
  <acceptance_criteria>
    - `app/services/finance.py` exports `next_seq`, `record_cash_movement`, `compute_balance`.
    - record_cash_movement raises `ValueError` for a category not in CASH_CATEGORIES and stages nothing.
    - record_cash_movement stamps `created_by == settings.operator_name` and `device_id == settings.device_id`, and seq increments per device.
    - compute_balance returns `int` — 0 on an empty ledger, the signed SUM otherwise; the query uses `func.coalesce(func.sum(CashMovement.amount_cents), 0)` with no WHERE.
    - No import of Product / Batch / Operation in finance.py.
    - `uv run pytest tests/test_finance.py -k "balance or contract" -x` exits 0 (tests from Task 2).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_finance.py -k "balance or contract" -x</automated>
  </verify>
  <done>finance.py is the only cash write path; compute_balance is a cacheless live SUM; unknown category rejected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend tests/test_finance.py with balance + contract tests</name>
  <files>tests/test_finance.py</files>
  <read_first>
    - tests/test_finance.py — the append-only tests from Plan 01 (extend this file, keep imports additive)
    - tests/test_ledger.py — `test_audit_trail` (:127-141, the seq-increment + next_seq assertion pattern) and `test_record_operation_unknown_product_raises_value_error` (:49-54, the raises + rollback + count==0 pattern)
    - app/services/finance.py — `record_cash_movement`, `compute_balance`, `next_seq` (Task 1)
  </read_first>
  <behavior>
    - balance: empty ledger → 0; after a +12500 and a -5000 movement → 7500.
    - contract: two recorded movements have seq 1 then 2, next_seq == 3, created_by == settings.operator_name.
    - contract: an unknown category raises ValueError and leaves zero rows.
  </behavior>
  <action>
    Extend `tests/test_finance.py`. Add `from app.services.finance import compute_balance,
    next_seq, record_cash_movement`. Add: `test_balance_empty_is_zero(session)` asserting
    `compute_balance(session) == 0`; `test_balance_sums_mixed(session)` recording
    `record_cash_movement(session, category="sale", amount_cents=12500)` then
    `record_cash_movement(session, category="return", amount_cents=-5000)` and asserting
    `compute_balance(session) == 7500`; `test_contract_stamps_audit_and_seq(session)` recording two
    movements and asserting the first's `created_by == settings.operator_name`, non-empty `created_at`,
    `mv1.seq == 1`, `mv2.seq == 2`, and `next_seq(session, settings.device_id) == 3` (mirrors
    test_ledger.py:127-141); `test_contract_unknown_category_raises(session)` asserting
    `record_cash_movement(session, category="bogus", amount_cents=1)` raises ValueError, then
    `session.rollback()` and the cash_movements row count is 0 (mirrors test_ledger.py:49-54).
  </action>
  <acceptance_criteria>
    - `test_balance_sums_mixed` asserts a signed sum of exactly 7500.
    - `test_contract_unknown_category_raises` asserts ValueError AND zero rows persisted.
    - `uv run pytest tests/test_finance.py -x` exits 0 (append-only + balance + contract all green).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_finance.py -x</automated>
  </verify>
  <done>Balance correctness and the write-path contract (audit stamps, seq, category allow-list) are proven green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → record_cash_movement | The `category` argument is the only allow-listed value; an out-of-list category must be rejected before any row is staged. |
| amount_cents type | Money must stay Integer cents through the whole write + SUM path; a float anywhere corrupts the balance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02 | Tampering | record_cash_movement category argument | mitigate | `if category not in CASH_CATEGORIES: raise ValueError` before staging — server-side allow-list mirroring ledger.py:74-75 / the writeoffs reason guard. Verified by `test_contract_unknown_category_raises`. |
| T-15-06 | Tampering / Info-integrity | money type (float drift) | mitigate | amount_cents stays Integer end-to-end; compute_balance uses `func.sum` over an Integer column; no Decimal/float. The frozen-conventions test (Plan 01) enforces Integer *_cents. |
| T-15-07 | Tampering | second write path bypassing the service | mitigate | finance.record_cash_movement is the ONLY sanctioned insert (documented in the module docstring, mirroring ledger.py); rows are otherwise trigger-protected from mutation (Plan 01). |
| V5 Input validation | Tampering | category | mitigate | See T-15-02 (the one server-side allow-list this phase; numeric manual-input validation is Phase 16). |
| T-15-SC | Tampering | supply chain | accept (N/A) | Zero packages installed. |
| V2/V3/V4/V6 | — | — | accept (N/A) | Offline single-operator; no auth/session/crypto surface. |
</threat_model>

<verification>
- `uv run pytest tests/test_finance.py -x` — append-only + balance + contract green.
- `uv run pytest` — full suite still green.
</verification>

<success_criteria>
- finance.py is the single write path with audit stamping, per-device seq, and a category allow-list.
- compute_balance is a cacheless live SUM (0 on empty, correct signed sum otherwise).
</success_criteria>

<output>
Create `.planning/phases/15-cash-ledger-foundation/15-02-SUMMARY.md` when done.
</output>

---
phase: 15-cash-ledger-foundation
plan: 04
type: execute
wave: 4
depends_on: [15-02, 15-03]
files_modified:
  - app/routes/finance.py
  - app/routes/mobile_finance.py
  - app/templates/pages/finance.html
  - app/templates/mobile_pages/finance.html
  - app/templates/base.html
  - app/templates/mobile_pages/home.html
  - app/main.py
  - tests/test_finance.py
autonomous: true
requirements: [FIN-06]

must_haves:
  truths:
    - "Operator can open a «Финансы» section from the desktop nav and from the mobile hub"
    - "The Финансы page shows «Баланс кассы» with the current balance formatted via the cents filter (0 → 0,00; 12500 → 125,00; no currency glyph)"
    - "The Финансы page shows the balance ONLY — no movement list this phase"
  artifacts:
    - path: "app/routes/finance.py"
      provides: "GET /finance → pages/finance.html with balance_cents"
      contains: "compute_balance"
    - path: "app/routes/mobile_finance.py"
      provides: "GET /m/finance → mobile_pages/finance.html with balance_cents"
      contains: "/m/finance"
    - path: "app/templates/pages/finance.html"
      provides: "«Баланс кассы» heading + cents-formatted balance figure"
      contains: "Баланс кассы"
    - path: "app/templates/mobile_pages/finance.html"
      provides: "mobile «Баланс кассы» balance figure"
      contains: "Баланс кассы"
    - path: "app/main.py"
      provides: "finance.router + mobile_finance.router registration"
      contains: "finance.router"
  key_links:
    - from: "app/routes/finance.py"
      to: "finance.compute_balance"
      via: "GET /finance handler"
      pattern: "compute_balance\\(session\\)"
    - from: "app/templates/base.html"
      to: "/finance"
      via: "desktop nav link"
      pattern: "href=\"/finance\""
    - from: "app/templates/mobile_pages/home.html"
      to: "/m/finance"
      via: "mobile hub tile"
      pattern: "href=\"/m/finance\""
---

<objective>
Make the cash balance observable: two thin read-only routes (`GET /finance`,
`GET /m/finance`) that render `compute_balance`, a «Баланс кассы» page on both
desktop and mobile (balance ONLY, no list), and a «Финансы» entry in BOTH the
desktop nav and the mobile hub.

Purpose: Implements FIN-06 and the UI decisions D-01 (balance-only), D-02 (nav in
both places), D-04 (heading «Баланс кассы», `cents` filter, no currency glyph).
This turns the phase's success criteria into something the operator can see.
Output: finance + mobile_finance routes, two balance templates, nav/tile edits,
router registration, and the page-render test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-cash-ledger-foundation/15-CONTEXT.md
@.planning/phases/15-cash-ledger-foundation/15-RESEARCH.md
@.planning/phases/15-cash-ledger-foundation/15-PATTERNS.md
@.planning/phases/15-cash-ledger-foundation/15-02-SUMMARY.md
@.planning/phases/15-cash-ledger-foundation/15-03-SUMMARY.md
</context>

## Artifacts this phase produces (Plan 04 scope)

Newly-created symbols in this plan (exclude from source-drift verification):
- `app/routes/finance.py` → `finance_page` (GET /finance) — NEW
- `app/routes/mobile_finance.py` → `mobile_finance_page` (GET /m/finance) — NEW
- `app/templates/pages/finance.html` — NEW
- `app/templates/mobile_pages/finance.html` — NEW
- `finance.router` / `mobile_finance.router` registration — `app/main.py`
- «Финансы» desktop nav link (base.html) + mobile tile (mobile_pages/home.html)
- page-render test in `tests/test_finance.py`

Consumes from Plan 02: `finance.compute_balance`. Consumes from Plan 01: the `cents`
Jinja filter (already registered at app/routes/__init__.py:12).

<tasks>

<task type="auto">
  <name>Task 1: Create finance + mobile_finance routes and register them</name>
  <files>app/routes/finance.py, app/routes/mobile_finance.py, app/main.py</files>
  <read_first>
    - app/routes/writeoffs.py — `writeoff_page` GET-page shape (:25-33) and the import block (:1-16, APIRouter/Depends/Request, `get_session`, `from app.routes import templates`)
    - app/routes/mobile_reports.py — the `/m/` prefix route pattern (:17-23)
    - app/routes/__init__.py — the shared `templates` env and the registered `cents` filter (:9-18)
    - app/main.py — the `from app.routes import (...)` block (:8-36) and the `include_router` list (:53-79)
    - app/services/finance.py — `compute_balance` (Plan 02)
  </read_first>
  <action>
    Create `app/routes/finance.py`: an `APIRouter`; imports `from fastapi import APIRouter, Depends,
    Request`, `from sqlalchemy.orm import Session`, `from app.db import get_session`, `from app.routes
    import templates`, `from app.services.finance import compute_balance`. Define
    `@router.get("/finance")` def `finance_page(request: Request, session: Session = Depends(get_session))`
    returning `templates.TemplateResponse(request, "pages/finance.html", {"balance_cents":
    compute_balance(session)})`. Create `app/routes/mobile_finance.py` mirroring
    `routes/mobile_reports.py`: `@router.get("/m/finance")` def `mobile_finance_page(...)` returning
    `mobile_pages/finance.html` with the same `{"balance_cents": compute_balance(session)}` context.
    Register BOTH in `app/main.py`: add `finance` and `mobile_finance` to the `from app.routes import
    (...)` block and add `app.include_router(finance.router)` and
    `app.include_router(mobile_finance.router)` to the include list. These routes are READ-ONLY — they
    call `compute_balance` only; NEVER import `record_cash_movement` or any cash-write function in a
    route (D-00c — no route writes cash).
  </action>
  <acceptance_criteria>
    - `GET /finance` returns 200 and renders `pages/finance.html` with context `{"balance_cents": compute_balance(session)}`.
    - `GET /m/finance` returns 200 and renders `mobile_pages/finance.html` with the same context.
    - `app/main.py` imports and registers both `finance.router` and `mobile_finance.router`.
    - Neither route module imports a cash-write function (only `compute_balance`).
    - `uv run pytest tests/test_finance.py -k page -x` exits 0 (test from Task 3).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_finance.py -k page -x</automated>
  </verify>
  <done>Both /finance and /m/finance serve the current balance via compute_balance and are registered in the app.</done>
</task>

<task type="auto">
  <name>Task 2: Create balance templates + desktop nav + mobile tile</name>
  <files>app/templates/pages/finance.html, app/templates/mobile_pages/finance.html, app/templates/base.html, app/templates/mobile_pages/home.html</files>
  <read_first>
    - app/templates/pages/reports_landing.html — the desktop `{% extends "base.html" %}{% block content %}` shape
    - app/templates/mobile_pages/reports_expiry.html — the mobile `{% extends "mobile_base.html" %}` + `{% block content %}` shape and how `| cents` renders money (:13)
    - app/templates/base.html — the nav link list (:26-42) and the active-state pattern `{% if request.url.path.startswith(...) %}`
    - app/templates/mobile_pages/home.html — the tile grid (:5-14, `<a class="mobile-tile" href="/m/...">`)
    - app/core.py — `format_cents` behavior (:49-53): 0 → "0,00", 12500 → "125,00", sign-aware, NO glyph
  </read_first>
  <action>
    Create `app/templates/pages/finance.html` extending `base.html` with a `{% block content %}`
    containing `<h1>Баланс кассы</h1>` (D-04 heading VERBATIM) and a prominent balance figure rendering
    `{{ balance_cents | cents }}` (D-04 — the registered `cents` filter yields 125,00 for 12500 and 0,00
    for 0, with NO ₽/currency glyph). Balance ONLY — do NOT add a movement list or table (D-01 — full
    history is Phase 16). Create `app/templates/mobile_pages/finance.html` extending `mobile_base.html`
    with the same `<h1>Баланс кассы</h1>` heading and the same `{{ balance_cents | cents }}` figure
    (two thin inline pages is fine given how small this is — sharing a partial is optional, Claude's
    discretion). Add the desktop nav link to `base.html` after the `/export` link (~base.html:38):
    an `<a href="/finance">Финансы</a>` with the active-state check
    `{% if request.url.path.startswith("/finance") %} class="active"{% endif %}` (D-02 — analytical
    section, near Отчёты/Экспорт). Add a mobile tile to the `mobile_pages/home.html` grid (~home.html:13):
    `<a class="mobile-tile" href="/m/finance">Финансы</a>` (D-02).
  </action>
  <acceptance_criteria>
    - `pages/finance.html` and `mobile_pages/finance.html` both contain the exact heading text «Баланс кассы» and render the balance via `{{ balance_cents | cents }}` (no currency glyph, no movement list).
    - `base.html` nav contains an `href="/finance"` link with a `request.url.path.startswith("/finance")` active check.
    - `mobile_pages/home.html` contains an `href="/m/finance"` tile with class `mobile-tile`.
    - `uv run pytest tests/test_finance.py -k page -x` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_finance.py -k page -x</automated>
  </verify>
  <done>«Финансы» is reachable from the desktop nav and the mobile hub; both pages show «Баланс кассы» formatted via the cents filter, balance-only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Route/render test for the balance page</name>
  <files>tests/test_finance.py</files>
  <read_first>
    - tests/test_finance.py — the tests from Plans 01-03 (extend this file)
    - tests/conftest.py — the `client` fixture (:128-156, TestClient with get_session overridden to the test `session`, backup disabled) and the `session` fixture
    - app/routes/finance.py — `finance_page` (Task 1)
    - app/services/finance.py — `record_cash_movement` (to seed a known balance in-test)
  </read_first>
  <behavior>
    - page (empty): GET /finance returns 200 and the body contains «Баланс кассы» and "0,00".
    - page (non-zero): after recording movements summing to 12500 on the test session, GET /finance body contains "125,00".
  </behavior>
  <action>
    Extend `tests/test_finance.py` with route tests using the `client` fixture (its get_session override
    yields the same test `session`, so movements recorded in the test are visible to the route). Add
    `test_page_empty_shows_zero(client)` — `client.get("/finance")` returns status 200 and its `.text`
    contains both «Баланс кассы» and "0,00". Add `test_page_shows_balance(client, session)` — record cash
    movements summing to 12500 via `record_cash_movement` on `session` (e.g. category="sale",
    amount_cents=12500), then `client.get("/finance")` and assert "125,00" is in the body. Assert on the
    rendered markup, not internals. Optionally add a mobile variant hitting `/m/finance` via the
    `mobile_client_factory` with `mobile_finance.router` (mirrors the Phase 11 mobile test pattern).
  </action>
  <acceptance_criteria>
    - `test_page_empty_shows_zero` asserts status 200 AND «Баланс кассы» AND "0,00" in the body.
    - `test_page_shows_balance` asserts "125,00" appears in the rendered body after a 12500-cent balance is recorded.
    - `uv run pytest tests/test_finance.py -x` exits 0 (append-only + balance + contract + integration + page all green).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_finance.py -x</automated>
  </verify>
  <done>GET /finance renders «Баланс кассы» with the balance formatted by the cents filter, proven by TestClient.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GET /finance → template | Read-only display of a computed integer; no user input crosses this boundary this phase. |
| free-text note → render | No user-entered note is rendered on the balance-only page this phase (history/CSV surfaces are Phase 16/17). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-10 | Tampering (write from a read route) | routes/finance.py, routes/mobile_finance.py | mitigate | Routes call `compute_balance` ONLY; acceptance criterion asserts no cash-write import in either route (D-00c). Cash rows remain trigger-protected from mutation (Plan 01). |
| T-15-11 | Injection (unescaped output) | pages/finance.html, mobile_pages/finance.html | mitigate | Jinja2 autoescape (default) + the sanctioned `cents` filter render an integer only; no raw/`|safe` output and no user-entered string is rendered this phase. |
| T-15-SC | Tampering | supply chain | accept (N/A) | Zero packages installed. |
| V2/V3/V4/V6 auth/session/access/crypto | — | — | accept (N/A) | Offline single-operator display route; no auth/CSRF surface. |
</threat_model>

<verification>
- `uv run pytest tests/test_finance.py -x` — all finance tests including page render green.
- `uv run pytest` — FULL suite green (phase gate before /gsd-verify-work).
- Human-check (human_verify_mode = end-of-phase): open `/finance` and `/m/finance` in the browser; confirm «Баланс кассы» shows the current figure with no currency glyph; register a sale and confirm the balance rises by the total; register a return against it and confirm the balance drops back to the pre-sale value.
</verification>

<success_criteria>
- «Финансы» reachable from desktop nav and mobile hub (FIN-06 / D-02).
- Balance page shows «Баланс кассы» via the cents filter, balance-only (D-01 / D-04).
- Registering a sale raises the displayed balance; registering the matching return restores it (phase success criteria 2 + 3, observed end-to-end through the UI).
</success_criteria>

<output>
Create `.planning/phases/15-cash-ledger-foundation/15-04-SUMMARY.md` when done.
</output>

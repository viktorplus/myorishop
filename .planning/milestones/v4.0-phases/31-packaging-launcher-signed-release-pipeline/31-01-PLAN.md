---
phase: 31-packaging-launcher-signed-release-pipeline
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_packaging.py
  - tests/test_launcher.py
  - tests/test_release_verify.py
autonomous: true
requirements: [PKG-01, PKG-02, PKG-03, PKG-04, PKG-05]
must_haves:
  truths:
    - "The three new packaging test files COLLECT without import errors (RED-by-design, never a collection ERROR)"
    - "PKG-01 (onedir layout), PKG-02 (per-user .iss installer), PKG-03 (data-sibling separation), PKG-04 (swap→migrate→restart + matched-pair rollback), and PKG-05 (manifest SHA-256 + minisign verify) are pinned as executable assertions before any implementation exists"
    - "Not-yet-built modules (launcher.swap, build_release) are imported INSIDE test bodies so Wave-0 collection stays green"
  artifacts:
    - path: "tests/test_packaging.py"
      provides: "PKG-03 data-dir-sibling gate + PKG-01 onedir layout + PKG-02 per-user .iss installer assertions (RED)"
      contains: "MYORISHOP_DATA_DIR"
    - path: "tests/test_launcher.py"
      provides: "PKG-04 swap/rollback state-machine + path-traversal-confinement assertions (RED)"
      contains: "apply_update"
    - path: "tests/test_release_verify.py"
      provides: "PKG-05 manifest sha256/version + minisign round-trip (tamper-fail) assertions (RED)"
      contains: "manifest"
  key_links:
    - from: "tests/test_packaging.py"
      to: "app.config.settings (reloaded under MYORISHOP_DATA_DIR)"
      via: "importlib.reload with monkeypatched env"
      pattern: "importlib.reload"
    - from: "tests/test_launcher.py"
      to: "launcher.swap.apply_update"
      via: "in-body import (module built in Plan 03)"
      pattern: "from launcher"
    - from: "tests/test_release_verify.py"
      to: "build_release (manifest + sha256)"
      via: "in-body import (module built in Plan 04)"
      pattern: "import build_release"
---

<objective>
Lay the Nyquist Wave-0 test scaffold for Phase 31: three RED-by-design test files that pin
the three automatable requirement contracts (PKG-03 data separation, PKG-04 launcher
swap/rollback, PKG-05 signed-release manifest+verify) as executable assertions BEFORE any
implementation module exists. Later plans turn these files green by BUILDING the modules —
they do not edit these test files.

Purpose: Establish the deterministic feedback gate the whole phase samples against. Every
implementation plan (02–05) reads its acceptance contract straight from these tests rather
than re-deriving it.
Output: `tests/test_packaging.py`, `tests/test_launcher.py`, `tests/test_release_verify.py`.
</objective>

<artifacts_produced>
Artifacts this phase produces (introduced by THIS plan):
- `tests/test_packaging.py` — PKG-03 sibling-data gate (reload `app.config` under `MYORISHOP_DATA_DIR`) + PKG-01 `build_release` onedir-layout assertions + PKG-02 `build_release.generate_iss` per-user `.iss` installer assertion.
- `tests/test_launcher.py` — PKG-04 `launcher.swap.apply_update` happy-path, migrate-fail rollback, health-fail rollback, and `parse_pending` path-traversal confinement.
- `tests/test_release_verify.py` — PKG-05 `manifest.txt` SHA-256 correctness + tamper-detection (pure Python) and a minisign sign→verify round-trip / tamper-fail (skip-gated when `minisign` binary absent); the vendored-pubkey assertion is skip-gated on `app/minisign.pub` existence (the operator supplies it offline — never CI).

Downstream artifacts these tests reference (built later, imported inside test bodies): `app/config.py` seam (Plan 02), `launcher/swap.py` + `launcher/adapters.py` (Plan 03), `build_release.py` (Plan 04), `app/minisign.pub` + `.github/workflows/release.yml` (Plan 05).
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-packaging-launcher-signed-release-pipeline/31-RESEARCH.md
@.planning/phases/31-packaging-launcher-signed-release-pipeline/31-PATTERNS.md
@tests/conftest.py
@tests/test_offline.py
@app/config.py
@restore.bat
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED scaffold for PKG-03 data separation + PKG-01 onedir layout (tests/test_packaging.py)</name>
  <files>tests/test_packaging.py</files>
  <read_first>
    - app/config.py (current db_path:22, backup_dir:68, env_file model_config:20, _resolve_local_identity:78-96 — the seam Plan 02 will root under MYORISHOP_DATA_DIR)
    - tests/test_offline.py:1-40 (the Nyquist Wave-0 RED convention: module docstring states RED-by-design; not-yet-built symbols imported INSIDE test bodies)
    - tests/conftest.py:22-33 (tmp_path file-based SQLite convention — never in-memory)
  </read_first>
  <action>
    Create tests/test_packaging.py as a RED-by-design Wave-0 scaffold with a module docstring
    stating the tests are RED until Plans 02/04 build the seam and build_release, mirroring
    tests/test_offline.py's contract note. Pin the PKG-03 gate: a helper that sets
    monkeypatch.setenv("MYORISHOP_DATA_DIR", str(tmp_path / "data")), then does
    importlib.reload(app.config) and returns the fresh settings — because _DATA_DIR is resolved
    at import time (RESEARCH Pattern 3). Write test_data_paths_are_siblings asserting that under
    a set MYORISHOP_DATA_DIR EVERY operator data path — db_path, backup_dir, the resolved
    env_file, and the secret_key/device_id files (Path(db_path).parent) — resolves UNDER that
    data dir and that NONE of them resolves under a simulated sibling app dir (tmp_path / "app").
    Write test_backup_dir_is_absolute_not_cwd_relative asserting Path(reloaded.backup_dir).is_absolute()
    — this is RESEARCH Pitfall 2, the single wipe-risk line. Write test_swap_of_app_dir_cannot_reach_data:
    build tmp install root with sibling app/ and data/, set MYORISHOP_DATA_DIR to data/, reload,
    resolve every data path, then assert not any path is relative to the app dir (the swap-safety
    invariant). For PKG-01, add test_onedir_layout_has_pth_and_site_packages and
    test_onedir_bundles_all_alembic_versions that import build_release INSIDE the body (module
    arrives Plan 04), call its onedir assembler against a synthetic embeddable zip + fake wheel
    dir, and assert the python313._pth contains "Lib\\site-packages" and "import site" (RESEARCH
    Pattern 1 / Pitfall 1) and that the bundled alembic/versions/*.py count equals the repo's 22.
    For PKG-02, add test_generate_iss_is_per_user_with_shortcut_and_uninstaller that imports build_release INSIDE the body (generate_iss arrives Plan 04), calls generate_iss against a tmp dist dir with version "1.<N>", and asserts the emitted MyOriShop.iss contains PrivilegesRequired=lowest, DefaultDirName={localappdata}\MyOriShop (per-user %LOCALAPPDATA%), an [Icons] {autoprograms} Start-Menu shortcut line pointing at the launcher, an UninstallDisplayIcon uninstaller directive, AppVersion=1.<N>, and that NO data\ source line is shipped (RESEARCH Pattern 2 / PKG-03 data-not-shipped). Cite PKG-03/PKG-01/PKG-02 in test docstrings. Do NOT import launcher/build_release at module top
    (keep collection green). Use only pytest + stdlib (importlib, os, pathlib).
  </action>
  <verify>
    <automated>uv run pytest tests/test_packaging.py --co -q</automated>
  </verify>
  <acceptance_criteria>
    `pytest --co` collects tests/test_packaging.py with zero collection errors (imports of not-yet-built build_release live inside test bodies). Running the file is RED (assertions fail / build_release import errors surface only at call time), never an ERROR at collection. The PKG-03 sibling gate, PKG-01 layout, and PKG-02 .iss (test_generate_iss_is_per_user_with_shortcut_and_uninstaller) assertions are present and reference MYORISHOP_DATA_DIR, python313._pth, "import site", the 22-migration count, PrivilegesRequired=lowest, and {localappdata}\MyOriShop.
  </acceptance_criteria>
  <done>All <acceptance_criteria> above are satisfied and independently verifiable via the task's <automated> check (RED-&gt;GREEN on the named test selectors).</done>
</task>

<task type="auto">
  <name>Task 2: RED scaffold for PKG-04 launcher swap/rollback + path confinement (tests/test_launcher.py)</name>
  <files>tests/test_launcher.py</files>
  <read_first>
    - .planning/phases/31-packaging-launcher-signed-release-pipeline/31-RESEARCH.md ("Launcher swap core as a testable pure function" apply_update skeleton; Pattern 4 stop→swap→migrate→restart; Security V12 path-traversal confinement)
    - .planning/phases/31-packaging-launcher-signed-release-pipeline/31-PATTERNS.md (launcher.py/swap.py section; restore.bat WAL-sidecar delete rollback)
    - restore.bat:10-14 (the DB-half of the matched-pair rollback: copy backup then delete -wal/-shm)
  </read_first>
  <action>
    Create tests/test_launcher.py as a RED-by-design scaffold. Import launcher.swap symbols
    (Paths, Pending, apply_update, parse_pending) INSIDE each test body (built in Plan 03) so
    collection stays green. Pin the PKG-04 contract with injected callbacks so it runs on any OS
    with fake dirs (no Windows/process specifics): test_apply_update_happy_path — build tmp app/,
    staged/, data/ dirs; a recording stub set for stop_app/start_app/migrate/health_ok(returns
    True)/backup_restore; call apply_update and assert the rename order left staged content now at
    app/, that app.prev/ was removed on success, and that migrate ran between the two os.replace
    calls. test_apply_update_rolls_back_on_migrate_failure — migrate raises; assert app/ is
    restored to the ORIGINAL content, the staged code is moved aside to app.failed/, backup_restore
    was called with pending.db_backup_path, start_app ran again, and apply_update re-raises.
    test_apply_update_rolls_back_on_failed_health_check — health_ok returns False; assert the same
    matched-pair rollback path. test_backup_restore_deletes_wal_sidecars — a small check that the
    Plan-03 backup_restore adapter contract (copy backup → data/myorishop.db then unlink -wal/-shm,
    mirroring restore.bat) is exercised, using a fake backup file. For security V12:
    test_parse_pending_rejects_path_traversal — feed pending payloads whose staged_dir /
    db_backup_path contain ".." or an absolute path escaping the install root and assert parse_pending
    raises/rejects; test_parse_pending_requires_valid_marker — malformed / missing-field JSON is
    rejected before any swap. Cite PKG-04 and threat T-31-04/T-31-05/T-31-06 in docstrings. Use only
    pytest + stdlib.
  </action>
  <verify>
    <automated>uv run pytest tests/test_launcher.py --co -q</automated>
  </verify>
  <acceptance_criteria>
    `pytest --co` collects tests/test_launcher.py with zero collection errors (launcher.swap imports are inside bodies). Tests are RED until Plan 03. The file pins: happy-path rename ordering, migrate-fail rollback, health-fail rollback, WAL-sidecar-delete on restore, and parse_pending traversal/marker rejection — each callback-injected and OS-agnostic.
  </acceptance_criteria>
  <done>All <acceptance_criteria> above are satisfied and independently verifiable via the task's <automated> check (RED-&gt;GREEN on the named test selectors).</done>
</task>

<task type="auto">
  <name>Task 3: RED scaffold for PKG-05 manifest + minisign verify (tests/test_release_verify.py)</name>
  <files>tests/test_release_verify.py</files>
  <read_first>
    - .planning/phases/31-packaging-launcher-signed-release-pipeline/31-RESEARCH.md (Pattern 5 two-stage offline flow; manifest.txt = version + archive SHA-256; Code Examples minisign -G/-S/-V; Environment Availability — minisign likely absent locally)
    - app/__init__.py:5 (__version__ single source, scheme "1.<N>")
    - .github/workflows/ci.yml:37-43 (the established skip-when-tool-absent convention — PG tests auto-skip without DATABASE_URL; mirror it with a shutil.which("minisign") skip)
  </read_first>
  <action>
    Create tests/test_release_verify.py as a RED-by-design scaffold. Pin the PKG-05 checksum half
    in PURE PYTHON (always runs, no external tool): import build_release INSIDE the body (Plan 04);
    test_manifest_binds_version_and_sha256 — assert build_release's manifest writer emits lines for
    version=1.<N>, the archive filename, and the archive's real hashlib.sha256 hex; write a small
    byte file, compute its sha256, and assert the manifest matches. test_manifest_tamper_is_detected
    — flip one byte of the archive after the manifest is written and assert a verify helper that
    re-hashes the archive against the manifest sha256 returns False (the checksum guard). Pin the
    tag↔version contract: test_build_asserts_tag_matches_version — build_release's assertion accepts
    tag "v1.<N>" only when it equals app/__init__.py __version__ ("1.<N>") and rejects a mismatch
    (RESEARCH tag↔version contract). Pin the signature half behind a skip: a module-level
    minisign_available = shutil.which("minisign") is not None and @pytest.mark.skipif on it (mirror
    ci.yml PG auto-skip). test_minisign_roundtrip_verifies_and_tamper_fails — generate a THROWAWAY
    keypair in tmp_path via `minisign -G` (never app/minisign.pub, whose secret is offline-only),
    sign a manifest.txt, assert `minisign -Vm` passes with the throwaway pubkey, then corrupt the
    manifest and assert verify FAILS. test_vendored_pubkey_present_and_bundled — skip-gated on Path("app/minisign.pub").exists() via @pytest.mark.skipif (the production keypair is generated OFFLINE by the operator with minisign -G, never by CI — threat T-31-02); WHEN the operator has supplied app/minisign.pub,
    assert its base64 line starts "RW" and that build_release's onedir bundles it; otherwise the test SKIPS so the suite stays green in CI/dev, and
    the operator supplies the production key offline per docs/RELEASE.md. Cite PKG-05 and threats T-31-02/T-31-03 in docstrings.
    Use pytest + stdlib (hashlib, shutil, subprocess) only.
  </action>
  <verify>
    <automated>uv run pytest tests/test_release_verify.py --co -q</automated>
  </verify>
  <acceptance_criteria>
    `pytest --co` collects tests/test_release_verify.py with zero collection errors. Pure-Python manifest/sha256/tamper/version-contract tests are present and RED until Plan 04; the minisign round-trip is `skipif` when the binary is absent (never a hard failure on the dev box); the vendored-pubkey test is `skipif` when app/minisign.pub is absent (the operator supplies it offline; asserts starts-with-RW + bundled only when present).
  </acceptance_criteria>
  <done>All <acceptance_criteria> above are satisfied and independently verifiable via the task's <automated> check (RED-&gt;GREEN on the named test selectors).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release asset → client | Downloaded archive/manifest is untrusted until SHA-256 + Ed25519 verified (Phase 32 gate; Phase 31 pins the assertions) |
| staged dir / pending.json → launcher | Marker + staged code are untrusted input the launcher acts on (path-traversal surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03 | Tampering | release archive / manifest | mitigate | test_release_verify pins SHA-256 tamper-detection + minisign tamper-fail as executable gates before Plan 04/05 implement them |
| T-31-05 | Tampering | pending.json / staged_dir paths | mitigate | test_launcher pins parse_pending rejection of `..`/absolute paths escaping the install root (V12) as a RED contract |
| T-31-06 | DoS (data integrity) | launcher rollback | mitigate | test_launcher pins matched-pair rollback (code restore + WAL-sidecar-delete DB restore) as a RED contract |
| T-31-SC | Tampering | test tooling (minisign) | accept | scaffold shells out to the vendored/official minisign binary only; skip-gated when absent; no new PyPI runtime dependency added |
</threat_model>

<verification>
- `uv run pytest --co -q` collects the whole suite (all three new files import-clean) with zero errors.
- The three new files are RED-by-design: running them fails on assertions / in-body imports of not-yet-built modules, never on collection.
- Full suite baseline (~1144 passed / 12 skipped) still collects; no existing test regressed by the additions.
</verification>

<success_criteria>
- Three test files exist pinning PKG-03, PKG-04, PKG-05 contracts as executable assertions.
- Collection is green; the tests are RED (or skip-gated for minisign) exactly as designed.
- Plans 02–05 can read their acceptance contract from these files without editing them.
</success_criteria>

<output>
Create `.planning/phases/31-packaging-launcher-signed-release-pipeline/31-01-SUMMARY.md` when done.
</output>

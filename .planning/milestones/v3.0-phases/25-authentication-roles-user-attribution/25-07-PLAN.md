---
phase: 25-authentication-roles-user-attribution
plan: 07
type: execute
wave: 4
depends_on: ["25-03", "25-04"]
files_modified:
  - app/services/ledger.py
  - app/services/finance.py
  - tests/test_attribution.py
autonomous: true
requirements: [USER-05]
must_haves:
  truths:
    - "record_operation stamps author_id + created_by from the request-scoped current user at the single write path"
    - "record_cash_movement stamps author_id + created_by the same way"
    - "A sale/receipt/cash op POSTed through the real authenticated TestClient persists author_id equal to the logged-in user's id (contextvars propagate through FastAPI's def-endpoint threadpool)"
    - "With no user in context (existing tests/fixtures/scripts) attribution falls back to settings.operator_name and author_id stays None — existing tests stay green"
  artifacts:
    - path: "app/services/ledger.py"
      provides: "author_id/created_by stamped via author_fields()"
      contains: "author_fields"
    - path: "app/services/finance.py"
      provides: "author_id/created_by stamped via author_fields()"
      contains: "author_fields"
    - path: "tests/test_attribution.py"
      provides: "contextvars threadpool-propagation proof at both write paths"
      contains: "author_id"
  key_links:
    - from: "app/services/ledger.py::record_operation"
      to: "app/services/security.py::author_fields"
      via: "author_id, created_by = author_fields()"
      pattern: "author_fields\\(\\)"
    - from: "app/services/finance.py::record_cash_movement"
      to: "app/services/security.py::author_fields"
      via: "author_id, created_by = author_fields()"
      pattern: "author_fields\\(\\)"
---

<objective>
Thread the logged-in user into the two single write paths — `record_operation` and `record_cash_movement` — via `author_fields()`, and PROVE (the phase's single riskiest mechanism) that the `contextvars` current-user set by the async guard actually propagates into FastAPI's sync `def`-endpoint threadpool so `author_id` is stamped correctly.

Purpose: USER-05 (every operation and cash movement records the logged-in user as its author, stamped at the single `record_operation()` write path). The write path stays single (no duplication, no new params on the ~7 service callers). The contextvars propagation caveat (RESEARCH Pitfall 4 / Assumption A1) is closed by a mandatory end-to-end proving test — if it fails, the documented fallback is explicit-parameter threading.
Output: two-line edits to both write functions + `tests/test_attribution.py` proving real author attribution.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-authentication-roles-user-attribution/25-RESEARCH.md
@.planning/phases/25-authentication-roles-user-attribution/25-PATTERNS.md
@app/services/ledger.py
@app/services/finance.py
@tests/conftest.py
</context>

<artifacts>
This plan produces:
- `app/services/ledger.py::record_operation`: `author_id, created_by = author_fields()` replaces the `created_by=settings.operator_name` read; the `Operation(...)` insert gains `author_id=author_id` and uses the resolved `created_by`.
- `app/services/finance.py::record_cash_movement`: the identical change on the `CashMovement(...)` insert.
- `tests/test_attribution.py`: the contextvars threadpool-propagation proof at both write paths (`-k attribution`), plus the fallback-when-unset assertion.
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stamp author_id at both single write paths via author_fields()</name>
  <files>app/services/ledger.py, app/services/finance.py</files>
  <read_first>
    - app/services/ledger.py: `record_operation` (lines 36-129), specifically the audit-stamp block building the `Operation(...)` at lines 104-118 (`device_id=settings.device_id`, `seq=next_seq(...)`, `created_at=utcnow_iso()`, `created_by=settings.operator_name`).
    - app/services/finance.py: `record_cash_movement` (lines 46-84), the `CashMovement(...)` build at lines 70-80.
    - app/services/security.py (Plan 03): `author_fields() -> tuple[str | None, str]` (returns `(None, settings.operator_name)` when no user in context, `(user.id, user.display_name)` otherwise).
    - app/models.py (Plan 02): `Operation.author_id`, `CashMovement.author_id` (nullable); `created_by` is String(100) — the `display_name` snapshot from author_fields is capped at 100 by create_user (Plan 03).
    - .planning/phases/25-authentication-roles-user-attribution/25-PATTERNS.md § "record_operation + record_cash_movement (single write path — stamp author_id)" (the minimal two-line change; do NOT duplicate the write path; do NOT touch the ~7 callers).
  </read_first>
  <behavior>
    - With a user set in contextvars, record_operation persists Operation.author_id == that user's id and created_by == that user's display_name.
    - With NO user in contextvars (bare session, fixtures, scripts), record_operation persists author_id == None and created_by == settings.operator_name (existing behavior preserved).
    - Same for record_cash_movement / CashMovement.
    - device_id/seq/created_at stamping is unchanged.
  </behavior>
  <action>
    In `app/services/ledger.py::record_operation`: import `author_fields` from `app.services.security`; immediately before constructing the `Operation(...)`, add `author_id, created_by = author_fields()`; replace `created_by=settings.operator_name` with `created_by=created_by` and add `author_id=author_id` to the insert. Leave `device_id=settings.device_id`, `seq`, `created_at` untouched. Do NOT add an `author`/`current_user` parameter to the function signature and do NOT modify any of the ~7 callers (single-write-path discipline). Apply the byte-identical change to `app/services/finance.py::record_cash_movement`'s `CashMovement(...)` build. Watch for an import cycle: `security.py` must not import `ledger`/`finance` at module top (it imports only `config`/`users`/`auth`/`db`), so `from app.services.security import author_fields` in ledger/finance is safe; if any cycle appears, import `author_fields` lazily inside the function.
  </action>
  <verify>
    <automated>uv run pytest tests/test_ledger.py tests/test_finance.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `record_operation` and `record_cash_movement` call `author_fields()` and set `author_id` + `created_by` from it (grep confirms `author_fields()` in both files).
    - No new parameter is added to either function signature; no caller is modified.
    - With no user in context, `tests/test_ledger.py` + `tests/test_finance.py` stay green: `author_id` is None and `created_by == settings.operator_name` on newly recorded rows.
    - `uv run pytest tests/test_ledger.py tests/test_finance.py` exits 0.
  </acceptance_criteria>
  <done>Both single write paths stamp author_id/created_by from the request-scoped user with a settings fallback; existing ledger/finance tests stay green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Contextvars threadpool-propagation proof (both write paths)</name>
  <files>tests/test_attribution.py</files>
  <read_first>
    - .planning/phases/25-authentication-roles-user-attribution/25-RESEARCH.md Pitfall 4 / Assumption A1 (the async guard sets a ContextVar; the endpoints are sync `def` run in a threadpool; AnyIO's run_in_threadpool copies the context, but this MUST be proven — if it fails, fall back to explicit-parameter threading) and § "Validation Architecture" (USER-05 test row).
    - tests/conftest.py (Plan 04): `anon_client` (real guard active), `login(...)` helper, seeded users; the `stocked_product`/`product`/`batch`/`customer` fixtures for building a real sale/receipt.
    - app/routes/receipts.py + app/routes/sales.py + app/routes/finance.py (the real POST endpoints that call the single write paths) — pick the simplest that reaches `record_operation` / `record_cash_movement` end to end.
    - .planning/phases/25-authentication-roles-user-attribution/25-VALIDATION.md (`-k attribution` selector; `-k filter_by_user` is added later in Plan 08 to the same file).
  </read_first>
  <behavior>
    - Log in as a known user through the REAL guard, then POST a receipt/sale that reaches record_operation; the persisted Operation.author_id equals that user's id (contextvars survived the def-endpoint threadpool hop).
    - Log in and POST a manual cash movement that reaches record_cash_movement; the persisted CashMovement.author_id equals that user's id.
    - A different logged-in user produces a different author_id on their rows (attribution is per-request, not global).
  </behavior>
  <action>
    Create `tests/test_attribution.py` using `anon_client` (real guard, no override) + the `login(...)` helper. Seed a user, log in, POST a real receipt or sale through its actual endpoint (with the CSRF token), then query the persisted `Operation` and assert `author_id == user.id` AND `created_by == user.display_name` — proving contextvars propagate through the sync-`def` threadpool (Pitfall 4). Add the equivalent for `record_cash_movement` via the manual cash-movement endpoint. Add a second user and assert their ops carry THEIR id (per-request isolation). Name the tests so `-k attribution` selects them. If this test reveals propagation does NOT hold, STOP and switch to the documented fallback (thread `current_user` explicitly from the route into the service call) — but implement contextvars first and only fall back on a proven failure.
  </action>
  <verify>
    <automated>uv run pytest tests/test_attribution.py -k attribution -x -q</automated>
  </verify>
  <acceptance_criteria>
    - A receipt/sale POSTed by a logged-in user persists `Operation.author_id == user.id` and `created_by == user.display_name`.
    - A manual cash movement POSTed by a logged-in user persists `CashMovement.author_id == user.id`.
    - Two different logged-in users produce rows with distinct author_ids (per-request, not global-singleton).
    - `uv run pytest tests/test_attribution.py -k attribution` exits 0 (contextvars propagation PROVEN; no fallback needed — or the fallback is in place and the same assertions pass).
  </acceptance_criteria>
  <done>Real end-to-end attribution is proven at both write paths through the authenticated client; the riskiest mechanism (A1/Pitfall 4) is verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| request scope → ledger insert | The author identity must come from the server-set session, never client input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-07-01 | Repudiation | operation/cash authorship | mitigate | `author_id` derived from `author_fields()` (server-set contextvars from the session), never from a form field — proven by the attribution test (USER-05). |
| T-25-07-02 | Tampering | wrong-author silent fallback | mitigate | The proving test asserts the REAL author_id (not the fallback) is stamped through the threadpool; a propagation failure is caught by the test, not shipped (Pitfall 4). |
| T-25-07-03 | Integrity | append-only immutability | mitigate | Attribution is set at INSERT only inside the single write path; the no_update trigger forbids any later rewrite (author_id is not backfilled on historical rows). |
</threat_model>

<verification>
- `uv run pytest tests/test_attribution.py tests/test_ledger.py tests/test_finance.py -x` green.
- `uv run pytest` full suite green — the fallback in `author_fields` keeps every non-authenticated call site (fixtures, scripts, the ~45 legacy tests) stamping `settings.operator_name` with `author_id=None`.
</verification>

<success_criteria>
- Both single write paths stamp the logged-in user's author_id + created_by (USER-05).
- Contextvars→threadpool propagation proven end-to-end (A1/Pitfall 4 closed).
- Existing attribution behavior preserved when no user is in context.
</success_criteria>

<output>
Create `.planning/phases/25-authentication-roles-user-attribution/25-07-SUMMARY.md` when done.
</output>

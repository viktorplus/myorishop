---
phase: 25-authentication-roles-user-attribution
plan: 03
type: execute
wave: 2
depends_on: ["25-02"]
files_modified:
  - app/services/auth.py
  - app/services/users.py
  - app/services/security.py
  - tests/test_auth.py
  - tests/test_users.py
autonomous: true
requirements: [AUTH-02, AUTH-05, USER-01, USER-02, USER-03, USER-04, ROLE-01, ROLE-04]
user_setup: []
must_haves:
  truths:
    - "A password can be hashed with Argon2id and later verified; a wrong password is rejected"
    - "An administrator can create a user with display name/login/role, deactivate/reactivate them, and reset their password — all validated against the ROLES allow-list"
    - "The current logged-in user is threaded via a contextvars ContextVar and read by author_fields(), falling back to settings.operator_name when unset"
    - "require_role enforces role server-side with administrator satisfying every operator check"
  artifacts:
    - path: "app/services/auth.py"
      provides: "Argon2id hash/verify/rehash + hmac token compare"
      exports: ["hash_password", "verify_password"]
    - path: "app/services/users.py"
      provides: "user CRUD + count_users/get_active_user + ROLES validation"
      exports: ["create_user", "deactivate_user", "reset_password", "list_users", "count_users", "get_active_user"]
    - path: "app/services/security.py"
      provides: "auth_guard, require_role, current_user, contextvars, CSRF helpers"
      exports: ["auth_guard", "require_role", "current_user", "author_fields", "NotAuthenticated"]
  key_links:
    - from: "app/services/security.py::author_fields"
      to: "contextvars _current_user"
      via: "ContextVar.get() with settings.operator_name fallback"
      pattern: "author_fields"
    - from: "app/services/users.py::create_user"
      to: "app/services/auth.py::hash_password"
      via: "password hashed before store"
      pattern: "hash_password"
---

<objective>
Build the pure-Python security core — Argon2id password hashing, the admin user service (create/deactivate/reactivate/reset), and the `security.py` guard module (contextvars current-user, `author_fields`, `auth_guard`, `require_role`, `NotAuthenticated`, CSRF issue/verify) — all testable with the plain `session` fixture before any app wiring exists.

Purpose: AUTH-02 (Argon2id hashes only), USER-01/02/03/04 (user profile + admin CRUD), ROLE-01 (two-role allow-list), ROLE-04 (admin satisfies every operator check), and the CSRF helper for AUTH-05. This isolates the greenfield security logic (no analog in the codebase) into services the wiring plan (Plan 04) then registers.
Output: `app/services/auth.py`, `app/services/users.py`, `app/services/security.py`, plus unit/service tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-authentication-roles-user-attribution/25-RESEARCH.md
@.planning/phases/25-authentication-roles-user-attribution/25-PATTERNS.md
@.planning/phases/25-authentication-roles-user-attribution/25-UI-SPEC.md
@app/services/warehouses.py
@app/services/finance.py
@app/config.py
@app/db.py
</context>

<artifacts>
This plan produces:
- `app/services/auth.py`: `hash_password(raw) -> str` (Argon2id PHC), `verify_password(session, user, raw) -> bool` (constant-time verify + `check_needs_rehash` rehash-on-login), and a `compare_token(a, b) -> bool` wrapper over `hmac.compare_digest`. Module-level `_ph = PasswordHasher()`.
- `app/services/users.py`: `count_users(session)`, `get_active_user(session, user_id)`, `list_users(session)`, `create_user(...) -> (User|None, errors)`, `deactivate_user(session, user_id, *, actor)`, `reactivate_user(session, user_id)`, `reset_password(session, user_id, raw) -> (User|None, errors)`; module-level RU error constants; validates `role` against `ROLES`, caps `display_name` to the created_by snapshot width.
- `app/services/security.py`: `_current_user` ContextVar + `current_user()` FastAPI dependency + `author_fields() -> (author_id|None, created_by)`; `NotAuthenticated(Exception)` with `.redirect`; `auth_guard(request, session)` dependency; `require_role(role) -> dependency`; CSRF helpers `issue_csrf(request)`, `require_csrf(request)`, `session_csrf(request)`; `PUBLIC_PATHS` set.
- New tests: `tests/test_auth.py` (password_hash, roles_allowlist helpers, csrf compare, author_fields fallback), `tests/test_users.py` (user_admin CRUD, deactivate, reset at the service level).
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Argon2id auth service (hash/verify/rehash) + token compare</name>
  <files>app/services/auth.py, tests/test_auth.py</files>
  <read_first>
    - .planning/phases/25-authentication-roles-user-attribution/25-RESEARCH.md § "Pattern 2" (PasswordHasher hash/verify/check_needs_rehash; VerifyMismatchError/InvalidHashError; store full PHC in String(255)) and § "Don't Hand-Roll" (hmac.compare_digest for tokens).
    - app/services/finance.py (lines 24-27): module-level RU error constant style + validation-in-service discipline (the fat-service convention to mirror).
    - app/models.py: `User` (from Plan 02) — `password_hash` String(255).
  </read_first>
  <behavior>
    - hash_password("secret") returns a PHC-encoded Argon2id string starting with "$argon2id$"; hashing the same input twice gives different strings (random salt).
    - verify_password(session, user, correct) is True; verify_password(session, user, wrong) is False (never raises).
    - verify_password with a stored hash from weaker params triggers check_needs_rehash → rehashes + commits; a corrupt/empty stored hash returns False (InvalidHashError caught).
    - compare_token equal strings → True; differing → False (wraps hmac.compare_digest, constant-time).
  </behavior>
  <action>
    Create `app/services/auth.py` with a module-level `_ph = PasswordHasher()` (library defaults are Argon2id, OWASP-reasonable — RESEARCH A5). `hash_password(raw: str) -> str` returns `_ph.hash(raw)`. `verify_password(session, user, raw) -> bool`: try `_ph.verify(user.password_hash, raw)`; on `VerifyMismatchError`/`InvalidHashError` return False; on success, if `_ph.check_needs_rehash(user.password_hash)` re-hash and `session.commit()`, then return True. `compare_token(a: str, b: str) -> bool` wraps `hmac.compare_digest(a, b)` (used by CSRF in Task 3). All security logic lives here, never in a route (fat-service V5/V6). Create `tests/test_auth.py` with the password/token unit tests from the behavior block (name them so `-k password_hash` and `-k csrf` select them per 25-VALIDATION).
  </action>
  <verify>
    <automated>uv run pytest tests/test_auth.py -k "password_hash or token" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `hash_password` output begins with `$argon2id$`; two hashes of the same input differ.
    - `verify_password` returns True for the correct password, False for a wrong one, and False (no exception) for a malformed stored hash.
    - `compare_token("x","x")` is True; `compare_token("x","y")` is False.
    - Tests are selectable by `-k password_hash`; `uv run pytest tests/test_auth.py -k "password_hash or token"` exits 0.
  </acceptance_criteria>
  <done>Argon2id hashing/verification/rehash + timing-safe token compare implemented and unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Admin user service (create/deactivate/reactivate/reset) + lookups</name>
  <files>app/services/users.py, tests/test_users.py</files>
  <read_first>
    - app/services/warehouses.py (lines 109-147): the validate→gate→single-commit `(obj|None, errors)` return shape, RU HTML-free error constants at module level (`add_warehouse`/`update_warehouse`), and the `list_*` `select()` read shape.
    - app/models.py: `User`, `ROLES` (from Plan 02); `Operation.created_by` String(100) — the snapshot width `display_name` must fit.
    - app/services/auth.py (Task 1): `hash_password`.
    - .planning/phases/25-authentication-roles-user-attribution/25-UI-SPEC.md § "Copywriting Contract" (lines 138-143): exact RU error strings («Введите логин.», «Укажите отображаемое имя.», «Такой логин уже занят.», «Выберите роль из списка.», «Задайте пароль.»).
  </read_first>
  <action>
    Create `app/services/users.py` mirroring the warehouses service. Module-level RU error constants matching the UI-SPEC copy. Functions:
    - `count_users(session) -> int` (scalar count over User).
    - `get_active_user(session, user_id) -> User | None` (returns the user only if it exists AND `is_active == 1`; used by the guard to reject deactivated users every request — USER-03).
    - `list_users(session) -> list[User]` (ordered by display_name; used by the user-mgmt page + history/report filter).
    - `create_user(session, *, display_name, login, role, password) -> (User|None, errors)`: strip/validate — display_name required (and ≤ 100 chars so the `created_by` snapshot fits), login required + not already taken (query existing → `errors["login"]`), role must be in `ROLES` else `errors["role"]`, password required else `errors["password"]`. On success hash the password via `hash_password`, insert with `is_active=1`, single commit.
    - `deactivate_user(session, user_id, *, actor_id) -> (bool, errors)`: set `is_active=0`; refuse to deactivate `actor_id` (an admin cannot disable themselves) with a distinct error; unknown id → error. Past operations keep their `author_id` (never touched).
    - `reactivate_user(session, user_id) -> bool`: set `is_active=1`; idempotent no-op on unknown/already-active.
    - `reset_password(session, user_id, new_password) -> (User|None, errors)`: validate password non-empty, re-hash via `hash_password`, write `password_hash`, commit. Never echo the password back.
    Create `tests/test_users.py` service tests (use the `session` fixture, no HTTP): create+assign role (`-k user_admin`), duplicate-login rejected, unknown/empty role rejected (`-k roles_allowlist`), deactivate then get_active_user returns None while the row + its past author_id persist (`-k deactivate`), reset_password changes the hash and verify_password accepts the new password.
  </action>
  <verify>
    <automated>uv run pytest tests/test_users.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `create_user` with a valid admin/operator role returns a persisted `User` with an Argon2id `password_hash`; a role not in `ROLES` returns `(None, {"role": ...})`; a duplicate login returns `(None, {"login": ...})`; a blank password returns `(None, {"password": ...})`.
    - `deactivate_user` flips `is_active` to 0; `get_active_user` then returns None, but the User row still exists and any Operation.author_id pointing at it is unchanged (USER-03).
    - An admin cannot deactivate their own id (distinct error, ZERO writes).
    - `reset_password` updates `password_hash` so `verify_password` accepts the new password and rejects the old one.
    - Tests selectable by `-k user_admin`, `-k roles_allowlist`, `-k deactivate`; `uv run pytest tests/test_users.py` exits 0.
  </acceptance_criteria>
  <done>Admin user service implements USER-01/02/03/04 + ROLE-01 at the service tier with the established (obj, errors) contract.</done>
</task>

<task type="auto">
  <name>Task 3: security.py guard module — contextvars, author_fields, auth_guard, require_role, CSRF</name>
  <files>app/services/security.py, tests/test_auth.py</files>
  <read_first>
    - .planning/phases/25-authentication-roles-user-attribution/25-RESEARCH.md § "Pattern 3" (app-level guard steps + `PUBLIC_PATHS`; issue CSRF before the public early-return), § "Pattern 6" (context values), § "Pattern 7" (contextvars `_current_user` + `author_fields` with `settings.operator_name` fallback), and Pitfall 3 (HTMX 4xx / `HX-Redirect` — the exception handler in Plan 04 consumes `NotAuthenticated.redirect`).
    - app/db.py: `get_session` (lines 70-73) — the `Depends(get_session)` DI idiom the guard reuses.
    - app/services/users.py (Task 2): `count_users`, `get_active_user`.
    - app/services/auth.py (Task 1): `compare_token`.
    - app/config.py: `settings.operator_name` (the attribution fallback).
    - .planning/phases/25-authentication-roles-user-attribution/25-PATTERNS.md § "app/services/security.py — NO ANALOG" (build from Patterns 3/6/7; the contextvars→threadpool proof is deferred to Plan 07).
  </read_first>
  <action>
    Create `app/services/security.py` (greenfield — no analog). Define:
    - `_current_user: contextvars.ContextVar = contextvars.ContextVar("current_user", default=None)` and `author_fields() -> tuple[str | None, str]`: read `_current_user.get()`; if None return `(None, settings.operator_name)` (keeps the ~45 existing tests + fixtures green), else `(u.id, u.display_name)`.
    - `current_user(request) -> User | None`: a FastAPI dependency returning `getattr(request.state, "user", None)` — overridable in tests (RESEARCH test pattern).
    - `class NotAuthenticated(Exception)` carrying `redirect: str` (e.g. `/login` or `/setup`); the exception handler is registered in Plan 04.
    - `PUBLIC_PATHS = {"/login", "/logout", "/setup"}`.
    - CSRF helpers: `issue_csrf(request)` — if `request.session` lacks `"csrf"`, generate a random token (`secrets.token_urlsafe`) and store it; `session_csrf(request)` — return the stored token; `require_csrf(request)` — read `X-CSRF-Token` header OR `csrf_token` form field, compare to the session token with `compare_token`, raise `HTTPException(403)` on mismatch/absence.
    - `auth_guard(request, session=Depends(get_session))`: (1) `issue_csrf(request)` FIRST so every request (incl. public) has a token available to render; (2) if `request.url.path in PUBLIC_PATHS` return; (3) if `count_users(session) == 0` raise `NotAuthenticated(redirect="/setup")` (AUTH-04 first-run); (4) `user_id = request.session.get("user_id")`; `user = get_active_user(session, user_id) if user_id else None`; if `user is None` clear any session user + raise `NotAuthenticated(redirect="/login")` (AUTH-01/USER-03); (5) if `request.method not in ("GET","HEAD","OPTIONS")` call `require_csrf(request)` (AUTH-05); (6) `_current_user.set(user)` and `request.state.user = user`.
    - `require_role(role: str)`: returns a dependency that reads `request.state.user`; administrators pass EVERY role check (ROLE-04 hierarchy: admin satisfies operator); a user whose role != required (and not administrator) → `HTTPException(403, detail="Доступ только для администратора.")`. A missing user → treat as 403 (defence-in-depth; the app-level guard already blocks anonymous).
    Add unit tests to `tests/test_auth.py`: `author_fields()` returns the fallback when the contextvar is unset and the user's `(id, display_name)` when set (`-k attribution` or `-k author_fields`); `require_csrf` raises 403 on missing/wrong token and passes on match (`-k csrf`, exercising a fake request-with-session); `require_role` logic — an operator-role user is rejected for an admin dependency, an administrator passes both (pure-function level, without HTTP).
  </action>
  <verify>
    <automated>uv run pytest tests/test_auth.py -k "csrf or author_fields or role" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `author_fields()` returns `(None, settings.operator_name)` when `_current_user` is unset and `(user.id, user.display_name)` after `_current_user.set(user)`.
    - `require_csrf` raises HTTP 403 when the token is missing or mismatched and returns normally when the header/form token matches the session token (constant-time compare).
    - `require_role("administrator")` rejects an operator-role user (403) and admits an administrator; an administrator also satisfies an operator-level requirement (ROLE-04).
    - `auth_guard` issues a CSRF token BEFORE the public-path early return (so `/login` renders a usable token).
    - `uv run pytest tests/test_auth.py -k "csrf or author_fields or role"` exits 0.
  </acceptance_criteria>
  <done>security.py provides the guard, role gate, contextvars attribution helper, and CSRF primitives; each is unit-tested without app wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → service | login/password + CSRF token + role claims arrive untrusted |
| request scope → write path | current-user identity flows via contextvars into attribution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03-01 | Information Disclosure | password storage | mitigate | Argon2id via argon2-cffi; only the PHC hash is stored; `verify_password` is constant-time and never logs the raw (AUTH-02). |
| T-25-03-02 | Tampering | CSRF on state-changing POST | mitigate | Synchronizer token in the signed session; `require_csrf` compares via `hmac.compare_digest`; 403 on mismatch (AUTH-05). |
| T-25-03-03 | Elevation of Privilege | role check | mitigate | `require_role` is a server-side dependency; admin-satisfies-operator hierarchy explicit; unknown role rejected by `ROLES` allow-list (ROLE-01/04). |
| T-25-03-04 | Elevation of Privilege | deactivated user | mitigate | `get_active_user` returns None for `is_active=0`; the guard reloads + rechecks every request (USER-03). |
| T-25-03-05 | Repudiation | attribution source | mitigate | `author_fields` derives identity from `_current_user` (server-set), never from client input (USER-05 repudiation control). |
</threat_model>

<verification>
- `uv run pytest tests/test_auth.py tests/test_users.py -x` green.
- `uv run pytest` full suite still green — these are new modules + new tests; nothing is wired into the app yet, so no existing test changes behavior. `author_fields` fallback keeps `record_operation`/`record_cash_movement` attribution unchanged until Plan 07 wires it.
</verification>

<success_criteria>
- Argon2id hash/verify/rehash implemented (AUTH-02).
- Admin user CRUD + deactivate + reset validated against ROLES (USER-01/02/03/04, ROLE-01).
- Guard, require_role (admin⊇operator), contextvars author_fields, and CSRF helpers implemented and unit-tested (ROLE-04, AUTH-05).
</success_criteria>

<output>
Create `.planning/phases/25-authentication-roles-user-attribution/25-03-SUMMARY.md` when done.
</output>

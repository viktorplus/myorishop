---
phase: 25-authentication-roles-user-attribution
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models.py
  - alembic/versions/0017_users_and_author_id.py
  - tests/test_pragmas.py
autonomous: true
requirements: [USER-01, ROLE-01, USER-05]
user_setup: []
must_haves:
  truths:
    - "A users table exists with login (unique), display_name, role, password_hash, is_active, timestamps"
    - "operations, cash_movements, and sales each have a nullable author_id column"
    - "The operations/cash_movements append-only UPDATE+DELETE triggers still exist after migration 0017 applies"
    - "Exactly two roles are defined in the ROLES allow-list: administrator, operator"
  artifacts:
    - path: "app/models.py"
      provides: "User model + ROLES allow-list + author_id columns"
      contains: "class User"
    - path: "alembic/versions/0017_users_and_author_id.py"
      provides: "users table + native author_id add_column migration"
      contains: "op.add_column"
    - path: "tests/test_pragmas.py"
      provides: "trigger-survival regression after 0017"
      contains: "operations_no_update"
  key_links:
    - from: "alembic/versions/0017_users_and_author_id.py"
      to: "operations / cash_movements / sales"
      via: "native op.add_column (NEVER batch_alter_table)"
      pattern: "op.add_column"
---

<objective>
Add the persistent schema for identity and attribution: a `users` table, an `author_id` column on the three write-path tables, a `ROLES` allow-list, and migration 0017 — added with NATIVE `op.add_column` so the append-only ledger triggers survive.

Purpose: USER-01 (user profile: display name, login, role), ROLE-01 (exactly two roles), and the storage half of USER-05 (`author_id` on `operations`/`cash_movements`, plus `sales` for header attribution). This is the greenfield data foundation for the guard, the user service, and attribution.
Output: `User` model, `ROLES` dict, `author_id` columns, migration `0017`, and a trigger-survival regression test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-authentication-roles-user-attribution/25-RESEARCH.md
@.planning/phases/25-authentication-roles-user-attribution/25-PATTERNS.md
@app/models.py
@alembic/versions/0008_batches.py
@app/db.py
@tests/test_pragmas.py
</context>

<artifacts>
This plan produces:
- New model `User` (`users` table): `id` String(36) PK (`default=new_id`), `login` String(50) unique, `display_name` String(200), `role` String(20), `password_hash` String(255), `is_active` Integer default 1, `created_at`/`updated_at` String(32).
- New module-level constant `ROLES = {"administrator": "Администратор", "operator": "Оператор"}` in `app/models.py`.
- New nullable columns: `Operation.author_id`, `CashMovement.author_id`, `Sale.author_id` (ORM `ForeignKey("users.id")`, bare column in the DB).
- Migration revision `0017` (`0017_users_and_author_id.py`, down_revision `0016`): `create_table("users")` + native `op.add_column` for the three `author_id` columns.
- Extended `tests/test_pragmas.py`: trigger-survival regression after 0017.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Add User model, ROLES allow-list, and author_id columns</name>
  <files>app/models.py</files>
  <read_first>
    - app/models.py: the `Operation` class (lines 322-355, esp. `id`/`created_at`/`created_by`/`device_id` column style and the bare-nullable-FK `sale_id`/`batch_id` at 339-350), `CashMovement` (438-469), `Sale` (419-435), `Batch.is_legacy` (262, the `Integer` soft-flag), and `WRITEOFF_REASONS` (50-57) / `CASH_CATEGORIES` (70-82) for the latin-key→RU-label dict shape.
    - app/core.py: `new_id` (line 15), `utcnow_iso` (line 20) — the exact helpers every model uses.
    - .planning/phases/25-authentication-roles-user-attribution/25-PATTERNS.md § "app/models.py" (column convention to mirror; `author_id` mirrors `batch_id`; ROLES mirrors WRITEOFF_REASONS).
  </read_first>
  <action>
    Add `ROLES = {"administrator": "Администратор", "operator": "Оператор"}` as a module-level constant near `WRITEOFF_REASONS`/`CASH_CATEGORIES` (latin key = stored value, RU label = display), with a comment that this is the exact server-side allow-list ROLE-01 validates against — exactly two roles, no dynamic/custom roles.

    Add a `User(Base)` model, `__tablename__ = "users"`, mirroring the codebase UUID-PK/ISO-timestamp convention: `id: Mapped[str]` String(36) `primary_key=True, default=new_id`; `login: Mapped[str]` String(50) `nullable=False, unique=True`; `display_name: Mapped[str]` String(200) `nullable=False`; `role: Mapped[str]` String(20) `nullable=False` (a `ROLES` key); `password_hash: Mapped[str]` String(255) `nullable=False` (full Argon2 PHC string, no separate salt column); `is_active: Mapped[int]` Integer `nullable=False, default=1` (soft-disable, mirrors `Batch.is_legacy`); `created_at`/`updated_at` String(32) with `default=utcnow_iso` / `onupdate=utcnow_iso`. Login is case-sensitive ASCII (RESEARCH A7) — do NOT add a `login_lc` shadow this phase.

    Add a nullable `author_id: Mapped[str | None]` column to `Operation`, `CashMovement`, and `Sale`, mirroring the existing bare-nullable FK `batch_id`/`sale_id`: `mapped_column(ForeignKey("users.id", name="fk_<table>_author_id_users"), index=True)`. The ORM `ForeignKey` gives insert ordering + PostgreSQL portability; the DB column is added bare by the migration (Task 2). Keep the existing `created_by` String(100) snapshot column unchanged (append-only display-name snapshot; never rewritten). Note in a comment: `display_name` (200) is snapshotted into `created_by` (100) at write time, so the user service (Plan 03) caps `display_name` for the snapshot to fit.
  </action>
  <verify>
    <automated>uv run python -c "from app.models import User, ROLES, Operation, CashMovement, Sale; assert set(ROLES)=={'administrator','operator'}; [getattr(m,'author_id') for m in (Operation,CashMovement,Sale)]; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `from app.models import User, ROLES` succeeds; `set(ROLES) == {"administrator", "operator"}` and `ROLES["administrator"] == "Администратор"`.
    - `User.__tablename__ == "users"`; `User` has `login`, `display_name`, `role`, `password_hash`, `is_active`, `created_at`, `updated_at`; `login` is unique.
    - `Operation.author_id`, `CashMovement.author_id`, `Sale.author_id` are all mapped, nullable, and reference `users.id`.
    - `uv run pytest tests/test_core.py tests/test_smoke.py -q` passes (models import cleanly; naming convention satisfied — every constraint is named).
  </acceptance_criteria>
  <done>User model + ROLES + three author_id columns exist and import; existing conventions/smoke tests stay green.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 0017 — users table + native author_id add_column (triggers preserved)</name>
  <files>alembic/versions/0017_users_and_author_id.py</files>
  <read_first>
    - alembic/versions/0008_batches.py (read in full): the `create_table` block with explicit `sa.Column`s + `sa.PrimaryKeyConstraint(..., name=op.f("pk_..."))`, the CRITICAL native `op.add_column("operations", sa.Column("batch_id", sa.String(36), nullable=True))` (NO batch_alter_table — a batch rebuild silently DROPS `operations_no_update`/`operations_no_delete`), the immutability rule (no app-module imports; frozen literals only), the `revision`/`down_revision` block, and the native `downgrade()` (drop index then column).
    - alembic/versions/0016_active_catalog.py (only its `revision`/`down_revision` header to confirm 0016 is the current head → `down_revision = "0016"`).
    - .planning/phases/25-authentication-roles-user-attribution/25-PATTERNS.md § "alembic/versions/0017_users_and_author_id.py" and 25-RESEARCH.md Pitfall 1 + "Migration skeleton".
  </read_first>
  <action>
    Create `alembic/versions/0017_users_and_author_id.py` with `revision = "0017"`, `down_revision = "0016"`, `branch_labels = None`, `depends_on = None`. Import stdlib + `sqlalchemy as sa` + `from alembic import op` ONLY — NEVER import app modules (WR-06 immutability rule).

    `upgrade()`: `op.create_table("users", ...)` with explicit `sa.Column`s matching the User model (id String(36), login String(50), display_name String(200), role String(20), password_hash String(255), is_active Integer, created_at String(32), updated_at String(32)), `sa.PrimaryKeyConstraint("id", name=op.f("pk_users"))`, `sa.UniqueConstraint("login", name=op.f("uq_users_login"))`. Then add the three attribution columns with NATIVE `op.add_column(<table>, sa.Column("author_id", sa.String(36), nullable=True))` for `"operations"`, `"cash_movements"`, and `"sales"` — bare columns, NO inline DB-level FK (the 0004 sale_id / 0008 batch_id precedent), and NEVER `op.batch_alter_table` on `operations`/`cash_movements`. Optionally `op.create_index(op.f("ix_<table>_author_id"), <table>, ["author_id"])` for each (mirrors the batch_id index).

    `downgrade()`: drop each author_id index (if created) then `op.drop_column` for the three tables (native), then `op.drop_table("users")`.

    Do NOT backfill `author_id` on historical rows (RESEARCH Anti-Patterns / Pitfall 2 — the no_update trigger would ABORT it and it would be a lie; pre-auth rows stay NULL).
  </action>
  <verify>
    <automated>uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head</automated>
  </verify>
  <acceptance_criteria>
    - `uv run alembic upgrade head` applies 0017 cleanly against the dev DB; `uv run alembic downgrade -1` then `upgrade head` round-trips without error.
    - The migration file imports NO app modules (grep: no `from app` / `import app` in the file).
    - `author_id` is added via `op.add_column` (grep confirms `op.add_column`); the file contains NO `batch_alter_table`.
    - After `upgrade head`, `PRAGMA table_info(operations)` / `cash_movements` / `sales` each list an `author_id` column, and the `users` table exists with a UNIQUE index on `login`.
  </acceptance_criteria>
  <done>Migration 0017 creates users + adds three bare author_id columns natively and round-trips up/down.</done>
</task>

<task type="auto">
  <name>Task 3: Trigger-survival regression in test_pragmas.py</name>
  <files>tests/test_pragmas.py</files>
  <read_first>
    - tests/test_pragmas.py (the existing pooled-connection PRAGMA test + the `engine` fixture it uses).
    - tests/conftest.py: the `engine` fixture (lines 19-29) — `Base.metadata.create_all(engine)` + applies `APPEND_ONLY_TRIGGERS`; note it now auto-picks up the new `User` model / `author_id` columns.
    - app/db.py: `APPEND_ONLY_TRIGGERS` (the 4 trigger names: `operations_no_update`, `operations_no_delete`, `cash_movements_no_update`, `cash_movements_no_delete`).
    - .planning/phases/25-authentication-roles-user-attribution/25-VALIDATION.md (the pre-flight regression row) and 25-RESEARCH.md Pitfall 1.
  </read_first>
  <action>
    Add a regression test asserting the four append-only triggers are present after the schema (which now includes migration 0017's tables/columns) is built. Query SQLite's `sqlite_master` for `type='trigger'` and assert the set contains `operations_no_update`, `operations_no_delete`, `cash_movements_no_update`, `cash_movements_no_delete`. Use portable ORM/`exec_driver_sql` against the test `engine` fixture (which mirrors the applied migration state). Add a comment tying the test to Pitfall 1 — this is the guard that a future author_id migration (0017) must NEVER use `batch_alter_table`. Additionally assert that an `UPDATE operations SET author_id=... ` (or any UPDATE) still ABORTs with the append-only message, proving the trigger is live after author_id exists.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pragmas.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - A new test asserts all four trigger names exist in `sqlite_master` after the schema builds.
    - A new assertion proves an UPDATE on `operations` still raises the append-only ABORT (trigger is functional, not merely present).
    - `uv run pytest tests/test_pragmas.py -x` exits 0.
  </acceptance_criteria>
  <done>test_pragmas proves the append-only triggers survive the author_id/users schema change and still block UPDATE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration DDL → ledger integrity | A wrong migration style (batch rebuild) silently removes the append-only guarantee |
| stored credentials | `users.password_hash` is the credential store — must be hash-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-02-01 | Tampering | migration 0017 on `operations`/`cash_movements` | mitigate | Native `op.add_column` only; `tests/test_pragmas.py` regression asserts the 4 triggers survive and still ABORT UPDATE (Pitfall 1). |
| T-25-02-02 | Information Disclosure | `users.password_hash` | mitigate | Column is String(255) for a full Argon2id PHC string only — never plaintext/reversible (hashing lands in Plan 03; column shape enforces hash-only storage). |
| T-25-02-03 | Repudiation | `author_id` provenance | mitigate | Nullable FK to `users.id`; historical rows stay NULL (never backfilled/faked); real attribution stamped server-side later (Plan 07). |
| T-25-02-04 | Elevation of Privilege | `role` values | mitigate | `ROLES` allow-list defines exactly two roles; service-layer validation (Plan 03) rejects any other value (ROLE-01). |
</threat_model>

<verification>
- `uv run alembic upgrade head` / `downgrade -1` / `upgrade head` round-trips.
- `uv run pytest tests/test_pragmas.py tests/test_core.py tests/test_smoke.py -q` green.
- `uv run pytest` full suite still green (adding nullable columns + a new table does not break existing fixtures; `create_all` in conftest auto-adopts them).
</verification>

<success_criteria>
- `users` table + `ROLES` (exactly two) exist.
- `author_id` nullable on operations/cash_movements/sales.
- Append-only triggers proven to survive 0017.
</success_criteria>

<output>
Create `.planning/phases/25-authentication-roles-user-attribution/25-02-SUMMARY.md` when done.
</output>

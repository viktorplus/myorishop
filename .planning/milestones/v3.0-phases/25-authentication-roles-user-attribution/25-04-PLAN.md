---
phase: 25-authentication-roles-user-attribution
plan: 04
type: execute
wave: 3
depends_on: ["25-01", "25-03"]
files_modified:
  - app/main.py
  - app/routes/__init__.py
  - app/routes/auth.py
  - tests/conftest.py
  - app/templates/auth_base.html
  - app/templates/pages/login.html
  - app/templates/pages/setup.html
  - tests/test_auth.py
autonomous: true
requirements: [AUTH-01, AUTH-03, AUTH-04, AUTH-05, ROLE-02]
user_setup: []
must_haves:
  truths:
    - "An unauthenticated GET of any protected route (including /export and /backup) redirects to /login (303) or returns 401 + HX-Redirect for HTMX"
    - "On first run with zero users, every route redirects to /setup, which creates the first administrator and self-closes once a user exists"
    - "A user logs in with login+password, stays logged in across a second request via the signed cookie, and logout ends the session"
    - "A state-changing POST to a protected route without a valid CSRF token is rejected with 403"
    - "The pre-existing ~45 test files stay green because the shared client fixture authenticates (guard overridden)"
  artifacts:
    - path: "app/main.py"
      provides: "SessionMiddleware + app-level auth_guard + NotAuthenticated handler"
      contains: "SessionMiddleware"
    - path: "app/routes/auth.py"
      provides: "/login /logout /setup routes"
      exports: ["router"]
    - path: "tests/conftest.py"
      provides: "authenticated client fixture + anon client fixture"
      contains: "dependency_overrides"
  key_links:
    - from: "app/main.py"
      to: "app/services/security.py::auth_guard"
      via: "FastAPI(dependencies=[Depends(auth_guard)])"
      pattern: "Depends\\(auth_guard\\)"
    - from: "app/routes/auth.py::/login"
      to: "request.session['user_id']"
      via: "session set on successful verify_password"
      pattern: "session\\[.user_id.\\]"
---

<objective>
Flip the app to require login: register `SessionMiddleware` + the app-level `auth_guard` + the `NotAuthenticated` exception handler, inject `current_user`/`csrf_token` into every template, add the `/login` `/logout` `/setup` routes and their standalone auth chrome, and update `tests/conftest.py` so the entire existing suite stays green under the new guard.

Purpose: AUTH-01 (login required before any page), AUTH-03 (signed-cookie session survives refresh + logout ends it), AUTH-04 (first-run admin setup, no shipped creds), AUTH-05 (CSRF enforced on protected POSTs), ROLE-02 (a single app-level guard covers every current + future router incl. export/backup). This is the atomic "security boundary on" change — all pieces land together or the suite breaks.
Output: wired middleware/guard/handler, context processor, auth routes + templates, authenticated test fixtures.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-authentication-roles-user-attribution/25-RESEARCH.md
@.planning/phases/25-authentication-roles-user-attribution/25-UI-SPEC.md
@.planning/phases/25-authentication-roles-user-attribution/25-PATTERNS.md
@app/main.py
@app/routes/__init__.py
@app/routes/warehouses.py
@tests/conftest.py
@app/templates/base.html
</context>

<artifacts>
This plan produces:
- `app/main.py`: `app.add_middleware(SessionMiddleware, secret_key=settings.secret_key, same_site="lax", https_only=False)`; `app = FastAPI(..., dependencies=[Depends(auth_guard)])`; `@app.exception_handler(NotAuthenticated)` returning 303 redirect (HTML) or 401 + `HX-Redirect` (HTMX). Static mount stays public.
- `app/routes/__init__.py`: `templates` gains `context_processors=[_auth_context]` exposing `current_user` + `csrf_token`; `ROLES` registered as a Jinja global.
- `app/routes/auth.py`: `router` with `GET/POST /login`, `POST /logout`, `GET/POST /setup`.
- Templates: `auth_base.html` (standalone chrome, CSRF hx-headers, no nav), `pages/login.html`, `pages/setup.html` (both extend auth_base).
- `tests/conftest.py`: the shared `client` fixture now authenticates (overrides `current_user` + seeds an admin, bypassing the guard/CSRF for the legacy suite); a new `anon_client` (real guard, no override) + `login(client, ...)` helper for auth tests.
- `tests/test_auth.py`: integration tests (guard_redirects, gated_export_backup, session_persist_logout, first_run_setup, csrf).
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Wire SessionMiddleware + app-level guard + exception handler + context processor + conftest</name>
  <files>app/main.py, app/routes/__init__.py, tests/conftest.py</files>
  <read_first>
    - app/main.py (read in full): `app = FastAPI(title="MyOriShop", lifespan=lifespan)` at line 56, `app.mount("/static", ...)` at 57 (a MOUNT — app-level `dependencies=` do NOT apply, so /static stays public), and the 33 `app.include_router(...)` calls (58-89).
    - app/routes/__init__.py (read in full): the shared `templates = Jinja2Templates(directory="app/templates")` (line 15) and how globals are registered (lines 22-39).
    - app/services/security.py (Plan 03): `auth_guard`, `NotAuthenticated`, `current_user`, `session_csrf`, `PUBLIC_PATHS`.
    - tests/conftest.py (read in full): the `client` fixture (129-157, `dependency_overrides[get_session]`, `monkeypatch.setattr(settings, "backup_on_startup", False)`, `with TestClient(app)`), the `engine` fixture (19-29), and the `mobile_client_factory` (bare-FastAPI) pattern.
    - .planning/phases/25-authentication-roles-user-attribution/25-RESEARCH.md Patterns 1, 3, 6 + Pitfall 3; § "Code Examples" test override note (`app.dependency_overrides[current_user]`). Verify the Starlette 1.3.1 `SessionMiddleware` + `Jinja2Templates(context_processors=...)` kwargs against the installed version (A6).
  </read_first>
  <action>
    In `app/main.py`: import `SessionMiddleware` from `starlette.middleware.sessions`, `settings` from `app.config`, and `auth_guard`/`NotAuthenticated`/`current_user`/`session_csrf` from `app.services.security`. Add `app.add_middleware(SessionMiddleware, secret_key=settings.secret_key, same_site="lax", https_only=False)`. Change the app construction to `FastAPI(title="MyOriShop", lifespan=lifespan, dependencies=[Depends(auth_guard)])`. Do NOT add per-router guards (the single app-level dependency is the ROLE-02 guarantee; adding it to each of 33 routers is 33 chances to forget one). Register `@app.exception_handler(NotAuthenticated)`: for `request.headers.get("HX-Request")` return `Response(status_code=401, headers={"HX-Redirect": exc.redirect})` (HTMX won't swap 4xx — Pitfall 3); otherwise `RedirectResponse(exc.redirect, status_code=303)`.

    In `app/routes/__init__.py`: add a `_auth_context(request)` function returning `{"current_user": getattr(request.state, "user", None), "csrf_token": session_csrf(request) if "session" in request.scope else ""}` and pass `context_processors=[_auth_context]` to the `Jinja2Templates(...)` constructor. Register `templates.env.globals["ROLES"] = ROLES` (import from app.models) alongside the existing RU-label globals so the role `<select>` and menu-hide can read labels.

    In `tests/conftest.py`: update the `client` fixture so the legacy suite stays green under the guard — seed one administrator User in the test `session`, and add `app.dependency_overrides[auth_guard] = <no-op that sets request.state.user + _current_user to that admin>` (so existing POST tests need no login and no CSRF token), keeping the existing `get_session` override + `backup_on_startup=False`. Add a separate `anon_client` fixture that builds `TestClient(app)` WITHOUT the `auth_guard` override (real guard active) for the auth/roles integration tests, plus a small `login(test_client, login, password)` helper that POSTs `/login`. Clear all `dependency_overrides` in teardown.
  </action>
  <verify>
    <automated>uv run pytest tests/test_smoke.py tests/test_home.py tests/test_receipts.py tests/test_sales.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/main.py` constructs `FastAPI(..., dependencies=[Depends(auth_guard)])` and registers `SessionMiddleware` with `secret_key=settings.secret_key`; the `NotAuthenticated` handler branches on `HX-Request` (401+HX-Redirect vs 303).
    - `/static` is still reachable without auth (mount is unguarded).
    - `templates` renders `current_user`/`csrf_token`/`ROLES` without a route re-passing them.
    - The updated `client` fixture keeps a representative slice of the legacy suite green: `uv run pytest tests/test_smoke.py tests/test_home.py tests/test_receipts.py tests/test_sales.py` exits 0.
  </acceptance_criteria>
  <done>Guard/session/handler/context-processor wired; legacy suite stays green via the authenticated client fixture.</done>
</task>

<task type="auto">
  <name>Task 2: /login, /logout, /setup routes + auth_base/login/setup templates</name>
  <files>app/routes/auth.py, app/templates/auth_base.html, app/templates/pages/login.html, app/templates/pages/setup.html</files>
  <read_first>
    - app/routes/warehouses.py (lines 100-160): thin route → service → 422-re-render / 303-redirect; `Form("")` params; `from app.routes import templates`; the HX dual-response branch.
    - app/services/auth.py (Plan 03): `verify_password`; app/services/users.py: `count_users`, `create_user`, and a login lookup (`select(User).where(User.login==...)`).
    - app/services/security.py (Plan 03): `PUBLIC_PATHS`, `session_csrf`, `require_csrf`.
    - .planning/phases/25-authentication-roles-user-attribution/25-UI-SPEC.md § "Screens & Components" (login/setup rows) + § "Copywriting Contract" (exact RU strings: «Вход», «Логин», «Пароль», «Войти», «Неверный логин или пароль.», «Учётная запись отключена…», «Первый запуск», «Создайте первого администратора…», «Создать администратора») + § "Interaction Contract" (login submit: 204+HX-Redirect on HX success / 422 re-render on bad creds; setup re-checks count_users==0).
    - app/templates/base.html (read in full): the `<head>` incl. the `htmx-config` meta (lines 18-19, 4xx non-swap / 422-swap — load-bearing) and `/static` links to copy verbatim into the standalone auth chrome; OMIT the `<nav>`.
    - .planning/phases/25-authentication-roles-user-attribution/25-PATTERNS.md § "Templates" (auth_base standalone chrome rules; reuse `.stacked-form`/`.field`/`.error-block`).
  </read_first>
  <action>
    Create `app/templates/auth_base.html`: standalone chrome (like mobile_base.html — does NOT inherit base.html). Copy the `<head>` verbatim including the `htmx-config` meta and `/static/style.css` + `/static/htmx.min.js` links; set `<body hx-headers='{"X-CSRF-Token": "{{ csrf_token }}"}'>`; OMIT the `<nav>`; center a `.stacked-form` (reuse `.mobile-shell` 480px centering — no new tokens). Provide a `{% block content %}`.

    Create `app/templates/pages/login.html` (extends auth_base): h1 «Вход», a POST form to `/login` with «Логин» (autofocus) + «Пароль» (`type="password"`) + a hidden `csrf_token` field; render a destructive `.error-block» above the form on bad creds/deactivated with the entered login preserved and the password cleared. Create `pages/setup.html` (extends auth_base): h1 «Первый запуск» + muted intro, POST to `/setup` with «Отображаемое имя» (autofocus) + «Логин» + «Пароль» + hidden csrf. Never `|safe` on login/display_name.

    Create `app/routes/auth.py` with `router = APIRouter()`, `from app.routes import templates`:
    - `GET /login`: render `pages/login.html` (if users==0, redirect to `/setup`).
    - `POST /login` (`Form("")` login/password): look up the user by login; if found + active + `verify_password` → set `request.session["user_id"] = user.id`, return 204 + `HX-Redirect: /` for HX else 303 → `/`. If the account is deactivated → 422 re-render with «Учётная запись отключена…». On bad creds → 422 re-render with «Неверный логин или пароль.», ZERO session written, password cleared. (Login is in PUBLIC_PATHS so the guard does not require CSRF here.)
    - `POST /logout`: `request.session.clear()`; 204 + `HX-Redirect: /login` for HX else 303 → `/login`.
    - `GET /setup`: if `count_users(session) > 0` redirect `/login`; else render `pages/setup.html`.
    - `POST /setup` (`Form`): re-check `count_users(session) == 0` server-side (closes the race); if users already exist → redirect/403 to `/login`; else `create_user(role="administrator", ...)`, on validation error 422 re-render, on success set `request.session["user_id"]` + 204/303 → `/`. No credentials are ever shipped/seeded.
  </action>
  <verify>
    <automated>uv run pytest tests/test_auth.py -k "first_run_setup or session_persist_logout" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `GET /login` on a seeded DB renders «Вход»; `POST /login` with correct creds sets `request.session["user_id"]` and returns 303→`/` (or 204+`HX-Redirect: /` for HX); wrong creds return 422 with «Неверный логин или пароль.» and NO session written.
    - `POST /logout` clears the session and redirects to `/login`.
    - With zero users, `GET /setup` renders «Первый запуск»; `POST /setup` creates the first administrator and logs them in; a second `POST /setup` once a user exists does NOT create another admin (redirects to `/login`).
    - auth_base.html contains the `htmx-config` meta verbatim and the `<body hx-headers>` CSRF line, and has no `<nav>`.
  </acceptance_criteria>
  <done>Login/logout/first-run-setup work end to end with the standalone auth chrome and exact RU copy.</done>
</task>

<task type="auto">
  <name>Task 3: Auth integration tests (guard redirect, gated export/backup, session, first-run, CSRF)</name>
  <files>tests/test_auth.py</files>
  <read_first>
    - tests/conftest.py (Task 1 updates): `anon_client`, the `login(...)` helper, the seeded admin.
    - .planning/phases/25-authentication-roles-user-attribution/25-VALIDATION.md § "Per-Task Verification Map" (the exact `-k` selectors: guard_redirects, gated_export_backup, session_persist_logout, first_run_setup, csrf) and § "Manual-Only Verifications".
    - .planning/phases/25-authentication-roles-user-attribution/25-RESEARCH.md Pitfall 3 (401 + HX-Redirect for HX; 303 for HTML) and Pattern 4 (first-run self-close).
    - app/routes/export.py + app/routes/backup.py (route paths `/export`, `/export/*.csv`, `/backup`) — the endpoints AUTH-01 must gate.
  </read_first>
  <action>
    Add integration tests to `tests/test_auth.py` using `anon_client` (real guard):
    - `guard_redirects`: an unauthenticated GET of a protected route (e.g. `/products`, `/history`) returns 303 with `Location: /login`; the same GET with an `HX-Request` header returns 401 with an `HX-Redirect: /login` header (Pitfall 3). Use `follow_redirects=False`.
    - `gated_export_backup`: unauthenticated GET `/export`, `/export/products.csv`, and GET `/backup` all redirect to `/login` (not 200) — AUTH-01 covers export/backup.
    - `session_persist_logout`: seed a user, `POST /login`, then a SECOND request to a protected route succeeds (session cookie persists via TestClient), then `POST /logout`, then the protected route redirects to `/login` again (AUTH-03).
    - `first_run_setup`: on a zero-user DB, any protected GET redirects to `/setup`; `POST /setup` creates the admin + logs in; a second `POST /setup` does not create a second user (self-closes).
    - `csrf`: after login, a state-changing POST to a protected endpoint WITHOUT `X-CSRF-Token` returns 403; the same POST WITH the session token (scraped from a rendered `hx-headers`/read via the login page) succeeds (AUTH-05). Ensure the CSRF token is issued on a public GET so it is retrievable.
  </action>
  <verify>
    <automated>uv run pytest tests/test_auth.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `-k guard_redirects` proves 303→/login (HTML) and 401+HX-Redirect (HTMX) for a protected route.
    - `-k gated_export_backup` proves `/export` and `/backup` require login.
    - `-k session_persist_logout` proves the signed cookie survives a second request and logout ends the session.
    - `-k first_run_setup` proves the zero-user redirect to /setup, admin creation, and self-close.
    - `-k csrf` proves 403 without a token and success with the valid session token.
    - `uv run pytest tests/test_auth.py` exits 0.
  </acceptance_criteria>
  <done>All AUTH-01/03/04/05 + ROLE-02 behaviors are covered by automated integration tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → app (every route) | Untrusted requests must present a valid session before reaching any protected router |
| unauthenticated first-run | Zero-user state must not expose a default-credential backdoor |
| HTMX partial responses | 4xx auth responses must trigger navigation, not silently vanish |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04-01 | Elevation of Privilege | a new/forgotten router | mitigate | Single app-level `dependencies=[Depends(auth_guard)]` (deny-by-default) covers all 33 current + future routers incl. export/backup (ROLE-02); test `gated_export_backup`. |
| T-25-04-02 | Spoofing | first-run backdoor | mitigate | No creds shipped/seeded; `/setup` POST re-checks `count_users==0` and self-closes (AUTH-04); test `first_run_setup`. |
| T-25-04-03 | Tampering | session cookie | mitigate | Starlette `SessionMiddleware` itsdangerous-signed with persisted `secret_key`; `same_site=lax`; only `user_id`+`csrf` stored (AUTH-03). |
| T-25-04-04 | Tampering | CSRF on protected POST | mitigate | `auth_guard` calls `require_csrf` on unsafe methods; auth_base injects the token via `hx-headers` (AUTH-05); test `csrf`. |
| T-25-04-05 | Information Disclosure | login error oracle | mitigate | Same «Неверный логин или пароль.» for unknown login vs wrong password; constant-time verify; password cleared on re-render. |
| T-25-04-06 | Denial of visibility | HTMX 4xx swallowed | mitigate | 401 + `HX-Redirect` for HX requests so the browser navigates to /login (Pitfall 3); test `guard_redirects`. |
</threat_model>

<verification>
- `uv run pytest tests/test_auth.py -x` green.
- `uv run pytest` FULL suite green — the conftest authenticated `client` fixture keeps all ~45 pre-existing test files passing under the app-level guard. This is the critical wave-merge gate (25-VALIDATION "after every plan wave").
</verification>

<success_criteria>
- Login required on every route incl. export/backup (AUTH-01, ROLE-02).
- Signed-cookie session survives refresh + logout ends it (AUTH-03).
- First-run guided admin setup, no shipped creds (AUTH-04).
- CSRF enforced on protected POSTs (AUTH-05).
- Full legacy suite green.
</success_criteria>

<output>
Create `.planning/phases/25-authentication-roles-user-attribution/25-04-SUMMARY.md` when done.
</output>

---
phase: 25-authentication-roles-user-attribution
plan: 06
type: execute
wave: 4
depends_on: ["25-04"]
files_modified:
  - app/templates/base.html
  - app/templates/mobile_base.html
  - tests/test_nav.py
autonomous: true
requirements: [AUTH-05, ROLE-03]
must_haves:
  truths:
    - "Every desktop and mobile page carries the CSRF token via <body hx-headers> so every hx-post is protected"
    - "A logout control showing the current user's name is present in the nav and posts to /logout"
    - "The admin-only «Настройки» menu item is hidden for an operator and shown for an administrator (cosmetic layer over the server-side guard)"
  artifacts:
    - path: "app/templates/base.html"
      provides: "CSRF hx-headers + logout control + role-gated «Настройки»"
      contains: "hx-headers"
    - path: "app/templates/mobile_base.html"
      provides: "CSRF hx-headers + logout control (mobile)"
      contains: "hx-headers"
    - path: "tests/test_nav.py"
      provides: "role-conditioned nav + csrf-header render tests"
      contains: "Настройки"
  key_links:
    - from: "app/templates/base.html"
      to: "request.session csrf token"
      via: "<body hx-headers='{\"X-CSRF-Token\": \"{{ csrf_token }}\"}'>"
      pattern: "X-CSRF-Token"
    - from: "app/templates/base.html"
      to: "/logout"
      via: "hx-post logout control showing current_user.display_name"
      pattern: "/logout"
---

<objective>
Add the authenticated chrome to the two shared layouts: the CSRF `hx-headers` line on `<body>`, a logout control showing the current user, and a role-conditioned hide of the admin-only «Настройки» nav item — on both `base.html` (desktop) and `mobile_base.html` (mobile).

Purpose: AUTH-05 (one `hx-headers` line protects every `hx-post` on the page — login, logout, create-user, deactivate, reset, and every existing state-changing form), and ROLE-03's cosmetic layer (menu-hide) paired with the server-side guard already shipped in Plan 05. Menu-hide is NEVER the boundary — the `require_role` dependency is.
Output: CSRF-carrying, logout-bearing, role-aware desktop + mobile chrome + a nav render test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-authentication-roles-user-attribution/25-UI-SPEC.md
@.planning/phases/25-authentication-roles-user-attribution/25-PATTERNS.md
@app/templates/base.html
@app/templates/mobile_base.html
</context>

<artifacts>
This plan produces:
- `app/templates/base.html`: `<body hx-headers='{"X-CSRF-Token": "{{ csrf_token }}"}'>`; a right-aligned logout control «{display_name} · Выйти» (`hx-post="/logout"`, link-styled, `margin-left:auto`); the existing «Настройки» link wrapped in `{% if current_user and current_user.role == "administrator" %}`.
- `app/templates/mobile_base.html`: the same `hx-headers` CSRF line + a logout affordance (44px touch target) in the tabbar/back row.
- `tests/test_nav.py`: asserts the CSRF header renders and «Настройки» is hidden for operator / shown for admin.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: base.html — CSRF hx-headers, logout control, role-gated «Настройки»</name>
  <files>app/templates/base.html</files>
  <read_first>
    - app/templates/base.html (read in full): the `<body>` tag (line 32) and the `<nav>` block (lines 34-43) — the «Настройки» link is line 42; nav is a flex row with `gap:16px`.
    - .planning/phases/25-authentication-roles-user-attribution/25-UI-SPEC.md § "A-UI-4" + "Interaction Contract" (logout is a POST, CSRF-carried, 204+HX-Redirect for HX; right-aligned «{display_name} · Выйти»; `margin-left:auto`; accent text, not filled) + § "Copywriting Contract" (logout copy) + § "New CSS additions" (reuse existing `nav`/`a` rules; only `margin-left:auto` on the logout control — no new color/token) + § "Screens & Components" nav row (operators see Главная·Товары·Продажи·Покупатели·История·Отчёты·Финансы; admins additionally «Настройки»).
    - .planning/phases/25-authentication-roles-user-attribution/25-PATTERNS.md § "base.html + mobile_base.html edits".
  </read_first>
  <action>
    Edit `app/templates/base.html`: set `<body hx-headers='{"X-CSRF-Token": "{{ csrf_token }}"}'>` (one line covers every `hx-post`/`hx-put`/`hx-delete` on the page). Wrap the existing «Настройки» nav link in `{% if current_user and current_user.role == "administrator" %}...{% endif %}` (hide for operators). Add a right-aligned logout control at the end of the nav: a link-styled `hx-post="/logout"` element rendering «{{ current_user.display_name }} · Выйти» (autoescape only — never `|safe`), pushed right with `margin-left:auto` on that control (no new CSS color/token; reuse the existing `a` accent-text rule; the control carries the CSRF token via the body `hx-headers`, so no extra hidden field needed for the hx-post). Guard the whole logout control in `{% if current_user %}` so the login/setup pages (which use auth_base, not base) and any anonymous render never show it. Do not otherwise reorder the operator nav links.
  </action>
  <verify>
    <automated>uv run pytest tests/test_nav.py -k "desktop" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `base.html` `<body>` renders `hx-headers` with `X-CSRF-Token` bound to `{{ csrf_token }}`.
    - The «Настройки» link is inside a `current_user.role == "administrator"` conditional.
    - A logout control with `hx-post="/logout"` renders the current user's display name + «Выйти», right-aligned.
    - `uv run pytest tests/test_nav.py -k desktop` exits 0.
  </acceptance_criteria>
  <done>Desktop chrome carries CSRF, shows logout, and hides «Настройки» from operators.</done>
</task>

<task type="auto">
  <name>Task 2: mobile_base.html — CSRF hx-headers + logout affordance</name>
  <files>app/templates/mobile_base.html</files>
  <read_first>
    - app/templates/mobile_base.html (read in full): the standalone `<head>` (it does NOT inherit base.html — the htmx-config/script tags are duplicated), the `<body>` (line 30), the `{% block tabbar %}` mobile tabbar (lines 31-40), and the `{% block back %}` row (line 43).
    - .planning/phases/25-authentication-roles-user-attribution/25-UI-SPEC.md § "Screens & Components" (Навигация row: mobile logout placed in the `{% block back %}` row or as the last tabbar affordance — a small POST-backed link, 44px touch height) + "Spacing Scale" (44px min touch target on `/m/*`).
  </read_first>
  <action>
    Edit `app/templates/mobile_base.html`: set `<body hx-headers='{"X-CSRF-Token": "{{ csrf_token }}"}'>` (duplicated here because mobile_base is standalone — same reasoning as its duplicated htmx-config). Add a logout affordance in the `{% block back %}` row (or as the last tabbar item), guarded by `{% if current_user %}`, `hx-post="/logout"`, rendering «{{ current_user.display_name }} · Выйти», with a 44px min touch height (reuse the existing `.mobile-back`/tabbar touch-target rules — no new CSS). Mobile has no «Настройки» tab today, so no admin menu-hide is needed here; if any admin-only mobile affordance is added later it must be wrapped in the same role conditional. Autoescape only.
  </action>
  <verify>
    <automated>uv run pytest tests/test_nav.py -k "mobile" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `mobile_base.html` `<body>` renders the `X-CSRF-Token` `hx-headers` line.
    - A `hx-post="/logout"` affordance showing the user + «Выйти» renders inside `{% if current_user %}` with a 44px touch target.
    - `uv run pytest tests/test_nav.py -k mobile` exits 0.
  </acceptance_criteria>
  <done>Mobile chrome carries CSRF and offers logout at a 44px touch target.</done>
</task>

<task type="auto">
  <name>Task 3: Nav render tests (CSRF header present, «Настройки» role-conditioned)</name>
  <files>tests/test_nav.py</files>
  <read_first>
    - tests/conftest.py (Plan 04): `anon_client`, the `login(...)` helper, seeded admin; add an operator login for the hide assertion.
    - app/templates/base.html + mobile_base.html (Tasks 1-2).
    - .planning/phases/25-authentication-roles-user-attribution/25-VALIDATION.md § "Manual-Only Verifications" (menu-hide is cosmetic; this automated test complements the manual visual check).
  </read_first>
  <action>
    Create `tests/test_nav.py` using `anon_client` + logins:
    - `desktop`: an admin GET `/` (or any base.html page) contains `X-CSRF-Token` in the rendered body and the «Настройки» link; an operator GET of the same page contains the `X-CSRF-Token` header line but does NOT contain the «Настройки» link (menu-hide). Both render a «Выйти» control with the logged-in display name.
    - `mobile`: an admin GET `/m/` contains the `X-CSRF-Token` `hx-headers` line and a «Выйти» affordance.
    Keep these as render/string assertions (menu-hide is cosmetic — the authoritative 403 block is already proven in Plan 05's test_roles.py).
  </action>
  <verify>
    <automated>uv run pytest tests/test_nav.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The rendered desktop page contains `X-CSRF-Token` for both roles.
    - «Настройки» appears for the admin render and is absent for the operator render.
    - A «Выйти» control renders for a logged-in user on both desktop and mobile.
    - `uv run pytest tests/test_nav.py` exits 0.
  </acceptance_criteria>
  <done>Automated tests confirm the CSRF header renders everywhere and the admin menu item is role-conditioned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered page → server POST | Every state-changing hx-post must carry the CSRF token |
| nav visibility vs access control | Hidden menu items must never be mistaken for the security boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-06-01 | Tampering | CSRF coverage on existing forms | mitigate | One `<body hx-headers>` line on base + mobile_base covers every hx-post site (login/logout/create-user/deactivate/reset + all existing forms) (AUTH-05). |
| T-25-06-02 | Elevation of Privilege | menu-hide mistaken for control | accept-with-note | Menu-hide is explicitly cosmetic; the authoritative boundary is `require_role` (Plan 05, tested in test_roles.py). Documented so no reviewer treats the hide as the control (ROLE-03). |
| T-25-06-03 | Information Disclosure | display_name in nav | mitigate | Autoescape only, never `|safe`, on `current_user.display_name` (untrusted-at-rest text). |
</threat_model>

<verification>
- `uv run pytest tests/test_nav.py -x` green.
- `uv run pytest` full suite green — adding the `hx-headers` line and role conditionals to the shared layouts must not break any existing page-render test (the authenticated `client` fixture provides a `current_user`).
</verification>

<success_criteria>
- CSRF token carried on every desktop + mobile page (AUTH-05).
- Logout control present showing the current user.
- «Настройки» hidden for operators, shown for admins (cosmetic; ROLE-03).
</success_criteria>

<output>
Create `.planning/phases/25-authentication-roles-user-attribution/25-06-SUMMARY.md` when done.
</output>

---
phase: 30-offline-self-uploading-file
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_offline.py
autonomous: true
requirements: [OFF-01, OFF-02, OFF-03, OFF-04, OFF-05, OFF-06, OFF-07]
must_haves:
  truths:
    - "tests/test_offline.py collects with no import errors (the rest of the suite stays green)"
    - "A helper builds a valid offline NDJSON body carrying a correct payload_sha256 header computed over the record lines only"
    - "A helper mints an offline-scoped itsdangerous token via settings.secret_key + salt 'offline-upload', plus an expired variant"
    - "A helper POSTs /api/offline/login on the REAL-guard anon_client to obtain an upload token"
  artifacts:
    - path: "tests/test_offline.py"
      provides: "Wave-0 RED test scaffold + fixtures for OFF-01..07 plus token/bypass/escaping/CRLF/rate-limit rows"
      contains: "def _offline_ndjson"
    - path: "tests/test_offline.py"
      provides: "Inline offline-token minter (mirrors the offline service contract)"
      contains: "offline-upload"
  key_links:
    - from: "tests/test_offline.py"
      to: "conftest.anon_client"
      via: "REAL-guard TestClient fixture (mirrors device_client rationale)"
      pattern: "anon_client"
    - from: "tests/test_offline.py"
      to: "settings.secret_key"
      via: "URLSafeTimedSerializer(settings.secret_key, salt='offline-upload')"
      pattern: "offline-upload"
---

<objective>
Seed the Nyquist Wave-0 validation scaffold for Phase 30: create `tests/test_offline.py`
with the shared fixtures/helpers and the full RED test map (OFF-01..07 plus the
token / auth-guard-bypass / `</script>`-escaping / CRLF / rate-limit rows) from
RESEARCH §Validation Architecture. No production code changes.

Purpose: every downstream implementation task (Waves 1-3) gets a concrete
`<automated>` command that turns a specific RED test GREEN — no task ships without
an automated verify. The helpers (offline NDJSON body with a correct
`payload_sha256`, direct offline-token minter + expired variant, login-then-token
helper) are the contract the ingest and export code must honour.

Output: `tests/test_offline.py` — collectable in Wave 0 (module-level imports touch
only already-existing symbols), RED where it exercises not-yet-built routes/services.
Each RED test is annotated with the wave/plan that turns it green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-offline-self-uploading-file/30-RESEARCH.md
@.planning/phases/30-offline-self-uploading-file/30-PATTERNS.md
@.planning/phases/30-offline-self-uploading-file/30-VALIDATION.md
@tests/test_sync_api.py
@tests/conftest.py
@app/services/merge.py
@app/config.py
</context>

<artifacts_this_phase_produces>
Newly-created symbols across Phase 30 — exclude these from source-drift/grounding
checks (they do not exist before this phase):

- Routes: `GET /offline/export`, `POST /api/offline/login`, `POST /api/offline/upload` (`app/routes/offline.py`)
- Service: `app/services/offline.py` — `mint_offline_token`, `verify_offline_token`, `schema_version_ok`, `OFFLINE_TOKEN_TTL`, `OFFLINE_TOKEN_SCOPE`
- Function promotion: `app/services/sync_client.collect_push_records` (public; was `_collect_push_records`)
- Function: `app/services/merge.payload_digest`
- Wire header field: `payload_sha256` (in `serialize_exchange` output)
- Constant: `app/services/security.OFFLINE_PATH_PREFIX = "/api/offline/"`
- Templates: `app/templates/offline/self_upload.html`, `app/templates/offline/result.html`
- Test module: `tests/test_offline.py`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Shared fixtures + helpers (offline NDJSON body, token minter, login-token helper)</name>
  <read_first>
    - tests/test_offline.py (target — new file)
    - tests/test_sync_api.py (analog: `_header_line`/`_ndjson`/`_op` body factory lines 42-95, `_fresh_buckets` autouse fixture lines 42-47, `_seed_admin` lines 98+)
    - tests/conftest.py (fixtures: `anon_client` line 191 REAL guard, `login` helper line 348, `session`/`product`/`batch`)
    - app/services/merge.py (serialize_exchange lines 527-559 — header shape + record-line JSON, FORMAT_VERSION)
    - app/config.py (settings.secret_key resolution lines 35, 86-87)
    - .planning/phases/30-offline-self-uploading-file/30-RESEARCH.md (§Validation Architecture Wave-0 Gaps; Pattern 2 token round-trip)
  </read_first>
  <files>tests/test_offline.py</files>
  <action>
    Create `tests/test_offline.py` with a module docstring stating it builds on the
    REAL-guard `anon_client` (NOT the wholesale-override `client` fixture, which would
    make the `/api/offline/` bypass untestable — mirror the `test_sync_api.py:1-19`
    rationale). Add the autouse `_fresh_buckets` fixture (copy `test_sync_api.py:42-47`
    calling `rate_limit.reset_buckets()`) so the rate-limit test cannot leak.

    Module-level imports MUST touch only already-existing symbols: `json`, `hashlib`,
    `pytest`, `itsdangerous.URLSafeTimedSerializer`, `app.config.settings`,
    `app.services.merge`, `app.core.new_id`, `app.models` (User, Operation, etc),
    `app.services.auth.hash_password`. Do NOT import `app.routes.offline` or
    `app.services.offline` at module level (they arrive in later waves) — defer those
    imports INSIDE the tests that need them so collection stays green in Wave 0.

    Add these helpers:
    - `_offline_ndjson(records: list[dict]) -> str`: build the NDJSON text exactly as
      `serialize_exchange` will — a header line then one JSON line per record. Compute
      the header's `payload_sha256` yourself as `hashlib.sha256("\n".join(record_lines).encode("utf-8")).hexdigest()`
      over the record lines only (header excluded, exact emission order). Header keys:
      `kind="header"`, `format_version=merge.FORMAT_VERSION`, `schema_version` (default `""`),
      `source_device_id`, `generated_at`, `counts` (kind→count map), `payload_sha256`.
      Return the LF-joined string (the route canonicalizes via splitlines()).
    - `_mint_offline_token(user_id: str) -> str`: build a token with
      `URLSafeTimedSerializer(settings.secret_key, salt="offline-upload").dumps({"scope": "offline_upload", "sub": user_id})`
      — the exact contract Wave 1's `app/services/offline.mint_offline_token` must honour.
    - `_seed_admin(session, login="boss", password="pw")`: seed one active administrator
      User with `hash_password` (copy `test_sync_api.py:_seed_admin`).
    - `_offline_login(anon_client, login, password) -> httpx.Response`: POST
      `/api/offline/login` with `data={"login":..,"password":..}` (form-urlencoded),
      `follow_redirects=False`; return the response so tests can read `.json()["token"]`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_offline.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_offline.py` collects with zero import/collection errors
    - `_offline_ndjson`, `_mint_offline_token`, `_seed_admin`, `_offline_login` helpers are defined
    - `_fresh_buckets` autouse fixture calls `rate_limit.reset_buckets()`
    - Module-level imports reference only existing symbols; `app.routes.offline`/`app.services.offline` are imported inside test bodies
    - `uv run pytest --ignore=tests/test_offline.py -q` stays at the green baseline
  </acceptance_criteria>
  <done>The Wave-0 helpers and fixtures exist and the module collects cleanly without disturbing the existing suite.</done>
</task>

<task type="auto">
  <name>Task 2: RED test bodies for OFF-01..07 + token / bypass / escaping / CRLF / rate-limit</name>
  <read_first>
    - tests/test_offline.py (target — extend Task 1's file)
    - tests/test_sync_api.py (analogs: `test_push_idempotent` lines 192-206, `test_push_all_or_nothing` lines 209-226, `test_device_token_cannot_reach_html` 165-173, `test_session_cookie_cannot_reach_sync` 176-186, `test_push_rejects_oversized_body` 232-240)
    - .planning/phases/30-offline-self-uploading-file/30-RESEARCH.md (§Validation Architecture Requirements→Test Map — every row = one test name)
    - .planning/phases/30-offline-self-uploading-file/30-UI-SPEC.md (§S2 locked RU result strings the rejection tests assert on)
  </read_first>
  <files>tests/test_offline.py</files>
  <action>
    Write one test per row of the RESEARCH Requirements→Test Map, using the exact test
    names it lists. Defer imports of `app.routes.offline` / `app.services.offline` inside
    each test body. Mark the intended landing wave in each test's docstring (e.g.
    "GREEN in 30-03"). The tests assert real behaviour so they turn green as code lands;
    they are RED (fail/404), never collection-errors, in Wave 0.

    Ingest (via `anon_client`, REAL guard — exercises the bypass):
    - `test_login_success_mints_token` / `test_login_wrong_password_no_token`: login with
      right vs wrong creds → 200 with a `token` field vs 401 with NO `token` key. Assert the
      401 body carries NO payload echo. (OFF-04, GREEN in 30-03)
    - `test_login_rate_limited`: exhaust `check_rate_limit` (loop > SYNC_BUCKET_CAPACITY) →
      429. (GREEN in 30-03)
    - `test_upload_twice_is_noop`: mint a token via `_mint_offline_token`, POST a valid
      `_offline_ndjson` body (with one operation over a seeded product/batch) to
      `/api/offline/upload` as a multipart form field `payload` + form field `token`; second
      identical POST inserts nothing (mirror `test_push_idempotent`). (OFF-05a, GREEN in 30-03)
    - `test_upload_all_or_nothing`: a body whose last record references a non-existent
      product_id rolls the whole batch back (mirror `test_push_all_or_nothing`); assert row
      count unchanged. (OFF-05b, GREEN in 30-03)
    - `test_upload_corrupted_checksum_rejected`: flip a byte inside a record line WITHOUT
      updating `payload_sha256` → rejected before any DB touch; result HTML contains
      «Файл повреждён». (OFF-07a, GREEN in 30-03)
    - `test_upload_incompatible_schema_rejected`: server reports a real revision but the
      header `schema_version` differs → 409, both versions named in «Файл собран для версии
      данных … сервер ожидает …». `test_upload_empty_server_schema_skips_gate`: empty
      server schema (create_all fixture) → gate skipped, upload proceeds. (OFF-07b, GREEN in 30-03)
    - `test_upload_bad_format_version_rejected`: header `format_version` != FORMAT_VERSION →
      rejected via parse_exchange. (OFF-07c, GREEN in 30-03)
    - `test_upload_expired_token_rejected`: mint a token, then monkeypatch
      `app.services.offline.OFFLINE_TOKEN_TTL` to a value in the past (e.g. -1) so verify
      raises → 401 / «Время на загрузку истекло». (GREEN in 30-03)
    - `test_offline_bypass_is_narrow`: a browser session (via `login` after `_seed_admin`)
      cannot use `/api/offline/*` as a session bypass, AND the `/offline/export` page requires
      a session (anonymous → 303 /login). Mirror `test_session_cookie_cannot_reach_sync`.
      (T-30-06, GREEN spans 30-02 guard + 30-03/30-04 routes)

    Export + serializer (unit, no network):
    - `test_export_header_counts_present` / `test_export_does_not_stamp_synced_at`: after
      `collect_push_records` + `serialize_exchange`, the header carries a `counts` map and a
      `payload_sha256`, and no `Operation.synced_at` was written. (OFF-06/OFF-01, GREEN in 30-02/30-04)
    - `test_export_bundle_fk_closure_complete`: the serialized bundle includes the FK
      parents (product/batch/sale/customer/warehouse) of the unsynced ledger rows. (OFF-02, GREEN in 30-04)
    - `test_export_html_contains_embedded_payload_and_form`: `GET /offline/export` (session)
      returns HTML embedding `<script type="application/x-ndjson">` and a top-level `<form ... action=".../api/offline/upload"`. (OFF-03, GREEN in 30-04)
    - `test_script_tag_escaping_round_trip`: a product name containing a literal `</script>`
      survives export→embed(`<\/script`)→unescape→digest→upload→parse unchanged. (T-30-08, GREEN in 30-04/30-03)
    - `test_crlf_payload_digest_matches`: a payload whose line endings are CRLF-normalized
      still verifies its digest (splitlines()+"\n".join() canonicalization on both sides).
      (Pitfall 1, GREEN in 30-03)
  </action>
  <verify>
    <automated>uv run pytest tests/test_offline.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - Every RESEARCH Requirements→Test Map row exists as a named test that collects cleanly
    - Each RED test docstring names the wave/plan that turns it green
    - Imports of not-yet-built modules are deferred inside test bodies (no collection error)
    - `uv run pytest --ignore=tests/test_offline.py -q` remains at the green baseline
  </acceptance_criteria>
  <done>The full RED test map is present and collectable; the tests are RED-by-design pending their implementing waves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → (future) offline routes | Wave-0 scaffold defines the contract the untrusted-file ingest must satisfy |

## STRIDE Threat Register (Phase 30 — this plan seeds the tests that PROVE the mitigations)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Tampering | offline_upload digest gate | mitigate | `test_upload_corrupted_checksum_rejected` proves SHA-256 verify before merge (impl 30-03) |
| T-30-02 | Integrity | offline_upload schema gate | mitigate | `test_upload_incompatible_schema_rejected` + `_empty_server_schema_skips_gate` (impl 30-03) |
| T-30-03 | Info Disclosure | offline_login handshake | mitigate | `test_login_wrong_password_no_token` proves no token/data on failure (impl 30-03) |
| T-30-04 | Spoofing/Elevation | upload token | mitigate | `test_upload_expired_token_rejected` proves TTL+scope enforcement (impl 30-02/30-03) |
| T-30-06 | Access Control | auth_guard bypass | mitigate | `test_offline_bypass_is_narrow` proves exact-prefix + session-guarded export (impl 30-02) |
| T-30-07 | Spoofing (brute-force) | offline_login rate limit | mitigate | `test_login_rate_limited` proves 429 (impl 30-03) |
| T-30-08 | Tampering (HTML breakout) | embedded NDJSON | mitigate | `test_script_tag_escaping_round_trip` proves `</script>` escape survives round-trip (impl 30-04) |
| T-30-09 | DoS | upload body cap | mitigate | oversized-body test (mirror `test_push_rejects_oversized_body`, impl 30-03) |
| T-30-SC | Tampering | package installs | accept | Zero new packages this phase (RESEARCH §Package Legitimacy Audit — gate not triggered); nothing to verify |
</threat_model>

<verification>
- `uv run pytest tests/test_offline.py --collect-only -q` → collects all named tests, zero errors.
- `uv run pytest --ignore=tests/test_offline.py -q` → green baseline (~1122 passing), no regression.
- The offline tests are intentionally RED until their implementing wave; do NOT gate this
  plan on them passing.
</verification>

<success_criteria>
- `tests/test_offline.py` exists, collects cleanly, and contains every RESEARCH
  Requirements→Test Map row as a named test plus the four helpers and the `_fresh_buckets`
  fixture.
- No production code changed; existing suite unaffected.
</success_criteria>

<output>
Create `.planning/phases/30-offline-self-uploading-file/30-01-SUMMARY.md` when done.
</output>

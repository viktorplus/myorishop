---
phase: 06-reports-data-export
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - app/services/stock.py
  - app/routes/reports.py
  - app/templates/pages/reports_stock.html
  - app/templates/pages/reports_landing.html
  - tests/test_reports.py
autonomous: true
requirements: [RPT-02]

must_haves:
  truths:
    - "Operator can view current stock levels for every active product on a dedicated /reports/stock page (D-03)"
    - "Operator can see, as a distinct actionable list, which active products are at or below their low-stock threshold"
    - "A product with an explicit low_stock_threshold of 0 appears in the low-stock list only when its stock is exactly 0, never falling back to the global default (D-04)"
    - "A product with no per-product threshold set still appears correctly using the global default (D-05)"
  artifacts:
    - path: "app/services/stock.py"
      provides: "effective_low_stock_threshold(product), low_stock_products(session), all_active_products(session)"
      contains: "def effective_low_stock_threshold"
  key_links:
    - from: "app/routes/reports.py"
      to: "app/services/stock.py"
      via: "GET /reports/stock calls low_stock_products/all_active_products"
      pattern: "from app.services.stock import"
    - from: "app/templates/pages/reports_landing.html"
      to: "app/routes/reports.py"
      via: "link to /reports/stock"
      pattern: "href=\"/reports/stock\""
---

<objective>
Give the operator RPT-02: a current-stock view and a distinct, actionable low-stock list, using the per-product/global-fallback threshold built in Plan 06-01.

Purpose: this is the phase's second success criterion ("operator can view current stock levels including a low-stock items list"). `Product.quantity` is already the authoritative cached projection (D-09, no new ledger recomputation needed) — the only genuinely new logic is the effective-threshold fallback, and Pitfall 3 (an explicit `0` must never fall back to the global default) is the correctness risk this plan exists to close.
Output: `app/services/stock.py`, `GET /reports/stock`, its template, and a new link on the reports landing page.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-reports-data-export/06-CONTEXT.md
@.planning/phases/06-reports-data-export/06-RESEARCH.md
@.planning/phases/06-reports-data-export/06-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: stock.py — effective threshold + low-stock/current-stock reads</name>
  <files>app/services/stock.py, tests/test_reports.py</files>
  <read_first>
    E:\dev\myorishop\app\services\operations.py
    E:\dev\myorishop\app\models.py
    E:\dev\myorishop\app\config.py
    E:\dev\myorishop\.planning\phases\06-reports-data-export\06-01-SUMMARY.md
  </read_first>
  <behavior>
    - effective_low_stock_threshold(product) returns product.low_stock_threshold when it is not None (even when it is 0), and settings.low_stock_threshold only when product.low_stock_threshold is None — implemented with an explicit "is not None" check, never a bare "or" (Pitfall 3: "or" would treat an explicit 0 as falsy and wrongly fall through to the global default).
    - A product with low_stock_threshold=0 and quantity=0 IS included in low_stock_products(); the same product with quantity=1 is NOT included (Pitfall 3's exact regression case).
    - A product with low_stock_threshold=None (never configured) and settings.low_stock_threshold=5 is included in low_stock_products() when quantity<=5.
    - low_stock_products() excludes soft-deleted products and is sorted by quantity ascending (most urgent first, per UI-SPEC).
    - all_active_products() returns every non-deleted product ordered by name_lc, for the full stock table.
  </behavior>
  <action>
    Create app/services/stock.py. Module docstring follows the read-only-service convention (references RPT-02, D-04/D-05, portable-ORM-only). Imports: "from sqlalchemy import select", "from sqlalchemy.orm import Session", "from app.config import settings", "from app.models import Product".

    Implement effective_low_stock_threshold(product: Product) -> int: "return product.low_stock_threshold if product.low_stock_threshold is not None else settings.low_stock_threshold".

    Implement low_stock_products(session: Session) -> list[Product]: fetch all active products via select(Product).where(Product.deleted_at.is_(None)) (mirrors app/services/operations.py::filter_products' shape), filter in Python to those where product.quantity <= effective_low_stock_threshold(product), then sort by quantity ascending (Python sort — the query itself does not need an ORDER BY since it is re-sorted after the Python filter).

    Implement all_active_products(session: Session) -> list[Product]: select(Product).where(Product.deleted_at.is_(None)).order_by(Product.name_lc) (mirrors filter_products exactly), returned as a list.

    Add tests to tests/test_reports.py: test_effective_threshold_zero_not_fallback (Pitfall 3, service-level, both the "included at qty 0" and "excluded at qty 1" cases from Behavior above), test_low_stock_uses_global_fallback (product with threshold=None, seeded quantity via a receipt operation at/under settings.low_stock_threshold), test_low_stock_excludes_deleted_products, test_low_stock_products_sorted_by_quantity_ascending.
  </action>
  <acceptance_criteria>
    - app/services/stock.py defines effective_low_stock_threshold, low_stock_products, all_active_products
    - effective_low_stock_threshold's implementation uses "is not None", not a bare "or" (read the function body to confirm — this is the exact Pitfall 3 regression to avoid)
    - uv run pytest tests/test_reports.py -x -q -k "threshold or low_stock" passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_reports.py -x -q -k "threshold or low_stock"</automated>
  </verify>
  <done>stock.py correctly resolves the effective threshold with explicit-zero support and returns correctly filtered, correctly sorted product lists.</done>
</task>

<task type="auto">
  <name>Task 2: GET /reports/stock route, template, landing-page link</name>
  <files>app/routes/reports.py, app/templates/pages/reports_stock.html, app/templates/pages/reports_landing.html, tests/test_reports.py</files>
  <read_first>
    E:\dev\myorishop\app\routes\reports.py
    E:\dev\myorishop\app\templates\pages\reports_landing.html
    E:\dev\myorishop\app\templates\pages\history.html
    E:\dev\myorishop\.planning\phases\06-reports-data-export\06-UI-SPEC.md
  </read_first>
  <action>
    In app/routes/reports.py, add "from app.services.stock import all_active_products, effective_low_stock_threshold, low_stock_products" to the imports, and add a new route @router.get("/reports/stock") reports_stock_page(request: Request, session: Session = Depends(get_session)). No period filter (RPT-02/D-03 — this report is "as of now", never period-based) — do NOT call _resolve_period here. Build context: "low_stock": low_stock_products(session), "all_products": all_active_products(session), and for each product, the template needs the effective threshold too — pass a precomputed list of dicts instead of raw Product objects where the low-stock table needs the threshold value displayed: build "low_stock_rows" as a list of {"product": p, "threshold": effective_low_stock_threshold(p)} (the full-stock table does not need the threshold column, per UI-SPEC's column list, so all_products can stay a plain Product list). Always renders the FULL page (pages/reports_stock.html) — no HX-Request branching, no partial needed, since there is no filter to re-apply in place (UI-SPEC: "Keep them simple; do not add HTMX machinery where a static page load already satisfies the requirement").

    Create app/templates/pages/reports_stock.html: extends base.html, h1>Остатки склада. No period_filter include. h2>Мало на складе first: if low_stock_rows is empty, p class="empty-state muted">Товаров с низким остатком нет. — else a table with header Код / Название / Остаток / Статус, one row per entry in low_stock_rows, Остаток cell class="num" showing row.product.quantity, Статус cell showing the plain text "Мало" (no color, per UI-SPEC's explicit "no color coding for report data" rule — never wrap it in any colored span/class). 24px gap (a new h2, which already carries that spacing per the existing h2 margin convention — no new CSS needed), then h2>Все товары (or similar section heading — Claude's discretion per UI-SPEC, since only "Мало на складе" has a UI-SPEC-mandated exact heading string) with the full table Код / Название / Остаток / Статус for all_products (Статус shows "Мало" for rows also present in low_stock_rows, blank/em-dash otherwise — cheapest correct approach: build a set of low-stock product ids in the route and pass it to the template, or just re-derive membership in Jinja by comparing quantity to a per-row threshold value — simplest is to also thread the low-stock id set into context as "low_stock_ids": {r["product"].id for r in low_stock_rows}, then the full table's Статус cell is "Мало" if product.id in low_stock_ids else empty).

    In app/templates/pages/reports_landing.html, append " · &lt;a href=\"/reports/stock\"&gt;Остатки склада&lt;/a&gt;" inside the SAME existing &lt;p&gt; element created by Plan 06-02 (do not add a second &lt;p&gt;).

    Add web-level tests to tests/test_reports.py: test_web_reports_stock_lists_low_stock_and_full_table, test_web_reports_stock_no_low_stock_shows_empty_state, test_web_reports_landing_links_to_stock.
  </action>
  <acceptance_criteria>
    - app/routes/reports.py defines reports_stock_page and imports from app.services.stock
    - app/templates/pages/reports_stock.html contains "Мало на складе" and "Мало" (plain text status, no color class wrapping it)
    - app/templates/pages/reports_landing.html contains "href=\"/reports/stock\"" inside the same &lt;p&gt; as the sales link
    - uv run pytest tests/test_reports.py -x -q passes (full file — Task 1 and Task 2 tests both green)
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_reports.py -x -q</automated>
  </verify>
  <done>/reports/stock shows the low-stock action list first (correct effective-threshold filtering) and the full active-product stock table below it; /reports links to it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser -> GET /reports/stock | No client-supplied parameters at all — pure server-side read, lowest-risk route in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|------------------|
| T-06-07 | Tampering (logic error, not an external attacker) | effective_low_stock_threshold | mitigate | Explicit "is not None" check (Pitfall 3), pinned by test_effective_threshold_zero_not_fallback — an incorrect bare-"or" implementation would silently hide correctly-configured zero-threshold products from the low-stock list, a data-correctness bug with no external trigger needed |
| T-06-08 | Information Disclosure | reports_stock.html | accept | Read-only stock levels; single local operator, no untrusted multi-tenant boundary; no new data exposed beyond what /products already shows |
</threat_model>

<verification>
uv run pytest tests/test_reports.py -x -q — full file green (Task 1 + Task 2 tests, plus Plan 06-02's tests still passing). uv run ruff check app/services/stock.py app/routes/reports.py app/templates/pages/reports_stock.html.
</verification>

<success_criteria>
RPT-02 is fully satisfied: current stock levels view plus a correctly-filtered, correctly-sorted low-stock action list, honoring per-product overrides down to an explicit 0.
</success_criteria>

<output>
Create `.planning/phases/06-reports-data-export/06-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `app.services.stock` — new module
- `app.services.stock.effective_low_stock_threshold(product: Product) -> int` — new function
- `app.services.stock.low_stock_products(session) -> list[Product]` — new function
- `app.services.stock.all_active_products(session) -> list[Product]` — new function
- `GET /reports/stock` — new route
- `app/templates/pages/reports_stock.html` — new template

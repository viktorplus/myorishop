---
phase: 06-reports-data-export
plan: 06
type: execute
wave: 4
depends_on: ["06-01", "06-05"]
files_modified:
  - app/services/reports.py
  - app/routes/reports.py
  - app/templates/pages/reports_products.html
  - app/templates/partials/top_selling_rows.html
  - app/templates/pages/reports_landing.html
  - tests/test_reports.py
autonomous: true
requirements: [RPT-04]

must_haves:
  truths:
    - "Operator can view the top-selling products for a chosen period, ranked by units sold, on a dedicated /reports/products page (D-03)"
    - "Operator can view products that have gone stale (no sale in longer than their effective per-product-or-global-default stale-days threshold — D-05, or never sold at all), independent of any period filter"
    - "A product with stale_days=0 is treated as an explicit, deliberate threshold, never silently replaced by the global default (D-04)"
    - "A soft-deleted product never appears in the stale/never-sold list, even though its past sales still count in period-based reports elsewhere in this phase"
  artifacts:
    - path: "app/services/reports.py"
      provides: "top_selling_products(session, start_iso, end_iso, limit=10), stale_products(session)"
      contains: "def stale_products"
  key_links:
    - from: "app/routes/reports.py"
      to: "app/services/reports.py"
      via: "GET /reports/products calls top_selling_products and stale_products"
      pattern: "stale_products\\("
    - from: "app/templates/pages/reports_landing.html"
      to: "app/routes/reports.py"
      via: "link to /reports/products"
      pattern: "href=\"/reports/products\""
---

<objective>
Close out RPT-04 and the phase's reporting scope: a period-ranked top-selling list and an independent, always-current stale-products list, both built on the effective-threshold pattern from Plan 06-01/06-03.

Purpose: this is the phase's fourth success criterion ("operator can view top-selling products and products with no sales for a long time"). Top-selling reuses SQL-side aggregation (potentially many rows across all history, per RESEARCH Pattern 4) while staleness reuses the effective-threshold-with-explicit-zero discipline already proven in Plan 06-03.
Output: `top_selling_products`/`stale_products` in `app/services/reports.py`, `GET /reports/products`, its template, the final landing-page link — this is the last plan in the phase.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-reports-data-export/06-CONTEXT.md
@.planning/phases/06-reports-data-export/06-RESEARCH.md
@.planning/phases/06-reports-data-export/06-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: top_selling_products (SQL aggregation) + stale_products (effective-threshold filter)</name>
  <files>app/services/reports.py, tests/test_reports.py</files>
  <read_first>
    E:\dev\myorishop\app\services\reports.py
    E:\dev\myorishop\app\services\stock.py
    E:\dev\myorishop\app\config.py
    E:\dev\myorishop\app\core.py
    E:\dev\myorishop\.planning\phases\06-reports-data-export\06-01-SUMMARY.md
    E:\dev\myorishop\.planning\phases\06-reports-data-export\06-03-SUMMARY.md
  </read_first>
  <behavior>
    - top_selling_products ranks products by units sold (descending) within [start_iso, end_iso), capped at 10, using SQL func.sum/.group_by()/.order_by()/.limit() (not a Python accumulator — RESEARCH Pattern 4: this is a potentially-large-history aggregation, unlike the small fixed-cardinality write-off grouping).
    - A product with 5 units sold in the period ranks above one with 3, regardless of insertion order.
    - stale_products(session) returns a product with NO sale ever (a genuinely never-sold active product) — this requires a LEFT OUTER JOIN so a product with zero matching Operation rows still appears in the base result set, with last_sale=None.
    - A product with stale_days=0 (explicit) and a sale yesterday IS included (0 days is an immediately-stale threshold, an explicit "alert me the instant it's been more than 0 days" choice) — never silently replaced by settings.stale_days because 0 is falsy in Python (the same Pitfall 3 class of bug as the low-stock threshold, applied here to _effective_stale_days, a SEPARATE small helper local to this module — do not import from app.services.stock, since stale_days is a reports-only concern distinct from stock levels).
    - A soft-deleted product with zero sales ever is EXCLUDED from stale_products (RESEARCH Open Question 2: "nothing actionable on a deleted product" — deleted products never appear in the stale/never-sold list, even though the SAME product's historical sales still appear correctly in sales_profit_report/writeoff_report for periods before its deletion, per Pitfall 5, which applies to THOSE functions, not this one).
    - stale_products is sorted with never-sold products first, then by days-since-last-sale descending.
  </behavior>
  <action>
    In app/services/reports.py, add "from datetime import date, datetime" and "from zoneinfo import ZoneInfo" to the imports (if not already present from a shared import block — check current file state first), "from sqlalchemy import func" (extend the existing "from sqlalchemy import select" line), and "from app.config import settings" (new import — this file did not need settings before this task).

    Add a local helper "_effective_stale_days(product: Product) -> int": "return product.stale_days if product.stale_days is not None else settings.stale_days" — explicit is-not-None check, mirroring app/services/stock.py::effective_low_stock_threshold's exact pattern but intentionally NOT imported from there (documented reasoning: stale_days is a reports-only concept, keeping the two effective-threshold helpers in their own domain modules avoids a cross-module coupling for a two-line function).

    Implement top_selling_products(session: Session, start_iso: str, end_iso: str, limit: int = 10) -> list[dict]. Build "units_sold = func.sum(-Operation.qty_delta).label('units_sold')"; query select(Product, units_sold).join(Operation, Operation.product_id == Product.id).where(Operation.type == "sale", Operation.created_at >= start_iso, Operation.created_at < end_iso).group_by(Product.id).order_by(units_sold.desc()).limit(limit) — exact shape from RESEARCH Pattern 4. Execute via session.execute(stmt).all(), return [{"product": p, "units_sold": units} for p, units in rows].

    Implement stale_products(session: Session) -> list[dict]. Build "last_sale = func.max(Operation.created_at).label('last_sale')"; query select(Product, last_sale).outerjoin(Operation, (Operation.product_id == Product.id) & (Operation.type == "sale")).where(Product.deleted_at.is_(None)).group_by(Product.id) — exact shape from RESEARCH Pattern 4, LEFT OUTER JOIN so never-sold products still appear. Execute, then in Python: compute "today_local = datetime.now(ZoneInfo(settings.display_tz)).date()"; for each (product, last_sale_iso) row, compute days_since as None when last_sale_iso is None (never sold), else "(today_local - datetime.fromisoformat(last_sale_iso).astimezone(ZoneInfo(settings.display_tz)).date()).days"; include the row in the result ONLY when last_sale_iso is None OR days_since >= _effective_stale_days(product). Sort the included rows: never-sold (days_since is None) first, then by days_since descending — implement via a sort key like "(0, 0) if days_since is None else (1, -days_since)" reversed appropriately, or simpler: two separate lists (never_sold, then stale_with_date sorted by days_since descending) concatenated — either is acceptable, pick whichever reads more clearly and document the choice in a one-line comment. Return a list of {"product": product, "last_sale_iso": last_sale_iso, "days_since": days_since}.

    Add tests to tests/test_reports.py: test_top_selling_orders_by_units (5-units product ranks above 3-units product), test_top_selling_respects_limit (11 distinct products sold, limit=10 default returns exactly 10), test_stale_includes_never_sold (a product with zero sale ops, active, appears with days_since None), test_stale_threshold_zero_not_fallback (Pitfall 3 applied to stale_days — a product with stale_days=0 and a sale from yesterday IS included; a product with stale_days=0 and a sale from TODAY is excluded), test_stale_excludes_soft_deleted_never_sold_product.
  </action>
  <acceptance_criteria>
    - app/services/reports.py defines top_selling_products, stale_products, and a local _effective_stale_days helper using an explicit "is not None" check (not a bare "or")
    - top_selling_products' implementation uses func.sum/.group_by()/.order_by()/.limit() (SQL-side aggregation, not a Python dict accumulator — verify by reading the function body)
    - stale_products' query uses .outerjoin(...) (verify by reading the function body — a plain .join() would silently drop never-sold products, defeating the requirement)
    - uv run pytest tests/test_reports.py -x -q -k "top_selling or stale" passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_reports.py -x -q -k "top_selling or stale"</automated>
  </verify>
  <done>top_selling_products correctly ranks via SQL aggregation; stale_products correctly includes never-sold products via LEFT OUTER JOIN and respects an explicit stale_days=0 without falling back to the global default.</done>
</task>

<task type="auto">
  <name>Task 2: GET /reports/products route, templates, final landing-page link</name>
  <files>app/routes/reports.py, app/templates/pages/reports_products.html, app/templates/partials/top_selling_rows.html, app/templates/pages/reports_landing.html, tests/test_reports.py</files>
  <read_first>
    E:\dev\myorishop\app\routes\reports.py
    E:\dev\myorishop\app\templates\pages\reports_sales.html
    E:\dev\myorishop\app\templates\partials\sales_report_results.html
    E:\dev\myorishop\app\templates\pages\reports_stock.html
    E:\dev\myorishop\app\templates\pages\reports_landing.html
    E:\dev\myorishop\.planning\phases\06-reports-data-export\06-UI-SPEC.md
  </read_first>
  <action>
    In app/routes/reports.py, add "stale_products, top_selling_products" to the "from app.services.reports import ..." line. Add @router.get("/reports/products") reports_products_page(request, from_: str = Query("", alias="from"), to: str = Query("", alias="to"), session: Session = Depends(get_session)). Period handling for the TOP-SELLING half only: call the SAME shared _resolve_period(from_, to, settings.display_tz), then on success local_day_bounds_utc + top_selling_products(session, start_iso, end_iso); on period error, top_selling = None. The STALE half has NO period dependency at all: always call stale_products(session) regardless of period state (it must render correctly even when the period query params are garbage, since staleness is entirely independent of the period filter per UI-SPEC). Branch on HX-Request: an HX request (fired only by the top-selling half's period_filter form/preset links, whose hx-target is scoped to ONLY the top-selling results div) renders ONLY "partials/top_selling_rows.html"; a plain/full request renders "pages/reports_products.html" with BOTH halves in context (top_selling/period fields AND stale_rows).

    Create app/templates/partials/top_selling_rows.html: mirrors sales_report_results.html's three-way branch structure but simpler (no summary block, just a ranked list). Error path: same shared error-block copy. Empty path (top_selling is an empty list): p class="empty-state muted">За выбранный период продаж не было. Data path: h2>Топ продаж (the heading lives INSIDE this partial, matching the reports_writeoffs.html precedent from Plan 06-05) followed by a table header Товар / Продано, шт., one row per top_selling entry (product cell "{{ row.product.name }} ({{ row.product.code }})", units_sold cell class="num").

    Create app/templates/pages/reports_products.html: extends base.html, h1>Топ и залежавшиеся товары. Section 1 (period-based): {% with period_action = "/reports/products", period_target = "#top-selling-results" %}{% include "partials/period_filter.html" %}{% endwith %}, then div id="top-selling-results">{% include "partials/top_selling_rows.html" %}&lt;/div&gt;. 24px gap (a following h2 already carries this per the established spacing convention — no new CSS). Section 2 (NOT period-based, rendered directly, no partial/no swap target — a plain full-page section): h2>Залежавшиеся товары; if stale_rows is empty, p class="empty-state muted">Залежавшихся товаров нет.; else a table header Код / Название / Последняя продажа / Дней без продаж, one row per stale_rows entry — Последняя продажа cell shows "Никогда" when row.last_sale_iso is None else the local-formatted date/time (reuse the "| local_dt" filter already registered in app/routes/__init__.py, applied to row.last_sale_iso), Дней без продаж cell class="num" shows row.days_since or, when None (never sold), a muted em-dash-style placeholder consistent with how other "no value" numeric cells render elsewhere in this app (e.g. history_rows.html's "&lt;span class=\"muted\"&gt;—&lt;/span&gt;" pattern for a null unit_cost_cents cell — copy that exact convention here).

    In app/templates/pages/reports_landing.html, append " · &lt;a href=\"/reports/products\"&gt;Топ и залежавшиеся товары&lt;/a&gt;" inside the same existing &lt;p&gt; element — this is the fourth and final link, completing the four-report set described in 06-CONTEXT.md.

    Add web-level tests to tests/test_reports.py: test_web_reports_products_top_selling_ranked, test_web_reports_products_stale_shows_never_sold_as_nikogda (asserts "Никогда" appears in the response for a never-sold active product), test_web_reports_products_stale_independent_of_bad_period (GET /reports/products?from=garbage still renders the stale section correctly even though the top-selling half shows the RU date error), test_web_reports_landing_links_to_all_four_reports (asserts all four hrefs — sales, stock, writeoffs, products — are present in one GET /reports response, closing out the phase's landing-page contract).
  </action>
  <acceptance_criteria>
    - app/routes/reports.py defines reports_products_page and calls stale_products UNCONDITIONALLY (not inside the "if not period error" branch — verify by reading the function body, since stale_products must render correctly even when the period query params are invalid)
    - app/templates/pages/reports_landing.html contains all four hrefs: /reports/sales, /reports/stock, /reports/writeoffs, /reports/products, all inside the same &lt;p&gt; element
    - uv run pytest tests/test_reports.py -x -q passes (full file — every plan's tests in this phase green together)
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_reports.py -x -q &amp;&amp; uv run pytest -q</automated>
  </verify>
  <done>/reports/products shows a period-ranked top-selling table and an always-current stale-products table, independently of each other; /reports links to all four reports; the full project test suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser -> GET /reports/products | Untrusted query params from/to (top-selling half only) — same shared _resolve_period path as every other period report in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|------------------|
| T-06-04 (inherited) | Denial of Service / Tampering | _resolve_period (shared) | mitigate | Already mitigated in Plan 06-02; reused unmodified here |
| T-06-13 | Tampering (logic error) | _effective_stale_days | mitigate | Explicit "is not None" check, pinned by test_stale_threshold_zero_not_fallback — the same Pitfall 3 class of bug as T-06-07, applied to a second threshold field |
| T-06-14 | Information Disclosure | stale_products query scope | accept | Deliberately excludes soft-deleted products (a scope decision, not a security control) — no untrusted-input risk; single local operator |
</threat_model>

<verification>
uv run pytest -q — the FULL project test suite green, confirming this final plan does not regress any of Plans 06-01 through 06-05 or any pre-Phase-6 test. uv run ruff check . — zero lint errors across the whole tree.
</verification>

<success_criteria>
RPT-04 is fully satisfied: period-ranked top-selling products via SQL aggregation, plus an always-current stale/never-sold list honoring per-product zero-threshold overrides and excluding soft-deleted products. All five Phase 6 requirements (RPT-01..04, BCK-02) are now complete; /reports links to all four report pages; /export offers all three CSV downloads.
</success_criteria>

<output>
Create `.planning/phases/06-reports-data-export/06-06-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `app.services.reports.top_selling_products(session, start_iso, end_iso, limit=10) -> list[dict]` — new function
- `app.services.reports.stale_products(session) -> list[dict]` — new function
- `app.services.reports._effective_stale_days(product) -> int` — new local helper
- `GET /reports/products` — new route
- `app/templates/pages/reports_products.html` — new template
- `app/templates/partials/top_selling_rows.html` — new partial

---
phase: 04-sales-customers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models.py
  - app/services/ledger.py
  - alembic/versions/0004_sales_customers.py
  - tests/conftest.py
  - tests/test_ledger.py
  - tests/test_sales.py
  - tests/test_customers.py
autonomous: true
requirements: [SAL-01, SAL-05]
must_haves:
  truths:
    - "A sales header row and a customers row can be created; each is a 36-char UUID-keyed table"
    - "record_operation(..., sale_id=...) stores the link on the sale op at INSERT time"
    - "After alembic upgrade head (chain 0001..0004) the operations ledger is STILL append-only (UPDATE/DELETE abort)"
    - "tests/test_sales.py and tests/test_customers.py exist and are RED (services/routes not yet built)"
  artifacts:
    - path: "app/models.py"
      provides: "Customer + Sale models and Operation.sale_id column"
      contains: "class Sale"
    - path: "app/services/ledger.py"
      provides: "record_operation with backward-compatible sale_id kwarg"
      contains: "sale_id"
    - path: "alembic/versions/0004_sales_customers.py"
      provides: "customers + sales tables, operations.sale_id native ADD COLUMN, indexes"
      contains: "revision = \"0004\""
    - path: "tests/test_sales.py"
      provides: "RED contract for SAL-01..05"
      min_lines: 80
    - path: "tests/test_customers.py"
      provides: "RED contract for CST-01/02"
      min_lines: 60
  key_links:
    - from: "app/services/ledger.py"
      to: "Operation.sale_id"
      via: "Operation(... sale_id=sale_id ...)"
      pattern: "sale_id=sale_id"
    - from: "alembic/versions/0004_sales_customers.py"
      to: "operations table"
      via: "native op.add_column (NOT batch_alter_table)"
      pattern: "op.add_column\\(\\s*\"operations\""
---

<objective>
Lay the Phase 4 schema + ledger foundation and write the phase-wide RED test contract. This wave creates the `customers` and `sales` tables plus a nullable `sale_id` FK column on `operations`, extends the single write path `record_operation` with one backward-compatible `sale_id` kwarg (D-03 header↔line link), adds the `customer`/`stocked_product` conftest fixtures, and lands the failing `tests/test_sales.py` + `tests/test_customers.py` contracts that later waves turn green.

Purpose: every downstream slice (sale basket, oversell, customers, purchase history) depends on these tables existing in `app/models.py` so `Base.metadata.create_all` (conftest) sees them, and on the ledger accepting `sale_id`. This is genuinely shared foundation — the established project pattern (mirrors Phase 01 Plan 01-02 "data foundation" + Wave-0 RED contract).
Output: models (Customer, Sale, Operation.sale_id), migration 0004, ledger kwarg, conftest fixtures, extended test_ledger.py, and two RED test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-sales-customers/04-RESEARCH.md
@.planning/phases/04-sales-customers/04-PATTERNS.md
@.planning/phases/04-sales-customers/04-VALIDATION.md
@CLAUDE.md
</context>

## Artifacts this phase produces (this plan)

- `Customer` model class (`app/models.py`): id `String(36)` PK, name (NOT NULL), surname (nullable), consultant_number (nullable), `search_lc` (indexed Cyrillic shadow), created_at/updated_at.
- `Sale` model class (`app/models.py`): id `String(36)` PK, `customer_id` nullable FK→customers (indexed), created_at (NOT NULL), created_by (NOT NULL), nullable `device_id`.
- `Operation.sale_id` column (`app/models.py`): `Mapped[str | None]` FK→sales, indexed.
- `record_operation(..., sale_id: str | None = None)` kwarg (`app/services/ledger.py`).
- Migration `alembic/versions/0004_sales_customers.py` (revision "0004", down_revision "0003").
- conftest fixtures: `stocked_product`, `customer`.
- New test files: `tests/test_sales.py`, `tests/test_customers.py`; extended `tests/test_ledger.py`.

<tasks>

<task type="auto">
  <name>Task 1: Add Customer + Sale models, Operation.sale_id, and the record_operation sale_id kwarg</name>
  <files>app/models.py, app/services/ledger.py</files>
  <read_first>
    - app/models.py (read fully — mirror Product lines 50-83, Dictionary lines 86-100, Operation lines 103-122, NAMING_CONVENTION lines 24-30)
    - app/services/ledger.py (read fully — record_operation signature lines 29-39 and the Operation(...) constructor lines 65-77)
    - tests/test_ledger.py::test_conventions_uuid_cents_utc (lines 75-99 — every table PK must be String(36); no Numeric/Float; *_cents must be Integer)
    - 04-RESEARCH.md Code Example 2 (models) + Pattern 1; 04-PATTERNS.md "app/models.py" and "app/services/ledger.py" sections
  </read_first>
  <action>
    In app/models.py add two classes and one column, using SQLAlchemy 2.0 `Mapped[]`/`mapped_column()` style and the frozen NAMING_CONVENTION.
    - `class Customer(Base)` `__tablename__ = "customers"`: `id: Mapped[str] = mapped_column(String(36), primary_key=True, default=new_id)`; `name: Mapped[str] = mapped_column(String(200), nullable=False)`; `surname: Mapped[str | None] = mapped_column(String(200))`; `consultant_number: Mapped[str | None] = mapped_column(String(50))`; `search_lc: Mapped[str | None] = mapped_column(String(400), index=True)` (Cyrillic-safe shadow maintained by the SERVICE layer, per D-07 and Product.name_lc comment lines 75-77 — never SQL lower()); `created_at: Mapped[str] = mapped_column(String(32), default=utcnow_iso)`; `updated_at: Mapped[str] = mapped_column(String(32), default=utcnow_iso, onupdate=utcnow_iso)`. NO unique constraint (RESEARCH A2: walk-in quick-create tolerates duplicate names/numbers).
    - `class Sale(Base)` `__tablename__ = "sales"`: `id` String(36) PK default new_id; `customer_id: Mapped[str | None] = mapped_column(ForeignKey("customers.id", name="fk_sales_customer_id_customers"), index=True)`; `created_at: Mapped[str] = mapped_column(String(32), nullable=False)`; `created_by: Mapped[str] = mapped_column(String(100), nullable=False)`; `device_id: Mapped[str | None] = mapped_column(String(36))` (nullable sync provenance — RESEARCH Open Q1). Header carries NO qty/price — stock is computed ONLY from ledger sale ops (D-03).
    - On `Operation` (after `payload`, line 117) add `sale_id: Mapped[str | None] = mapped_column(ForeignKey("sales.id", name="fk_operations_sale_id_sales"), index=True)`. Declaring the ORM ForeignKey makes the Unit-of-Work insert the Sale header BEFORE the sale ops in one flush (satisfies foreign_keys=ON — Pitfall 2). Do NOT edit OPERATION_TYPES ("sale" already present, line 35).
    In app/services/ledger.py extend `record_operation`: add keyword-only param `sale_id: str | None = None` AFTER `payload` (before `commit`), and pass `sale_id=sale_id` into the `Operation(...)` constructor. This is set at INSERT time only — the operations_no_update trigger ABORTs any later UPDATE (Pitfall 3). All 5 existing callers keep working (default None).
  </action>
  <verify>
    <automated>uv run pytest tests/test_ledger.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/models.py` contains `class Customer(Base)` and `class Sale(Base)` and `sale_id: Mapped[str | None]` on Operation.
    - `app/services/ledger.py` contains `sale_id: str | None = None` in the record_operation signature and `sale_id=sale_id` in the Operation constructor.
    - `uv run pytest tests/test_ledger.py -q` passes, INCLUDING `test_conventions_uuid_cents_utc` (customers/sales tables now iterated: String(36) PKs, no float/_cents violations).
    - `uv run ruff check app/models.py app/services/ledger.py` exits 0.
  </acceptance_criteria>
  <done>Customer, Sale, Operation.sale_id exist and the ledger accepts sale_id without breaking any existing test.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 0004 (customers + sales + native operations.sale_id) and conftest fixtures</name>
  <files>alembic/versions/0004_sales_customers.py, tests/conftest.py</files>
  <read_first>
    - alembic/versions/0002_catalog_dictionary.py (frozen-file rule; native op.add_column lines 34-40; no app imports)
    - alembic/versions/0001_initial_schema.py (create_table + FK/PK/index naming via op.f, lines 55-92; the batch-drops-triggers WARNING lines 11-14)
    - alembic/env.py (render_as_batch=True is set globally — but plain op.add_column stays a NATIVE ALTER, as proven by 0002)
    - tests/conftest.py (product fixture lines 37-48; engine create_all + APPEND_ONLY_TRIGGERS lines 17-27)
    - 04-RESEARCH.md Code Example 3 + Pitfall 1 + Assumption A1
  </read_first>
  <action>
    Create `alembic/versions/0004_sales_customers.py`, frozen style (NEVER import app modules): `revision = "0004"`, `down_revision = "0003"`, `branch_labels = None`, `depends_on = None`. In `upgrade()`, IN THIS ORDER:
    1. `op.create_table("customers", ...)` — columns id String(36) NOT NULL, name String(200) NOT NULL, surname String(200) NULL, consultant_number String(50) NULL, search_lc String(400) NULL, created_at String(32) NOT NULL, updated_at String(32) NOT NULL; `sa.PrimaryKeyConstraint("id", name=op.f("pk_customers"))`. Then `op.create_index(op.f("ix_customers_search_lc"), "customers", ["search_lc"])`.
    2. `op.create_table("sales", ...)` — id String(36) NOT NULL, customer_id String(36) NULL, created_at String(32) NOT NULL, created_by String(100) NOT NULL, device_id String(36) NULL; `sa.PrimaryKeyConstraint("id", name=op.f("pk_sales"))`; `sa.ForeignKeyConstraint(["customer_id"], ["customers.id"], name=op.f("fk_sales_customer_id_customers"))`. Then `op.create_index(op.f("ix_sales_customer_id"), "sales", ["customer_id"])`.
    3. NATIVE add-column on operations (CRITICAL — NEVER `op.batch_alter_table("operations")`, which rebuilds the table and DROPs operations_no_update/operations_no_delete per the 0001 warning): `op.add_column("operations", sa.Column("sale_id", sa.String(36), sa.ForeignKey("sales.id", name=op.f("fk_operations_sale_id_sales")), nullable=True))`. Default NULL permits the inline REFERENCES on SQLite (A1). Then `op.create_index(op.f("ix_operations_sale_id"), "operations", ["sale_id"])`.
    Provide `downgrade()` in reverse: drop ix_operations_sale_id, drop_column operations.sale_id, drop sales indexes/table, drop customers index/table (native drops — SQLite >=3.35, mirror 0002 downgrade).
    FALLBACK (A1): if `alembic upgrade head` emits a table rebuild for the inline FK (diff shows `_alembic_tmp_operations`), replace step 3 with a BARE column `sa.Column("sale_id", sa.String(36), nullable=True)` (no DB-level FK) — the ORM ForeignKey in models.py still gives UoW ordering + PostgreSQL portability. Trigger preservation OUTRANKS the physical FK.
    In `tests/conftest.py` add two fixtures after `product` (mirror it): `stocked_product(session)` — build a Product (code "STK-001", name "Товар со склада", quantity 0), session.add + commit, then `record_operation(session, type_="receipt", product_id=..., qty_delta=8, unit_cost_cents=1000, unit_price_cents=1500)` so it has real ledger-backed stock=8; return the product. `customer(session)` — build `Customer(id=new_id(), name="Анна", surname="Иванова", consultant_number="12345", search_lc="анна иванова 12345")`, session.add + commit, return it. Import `record_operation` and `Customer` at top of conftest.
  </action>
  <verify>
    <automated>uv run pytest tests/test_ledger.py -k "append_only or sale_id or conventions" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `alembic/versions/0004_sales_customers.py` contains `revision = "0004"`, `down_revision = "0003"`, a native `op.add_column("operations", ...)` and NO occurrence of `batch_alter_table("operations")`.
    - `tests/conftest.py` defines `def stocked_product(` and `def customer(` fixtures; `stocked_product` yields a product whose `.quantity == 8`.
    - The migration-chain test added in Task 3 (`test_migration_0004_preserves_append_only_triggers`) can run (fixtures + migration present).
    - `uv run ruff check alembic/versions/0004_sales_customers.py tests/conftest.py` exits 0.
  </acceptance_criteria>
  <done>Migration 0004 creates the three schema changes with triggers preserved; conftest exposes stocked_product + customer.</done>
</task>

<task type="auto">
  <name>Task 3: RED test contract — test_sales.py, test_customers.py, and extend test_ledger.py</name>
  <files>tests/test_sales.py, tests/test_customers.py, tests/test_ledger.py</files>
  <read_first>
    - tests/test_receipts.py (whole file — service + web conventions: (session, product) service tests asserting on select(Operation)/compute_stock; client web tests asserting status + RU text + "<html" not in response.text; test_web_ prefix; unexpected-error monkeypatch lines 240-253)
    - tests/test_ledger.py (append-only assertions lines 42-60; conventions lines 75-99)
    - 04-VALIDATION.md Requirements→Test Map (the -k selectors and observable signals) and Wave 0 Requirements
    - 04-RESEARCH.md § Validation Architecture "Observable signals"; 04-UI-SPEC.md Copywriting Contract (RU strings the web tests assert)
  </read_first>
  <action>
    Write the failing behavioral contract for the whole phase. Tests import `from app.services.sales import register_sale, lookup_prefill, recent_sales` and `from app.services.customers import create_customer, update_customer, get_customer, search_customers, purchase_history` — these modules DO NOT EXIST yet, so both files are RED by design (collection error / failing assertions) until later waves. Do NOT stub the services here.
    `tests/test_sales.py` — cover, keyed to the VALIDATION -k selectors:
    - `stock`: register a basket of 2 lines against `stocked_product`; assert N sale ops with qty_delta<0, `Product.quantity` and `compute_stock` both drop by summed qty.
    - `empty_basket`: register with zero lines → (None, errors) with «Добавьте хотя бы одну строку, чтобы оформить продажу.» in errors.values(); `select(Operation).where(type=="sale")` empty.
    - `one_transaction`: a basket where the 2nd line has an unknown code → (None, errors), ZERO sale ops committed (all-or-nothing).
    - `price_override`: line price differs from card sale_cents; assert the sale op `unit_price_cents` == entered price (not card price).
    - `customer_link`: register with `customer_id=<customer.id>` → Sale header customer_id set; and register with `customer_id=""`/None → Sale header customer_id IS NULL (walk-in valid).
    - `snapshot`: after a sale, mutate `Product.cost_cents`/`sale_cents`; re-read the op → `unit_cost_cents`/`unit_price_cents` UNCHANGED.
    - `null_cost`: product with `cost_cents=None` → sale succeeds, op `unit_cost_cents is None`; a line with EMPTY price → (None, errors) «Укажите цену продажи.», 0 writes.
    - `oversell`: request qty > stock with no confirm → result signals oversell (e.g. `result["oversell"]` non-empty) with 0 sale ops; then confirm="1" → writes and `Product.quantity` goes negative (D-09). Aggregate duplicate lines of the SAME product before comparing (Pitfall 6).
    - Web (`test_web_` prefix, using `client`): page renders «Продажа» + basket; POST success shows «Продажа оформлена:» + 200 partial (no "<html"); empty basket → 422 with the empty-basket RU error; per-line lookup prefills Цена продажи (`test_web_..._lookup`); oversell POST (no confirm) shows «Товара не хватает на складе» + «Продать всё равно» and writes 0 sale ops, then a `hx-vals` confirm POST writes; nav has `href="/sales/new"` (`test_web_nav_has_sales_link`); recent-sales oob (`id="recent-sales"` + `hx-swap-oob="true"`). ALSO add the customer-picker web tests (green only after 04-05): `test_web_customer_search_returns_rows` (GET /sales/customer-search?q= returns picker rows), `test_web_customer_quick_create_returns_chip` (POST /sales/customer returns a chip carrying the customer id), `test_web_sale_links_selected_customer`.
    `tests/test_customers.py` — keyed to selectors:
    - `crud`: create_customer maintains `search_lc` (lowercased "name surname consultant"); RU «Укажите имя покупателя.» when name blank; update_customer changes fields + refreshes search_lc.
    - `search`: search_customers folds Cyrillic (query "ан" matches "Анна"), capped at 20.
    - `history`: purchase_history(customer_id) returns rows with product, date, qty (=-op.qty_delta), unit price for THAT customer only.
    - `history_frozen`: after a linked sale, mutate the product `sale_cents`; purchase_history still returns the FROZEN `unit_price_cents`.
    - Web: `test_web_customers_list` («Покупатели»), `test_web_customer_create_redirect` (POST → 303 /customers), `test_web_customer_detail_history` (detail page shows «История покупок» + the frozen price), `test_web_nav_has_customers_link` (`href="/customers"`).
    Extend `tests/test_ledger.py` with: `test_record_operation_sets_sale_id` — create a Sale header, `record_operation(session, type_="sale", product_id=..., qty_delta=-1, sale_id=header.id)`, assert `op.sale_id == header.id` AND a subsequent `UPDATE operations` still raises the append-only ABORT. And `test_migration_0004_preserves_append_only_triggers` — run the full chain against a FRESH tmp sqlite file (monkeypatch `settings.db_path` to `tmp_path/"mig.db"` and invoke Alembic upgrade to head via the Alembic command API / `uv run alembic upgrade head`), then open it with `build_engine` and assert (a) `UPDATE operations SET qty_delta=99` raises the "append-only" ABORT, (b) `sqlite_master` contains triggers `operations_no_update` and `operations_no_delete` and NO `_alembic_tmp_operations` table (A1).
  </action>
  <verify>
    <automated>uv run pytest tests/test_sales.py tests/test_customers.py --co -q; uv run pytest tests/test_ledger.py -k "sale_id or migration_0004" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" tests/test_sales.py` returns at least 14; `grep -c "def test_" tests/test_customers.py` returns at least 8.
    - `tests/test_ledger.py` contains `def test_record_operation_sets_sale_id` and `def test_migration_0004_preserves_append_only_triggers`, and both PASS now (`uv run pytest tests/test_ledger.py -k "sale_id or migration_0004" -q` exits 0).
    - `uv run pytest tests/test_sales.py tests/test_customers.py -q` is RED (non-zero) — services/routes not built yet (expected).
    - `uv run ruff check tests/` exits 0.
  </acceptance_criteria>
  <done>The phase-wide RED contract exists; ledger sale_id + migration-trigger tests are GREEN; sales/customers suites fail pending later waves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alembic migration → operations table | Schema change must not weaken the append-only guarantee (ledger integrity) |
| record_operation → operations INSERT | The only sanctioned write path; sale_id set at INSERT only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-03 | Tampering/Repudiation | migration 0004 on `operations` | mitigate | Native `op.add_column` only (NO batch); `test_migration_0004_preserves_append_only_triggers` asserts UPDATE aborts + triggers present + no `_alembic_tmp_operations` after `alembic upgrade head` (A1) |
| T-4-03b | Tampering | `record_operation(sale_id=...)` | mitigate | sale_id set at INSERT time only; `test_record_operation_sets_sale_id` also asserts a subsequent UPDATE still aborts |
| T-4-SC | Tampering | package installs | accept | Phase 4 installs NO packages (RESEARCH Package Legitimacy Audit) — not applicable |
</threat_model>

<verification>
- `uv run pytest tests/test_ledger.py -q` fully green (append-only preserved after 0004; sale_id kwarg wired).
- `uv run pytest tests/test_sales.py tests/test_customers.py -q` RED by design (turned green by waves 2-4).
- `uv run ruff check .` and `uv run ruff format --check .` exit 0.
</verification>

<success_criteria>
- customers + sales tables and operations.sale_id exist in models and migration 0004; alembic upgrade head keeps the ledger append-only.
- record_operation accepts sale_id with zero breakage to existing callers.
- conftest exposes stocked_product + customer; the phase RED contract is authored.
</success_criteria>

<output>
Create `.planning/phases/04-sales-customers/04-01-SUMMARY.md` when done.
</output>

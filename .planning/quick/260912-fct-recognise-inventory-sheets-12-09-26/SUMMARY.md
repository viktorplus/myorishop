---
id: 260912-fct
slug: recognise-inventory-sheets-12-09-26
date: 2026-09-12
status: complete
commit: bf8c084
---

# Итог

24 рукописных листа из `input/12-09-26` (`IMG_7837…IMG_7860`) распознаны
и превращены в файл прихода `reports/оприходование-2026-09-12.csv` —
286 строк, 2197 шт, 181 код, 13 мест хранения (полки 1, 8, 16, 23, 24, 58,
63, 88, 89, 90, 91, 113, 114).

Отчёт распознавания: `reports/отчет-распознавание-2026-09-12.md`.
Генератор файла, чтобы можно было перевыпустить CSV после ответов оператора:
`build_csv.py` в этой же папке.

## Что проверено

- Контрольная сумма каждого листа (итог у кода = сумма разбивки) сошлась везде.
- 177 из 181 кода есть в справочнике `data/myorishop.db` и у них есть цена
  в `catalog_prices`. Четыре кода (`48239`, `45304`, `30960`, `19140`)
  в справочнике отсутствуют и оформлены как `Не опознан (код NNNNN)` —
  заведённый порядок из `оприходование-офис-2026-08-31.csv`.
- Названия внутри каждой полки принадлежат одной товарной группе.
- Сухой прогон `scripts/import_inventory_receipt.py` на локальной базе прочитал
  все 286 строк, 0 пропущено, 267 новых партий, 16 строк с признаком
  состояния «б/у», 4 кода без цены. Ничего не записано.

## Импорт проведён

12.09.2026 партия записана на s1, склад «Офис». Бэкап до записи —
`backups/myorishop-20260912-pre-import.dump` (11,7 МБ, проверен `pg_restore --list`
внутри контейнера). Отчёт по приходу: `reports/отчет-приход-офис-2026-09-12.md`.

Дельты сошлись по всем осям: товары 791 → 934 (+143), партии 1242 → 1479 (+237),
операции 2196 → 2625 (+429 = 286 приходов + 143 создания карточек), склад «Офис»
7136 → 9333 шт (+2197). Остальные склады не тронуты. В браузере карточка `48239`
отдаёт 161 шт и 3 партии, все четыре неопознанных кода стоят на странице
«Нет в справочнике».

## Что осталось

Не приходовано 6 шт — товар, записанный на листах без кода: товар, записанный без кода. Это две
пилки и «+1 из этой же серии только синяя» с `IMG_7849`, а также колготы FS 20,
носки комфорт до бедра и гольфы сетка с `IMG_7852`. Им нужны свои коды
(следующий свободный — 0005) либо существующие из справочника. Плюс четырём
кодам — `48239`, `45304`, `30960`, `19140` — нужны названия; они лежат под
именем `Не опознан (код *)` с пустой ценой и ждут на странице
«Нет в справочнике».

Закрыто оператором 12.09.2026: склад — «Офис»; «бу» = «бывшее в употреблении»,
то есть признак состояния, а не срок; ненайденные коды оформляются как
`Не опознан (код *)`; полка на `IMG_7853` — 63, на `IMG_7848` — 58, на
`IMG_7852` верхний блок — 8, нижний — 1; затёртые месяцы срока — `36153` 04/27
и `35910` 10/24; запись «3+1» и «2+2» означает итог 4.

## Дальше

1. Назвать шесть позиций без кода и четыре неопознанных кода.
2. Довести их отдельным, вторым приходом через `build_csv.py` — в уже
   записанную партию дописывать нельзя, импорт не идемпотентен.

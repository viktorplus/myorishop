---
phase: quick-260903-31d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/static/public/artifact-143c5a2c-d93f-4361-ae44-0059c828962a.html
  - app/routes/public_pages.py
  - app/services/security.py
  - app/__init__.py
  - tests/test_public_page.py
autonomous: true
requirements: [QUICK-260903-31d]
must_haves:
  truths:
    - "Аноним без сессионной куки получает 200 и text/html на /code/artifact/143c5a2c-d93f-4361-ae44-0059c828962a"
    - "На странице виден заголовок «Приход на склад «Офис», 3 сентября 2026»"
    - "Обёртка не навязывает светлую тему: в файле нет color-scheme:light и нет background на body из обёртки, а собственные тёмные правила страницы (prefers-color-scheme: dark, [data-theme=\"dark\"]) целы"
    - "Тег <title> в файле ровно один — обёртка его не продублировала"
    - "Соседние пути (/code/artifact/, /code/artifact/other, /products) по-прежнему НЕ отдают 200 анониму"
    - "Версия приложения в шапке — 1.56"
  artifacts:
    - path: "app/static/public/artifact-143c5a2c-d93f-4361-ae44-0059c828962a.html"
      provides: "Статическая страница отчёта по приходу на склад «Офис» 03.09.2026"
      contains: "Приход на склад «Офис», 3 сентября 2026"
      min_lines: 680
    - path: "app/routes/public_pages.py"
      provides: "Четвёртая пара констант RECEIPT_PATH/RECEIPT_FILE + GET-роут"
      contains: "RECEIPT_PATH"
    - path: "app/services/security.py"
      provides: "Точное вхождение нового пути в PUBLIC_PATHS"
      contains: "/code/artifact/143c5a2c-d93f-4361-ae44-0059c828962a"
    - path: "tests/test_public_page.py"
      provides: "Регрессия: аноним получает страницу, тёмная тема не сломана"
      contains: "RECEIPT_PATH"
  key_links:
    - from: "app/routes/public_pages.py"
      to: "app/services/security.py"
      via: "один и тот же строковый литерал пути (EXACT-match в PUBLIC_PATHS)"
      pattern: "/code/artifact/143c5a2c-d93f-4361-ae44-0059c828962a"
    - from: "app/routes/public_pages.py"
      to: "app/static/public/artifact-143c5a2c-d93f-4361-ae44-0059c828962a.html"
      via: "FileResponse(RECEIPT_FILE)"
      pattern: "RECEIPT_FILE"
---

<objective>
Опубликовать отчёт по приходу на склад «Офис» от 03.09.2026 как четвёртую
публичную страницу в уже существующем механизме `app/routes/public_pages.py`,
чтобы владелец открывал его по ссылке на ori.viktorplus.com (claude.ai в России
недоступен).

Purpose: артефакт нужен по собственному домену; механизм уже есть, новый не
заводим.
Output: один статический HTML, одна пара констант + роут, одна строка в
`PUBLIC_PATHS`, бамп версии, расширенный тест.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

Образцы, по которым делается ВСЁ ниже (читать перед правкой):
@app/routes/public_pages.py
@app/services/security.py
@tests/test_public_page.py

Уже проверенные факты (перепроверять не нужно):
- `app/main.py:207` уже содержит `app.include_router(public_pages.router)` — **не трогать**.
- `app/static/public/` уже существует (там лежат три соседние страницы).
- Фикстура `anon_client` есть в `tests/conftest.py:191` — TestClient с НАСТОЯЩИМ `auth_guard`.
- `ruff` line-length = 100 (`pyproject.toml:33`); строка `RECEIPT_FILE = Path(...)` в лимит укладывается.
- Исходник: 673 строки, заканчивается на `</div>` (БЕЗ `</body></html>` — их даёт обёртка),
  начинается с `<title>` (первая строка), `<script>` и рантайма claude.ai внутри НЕТ.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Собрать standalone-HTML из исходника без единой правки тела</name>
  <files>app/static/public/artifact-143c5a2c-d93f-4361-ae44-0059c828962a.html</files>
  <action>
Собрать целевой файл КОНКАТЕНАЦИЕЙ, а не перепечаткой — тело исходника обязано
попасть в файл побайтово. Не открывать исходник в редакторе, не пересобирать
вёрстку, не «причёсывать» стили.

Одной командой Bash (Git Bash, прямые слэши в путях):

SRC="C:/Users/Admin/AppData/Local/Temp/claude/E--dev-myorishop/0809a767-5a14-4b03-bec1-ad1cefb91478/scratchpad/prihod-ofis.html"
DST="E:/dev/myorishop/app/static/public/artifact-143c5a2c-d93f-4361-ae44-0059c828962a.html"
{ printf '&lt;!doctype html&gt;\n&lt;html lang="ru"&gt;\n&lt;head&gt;\n&lt;meta charset="utf-8"&gt;\n&lt;meta name="viewport" content="width=device-width,initial-scale=1"&gt;\n&lt;/head&gt;\n&lt;body&gt;\n'; cat "$SRC"; printf '\n&lt;/body&gt;\n&lt;/html&gt;\n'; } &gt; "$DST"

(в команде — обычные угловые скобки, экранирование здесь только для XML плана).

КРИТИЧНО, чем эта обёртка отличается от трёх соседних страниц (не копировать их
шапку!): NO `color-scheme:light`, NO `background`, NO `font`, NO `margin` на
`body`. Соседям это подходит, а эта страница сама объявляет и светлую, и тёмную
палитру через `:root`, `@media (prefers-color-scheme: dark)` и
`:root[data-theme="dark"]` и сама красит `body` — жёсткий светлый фон в обёртке
сломает её в тёмной теме.

Тег `&lt;title&gt;` уже есть первой строкой исходника и остаётся в `&lt;body&gt;` —
в `&lt;head&gt;` его НЕ дублировать (парсер HTML всё равно отдаёт его как
document.title; так же, как у соседей, только там его перенесли — здесь нет).

Ничего больше в этой задаче не менять: роут и `PUBLIC_PATHS` — следующая задача.
  </action>
  <verify>
    <automated>DST="E:/dev/myorishop/app/static/public/artifact-143c5a2c-d93f-4361-ae44-0059c828962a.html"; SRC="C:/Users/Admin/AppData/Local/Temp/claude/E--dev-myorishop/0809a767-5a14-4b03-bec1-ad1cefb91478/scratchpad/prihod-ofis.html"; head -c 15 "$DST" | grep -q '<!doctype html>' && tail -c 9 "$DST" | grep -q '</html>' && [ "$(grep -c '<title>' "$DST")" -eq 1 ] && grep -q 'Приход на склад «Офис», 3 сентября 2026' "$DST" && grep -q 'prefers-color-scheme: dark' "$DST" && grep -q 'data-theme="dark"' "$DST" && ! grep -q 'color-scheme:light' "$DST" && ! grep -q 'background:#faf9f5' "$DST" && D=$(( $(stat -c %s "$DST") - $(stat -c %s "$SRC") )) && [ "$D" -gt 100 ] && [ "$D" -lt 200 ] && echo OK</automated>
  </verify>
  <done>
Файл существует, начинается с `<!doctype html>`, заканчивается `</html>`,
содержит h1 отчёта, ровно один `<title>`, оба тёмных блока правил на месте,
светлых маркеров соседской обёртки нет, а прирост размера относительно
исходника лежит в 100–200 байт (то есть добавилась ровно обёртка — тело не
обрезано и не продублировано).
  </done>
</task>

<task type="auto">
  <name>Task 2: Зарегистрировать четвёртый публичный путь (роут + PUBLIC_PATHS)</name>
  <files>app/routes/public_pages.py, app/services/security.py</files>
  <action>
**app/routes/public_pages.py** — дописать В КОНЕЦ файла четвёртый блок строго по
образцу блока `UNKNOWN_*` (строки 46-55): русский комментарий, пара констант,
`@router.get(..., include_in_schema=False)`, проверка `is_file()` с
`HTTPException(404, detail="page not found")`, `FileResponse(...,
media_type="text/html; charset=utf-8")`. Ни строчки в существующих трёх блоках и
в докстринге модуля не менять.

Имена: `RECEIPT_PATH` / `RECEIPT_FILE`, функция `public_office_receipt_report`.
Значения:
- `RECEIPT_PATH = "/code/artifact/143c5a2c-d93f-4361-ae44-0059c828962a"` —
  совпадает с адресом артефакта на claude.ai, как у первой страницы;
- `RECEIPT_FILE = Path("app/static/public/artifact-143c5a2c-d93f-4361-ae44-0059c828962a.html")`.

Комментарий над константами — по-русски, в тоне соседей: это отчёт по приходу
1355 шт на склад «Офис» 03.09.2026; режим тот же — статический файл, без
запросов к базе и без параметров запроса.

**app/services/security.py** — добавить `"/code/artifact/143c5a2c-d93f-4361-ae44-0059c828962a",`
последним элементом множества `PUBLIC_PATHS` (строки 41-49). Это EXACT-match
множество, не префиксное — путь пишется целиком, никаких `/code/` и никаких
`startswith`. Комментарий над `PUBLIC_PATHS` уже описывает эту ситуацию —
менять его не нужно.

`app/main.py` НЕ трогать: роутер подключён на строке 207.

Коммит (атомарный, вместе с файлом из Task 1; docs-артефакты НЕ добавлять):
`git add app/static/public/artifact-143c5a2c-d93f-4361-ae44-0059c828962a.html app/routes/public_pages.py app/services/security.py`
затем `git commit` с сообщением
`feat(public): publish office receipt report 2026-09-03 at its own domain`
  </action>
  <verify>
    <automated>cd /e/dev/myorishop && grep -q 'RECEIPT_PATH = "/code/artifact/143c5a2c-d93f-4361-ae44-0059c828962a"' app/routes/public_pages.py && grep -q 'RECEIPT_FILE' app/routes/public_pages.py && grep -v '^#' app/services/security.py | grep -c '"/code/artifact/143c5a2c-d93f-4361-ae44-0059c828962a"' | grep -q '^1$' && uv run ruff check app/ && uv run python -c "from app.main import app; print([r.path for r in app.routes if '143c5a2c' in getattr(r,'path','')])" | grep -q 143c5a2c</automated>
  </verify>
  <done>
`ruff check app/` чист; роут `/code/artifact/143c5a2c-d93f-4361-ae44-0059c828962a`
присутствует в `app.routes`; тот же литерал ровно один раз лежит в
`PUBLIC_PATHS`; соседние три блока не изменены (`git diff` по ним пуст).
  </done>
</task>

<task type="auto">
  <name>Task 3: Тест на новый публичный путь + бамп версии + прогон гейтов</name>
  <files>tests/test_public_page.py, app/__init__.py</files>
  <action>
**tests/test_public_page.py** — расширить существующий файл (он уже покрывает три
страницы тем же способом): добавить `RECEIPT_FILE, RECEIPT_PATH` в импорт из
`app.routes.public_pages` (порядок имён — алфавитный, как сейчас, иначе ruff `I`
ругнётся) и дописать в конец два теста по образцу соседних:

1. `test_office_receipt_report_is_served_to_anonymous_visitor(anon_client)` —
   `GET RECEIPT_PATH` → 200, `content-type` начинается с `text/html`, в теле есть
   `Приход на склад «Офис», 3 сентября 2026`.
2. `test_office_receipt_report_keeps_its_own_dark_theme(anon_client)` — читает
   `RECEIPT_FILE.read_text(encoding="utf-8")` и пинит ровно то, ради чего обёртка
   сделана иначе, чем у соседей:
   - начинается с `<!doctype html>`, `rstrip()` заканчивается на `</html>`;
   - `html.count("<title>") == 1`;
   - есть `@media (prefers-color-scheme: dark)` и `:root[data-theme="dark"]`;
   - НЕТ `color-scheme:light` и НЕТ `background:#faf9f5` (маркеры соседской
     светлой обёртки — если кто-то скопирует её сюда, тест покраснеет).

Существующий `test_neighbouring_paths_stay_behind_the_login` НЕ менять: он и так
доказывает, что дырка точечная (`/code/artifact/other` != 200).

**app/__init__.py** — `__version__` `"1.55"` → `"1.56"` (схема из
CLAUDE.md/памяти: бамп на каждом коммите, завершающем задачу).

Гейты, оба обязательны:
- `uv run pytest tests/ -q`
- `uv run ruff check app/`
Известны 4 предсуществующих падения в `tests/test_sync_ui.py` (lock от
lifespan-потока автосинка) — это НЕ регрессия этой задачи, чинить их нельзя, но
назвать их в SUMMARY с точными именами тестов. Любое ДРУГОЕ падение — блокер.

Коммит (docs-артефакты НЕ добавлять):
`git add tests/test_public_page.py app/__init__.py` →
`test(public): cover office receipt page, bump version to 1.56`

Деплой на s1 и проверку живьём НЕ делать — это на оркестраторе после мержа.
  </action>
  <verify>
    <automated>cd /e/dev/myorishop && uv run pytest tests/test_public_page.py -q && uv run ruff check app/ && grep -q '__version__ = "1.56"' app/__init__.py && uv run pytest tests/ -q 2>&1 | tail -n 25</automated>
  </verify>
  <done>
`tests/test_public_page.py` — все тесты зелёные (7 старых + 2 новых);
`ruff check app/` чист; `__version__ == "1.56"`; полный прогон `pytest tests/ -q`
не даёт новых падений сверх 4 известных в `tests/test_sync_ui.py`; сделаны два
атомарных коммита без docs-артефактов.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| аноним из интернета → `auth_guard` | добавляется четвёртый путь, проходящий мимо сессионной проверки |
| файловая система → `FileResponse` | путь к файлу — константа модуля, ввод пользователя в него не попадает |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-Q31d-01 | Elevation of Privilege | `security.PUBLIC_PATHS` | mitigate | путь добавляется ТОЧНЫМ совпадением в EXACT-match множество; никакого `startswith` и никакого `/code/` префикса. Регрессия: существующий `test_neighbouring_paths_stay_behind_the_login` (`/code/artifact/`, `/code/artifact/other` != 200) |
| T-Q31d-02 | Information Disclosure | содержимое страницы | accept | владелец сознательно публикует отчёт по своему складу; персональных данных, цен закупки клиентов и секретов в файле нет — только коды, названия, количества и сроки. Тот же режим, что у трёх соседних страниц |
| T-Q31d-03 | Tampering | `public_office_receipt_report` | mitigate | роут не принимает ни path-, ни query-параметров; `RECEIPT_FILE` — константа, `is_file()` → 404. Path traversal структурно невозможен |
| T-Q31d-04 | Information Disclosure | mount `/static` | accept | файл лежит под `app/static/public/`, значит доступен и как `/static/public/...` — ровно как три соседние страницы; страница и так публичная, второй адрес ничего не раскрывает дополнительно |
| T-Q31d-05 | Spoofing | внешние шрифты Google Fonts в теле страницы | accept | исходник копируется дословно по требованию задачи; страница публичная и read-only, без форм и токенов — сторонний CSS не может ничего украсть. Локальное приложение при этом не ломается: шрифты деградируют до системных без сети |
| T-Q31d-SC | Tampering | package installs | mitigate | новых пакетов не ставится вообще (`uv add` не вызывается) — цепочка поставок не трогается |
</threat_model>

<verification>
1. `uv run pytest tests/test_public_page.py -q` — 9 тестов зелёные.
2. `uv run pytest tests/ -q` — новых падений нет (4 известных в `tests/test_sync_ui.py` остаются).
3. `uv run ruff check app/` — чисто.
4. `git diff --stat` последних двух коммитов затрагивает ровно 5 файлов из `files_modified` и ни одного docs-артефакта.
</verification>

<success_criteria>
- `GET /code/artifact/143c5a2c-d93f-4361-ae44-0059c828962a` без куки → 200 + `text/html`.
- Тело страницы совпадает с исходником побайтово (прирост размера = только обёртка, 100–200 байт).
- Тёмная тема страницы жива: обёртка не содержит `color-scheme:light` и не красит `body`.
- Соседние пути под `/code/artifact/` анониму по-прежнему не отдаются.
- Три существующие публичные страницы не изменены.
- `__version__ == "1.56"`.
</success_criteria>

<output>
Create `.planning/quick/260903-31d-publish-office-receipt-report-on-own-dom/260903-31d-SUMMARY.md` when done.
В SUMMARY обязательно назвать поимённо 4 предсуществующих падения `tests/test_sync_ui.py`
и явно пометить их как НЕ регрессию этой задачи.
</output>

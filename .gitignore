# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# Local database (SQLite + WAL sidecar files) and its backups
data/
backups/
*.db
*.db-wal
*.db-shm

# Local environment config (never commit real values)
.env

# Git Bash crash artifacts (Windows)
*.stackdump

# GSD research cache (ephemeral, regenerable)
.planning/research/.cache/
